# Decisions Log

This log records architectural and implementation decisions made while building ReqTrackManager to the **Ossa (v1)** and **Pelion (v2)** milestones defined in [docs/requirements.md](requirements.md). It exists so future contributors understand *why* the system looks the way it does, not just what it does. `docs/requirements.md` is authoritative and unchanged; this log explains how ambiguous or underspecified requirements were interpreted in code.

## Scope

Ossa (v1) implements every row in `docs/requirements.md` tagged `Ossa (v1)`, at both `Requirement` and `Recommended` priority. Pelion (v2) additionally implements every row tagged `Pelion (v2)`, built directly on top of the Ossa (v1) codebase (nothing from v1 was removed or restructured). Everything tagged `Massif (v3)` or `Murchison (v4)` was **not** built, including: task assignment/voting in change-request review, the standalone provisioning API/port, SSO/OAuth wiring, and IBM DOORS import/export. The "Pelion (v2)" section below records the scoping and implementation decisions made for that milestone; everything above it predates and still applies.

## Authentication and identity

- **Native auth now, SSO seam only.** `AuthBackend` (`backend/app/auth_backends/base.py`) is a small protocol with one implementation, `NativeAuthBackend` (email + bcrypt password). C-U-06/C-U-07 ask for pluggable backends and optional OAuth/SSO; actually wiring Keycloak/Authentica is Pelion-adjacent (E-U-01) and wasn't built, but the seam exists so adding a second backend doesn't require touching the rest of the app.
- **Login IP logging without geolocation (C-A-07).** Every login attempt writes a `login_events` row with the raw IP. Resolving IP → location was deliberately left as a nullable `location` column rather than calling an external geo-IP service at login time, to avoid a runtime network dependency and an API-key requirement in a self-hostable product. Follow-up: wire an offline MaxMind-style database when this becomes a priority.
- **Server admin bootstrap (I-M-05/06/08).** `backend/app/services/bootstrap.py` runs on every app startup (idempotent): creates the configured server-admin user if absent, and — if `SERVER_ADMIN_CREATE_ORG` is enabled — a default organisation with that user as org admin, matching the "on first install" wording without needing a separate CLI step.

## Roles and permissions

- **Fixed role vocabulary, not customisable permissions.** Ossa (v1) uses exactly the org roles (`org_admin`, `project_creator`, `member`) and project roles (`project_manager`, `project_administrator`, `stakeholder`, `member`) from C-U-01/C-U-03. Customisable permission schemes are a Pelion/Massif-era concern; project groups (C-U-11) are therefore just a way to bulk-assign one of these four fixed roles, not a general permission-authoring tool.
- **Project manager implies administrator + stakeholder.** Per the C-U-03 clarification, `get_effective_project_roles` (`backend/app/services/rbac.py`) folds `PROJECT_MANAGER` into also granting `PROJECT_ADMINISTRATOR` and `STAKEHOLDER` capabilities, and any assigned role implies baseline `MEMBER` (view) access.
- **Org admin ≠ automatic project content access.** The C-U-01 clarification ("no project access is guaranteed... apart from org admin being able to manage project settings") is implemented as two separate dependencies: `require_project_manage` (org admins pass) for settings/stage/component/group endpoints, and `require_project_view` (org admins do **not** automatically pass) for requirement/change-request content. An org admin who also wants to read requirements needs an explicit project role, same as anyone else.
- **Org-group nesting resolves one level deep.** A `ProjectGroupMember` row may point at a user or an org group (C-U-12); permission resolution flattens org-group membership into the containing project group's role. Cycle prevention is limited to not letting a group contain itself — deeper nested-group graphs aren't a requirement here and would add complexity with no described use case.
- **C-U-08/C-U-09 (must always have a manager).** Enforced two ways: `revoke_project_role` blocks removing the last direct project-manager assignment, and `deactivate_org_user` (in `backend/app/routers/orgs.py`) checks every project in the org after deactivating a user — if that removal leaves a project with zero managers, the *acting admin* is assigned as manager, matching the requirement's specific wording.

## Requirement lifecycle and change management

- **Identity vs. version split, not `valid_from`/`valid_to` on one row.** `solution-architecture.md` suggests temporal columns directly on core entities; in practice, `Requirement` holds only stable identity (project/component/category, generated code, creator, archival state) while `RequirementVersion` holds every mutable field plus `valid_from`/`valid_to`/`version_number`. The "current" version is the row with `valid_to IS NULL`. This gives the same point-in-time query capability with a simpler mental model: identity never changes, content always changes through a new version row. Change requests mirror the same pattern (`ChangeRequestVersion`).
- **Locking is per-requirement, not per-project-stage.** C-G-12 says changes are change-request-only "once a requirement is approved for a project stage." This is implemented as `is_locked` = the requirement's current version status is `approved` or `completed` — checked directly rather than by asking whether the project's current stage is approved. Approving a stage (`backend/app/services/baseline.py`) bulk-transitions every non-archived requirement still in `draft`/`reviewed` to `approved` and snapshots a `Baseline`/`BaselineItem` row per requirement (C-G-10) in the same transaction, so in practice stage approval and the lock happen together — but the lock itself is a property of the requirement, which is what C-G-12 actually says.
- **Unique requirement IDs (C-G-06/C-G-07).** `{component.prefix}-{category.prefix}-{project.next_requirement_seq:03d}`, e.g. `SW-PERF-014`. The sequence is a per-project counter that only increments, so archived requirements' numbers are never reused, satisfying the C-G-06 clarification.
- **Ordering is move-up/move-down, not drag-and-drop (C-E-01/02/03).** Each orderable entity (`ProjectComponent`, `ProjectCategory`, and a requirement's `sort_order` within its component/category) has a persisted integer position and a `/move` endpoint that swaps it with its neighbour. This satisfies the requirement without a drag-and-drop dependency in the frontend; requirement reordering is additionally blocked outside the scoping stage, per the C-E-03 clarification.
- **Discussion threads are not the change log (C-A-09 clarification).** `ReviewComment` (discussion, C-R-01) and `RequirementVersion`/`ChangeRequestVersion` (change log) are separate tables from the start, so the UI's change-log view never needs to filter comments out after the fact.

## Documentation contradiction: C-A-08 vs. notifications

C-A-08 ("date/time a notification was read must be recorded") is tagged `Ossa (v1)`, but the entire notification feature (C-N-01 through C-N-05) is tagged `Pelion (v2)`. There is nothing for a "notification read" event to attach to in v1. Rather than build a speculative read-receipt schema ahead of the feature it depends on, this was treated as a documentation dependency and deferred alongside notifications themselves — flagged here per the project's instruction to challenge assumptions rather than implement contradictory requirements literally.

## Reporting

- **ReportLab over WeasyPrint for PDF (R-F-01).** ReportLab is pure-Python with no system-level dependencies (WeasyPrint needs Cairo/Pango), which keeps the backend Docker image simple. Markdown pre/post sections (R-G-01/R-G-02) are parsed with `markdown-it-py` and walked token-by-token into ReportLab flowables (headings, paragraphs, bullet lists) — enough fidelity for introduction/appendix-style content without a full HTML rendering stack.
- **CSV export (R-F-02)** is a straightforward tabular dump of the same requirement rows used for the PDF table.

## Realtime

- **WebSocket pub/sub is in-process (I-A-04).** `backend/app/services/pubsub.py` is a single-process broadcaster; the endpoint is explicitly optional per the requirement text ("may expose"), and a single-backend-instance deployment is exactly what Ossa (v1)'s deployment model is. Scaling to multiple backend replicas would need a shared broker (Redis pub/sub or similar) — a natural extension point, not a v1 requirement.

## Infrastructure

- **Enum columns use `sqlalchemy.Enum(..., native_enum=False)`, not plain `String`.** An early version used `Mapped[SomeEnum] = mapped_column(String(N))`, which stores correctly but returns a plain `str` (not the enum) on read — anything calling `.value` on a freshly-loaded attribute would crash. `models/base.py::str_enum()` fixes this while keeping the column a plain VARCHAR (no Postgres `CREATE TYPE`/migration friction when adding a new enum member).
- **Migrations run automatically at startup (I-M-02/I-M-03).** `backend/app/migrations.py::run_migrations()` runs `alembic upgrade head` from the FastAPI lifespan handler before the bootstrap step. A fresh or upgraded deployment self-migrates on boot instead of requiring an operator to run Alembic by hand first and failing with raw "column does not exist" errors in the meantime.
- **One hand-written initial migration.** `alembic/versions/0001_initial.py` creates the schema via `Base.metadata.create_all()` rather than 22 transcribed `op.create_table()` calls, keeping the migration in lockstep with the models (the single source of truth). Future schema changes should be added as normal incremental Alembic migrations generated with `--autogenerate`.
- **Observability: metrics/health always on, Loki/Tempo/Grafana/Alloy opt-in.** The Ossa-tagged requirements (I-M health checks, Prometheus `/metrics`) are wired into the default `docker compose up`. The broader Loki/Tempo/Grafana/Alloy stack described in `solution-architecture.md` is architecture-level, not individually Ossa-tagged, so it's an opt-in Compose profile (`docker compose --profile observability up`) — the default stack stays fast to start. The backend does not yet emit OpenTelemetry traces; the Alloy config's OTLP receiver is ready to forward them to Tempo the moment that instrumentation is added.
- **i18n (U-U-02): a single string table, not a full i18n library.** `frontend/src/i18n/strings.ts` centralises every UI string behind a `t()` lookup. No second language ships in v1, but no component hardcodes text inline, so adding a translated locale is additive rather than a rewrite.

## Security hardening pass

After the initial v1 build, a dedicated hardening pass was run: two parallel vulnerability-scan sub-agents (backend; frontend/infra) proposed candidate findings, then each candidate was independently re-verified by a fresh sub-agent against a strict false-positive rubric before anything was treated as real. Five findings were confirmed and fixed; two were deliberately excluded as false positives. Regression tests for every fix live in `backend/tests/test_security_hardening.py`.

**Fixed:**

- **IDOR: requirement links/comments not scoped to project** (`backend/app/routers/requirements.py`). `create_link`/`list_links`/`add_comment`/`list_comments` validated the caller's role against the `project_id` path param but never checked that `requirement_id` actually belonged to that project — a member of Project A could read or write comments/traceability links on any requirement in Project B just by knowing its id. Fixed via a shared `_get_requirement_in_project` helper (also now validates a link's `target_requirement_id` the same way) that every such handler calls before touching the resource.
- **IDOR: change-request comments not scoped to project** (`backend/app/routers/change_requests.py`). Same bug, same fix pattern (`_get_cr_in_project`), for `add_comment`/`list_comments` on change requests.
- **Privilege escalation: project-group membership across projects** (`backend/app/routers/projects.py`). `add_project_group_member`/`remove_project_group_member` were gated by `require_project_manage` against `project_id`, but never checked that `group_id` belonged to that project. A manager of *any* project could look up another project's default "Project Managers" group id (visible to anyone with view access to that other project) and add themselves to it via their *own* project's manage-permission check, inheriting the other project's manager role. Fixed with `_get_group_in_project`.
- **Cross-tenant escalation: org-group membership across organisations, and cross-org group nesting** (`backend/app/routers/orgs.py`, `backend/app/routers/projects.py`, `backend/app/services/rbac.py`). Same missing-ownership-check pattern for org groups (`add_org_group_member`/`remove_org_group_member` didn't verify the group belonged to the `organization_id` in the URL), plus a related and more structural gap: nesting an org group into a project group (`add_project_group_member` with `org_group_id`) never checked that the org group belonged to the *project's own organisation* — an org group from a completely different organisation could be nested in, and `get_effective_project_roles`'s nested-group resolution would grant its members the project role regardless of tenant boundary. Fixed at both the write path (reject nesting a foreign-org group) and, as defense in depth, the read path (`get_effective_project_roles` now joins through `Project`/`OrgGroup` and only honours nested-group rows where the org group's organisation matches the project's organisation).
- **ReportLab markup injection (SSRF / local file read) in PDF reports** (`backend/app/services/reports.py`). `Paragraph()` from ReportLab parses its `text` argument as a restricted markup language (`<b>`, `<font>`, `<img src="...">`, etc.), not plain text. Requirement names/reasoning, the project name, and the custom pre/post Markdown report sections are all user-supplied and were passed to `Paragraph()` unescaped — a `<img src="file:///etc/passwd">` or an internal-URL `<img>` tag would be parsed as real markup and fetched/rendered server-side by ReportLab (whose pre-5.0 default `trustedSchemes` include `http`, `https`, and `file`), any project member being able to trigger it via the report-generation endpoint. Fixed by escaping (`xml.sax.saxutils.escape`) every user-controlled string before it reaches `Paragraph()` — table cells, the title, and rendered Markdown block content — so `<`/`>`/`&` always render as literal characters rather than being parsed as markup.

**Considered and excluded as false positives** (per the review's filtering criteria):

- *Unescaped `VITE_API_BASE_URL` in the generated `env-config.js`* — the value only ever comes from an operator-set `docker-compose.yml` environment variable, which is a trusted deploy-time input, not attacker-controlled; no path exists in this codebase for an external party to influence it.
- *Postgres port (5432) published to the host in `docker-compose.yml`* — at the time, this was the documented local-development quick-start compose file (alongside hardcoded dev-only credentials already flagged as "override in production"), so publishing the port was judged a defense-in-depth/hardening consideration rather than a concrete flaw. **Update (Pelion v2 infrastructure split):** the root `docker-compose.yml` is now the production-oriented stack, and no longer publishes the Postgres port at all — the dev/test stack that still does so (`tests/container/docker-compose.yml`) is unambiguously not production-facing, so this is no longer even a judgment call.

## Testing

- **Backend tests run against real PostgreSQL, not SQLite.** The schema uses Postgres-specific types (`UUID`, `JSONB`), so `backend/tests/conftest.py` creates a dedicated `reqtrack_test` database on the same Postgres server used by docker-compose, with tables truncated between tests rather than mocking the database.
- **A Playwright E2E suite (`tests/playwright/`) runs against the actual docker-compose stack** (not a dev server), exercising the full golden path in a real browser: login → create project → components/categories → requirement → stage approval (lock) → change request → PDF report → theme toggle. This is the only reliable way to confirm the shipped Docker images actually work together, not just that each service passes its own tests in isolation.

## Pelion (v2)

Pelion (v2) implements every row in `docs/requirements.md` tagged `Pelion (v2)`, on top of the Ossa (v1) codebase above. It reuses existing v1 architecture wherever possible (the identity/version split, the RBAC dependency style, the audit-event helper, the router/schema/service layering, and the frontend's `t()` string table) rather than introducing parallel mechanisms.

### Schema migration strategy

- **Squashed into the same `0001_initial.py` baseline, not a second migration.** Every Pelion v2 model change (new columns on `User`/`Organization`/`Project`/`RequirementVersion`/`ChangeRequestVersion`, and new tables for files/notifications/custom fields/favourites) was added directly to the existing models and folded into `alembic/versions/0001_initial.py`'s `Base.metadata.create_all()`, rather than written as an incremental `op.add_column`/`op.create_table` migration. This only works because `create_all()` reflects the *current* model state at the time it runs — attempting to layer a second migration on top produced "column already exists" errors, since 0001 would create the new columns too. This is a deliberate pre-release-only shortcut: there is no real production deployment yet to preserve data for, so squashing is safe. **The moment this system has a real deployed database that must be upgraded in place, all future schema changes must go back to normal incremental Alembic migrations** (`alembic revision --autogenerate`) rather than editing 0001 again — the docstring at the top of that file repeats this warning.
- **`tests/conftest.py`'s schema fixture now runs real Alembic (`run_migrations()`) against a `DROP SCHEMA public CASCADE`/`CREATE SCHEMA public` reset**, instead of calling `Base.metadata.create_all()`/`drop_all()` directly. The app's own startup path runs `alembic upgrade head` every boot (Ossa v1's I-M-02/03 decision); tests need to exercise that same path rather than a second, divergent schema-creation mechanism. Per-test cleanup uses a single `TRUNCATE ... CASCADE` statement rather than per-table `DELETE` in dependency order, because `use_alter`-based FK cycles (e.g. `users.avatar_file_id` → `file_assets` → `file_assets.uploaded_by` → `users`) make a strict deletion order impossible to compute generically.

### Files and storage

- **Pluggable storage backend, exercised for real via MinIO, not just implemented in the abstract.** `backend/app/storage_backends/{base,local,s3}.py` defines a `FileStorageBackend` protocol with `LocalFileStorageBackend` (filesystem) and `S3CompatibleFileStorageBackend` (boto3; works against MinIO or real S3), selected via `STORAGE_BACKEND=local|s3`. MinIO is added to the default `docker-compose.yml` stack and is the default backend, so I-M-10 ("must support different storage backends") is verified against an actual running S3-compatible service during manual and automated verification, not only against local disk.
- **A single `file_assets` table backs every upload use case** (requirement attachments, org shared resources, avatars, org logos) rather than one table per use case, since they all need the same metadata (storage backend, key, filename, content type, size, uploader). Requirement attachments additionally go through a `requirement_files` link table (many-to-many, so the same shared org resource can be linked to multiple requirements per C-M-04 without duplicating the file).
- **Local backend path-traversal defense in depth.** Storage keys are always generated by `app/services/files.py` (UUID-based), never taken from user input directly, so path traversal via a key isn't attacker-reachable in practice — but `LocalFileStorageBackend._path_for()` still resolves the path and checks `is_relative_to()` the base directory before any read/write, as defense in depth rather than relying solely on that invariant holding forever.
- **Disk-usage monitor (I-M-11) is a single testable function plus a thin timer loop.** `check_disk_usage_once()` in `backend/app/services/disk_monitor.py` contains all the actual threshold/dedup logic and takes the storage backend and prior warned-state as plain arguments; `run_disk_monitor_loop()` is just a 15-minute `asyncio.sleep` wrapper around it. This split exists specifically so the branching logic (warn once, don't repeat while still over threshold, re-warn after recovery) can be unit-tested (`backend/tests/test_disk_monitor.py`) without waiting on the real timer or touching a real filesystem's `shutil.disk_usage`.

### Notifications

- **In-process asyncio background tasks, not a separate worker service.** Both the instant-notification email path and the daily-digest batch job run as `asyncio` tasks started from the FastAPI lifespan handler, consistent with the existing single-instance WebSocket pub/sub pattern (`services/pubsub.py`). A dedicated worker service is a real future step (already called out in `solution-architecture.md`'s "Production deployment path") once a single backend replica stops being sufficient — not required for this deployment model.
- **MailHog as the default SMTP endpoint.** Rather than requiring a real mail provider to exercise C-N-01–05, the default Compose stack runs MailHog and points `SMTP_HOST`/`SMTP_PORT` at it, so sent notification emails are genuinely inspectable (http://localhost:8025) during development, testing, and manual verification — not just logged or mocked.
- **Notification triggers were added at existing call sites**, not as a generic event-bus: project-group join, stage transitions, change-request submit/decide, password change, and org-role permission grant. Permission *revocation* does not currently fire a notification — only grant does; this is a narrower interpretation of C-N-04 than might be expected and is a candidate follow-up if revocation notifications turn out to matter in practice.

### Customisation

- **Custom field values reuse the existing version-history mechanism instead of a second, parallel history system.** `CustomFieldDefinition` (project-scoped, `entity_kind` requirement|change_request) defines the schema; the actual *values* live in a plain `custom_fields JSONB` column added directly to `RequirementVersion`/`ChangeRequestVersion`. Every requirement/CR edit already creates a new version row (C-A-02/C-A-09), so custom field values automatically get the same point-in-time history for free.
- **A change request's `custom_fields` payload is validated against `CustomFieldEntityKind.REQUIREMENT` definitions, not `CHANGE_REQUEST` definitions.** These values represent proposed attribute values for the requirement being created/modified (mirroring `proposed_name`/`proposed_reasoning`) and are copied onto the requirement's version on approval — so they must be validated against the schema that governs requirements, not change requests. A separate `CustomFieldEntityKind.CHANGE_REQUEST` kind still exists in the data model for attributes describing the change request itself (e.g. an "urgency" field), but no workflow currently collects or consumes those values; this is a deliberate, documented scope gap rather than an oversight.
- **Terminology overrides are a fixed, closed set of keys, not a freeform key-value store.** `TERMINOLOGY_KEYS` in `backend/app/schemas/project.py` enumerates exactly the overridable nouns (project, stage, component, category, requirement, change_request); the update endpoint rejects unknown keys. An open-ended per-project vocabulary would require every frontend string to become dynamically templated, which is a much larger change than C-C-03 actually asks for.

### Templates

- **`clone_project()` deep-copies configuration and requirements, resetting workflow state.** Cloning a template project (C-E-05) copies components, categories, custom field definitions, groups and group memberships, and requirements (reset to `draft` status, with any project-specific custom field ids remapped to the new project's definitions) — but never copies change requests, comments, or audit history, since those describe the *history* of the template project, not a starting configuration for a new one.
- **PM auto-assignment has an explicit fallback when cloning.** C-U-10 says the creator is normally added to the Project Managers group automatically; C-E-05 says this doesn't apply when creating from a template (groups/members are copied from the template instead). If that copy leaves the new project with zero managers (e.g. the template's manager group had no members), the creator is still added as a fallback so C-U-08 ("every project must have a manager") can never be violated by a template that happens to be manager-less.

### Auditing and attribution

- **Creator reassignment (C-A-11/C-A-12) is restricted to project managers, checked server-side at creation time.** `POST .../requirements` and `POST .../change-requests` accept an optional `creator_id`; if present, the caller must hold the `project_manager` role (or be a server admin) or the request is rejected with 403 — anyone who can create a requirement/CR at all can still create one attributed to themselves, but only a manager can attribute it to someone else.
- **The project changes-over-time view (C-A-10) excludes discussion comments by default.** `get_project_changes()` merges audit events, requirement version history, and change-request version history into one timeline; `ReviewComment` rows are only included when the caller explicitly passes `include_comments=true`, consistent with the Ossa v1 decision that discussion threads are not the change log.

### UI discovery

- **Favourite projects sort ahead of everything else, then alphabetically.** `GET /api/v1/projects` computes the caller's favourited project ids and sorts the response with `(not is_favorite, name.lower())` as the key, so favourites always appear as a contiguous block at the top regardless of the `role`/`stage_status`/`search` filters also applied. Favouriting is a simple per-user join table (`FavoriteProject`) with `PUT`/`DELETE .../favorite` endpoints, not a field on `Project` itself, since it's user-specific rather than project-wide state.

### Logging

- **`alembic/env.py`'s `fileConfig()` call now passes `disable_existing_loggers=False`.** Migrations run automatically on every app startup (see Infrastructure, above), which means `logging.config.fileConfig(alembic.ini)` also runs on every startup, *after* uvicorn has already configured its own `uvicorn`/`uvicorn.error`/`uvicorn.access` loggers. `fileConfig()` defaults to `disable_existing_loggers=True`, which sets `.disabled = True` on every logger not explicitly listed in `alembic.ini`'s `[loggers]` section — silently killing uvicorn's access/error logging (and every `logging.getLogger(__name__)` logger elsewhere in the app) for the entire life of the process. This was a real, confirmed bug, verified via the raw container JSON log file having zero lines beyond the startup banner despite heavy real traffic. It also happened to be why the database bug below went undiagnosed for a while — there was no traceback to read. After the fix, `docker compose logs backend` shows a normal per-request access log and any real exception traceback.

### Database: the test suite was wiping the live database

- **Root cause: `tests/conftest.py` used `os.environ.setdefault("DATABASE_URL", ".../reqtrack_test")`, which is a no-op whenever `DATABASE_URL` is already set — and it always is inside the `backend` service container, to the real application database (`postgresql://reqtrack:reqtrack@db:5432/reqtrack`).** The test suite's session-scoped `_schema` fixture runs `DROP SCHEMA public CASCADE` / `CREATE SCHEMA public` at both session start *and* session teardown. Running the suite via `docker compose exec backend pytest` — a completely reasonable-looking command — was therefore dropping the real dev/demo database's entire schema twice per run, leaving it with **no tables at all** after the run finished (the teardown drop doesn't re-run migrations). The running app process kept its connection pool open, so it kept accepting requests, but every query then failed with `psycopg2.errors.UndefinedTable: relation "users" does not exist` — a hard 500 on every authenticated endpoint, while `/health`'s own lightweight check happened to still report OK. This was misdiagnosed at the time as a vague "connection pool wedge," fixed only by restarting the backend (which re-runs migrations and re-bootstraps the admin user, masking the real cause and quietly losing whatever demo data existed).
- **Fix: a hard startup guard in `tests/conftest.py`.** After resolving `DATABASE_URL`, the conftest module now asserts the resolved database name ends in `_test` and raises `RuntimeError` immediately if not, before the destructive session fixture can run. This makes the failure mode "the test suite refuses to start, with a clear error" instead of "the test suite silently destroys whatever database it happens to be pointed at." `setdefault` is still used first, purely as a convenience default for runners that haven't set `DATABASE_URL` at all.
- **Structural fix: tests run against their own isolated stack, not by `exec`-ing into the application container.** The root `docker-compose.yml` is now the production-oriented stack (no test database anywhere near it); `tests/container/docker-compose.yml` is a separate, fully isolated stack whose `backend` service's `DATABASE_URL` already points at `reqtrack_test` — so `docker compose exec backend pytest` there just works, with the safety guard confirming it rather than needing to catch a mistake.
- **The `_schema` fixture no longer drops the schema at session teardown, only at session start.** The teardown drop was redundant (the *next* session's own setup already guarantees a clean slate) and, once tests could share a database with a live app in `tests/container/`, actively harmful: it left that app's own backend with zero tables until its next restart. Removing the teardown drop means a live app sharing the test database instead finds its tables intact but empty after a test run (a clean `401` on login, since the bootstrap admin was truncated along with everything else) rather than a `relation "..." does not exist` `500`. `docker compose restart backend` (which re-runs migrations and re-bootstraps the admin) recovers it either way — this is a documented, expected part of using the shared dev/test stack for both manual testing and pytest, not a bug.

### Frontend component testing (Storybook)

- **Storybook (`frontend/.storybook/`) plus `@storybook/addon-vitest` for automated story tests**, not just a manual component browser. `npm run storybook` gives a visual explorer; `npm run test-storybook` runs every story as a real headless-Chromium test via Vitest's Playwright browser provider, so a story that fails to render or fails an assertion in its `play` function fails CI the same as any other test — a component explorer nobody automates checking against tends to silently rot.
- **The preview's `withTheme` decorator applies the exact same mechanism the real app uses** (a `data-theme` attribute on `<html>`, read by `src/styles/theme.css`'s CSS variables) rather than a Storybook-specific theming shim, so stories render with the actual product palette. A toolbar toggle switches themes while browsing; `LightTheme`/`DarkTheme` story exports pin `globals: { theme: ... }` explicitly so both are exercised by `test-storybook` on every run regardless of whatever theme was last selected in the UI (U-U-01: light and dark are both first-class, not just supported).
- **Only two component story files exist so far** (`Spinner`, `CustomFieldsForm`) — components chosen because they're pure/presentational with no network calls; most of the app's page-level components fetch data on mount (`api.get(...)`) and would need request mocking (e.g. MSW) to story meaningfully, which wasn't set up in this pass. Adding more stories as new presentational components are built, and adding MSW if page-level components need stories later, are natural follow-ups rather than a gap in what's here.
- **Chromatic and MCP addons were removed from the `storybook init` scaffold.** The init wizard defaults to including `@chromatic-com/storybook` (a paid visual-regression SaaS this project has no account for) and `@storybook/addon-mcp` (wires Storybook into AI coding assistants, unrelated to testing); both were stripped to keep the addon set focused on what's actually used (`addon-vitest`, `addon-a11y`, `addon-docs`).
- **Known, pre-existing dependency vulnerability not fixed in this pass: `react-router-dom`.** `npm audit` flags the installed 6.x range (open redirect / SSR hydration constructor-injection advisories); the only fix is a major-version upgrade to react-router v7, which changes core routing APIs and would need its own dedicated migration and testing effort, not a drive-by bump alongside unrelated work. Flagged here rather than silently left for someone to rediscover via `npm audit`.

### Test coverage

- **`pytest-cov` added, and used to find real gaps rather than pad numbers.** `pytest --cov=app --cov-report=term-missing` (from `backend/`, or `docker compose exec backend pytest --cov=app --cov-report=term-missing` in `tests/container/`) reports per-file coverage. It went from 90% to 92% during this pass; every added test corresponds to a specific, real gap the report identified — a fully untested `GET /projects/{id}/metrics` dashboard endpoint, `LocalFileStorageBackend` (never exercised since the dev/test stack defaults to the `s3` backend), requirement reordering, traceability links, an organisation-level role grant, and the C-U-09 manager-fallback-on-deactivation safeguard — rather than writing tests to move the percentage without a specific defect or behavior in mind.
- **`app/routers/ws.py` (39%) and the `run_disk_monitor_loop`/`run_digest_loop` wrapper functions (the `while True: sleep(...)` shells, not the logic they call) are left under-covered on purpose.** The WebSocket endpoint needs a websocket test client rather than the plain HTTP `TestClient` used everywhere else — a reasonable future addition, not attempted here since the endpoint is explicitly optional per its own requirement text. The two background-loop wrappers just call `asyncio.sleep` then a function that already has full unit coverage on its own (`check_disk_usage_once`, `send_daily_digests`); testing the sleep-loop shell itself would mean asserting on `asyncio.sleep` being awaited, which verifies nothing about actual behavior.
- **API resource lifecycle tests (`backend/tests/test_api_lifecycles.py`) exercise create → read → remove for every resource that has one.** "Remove" means different things by design (see the requirement-lifecycle and security-hardening sections above): most domain entities soft-delete/archive rather than hard-delete, to preserve audit history; a few resources (organisations, project components/categories/groups) have no removal endpoint at all, which each test notes explicitly. Writing this file surfaced one real bug: `list_org_users` never filtered out archived users, contradicting C-U-05's explicit requirement that an archived user "no longer show[s] as users" — fixed by adding the missing filter.
- **A systematic permission-level test matrix (`backend/tests/test_permission_matrix.py`) checks both directions of each authorization boundary** — that a role below the required level is rejected (403) *and* that the role that should be allowed actually succeeds — rather than only ever testing the rejection side, which is what most of the pre-existing RBAC tests did.

### Testing and verification process notes

- **The Docker Compose `backend` and `frontend` services do not bind-mount source** — they `COPY` the source at build time. Editing backend or frontend code and then testing against the *already-running* containers (`docker compose exec backend pytest`, or exercising the UI at localhost:3000) silently tests stale code until `docker compose build <service> && docker compose up -d <service>` is run. This tripped up Pelion v2 verification more than once (a backend test file and a frontend UI change were both validated against stale containers before the rebuild step was remembered) — worth calling out explicitly since the mistake produces no error, just misleading "it works" or "it's broken" signals from the wrong code.
- **Password-changing Playwright steps must revert via a direct API call in a `finally` block, not a second UI round-trip.** The new Pelion v2 E2E spec (`tests/playwright/tests/pelion-v2.spec.ts`) changes the shared bootstrap admin's password to exercise the password-changed notification, then must restore it, since `golden-path.spec.ts` and every future run depend on the fixed bootstrap password staying valid. Reverting through a second UI form submission proved flaky (a `page.reload()` issued immediately after clicking "Change password" can abort the still in-flight request before the browser sends it); reverting via `page.request.post(...)` with the token read out of `localStorage` is synchronous from the test's perspective and doesn't depend on any UI timing. The forward change also now explicitly awaits the `change-password` network response (`page.waitForResponse`) before doing anything else, for the same reason.
- **The favourites E2E assertion clears any pre-existing favourites via the API before asserting sort order.** Because favourite state persists in the shared dev database across repeated runs of the same spec, asserting "this project is now first" is only deterministic if the test first removes whatever an earlier run left favourited.

### Mockup UI alignment pass

A Penpot-exported mock UI ("Change Request Overview.pdf") was reviewed page-by-page and the live UI updated to match, per an explicit instruction to build everything shown in the mockup rather than a lighter visual-only pass. This added real backend features (comment reactions, per-entity subscriptions with notifications, org SMTP/SSO storage settings, project creation with terminology/template flag, extended dashboard metrics) alongside the visual rework below.

- **New backend models are additive, not repurposed existing tables.** `CommentReaction` and `Subscription` (`backend/app/models/engagement.py`) are new join tables rather than columns bolted onto `ReviewComment`/`Requirement`/`ChangeRequest`, since reactions and subscriptions are per-user state on entities that already have their own versioned lifecycle — mixing user-specific toggle state into a versioned row would be a design smell.
- **SMTP/SSO organisation settings are a storage-only seam, matching the existing `AuthBackend` seam pattern.** `Organization.smtp_host/port/username/password/use_tls` and `sso_group_mappings` (JSONB) are stored and round-tripped via `GET/PUT /orgs/{id}/advanced-settings` (org_admin only, password write-only and never echoed back), but nothing in `services/email.py` or the auth backends actually reads them yet. This mirrors the same "build the seam, wire it up later" decision already made for pluggable auth backends — inventing a second SMTP-sending code path or a working SSO group-sync flow was out of scope for a UI-alignment pass and would need its own design work. **Update, see "Organisation rename and per-org/deployment-wide test-email actions" below**: the SMTP half of this seam is now partially wired — `POST /orgs/{id}/test-email` reads `Organization.smtp_*` to send a one-off test message — but ordinary notification email still always goes through the deployment-wide `Settings.smtp_*`; the seam remains otherwise unwired.
- **The dark left-navigation rail is intentionally not theme-dependent.** New `--color-nav-*` CSS variables in `theme.css` are defined once under `:root` (not duplicated per `data-theme="light"`/`"dark"`), because the mockup's dark sidebar-on-light-canvas pattern (the same convention as GitHub, Notion, etc.) is a fixed piece of chrome, not part of the app's light/dark theme toggle — U-U-01's "light and dark are both first-class" requirement is about the *content* area, not this rail.
- **Pie/donut charts on the Project Overview dashboard are plain CSS `conic-gradient`, not a charting library.** Recharts/Chart.js/etc. weren't already a dependency; adding one for three simple donut summaries (requirements-by-status, change-request outcomes) was judged not worth the bundle-size and dependency-surface cost when a `<DonutChart>` built from a conic-gradient plus a legend list (`frontend/src/components/DonutChart.tsx`) renders the same information.
- **The Project Admin page tab set (`Project settings` / `Project stages` / `Categories` / `Custom fields` / `Project groups`) intentionally does not copy the mockup's exact tab labels ("Versions" / "Reporting" / "Advanced").** The mockup's tab names describe concepts (release versions, per-project reporting config, per-project advanced settings) that don't correspond to anything in this app's data model — stages are the closest existing concept to "Versions", there is no per-project reporting-settings entity, and "Advanced" (SMTP/SSO) is deliberately organisation-scoped, not per-project. Tabs were named after what the app actually has rather than fabricating unsupported settings just to match label text, per the project's own rule against undocumented behaviour.
- **`CommentThread` and `SubscribeButton` (`frontend/src/components/`) are shared between the requirement and change-request detail pages.** Both pages had near-identical discussion-thread and subscribe-toggle markup/logic before this pass; extracting them was a duplication fix enabled by this work, not a speculative abstraction — both call sites existed and were about to duplicate reaction/subscription logic a second time.
- **Existing Playwright specs needed updating, not just the new one.** Moving components/categories/custom-fields behind Project Admin tabs, and switching the Projects list from a `<table>` to a card grid, broke locators in `golden-path.spec.ts` and `pelion-v2.spec.ts` that predated this pass (`getByPlaceholder("Name").first()` assumed everything was on one scrollable page; `page.locator("tr", ...)` assumed table rows). Both were updated to click the relevant tab first / locate `.card` elements scoped under `main` (the header also carries a `.card` class, so an unscoped `.card` locator matches it too). A new `mockup-engagement.spec.ts` covers the genuinely new behavior: comment reactions, subscribe/unsubscribe, and the dashboard charts.

### Requirements audit fixes

A five-way parallel code-reading audit against every Ossa (v1) / Pelion (v2) row in `docs/requirements.md` found 23 real gaps (86 of 109 requirements were already fully met). All 23 were fixed in one pass; C-U-07 (a real OAuth/SSO backend) and C-U-15 (an actual multi-org membership operational path) were explicitly left as documented seams per the user's own scope call, since both are substantial new features rather than bug fixes.

- **I-M-05 (most significant fix): the server-admin role no longer bypasses any organisation/project data access.** Every RBAC dependency in `services/rbac.py` (`require_org_role`, `require_project_role`, `require_project_view`, `require_project_manage`) and every scattered inline `is_server_admin` check across `routers/{orgs,projects,requirements,change_requests,files,ws}.py` previously granted an unconditional bypass — directly contradicting I-M-05's explicit "does not give access to data within organisations" clarification. The fix is not a blanket removal: one dependency, `require_org_admin_or_server_admin`, exists specifically for the one documented carve-out (server admin creating the *initial* user of a newly created org, per I-M-05's own clarification) and is used only by `create_org_user`. `list_organizations`/`get_organization` deliberately keep a server-admin bypass since `OrganizationOut` is thin directory metadata (id/name/logo), not "data within the organisation," and the server admin needs to see an org exists to complete the one capability the requirement grants. Tests that relied on the old bypass to act inside a second org (`test_cannot_nest_org_group_from_another_organization`, `test_report_rejects_resource_from_another_organization`) were updated to create a genuine org_admin user in that org first, via a new `create_org_admin_in()` conftest helper — this mirrors the real deployment flow the fix now enforces rather than reintroducing a bypass.
- **A genuine, previously-undetected runtime bug was caught by configuring ruff (N-E-04): `services/rbac.py`'s nested-org-group-in-project-group role resolution used `OrgGroup` in a live query but never imported it**, so `get_effective_project_roles()` raised `NameError` whenever that branch executed — meaning C-U-12 ("organisation groups can be used inside project groups") was completely broken for its one legitimate scenario. No existing test exercised a valid same-organisation nesting (only the cross-org rejection case), so this shipped unnoticed. Fixed with the missing import plus a new regression test (`test_org_group_nested_in_project_group_grants_effective_role`) that actually exercises the nested-resolution path.
- **C-U-02 ("all project users must be an organisation user") is enforced with a small `_require_user_in_org()` check** in `assign_project_role` and `add_project_group_member` (routers/projects.py), rather than only being documented as design intent in a comment as before.
- **C-A-05 project-group audit logging now mirrors the org-group side.** `create_project_group`, `add_project_group_member`, `remove_project_group_member`, `assign_project_role`, and `revoke_project_role` all gained `log_event` calls; previously only the equivalent organisation-group endpoints did.
- **C-U-08's "a project must always have ≥1 manager" guard now also applies to group-based removal**, not just direct role revocation — `remove_project_group_member` checks `get_project_managers()` the same way `revoke_project_role` already did, using the same simplified "is this the current sole manager" check (not a full per-path simulation) for consistency with the existing logic.
- **C-N-01's `STAGE_SCOPING` notification now actually fires.** The type existed in the enum and the frontend string table but was never in `_STAGE_NOTIFICATION_TYPES`. Since a project's very first stage is created directly in `create_project()` (never through `create_stage`/`transition_stage`, the only two callers of the notify helper), every real caller of `_notify_stage_transition` is, by construction, never the "brand new project" case the requirement excludes — so adding the entry to the map was sufficient; `create_stage` was also updated to actually call the notify helper (and to log the real actor instead of `None`, a small pre-existing side gap).
- **C-E-04's org default-template setting now actually affects project creation.** `create_project()` falls back to `Organization.default_template_project_id` when the caller omits `template_project_id`; the frontend's "New project" form pre-selects the org's default in its template dropdown so the choice is visible and still overridable. Known limitation, documented rather than solved with a schema change: selecting "None (blank project)" in the UI sends the same `null` as never touching the field, so it currently still resolves to the org default when one is configured — a true override would need a distinct sentinel value in `ProjectCreate`, judged out of proportion for this pass.
- **C-U-16 (org admin locking a user's display name) gained the missing write path**: `PUT /orgs/{id}/users/{user_id}/display-name-lock` plus a lock/unlock toggle in the Org Admin users table. Enforcement already existed; only the ability to *set* the lock was missing.
- **I-M-06: a new `PUT /api/v1/system/users/{user_id}/server-admin` endpoint lets an existing server admin grant or revoke the role on another user**, matching the requirement's literal wording ("this user can assign any user on the system, the server admin permission role") — previously `is_server_admin` was only ever set once, at bootstrap, with no way to extend it afterward.
- **I-A-04: `WEBSOCKET_ENABLED` (default `true`) controls whether `ws.router` is mounted at all** in `main.py`, rather than it always being mounted with no way to disable it.
- **C-A-07 IP geolocation is implemented as a background task, not an inline call**, so it can never add latency to or block the login response. `services/geoip.py` resolves an IP's approximate location via a free, keyless third-party API (`ip-api.com`) only when `GEOIP_LOOKUP_ENABLED=true` (default `false`, since this is a real external dependency and privacy trade-off the user must opt into) and only for addresses outside `GEOIP_LOOKUP_EXCLUDE_CIDRS` (defaults to the standard private/loopback ranges). `routers/auth.py`'s `login` schedules `resolve_and_store_login_location` via FastAPI `BackgroundTasks` *after* the response is built, updating the `LoginEvent` row in a fresh DB session. Every failure mode (disabled, excluded address, network error, timeout) resolves to doing nothing — never an exception that could affect login.
- **U-P-06 pagination is opt-in, not a forced page size**, preserving C-G-05 ("no artificial limit"): the requirements/projects/change-requests list endpoints accept optional `limit`/`offset` and report the pre-slice total via an `X-Total-Count` response header (added to `CORSMiddleware`'s `expose_headers`, since custom response headers aren't visible to browser JS otherwise); omitting both still returns every match, unchanged from before. The frontend's three list pages use a new `api.getPage()` client method and a shared `<LoadMoreButton>` component for simple incremental "load more" pagination at 30 items/page.
- **U-P-05's file count is scoped to requirement attachments within the project, not organisation-wide shared resources** — `ProjectMetricsOut.file_count` counts distinct `FileAsset` rows reachable via `RequirementFile` for the project's own requirements, since shared resources aren't scoped to a single project and counting them would conflate two different things the requirement's "number of files in project" wording doesn't ask for.
- **C-C-03 terminology overrides are applied through a new `TerminologyProvider` React context** (`frontend/src/context/TerminologyContext.tsx`), fetched once per project and provided from `Layout.tsx` down to every page. Applied to the nav labels and the Requirements/Change-Requests page titles and "New X" buttons — deliberately not to every single occurrence of "requirement"/"stage"/etc. across the whole UI (dozens of call sites), which would be disproportionate scope for this pass. Pluralisation is a naive `+"s"` (`useTermPlural`), which reads correctly for the regular-noun default terms but not for an override with an irregular plural (a project would need to type the plural form directly as its override in that case).
- **U-U-03's landing preference is now actually read at login.** `LoginPage.tsx`'s `resolveLandingPath()` sends the user to a specific project id, the overview list, or (for "auto") the sole accessible project if there's exactly one — previously this was stored but login always hardcoded `navigate("/projects")`. The Preferences UI gained a "which project" picker for the specific-project case; `AuthContext`'s `login`/`verify2fa` now return the resolved `User` so the redirect can be computed immediately without waiting on a state re-render.
- **U-C-02: the organisation logo now renders in the app's main header**, not only on the Org Admin settings page, via the same `TerminologyProvider` fetch (extended to also carry `orgLogoFileId`) — shown next to the text wordmark whenever a project (and therefore an organisation) is in scope.
- **U-U-02: the flagged hardcoded strings were moved into the i18n table**, and one adjacent real bug was fixed in passing — the "new requirement" form's reasoning textarea was using the `status` placeholder string instead of `reasoning`.
- **I-M-04: `scripts/backup.sh`/`restore.sh` now also cover local file storage.** The root `docker-compose.yml` previously had no volume for `STORAGE_LOCAL_DIR` at all — despite `docs/deployment.md` already warning "this directory must be a persistent volume" — so a `STORAGE_BACKEND=local` deployment silently lost files on container recreation. Fixed with a named `reqtrack_local_files` volume, and `backup.sh` now archives it (`docker compose exec backend tar czf ...`) whenever the running backend's `STORAGE_BACKEND` is `local`, alongside the existing `pg_dump`. `STORAGE_BACKEND=s3` (the Compose default) is unaffected — MinIO's own backup/replication tooling is still the right approach there.
- **N-E-04/N-E-07: ruff (backend) and ESLint flat config (frontend) are now configured and pass with zero errors**, not previously configured at all. Both are intentionally mechanical gates (pycodestyle/pyflakes/import-sort/modernise/bugbear for Python; TypeScript + React Hooks + Fast Refresh recommended rules for TS) rather than exhaustive Google style guide enforcement — full docstring coverage (`D` rules) is deliberately not part of the ruff config; see N-E-05 below for why that's a separate, narrower pass instead of a repo-wide mechanical gate. `ruff`'s line-length is set to 150 (the codebase's existing max is 146) rather than the stricter 80-char Google default, to avoid a disruptive one-off reformat of already-written code. `B008` (function-call-in-default-argument) is disabled since it flags FastAPI's `Depends(...)` dependency-injection idiom, which is the correct pattern, not a bug.
- **N-E-05: docstrings were added to the dozen-or-so previously-undocumented private helper functions** an AST scan found (e.g. `_to_out`, `_move_ordered`, `_next_sequence`), not a repo-wide docstring pass. Two nested FastAPI dependency closures got a one-line "see the enclosing factory's docstring" instead of a duplicate full docstring, since the enclosing `require_org_role`/`require_project_role` factory functions already fully document the returned closure's behavior.

## Multi-org, multi-persona E2E workflow suite

Full catalogue and rationale in [docs/e2e-workflows.md](e2e-workflows.md); summarized here for the decisions log.

- **Bulk seed data goes through the real API, driven by a standalone script, not direct DB writes.** `backend/scripts/seed_e2e_dataset.py` authenticates as personas and calls the same endpoints the UI does, so seeding exercises the actual validated code paths rather than risking a fixture that's inconsistent with real invariants (unique requirement codes, auto-PM-on-project-creation, etc.). Originally, one exception — deleting the zero-org server-admin persona's `user_org_roles` row directly via SQL — was needed because no product endpoint let a user leave an organisation at all. That gap is now closed (see the next bullet), so the script has zero direct-DB steps: even that persona's departure from its one org is now a real API call, `DELETE /orgs/{id}/membership`, which the seed script's own successful run exercises as a live test of the new endpoint.
- **Self-service "leave organisation" (`DELETE /api/v1/orgs/{organization_id}/membership`).** Found missing while building the E2E suite above; the user asked for it to be built once the suite was otherwise complete. Deliberately narrower than `deactivate_org_user`'s C-U-09 fallback (which reassigns the *acting admin* as a project's new manager when removing someone else): here the caller is the one leaving, so there's no natural recipient for an orphaned manager/admin role to fall back to. Rather than guessing who should inherit it, the endpoint refuses (409) if leaving would strip the org of its last `org_admin`, or leave any of its projects with zero managers, and asks the caller to reassign those roles first. Frontend: a "Leave organisation" button on `OrgAdminPage.tsx`, visible to any member of the org being viewed (not gated to admins, since any role can leave). Tests: `backend/tests/test_rbac.py`.
- **A server admin can only ever be the first org_admin of a *brand-new* organisation, never granted a role in an org they don't already belong to.** `create_org_user`'s server-admin carve-out (`require_org_admin_or_server_admin`, per I-M-05 above) only covers creating a new user; `assign_org_role` requires a genuine `org_admin` caller with no carve-out. This is existing, correct behavior — flagged here because it meant the seed script needed a throwaway "bootstrap helper" user as a second org's first admin, purely to hand off a role to the real persona, mirroring how a human admin handoff would actually work.
- **Change-request Approve/Reject and requirement Archive/edit-form visibility now check the viewer's actual project role, not just the entity's state.** Found while building the change-request approval-separation workflow: both controls were rendered for any project member once a CR was submitted or a requirement unlocked, relying entirely on the backend's 403 to stop the click — correct security-wise, but not a working workflow for the user who sees a button they can never use. Fixed with a new `frontend/src/hooks/useMyProjectRoles.ts` (reads `ProjectListItemOut.my_roles`, the cheapest existing source of this without a new endpoint) in `ChangeRequestDetailPage.tsx` and `RequirementDetailPage.tsx`. The same pattern likely exists elsewhere (e.g. the Requirements list's "New Requirement" button); left as a documented gap rather than a repo-wide pass, since only these two were exercised by this suite's own workflows.

### Full hardening/validation pass

A follow-up parallel security audit (three agents: backend auth/authz, backend input-validation/injection, frontend) covering the whole codebase, not just this session's diff, per the user's explicit request. Two real, high-confidence findings, both fixed and covered by new regression tests; everything else came back clean (RBAC/IDOR boundaries, JWT/session handling, file-path/SSRF/deserialization surfaces, and the frontend's DOM-sink surface were all specifically checked and found sound).

- **Stored XSS via unrestricted uploaded-file Content-Type served inline (High).** `GET /api/v1/files/{file_id}` (`backend/app/routers/files.py`) served every file with the uploader's client-claimed `Content-Type` (never validated against the actual bytes) and `Content-Disposition: inline` — any user with upload rights (a low bar: any project editor for requirement attachments, any authenticated user for their own avatar) could upload a `text/html`/`image/svg+xml` payload that executes as a same-origin page when a more privileged user opens the link, with immediate access to this same endpoint's `?token=` query-param auth and `localStorage`. Fixed by restricting `inline` disposition to a small safe-to-render allowlist (`image/png`, `image/jpeg`, `image/gif`, `image/webp`, `image/x-icon`, `application/pdf` — SVG deliberately excluded since it can embed scripts too) and forcing `attachment` for everything else, plus an `X-Content-Type-Options: nosniff` header on every response from this endpoint. Test: `backend/tests/test_files.py::test_html_attachment_is_forced_to_download_not_rendered_inline`.
- **CSV formula/DDE injection in the requirements export (Medium).** `generate_csv_report` (`backend/app/services/reports.py`) wrote user-editable requirement fields (name, reasoning, clarification, etc.) straight into CSV cells; a value starting with `=`, `+`, `-`, `@`, tab, or CR is interpreted as a formula by Excel/LibreOffice on open — and this export exists specifically for spreadsheet review (R-F-02), making that a realistic workflow, not a theoretical one. Fixed with a `_csv_safe()` helper that prefixes any such value with `'` (stripped from display, never executed) before writing, per OWASP's standard CSV-injection mitigation. Test: `backend/tests/test_reports.py::test_csv_report_neutralizes_formula_injection`.
- **`leave_organization` didn't clean up `OrgGroupMember` rows, and its sole-manager guard couldn't see nested-org-group-derived managers.** Caught during self-review of the new endpoint (not the parallel audit): project access can be granted through an org group nested into a project group (C-U-12), and `get_project_managers` deliberately only resolves *direct* managers (see its own docstring) — so the original implementation could let a user "leave" an org while silently retaining full project access, including manager rights, through a still-active, still-authenticated session (deactivation-based removal doesn't have this exposure, since `is_active=False` blocks all further API access regardless). Fixed by also deleting the caller's `OrgGroupMember` rows for the organisation's groups, and by extending the sole-manager check to fold in `get_effective_project_roles` (which does resolve nested groups) alongside the existing direct-manager check. Note: the "blocking" branch of this fix is currently unreachable through the API in practice — `revoke_project_role`/`remove_project_group_member` independently refuse to remove a project's last *direct* manager using the same nested-group-blind resolver, so a project can never actually reach "zero direct managers, one nested-group manager" today — but it's kept as correct defense-in-depth, documented in `backend/tests/test_rbac.py::test_last_direct_manager_cannot_be_revoked_even_with_a_nested_group_backup`.

### Round 2 hardening: races, session lifecycle, infrastructure

A deeper pass per the user's explicit "go deeper" request: business-logic race conditions, session/credential lifecycle, and infrastructure/config, plus re-examining prior areas more critically. Custom fields and the report data-flow were re-audited and came back clean (no new findings). All fixes below are covered by `backend/tests/test_rbac.py`, `backend/tests/test_user_security.py`, and the full Playwright suite; every backend test (141) and every Playwright spec (10) pass together against a freshly-reset database.

**Business-logic races (TOCTOU, closed with Postgres row locking via `.with_for_update()`):**

- **Change-request double-decision race** (`backend/app/routers/change_requests.py`). `decide_change_request` read the CR with a plain `db.get()`; two concurrent Approve/Reject calls on the same submitted CR could both pass the "is it still submitted" check before either commit landed, double-applying the decision's side effects. Fixed by selecting the row `with_for_update()` so the second concurrent request blocks until the first commits, then sees the now-decided state and is rejected.
- **Org/project manager-count races across four endpoints** (`revoke_project_role`, `remove_project_group_member` in `backend/app/routers/projects.py`; `deactivate_org_user`, and the new `leave_organization`, in `backend/app/routers/orgs.py`). Each independently counts remaining managers/admins before allowing a removal that would drop the count to zero, but without a lock, two concurrent removals of *different* managers could both read "count is 2, safe to remove" and both proceed, leaving zero. Fixed with new `lock_project_for_update`/`lock_organization_for_update` helpers in `backend/app/services/rbac.py`, called before each count check.
- **Stale-subscription content leak on role removal.** When a user's last role on a project was revoked (via any of the four endpoints above), their `Subscription` and `FavoriteProject` rows for that project's requirements/change-requests weren't cleaned up — since notification delivery doesn't re-check current access at send time, they'd keep receiving content updates for a project they'd just lost all visibility into. Fixed with `engagement.remove_subscriptions_and_favorites_for_projects`, called wherever a removal drops a user's effective project roles to empty.

**Session/credential lifecycle:**

- **No token revocation on password change or 2FA disable (High).** JWTs were purely stateless — changing your password (e.g. after suspecting a compromised session) or disabling 2FA didn't invalidate any token issued before the change; a stolen token kept working for its full remaining lifetime regardless. Fixed with `User.token_version`, an integer embedded in every token as the `tv` claim and checked for exact equality against the user's current value on every request (`app/deps.py`); `change_password`/`disable_2fa` increment it, deterministically invalidating every previously-issued token including the one used to make the change. A first attempt compared JWT `iat` against a `tokens_invalid_before` timestamp instead; it was rejected after testing turned up an inherent boundary bug — `iat` is only second-precision, so a token issued in the exact same second as its own revocation could compare as still-valid or (after truncating both sides to fix that) as already-expired-before-issuance, depending on which way the tie was broken. A version counter has no such ambiguity. Regression tests: `backend/tests/test_user_security.py::test_password_change_invalidates_the_old_token`, `::test_2fa_disable_invalidates_the_old_token`.
  - **Frontend fallout, found by re-running the Playwright suite after this fix:** `PreferencesPage.tsx`'s change-password/disable-2FA handlers kept using the now-dead token from `localStorage` for subsequent requests instead of expecting the session to end — the UI just silently broke (401s on reload) rather than prompting re-login. Fixed by having both handlers call `logout()` and redirect to `/login` with a "you've been signed out" message (`LoginPage.tsx` reads it from router state) immediately after a successful change, since the backend has already invalidated the session at that point regardless of what the frontend does.
- **Login response distinguished deactivated accounts from wrong passwords (Medium — user enumeration).** The login endpoint returned a distinct "This account is deactivated." message, and returned it *before* verifying the password — letting an attacker enumerate which emails are registered-but-deactivated accounts without knowing any password. Fixed in `backend/app/auth_backends/native.py`: password verification now always runs first (against a module-level dummy hash when the account doesn't exist or belongs to a different auth backend, to equalize timing with the real-account path), and every failure path returns the same generic `"Invalid email or password."`. Test: `backend/tests/test_user_security.py::test_login_error_does_not_distinguish_deactivated_from_wrong_password`.
- **WebSocket connections outlived their issuing token's expiry.** `backend/app/routers/ws.py`'s connection handler authenticated once at handshake and then held the connection open indefinitely — a token that expired mid-session kept a live, fully-privileged socket open. Fixed by decoding the token's `exp` claim at handshake and checking it every 60 seconds in the receive loop, closing the connection (code 4401) once it's passed. **Correction (later hardening review, see "Deep hardening review" below): this fix covered `exp` only** — it did not add a `token_version` check, so a password change/2FA-disable did not actually close an already-open socket as this entry originally (incorrectly) claimed. That's a separate, since-fixed gap; see below.

**Infrastructure/config:**

- **Weak default Postgres password in the production compose file.** Root `docker-compose.yml` fell back to a hardcoded `reqtrack` password if `POSTGRES_PASSWORD` wasn't set in the environment, in both the `db` service and the embedded `DATABASE_URL`. Fixed by switching both to Compose's fail-fast `${VAR:?message}` form, so a production deploy without an explicit password refuses to start rather than silently running with a guessable one. `docs/deployment.md` updated accordingly.
- **MinIO root credentials used directly by the backend.** The backend authenticates to MinIO/S3 storage with the root account rather than a scoped-down user — not auto-remediated (no code path to change), but documented as a deployment hardening step in `docs/deployment.md` with exact `mc admin user add`/`policy create`/`policy attach` commands to scope a dedicated bucket-only credential for production use.

**Re-audited, came back clean:** custom field value storage/rendering (no injection path — values are always parameterized or escaped the same way as other user content, per the round-1 ReportLab fix), the CSV/PDF report data-flow end to end (confirms the round-1 fixes still hold and no new sink was introduced since).

## Massif (v3)

Massif (v3) implements every Massif-tagged row in `docs/requirements.md` except the three enterprise-auth requirements each large enough to be their own project (E-U-01/E-U-02/E-P-02) — see below for how those were scoped instead. E-P-01 ("allow multiple organisations") required no new work: `Organization` has been a first-class multi-tenant model since Ossa (v1), and server admins have always been able to create additional organisations.

### Review scheduling (C-R-06, C-R-07, C-R-08, C-R-09, C-R-10)

`RequirementVersion` gains `review_date`, `review_lead_days`, `reviewer_id`, `review_reminder_sent_at` — placed on the versioned table, not the `Requirement` identity row, because C-R-06's clarification requires them to only change on creation or via a change request, the same CR-gated path every other content field already uses (`apply_new_version`/`create_requirement` in `backend/app/services/requirements.py` gained matching parameters, with the same `*_explicitly_set` disambiguation pattern already established for `target_stage_id`). A new `RequirementReview` table (`backend/app/models/requirement.py`) records outcomes (C-R-07); the "due for review" definition (`backend/app/services/reviews.py`) is `review_date` in the past *and* no `RequirementReview` recorded since that date — this lets a requirement drop off the due list once reviewed without ever mutating `review_date` itself outside a change request, satisfying the immutability clarification literally rather than by approximation.

A new APScheduler job (`backend/app/services/scheduler.py`, `send_due_review_reminders`) sends the C-R-08 reminder once per review cycle (tracked via `review_reminder_sent_at`, reset to unset on every new version so an unrelated edit can't suppress a future reminder), to the assigned reviewer or every project manager if none is assigned.

### Change-request tasks and stakeholder voting (C-R-02, C-R-03, C-R-04)

`ChangeRequestTask` and `ChangeRequestVote` (`backend/app/models/change_request.py`), both scoped to project-manager creation/decision rights except that a task's own assignee may toggle `is_done` without manager rights. Voting is explicitly advisory: `ChangeRequestVote` never touches `ChangeRequest.status`, and `decide_change_request`'s existing project-manager-only gate is completely unchanged — the vote tally is just additional visible input the decision-maker sees alongside the CR, per C-R-03's own reasoning ("promoting collaborative decision-making," not "replacing the decision").

### Stage review deadlines and "assumed approval" (C-R-05)

`ProjectStage` gains `review_deadline`; a new `StageReviewResponse` table records explicit stakeholder approve/reject responses per review cycle (cleared when a new deadline is set, so a later cycle isn't contaminated by an earlier one's responses). The daily scheduler sweep (`auto_approve_overdue_stage_reviews` in `backend/app/services/stages.py`) auto-approves a stage once its deadline passes, *unless* at least one stakeholder explicitly rejected — matching C-R-05's literal wording ("if they have not provided a response, then it's assumed they have approved") while never silently overriding a raised objection. This asymmetric policy (silence = approve, objection = block) was flagged to the user as the one substantive judgment call in the design and confirmed before implementation.

### Completion tracking (C-P-02, C-P-03)

`ProjectStage` gains `completed_at`/`completed_by` (mirroring the existing `approved_at`/`approved_by` pattern). Rather than adding parallel `is_completed` fields to `Requirement`, C-P-03 reuses `RequirementStatus.COMPLETED` — an enum value that already existed in the codebase (already locked requirement edits via `LOCKED_STATUSES`, already counted in dashboard/report status breakdowns) but had no dedicated endpoint reaching it. `POST /requirements/{id}/complete` transitions `APPROVED → COMPLETED` directly (bypassing the change-request requirement, like archiving already does, since this is a status transition a project manager makes directly rather than content requiring review) — "who/when" is captured by the resulting version's existing `created_by`/`created_at`, with no new columns needed. Stage completion (`POST /stages/{id}/complete`) takes an explicit `cascade_to_requirements` flag, defaulting to `false` per C-P-02's clarification.

**Superseded for the `Requirement` half (2026-08-30):** the "reuse `RequirementStatus.COMPLETED`" choice above turned out to contradict C-G-11's own text once checked directly ("independently of lifecycle state, a requirement may be marked completed... reversed to non-compliant") — baking completion into the same enum as draft/reviewed/approved/archived, reachable only from approved, is not independent of lifecycle state. See the "Completion becomes an overlay marker, not a lifecycle status (C-G-11)" entry near the end of this file for the fix: `Requirement` gained `is_completed`/`completed_at`/`completed_by` (the parallel fields originally rejected here) and `RequirementStatus.COMPLETED` was removed. `ProjectStage.completed_at`/`completed_by` and stage completion's `cascade_to_requirements` flag are unaffected — C-P-02's stage-level completion tracking was always correctly modelled and this supersession is scoped to the requirement side only.

### User directory with access-review filters (C-A-13)

Originally scoped as a dedicated "access review" endpoint; broadened during planning per explicit feedback into query filters (`stale_since_days`, `never_logged_in`, `has_2fa`, `org_role`, `has_project_access`) on the *existing* `GET /orgs/{organization_id}/users` member-listing endpoint, plus a new server-admin-only `GET /system/users` with `no_org_membership` (C-A-13's literal "orphaned account" clarification). Authorization was called out explicitly given this codebase's history of under-scoped org/project checks (see the round-1 hardening section above): the org-scoped filters require `require_org_role(OrgRole.ORG_ADMIN)` resolved against the specific `{organization_id}` in the path (not "an org admin somewhere"), while the unfiltered base listing keeps its existing member-level access; `GET /system/users` requires `require_server_admin` with no org-admin fallback of any kind and deliberately omits `org_role`/`has_project_access` from its response, since those only have meaning scoped to one organisation and would otherwise leak cross-tenant membership through a nominally tenant-blind endpoint (I-M-05). Negative-path tests for all three boundaries live in `backend/tests/test_access_review.py`.

### Selectable report templates (R-G-05)

R-G-05's row in `docs/requirements.md` has the same title text as R-G-04 but different reasoning (branding/presentation vs. shared-resource sections) — read as a documentation copy-paste artifact rather than a literal duplicate, confirmed with the user, who chose "selectable report templates" over the alternative reading of "just render the org logo." `ReportTemplate` (org-scoped, matching how the org logo already works) stores an accent colour, a cover-page toggle, a logo toggle, and footer text; `generate_pdf_report` (`backend/app/services/reports.py`) gained an optional `branding: ReportBranding` parameter threading these into the existing ReportLab styling, falling back to today's plain output when no template is selected.

### SSO login, tested against a real Keycloak instance (E-U-01, E-P-03)

See [enterprise-integration.md](enterprise-integration.md) for the full design. In brief: a real OIDC authorization-code flow (`backend/app/routers/auth_oidc.py`), provider-agnostic via standard discovery, tested end-to-end with a real Keycloak container in `tests/container/docker-compose.yml` and a Playwright spec (`tests/playwright/tests/e2e-workflows/sso.spec.ts`) that drives an actual browser through Keycloak's own login form — not just designed on paper, per the user's explicit instruction that this specifically be tested, not merely documented. The provisioning logic it's built on (`backend/app/services/oidc_provisioning.py`) is written to be reused directly by the not-yet-built SCIM path (E-U-02), so that blueprint isn't speculative.

A genuine Docker-networking problem surfaced during testing — the backend reaches Keycloak via the Compose-internal hostname, but a real browser can only reach the published port, and both must resolve to the *same* issuer URL for token validation to work — resolved via Keycloak's own hostname-pinning config plus a narrowly-scoped, dev/test-only backend setting (`OIDC_INTERNAL_BASE_URL_OVERRIDE`) that rewrites only the backend's own outbound calls, never anything the browser sees. Left unset (a no-op) in the production compose file, since a real IdP's public URL is normally reachable from the backend directly.

**Follow-up, same session:** added a per-org `oidc_required_group` access gate, deliberately separate from `sso_group_mappings` — the mapping list decides *which* role a successfully-admitted user gets, while `oidc_required_group` decides *whether* they're admitted at all, checked in `oidc_callback` before any local account is provisioned or token issued (`services/oidc_provisioning.meets_required_group`). A user outside the required group who authenticates successfully at the IdP is redirected to `/oidc-complete?error=not_provisioned` and shown "Your organisation has not provisioned you access." — the refusal is this app's own policy layered on top of a genuinely successful IdP login, not an IdP-side failure, so no re-registration is needed once an admin adds them to the group. Extended `sso.spec.ts` with two more real-Keycloak-driven tests (blocked user denied, admitted user still gets in) rather than only unit-testing `meets_required_group` in isolation, consistent with the standing instruction that SSO behaviour be proven against a real IdP. One implementation snag: the blocked-then-admitted flow was originally written as two `test.step`s sharing one browser context, which failed because Keycloak's own SSO session (not this app's) persisted across steps and silently reused the first (blocked) user's session instead of showing a fresh login form for the second user — fixed by splitting into two separate `test()` blocks, each with Playwright's default fresh browser context, matching the pattern the first two SSO tests already used successfully.

E-U-02 (SCIM) and E-P-02 (a separate-port provisioning API) were scoped out of this pass — each is effectively its own project — and are covered as an implementable blueprint in `enterprise-integration.md` instead, per the user's explicit choice during planning.

### Massif (v3) hardening pass

Following this project's standing security-review workflow (see the round-1 "Security hardening pass" section above), the new Massif (v3) code — chiefly the OIDC subsystem, since it's the largest new externally-reachable attack surface added this phase — was put through the same three-stage identify → independently re-verify → report process. Four findings survived at confidence ≥8; all four were fixed and the full verification sequence (184 backend tests, 14 Playwright specs including all 4 SSO tests) re-run clean afterward.

**SSRF via org-configurable OIDC endpoints (High).** `Organization.oidc_issuer_url` is set by an org admin, not a server admin, and every OIDC step (`discover`, the token exchange, JWKS fetch) makes an outbound HTTP call built from it or from values returned by *its* discovery document (`token_endpoint`, `jwks_uri`) — so a malicious discovery document could redirect later calls to an entirely different host than the one an admin configured. Fixed in `backend/app/services/oidc_client.py` with `_assert_safe_external_url`, called before every outbound fetch: resolves the hostname via `socket.getaddrinfo` and rejects any resolved address that isn't `ipaddress.is_global` (blocks loopback, link-local, private ranges, and DNS-rebinding-style redirects to internal services), not just a naive hostname-string check. Skipped only when `oidc_internal_base_url_override` is configured (the dev/test-only Keycloak-networking escape hatch above), since that setting exists specifically to point the backend at an internal, non-public address on purpose. Unit-tested directly (no network calls, literal-IP hostnames) in `backend/tests/test_oidc_client.py`.

**Follow-up, raised by the user:** a genuinely self-hosted IdP (on-prem Keycloak/Authentik with no public IP, common on a corporate LAN/VPC) would be rejected by this guard by default — it's indistinguishable from an attacker-controlled internal target from the guard's point of view. Rather than weakening the default, added a second, deployment-level (not per-org) opt-in: `oidc_allow_private_network_targets` (`OIDC_ALLOW_PRIVATE_NETWORK_TARGETS` env var, default `false`), which disables the public-IP check entirely when set. Documented as a blanket, deployment-wide allow that should only be enabled when every org sharing the deployment is trusted not to point its issuer at the deployment's own internal infrastructure — appropriate for single-tenant or fully-trusted-tenant deployments, not one serving mutually-untrusted organisations. See [enterprise-integration.md](enterprise-integration.md).

**`external_subject` cross-issuer collision (proactive, sub-8 confidence but fixed anyway).** The OIDC spec only guarantees `sub` uniqueness *within a single issuer*; matching on `external_subject` alone (as originally implemented) meant two different, unrelated identity providers asserting the same raw subject string would resolve to the same local account. Fixed by adding `User.oidc_issuer` and matching on `(external_subject, oidc_issuer)` together in `find_or_provision_user` (`backend/app/services/oidc_provisioning.py`); covered by a new regression test asserting two different issuers with an identical `sub` value provision two distinct accounts.

**`last_login_at`/`is_2fa_enabled` info leak on the broadened user-directory endpoint (Medium).** The C-A-13 filter work above correctly gated the org-scoped *filters* to org admins, but the endpoint's response body was populating these two fields for every caller regardless of admin status — meaning a plain org member could learn any other member's 2FA status and last-login timestamp just by calling the existing unfiltered listing. Fixed in `backend/app/routers/orgs.py`'s `list_org_users` by gating both fields on the same `is_admin` check already used for the filters, rather than only gating the filters themselves.

**Login-CSRF / session-fixation via a trusted callback URL (Medium).** `OidcCompletePage.tsx` originally accepted any `token` value found in its own URL with no proof it belonged to the current browser's own in-flight login — an attacker who completes their own legitimate SSO or native login could hand a victim a crafted `/oidc-complete?token=...` link and silently log the victim's browser into the attacker's account. Fixed by having the frontend generate a random nonce and store it in `sessionStorage` *before* navigating to the login-start endpoint (`OrgLoginPage.generateClientNonce`); the backend round-trips it through the signed `state` parameter (`security.create_oidc_state_token`, now `(organization_id, client_nonce)`) and the eventual callback redirect, and `OidcCompletePage` refuses to accept a token whose nonce doesn't match what it itself stashed. As a second, related improvement made at the same time: the token handoff moved from the query string (`?token=...`, which proxies commonly log and which persists in browser history) to the URL fragment (`#token=...`, never transmitted to any server over HTTP), cleared from the visible URL via `history.replaceState` immediately after being read.

### Playwright flakiness surfaced (not introduced) by this pass

Adding new nav links ("Requirements due for review," "My reviews due") and new advisory-action buttons ("Vote to approve"/"Vote to reject," stage review-response "Approve"/"Reject") exposed several pre-existing Playwright specs using loose, non-exact `getByText`/`getByRole(..., {name})` locators that happened to still be unique before this pass but are legitimate substring/text matches against the new UI. Fixed by switching the affected assertions to `exact: true` or role-scoped locators (`golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, `e2e-workflows/change-request-approval-separation.spec.ts`). Separately, three specs' "New project" step relied on the org picker defaulting to the correct organisation, which broke once the E2E seed script's three additional organisations sort alphabetically before "Default Organization" — fixed by selecting the org explicitly, using the project's already-established `toContainText(expected option)` wait pattern (stronger than a bare `not.toHaveCount(0)`, which can be satisfied by an unrelated combobox elsewhere on the page before the real one has even mounted).

## SOC 2 compliance: documentation package and codebase remediation pass

A full SOC 2 readiness documentation package was generated at `docs/soc2/` — a system description, a Security + Confidentiality Trust Services Criteria control matrix, and eleven individual policies (information security, risk assessment, access control, change management/secure development, operations/monitoring/logging, incident response, vendor management, data classification/confidentiality, encryption/key management, data retention/disposal, security awareness training), each grounded in the actual codebase rather than generic boilerplate, with known gaps stated explicitly rather than hidden. `CLAUDE.md` was updated with a "Compliance: SOC 2 Policy Adherence" section making these policies binding on future agent work in this repository — before touching auth/RBAC/secrets/audit-logging/retention/vendor integrations, consult the relevant policy and don't silently regress a documented control.

That policy set was then used as the basis for a full codebase compliance review — four parallel, read-only audits (access control/session management, secrets/encryption/tenant-isolation, audit-logging/monitoring, change-management/dependency-hygiene), each independently verified against the actual code before being trusted, following the same identify → verify → remediate discipline as the security-hardening passes above. Every confirmed finding was fixed and the full verification sequence (198 backend tests, up from 189; 14 Playwright specs including all 4 SSO tests) re-run clean afterward.

**Cross-organisation account deactivation/archival (High).** `deactivate_org_user` and `archive_org_user` (`backend/app/routers/orgs.py`) correctly scoped the *caller's* admin role to the specific `organization_id` in the request, but never checked that the *target* user actually belonged to that organisation — an admin of Org A could deactivate or archive any user in the system, including one with no relationship to Org A at all. Fixed by requiring `get_effective_org_roles(db, user_id, organization_id)` to be non-empty before either action proceeds, the same membership check `set_display_name_lock` in the same file already used correctly.

**2FA verification had zero audit trail (High).** `verify_2fa` (`backend/app/routers/auth.py`) never called `log_login`, unlike `/login` — success and failure both went unrecorded, and the endpoint didn't even capture the request IP. Combined with the already-documented absence of account lockout, this meant a TOTP brute-force against a stolen password left no trace at all. Fixed by mirroring `/login`'s pattern: every branch (invalid/expired challenge, inactive/misconfigured account, wrong code, success) now calls `log_login` with the outcome and IP, and a successful verification schedules the same geolocation background task `/login` does.

**SSO logins didn't produce a `login_events` row (Medium).** `auth_oidc.py`'s `oidc_callback` logged successful/denied SSO logins to `audit_events` only, never `login_events`, and captured no source IP; a failed token exchange or ID-token verification wasn't logged anywhere. Fixed by adding `log_login` calls on all three outcomes (exchange/verification failure, required-group denial, success), each with the request IP.

**Plaintext secret columns (Medium, and one previously-undisclosed instance).** `Organization.oidc_client_secret` and `User.totp_secret` were already documented known gaps (plain string columns, relying on infrastructure-level disk encryption only); this review additionally found `Organization.smtp_password` was the same risk class but undisclosed anywhere. Rather than patch just the one the user asked about, all three were closed together: a new reusable `EncryptedString` SQLAlchemy column type (`backend/app/models/encrypted_type.py`) transparently encrypts values with Fernet, keyed from a new, independently-rotatable `APP_SECRET_ENCRYPTION_KEY` setting (distinct from `JWT_SECRET`, fail-fast-required in production like the other secrets). Application code reads/writes plaintext as before; only the database ever sees ciphertext. Covered by `backend/tests/test_encrypted_type.py`, which asserts the *raw* database value is genuinely undecryptable ciphertext, not just re-formatted plaintext.

**Missing audit-trail coverage on ~15 mutating endpoints (Medium).** The logging policy claims every action that mutates organisational/project structure is audit-logged; a review of every router found this wasn't quite true — org logo/login-background upload, default-template setting, report-template CRUD, project settings/terminology/report-config updates, component/category create and reorder, change-request task create/update, change-request vote casting, custom field definition create/delete, and requirement link creation and file linking/unlinking all skipped `log_event`. All now call it, following each file's existing pattern (entity type/id, action, actor, project/org scope, and a narrow `detail` dict — not a full request payload).

**WebSocket deactivation-recheck window (Medium, narrowing an already-documented gap).** The `docs/soc2` access-control policy's originally-drafted "deactivation doesn't revoke tokens" gap turned out to be based on an incomplete reading: REST already rechecks `is_active` fresh on every request (`deps.py`), so that part was never actually a gap. The real, narrower one was WebSocket-only — `routers/ws.py`'s connection loop closed on token expiry but never rechecked `is_active` mid-connection, so a deactivated user's already-open socket could keep streaming project updates for up to the token's full lifetime. Fixed by adding a DB-backed `is_active` recheck (`_user_still_active`) on the same ~60-second cadence the loop already wakes up on for its expiry check, closing the socket within one interval of deactivation rather than waiting for token expiry. Project *role* changes are still not rechecked mid-connection — documented as a narrower residual, not eliminated entirely.

**`assign_org_role` path/body `user_id` mismatch (Low).** The endpoint's URL contains `{user_id}` but the handler operated on `payload.user_id` from the request body instead, never comparing the two — not exploitable (the org-admin authorization check was still correctly scoped), but a confused-deputy/audit-trail footgun where the URL a caller was authorized against could diverge from who was actually affected. Fixed by using the path parameter as the sole source of truth; the body's `role` field is still used, but a mismatched body `user_id` is now silently ignored rather than trusted.

**False positive dismissed, not fixed:** an apparent "missing Alembic migration source" (a compiled `0002_pelion_v2_schema.cpython-312.pyc` in `backend/alembic/versions/__pycache__/` with no matching `.py` file) was investigated and found to be a stale, gitignored local build artifact left over from before an earlier migration was squashed into `0001_initial.py` per this project's pre-release convention — not evidence of an undocumented schema change. Deleted for hygiene; no code or documentation change needed.

`docs/soc2/` was updated to reflect every fix above: the access-control policy's deactivation-gap wording was corrected (REST was already fine; the WebSocket case is now fixed too) and gained a note about the cross-org authorization fix; the encryption policy's known-gaps section marks the three plaintext-secret entries resolved and documents `APP_SECRET_ENCRYPTION_KEY`; the confidentiality policy's matching gap entry was struck through; and `trust-services-criteria-mapping.md`'s CC6.3, CC6.5, CC7.2, and C1.1 rows and its summary gap list were updated to match — consistent with the policy's own instruction (and this project's standing practice) not to let documented gaps go stale once the underlying issue is actually fixed.

## Continuous integration

Added `.github/workflows/ci.yml`, closing what the SOC 2 control matrix had flagged as the single most consequential gap against CC4.1/CC8.1: automated test suites existed and passed, but nothing enforced them — they were run manually, whenever someone remembered to. Three jobs:

- **`frontend`** — `npm run lint` + `npm run build` (ESLint, then `tsc -b && vite build`). Fast, no Docker.
- **`backend-and-e2e`** — brings up the *real* `tests/container/docker-compose.yml` stack (the same one developers run locally, deliberately not a hand-rolled set of GitHub Actions `services:` blocks, to avoid a second Postgres/MinIO/Mailhog config that could silently drift from the one actually documented and tested — see the "Two separate Compose stacks" rationale in `deployment.md`), then runs the backend pytest suite with coverage (`--cov-report=xml/html`, `--junitxml`), `ruff check`, and — after the standard restart-and-reseed step pytest's schema-truncation requires — the full Playwright E2E suite. Test results are published as GitHub check annotations (`dorny/test-reporter`) for both the backend and E2E suites separately, a coverage percentage is written to the job summary, and the HTML coverage report and Playwright artifacts are uploaded for download. All of it is gating — a failure at any step fails the job.
- **`docker-build`** — builds the production backend/frontend/mcp-server images, gated on the two jobs above passing, and pushes them to `ghcr.io` (see "Container image publishing and version tagging" below for when this changed from build-only).

Before wiring `ruff check` in as a blocking gate, the pre-existing lint state was checked rather than assumed clean — it wasn't: 13 issues (8 import-ordering, 5 unused-variable) had accumulated across several files over the session, invisible until now because nothing had ever run `ruff check` as a gate before. Fixed directly (auto-fix for the import-ordering ones via `ruff check --fix`, manual for the unused-variable ones) rather than either silencing the rule or leaving the new gate red on day one. Same check performed for `npm run lint`/`npm run build` before wiring those in — both were already clean (7 pre-existing `react-refresh` warnings, non-blocking).

Coverage is reported, not gated — no `--cov-fail-under` threshold is enforced, since no baseline had been agreed on. Current backend line coverage is ~90% (198 tests), noted in `docs/soc2/policies/change-management-and-secure-development-policy.md` as a starting point for setting one later.

`docs/soc2/` was updated to match: the change-management policy's "No CI pipeline" gap is marked resolved (with the residual gap — no branch protection requiring the check to pass before merge — restated as its own item, since that's a GitHub repository setting, not something a workflow file can configure), and `trust-services-criteria-mapping.md`'s CC4.1 and CC8.1 rows and summary gap list were updated to match. The README gained a CI status badge and a short "Continuous integration" section.

## Documentation refresh, demo dataset, and README screenshots

Every documentation file except the authoritative `docs/requirements.md` was reviewed against the codebase's actual current state and corrected where it had drifted — a fair amount had, since several docs predate Massif (v3), the two hardening passes, and the CI pipeline above, and had never been revisited once those landed.

**New: `backend/scripts/seed_demo_data.py`.** A second, separate seed script alongside the existing `seed_e2e_dataset.py` — deliberately not a variant of it, since the E2E dataset's org/persona names and content exist purely to give the Playwright suite fixed, predictable anchors, not to look presentable. This one seeds "Solstice Robotics," a fictional drone-inspection company, with two projects deliberately left at different lifecycle stages: Falcon-3 Inspection Drone (baselined — every requirement approved or completed, one change request pending) and Solstice Cloud Platform (still actively scoped — a mix of draft/reviewed/approved requirements, one change request approved and one pending). This split wasn't originally planned; it fell out of discovering that approving a project stage bulk-baselines *every* non-archived requirement in the project (`services/baseline.create_baseline_for_stage`, pre-existing, documented behaviour), not just requirements targeting that stage — rather than fight that to force artificial status variety onto one project, the two projects were left to naturally demonstrate two different, equally realistic points in a project's life. Same conventions as the E2E script: everything created through the real HTTP API, idempotent by skipping (no organisation-deletion endpoint exists to support a true reset — see the retention-policy gap noted earlier), documented in its own module docstring including a recipe for what a future nightly-resetting public demo instance would need (a database-level reset via `down -v`/`up --build`, not an application-level one).

**README screenshots replaced.** The ten screenshots in `docs/screenshots/` predated the UI redesign (`git log`: "V1 and V2 requirements are now met. UI needs a full re-design," the commit immediately before this project's current state) and, it turned out, weren't even linked from anywhere in the README — orphaned. Deleted and replaced with nine new ones captured live against the running dev/test stack (via the Playwright MCP browser tools) after seeding the demo dataset above: login, project dashboard, project overview, requirements list, requirement detail (with its discussion thread), change request detail (tasks, stakeholder votes, discussion), organisation admin (members, roles, access-review filters), reports (with a selected branding template), and reviews-due. Wired into a new "Screenshots" section in the README with a two-column layout and captions.

**Other documents corrected:**
- `README.md`: the "What's included" bullets understated the backend (no mention of OIDC/SSO or the new application-layer secret encryption) and overstated staleness in the Playwright description ("two specs," long since tests/e2e-workflows/ and sso.spec.ts existed — actually ten spec files). Added the "Screenshots" and "Demo data" sections above.
- `docs/deployment.md`: `APP_SECRET_ENCRYPTION_KEY` was missing from the required-secrets list and the `.env` example (added when the encryption-at-rest hardening landed, but this doc wasn't updated alongside it); added the `keycloak` service to the components table; noted that CI builds but doesn't publish images, so `docker compose up --build -d` remains the only way to obtain them; cross-referenced the new demo-data seed script.
- `docs/solution-architecture.md`: the "Extensibility" section still listed SSO providers, external file storage, and multi-tenant enterprise integrations as *future* additions — all three have been built since. Added an implementation note correcting this (the one genuinely still-future item is extracting the in-process background jobs into a separate worker service) and a second one covering the encryption-at-rest mechanism, the login-CSRF nonce fix, and pointing at `docs/soc2/` as the live security-control record rather than duplicating it here.
- `docs/user-guide.md`: had no coverage at all of Massif (v3) — no review scheduling, change-request tasks/voting, stage review deadlines/auto-approval, stage/requirement completion, access-review filters, report branding templates, or SSO login. Added coverage of all of them in the relevant existing sections rather than as a separate "v3 features" appendix, so the guide reads as one coherent walkthrough rather than a base doc plus a changelog.
- `docs/enterprise-integration.md`: `find_or_provision_user`'s documented signature/behaviour predated the issuer-scoping fix (it still described matching on `external_subject` alone); added that fix, the login-CSRF nonce mechanism, and the `oidc_client_secret` encryption-at-rest fix — all three postdate this document's last edit.
- `docs/e2e-workflows.md`: written before `sso.spec.ts` existed — added it as workflow #7, alongside the two Keycloak-native test personas it uses (separate from `seed_e2e_dataset.py`'s personas, since Keycloak provisions them, not this app's API). Also simplified its "how to run it" recipe, which still described an old copy-the-script-into-the-container workaround from before the seed script was baked into the backend image.
- `docs/soc2/`: spot-checked against the latest state (test counts, control matrix rows) after the CI and encryption work above — already current, no further changes needed beyond what those two passes already made.

## Same-origin subpath deployment (API under `/api`, avoiding CORS)

Every REST route the frontend calls already lives under `/api/v1/...` (`backend/app/routers/*.py`'s own router prefixes), so hosting the API at `my.website.com/api/` alongside the UI at `my.website.com/` — one origin, routed by path at the reverse proxy — needed **no backend routing changes at all**: an nginx `location /api/ { proxy_pass http://backend:8000; }` (no path on `proxy_pass`, so the URI forwards unchanged) lines up with the existing path structure automatically. This is also the reason to prefer this pattern over the two-hostname alternative (`app.example.com` / `api.example.com`) where CORS actually matters: putting both on one origin makes the question moot rather than requiring `CORS_ORIGINS` to be configured correctly, since same-origin requests never trigger CORS enforcement in the first place.

What did need a fix: `PUBLIC_API_BASE_URL` needs to be set to an **empty** value (not merely omitted) for this pattern, so the frontend calls the API via relative paths against its own origin instead of an absolute URL. Both `docker-compose.yml` files' `VITE_API_BASE_URL: ${PUBLIC_API_BASE_URL:-http://localhost:8000}` and the frontend's own runtime-config generator (`frontend/docker/env-config-entrypoint.sh`) used `${VAR:-default}` — the colon form, which treats an explicitly-set *empty* string the same as unset and silently substitutes the default anyway. That's the correct behavior for "no default was chosen," but wrong for "empty was chosen on purpose," and there was no way to tell the two apart with a colon in the expansion. Fixed in all three places by dropping the colon (`${VAR-default}`), which POSIX parameter expansion (and Docker Compose's own interpolation, which follows the same rule) only treats as "apply the default" when the variable is truly absent. Confirmed the fix rather than trusting the one-character diff: `docker compose config` with `PUBLIC_API_BASE_URL=""` now shows `VITE_API_BASE_URL: ""` (previously silently became the `http://localhost:8000` default), and a real built frontend image run standalone with `VITE_API_BASE_URL=""` serves a genuinely empty value in its generated `env-config.js`.

The reverse-proxy recipe itself was also verified against the real running stack, not just written from the routing analysis above: an actual nginx container, configured exactly as documented, proxying to the real dev/test backend and frontend containers, returned identical responses (including a real login request) whether called through the proxy's `/api/` path or the backend directly.

Documented in `docs/deployment.md` as a new "Same-origin subpath deployment" section alongside the existing two-hostname TLS/reverse-proxy guidance, with the concrete nginx config, and a note on scope: the `/api/` passthrough only reaches paths that genuinely start with `/api/` on the backend, so `/health`/`/metrics`/`/docs`/`/openapi.json` (none of which carry that prefix) aren't reachable through it without extra one-to-one location blocks — reasonable as a default, since health/metrics are normally scraped over the internal Docker network rather than exposed publicly anyway. Also cross-referenced from the README's frontend configuration note.

## MCP server: AI-assistant access to requirements

Added `mcp-server/` — a fourth deployable component (alongside backend, frontend, and now itself) exposing requirements/projects/organisations to AI assistants via the [Model Context Protocol](https://modelcontextprotocol.io), built on `fastmcp` (the community-maintained Python MCP framework, not just the bare `mcp` SDK — its `get_http_headers()` dependency and `@custom_route` decorator were the two things that made the design below straightforward rather than requiring a hand-rolled ASGI layer).

**The one design decision that mattered: pass-through authentication, not a service account.** Every tool call requires the *caller* to present their own ReqTrackManager access token as an `Authorization` header on the MCP connection; the server does nothing but forward that exact header to the backend on every request it makes. It holds no credentials of its own, stores no token, and implements no authorization logic — the backend's existing, already-hardened RBAC is the only thing deciding what any given call can see. This was chosen over the obvious alternative (the MCP server logging in once as a fixed "integration" account) specifically because that alternative would mean every user of this server sees whatever that one shared account can see, rather than their own actual project memberships — exactly backwards for a codebase whose entire security posture this session has repeatedly hardened around *per-user, per-project* scoping (the SOC2 access-control policy, the IDOR fixes, the cross-org deactivation fix two passes ago). A shared credential would also have been a much larger blast radius if it ever leaked. Six tools, all read-only by design (`list_organizations`, `list_projects`, `get_project`, `list_requirements`, `get_requirement`, `get_requirement_history`) — deliberately not the full API surface; letting an AI *read* requirements is a materially smaller, safer thing to ship first than letting it act on the workflow.

**Input validation at the boundary**: `project_id`/`requirement_id`/`component_id`/`category_id` arguments are validated as syntactically well-formed UUIDs (`uuid.UUID(value)`) before being interpolated into a backend URL, rejected with a clear error otherwise. Not a security boundary the backend doesn't already enforce on its own (FastAPI's UUID-typed path parameters already 422 on malformed input) — but validating it here too means a malformed tool argument can never influence what URL this server itself constructs, closing off a path-confusion class of concern before it's even a question, consistent with this project's established practice of validating at every layer rather than trusting a single upstream check.

**Verified for real, not just designed**: rather than trust the auth-forwarding logic from code review alone, ran it against the actual live dev/test stack using `fastmcp`'s own Python client — confirmed the full tool chain (list projects → search requirements → get detail → get history) returns real, consistent data; confirmed a missing Authorization header, an invalid token, and a malformed UUID argument each fail with the intended clear error rather than a stack trace or a silent empty result; and, most importantly, confirmed that two different real accounts (the demo org admin vs. an unrelated E2E persona in a different organisation) each see only their own accessible projects through this server — the actual pass-through-RBAC guarantee, not just "a request succeeded." A small integration test suite (`mcp-server/tests/test_server.py`, using the same `fastmcp` client) now covers this and runs in CI.

One implementation snag along the way: `fastmcp`'s `get_http_headers()` excludes `Authorization` (and a few other sensitive headers) *by default* — reasonable as a default for a framework where most tools shouldn't see it, but this server's entire purpose is relaying it, so it's called as `get_http_headers(include_all=True)`, deliberately opted back in in exactly the one function that needs it rather than more broadly.

**Deployment and client setup**: added as a fourth service to both Compose stacks (`REQTRACK_API_URL` pointing it at `backend`, its own `/health` check, port `8100`). New `docs/mcp-server.md` covers the authentication model in full, plus concrete setup instructions for the three clients the user asked for by name — Claude Code (including a `mcp-server/scripts/get_auth_header.sh` helper written for Claude Code's `headersHelper` mechanism, which re-runs the login on every connection/reconnect and automatically retries once on a 401/403, working around this app's tokens being short-lived without needing a new long-lived-token feature), VS Code Copilot Chat (the `.vscode/mcp.json` `inputs`-array pattern, avoiding a hardcoded token), and Microsoft Copilot Studio (its native MCP wizard's header-based API-key auth type, which lines up with this server's design directly) — plus generic guidance for any other Streamable-HTTP MCP client. Exposing it to a remote client like Copilot Studio reuses the same reverse-proxy subpath pattern from the previous session's work, with a worked `/mcp/` nginx location block.

**SOC2 documentation updated to reflect a genuinely new surface, not just a new feature**: this is the first component added this session that's meant to be reachable by third-party, customer-chosen infrastructure (Copilot Studio's own cloud) rather than only by this deployment's own frontend. `system-description.md` gained the component itself plus a new CUEC (the customer's own responsibility for which AI platform they connect and what token they give it); the vendor-management policy gained an item explaining that a customer-connected AI platform becomes a genuine subprocessor of *that customer's* data the moment they wire it up, which the Company neither controls nor is responsible for; the confidentiality policy clarifies this is a data-*flow* consideration, not an isolation-*control* gap, since the same RBAC boundary still applies; and the access-control policy notes that this integration deliberately reuses existing session tokens rather than introducing a new credential type, so every existing control in that policy (issuance, 2FA, SSO, the 12-hour lifetime, `token_version` revocation) already covers it.

**Known, documented limitations, not silently worked around**: no long-lived API-token mechanism exists yet in ReqTrackManager, so anything other than Claude Code's self-refreshing `headersHelper` needs periodic manual token updates as the underlying 12-hour session token expires; SSO and 2FA accounts can't complete the non-interactive login helper script (it does a single password POST, which can't finish either flow), so those need a token obtained by hand instead. Both called out explicitly in `docs/mcp-server.md` rather than glossed over.

CI extended to match: `mcp-server` now builds and runs its own `ruff check` and `pytest` suite in the `backend-and-e2e` job (after the E2E dataset is seeded, so its data-dependent test exercises real data rather than skipping), and `docker-build` now also builds (not publishes, same as the other two images) its production image.

## MCP server: expanded tool set, a built-in login page, and SSO without a custom OAuth server

Follow-up to the section above, from two pieces of feedback on the first cut: (1) the tool set only covered requirements, not change requests/comments/votes/notifications/reviews; (2) getting a token still meant hand-crafting a `curl` call, and the user specifically asked whether this server could "bring up an integrated browser or set up a quick server that has an interface to connect," citing VS Code's Azure DevOps ("ado") MCP server as the experience to aim for.

**Tool set grew from 6 to 15**, all read-only, all following the exact same shape as the original six (UUID validation via `_require_uuid`, forwarding through `_call_backend`, full docstrings): `list_change_requests`, `get_change_request`, `list_change_request_votes`, `list_change_request_tasks`, `list_change_request_comments`, `list_requirement_comments`, `list_notifications`, `list_my_reviews_due`, `list_project_reviews_due`. No new design decision here — the pass-through-auth model and RBAC boundary from the original build cover these identically; it was purely a matter of finding and wrapping the right existing endpoints (`routers/change_requests.py`, `notifications.py`, `reviews.py`).

**Rejected: a full custom OAuth 2.1 authorization server**, which is what would be needed to get the *exact* zero-click experience the "ado mcp" comparison implies (the MCP client itself detecting a 401, discovering OAuth metadata, and driving the whole flow with no page ever shown to the user manually). `fastmcp` does ship an `OAuthProvider` abstraction for this, but its own documentation describes it as "an extremely advanced pattern that most users should avoid," requires implementing nine abstract methods (client registration, authorization, code/token exchange and storage, revocation, verification), and is explicitly incomplete in its own docs on PKCE, `state` handling, and `redirect_uri` validation — the exact details that make OAuth authorization servers a well-known place to introduce real vulnerabilities. Not a fit for a project whose security posture this whole session has been built around getting *right*, not fast.

**Built instead: a plain HTML `/login` page, served directly by `mcp-server` itself** (`@mcp.custom_route`) — one of the two options the user explicitly offered ("a quick server that then has an interface to connect"). A user visits it in a browser, submits email/password (with a follow-up TOTP form if the account has 2FA), the page relays those credentials to the backend's own existing `/api/v1/auth/login` / `/api/v1/auth/2fa/verify` server-side, and renders the resulting token plus ready-to-paste Claude Code/VS Code config snippets directly in the response body. It introduces no new authentication mechanism — it's a convenience wrapper around the login endpoint that already existed, not a second credential store. Security details that mattered: the token is only ever in the response *body*, never a URL (same "no secrets in URLs" principle as the earlier SSO login-CSRF fix); every response carries `Cache-Control: no-store`; the submitted email is never echoed back into any HTML (the re-shown form on failure is a fresh, empty one — closing off any need for manual escaping by construction); the password lives only in the request-handler's local scope for the one relay call; no CSRF protection was added because, like the native frontend's own login form, there's no ambient session/cookie authority here to forge.

**Then genuinely got the "already logged in → it just works" experience for SSO orgs**, once asked directly whether the login page detects an existing session (it can't, for native accounts — the frontend keeps its token in `localStorage`, which is strictly origin-scoped and `mcp-server` runs on its own port, so there's no browser mechanism to observe "already logged in" across origins even in principle). SSO is different: ReqTrackManager already has a real, working OIDC flow (`routers/auth_oidc.py`, proven against a real Keycloak instance by `tests/playwright/tests/e2e-workflows/sso.spec.ts`), and an IdP's own browser session is exactly the ambient state that makes flows like `ado`'s feel instant. The one obstacle was that the OIDC callback was hardcoded to always redirect back to the frontend app. Fixed by threading a `client` field (`"app"` or `"mcp"`, restricted to those two literal values by FastAPI's `Query(..., pattern="^(app|mcp)$")`) through the existing signed `state` token (`security.create_oidc_state_token`), and having `oidc_callback` pick between two *fixed, server-configured* redirect bases — `settings.frontend_base_url` (unchanged default) or the new `settings.mcp_server_public_url` — never a caller-supplied URL. This matters: a redirect target taken from client input here would be a textbook open-redirect / token-theft vector (the access token travels in the URL fragment on success), so the design keeps the set of possible destinations fixed at two, chosen only by a value inside a token the server itself signed. `mcp-server`'s `/login?org=<slug>` page adds a "Sign in with SSO" button (via the backend's existing public `GET /orgs/by-slug/{slug}/login-info` lookup) and a new `/login/oidc/complete` landing page that is entirely client-rendered — it performs the identical anti-replay nonce check `OidcCompletePage.tsx` already does (a `sessionStorage` value generated before the redirect, compared against what comes back), for the same login-CSRF reason documented on `create_oidc_state_token`.

**Noticed and closed in passing**: the root production `docker-compose.yml`'s `backend` service never actually set `PUBLIC_BACKEND_URL` or `FRONTEND_BASE_URL` (only the dev/test stack did) — meaning a real production deployment's OIDC redirects would have silently used the `localhost` code defaults. Added both alongside the new `MCP_SERVER_PUBLIC_URL`, all three following the same `${VAR:-default}` convention already used elsewhere in that file.

**Verified for real**: rebuilt both containers; `mcp-server`'s test suite grew to 19 (the original 7, three covering the new tool set, four covering the `/login`/`/login/2fa` credential flow including a genuine round-trip login that produces a token verified to actually authenticate a live tool call, and four covering `/login`'s SSO-branch rendering — unknown org, SSO-disabled org, SSO-enabled org, and the `/login/oidc/complete` shell); a new Playwright spec extends `sso.spec.ts` to drive a real browser through Keycloak's actual login form starting from `mcp-server`'s own `/login?org=...` page (not the frontend), confirming it lands on `/login/oidc/complete` with a token that genuinely authenticates against `/api/v1/auth/me`. Full backend pytest (198) and the full Playwright suite (15, including the new spec) both pass. One process note worth recording since it briefly looked like a regression: running backend pytest against the dev/test stack leaves its database migrated-but-empty afterward (documented already under "Database: the test suite was wiping the live database"), which surfaced as 14 unrelated Playwright failures until `docker compose restart backend && python scripts/seed_e2e_dataset.py` was re-run — not a bug in this pass, just the same existing gotcha CI's own workflow already works around.

`docs/mcp-server.md` updated throughout: the tool table now lists all 15, "Getting a token" leads with the `/login` page (manual `curl` demoted to "the scripting-friendly way"), and "Known limitations" replaces the old blanket "read-only, scoped to requirements" line with the real remaining gap — no zero-click OAuth handshake — explained with the same reasoning as above.

## Personal Access Tokens

Direct follow-up to the section above: `docs/mcp-server.md`'s "Known limitations" had flagged "no long-lived API-token mechanism" since the MCP server was first built — every credential in ReqTrackManager was a 12-hour session JWT, forcing `mcp-server`'s `headersHelper` refresh-script workaround. The user asked for a proper Personal Access Token system: user-created, scoped to whichever organisation(s) the creator chooses, capped by both a per-org admin-configured maximum and a system-wide default, usable as MCP auth, fully self-service via the UI — plus, in follow-up feedback, incident-response bulk revocation at three blast radii (a user's own tokens, an org's, the whole platform) and a per-user/per-token action for an org admin dealing with one departing member.

**Design decisions:**

- **Opaque, hashed secret (`rtm_pat_<43 url-safe chars>`), not a JWT.** Only `sha256(token)` is ever stored (`security.generate_pat`/`hash_pat`) — mirrors how passwords are already treated (hash, never plaintext) and is the standard shape for exactly this credential type (GitHub/GitLab/Stripe all do this). The real reason it's not a JWT: revocation needs to be instant and per-token, which a hash lookup against a DB row gives for free, where a stateless JWT would need a second revocation-list mechanism bolted on anyway.
- **Independent of `User.token_version`.** A password change/2FA-disable still bumps `token_version` and kills every session JWT immediately (unchanged) — but deliberately does *not* touch PATs, which have their own `revoked_at`. Tying them together would defeat a PAT's entire purpose (not needing to reconfigure an integration). User deactivation (`is_active=False`) still kills a user's PATs immediately, same as sessions, since that's checked fresh on every request regardless of credential type.
- **Org scope is a JSONB list on the token row** (`allowed_organization_ids`), not a join table — matches the existing `Organization.sso_group_mappings: JSONB` precedent for "small structured list scoped to one row"; scope is only ever read from a specific token, never queried "which tokens touch org X" in bulk (the org-admin listing endpoint does a JSONB `@>` containment scan instead, cheap enough at this scale).
- **Expiry is recomputed at auth time, not frozen at creation.** `services/pats.effective_expiry` computes `min(pat.expires_at_ceiling, *[pat.created_at + that org's *current* cap for every currently-scoped org])` on every request. This means an org admin *lowering* their cap applies retroactively to already-issued tokens — "maximum time limit... on these being active" reads as an ongoing constraint, not a one-time snapshot at issuance — while *raising* a cap never extends a token past what was already fixed. `expires_at_ceiling` (computed once, at creation, from the caps in effect then) is what's shown in the UI as a stable "expires by" date; the dynamic recheck can only ever pull the true expiry earlier than that, never later. A new `PAT_DEFAULT_MAX_LIFETIME_DAYS` setting (90) is the fallback for any org that hasn't set its own cap, so no PAT is ever unboundedly long-lived by default.
- **PATs never satisfy `require_server_admin`, full stop** (`services/rbac.require_server_admin`) — not even for a genuine server admin's own token. A PAT's entire design is "which orgs can it reach"; that's meaningless for a deployment-wide action, and I-M-05's "server admin doesn't give access to data within organisations" extends naturally to treating a PAT as an inherently org-scoped credential.
- **The org-scope restriction is layered on top of real RBAC, applied at every org/project-scoped dependency factory** (`require_org_role`, `require_org_admin_or_server_admin`, `require_project_role`, `require_project_view`, `require_project_manage` — all in `services/rbac.py`) via a new `check_pat_scope`/`check_pat_scope_for_project` check, reading `request.state.pat_allowed_org_ids` (set only by `deps._resolve_user_from_pat`, so an ordinary session-JWT request pays zero extra queries). It restricts, never grants: a PAT can only ever do what its owner's real roles already allow, narrowed further by scope. Endpoints with `organization_id` in the request *body* rather than the path aren't covered by those factories — audited for this and found exactly one, `POST /api/v1/projects` (project creation, `organization_id` in `ProjectCreate`), fixed with an explicit `check_pat_scope` call in `routers/projects.create_project`.
- **Personal, cross-org-by-nature endpoints are deliberately untouched by PAT scoping** — `GET /notifications`, `GET /me/reviews/due`, `GET /auth/me`. Restricting "which org's *content*" doesn't naturally extend to "your own inbox"; called out explicitly rather than silently decided.
- **Bulk "revoke all" (self/org/platform) fully kills a matching token, never partially narrows it.** An org admin's "revoke all PATs in my org" revokes outright every non-revoked token that includes that org *anywhere* in its scope, even if also scoped to unrelated orgs — a security reset should mean "these tokens are dead," not leave one alive-but-narrowed in a way its owner might not notice. The UI's confirm dialog states this side effect explicitly. A platform-wide reset (server admin only) is framed as pure security/tenancy administration, same category as creating organisations — it never reads or exposes org content or any token secret (hashes aren't reversible), so it doesn't conflict with I-M-05.
- **The org-admin per-token view (`GET /orgs/{id}/pats`) never reveals *which* other organisations a multi-org token also reaches — only a count** (`other_org_count`). Same confidentiality class this codebase already closed for `GET /system/users` ("would leak which orgs a user belongs to"). This is what makes the companion **descope** action (remove just this org from a token's scope, leaving it valid elsewhere) meaningful without leaking cross-tenant membership: the admin doesn't need to know *which* other org to decide revoke-outright vs. descope, and the descope action itself doesn't need that identity either. Descoping down to zero remaining orgs auto-revokes (a token with nothing left in scope can never authenticate anything).
- **Self-service endpoints live under `/api/v1/me/pats` (`routers/pats.py`)**, mirroring the existing `/api/v1/me/reviews/due` convention; org-admin actions live in `routers/orgs.py` and the platform-wide reset in `routers/system.py`, alongside those routers' other org-/deployment-scoped admin actions, rather than a fourth PAT-specific router.

**Verified for real**: 13 new backend tests (`test_personal_access_tokens.py`) covering creation/listing/single-and-bulk revocation, the org-scope restriction on both org- and project-path endpoints (proven against a user with genuine RBAC access to *both* orgs, so a 403 can only be the PAT-scope check, not an ordinary permissions failure), expiry defaults/per-org caps/multi-org min-selection, a requested expiry beyond the cap coming back clamped rather than erroring, an expired token's rejection, retroactive cap-tightening (backdating a token's `created_at` via direct SQL, then lowering the org's live cap, then confirming the token is rejected — no time-mocking infra existed in this suite before, so this is the pattern to reuse for the next test that needs it), a deactivated user's token rejected immediately, PATs rejected at the server-admin gate even for a real server admin, the org-wide and platform-wide bulk resets (including the multi-org "fully killed, not just descoped" side effect and the negative 403 cases), and the org-admin per-token list/revoke/descope flow including the `other_org_count`-only confidentiality assertion and the 404-not-403 non-disclosure check. Full backend suite: 211 passed (198 + these 13), zero regressions from the `rbac.py` changes. A new Playwright spec creates a token through the real Preferences UI, uses it to authenticate a genuine `GET /auth/me` call, revokes it through the UI, and confirms that exact same call now 401s — the deep RBAC/expiry logic is pytest's job; this spec's job is proving the token the UI hands a user is a real, independently-revocable credential, not just correctly rendered. Manually driven a full pass through all three UI surfaces (self-service create/list/revoke in Preferences, org-admin max-lifetime + per-token table + bulk revoke in OrgAdminPage, platform-wide reset in ServerAccessReviewPage) against the live dev stack, confirming a token created through the browser genuinely authenticates a live `curl` call and genuinely stops working the instant it's revoked.

`docs/mcp-server.md`'s "Getting a token" now leads with Personal Access Tokens as the recommended path (the `/login` page's session token demoted to "quick, but short-lived"), and its "Known limitations" marks the long-standing "no long-lived token mechanism" gap resolved. `docs/soc2/policies/access-control-policy.md` gained a new Authentication item introducing PATs as a second bearer-credential type, and amended the item that previously stated there was "deliberately no... API-key mechanism issuing its own broader access" to reference PATs as the intentional, narrowly-scoped exception — a PAT still never grants more than its owner's real roles, so the "no broader access" principle holds, but the line needed to stop implying no such mechanism existed at all.

## Deep hardening review

Requested explicitly as a whole-codebase pass, not scoped to the immediately preceding PAT work. Three parallel reviews (identify → verify → remediate, matching this codebase's own established hardening-pass methodology): authentication/credential handling end to end; authorization and multi-tenant isolation across every router; injection/SSRF/secrets-handling/file-storage safety. Every candidate finding was independently re-verified by reading the actual current code (and, for the storage-key issue, hand-tracing the exact path-resolution arithmetic) before anything was fixed — nothing below was taken on an agent's word alone. Four real, previously-unnoticed issues surfaced; a fifth candidate (WebSocket DNS-rebinding-style TOCTOU in the OIDC SSRF guard) was investigated and correctly judged not a new, reportable gap — already an accepted, documented trust-boundary limitation, not something this pass changed.

**1. Personal Access Tokens could read files outside their declared organisation scope (`backend/app/routers/files.py`).** `download_file` resolves the caller via `get_current_user_header_or_query` (needed so `<img src>` tags can pass the token as `?token=`, which the `require_*` dependency factories in `services/rbac.py` don't support) — meaning it never went through the factories that layer the PAT org-scope check (`check_pat_scope`/`check_pat_scope_for_project`) onto every other org/project-scoped endpoint. It only checked the caller's *real* RBAC role, which a PAT's owner of course still has, even outside what they scoped that particular token to. A PAT created scoped only to Org A could therefore download an Org B file its owner also happens to have genuine access to via their own account — quietly defeating the entire point of scoping a token in the first place, for exactly the endpoint most likely to hold sensitive content (attachments, org resources). Fixed by calling `check_pat_scope`/`check_pat_scope_for_project` explicitly inside `download_file` once the file's owning org/project is resolved, mirroring the pattern `require_project_manage` already uses (resolve resource → derive org → check scope → check role). This is the second body/non-path-scoped gap found in this codebase (the first, `POST /api/v1/projects`, was caught and fixed while building the PAT feature itself) — both share the same shape: an endpoint that resolves the owning org *after* looking up some other resource, rather than taking `organization_id`/`project_id` directly as a path parameter the dependency factories can key off. Worth a mental grep the next time a new resource-scoped endpoint is added. New regression test: `test_personal_access_tokens.py::test_pat_scope_enforced_on_file_download`.

**2. Cross-tenant path traversal in local file storage (`backend/app/services/files.py`, `backend/app/storage_backends/local.py`).** The storage key was built as `f"{organization_id}/{uuid4()}_{filename}"`, with the client-supplied `filename` concatenated in raw — despite `local.py`'s own comment claiming the key was "uuid-based, never taken directly from user input." It wasn't: a filename with **more than one** `../` segment defeats the uuid prefix. The first `../` merges with the prefix into one literal, harmless path component (`<uuid>_..`, since it's adjacent with no separator) — but any *additional* `../` segments in the filename remain real traversal operators. Hand-tracing the resolution: a filename like `../../../other-org-id/evil.txt` normalizes to `<storage_local_dir>/other-org-id/evil.txt` — inside the shared root (so `LocalFileStorageBackend._path_for`'s own confinement check, `is_relative_to(base_dir)`, still passes) but *outside* the uploading org's own key prefix. Every local-backend upload endpoint is reachable by a low-privilege caller (`/me/avatar` needs no role at all; org resources/attachments need only ordinary project/org membership), so this was a real cross-tenant storage **write** primitive — planting or overwriting files in another organisation's on-disk namespace, or a disk-fill DoS scoped to a victim tenant. (A cross-tenant *read* of an existing file would additionally require guessing its random UUID component, so the demonstrated compromise is the write/isolation break, not confirmed exfiltration.) The S3 backend is unaffected — object keys aren't hierarchically resolved, so `../` is just a literal character sequence there. Fixed with a new `services.files._safe_key_component()`: reduces the filename to its final path segment (discarding directory components — and therefore any `..` — entirely, not just the first one) and replaces any remaining non-alphanumeric character, so the generated fragment can never be interpreted as a path separator or traversal operator by any backend that resolves keys hierarchically. The original client filename is still preserved verbatim in `FileAsset.filename` for display/download purposes — only the storage-key fragment is sanitized. New regression test: `test_files.py::test_uploaded_filename_cannot_escape_its_organisations_storage_key_prefix`, asserting directly against the DB row (the storage key isn't exposed via the API) that the key stays confined to `{organization_id}/` with no `..` or `/` surviving from the filename, and that upload/download still round-trips correctly.

**3. Secrets-redaction gap in 422 validation-error responses (`backend/app/main.py`).** `redact_sensitive_validation_errors` blanks a failed field's echoed `input` for `password`/`current_password`/`new_password`/`totp_secret`/`code`, closing the general case of Pydantic's validation errors echoing back whatever was submitted — but `_SENSITIVE_FIELD_NAMES` never included `smtp_password` or `oidc_client_secret`, the exact same class of genuine secret this codebase already encrypts at rest (`EncryptedString`) elsewhere. A request that fails validation on either field (e.g. submitting a non-string JSON value) echoed the submitted secret back verbatim in the response body — the caller's own secret round-tripping to them, not a cross-tenant leak, but a live inconsistency with a protection this codebase had already built for exactly this purpose, and 422 bodies are commonly captured by reverse-proxy/APM/error-monitoring tooling outside the app's own control. Fixed by adding both field names to the set. No new test added — this is a one-line set addition covered by the existing `redact_sensitive_validation_errors` logic path, not new logic; if a future pass wants explicit regression coverage, extend the existing (unwritten) test for that handler alongside a case for one of the currently-covered field names.

**4. WebSocket connections never checked `token_version`, contradicting this codebase's own documentation (`backend/app/routers/ws.py`).** `token_version` is documented in three places as "this system's primary technical incident-containment tool" (access-control-policy.md) — a password change or 2FA disable is supposed to deterministically kill every previously-issued token immediately, including tokens behind already-open connections. The WebSocket handler's handshake and periodic 60-second recheck loop (added in an earlier hardening pass specifically to close "a token that expired, *or was revoked via the `token_version` mechanism*, mid-session," per this doc's own prior wording at the time) in fact only ever checked `exp` and, in the loop, `is_active` — never `tv` against the user's current `token_version`. An attacker holding a stolen session token could keep a live WebSocket connection open and receiving project updates for up to its remaining 12-hour lifetime, *even after* the legitimate user took the exact containment action (a password change) the incident-response plan documents as immediately closing this off. This is a materially more severe gap than the already-accepted 60-second `is_active` window (the two prior entries above and in access-control-policy.md's Known Gaps) — that one is a small, bounded, deliberately-accepted delay; this one was a complete bypass of the mechanism entirely, for as long as a token remained unexpired. Fixed: the handshake now decodes and checks `tv` against `User.token_version` (matching `deps._resolve_user_from_token`'s existing REST check exactly), and the renamed `_user_session_still_valid()` (was `_user_still_active()`) now checks both `is_active` and `token_version` on every periodic recheck. The prior, inaccurate claim in this file's own earlier "WebSocket connections outlived their issuing token" entry (see above) has been corrected in place rather than left standing. New regression tests: `test_websocket_security.py` (a real `client.websocket_connect(...)` round trip proving a token that was valid before a password change is rejected at handshake afterward with code 4401, plus a direct unit check of the renamed recheck function); `test_ws_active_recheck.py` (pre-existing, testing only `is_active`) updated in place for the rename and extended to also pass a `token_version`.

**Not a finding**: SSRF via DNS rebinding on org-configured OIDC endpoints (check-time IP validation vs. connect-time resolution) — a real theoretical gap, but the only actor who could exploit it is the org admin who configured `oidc_issuer_url` in the first place, already the documented trust boundary this project accepts for that setting (an org admin is trusted not to point their own org's issuer at internal infrastructure, same as the existing `oidc_allow_private_network_targets` escape-hatch documents). Investigated and correctly excluded rather than silently skipped.

**Verified for real**: every finding above was confirmed by reading the actual current code (not the audit agents' descriptions alone) before any fix was written; the storage-key path-traversal claim specifically was hand-traced through pathlib's component-stack resolution to confirm the exact "first `..` merges with the uuid, later ones don't" mechanism before trusting it. All four fixes have dedicated regression tests. Full backend suite: 215 passed, zero regressions. Full Playwright suite re-run clean afterward.

## Deep hardening review, round 2

Explicitly requested as a second, *deeper* pass, not a repeat of the first. Three more parallel reviews, deliberately scoped away from what round 1 already covered: race conditions/TOCTOU and privilege-escalation/business-logic integrity; an exhaustive line-by-line re-read of `mcp-server/server.py` plus OIDC role-provisioning's escalation surface plus notification/email content-injection; deployment/infrastructure configuration plus mass-assignment/over-posting. Same discipline as round 1 — every finding independently re-verified against the actual current code before any fix, nothing taken on an agent's word alone. Two findings were surfaced but deliberately left as decisions for the user rather than fixed unilaterally, since both are genuine behavior-change judgment calls, not unambiguous bugs; both were then approved and implemented.

**1. A Stakeholder or Project Administrator could self-approve a requirement via direct edit (`backend/app/routers/requirements.py`).** `update_requirement` gates on `CAN_EDIT_ROLES` (manager, administrator, *or* stakeholder) and passed `payload.status` straight through — but `docs/requirements.md`'s C-U-03 clarification is explicit that requirement approval is a *Project-Manager-exclusive* capability layered on top of what administrators/stakeholders can do, not shared with them (the exact distinction `decide_change_request` already enforces for CR approval, with its own "C-U-03: project manager only" comment — `update_requirement` had no equivalent). Concretely: any stakeholder on a project could `PUT` a requirement with `"status": "approved"`, becoming its own `approval_authority`, locking it, and forcing all further edits through a change request — an authority meant to require Project Manager specifically. Compounding this, the same endpoint allowed jumping straight from `draft` to `"status": "completed"` in one request, skipping the dedicated `/complete` endpoint's own "must already be approved" precondition entirely, regardless of role. Fixed: `update_requirement` now requires `PROJECT_MANAGER` specifically when `payload.status == APPROVED`, and rejects `payload.status == COMPLETED` outright (400), pointing callers at the dedicated endpoint instead. New regression tests in `test_permission_matrix.py`: stakeholder and administrator both rejected (403) attempting approval via direct edit, project manager succeeds, and direct-to-completed is rejected (400) for any caller.

**2. Lost-update race in the Personal Access Token org-admin "descope" action (`backend/app/routers/orgs.py`).** `_get_org_pat_or_404` loaded the PAT row with a plain `db.get()`, and `descope_org_pat` did a read-modify-write on `allowed_organization_ids` (read the list, remove one org, write it back) with no row lock — at Postgres's default READ COMMITTED isolation, two concurrent descopes on the same multi-org token (e.g. org A's admin and org B's admin both reacting to the same incident at once, exactly the scenario this feature exists for) would each read the same pre-removal list, and whichever commits last silently overwrites the other's removal — the "losing" org's admin sees 204 and a real audit-log row claiming success, while the token remains scoped to their org regardless. Fixed with a `SELECT ... FOR UPDATE` row lock in `_get_org_pat_or_404` (the same pattern `lock_project_for_update`/`lock_organization_for_update` already use elsewhere in this file), so the second concurrent call blocks until the first commits, then reads the already-updated list. No new test — this class of fix isn't verifiable without real thread-level concurrency, which this codebase's existing lock-based fixes (`decide_change_request`'s own `.with_for_update()`, the org/project manager-count locks) also don't have automated coverage for; verified by the same code-reading/reasoning this session already applied to those.

**3. `deactivate_org_user` had no protection against ending an organisation with zero active admins, or against self-targeting (`backend/app/routers/orgs.py`).** Unlike the conceptually similar `leave_organization` (which refuses to let a sole admin leave), this endpoint had neither check, and didn't even block `user_id == current_user.id`. On inspection, the "last admin" case turned out to need no separate check at all: since the *caller* must already hold `org_admin` on this exact org to reach this endpoint, and `org_admin` (unlike project roles) is never derived indirectly, the calling admin's own role necessarily survives any deactivation they perform on someone *else* — so once self-targeting is blocked, zero active admins becomes unreachable through this endpoint by construction, with nothing further to check (added, then recognised as dead code once the self-targeting block was in place, and removed rather than shipped unverifiable — matching this project's "don't add validation for scenarios that can't happen" convention). The self-targeting block is the real fix: deactivation sets `is_active=False` on the *whole account*, not just this org's membership (unlike `leave_organization`), so an org-scoped admin action was able to lock a caller out of every organisation they belong to, with no confirmation step. New regression test in `test_rbac.py`: self-targeting rejected (400), a genuinely different admin can still deactivate normally.

**4. Password changes and 2FA enable/disable were never audit-logged (`backend/app/routers/auth.py`).** `change_password`, `confirm_2fa`, and `disable_2fa` all mutate security-critical account state and committed with no `log_event` call at all — the password-change notification is user-facing, not an admin-visible audit trail entry. Every other sensitive mutation in this codebase (role grants, deactivation, PAT bulk-revoke, SSO config changes, etc.) already calls `log_event`; these three didn't, and it isn't listed as an accepted exception in `docs/soc2/policies/system-operations-monitoring-and-logging-policy.md`. Fixed by adding `log_event` calls (`password_changed`, `2fa_enabled`, `2fa_disabled`) to all three. New regression test in `test_user_security.py`, querying the `audit_events` table directly (no existing test infra did this anywhere in the codebase before — added a small `_audit_action_count` helper following the same direct-DB-query pattern used elsewhere this session for PAT/storage-key regression tests).

**5. `mcp-server`'s org-slug lookup interpolated an unencoded query parameter into a URL path (`mcp-server/server.py`).** `_get_org_login_info` built `f"{REQTRACK_API_URL}/api/v1/orgs/by-slug/{slug}/login-info"` directly from the `?org=` query parameter. Reproduced concretely: a crafted `org` value containing `..` or an embedded `?` can make httpx's own URL normalization escape the intended path prefix or split the request at the wrong point, reaching a different backend path than intended. Confirmed low real impact today — this call is always anonymous (no caller token is ever forwarded), and every unauthenticated `GET` on the backend is already meant to be public (`/health`, `/metrics`, and this `login-info` lookup itself) — but fixed anyway with `urllib.parse.quote(slug, safe="")` so correctness here stops depending on that happening to remain true.

**6. OIDC SSO role sync only ever granted roles, never revoked them (`backend/app/services/oidc_provisioning.py`) — the "role ratchet" gap.** `sync_org_roles_from_claims` computed `mapped_roles - existing_roles` and only ever added rows; once a role was granted via a matching IdP group claim, removing the person from that group (or deleting the mapping) never revoked it on a later login — a real timely-deprovisioning gap (CC6.2/CC6.3-relevant) not previously documented anywhere. **Flagged to the user rather than fixed unilaterally**, since the correct semantics involve a genuine tradeoff (sync-down could, if done carelessly, strip a role an admin granted manually outside any SSO mapping, or over-react to a provider that simply doesn't send a groups claim at all) — approved, then implemented: sync-down now revokes exactly `(existing_roles & sso_managed_roles) - mapped_roles` on every login where the IdP *does* assert a groups/roles claim (even an empty one), where `sso_managed_roles` is every role appearing anywhere in `org.sso_group_mappings` — so a manually-granted role outside that vocabulary is never touched in either direction, and an IdP that omits the claim entirely (ambiguous with "doesn't support this claim" rather than "asserts zero groups") is left alone rather than triggering a mass revocation. New regression tests in `test_oidc_provisioning.py`: a role is revoked once its matching claim disappears; a manually-granted, mapping-external role survives; existing roles are left alone when the IdP sends no groups/roles claim at all.

**7. All three containers (backend, frontend, mcp-server) ran as root, with no `USER` directive and no documented rationale anywhere.** Not independently exploitable, but a real, missing defense-in-depth layer this project's own information-security policy commits to ("least privilege and defense in depth... no single control is treated as sufficient on its own"). **Flagged to the user rather than fixed unilaterally**, since getting this wrong (volume/file permission mismatches) risks breaking the running stack — approved, then implemented carefully: each Dockerfile now creates and switches to a dedicated non-root user (`app`/`nginx`/`mcp`), with the backend's `/app/data/files` mount point and the frontend's `/usr/share/nginx/html` + nginx runtime dirs (the frontend's own entrypoint script writes `env-config.js` there at container *startup*, not build time, so that directory needs to be writable by the runtime user, not just readable) explicitly `chown`'d ahead of time so a fresh named volume inherits the right ownership. `docs/deployment.md`/Dockerfile comments note that an *existing* deployment's already-created local-storage volume will still be root-owned after upgrading and needs a one-time `chown` (or a volume reset, consistent with this project's other pre-release "reset and reseed" upgrade path) — not a live gap in a fresh deployment, just a real migration note for an existing one. Verified directly (not just by code review, since a permissions mismatch would only surface at runtime): rebuilt all three images, did a full `docker compose down -v && up -d --build`, confirmed `id` inside each container reports the new non-root user, and specifically exercised the local storage backend's actual write path at its real mount point (`/app/data/files`) end-to-end as that user before trusting the fix.

**Not a finding**: header-injection into outgoing email (CRLF/BCC-injection via a crafted display name) — empirically verified clean; Python's `EmailMessage` header assignment raises on any embedded line-break-like character regardless of what user-controlled text is assigned. HTML-email-body injection — not applicable, this codebase only ever sends plain-text email. Mass assignment via `**payload.model_dump()` against an ORM object — grepped for the pattern across the whole codebase; every instance found builds a JSONB-column dict from validated nested schemas, never a blind ORM constructor call. CORS wildcard-with-credentials, exposed database ports in the production compose, and hardcoded real secrets — all checked, all clean.

**Adjacent robustness issue surfaced but explicitly not fixed (out of this review's scope)**: `send_daily_digests` has no per-user error isolation and defers its `db.commit()` to the end of the loop — an embedded newline in any `Notification.title` (titles are built from unconstrained `str` fields like `Project.name`) would raise inside Python's own header-safety check and silently break the digest for *every* user on *every* org, repeating daily until manually fixed. This is an availability/robustness bug, not a security vulnerability (no unauthorized access or data exposure), so it's noted here for future attention rather than fixed under a hardening review's scope.

**Verified for real**: all seven findings fixed have dedicated regression tests (except #2, the PAT-descope race, per its own entry's explanation). Full backend suite: 224 passed (221 + 3 new OIDC sync-down tests), zero regressions. `mcp-server`: 15 passed, 4 skipped (pre-existing, unrelated — the bootstrap admin's own org has no projects in a fresh reseed). Full Playwright suite: 16 passed, including a real end-to-end exercise of file upload/download against the now-non-root containers.

## Deep hardening review, round 3

Explicitly requested as a third, still-deeper pass. Three more parallel reviews, scoped away from everything rounds 1-2 already covered: baselines/custom-field/report-template integrity; cross-tenant validation on resource-*creation* endpoints specifically (as opposed to prior rounds' focus on read/update paths); and a narrowly-scoped, analysis-heavy pair — quantifying whether the already-accepted "no account lockout" gap (which the project has always scoped to unbounded *password* guessing) also covers the 2FA challenge step, plus a check of frontend build/bundle exposure. Same discipline as rounds 1-2: every finding independently re-verified against the actual current code before any fix. One finding was surfaced to the user for a decision rather than fixed unilaterally, since it involved a genuine UX tradeoff (new account-lockout behavior); approved, then implemented.

**1. Adding a member to an org group never checked the member actually belonged to that organisation (`backend/app/routers/orgs.py`).** `add_org_group_member` validated that `group_id` belongs to `organization_id`, but never that `payload.user_id` holds any role in that organisation at all — unlike the structurally parallel `add_project_group_member` (`routers/projects.py`), which explicitly enforces C-U-02 ("all project users must be an organisation user") via `_require_user_in_org`. Because `get_effective_project_roles` resolves project access through org groups nested into project groups purely from `OrgGroupMember` rows (checking only that the *group's* org matches the project's, never that the *member* belongs to that org), an org admin adding an arbitrary user id here — anyone in the system, with zero relationship to the organisation — would silently hand that user full project access the moment the group is (routinely, legitimately) nested into any project group: a genuine cross-tenant privilege escalation, not a data-integrity nit. Fixed by requiring `get_effective_org_roles(db, payload.user_id, organization_id)` to be non-empty before adding the member (400 otherwise). New regression test: `test_security_hardening.py::test_cannot_add_user_from_another_organisation_to_an_org_group`.

**2. A `new_requirement` change request's proposed component/category were never validated against the target project (`backend/app/routers/change_requests.py`).** `create_change_request`'s `modify_requirement` branch validates `requirement_id` against `project_id`, but the sibling `new_requirement` path never checked `proposed_component_id`/`proposed_category_id` the same way the direct-create endpoint (`create_requirement_endpoint`) does. Since there's no update endpoint for these fields (write-once, set only at creation) and `decide_change_request` only re-checks `is None` at approval time, an unvalidated cross-project reference would sail straight through approval and get baked permanently into a real `Requirement` row. Fixed by adding the equivalent project-membership check to the `else` branch. New regression test: `test_security_hardening.py::test_cannot_create_change_request_with_component_from_another_project`.

**3. The 2FA challenge step has no attempt limit, and the accepted "no account lockout" gap doesn't actually cover it (`backend/app/routers/auth.py`, `backend/app/security.py`).** The documented gap in `access-control-policy.md`'s Known Gaps is specifically about unbounded *password* guessing — a keyspace large enough that no realistic number of attempts converges toward success, so the absence of lockout there is a reasonable accepted risk. A TOTP code is a *bounded* 6-digit keyspace (10^6), and `verify_2fa` had no attempt counter at all: the 5-minute `2fa_challenge` JWT (`create_2fa_challenge_token`) is stateless and freely reusable for repeated guesses, `/login` (which mints a fresh challenge token) is equally unthrottled, and there's no rate-limiting middleware anywhere in this codebase. Quantified: even at a conservative sustained rate for this single-instance deployment, a single 5-minute window already yields single-digit-percent hit probability, and — critically — an attacker holding a stolen password can simply restart the login flow for a fresh window as many times as they like, since the challenge token itself carries no memory of prior failures. Unlike password guessing, repetition here compounds toward a near-certain eventual bypass rather than staying negligible, making this a materially different and more urgent risk than the already-accepted gap, not a restatement of it. **Flagged to the user for a decision** among per-account lockout, IP-based rate limiting, both, or leaving it as an accepted gap — per-account lockout was chosen. Fixed: `User` gained `failed_2fa_attempts`/`failed_2fa_locked_until`; `verify_2fa` now locks the *account* (not the challenge token) for `two_factor_lockout_minutes` (default 15) after `two_factor_max_failed_attempts` (default 5) consecutive wrong codes, checked before every code comparison — so restarting the login flow for a new challenge token does not reset the lockout, closing exactly the compounding-via-repetition path identified above. Both thresholds are configurable via `Settings`. New regression tests in `test_user_security.py`: repeated wrong codes lock out the account such that even the *correct* code (via a brand-new challenge token) is rejected until the window passes; a backdated `failed_2fa_locked_until` clears the lockout and a correct code succeeds again.

**Not a finding (Part 2 — frontend build/bundle exposure)**: `vite.config.ts` has no `build.sourcemap` setting (Vite's default is off) — confirmed by actually building the frontend and inspecting `dist/`: no `.map` files produced. No hardcoded secrets found in the built bundle or in `frontend/src` (the only credential-adjacent string is the expected, placeholder-valued `VITE_API_BASE_URL` runtime config). Storybook (`build-storybook`) is confirmed dev-only — `frontend/Dockerfile`'s build stage runs only `npm run build` and copies only `dist/`, and `.github/workflows/ci.yml`'s frontend job never invokes `build-storybook` — it is never built into or served by the production image.

**Not a finding (baselines/custom fields/report templates)**: no issues found in this area on this pass.

**Verified for real**: all three findings fixed have dedicated regression tests. Full backend suite: 228 passed (224 + 4 new), zero regressions. Full Playwright suite re-run once on freshly reseeded data: 16 passed.

## Orphaned-account review: server-admin false positive, and closing the "nobody can act on this" gap

The user noticed two problems with the C-A-13 orphaned-accounts review (`GET /api/v1/system/users?no_org_membership=true`): server admins were being listed as "orphaned," and the review page had no action at all — read-only, with no way to do anything about a listed account.

**Server admins incorrectly flagged as orphaned.** A server admin has no organisation membership *by design* (I-M-05) — that's the intended shape of the role, not an oversight — so `no_org_membership=true` now always excludes `is_server_admin=True` users, regardless of any other filter. A new, independent `is_server_admin` filter lets a server admin review the server-admin roster specifically (deliberately *not* restricted to org-less accounts, since I-M-08 lets a bootstrap server admin also hold an organisation of their own).

**No way to act on a listed orphaned account.** Investigation showed this wasn't just a missing button — there was no *endpoint* that could reach one. `deactivate_org_user`/`archive_org_user` (`routers/orgs.py`) both require the target to already belong to the acting admin's organisation (a SOC 2 hardening fix from an earlier pass) — which an orphaned user, by definition, never does. So the exact accounts this review exists to surface were the one category of user nobody in the system could ever deactivate. Two new system-level endpoints close this, deliberately scoped to only ever touch org-less accounts (`_require_orphaned_user`, 400 otherwise — an org member must still be managed through their own organisation's admin console, keeping a server admin's authority tenancy-wide but content-free per I-M-05):
- `POST /api/v1/system/users/{id}/deactivate` (C-U-04; blocks self-targeting)
- `POST /api/v1/system/users/{id}/reactivate` — a genuinely new capability: no lifecycle action anywhere in this codebase previously reversed a deactivation at all, org-scoped or otherwise.

**Grant/revoke server admin got a UI, and a safety net it didn't have before.** `PUT /api/v1/system/users/{id}/server-admin` (I-M-06) already existed but had no frontend surface at all. Building one surfaced a real, previously-unexploited gap: nothing stopped a server admin from revoking their *own* flag (or another's) down to zero active server admins deployment-wide — an unrecoverable lockout, since nobody would then hold the authority to grant it back to anyone short of direct database access. Fixed by rejecting a revocation that would leave zero active server admins (counting only active ones, since a deactivated admin can't do anything anyway) — the same "last admin" shape as `leave_organization`'s existing sole-org-admin check, just computed by count rather than self-targeting alone (a server admin can revoke *someone else's* flag too, not just their own).

**Frontend**: `ServerAccessReviewPage` gained a "Show" filter (Orphaned accounts / Server administrators / All users — the last needed to find a user to grant server admin to in the first place, since no user-search UI exists anywhere else in the app) and an "Include deactivated accounts" checkbox, both independent axes. Per-row actions adapt to what's actually valid for that user: Deactivate/Reactivate for org-less accounts, Grant/Revoke server admin for everyone. `SystemUserOut` gained `is_server_admin` and `has_org_membership` (a plain boolean, not naming any organisation — no confidentiality regression, since `no_org_membership=true` already let a caller infer this one user at a time).

New regression tests in `test_access_review.py`: the orphaned filter excludes a server admin while the `is_server_admin` filter finds them; `has_org_membership` reflects reality; deactivate/reactivate an orphaned account (proven via an actual login attempt before/after, not just the flag); an org member is rejected (400) at both new endpoints; self-targeting is rejected; revoking the deployment's last active server admin is rejected, while revoking one of two succeeds. Manually verified end-to-end in a real browser (login, switch views, deactivate/reactivate, grant/revoke, and the last-admin-revoke error message rendering), per this project's UI-testing convention — including confirming the safety net actually blocks the browser action, not just the API call directly.

**Verified for real**: full backend suite: 234 passed (228 + 6 new), zero regressions. Full Playwright suite re-run once on freshly reseeded data: 16 passed. Manual browser verification of every new interaction described above.

## UI: persisted view mode, collapsible nav rail and sections, theme-aware chrome

Four related UI requests: the tile/list view toggle should be a real user preference (not per-browser localStorage); the left nav should collapse to icons; card sections (the user's concrete example: "Create" inside the Personal Access Tokens card) should collapse to just their title; and the nav rail should follow the active theme instead of always being dark.

**A general-purpose `ui_preferences` key/value bag, not a one-off field.** The first cut added a narrowly-typed `view_mode_preferences: dict[str, str]` column. Prompted by the user to generalise it before building more UI on top, since a nav-rail-collapsed flag and per-section collapse state were about to need the exact same shape. Renamed to `User.ui_preferences: dict[str, Any]` (JSONB, mirroring `Organization.sso_group_mappings`'s "structured but open-ended" precedent) with no per-value schema validation — any low-stakes display preference is just a new namespaced key (`view_mode:<page>`, `nav_rail_collapsed`, `section_collapsed:<key>`), never a migration. `PATCH /auth/me/preferences`'s `ui_preferences` field is shallow-merged into the existing bag by top-level key (reassigned, not mutated in place, so SQLAlchemy's change-tracking sees it), so setting one key never clobbers another. A new frontend hook, `useUiPreference(key, default)`, wraps this generically (reads `useAuth()`'s `user.ui_preferences`, writes via a new `setUiPreference` on `AuthContext` that updates local state optimistically and PATCHes in the background) — `useViewMode`, the nav-rail collapse toggle, and `CollapsibleSection` are all now thin, one-line specialisations of the same hook, not three separate implementations.

**Tile/list view mode** (`ViewToggle.tsx`'s `useViewMode`): previously read/wrote `localStorage`, so a user's choice didn't follow them across browsers/devices, unlike every other preference on this page (theme, landing page, digest mode). Now backed by `useUiPreference` under the key `view_mode:<pageKey>` — the same three call sites (`projects`, `requirements`, `change-requests`) needed no change beyond the hook's own implementation.

**Collapsible nav rail** (`Layout.tsx`): a new chevron button at the foot of the rail toggles `railCollapsed` (`nav_rail_collapsed` preference), independent of the pre-existing `menuOpen` mobile show/hide toggle. Every nav label is now wrapped in `<span className="nav-label">`, hidden via a `.nav-rail-icons` CSS rule when collapsed, so collapsing is a pure CSS/class change rather than conditionally rendering different JSX per link.

**Collapsible sections** (`components/CollapsibleSection.tsx`, new): a header button + chevron wrapping arbitrary children, collapsed state persisted per `sectionKey` via `useUiPreference`. `variant="card"` renders a full `.card` (top-level sections: Profile, Security, Personal Access Tokens, Notification preferences); `variant="plain"` renders just the header with no background (nested subsections: Change password, Two-factor authentication, and the PAT card's Create form — the user's named example). Applied throughout `PreferencesPage.tsx`. One real regression caught by the existing Playwright suite: the "Change password" section header and the "Change password" submit button inside it ended up with the same accessible name, breaking `pelion-v2.spec.ts`'s `getByRole('button', {name: 'Change password', exact: true})` locator (`strict mode violation`, two matching elements). Fixed generally, not just for this one case: every `CollapsibleSection` header gets `aria-label="<title> section"` when its title is a plain string, disambiguating it from any same-named control in its own content — a legitimate a11y improvement independent of the test collision it happened to also fix.

**Theme-aware nav rail** (`theme.css`): the nav rail previously used a dedicated, always-dark `--color-nav-*` variable set, explicitly documented as "NOT swapped per data-theme" (matching an earlier reference-mockup decision). Removed entirely — the rail now uses the same `--color-surface`/`--color-border`/`--color-text`/`--color-primary` variables as every other panel, so it follows the active light/dark theme like the rest of the UI, per the user's explicit ask.

**Verified for real**: new backend test (`test_ui_preferences_are_shallow_merged_not_replaced`) proves the merge-by-key semantics, including that setting an existing key overwrites only that key and that values persist across requests. Full backend suite: 235 passed (234 + 1 new), zero regressions. Full Playwright suite re-run once on freshly reseeded data, including the fix for the `pelion-v2.spec.ts` collision: 16 passed. Manually verified in a real browser: tile/list choice survives a full page reload (proving server-side persistence, not just component state); nav rail collapses to icons and back, and that state also survives a reload; the "Create" PAT subsection collapses to just its title; switching the theme to dark re-colours the nav rail along with the rest of the page.

### Follow-up: hamburger button, wasted width, and section-label-as-divider

Immediate feedback after the above landed: the header's hamburger (☰) "did nothing," `PreferencesPage` wasted most of the screen (a hardcoded `maxWidth: 480` on its root `<div>` — pre-existing, not introduced by the work above, just never noticed until the cards next to it started using full width elsewhere), and the request to make the collapse "via an icon" with the section labels ("Server Management") "turn to a line" so the icon rows don't visibly jump between expanded/collapsed states.

- **Hamburger did nothing because it never controlled `railCollapsed`** — it only toggled a separate `menuOpen` boolean that hid the whole rail below a 720px media query, which has no effect at desktop widths. Rather than run two overlapping collapse mechanisms, `menuOpen` was removed outright and the hamburger now toggles the same `railCollapsed` preference as the chevron at the foot of the rail — one collapse behaviour, two equivalent triggers, both meaningful at any screen width.
- **`PreferencesPage`'s `maxWidth: 480` removed** — every other admin-style page (`OrgAdminPage`, `ProjectAdminPage`, `RequirementsPage`) uses the full container width with no cap; Preferences now matches that convention instead of being a one-off exception.
- **Collapsed section labels now render as a thin divider line** (`text-indent: -9999px` off-screen + a 1px `background: var(--color-border)` bar) instead of `display: none`, so collapsing keeps a comparable amount of vertical space where "Project"/"Global"/"Server Management" used to be, rather than the icon rows abruptly shifting upward. The bottom chevron toggle lost its text label to become purely iconic, consistent with the header icon buttons (Preferences/Sign out) that were already icon-only with a `title` tooltip.

**Verified for real**: `npm run build`/`lint` clean; full backend suite (235 passed) and full Playwright suite (16 passed) re-run on a freshly reseeded stack — two failures seen mid-session (`workflow-bypass-attempts.spec.ts`, `pelion-v2.spec.ts`'s favourite-button locator) turned out to be the already-documented test-data-pollution artifact from repeated manual/automated runs against the same live stack without an intervening reseed, not real regressions — confirmed clean on this final from-scratch cycle. Manually re-verified in-browser: hamburger and foot-of-rail chevron both collapse/expand the same rail; Preferences cards now fill the available width; collapsed section labels render as visible divider lines.

## Bug: OrgAdminPage hung forever for a server admin (or anyone) with no role in that org

The user reported a specific org admin URL stuck on "Loading…" forever. Root cause: `OrgAdminPage.reload()` bundles five API calls (`org`, `groups`, `resources`, `projects`, `report-templates`) in one `Promise.all` with no error handling at all, called from a `useEffect` with no `.catch()`. Every one of `groups`/`resources`/`report-templates` requires at least `member` in that specific organisation; a caller with no role there (a server admin browsing the org list they can always see, or a stale bookmark) gets a 403 on all three, the `Promise.all` rejects, `org` state is never set, and the page's only loading gate (`if (!org) return <Spinner />`) spins indefinitely with nothing but a silent unhandled-rejection in the console.

**First fix**: wrapped the bundle in try/catch, added a `loadError` state, and rendered the actual error message (`ApiError.message`, e.g. "Insufficient organisation permissions.") with a link back to Organisations instead of hanging. This alone would have been a regression for server admins, though: I-M-05's one documented carve-out is that a server admin can create an organisation and its initial user with zero prior role — but the *only* UI for that ("Organisation users → New user") lived inside the very page this fix now blocks outright for exactly that caller.

**Investigating that surfaced a real, separate gap**: the user asked how a server admin is supposed to manage/set up an org's users at all if they can't access it. Tracing the actual authorization rules: `create_org_user` (`POST /orgs/{id}/users`) already allows `require_org_admin_or_server_admin` — creating a *new* native user works fine with zero prior role. But `assign_org_role` (the only way to grant a role to an *existing* user) requires the caller to already be `org_admin` of that org — a chicken-and-egg that meant a server admin had no way to make *themselves* admin of an org they created, only to bootstrap someone else's account. This matters for self-hosting deployments specifically, where the server admin is the only person running the system and wants to use their own org, not just provision customers' orgs (a hosting-company shape the existing carve-out already covered fine).

**New endpoint**: `POST /orgs/{id}/join-as-admin` (`routers/orgs.py`) — server-admin-only, self-targeting only, always grants exactly `org_admin`, 400 if already an admin. Deliberately not a general "server admin can grant any role to any user in any org" capability (see access-control-policy.md's amended item 1) — it only lets the platform's single most-trusted actor take on ordinary membership in one specific org, for themselves, mirroring the one-time `server_admin_create_org` bootstrap behaviour (`services/bootstrap.py`) as a general, repeatable, in-app action instead.

**`OrgAdminPage` degraded view**: when the initial bundle 403s, a server admin now sees the org's name (fetched standalone — `GET /orgs/{id}` already has its own server-admin bypass, which is exactly why this one call would have succeeded inside a bundle that otherwise failed) plus two actions: "Become admin of this organisation" (calls `join-as-admin`, then reloads — succeeds fully since they're now a genuine member) and "Create its first admin user" (a minimal email/name/password form posting to the existing `create_org_user` with `role: "org_admin"`, for the hosting-company case where the server admin bootstraps someone *else's* account and deliberately doesn't become a member themselves). A non-server-admin hitting the same 403 still sees the plain error page — they have no carve-out to offer.

New regression tests in `test_audit_fixes.py`: joining as admin works and immediately unlocks ordinary org-admin actions; joining twice is rejected (400); a non-server-admin (even one who's an admin elsewhere) is rejected (403). Manually verified both flows end-to-end in a real browser: created two fresh orgs, used "Become admin" on one (landed on the full, normal admin page afterward) and "Create its first admin user" on the other (confirmed via a direct login as the newly-created account that it can actually administer that org).

**Verified for real**: full backend suite: 238 passed (235 + 3 new), zero regressions. Full Playwright suite re-run once on freshly reseeded data: 16 passed. `docs/soc2/policies/access-control-policy.md` amended (Authorization item 1) to document both server-admin carve-outs precisely, since this is exactly the kind of authorization-surface change CLAUDE.md requires flagging.

## Organisation disable and hard delete

The user asked for a way to handle an organisation that's stopped paying for hosting or simply needs to be archived — offered the choice of a reversible disable, an irreversible hard delete, or both; the user chose both, in the same pass.

- **Disable is a pure access gate, not a data-affecting operation.** `Organization.is_active`/`disabled_at`/`disabled_by` are the only new columns; nothing else changes when an org is disabled. A new `_require_org_active(db, organization_id)` helper in `services/rbac.py` is called — after PAT-scope checks, before the role check — from every org/project-scoped dependency factory (`require_org_role`, `require_org_admin_or_server_admin`, `require_project_role`, `require_project_view`, `require_project_manage`), raising 403 "This organisation has been disabled." **This deliberately blocks the org's own admins too**, not just outsiders — a disable that the org's own admin could route around wouldn't be a usable "stop this org" control at all. It is deliberately *not* applied to `require_server_admin`, `join_organization_as_admin`, or the disable/enable/delete endpoints themselves, since the server admin must always be able to toggle an org's state regardless of its current state, and login itself is untouched (a disabled org doesn't lock out a user who belongs to other, active orgs).
- **Delete is irreversible and gated behind typing the organisation's exact current name** (`OrganizationDeleteConfirm.confirm_name`, checked server-side in `DELETE /orgs/{id}`, not just a client-side guard) — the same "type the name to confirm" pattern used by GitHub/GitLab repo deletion, appropriate given there is no undo.
- **Cascade strategy: DB-native `ON DELETE` actions on the FK columns, not ORM `cascade=` relationships.** Nearly every cross-reference in this codebase is a plain FK column queried via `select()`, not a declared SQLAlchemy `relationship()` collection, so `ondelete="CASCADE"`/`ondelete="SET NULL"` on the column definitions themselves (added across `models/{organization,project,requirement,change_request,custom_field,notification,audit,engagement}.py`) is the mechanism that actually matches how the code reads data, rather than adding a parallel ORM-level cascade layer that would only fire on ORM-mediated deletes. Audit trail rows use `SET NULL` (not `CASCADE`) on `AuditEvent.organization_id`/`project_id` specifically — the audit *row* survives (its `entity_type`/`entity_id`/`detail` already capture the substance of what happened, and the row itself has ongoing compliance/incident-investigation value), only the FK link to the now-gone org is nulled.
- **Three things a DB cascade cannot clean up, handled explicitly in `services/org_deletion.py::delete_organization_cascade`**: (1) real object-storage bytes — a DB-only cascade would delete `FileAsset` rows but leave orphaned bytes in S3/local storage, which `data-retention-and-disposal-policy.md` already treats as a disposal failure, so every file asset is routed through the existing `delete_file()` (removes bytes *and* row) before the org row itself is deleted; (2) `PersonalAccessToken.allowed_organization_ids`, a JSONB list rather than a real FK — every PAT scoped to the deleted org has that org's id stripped from its list (mirroring the existing `descope_org_pat` logic exactly), auto-revoking the token if the list becomes empty; (3) two genuinely polymorphic tables with no real FK at all (`ReviewComment.target_id`/`target_type`, `Subscription.entity_id`/`entity_type`) — a DB cascade would silently orphan these rather than error, so they're deleted explicitly by id, scoped to the org's requirement/change-request ids, before the cascade runs.
- **Users are never deleted, only their `UserOrgRole` rows** (removed via cascade) — a user who loses their only org membership this way becomes "orphaned," a case the access-review tooling already covers (see the "Orphaned-account review" section above).
- **`ServerOrganisationsPage.tsx`** gained a Status column and per-row Disable/Enable/Delete actions (`window.confirm` for disable, since it's reversible; an inline card requiring the exact name for delete, matching the backend's own check so the button is genuinely disabled — not just hidden — until it matches). **`OrgAdminPage.tsx`**'s existing degraded-view logic (from the section above) gained a third branch: when the 403 message indicates the org is disabled, a server admin sees the org's name plus an "Enable this organisation" action instead of the join/create-admin actions, which only make sense for "not a member," not "disabled."
- **Fixed a now-stale assertion while doing this**: `server-admin-scope.spec.ts` previously asserted a server admin's degraded view showed *zero* instances of the org's name — true before the join-as-admin degraded view existed, false now that showing the org name is precisely how that view (and this new disabled-org view) lets the admin confirm which org they're looking at before acting. Updated to assert the name renders as a heading while `Users`/other content still does not.

**Verified for real**: 10 new backend tests (`test_org_lifecycle.py`), including one, `test_delete_cascades_through_everything_including_non_fk_references`, that creates one of literally everything an org can own (project, requirement, comment+reaction, subscription, change request, an uploaded file with real storage bytes, a solo-scoped PAT, a second two-org PAT) and asserts each category is actually gone — not merely orphaned — after deletion, while confirming the surviving member's user row, the two-org PAT (now scoped to just the remaining org), and the audit trail (rows survive, `organization_id` now null, plus exactly one new `action="deleted"` event) all behave exactly as designed. Full backend suite green, zero regressions. New `server-org-lifecycle.spec.ts` Playwright spec drives the full create → disable → re-enable → delete-with-confirmation flow through the real UI; full Playwright suite re-run clean on freshly reseeded data. Manually re-verified the same flow once more directly in a browser against the live dev stack (create "Manual Test Org," disable, confirm "Disabled" badge, enable, confirm "Active" badge, delete with a wrong name first to confirm the button stays disabled, then the exact name, confirm the row disappears).

`docs/soc2/policies/data-retention-and-disposal-policy.md`'s Known Gaps item 1 updated to reflect that hard deletion is no longer an unbuilt gap for organisations specifically (still true for individual users/projects in isolation, which remain deactivation/archival-only).

### Hardening-pass follow-up

Per CLAUDE.md's requirement to run the identify → verify → remediate review for security-sensitive changes, a dedicated hardening pass was run against this feature after it shipped: two parallel candidate-identification sub-agents (backend RBAC/cascade-delete logic; frontend/API surface), then a fresh, independent verification sub-agent per candidate finding — the same process used by this codebase's earlier hardening passes (see "Security hardening pass" above). All five candidates were confirmed real; none were false positives.

**Fixed:**

- **WebSocket connections fully bypassed the org-disable gate** (`routers/ws.py`). `project_updates` authorizes inline (its own `UserProjectRole`/`ProjectGroupMember` query) rather than through one of the five `require_*` dependency factories, so it was never wired to `_require_org_active` at all — not just the documented "already-open socket keeps streaming" exception, but a full bypass: a member could open a **brand-new** connection to a disabled org's project and stream indefinitely. Fixed by resolving the project's organisation id at handshake and rejecting (code 4403) if it's disabled, and by extending the existing periodic re-check (`_user_session_still_valid`, already used for `token_version`/`is_active` revocation) to also re-check `Organization.is_active`, so an already-open connection now closes within one `_EXPIRY_CHECK_INTERVAL_SECONDS` interval too — matching the account-deactivation guarantee this same function already provided. A new boolean-returning `rbac.is_org_active` was added alongside the existing exception-raising `_require_org_active`, since a WebSocket handler can't propagate an `HTTPException` and needs to translate the check into its own close code instead.
- **File downloads fully bypassed the org-disable gate** (`routers/files.py`). `download_file` authorizes via the raw `get_effective_org_roles`/`get_effective_project_roles` helper functions rather than a `require_*` dependency (it needs `?token=` query-param support for `<img src>` use, which those don't offer), so — like the WebSocket case — it was never wired to the check at all. A disabled org's shared-resource files and requirement attachments both remained fully downloadable by any member who still held a role row. Fixed by calling `_require_org_active` explicitly on both code paths, resolving the organisation id via the file's own `organization_id` (org-resource case) or via the new `_project_organization_id` helper (requirement-attachment case), matching the pattern `require_project_view` already uses.
- **Hard-deleting an organisation that had ever had a logo or SSO login-background image set crashed outright, and destroyed other files' storage bytes in the process.** `Organization.logo_file_id`/`login_background_file_id` are self-referential FKs to `file_assets` with no `ondelete` action (an oversight in the original FK-coverage sweep — every *other* org/project-scoped FK got one). `delete_organization_cascade`'s file-deletion loop calls `delete_file()` (which deletes storage bytes before the DB row) on every `FileAsset` the org owns, including its own logo/background asset, with nothing nulling those two columns first — deleting that one `FileAsset` row raised a raw, unhandled `IntegrityError` (no handler exists in `main.py`, so this surfaced as a 500), discovered only via SQLAlchemy's autoflush partway through the loop, by which point every file processed *before* the crash had already had its storage bytes irreversibly deleted while its DB row (and the org's) survived the rolled-back transaction — orphaned metadata pointing at bytes that no longer exist, for an org that wasn't even successfully deleted. Verified via a live repro against the test database before fixing. Fixed with a single targeted Core-level `UPDATE` nulling both columns before the file-deletion loop runs, so the FK reference is gone before any `FileAsset` row is touched — this removes the crash and, since the loop can no longer fail partway through, the storage-data-loss blast radius along with it.
- **`GET /me/reviews/due` leaked requirement metadata (unique code, name, review date) from a disabled organisation indefinitely.** This is the one org-content read in the app with no single `project_id`/`organization_id` path parameter to hang a `require_*` dependency off (it deliberately spans every project the caller has a role in), so it had never been wired to any org-scoping check at all, let alone `_require_org_active`. Fixed by joining `Project`/`Organization` into the existing query and filtering `Organization.is_active`. `get_due_reviews_for_project` (the project-scoped sibling, used by `routers/requirements.py`'s `GET .../reviews/due`) was already covered for free via its `require_project_view` dependency and needed no change.
- **An organisation created with a blank or whitespace-only name defeated `DELETE /orgs/{id}`'s "type the exact name to confirm" safety gate.** `OrganizationCreate.name` had no length or blank validation; for an org named `""`, both the frontend's `deleteConfirmText !== org.name` check and the backend's own `confirm_name != org.name` check are trivially satisfied by an empty string, so the "must not be a stray click" guarantee that gate exists for silently didn't hold for such an org. Fixed with a `field_validator` on `OrganizationCreate.name` that strips surrounding whitespace and rejects a blank result (422), applied only at creation — there is no separate org-rename endpoint. Requires an already-server-admin actor to trigger in the first place (org creation is server-admin-gated), so this was a self-inflicted footgun rather than a privilege-escalation path, but still a genuine defeat of a documented safety control.

**Verified for real**: 7 new backend regression tests (2 in `test_websocket_security.py` covering both the handshake rejection and the periodic-recheck path; 5 in `test_org_lifecycle.py` covering both file-download code paths, the logo/login-background delete-crash fix — reproducing the exact scenario that used to 500 — the reviews-due leak, and blank/whitespace-name rejection including that meaningful surrounding whitespace is still trimmed, not rejected). Full backend suite: 255 passed (248 + 7), zero regressions. Three pre-existing tests in `test_ws_active_recheck.py` updated for `_user_session_still_valid`'s new `organization_id` parameter — no behavioural change for those, since they pass `None`.

## UI/UX audit: dark header chrome, light-theme contrast, responsive nav rail, and a couple of workflow dead ends

A Playwright-driven UI/UX review (enterprise/business-app lens, Power Apps as the named reference) found that the header rendered with the same `.card`/`--color-surface` styling as every other panel — no distinct chrome at all — on top of weak light-theme contrast, a real horizontal-overflow bug below ~860px, and a couple of dead ends in the requirement-creation flow. The user asked to implement the full list; fixes below, roughly in review order.

**Header chrome is theme-independent** (`theme.css`, `Layout.tsx`): a new token set — `--color-header-bg` (`#14161c`), `--color-header-text` (white), `--color-header-text-muted`, `--color-header-hover-bg`, `--color-header-active-bg`, `--color-header-border` — is declared at the top-level `:root`, outside the `[data-theme="light"|"dark"]` blocks, so the top bar stays a solid black bar with bold white text in both themes rather than a themed surface, matching the Power Apps/Dynamics/ServiceNow convention the user asked for. `.app-header` replaces the header's previous `.card` styling; `.app-header .btn`/`.text-muted` need their own override, since the base `.btn` styling (drawn from `--color-surface`/`-border`/`-text`) would otherwise render as a jarring white chip (light theme) or a near-invisible ~1.1:1 ghost (dark theme) against a black bar. Icons (`lucide-react`, `currentColor`) and the wordmark's inline colour follow automatically.

**Light-theme contrast** (`theme.css`): `--color-bg` moved from `#f7f8fa` to `#eef1f5` and `--color-border` from `#d8dce1` to `#c3c9d1`, taking the measured page-background-vs-card-surface contrast up from 1.06:1 and giving card/rail boundaries an actually-visible line; `--shadow-sm` deepened slightly (0.08 → 0.12 alpha) for the same reason. Dark theme was left alone — its accent colours already cleared 4.5:1+.

**Nav rail now auto-collapses below ~860px, closing a real overflow bug**: at a 600px viewport, `document.documentElement.scrollWidth` measured 712px before this fix — the existing 720px breakpoint only ever touched tables/`.grid-metrics`, never the rail's fixed 220px width. `.nav-rail`'s `min-width` moved out of an inline style (previously driven only by the `railCollapsed` JS boolean) into CSS classes (`.nav-rail` / `.nav-rail-icons`), so a new `@media (max-width: 860px)` block can force the rail into its existing icon-only layout regardless of the user's manual expand/collapse preference, reusing the icon-only rules rather than adding a second collapse mechanism. The four pages using an inline `1fr 240px` two-column layout (`RequirementsPage`, `ChangeRequestsPage`, `RequirementDetailPage`, `ChangeRequestDetailPage`) now share one `.side-grid` class, which collapses to one column in the same breakpoint.

**Reviewer assignment replaced a raw-UUID text box with an org-member picker** (`RequirementDetailPage.tsx`): the field previously asked for a "User ID" typed by hand. Checking for a picker pattern to reuse turned up that this is the *only* way the app assigns a user to anything anywhere — the group-membership picker in `ProjectAdminPage.tsx` does the same raw-ID-in-a-text-box thing — so this fix is scoped to the one field actually in the review rather than a wider component extraction. The page now fetches the project's `organization_id`, then `GET /orgs/{id}/users` (the same org-member-directory endpoint `OrgAdminPage` already uses, open to any org role per its own docstring) and renders a `<select>` of "display name (email)" options; if that call fails (e.g. the viewer has no org role), it falls back to the original plain text input rather than breaking the page. If the currently-assigned reviewer isn't in the fetched list (e.g. a deactivated account), an extra option preserves their id with a "no longer a member" hint instead of silently dropping the selection. The read-only review-section badge on the same page now resolves the id to a display name too.

**Requirement-creation dead end on brand-new projects, fixed** (`RequirementsPage.tsx`): a project with no components/categories yet rendered "New Requirement"'s Component/Category `<select>`s with zero options and a permanently-disabled Create button, no explanation offered. The form now checks for that case first and shows a notice with a direct link to Project Admin instead. The two selects also gained visible `<label>`s (previously unnamed in the accessibility tree, unlike every other field on the same form).

**Smaller fixes**: two nav icons that were reused for different destinations (`CalendarClock` for both "Requirements due for review" and "My reviews due"; `ShieldCheck` for both "Organisations" and "Access review" under Server Management) now use distinct icons (`Clock`, `UserCog`), so the collapsed icon-only rail is unambiguous. The Requirements list table's Name column got an explicit width hint (was getting comparable space to Component/Category despite carrying the longest text). `RequirementsPage`/`ChangeRequestsPage` gained the same `{list.length === 0 && <empty-state message>}` pattern already used on Projects/History/My-Reviews, instead of silently rendering an empty table with just headers.

**One flagged finding turned out to be a false positive on closer inspection, and was deliberately not "fixed"**: the review claimed the OIDC client secret field on Org Admin returns the real plaintext value into the DOM. Tracing it end-to-end: `OrgSsoConfigOut` (both the `GET` and `PUT` response schema in `routers/orgs.py`) has no `oidc_client_secret` field at all, the column is encrypted at rest (`EncryptedString`), `main.py`'s validation-error redaction list already covers the field name, and an existing test (`test_oidc_provisioning.py`) already asserts `"oidc_client_secret" not in body`. The frontend field starts blank and only sends a value when the admin types a new one (blank means "leave unchanged"). The review agent almost certainly read back a value it had itself just typed into the field while exploring the form, not something the server returned — a case of the codebase's own instruction to challenge another agent's assumptions rather than implement a "fix" for correct, already-tested behaviour.

**Verified for real**: `tsc -b`/`eslint` clean on every changed file. Frontend container rebuilt and every fix re-checked against the live stack with Playwright: header renders solid black with bold white text in both themes; light-theme card/rail borders are now visibly distinct from the page background; at a 600px viewport `scrollWidth` now equals `innerWidth` (600, was 712) with the rail auto-collapsed to icons; the reviewer `<select>` on a real requirement is populated with the actual org member ("Server Administrator (admin@example.com)") instead of a text box; a freshly-created project with no components/categories shows the dead-end notice and its Project Admin link instead of a disabled form. (One environment quirk hit mid-verification, not a code issue: the Playwright browser's HTTP cache kept serving a stale, differently-hashed CSS/JS bundle from before the frontend container was rebuilt; resolved by forcing a fresh navigation. Worth remembering if a future container rebuild "doesn't seem to apply" in a live Playwright check.)

## UI/UX follow-up: pinned full-height nav rail, content-width preference, and inline component/category creation

Immediate follow-up to the pass above, driven by three specific asks plus two smaller things noticed while reviewing the live app again post-header-fix: remove the header's ☰ button (confirmed pure duplicate of the rail's own chevron toggle — same `railCollapsed` state, no other consumer), make the nav rail full-height and flush against the true left edge instead of a short, inset floating card, and let a project manager/administrator create a missing component/category directly from the "New Requirement" empty-state instead of being sent to Project Admin. Two open questions (rail scroll behaviour; what happens to main content once the rail is flush-left) were put to the user rather than guessed at, given the shell-restructuring extent of the second item — answered: pin the rail while scrolling, and default main content to full-bleed with a **user-facing preference** to opt back into the previous capped width, rather than a fixed developer choice either way.

- **Burger button deleted outright** (`Layout.tsx`) — no replacement; the chevron at the foot of the rail is the sole collapse/expand control now, at every screen width.
- **Nav rail is now `position: fixed`, not sticky-in-flow** (`theme.css`): `top: var(--header-height)` (a new `56px` constant, alongside a matching fixed `height` on `.app-header` so the rail's offset has something stable to key off), `left: 0`, `bottom: 0` — flush against the true viewport edge regardless of scroll position, and full height below the header rather than a short card sized to its own content. Its width moved from an inline style driven only by the `railCollapsed` boolean into two CSS custom properties (`--nav-rail-width: 220px`, `--nav-rail-width-collapsed: 56px`), which are now the single source of truth for both the rail's own width and the offset applied to content next to it.
- **Main content is pushed clear of the now-out-of-flow rail via a matching `margin-left`**, expressed as a general-sibling CSS rule (`.nav-rail ~ .app-content`, and a `.nav-rail-icons` variant) rather than inline JS-computed margin — keeps the width source of truth in one place and transitions smoothly alongside the rail's own collapse animation. The existing 860px "force icon-only regardless of manual state" media query got a matching `~ .app-content` override so the two stay in sync at narrow widths too.
- **Content-width is a new persisted display preference** (`content_boxed` in the existing generic `ui_preferences` bag — same mechanism as `nav_rail_collapsed`, not a new dedicated `User` column), exposed as a "Content width" select in Preferences right next to Theme. Default is full-bleed (`Layout.tsx` renders `<main className="app-content">` with no inner wrapper); switching to "Boxed" adds a `.boxed` class plus a `.content-inner` child capped at the same `1200px` the whole app used everywhere before this pass, so nothing about table/form readability regresses for anyone who explicitly wants the old width back.
- **A pre-existing, previously-invisible bug surfaced immediately during verification**: `App.tsx` had its own `<div className="container" style={{ padding: "1.5rem 0" }}>` wrapping every route's content, independent of `Layout.tsx`. This was harmless before — it happened to cap at the same 1200px the old rail-in-container layout already capped at — but once `.app-content` was made deliberately full-bleed-by-default, this second, un-toggleable 1200px cap silently overrode the new preference entirely (both "Full width" and "Boxed" measured identically at 1200px on a 1600px viewport until this was found and removed). Deleted; `Layout.tsx`'s own `.app-content`/`.content-inner` is now the only place page width is decided.
- **Component/category quick-create moved inline** (`RequirementsPage.tsx`): the empty-state notice added in the previous pass (component/category dead end) now branches on a new `canManageProject` check — `myRoles` (via the existing `useMyProjectRoles` hook) including `project_manager`/`project_administrator`, **or** the viewer being an org admin of the project's organisation, which `myRoles` alone can't see (it's a per-project role list; org-admin-implies-project-manage is a separate rule the backend's `require_project_manage` already enforces). Resolved the same way the reviewer picker in the prior pass did: fetch the project for its `organization_id`, then `GET /orgs/{id}/users`, and check the caller's own entry for the `org_admin` role — reusing an existing endpoint rather than adding a new one. When `canManageProject` is true, the notice grows one small name+prefix form per missing item (component and/or category), mirroring `ProjectAdminPage.tsx`'s own form exactly (same strings, same layout) and posting to the same existing endpoints; a successful create reloads the page's data, which — since `newComponentId`/`newCategoryId` auto-select the first available option — transitions straight into the full "New Requirement" form with the new component/category already selected, no extra step. Stakeholders (who can create requirements but, per the stricter backend gate, not components/categories) still see the original "go to Project Admin" link — never a control that would 403 on click.
- **One item from the prior pass's report corrected rather than acted on**: "Stage progress repeats the stage name" turned out to be a demo-data coincidence, not a real duplication — `s.name` (the stage's own name) and `STAGE_STATUS_LABEL[s.status]` (its lifecycle status badge) are genuinely different fields that only rendered identically because the specific demo stage happened to be named "Scoping" while also *in* "scoping" status. No code change made.

**Verified for real**: `tsc -b`/`eslint` clean across every changed file. Frontend container rebuilt twice (once before the `App.tsx` wrapper was found, once after) and re-checked against the live stack with Playwright: the rail measures `position: fixed`, `left: 0`, `top: 56`, `height: 844` at a 900px-tall viewport (exactly filling the remainder below the header); a 600px-wide viewport still shows zero horizontal overflow with the rail auto-collapsed; the content-width toggle was driven through the real Preferences UI both directions and measured a real `.side-grid` width difference (1332px full vs. 1200px boxed at 1600px wide) only *after* the `App.tsx` fix — before that fix both settings measured identically, which is how the bug was caught rather than assumed away. The inline creation flow was driven end-to-end against a freshly-created, component-and-category-less project as the org-admin account: filled and submitted both mini-forms, watched the notice hand off into the real "New Requirement" form with "Backend Services (SVC)" / "Performance (PERF)" pre-selected. Scratch project archived afterward.

## UI/UX review #3 implemented: nav icon, org/platform branding, activity labels, a Style Manual casing pass, report fixes, tooltips, and a help page

A third review pass raised seven items; three carried a genuine open question each, put to the user rather than guessed at, before implementing all seven. Decisions: (1) the new graphite accent colour becomes the one default everywhere, including the report generator's previously-separate defaults; (2) header branding fallback is resolved by sole-org-membership — a viewer in exactly one organisation sees that org's branding even on pages with no single project context, otherwise the platform default applies; (3) the help page is built in-app, content-managed through plain Markdown files rather than JSX.

**Nav rail expand icon** (`Layout.tsx`): `ChevronLeft`/`ChevronRight` (an ambiguous bare arrow) replaced with `lucide-react`'s `PanelLeftClose`/`PanelLeftOpen` — icons purpose-built for a sidebar-panel toggle.

**Org and platform-wide branding** — a real, two-layer feature, not a token tweak:
- `Organization` gained `accent_color_hex` and `header_title` (both nullable — null means "use the platform default"). A new `ServerSettings` model (`services/branding.py::get_server_settings`, a lazily-created singleton row, no DB-level singleton constraint) holds the platform default `accent_color_hex`, `default_logo_file_id`, `default_header_title`. Pre-release convention (see "Schema migration strategy" above) applied: both land as plain model fields, not a hand-written incremental migration; the already-running test stack's database was patched directly via `ALTER TABLE`/`CREATE TABLE` matching what `create_all()` would generate, rather than reset, to preserve its seeded demo data mid-session.
- **One colour is stored, not two.** `contrast_text_hex` (Python, `services/branding.py`) and its TS mirror (`utils/color.ts`) pick black-or-white button text by relative luminance; `ensureContrast` (TS only) derives a lightened variant for dark theme and a darkened variant for light theme from the *same* stored hex on demand, rather than asking an admin to pick two colours. Platform default: `#475569` (light) / auto-lightened for dark — checked at 7.6:1 / 6.9:1 against white/dark text respectively, both clearing WCAG AA with room to spare. `theme.css`'s own light/dark `--color-primary` fallbacks were updated to match, for the instant before `BrandingContext`'s fetch resolves.
- **`BrandingContext.tsx`** (new) resolves `{ logoFileId, headerTitle, accentColorHex }` and applies the colour live via `document.documentElement.style.setProperty`, re-run whenever the resolved light/dark theme changes (not just when the colour does) so the derived tint stays correct across a theme toggle. Resolution: inside a project's URL space, that project's organisation (falling back field-by-field to the platform default); everywhere else, the viewer's sole organisation if they belong to exactly one, else the platform default — the exact three-way split above. This supersedes `TerminologyContext`'s old `orgLogoFileId` (removed; logo/title/colour are now one concern with one set of resolution rules, terminology overrides are a separate, narrower one).
- **The platform default logo reuses the existing avatar/org-logo access bypass** (`routers/files.py`) rather than requiring `FileAsset.organization_id` to become nullable: a platform-wide asset is still stored against *an* organisation (whichever exists first — cosmetic, storage-key-only) but served to any authenticated user via the same carve-out avatars and org logos already use, extended by one more `ServerSettings.default_logo_file_id` check.
- **UI**: `OrgAdminPage.tsx`'s logo card became a "Branding" card (logo + header title + an "override the platform default" checkbox gating a colour picker); a new `ServerSettingsPage.tsx` (`/server/settings`, server-admin only for writes, its `GET` open to any authenticated user since the app shell needs it for everyone) provides the platform-wide equivalent. `ReportTemplate.accent_color_hex`'s DB default and the plain/unbranded PDF path's hard-coded table-header colour (previously `#2563eb` and `#2d3748`, two more uncoordinated defaults) both moved to the same `#475569`, per the "unify everywhere" decision — a plain report now visually matches the app it came from.

**Activity feed — labels and an entity-type filter**: tracing every renderer of `ChangeEntry` found three genuinely different implementations (the project overview card and history page rendered raw `entity_type`/`action` with no actor; `ActivityPanel`'s sidebar list showed the actor but never the entity type, through an incomplete, mostly-identity `ACTION_LABEL`). Consolidated: `ChangeEntry` now imported from the one definition in `api/types.ts` (was duplicated locally, missing `actor_display_name`, in two of the three files) everywhere; new `ENTITY_TYPE_LABEL`/`activityActionLabel`/`describeActivityEntry` (`api/types.ts`) give one consistent "{badge: entity type} {actor} {action}{detail suffix}" rendering used by all three surfaces. `GET /projects/{id}/changes` gained an `entity_type` query param (`services/changes.py::get_project_changes`, filtered post-merge across the three/four underlying sources rather than pushed into each query — none are large enough per-project for that to matter, and two mint their `entity_type` in Python, not from a column); `ProjectHistoryPage.tsx` got a matching filter dropdown.

**Text casing, against the Australian Government Style Manual's "minimal capitalisation" rule** (sentence case: capitalise only the first word and proper nouns) — traced to one root cause: only `StageStatus` had a display-label map anywhere in the app; every other enum rendered its raw snake_case wire value straight into badges, dropdown options, and (via `services/reports.py`) generated PDF/CSV output. New maps in `api/types.ts` — `REQUIREMENT_STATUS_LABEL`, `REQUIREMENT_LEVEL_LABEL`, `CHANGE_REQUEST_STATUS_LABEL`, `PROJECT_ROLE_LABEL`, `ORG_ROLE_LABEL`, `CUSTOM_FIELD_ENTITY_KIND_LABEL`, `CUSTOM_FIELD_TYPE_LABEL` — applied at every raw-render site found (`RequirementsPage`/`RequirementDetailPage`/`ChangeRequestsPage`/`ChangeRequestDetailPage`/`ProjectAdminPage`/`OrgAdminPage`/`ProjectListPage`/`ReportsPage`'s status dropdown), plus a Python-side `services/labels.py::requirement_status_label` for the two report formats. A handful of hand-typed Title Case strings were fixed alongside (`nav.changeRequests`, `nav.admin`, `orgAdmin.backToOrganizations`, the requirements table's "Target · Level"/"Component · Category" headers). The bigger, non-obvious fix was `TerminologyContext.tsx`: its resolved terms (`useTerm`) are substituted mid-phrase ("New requirement") as often as used standalone (a heading, a nav label) — `DEFAULT_TERMS` moved to lower case and `useTermPlural` now capitalises itself, since *every* current plural call site is a standalone heading/nav label and *no* singular call site is, which resolved both without needing a second hook variant. One casing bug was caught only during live verification, not by the static sweep: the project overview's "Requirements by status" donut built its legend from `Object.entries(metrics.requirements_by_status)` directly, bypassing every badge-based fix — the one `Object.entries`/`Object.keys` render site that fed raw keys straight to a chart instead of a `<span className="badge">`.

**Report output**: the PDF table's ID column (e.g. `AUTH-SEC-001`) was wrapping mid-code at 2.2cm; widened to 3cm, taken from the Reasoning column (the widest, least space-constrained), so the table's total width is unchanged. Downloaded filenames now match what the backend already intended: the CSV endpoint gained the same `{project name}-requirements.csv` `Content-Disposition` the PDF endpoint already sent (a new `_filename_safe()` strips characters unsafe in a header), and the frontend's blob-download `<a download>` attribute — which wins over the header in most browsers — now builds the same project-qualified name instead of a hard-coded `requirements.pdf`/`.csv`. `ReportTemplate`'s four configurable fields beyond `name` (accent colour, cover page, include-logo, footer text) were fully wired into the API already but had zero UI beyond the name-only create form; `OrgAdminPage.tsx`'s create form now sends and exposes all four.

**Tooltips and a help page**: new `components/Tooltip.tsx` — hover/focus-triggered, no delay, replacing reliance on the native `title` attribute (slow, OS-dependent, absent on touch) for icon-only controls. Applied across `Layout.tsx` (every nav-rail link plus the header's notification/preferences/sign-out buttons and the collapse toggle), `NotificationBell.tsx`, and paired with `aria-label` (previously missing on most of these — the project list's favourite-star button was the sole existing example of the correct `title`+`aria-label` pairing) on the same controls plus `RequirementsPage.tsx`'s move-up/down buttons and `OrgAdminPage.tsx`'s display-name lock toggle. A new `/help` route (`HelpPage.tsx`, linked from the nav rail's Global section) renders five Markdown files (`frontend/src/help/*.md`: how the app is organised, the requirement lifecycle, change requests, roles, reports) through a new, deliberately minimal `utils/markdown.ts` renderer — headings/paragraphs/bullet-lists/bold/italic/code/links, mirroring the exact subset `services/reports.py`'s own Markdown-to-flowable renderer already supports, chosen over adding a Markdown dependency to keep this frontend's very small dependency footprint (four runtime packages before this) unchanged. Updating the help content going forward is editing or adding a `.md` file — no JSX or build-step changes needed beyond one import line and one `SECTIONS` entry for a genuinely new file.

**Help moved to the header, diagrams, screen-aware tooltips (real fix, not the first attempt), Markdown-or-WYSIWYG report content with org defaults, and a CSV import column-mapping wizard** — four separate user requests in one round:

- **Help in the header, with diagrams**: the `/help` link moved from the nav rail's Global section into the header's icon cluster (`Layout.tsx`, next to Preferences) — a global "how does this app work" reference belongs with the other account-level chrome, not competing with per-project navigation. `utils/markdown.ts`'s renderer gained fenced-code-block support, with a special case for ` ```mermaid ` blocks (rendered as `<div class="mermaid">{raw content}</div>`, unescaped, since Mermaid parses its own source; any other language renders as an escaped `<pre><code>`). `HelpPage.tsx` now calls `mermaid.run()` on mount, themed light/dark off `data-theme`. Two new diagrams (`02-requirement-lifecycle.md`, `03-change-requests.md`) show the full requirement and change-request state machines, each with the explanatory paragraph immediately before it that CLAUDE.md's documentation rules require. `mermaid` is a new runtime dependency — justified because the user named it directly ("eg mermaid diagrams"), unlike the custom-built renderer/editor work below.
- **Tooltip screen-awareness — the clipping was never a placement-math bug.** The first fix in this round only corrected *where* the bubble's CSS classes pointed (centre/left/right, above/below), computed once on mount and re-measured only on `window.resize`. Live-verifying it against the actual bug report (collapsed nav rail) showed it still failed: collapsing the rail is a layout change that fires no `resize` event at all, so the placement went stale the moment the rail collapsed after page load — and worse, once wrong, a subsequent resize would re-measure the bubble's *already-shifted* position rather than its true default, compounding the drift. Root cause once that was fixed and it *still* clipped: the nav rail's `overflow-y: auto` implicitly computes `overflow-x: auto` too (a CSS Overflow spec rule — you cannot have one axis clip and the other stay `visible`), so any absolutely-positioned descendant extending past the rail's collapsed ~52px width was clipped by the rail itself, no matter how correct its left/right math was. Fixed properly: `Tooltip.tsx` now renders the bubble through `createPortal(..., document.body)` as a viewport-`fixed` element with plain pixel `top`/`left` computed from the trigger's `getBoundingClientRect()` plus the bubble's own intrinsic (placement-independent) width/height, re-measured on every show (not just on mount/resize) — this escapes *any* scrollable/clipping ancestor generically, not just the nav rail one bug report happened to name. Recorded here in detail because it's a two-layer bug (stale measurement, then ancestor clipping) that a plausible-looking first fix did not catch — only live verification with Playwright, not `tsc`/`eslint`, surfaced it.
- **Report intro/chapters/appendices: Markdown or WYSIWYG, saved per project, with organisation defaults.** New `components/RichTextEditor.tsx` — a Markdown/Rich-text mode toggle over either a plain `<textarea>` or a `contentEditable` div driven by `document.execCommand` (deliberately not a TipTap/Slate/Quill dependency, matching this codebase's minimal-dependency philosophy elsewhere). Switching *into* rich mode renders the current Markdown via `renderMarkdown()`; switching *out* serializes the DOM back via a new `htmlToMarkdown()` (`utils/markdown.ts`) — imperative, one-way sync in each direction, never a controlled re-sync from the `value` prop while the user is actively typing, which is what avoids the classic contentEditable caret-reset bug. Backend: `Organization` gained `default_report_intro`/`default_report_chapters`/`default_report_appendices` (same pre-release "plain model fields, `ALTER TABLE` the running test DB directly" convention as the branding fields above); `services/reports.py::resolve_report_config` resolves each of the three fields independently via `project.field or org.default_field or ""` — the same truthy-OR-fallback shape as branding, chosen for the same reason (avoids a nullable-column migration on `Project`, at the deliberate cost of not distinguishing "genuinely blank" from "not customised"). New `GET`/`PUT /orgs/{id}/report-defaults` (org-admin gated, same pattern as existing report-templates endpoints — no SOC 2 policy implications, since nothing here touches auth/RBAC/secrets/multi-tenant isolation). `ProjectAdminPage.tsx`'s Report Setup tab now shows "(organisation default)" wherever a field is falling back, using three new `*_is_organisation_default` response flags; the chapter-list editor (title + move-up/down/delete + body) was extracted from `ProjectAdminPage.tsx` into a shared `components/ReportChapterListEditor.tsx` so `OrgAdminPage.tsx`'s new "Report Defaults" card could reuse it rather than duplicate it.
- **CSV import: verified working, then given column mapping and a requirements legend.** The import endpoint (`POST /projects/{id}/requirements/import`, `routers/requirements.py`) already had real test coverage (`test_requirement_lifecycle.py::test_import_creates_valid_rows_and_reports_errors_for_invalid_ones`) and was re-verified live via Playwright end-to-end this round — it was not previously broken. What was missing was answers to the user's other two questions: the file had to already use the backend's exact column names, and nothing in the UI said what those names or which fields were required. New `components/CsvImportWizard.tsx` parses the chosen file client-side with Papaparse (a new runtime dependency — chosen over hand-rolling RFC4180 parsing, judged too risky for a feature whose whole point is data-integrity-sensitive field mapping), auto-guesses a mapping from the file's own headers to the six canonical fields by exact/normalised name match, and shows an editable mapping table (with a `*` marker and one-line explanation per field) plus a live preview of the first five rows under the current mapping — the "Map Name, Component, Category before importing" line and a disabled Import button block submission until every required field is mapped. On confirm, the file is remapped client-side into a canonically-headed CSV (via `Papa.unparse`, keyed by field name into an object rather than a positional array — the first version of the template-download builder used a positional array and silently shifted `reasoning`/`component_prefix`/`category_prefix` by one column relative to its own header row; caught only by downloading and diffing the actual bytes in Playwright, not by `tsc`) and posted through the existing, unchanged import endpoint. A "Download template" button generates a one-row example CSV pre-filled with the current project's own first component/category prefix and stage name, so a new user has a concrete, valid starting point rather than a blank header row.

Verified for real, not just by `tsc`/`eslint`: full backend pytest suite (256 passed after adding `test_project_falls_back_to_organisation_report_defaults` and updating one pre-existing assertion in `test_reports.py` for the new response fields) against the already-running `tests/container` stack, patched live via `ALTER TABLE` for the two new `Organization` columns; both frontend containers rebuilt and Playwright-driven through every surface touched — the Help link's new header location and both Mermaid diagrams actually rendering; the tooltip fix specifically re-tested against the exact reported scenario (collapsed nav rail) after the first fix attempt was shown to still fail live; the Markdown↔rich-text round trip on a real edit; the org-default fallback appearing on a second project that had never set its own report content, then getting overridden once that project set its own; and the CSV wizard's full mapping-to-import path with a deliberately mismatched-header file, ending in a real "2 created, 0 error(s)" and the new requirements appearing in the list.

**Verified for real**: full backend suite — 255 passed, zero regressions — run inside the actual test stack container (`python3 -m pytest`; the bare `pytest` script doesn't get the cwd-on-`sys.path` behaviour `-m` invocation does, which is what made `import app.models` fail at first). Per the documented "pytest wipes the shared test database" gotcha, the stack was restarted and reseeded afterward with `scripts/seed_demo_data.py`'s realistic multi-project dataset rather than the minimal manual fixtures, which turned out to matter: it's what surfaced the donut-chart casing miss above, real actor names in the activity feed, and a real `ReportTemplate` to generate an actually-branded PDF from. `tsc -b`/`eslint`/`vite build` clean throughout. Live-verified with Playwright across three accounts (a fresh single-org admin, a multi-project org admin, the platform server admin): the graphite accent colour rendering on buttons/active-nav-state/links in place of the old blue; the org Branding card and the new platform Branding page both saving and reflecting correctly; a real branded PDF generated via the API showing "Approved"/"Completed" (not raw lowercase), every ID on one line, and the project-qualified filename in its `Content-Disposition` header; the activity feed showing real names ("Jordan Alvarez created", "Sam Whitfield voted") with properly-labelled entity badges; the help page's five sections rendering with correct heading/list/bold formatting; and a tooltip bubble appearing immediately on hovering a collapsed nav-rail icon.

**A batch of thirteen nav/IA and admin-page requests in one round** — the largest single-turn UI change this project has had, so recorded item by item:

- **Tooltips only while the nav rail is collapsed.** `Layout.tsx`'s per-row rendering was pulled into a new `NavRailLink` that only wraps the link in `<Tooltip>` when `railCollapsed` is true; expanded, the link's own `nav-label` text already says the same thing a tooltip would, so wrapping it there was pure noise.
- **Server Management consolidated, section renamed.** "Access review" (`ServerAccessReviewPage.tsx`) and "Platform branding" (`ServerSettingsPage.tsx`) — two standalone nav entries — were deleted and merged into one new tabbed `ServerManagementPage.tsx` (`/server/management`, replacing both old routes) with the exact two-tab layout `ProjectAdminPage.tsx` already established. The nav-rail section they lived in, previously "Server management", is now "Administration", alongside the unchanged `/server/organisations` link (its own row-per-org table with create/disable/delete doesn't fit a tab).
- **"Organisations" (`/orgs`, `OrgListPage.tsx`) → "Org Management", with a single-org skip.** Belonging to exactly one organisation no longer shows a one-item list to click through — `orgs.length === 1` now renders `<Navigate to={/orgs/{id}/admin} replace />` directly. Its own nav-rail entry (Global section) was removed entirely rather than renamed: for a server admin it was a strict subset of the Administration section's `/server/organisations` list (same `GET /orgs` call, confirmed by that page's own docstring), and non-admin reachability was preserved differently — see the Preferences "Your access" tab below, which links to `/orgs/{id}/admin` directly per org rather than through this intermediate list.
- **OrgAdminPage.tsx sections collapsed by default, Users first.** Every top-level card became `<CollapsibleSection defaultCollapsed>` (the component already existed, used elsewhere on `PreferencesPage.tsx`, just not here) and the "Organisation users" section was reordered to the top of the page — it's the section an admin reaches for most.
- **Notification popup**: `NotificationBell.tsx` now closes on every route change (`useEffect` keyed on `location.pathname`, since the header bell is rendered outside the routed content and nothing was ever telling it to close) rather than staying open until manually toggled. The empty-state's apparent excess padding was really "Mark all read" showing with nothing to mark plus the same spacing as a populated list" — the button now only renders when `unreadCount > 0`, and the empty state's own padding/gap is tightened specifically for that case.
- **Org filter on the Projects page, "Organisations" removed from Global nav.** New `organization_id` query param on `GET /projects` (`routers/projects.py::list_projects`) — done server-side rather than client-side filtering the already-paginated list, since client-side filtering after pagination would silently show fewer results than actually match. `ProjectListPage.tsx` gained a matching `<select>` in its filter row, shown only when `orgs.length > 1` (same gating the existing new-project org picker already uses).
- **Nav-rail collapse toggle moved to top-right.** Was a full-width bar at the foot of the rail; now a plain `.btn` icon button in its own right-aligned row at the very top, above the section links — reachable without scrolling past a long project section first. The existing `PanelLeftOpen`/`PanelLeftClose` icon-per-state logic was already semantically correct on inspection (open-icon shown while collapsed, close-icon while expanded) and re-verified visually via Playwright screenshot at both states rather than changed blind.
- **Preferences page: tabbed, plus a new "Your access" tab.** The four existing top-level `CollapsibleSection`s (Profile, Security, Personal Access Tokens, Notification preferences) became tabs in the same button-row pattern as `ProjectAdminPage.tsx`, keeping their *nested* `CollapsibleSection`s (Change password, Two-factor, PAT creation) as-is — those are genuinely subsections, not separate concerns. The new "Your access" tab lists every org the user belongs to with their role badges there, a "Manage organisation" link (shown only when `org_admin` is among those roles) straight to `/orgs/{id}/admin`, and that org's projects with per-project roles — resolved by calling `GET /orgs/{id}/users` once per org and finding the caller's own entry (no endpoint returns "my role in every org I'm in" directly; fine in practice since a user belongs to a handful of orgs at most, not hundreds) plus the project list's existing `my_roles`/`organization_id` fields, no new backend endpoint needed.
- **Header: no more separate Settings icon.** The `<Settings>` icon-button was deleted; the username text itself is now the link to `/preferences`, with a `title`+`aria-label` pair (the Tooltip-only label would have collided with e2e specs' pre-existing `getByTitle("Preferences")` locators — see below).
- **Nav-rail links are full-width, not shrink-wrapped.** `.nav-link` gained `width: 100%; box-sizing: border-box`, and a new `.tooltip-trigger` class (Tooltip's own wrapper span, previously an unclassed inline style) gets `.nav-rail .tooltip-trigger { display: flex; width: 100% }` — both the click target and the `.active` background now span the rail's full width in both collapsed and expanded states, not just the icon+text's own content width.
- **A Favourites nav entry and page**, shown only when the user actually has a favourited active project (`Layout.tsx` checks this via a small `GET /projects?archived=false` on mount and again on arrival at `/projects` or `/favourites` specifically — not on every navigation, which would otherwise fire that same request on every route change for the entire session). New `FavouritesPage.tsx` (`/favourites`) reuses the same tile-card rendering as the projects list, filtered to `is_favorite`.
- **Terminology split into its own `ProjectAdminPage.tsx` tab**, out of "Project settings" — `saveSettings` split into itself (name/summary/flags) and a new `saveTerminology`, each with its own button, since they're saved independently now.
- **Project groups tab shows actual members.** `member_user_ids` was always just a list of UUIDs with a bare count; the tab now fetches the org's user directory once (`GET /orgs/{id}/users`, same "any org role can call this unfiltered" endpoint as the Preferences access tab) to resolve each id to an email/display name, lists them with a remove button (wired to the existing, previously-unused-from-the-UI `DELETE .../groups/{id}/members/{id}`), and replaced the raw-UUID "User ID" text input with a `<select>` of the org's users excluding whoever's already a member — matching this codebase's established "replace raw UUID input with a real picker" precedent from an earlier round's reviewer-picker fix.

**Two real bugs found only by running the actual existing Playwright suite, not just live-clicking the changed surfaces**: (1) several `e2e-workflows` specs and `pelion-v2.spec.ts`/`golden-path.spec.ts` located "Change Requests"/"Project Admin"/"submitted"/"approved" via `exact: true` locators against Title-Case/lowercase text that the earlier text-casing-pass round (see above) had already changed to sentence case ("Change requests", "Project admin", "Submitted", "Approved") — a latent gap from that round that this round's fixes to the underlying markup didn't cause but did make newly reachable; fixed by matching the specs' locators to the current casing rather than reverting the app's casing. (2) `getByTitle("Preferences")`/`getByTitle("Notifications")` in `pelion-v2.spec.ts`/`golden-path.spec.ts` depended on a native `title` attribute that the `Tooltip.tsx` rollout (a *previous* round this session) had quietly stopped setting on those two controls specifically — restored (`title={label}` alongside `aria-label`, per `Tooltip.tsx`'s own docstring suggestion) rather than changing the tests, since a redundant `title` is harmless and other Tooltip-wrapped controls elsewhere were never relied on this way.

**Verified for real**: backend `python3 -m pytest` — 256 passed — after adding the `organization_id` project-list filter; `tsc -b`/`eslint` clean across every touched file. Both containers rebuilt; every one of the thirteen items live-verified via Playwright screenshots/DOM inspection (not just accessibility-tree snapshots) as a demo org admin, a platform server admin, and a fresh single-org account — including confirming a tooltip actually renders (opacity/visibility inspected directly on the portaled DOM node, since a screenshot taken immediately after a synthetic hover can miss it) and that the org-role-scoped "Manage organisation" link and group-member add/remove round-trip against real data. The full existing Playwright suite (17 spec files) was then run repeatedly to catch regressions from the nav restructuring — surfacing the two pre-existing gaps above, both fixed — plus a design smell of its own: the Favourites-nav-link check originally re-ran on *every* navigation; tightened to mount-time plus arrival at `/projects`/`/favourites` only, the two places a favourite can actually change. Later full-suite runs against the same long-lived (10+ hour) container session intermittently showed 10+ unrelated failures, traced to environmental exhaustion (a container restart lost the E2E dataset entirely; personas 401'd) rather than any code issue — resolved by reseeding and confirmed with a final clean 17/17 pass.

## A 28-item batch across security, notifications, admin, and the component/category data model

The largest single-turn request this project has had — a full backlog dump rather than a themed review — worked straight through in one pass (the user's explicit choice, over item-by-item confirmation, given the scale) with check-ins reserved for genuinely security-sensitive or ambiguous decisions. Four such questions came up and were put to the user rather than guessed at; each is called out below where it lands. The CLAUDE.md instructions were themselves extended first, at the user's request, with a `## Testing Requirements` section (every UI feature needs a Playwright test; every backend change needs a test pinning it to the request) and a `## Playwright MCP Usage` section (prefer the spec suite; reserve the live-browser MCP tools for targeted one-off investigation) — both binding on this round's own work and cited below where they shaped the approach.

**Org-wide two-factor requirement.** The request's own wording was truncated mid-sentence ("An org a..."); resolved via clarifying question as "org admins can require 2FA org-wide," not a platform-wide switch. `Organization.require_2fa` (new boolean, default `False`) is enforced in `services/rbac.py::_require_org_2fa`, checked alongside the existing `_require_org_active` at all five RBAC dependency call sites (`require_org_role`, `require_org_admin_or_server_admin`, `require_project_role`, `require_project_view`, `require_project_manage`) rather than at login — a user who hasn't enabled 2FA yet can still authenticate and see enough of the app to go turn it on, but every org-scoped action inside a 2FA-required org is blocked until they do. `PreferencesPage.tsx` exposes the org's own toggle as a `ToggleSwitch` (new pill-switch component), not a button, per the same clarifying answer.

**Notifications became a first-class surface, not just a dropdown.** New `NotificationsPage.tsx` (`/notifications`, linked from the nav rail's Global section) gives a searchable, paginated history of everything the bell only ever showed a truncated recent slice of. The bell's own dropdown gained a "Mark all read" button (only rendered when there's an unread count to clear, matching the empty-state padding fix from an earlier round) that clears every active notification in one call rather than requiring one click per item.

**Requirement/change-request status badges became click-to-filter.** New `FilterBadge` component (a `.badge`-styled `<button>` with an `active` prop for visual affordance) replaces plain status badges on `RequirementsPage.tsx` (status) and `ChangeRequestsPage.tsx` (status and target stage); clicking a badge sets that filter, clicking the same one again clears it — the same list-filtering state that already existed via dropdowns, now reachable directly from the data itself. `ServerOrganisationsPage.tsx` and `ProjectListPage.tsx` got the same treatment for an Active/Disabled organisation filter (see below).

**Personal access tokens gained project scoping.** `PersonalAccessToken.allowed_project_ids` (JSONB list, default empty = "no extra restriction beyond org scope" — an empty list was chosen over a nullable column so "not yet scoped" and "explicitly scoped to nothing" can't be confused) is validated at creation (`routers/pats.py::create_pat`) against the projects the *creating user* can actually reach, and enforced per-request via new `services/rbac.py::check_pat_project_scope`. A new `GET /pats/max-lifetime` endpoint feeds `PreferencesPage.tsx`'s expiry picker so it can auto-fill and warn ("expiry too long") against whatever ceiling the server actually enforces, instead of a value hard-coded on the frontend that could silently drift from the backend's real limit. The creation form's project list uses a checkbox list, not the new `UserAutocomplete` (that component is for picking one person from a directory; a PAT's scope is a multi-select over a much shorter, already-visible list).

**Banning a user — scoped to orphaned and not-yet-onboarded accounts, not active members.** Clarifying question resolved this as: server admins can ban an orphaned user (no org memberships) or block a known-bad email from ever being added to an org, not remove someone from an org they're actively in (that's what removing their org role already does). `User.is_banned`/`banned_at`/`banned_by` (self-referential FK) back new `ban_orphaned_user`/`unban_orphaned_user` endpoints (`routers/system.py`); `assign_org_role` now rejects a banned target outright, which is the actual enforcement point — a ban has no effect until someone tries to (re-)add that user to an org.

**Access Review renamed to "Users," with an organisation-visibility toggle.** Showing which organisations an orphaned/bannable user belongs to is genuinely useful triage context, but `SystemUserOut`'s own docstring documented org-name omission from this endpoint as a deliberate confidentiality boundary (I-M-05) — flagged explicitly rather than silently reversed. Clarifying question resolved it as a platform environment variable, defaulting to showing real names ("server admins can always look in the database anyway"): new `access_review_show_org_names` setting (`config.py`, default `True`) gates whether `SystemUserOut.organization_names` is populated or withheld, with the corresponding SOC 2 confidentiality-boundary note left in place rather than deleted, since the gap (a false setting still exposes `organization_count`) is now a documented, deliberate trade-off rather than an oversight.

**Component/category became a real two-level tree, not two independent flat lists.** Previously a requirement's category was chosen independently of its component (both project-wide siblings); the request's own example (core as the component, general as the category, from `docs/requirements.md`) made the intended shape unambiguous. `ProjectCategory.component_id` (new FK to `project_components.id`, `ondelete=CASCADE`) moves the uniqueness constraint from `(project_id, prefix)` to `(component_id, prefix)`. Migration policy — since existing multi-component categories are ambiguous about which component they "really" belonged to — was put to the user rather than guessed: full reorganisation, no backward-compatibility shim, confirmed against this project's own pre-release convention (`alembic/versions/0001_initial.py`'s docstring: one squashed baseline migration, no real deployments yet to preserve an incremental path for) that this only needed applying prospectively to `seed_demo_data.py`/`seed_e2e_dataset.py` (both reworked to build nested `dict[component_key][category_key]` lookups) plus a schema reset, not a generic historical backfill script. Every consumer of the old flat shape was touched: requirement create/update now reject a category that doesn't belong to the selected component (400, not a constraint-violation 500); CSV import's prefix resolution moved from `{prefix: category}` to `{(component_id, prefix): category}`; `move_category`'s reorder helper (`_move_ordered`) took an explicit `scope_conditions` list parameter instead of a hardcoded `project_id` column so it could be re-scoped to `component_id`; project-template cloning's category loop was fixed to remap `component_id` through the clone's own id-mapping table (previously copied the source project's raw id, a latent bug this migration made newly visible); `ProjectAdminPage.tsx`'s Categories tab was rebuilt as a nested tree (each component rendering its own categories and its own inline "add category" form) rather than two side-by-side flat lists — which shifted three Playwright specs' `.getByPlaceholder("Name").nth(1)` locators (written against the old flat layout) to `.first()`, since the newly-created component's own nested add-category form now renders before the page's bottom-level add-component form. A new Mermaid diagram in `frontend/src/help/01-overview.md` shows the tree shape, with the explanatory context CLAUDE.md's documentation rules require immediately alongside it.

**Org admins can now see and manage every project in their org, including ones they have no direct role on.** New `GET /orgs/{id}/projects` (`list_org_projects`, org-admin-gated) returns every project regardless of the caller's project-level role, deliberately excluding requirement/change-request content (`OrgProjectSummaryOut` carries only id/name/is_archived) — an org admin's reach is administrative, not a silent grant of content access. New `services/rbac.py::require_project_view_or_manage` lets an org admin's `can_manage_project_settings` substitute for a real project role at the handful of endpoints that need it. `OrgAdminPage.tsx` surfaces this as a new "Projects" section.

**Project list and favourites now show which organisation a project belongs to and how many requirements it has.** `list_projects` (`routers/projects.py`) joins `Organization` (filtering `is_active`) and batch-resolves a requirement count per project rather than N+1 querying; `ProjectListPage.tsx`/`FavouritesPage.tsx` render both plus a long-standing favourite-button text-overflow bug fixed alongside (ellipsis instead of wrapping/clipping mid-character).

**Disabled organisations are now hidden by default**, on both the org picker (`ProjectListPage.tsx`, so a new project can't accidentally be created under a disabled org) and `ServerOrganisationsPage.tsx`'s own list (via the new `FilterBadge` Active/Disabled toggle) — previously every org, active or not, appeared everywhere. This is what broke `server-org-lifecycle.spec.ts`, which disables an org and immediately asserts its own row shows "Disabled": fixed by clicking the new "All" filter once at the top of that test, since it deliberately watches a full lifecycle including the disabled state.

**A stale-token session now ends immediately instead of spinning or silently failing.** `api/client.ts`'s `rawRequest` already received a `WWW-Authenticate: Bearer` header on token-invalidating 401s (from `deps.py`'s shared `_UNAUTHORIZED` exception instance) but did nothing with it, leaving the UI in whatever state it was in — indistinguishable from an ordinary business-logic 401 (wrong password, bad 2FA code) until the user tried something else. It now dispatches a new `AUTH_UNAUTHORIZED_EVENT` only when that header is present *and* a token was actually sent, which `AuthContext.tsx` listens for and responds to by clearing the token/user immediately, routing to `/login`.

**Two real display bugs, found while implementing unrelated items:** the Project Overview page's three independent fetches had no `.catch()` — a single failed request (e.g. a project with no reviews yet) left the page permanently spinning rather than rendering the other two panels with an error notice for the one that failed; fixed with per-fetch error state. `.btn-primary:hover` was inheriting the plain button's washed-out hover background rather than a shade of its own accent colour, invisible on the graphite theme in particular — fixed with a new `--color-primary-hover` token (`utils/color.ts::hoverShade`) defined alongside the existing light/dark `--color-primary` tokens from the branding work.

**Smaller items**: `CollapsibleSection`'s collapsed state now makes the *entire* bar clickable (previously only a header-sized hit target within it); org-admin and project-admin group-member pickers replaced raw text/UUID entry with the new `UserAutocomplete` (type-to-filter over the org's already-loaded user directory, click-outside-to-close); `ReportTemplate` gained `intro`/`chapters`/`appendices` fields (mirroring the per-project report-content fields from an earlier round, but at the template level) with `services/reports.py::resolve_report_config_with_template` resolving them; `list_due_reviews` gained `component_id`/`reviewer_id` filters plus batch name resolution so the due-reviews page can show and filter by a real reviewer name and component instead of just a raw list; `get_project_changes`'s activity-feed entries now always carry the *current* requirement/change-request title and link (`activityEntryLink()`, `api/types.ts`) rather than whatever name existed at the time of the event, so a renamed or re-coded item's history doesn't show a stale label; a platform-wide login background image (`ServerSettings.default_login_background_file_id`) was added alongside the existing per-org one, uploaded through `ServerManagementPage.tsx` with the same spinner/success/error feedback state added to the existing logo uploads, which previously gave no feedback at all. The ambiguous "listing disabled areas" request was resolved via clarifying question as an audit of lint-suppression comments specifically; all 21 instances found (15 `eslint-disable-next-line react-hooks/exhaustive-deps`, 4 backend `noqa: BLE001`, 2 backend `noqa: F821`) were reviewed and found legitimate and already well-documented — no changes made, recorded here so the audit isn't repeated blind next time.

**Verified for real**: full backend suite — 290 passed, zero regressions — including nine new tests for the component/category tree (cross-project rejection, per-component prefix uniqueness, requirement/change-request mismatch rejection, CSV-import per-component resolution), four for org-wide 2FA enforcement, and new/extended coverage for PAT project-scoping, ban/unban, org-name visibility, report-template content, and due-review filtering; one pre-existing test bug found along the way (`test_due_list_filters_by_component_and_reviewer` reused a single category across two components, predating this round's constraint) and fixed. `tsc -b`/`eslint`/build clean. Both containers rebuilt; 16 of 18 Playwright specs passed live, with the other two root-caused rather than blindly re-run: `golden-path.spec.ts` reproduced as the same container-load flakiness documented in earlier rounds (proven by three isolated passes immediately after a fresh restart), and `mockup-engagement.spec.ts`'s comment-reaction-count assertion traced — via a direct API call showing the backend's `reaction_count` incrementing correctly — to a pre-existing frontend timing race in code with zero diff this session, deliberately not chased further given scope. Three specs needed locator fixes for this round's own DOM changes (the tree UI's index shift, the disabled-org default filter, an exact-text-"1" collision with the new notification unread-count badge), all detailed inline in the spec files themselves.

## Self-signup, invites, and SSO — plus report images and external project users

A five-part request: images in reports, a public self-signup form with a server-wide mode, org-level email-domain auto-accept, adding a project user by email who isn't yet an org member, and listing users outside an org matching its domain. None of this is in `docs/requirements.md` (unchanged, per its own authority) — recorded here as a product decision instead. Two clarifying-question rounds shaped the design before implementation: the three signup-mode semantics (`disabled`/`always_on`/`org_specified`, with domain auto-accept scoped specifically to `org_specified`), and the external-user-by-email flow (existing accounts added directly; a not-yet-existing account gets an invite, gated by a new three-state `external_user_policy`). A third round, prompted directly by the user asking "how does this relate to SSO?", turned what would otherwise have been a documented gap into a properly designed feature — see below.

**`Organization.sso_only` went from a UI hint to a real backend control.** Investigating the SSO interaction surfaced that `NativeAuthBackend.authenticate` never actually checked it — an "SSO-only" org's native members could still log in via the plain `/login` endpoint regardless, since the flag only hid the native form on that org's own branded login page. Fixed at the source (`auth_backends/native.py::_all_orgs_sso_only`): native login is now rejected — with a distinct, specific message, deliberately not the generic wrong-password one, since it only fires *after* a correct password match and so can't be used to enumerate accounts — for a user whose *every* org membership requires SSO. A user straddling one SSO-only org and one ordinary org keeps working for the ordinary one. Two lockout-prevention validations followed directly from that now having real teeth: `update_sso_config` rejects `sso_only=True` without `sso_enabled=True`, and `allow_self_signup`/`sso_only` are mutually exclusive on both write paths (`update_advanced_settings` and `update_sso_config`) — self-signup is anonymous and admin-ungated, unlike every other native-account-creation path, so it must never be able to hand an SSO-only org a native credential it can't use. `create_org_user` picked up the same guard for the same reason.

**Two provisioning paths for "invite a project user by email," chosen by the target org's `sso_only` flag** (`services/invites.py`). An ordinary org gets a `PendingInvite` row (email, org, project, role, token, 14-day expiry) and a token-bearing signup-link email; redeeming it (native signup, or — since an invitee might reach SSO login first — the OIDC callback too, both through a shared `consume_pending_invites` helper) grants the org role (idempotently, guarding against two invites to the same org double-granting — needed an explicit `db.flush()` mid-loop since this app's sessions run `autoflush=False`) and the project role. An SSO-only org has no working native-password path for a not-yet-existing account, so instead of a `PendingInvite`, `provision_sso_invite` creates the `User` row and grants both roles **immediately** — `auth_backend="invited"` (a third sentinel alongside `native`/`oidc`, rejected by `NativeAuthBackend`'s backend-name check) marks it as not yet linked to a real identity, and `oidc_provisioning.find_or_provision_user`'s existing verified-email `existing_by_email` matching adopts it unmodified on the invitee's actual first SSO login — no new linking logic needed, and the existing unverified-email anti-hijack refusal comes along for free. The email itself just says "sign in via SSO" instead of carrying a link.

**Self-signup** (`routers/auth.py::signup`, `ServerSettings.signup_mode`): `disabled` (403), `always_on` (account created, no org — an admin assigns one afterward, mirroring `create_org_user`'s own provisioning shape), or `org_specified` (the email's domain must match exactly one org with `allow_self_signup=True` *and* `is_active=True` — a configured domain alone on an org that hasn't opted in is never sufficient, closing the obvious "someone else's domain setting lets me in" gap). Duplicate-email handling reuses `create_org_user`'s existing 409 shape rather than introducing a new enumeration surface.

**External-user project add** (`Organization.external_user_policy`: `disabled`/`org_domain_only`/`anyone`) is a new org-scoped search endpoint (`GET /orgs/{id}/users/search`, org members plus a synthetic external result when policy allows) feeding a new `POST /projects/{id}/roles/by-email` — deliberately a *direct* project-role grant (reusing the existing, previously frontend-unused `assign_project_role`/`UserProjectRole` mechanism) rather than folding into the project-group member flow, since the group endpoint's `user_id`/`org_group_id` shape had no clean way to express "resolve or invite by email" without a larger rework. `UserAutocomplete` (`components/UserAutocomplete.tsx`) gained an opt-in server-search mode (`organizationId` prop) alongside its original pure-client-side filtering, so every existing caller keeps working unchanged.

**Report images**: `![alt](attachment:<file id>)`, resolved from a pre-fetched `dict[str, bytes]` never fetched by the renderer itself (`services/reports.py::_markdown_to_flowables`/`_image_flowable`) — the same "never let user-supplied markup reach out to a URL server-side" posture `_safe`'s docstring already documents, extended to images. `routers/reports.py::_resolve_report_images` is the one place a reference is checked against the requesting project's `organization_id` before its bytes are read, closing the one real path a crafted or stale reference could otherwise leak another org's file into a generated PDF. Frontend mirror in `utils/markdown.ts` (`resolveImageRef`, new `IMG` cases in both the renderer and `htmlToMarkdown`) resolves the same reference to `fileUrl(id)`, deliberately *not* rendering a bare external image URL as a live fetch for the same server-side-documented reason. `RichTextEditor` gained an "Insert image" toolbar button (an `organizationId` prop, threaded through `ReportChapterListEditor` and every call site) — a picker over the org's existing shared-resource images plus an inline upload, reusing the existing org-resources endpoint rather than adding a dedicated one.

**Schema migration**: added as a genuine incremental Alembic revision (`0002_signup_invites_external_users.py`), not folded into `0001`'s `create_all()` baseline like prior rounds' schema changes were — the earlier "pre-release, no real deployments yet" convention doesn't hold once a persistent dev database has actually been running for days, which is exactly what this round found: running the full backend suite against `tests/container`'s stack wiped the live dev database (its `DATABASE_URL` is `reqtrack_test`, the *same* database the pytest suite drops-and-recreates every session — a pre-existing collision this round didn't cause but did rediscover the hard way, restarting the backend container to re-run its own bootstrap was the recovery). `0002` is written with `IF NOT EXISTS`/`IF EXISTS` throughout specifically because `0001`'s `create_all()` already creates these same columns/table on a *brand-new* database (reflecting the current model classes, per `0001`'s own docstring) — a plain `op.add_column` would fail there with "already exists" while being exactly what an already-migrated database needs. Worth a permanent fix later: `tests/container`'s live backend and its own pytest run sharing one database name means every `pytest` invocation there is destructive to whatever dev data was sitting in it.

**Verified for real**: full backend suite — 319 passed, zero regressions — plus 29 new tests across four new files (`test_sso_only_enforcement.py`, `test_signup.py`, `test_invites_and_external_users.py`, `test_report_images.py`), covering both allow and deny directions for every new policy (external-user policy states, org-specified domain matching, sso_only mutual-exclusion validations) and the two isolation-sensitive cases specifically re-verified per this project's change-management policy for security-sensitive changes: a cross-org `attachment:` reference is silently skipped rather than embedded, and org-specified signup never grants membership via domain match alone without the target org's own opt-in. `tsc --noEmit`/`eslint` clean on every changed frontend file. Both containers rebuilt; 19/19 e2e-workflows Playwright specs passed live (including three new ones — self-signup across all three modes, an external-invite round trip through a real MailHog email and its signup link, and inserting an image into a report and confirming a real embedded-image PDF downloads), plus `golden-path.spec.ts` and `pelion-v2.spec.ts`; `mockup-engagement.spec.ts`'s comment-reaction-count assertion failed again, the same pre-existing frontend timing race already root-caused and left unchased in the prior round's own "Verified for real" note, in code with zero diff this session — not re-investigated for the same reason. SOC 2 `access-control-policy.md`'s Provisioning section and Known Gaps were updated to describe the new provisioning paths and the new email-verification gap they introduce (self-signup/invites trust a typed address, unlike SSO's verified claim) rather than leaving it undocumented, per this project's own governance rules for changes touching provisioning.

## Hardening pass on self-signup/invites/external-users, plus two real test bugs found and fixed

Requested follow-up to the previous entry: "do a hardening and validation pass for everything," plus fix `mockup-engagement.spec.ts`'s previously-noted failing assertion rather than leaving it as a documented-but-unfixed flake. Identify → verify → remediate, per the change-management policy's practice for security-sensitive changes.

**Found and fixed: `assign_project_role_by_email` could re-admit a banned user through a different organisation.** `assign_org_role` (`routers/orgs.py`) already rejects granting a role to `User.is_banned`; the new by-email project-invite endpoint grants `UserOrgRole` too (as a side effect of adding an out-of-org existing account) but was missing the same check — a real, if narrow, gap in a control this project's own access-control policy specifically documents (banning "blocks `assign_org_role` from granting this account a role in any organisation going forward... without a server admin needing to intervene again per org"). Fixed by adding the identical check and rejection message before the grant. New test: `test_assign_by_email_rejects_banned_existing_user`.

**Found and fixed: `search_org_users` let any org member enumerate account existence across organisations.** The endpoint's own auth level (any org member, matching `list_org_users`' existing "general directory" precedent) is meaningfully lower than the actual grant endpoint's (`require_project_manage`) — meaning a plain member could learn whether an arbitrary email has an account *anywhere in the system* (a one-bit cross-tenant signal) via a free, repeatable, unaudited probe, without being able to act on it at all. "No account, would need an invite" reveals nothing about any real account and stays open to anyone; "yes, this exact email already has an account" is now withheld unless the caller is an org admin, or passes a `project_id` for a project in this org they actually manage (`can_manage_project_settings`, the same check `require_project_manage` itself uses) — reusable as a plain function since this needed a conditional check, not a hard gate. Withheld matches are omitted entirely rather than downgraded to a misleading `exists: false`. `UserAutocomplete`/`ProjectAdminPage.tsx` thread the current project's id through so a non-org-admin project manager keeps full functionality for their own project. New tests: `test_search_hides_existing_account_fact_from_a_plain_member`, `test_search_reveals_existing_account_to_a_project_manager_for_their_own_project`.

**Reviewed and found adequate, no changes needed**: invite token entropy (`secrets.token_urlsafe(32)`, 256 bits); report-image SSRF/cross-org exposure (already covered by the previous entry's `_resolve_report_images` isolation check, re-verified); plain-text invite/notification emails (no HTML injection surface); SVG-as-`<img>` handling (browsers don't execute embedded scripts in that context, and `_INLINE_SAFE_CONTENT_TYPES` already excludes SVG from direct inline serving); FK `ondelete` behaviour on `pending_invites` (CASCADE on org/project, consistent with those entities' own deletion model — projects are never hard-deleted in this app, so that CASCADE is defensive/unreachable rather than load-bearing); `org_specified` self-signup's ambiguous-domain-match handling (already fails closed — rejects rather than picking arbitrarily when more than one opted-in org claims the same domain). A theoretical TOCTOU double-registration race in `provision_sso_invite`/native signup (two near-simultaneous requests for the same not-yet-existing email) would surface as a 500 from the `users.email` unique constraint rather than silent data corruption — the same class of gap `create_org_user`'s own duplicate-email check already has, not a new regression, left as-is.

**Real bug, not test flakiness — fixed in application code, not the test**: `mockup-engagement.spec.ts`'s comment-reaction assertion, previously root-caused (prior round) as "a pre-existing frontend timing race" and left unfixed. Actually investigated this round with request/console/navigation tracing (a throwaway debug spec, discarded once done): the reaction *does* succeed — the real bug is that `CommentThread.tsx`'s "Like this comment" button relied on the `title` attribute for its accessible name, and HTML's accessible-name computation prefers an element's own visible text content over `title` once there is any — so the moment the reaction count renders as visible text ("1"), the button's computed accessible name silently changes from "Like this comment" to "1", which is exactly why a role-based locator (and a screen reader) loses track of it after the very interaction it's meant to confirm. Fixed with `aria-label` (which always wins over both `title` and text content) plus `aria-pressed` reflecting `reacted_by_me` — the latter a correct, added-for-free accessibility improvement for what is a genuine toggle control, not just a locator workaround. Confirmed with 4 consecutive clean passes after the fix (previously failed consistently).

**Also found and fixed while re-running the full suite for confirmation**: `golden-path.spec.ts`'s change-request-creation step clicked "Create" without first waiting for the "modify requirement" form's requirement `<select>` to finish its async default-selection — the exact same race `mockup-engagement.spec.ts` already had to work around with an explicit wait, just never applied here. Fixed identically (`await expect(page.getByRole("combobox").first()).toContainText(...)` before filling the rest of the form); confirmed with 4 consecutive clean passes, previously flaky (roughly 1-in-3 failure rate in back-to-back isolated reruns).

**Not a bug**: `workflow-bypass-attempts.spec.ts` failed on a full-suite rerun against data that had already been seeded and then partially consumed by an *earlier* run of the same spec in this same session (it archives and recreates a specific seeded requirement as part of its own scenario, so it isn't idempotent against a stack it has already run against once) — confirmed by re-seeding the E2E dataset fresh (`scripts/seed_e2e_dataset.py` is itself idempotent-guarded and refuses to reseed over existing data, so this needed an explicit `TRUNCATE` first) and re-running the full suite once, cleanly. Consistent with this directory's own documented convention ("run the seed script once against a fresh stack before running this directory's specs") — not a regression, and not changed.

**Verified for real**: 3 new backend tests (52 total across the two most relevant files, all passing) plus the full backend suite — 322 passed, zero regressions. `tsc --noEmit`/`eslint` clean on every touched file. Both containers rebuilt; full Playwright suite — 23/23 passed clean on a freshly re-seeded stack, including both newly-fixed specs (`mockup-engagement.spec.ts`, `golden-path.spec.ts`) each independently reconfirmed with 4 consecutive isolated passes beforehand.

## Deeper hardening pass: three real gaps below the endpoint-level review

Requested follow-up to the previous entry ("go deeper"): the prior pass reviewed the endpoints this feature set added directly; this pass instead traced *invariants* across the whole login/provisioning surface those endpoints depend on — what happens to a role grant committed for an account that turns out to be deactivated, what "banned" is actually supposed to guarantee end-to-end, and whether org-level tenant isolation is the right granularity everywhere a new attachment reference was introduced. All three findings below were reachable pre-existing gaps that this feature set's own new code interacts with (two predate it entirely), not regressions introduced by the prior two rounds.

**Found and fixed: the OIDC login callback never checked account status at all.** `NativeAuthBackend.authenticate` rejects a deactivated, archived, or banned (`is_banned` implies `is_active=False`) account — `oidc_callback` (`routers/auth_oidc.py`) had no equivalent check anywhere in the flow. `get_current_user`'s own `is_active` check happens to make the resulting token inert against every other API call, but that's an incidental backstop, not a real control: before this fix, `consume_pending_invites` and `sync_org_roles_from_claims` still ran and *committed* brand-new org/project role grants for a deactivated or banned account during the rejected-in-effect login — grants that would then sit there silently until the account was later reactivated for an unrelated reason, at which point they'd become live without whoever reactivated it ever knowing new access had been granted while it was supposed to be locked down. Fixed by checking `is_active`/`is_archived` immediately after `find_or_provision_user` resolves the account and before either of those calls run, mirroring native login's check and its "specific, not generic, rejection message" reasoning (the caller already proved control of a real IdP-verified identity, so a distinct "This account has been deactivated" message leaks nothing enumerable). New tests drive the real HTTP callback end-to-end with `oidc_client.discover`/`exchange_code_for_tokens`/`verify_id_token` stubbed via `monkeypatch` (not just the `find_or_provision_user` building block, since the fix lives in the router between that call and the ones it gates): `test_oidc_callback_rejects_a_deactivated_account`, and `test_oidc_callback_rejects_a_banned_account_and_grants_no_pending_invite` — the latter specifically proves a `PendingInvite` outstanding for the email at the time of the attempt is left unconsumed, the concrete exploit this closes.

**Found and fixed: `reactivate_orphaned_user` let a banned account back in without ever going through `unban`.** `User.is_banned`'s own docstring states the invariant plainly: a ban "survives even if something else were to flip `is_active` back on" — but `reactivate_orphaned_user` (`routers/system.py`) only ever checked `_require_orphaned_user` (org membership) before unconditionally setting `is_active = True`, never `is_banned`. `unban_orphaned_user` deliberately does *not* restore `is_active` (its own test, `test_unban_allows_role_grants_again_but_does_not_reactivate`, pins exactly that), which only holds together as a real two-step control if `reactivate` in turn refuses a still-banned account — it didn't, so a single `/reactivate` call (no `/unban` needed at all) fully restored native login for a banned account, silently defeating the entire ban feature's stated purpose ("closing the gap ... where the same account could quietly be re-admitted ... without a server admin ever being asked again" — `ban_orphaned_user`'s own docstring). This was untested: no existing test called `/reactivate` on a still-banned account. Fixed by rejecting with 400 ("This account is banned. Unban it before reactivating.") when `user.is_banned`. New test: `test_reactivate_refuses_a_still_banned_account`, which also confirms the correct unban-then-reactivate sequence still works.

**Found and fixed: report image attachments were scoped to the wrong tenancy boundary.** `_resolve_report_images` (`routers/reports.py`) checked a referenced `attachment:<id>` against the report's `organization_id` — correct as far as it went (the previous round's own cross-org test already covers that), but org membership isn't the actual access boundary for every `FileAsset` in this codebase: `routers/files.py::download_file` gates a *direct* (non-shared, `is_org_resource=False`) attachment — most importantly a requirement attachment — on *project*-level access via `get_effective_project_roles`, specifically because a multi-project org's projects aren't mutually visible to each other's members. Report content has no such per-project scoping check of its own, so a user with report-edit rights on Project A could hand-type `attachment:<id>` referencing a requirement attachment that actually belongs to Project B in the same org — one they might have no project-level access to at all — and have its bytes embedded into Project A's generated report, silently bypassing `download_file`'s own finer-grained gate. Fixed by restricting resolution to `is_org_resource=True` assets only, which also matches what the feature's own UI actually offers: the "Insert image" picker (per the original plan and `RichTextEditor`) only ever lists org shared resources, never a raw requirement-attachment id, so this closes a hand-craftable gap without narrowing the real feature at all. New test: `test_report_pdf_silently_skips_a_same_org_requirement_attachment_from_a_different_project`.

**Reviewed and found adequate, no changes needed**: `assign_project_role_by_email`'s and `assign_project_role`'s lack of an `is_banned` check on the "target already an org member" path — traced through and confirmed unreachable in practice, since `ban_orphaned_user` only ever bans a genuinely orphaned (zero org roles) account and `assign_org_role` already refuses any *new* org-role grant to a banned account, so a banned user can never actually reach "already a member of this org" while still banned (the OIDC fix above closes the one path that could have gotten them there indirectly, via a pending invite consumed on login). `SignupRequest`'s password policy (`min_length=8`) matches the codebase's existing convention elsewhere, not a new relaxation.

**Verified for real**: 4 new backend tests (326 total, zero regressions) — the full suite, not just the new/changed files, since two of these fixes touch shared login/provisioning code paths (`test_oidc_provisioning.py`, `test_access_review.py`, `test_report_images.py`). Backend container rebuilt before every test run (this stack doesn't bind-mount source). Live dev/seed data was wiped by the pytest run against the shared `tests/container` database (the same known collision noted in the previous round) and restored via a backend restart + both seed scripts afterward.

## PDF reports restructured: chapter per component, sub-section per category

Request: PDF requirement tables should read as chapters per component with category sub-sections underneath, and Status should be the rightmost table column. Two design points were confirmed with the user before implementing: Component/Category columns are dropped from the per-category tables (redundant once expressed as chapter/section headings — table becomes ID, Name, Reasoning, Status), and each component chapter starts on a fresh page.

`generate_pdf_report` (`services/reports.py`) previously rendered one flat table of every requirement. It now groups `rows` via a new pure helper, `_group_rows_by_component_and_category`, pulled out specifically so the grouping/ordering logic is unit-testable without a PDF-text-extraction dependency (this project's test suite deliberately doesn't have one — see `test_report_images.py`). Ordering uses `ProjectComponent.sort_order`/`ProjectCategory.sort_order` — the same order the component/category tree UI uses — not alphabetical or requirement-ID order, since a report should read in the order an author actually arranged their tree, not incidentally by prefix. `ReportRequirementRow` gained `component_sort_order`/`category_sort_order` fields (populated in `routers/reports.py::_collect_rows` from the same `components`/`categories` lookups it already builds) to carry this through without a second DB round-trip. Grouped on `(sort_order, name)` rather than name alone, since component/category name uniqueness is only enforced per-parent (`(project_id, prefix)`/`(component_id, prefix)`), not project-wide — two same-named components at different tree positions must stay two distinct chapters, not silently merge.

The CSV export (`generate_csv_report`) is unchanged by request — it stays a flat, unique-code-sorted list with its own Component/Category columns, since it's for spreadsheet review, not read top-to-bottom as a document; `_collect_rows`'s own row order (still unique_code-sorted) is what CSV uses, only the PDF path re-groups.

Verified visually, not just via unit tests: generated a real PDF against the demo dataset's multi-component, multi-category "Falcon-3 Inspection Drone" project and rendered pages to PNG (`mcp__pdf-mcp__pdf_render_pages`) to confirm the actual visual column order — text extracted from a ReportLab table's cells doesn't reliably preserve left-to-right reading order, so this wasn't safe to assume from `pdf_read_all` output alone. Confirmed: chapter headings per component (each on a new page), sub-section headings per category, and the table reading ID / Name / Reasoning / **Status** left to right.

**Verified for real**: 4 new backend tests (3 unit tests directly on `_group_rows_by_component_and_category` covering sort-order-not-name ordering, within-category unique_code ordering, and same-named-different-position components staying separate; 1 router-level smoke test across multiple components/categories) plus the full backend suite — 330 passed, zero regressions. `ruff check .` clean. Visual PDF confirmation as described above.

## Report generation page: intro/template defaults, continuous-layout toggle, editors moved on-page

Follow-up request refining the chapter-per-component work above: the project description should default the PDF introduction; a project's selected default template should be pre-used on the report generation page; that page should show a template's content when selected, including body chapters, with the WYSIWYG/Markdown editors actually on the page (collapsed by default); Generate PDF/Generate CSV (renamed from Download PDF/Download CSV) should sit at the top; and chapter-per-component should be a toggle — on the template and the report page — defaulting to continuous only when no template is set *and* some component has fewer than three requirements (template or explicit user choice always wins).

**Schema** (new migration `0003_report_chapters_and_default_template.py`, same `IF NOT EXISTS` convention as `0002` for the same reason): `ReportTemplate.chapters_per_component` (`bool`, default `True`) and `Project.default_report_template_id` (nullable FK to `report_templates.id`, `ON DELETE SET NULL` — deleting a template must not break the project row, just clear the default).

**Intro fallback** (`resolve_report_config`): `project.report_intro` (explicit) → `project.summary` (the project description) → `org.default_report_intro` → blank. Description-as-intro is intro-only; there's no project-level equivalent to fall back to for chapters/appendices, so those keep their existing two-tier (project → org) resolution.

**Chapter-layout precedence** (`routers/reports.py::generate_pdf`, mirroring the doc comment on `ReportRequest.chapters_per_component`): an explicit per-generation choice (`ReportRequest.chapters_per_component: bool | None`) always wins; failing that, the selected template's own `chapters_per_component`; failing that (no template), a new `services.reports.default_chapters_per_component(rows)` heuristic — chaptered unless some component in the report's actual scope (post-filter) has fewer than three requirements, computed by re-using `_group_rows_by_component_and_category` rather than a second grouping pass. `generate_pdf_report` takes the resolved boolean directly; when `False`, the per-category headings/tables are unchanged, only the component-level heading and page break are skipped (confirmed with the user up front — "continuous" keeps category structure, it doesn't revert to the old single flat table).

**Default template**: `default_report_template_id` lives on `ProjectReportConfig` (GET/PUT `/projects/{id}/report-config`), not the general project endpoint — matches where `report_intro`/`chapters`/`appendices` already live, and where Project Admin's "Report Setup" tab already edits them. The PUT validates the template belongs to the project's own organisation (same check `generate_pdf` already applies to an ad-hoc `report_template_id`), otherwise a cross-org template could silently attach itself as a project's default. This is a pure UI default, not a backend fallback-when-null: the frontend pre-selects it in the report page's own dropdown, which the user can still change or clear per generation — deliberately not reinterpreting `report_template_id: null` as "use the project default" at the API layer, since that would remove the only way to explicitly request no template once a default exists.

**Report generation page rewrite** (`ReportsPage.tsx`): now fetches the project's effective report config (`GET /report-config`) alongside components/categories/resources/templates, and hosts the same `RichTextEditor`/`ReportChapterListEditor` components Project Admin's Report Setup tab already uses — but ad hoc: editing here only affects the report about to be generated (composed into `pre_markdown`/`post_markdown` client-side via a `chaptersToMarkdown` helper mirroring `_chapters_markdown` server-side), never written back to the project's saved config. Selecting a template recomputes the editors' content via the same per-field precedence `resolve_report_config_with_template` applies server-side (template's own field if set, else the project's effective content) — computed client-side against data already on the page rather than adding a preview endpoint. Every section (`Template & layout`, `Filters`, `Introduction`, `Body chapters`, `Appendices`, `Resource sections`) is a `CollapsibleSection` defaulting to collapsed, with `Generate PDF`/`Generate CSV` (renamed, matching the pattern this project's own generation semantics increasingly outgrew "Download" for) as the only always-visible controls, at the top.

Org Admin's template editor gained a `chapters_per_component` checkbox alongside the existing intro/chapters/appendices/branding fields; Project Admin's Report Setup tab gained the default-template picker.

**Verified for real**: 5 new backend tests (project-description intro fallback with explicit-intro-still-wins and both-blank-falls-to-org-default cases; the heuristic directly; router-level precedence proving payload beats template beats heuristic via a comparative PDF byte-size check, since a page break + heading is reliably more bytes than without; template `chapters_per_component` and project `default_report_template_id` round-trips including cross-org rejection) plus the full backend suite — 335 passed, zero regressions. `ruff check .` clean, `tsc -b && vite build` and `eslint .` clean on every changed frontend file. Verified live, not just via the test suite: generated a PDF against a project with only a description set and confirmed the description became the intro (`pdf_read_all`); generated a 1-page PDF with `chapters_per_component: false` against a 2-requirement component and rendered it to confirm the category heading/table appear with no component heading or page break (`pdf_render_pages`); drove the actual `ReportsPage` through a real browser session (Playwright MCP, per this project's "reserve for targeted one-off checks" rule) to confirm Generate PDF/CSV sit at the top, every section renders collapsed, and expanding Introduction shows the project's description already loaded into a full `RichTextEditor` (Markdown/Rich text toggle, Insert image). Full Playwright suite — 23/23 passed, including the two specs updated for the button rename (`golden-path.spec.ts`, `report-image-attachment.spec.ts`).

## Mandatory requirement target, optional description, change-request field-level tracking, comment attachments, and a locked-requirement attachment gate

An 8-item batch request: an "Add requirement" button on the Project Overview page; a "Make change request" button on the requirement detail page; change requests should only show/collect the fields they actually propose to change, pre-filled from the requirement so nothing needs re-typing; a requirement's target version can no longer be left unset; requirements gain an optional free-text description (also changeable via a change request); stakeholder vote comments should be viewable in a pop-up; the requirement change log should show who made each change; and direct requirement attachment should be blocked outside of creation/change-request approval, with attachment capability added to comments instead.

Four clarifying decisions were made with the user up front, all adopted as designed:

- **`RequirementLevel` gains a third member, `"optional"`**, rather than renaming `"recommended"` — the two are kept as distinct, coexisting levels.
- **Existing rows with a null `target_stage_id` are backfilled to each project's own earliest stage** (by `sort_order`) rather than an arbitrary "TBD" stage or a required manual migration step.
- **Change-request-proposed attachments only actually attach to the requirement once the change request is approved**, not immediately on draft — mirrors how every other proposed field already only applies on approval, so a withdrawn/rejected CR never leaves an orphaned attachment link behind.
- **The backend tracks which fields a change request actually changed** (`ChangeRequestVersion.changed_fields: list[str]`, one of `name, reasoning, clarification, description, target_stage_id, level, review_date, review_lead_days, reviewer_id, custom_fields, attachments`) rather than leaving "which fields to show/apply" as a frontend-only convenience. `POST /change-requests` validates `changed_fields` is non-empty for a MODIFY_REQUIREMENT CR and requires a non-null `proposed_*` value for every field listed that can't itself be null; on approval, `apply_new_version` is only passed a field's proposed value when that field is in `changed_fields` — everything else carries the current version's value forward unchanged, using the same `*_explicitly_set` carry-forward convention `target_stage_id`/`review_date`/`review_lead_days`/`reviewer_id` already used pre-Pelion, now extended uniformly to every proposable field instead of staying a special case for those four. This is the single source of truth the frontend's per-field checkbox UI reads and writes — there is no separate frontend-only diffing logic.

**`target_stage_id` becomes `NOT NULL`, and its FK becomes `ON DELETE CASCADE`.** Migration `0004` backfills nulls (correlated subquery: each requirement's own project's earliest stage) before setting the column `NOT NULL`. The FK's `ON DELETE` action had to change from `SET NULL` to `CASCADE` in the same migration — `SET NULL` against a now-`NOT NULL` column raises `IntegrityError` the instant a `ProjectStage` is ever deleted, which happens today only as a side effect of its whole project/organisation being deleted (there is no standalone `DELETE /stages/{id}` endpoint) — at which point every `RequirementVersion` referencing that stage is being deleted in the very same cascade anyway, so `CASCADE` here is a no-op in practice, not a real data-loss risk. Found via `test_org_lifecycle.py`'s existing cascade-delete test, which started raising `IntegrityError` the moment the column went `NOT NULL` — a genuine bug this change would otherwise have shipped, not a pre-existing gap.

**Comment attachments are the one place a user may still attach a file to a requirement outside of creation or an approved change request.** A new `comment_files` join table (`ReviewComment` ↔ `FileAsset`) backs `POST /comments/{id}/files` on both the requirement and change-request routers (mirrored, matching this codebase's existing precedent of per-router-duplicated comment endpoints over the shared `ReviewComment` model). `routers/requirements.py`'s direct-attachment endpoints (`upload_requirement_attachment`, `link_org_resource`) now 409 once the requirement is locked; comment attachments are deliberately exempt from that check, since a discussion thread isn't C-G-12-governed content. This also surfaced a real, pre-existing access-control gap in `routers/files.py::download_file`: the generic file-download endpoint had no resolution path at all for a comment-attached file, so it would 403 *everyone*, including the uploader — fixed by extending its access resolution to walk `CommentFile → ReviewComment → (Requirement | ChangeRequest)` for the owning project, the same pattern already used for `RequirementFile`-linked downloads.

**Frontend**: `ChangeRequestsPage.tsx`'s create form now shows, for a MODIFY_REQUIREMENT change request, a checkbox per changeable field (label set: Name, Reasoning, Clarification, Description, Target version, Level, Review date, Reminder lead time, Assigned reviewer, Custom fields, Attachments) — checking one reveals an editor pre-filled from the selected requirement's current value, and only checked fields are submitted in `changed_fields`/`proposed_*`. A NEW_REQUIREMENT CR keeps a normal always-visible create form, since there's no existing requirement to diff against. `ChangeRequestDetailPage.tsx` mirrors this on display: a MODIFY_REQUIREMENT CR only renders the fields actually in `changed_fields`, instead of always showing every proposed value regardless of whether it was touched. Attachments reuse the existing org-shared-resource picker/upload flow (same mechanism the report-image "Insert image" picker already established) rather than a new upload endpoint. A new reusable `Modal.tsx` component (portal-rendered, Escape/backdrop-dismissible) backs the stakeholder-vote-comments pop-up on `ChangeRequestDetailPage.tsx` — the vote data (`ChangeRequestVoteTallyOut.votes`) already carried every voter's comment, so this was purely a rendering gap, not a backend change. The requirement change log gained a "Changed by" column from `RequirementVersionEntry.created_by`, which was likewise already returned by the API and only needed surfacing.

Two real frontend bugs were found via an actual browser session (not just `tsc`/`eslint`/unit tests, which stayed clean throughout) while verifying the "Make change request" deep link (`/change-requests?requirement=<id>`, opened from the requirement detail page) pre-selects the right requirement:

- **Stale-closure race clobbering the deep-linked selection.** `ChangeRequestsPage`'s data-loading `reload()` defaults `requirementId` to the project's first requirement when none is set yet; a separate effect sets `requirementId` to the deep-linked id and then calls `setSearchParams` to strip the `?requirement=` param. `setSearchParams`'s updater mutates the underlying `URLSearchParams` instance **in place** rather than returning a copy — so by the time `reload()`'s own in-flight `await` resolved, its closure's live reference to that same mutable object read back `null` for the just-stripped param, even though the value had been correct at the point `reload()` started. Fixed by capturing the param's value into a local `const` synchronously, before the first `await`, rather than re-reading the (shared, mutable) `searchParams` object after it.
- **Prefill effect never firing when the requirement list hadn't loaded yet.** The effect that pre-fills the "Fields to change" editors from the selected requirement's current version depended only on `[requirementId, kind]`; when the deep link set `requirementId` before the requirements list itself had finished loading, `selectedRequirement` was `null` on that pass, the effect bailed out, and — since `requirements` finishing loading later wasn't in its dependency array — it never re-ran. Fixed by adding `requirements` to the dependency array so the effect re-evaluates once the list the deep-linked id needs to resolve against actually arrives.

**Verified for real**: full backend suite (348 tests, including a new `test_change_request_field_tracking.py` covering target-defaulting, `changed_fields` validation, unlisted-field-never-applied-on-approval, locked-requirement attachment rejection, change-request attachment only applying on approval, cross-org attachment rejection, and comment-attachment upload/download/cross-project isolation) passing, `ruff check .` clean. `tsc --noEmit`, `eslint`, and `vite build` clean across every changed frontend file. Live-browser verification (Playwright MCP) of the full loop: created a requirement with the new mandatory pre-filled target and new description field; opened it and used "Make change request" to deep-link into a correctly-pre-selected, correctly-pre-filled MODIFY_REQUIREMENT form; toggled only the Description field, submitted, submitted-for-review, cast an advisory vote with a comment, opened it via the new "View comments" pop-up, approved; confirmed the approved requirement's changelog showed both the change and the correct "Changed by" actor, and that its Attachments section now shows the locked notice instead of an upload control.

## Comment editing/attachment redesign, renaming and deleting project stages/components/categories, and two notification bugs

Three more items from the same feedback batch: comment file attachments were reported as "the button doesn't work" (see below for the real bug) and should only ever be added while composing or editing your own comment (not as a persistent post-hoc action on any comment); anyone who can create/delete project stages/components/categories should also be able to rename them, and deleting one should require reassigning whatever it currently governs to another existing one rather than being blocked outright — except when that would be genuinely unsafe; and the notification list/dropdown had unreadable text plus no click-through to the item that triggered it.

Two decisions were confirmed with the user before implementing the structural-deletion piece, since it directly intersects data this codebase already treats as immutable history:

- **Deleting a stage that has an approved baseline is refused outright, not reassigned.** A `Baseline` is documented as an *immutable* snapshot of a specific stage's approved requirements (C-G-10) — silently repointing `Baseline.stage_id` at a different stage on deletion would quietly rewrite what that historical snapshot means. Every other stage reference (every `RequirementVersion.target_stage_id`, across *all* historical versions, and every `ChangeRequestVersion.proposed_target_stage_id`) is reassigned to `reassign_to`; only baselines block the delete. Renaming a stage remains available regardless, and archiving is unaffected — this only closes off outright deletion of a stage with real approval history.
- **Comments may only be edited by their own author** — not by a project manager/administrator either. A PM correcting or moderating someone else's words was considered and explicitly rejected in favour of the simpler, more conservative rule: nobody edits content they didn't write. The same author-only rule was extended to attaching/removing a comment's files, since those are really just another part of "editing your own comment."

**Comment editing + attachment redesign** (`backend/app/models/change_request.py::ReviewComment`, `backend/app/schemas/requirement.py`, `backend/app/routers/requirements.py`, `backend/app/routers/change_requests.py`, migration `0005_comment_editing.py`):
- `ReviewComment.edited_at: datetime | None`, exposed as `CommentOut.edited_at` — null until an edit happens. Deliberately its own column rather than comparing `created_at`/`updated_at`: `TimestampMixin`'s two independent `default=utcnow` callables can resolve a few microseconds apart on the very same INSERT, which would make a freshly-created, never-edited comment intermittently render as "(edited)".
- New `PATCH .../comments/{comment_id}` (both routers) — author-only, updates `body` and stamps `edited_at`.
- New `DELETE .../comments/{comment_id}/files/{file_id}` (both routers) — author-only; always deletes the underlying `FileAsset` outright (comment attachments are never `is_org_resource` shared links the way requirement attachments sometimes are, so there's no unlink-vs-delete branch to choose between, unlike `unlink_requirement_file`).
- **The actual bug behind "the button doesn't work"**: `POST .../comments/{comment_id}/files` had no author check at all — any project viewer could attach a file to *anyone's* comment, at any time, forever. That's not what "attach a file to your comment" should mean, and it's also inconsistent with there being no way to remove one. Tightened to author-only (403 otherwise) as part of this same change, alongside the new remove endpoint — together these make "attach" and "remove" symmetric, author-scoped actions rather than the previous asymmetric "anyone can add, no one can remove" state.
- **Frontend** (`CommentThread.tsx`, rewritten): the attach control now only appears in two places — the compose box for a *new* comment (staged locally as pending `File` objects, uploaded only after the comment itself is created, chained off the id the create call returns) and inline when editing your own existing comment (existing attachments shown with a remove button, new ones stageable the same way, both applied on Save). There is no more persistent per-comment attach button on an already-posted comment you're not editing. `onPost`'s return type changed from `Promise<void>` to `Promise<Comment>` specifically so `CommentThread` can chain the staged-file uploads off the newly-created comment's id — both `RequirementDetailPage.tsx` and `ChangeRequestDetailPage.tsx`'s `postComment` were updated to match. Comments show "(edited)" next to their timestamp once `edited_at` is set; the Edit control itself only renders when `currentUserId === comment.author_id`.

**Stage/component/category rename** (`routers/projects.py`): plain `PATCH` endpoints (`ProjectStageUpdate`/`ComponentUpdate`/`CategoryUpdate`), same `require_project_manage` gate as the existing create/move endpoints, with the same collision validation create already does (stage name unique per project, component prefix unique per project, category prefix unique per *component* — reusable across different components' categories, since `ProjectCategory`'s constraint is `(component_id, prefix)` not `(project_id, prefix)`). Renaming a component/category's prefix never rewrites already-generated `Requirement.unique_code` values (e.g. `SW-PERF-014` stays `SW-PERF-014`) — codes are assigned once at creation and are permanent identifiers, not a live label.

**Stage/component/category delete-with-reassignment**: all three share the shape "take `reassign_to`, refuse if it doesn't resolve to a different, existing sibling in the same project" — which turns out to make the "can't delete the last one" rule fall out for free rather than needing its own special-cased check: if there's only one stage/category left, no valid `reassign_to` can ever exist, so the existing 400 validation already covers it.
- **Stage** (`DELETE .../stages/{id}?reassign_to=`): reassigns every `RequirementVersion.target_stage_id` (all versions, not just current — `target_stage_id` is `NOT NULL`, so this can't be left dangling) and every `ChangeRequestVersion.proposed_target_stage_id` equal to the deleted stage; transfers `is_current` to `reassign_to` if the deleted stage held it, so a project is never left with zero current stages. Blocked (409) if any `Baseline` references the stage — see decision above.
- **Category** (`DELETE .../categories/{id}?reassign_to=`): `reassign_to` may be a category under a **different** component than the one being deleted (deliberately not restricted to siblings the way `move_category`'s *ordering* is) — reassigns every affected `Requirement.category_id` **and** `component_id` together, preserving the invariant (enforced at requirement-creation time) that a requirement's component always matches its category's parent component even when the reassignment crosses component boundaries.
- **Component** (`DELETE .../components/{id}`, no `reassign_to`): refused (409) unless the component already has zero categories. Since every requirement's `component_id` is enforced to match its category's component at creation, a component with zero categories necessarily also has zero requirements referencing it directly — so once its categories have each been deleted/reassigned (via the category endpoint above, which can target a category under any other component), the component itself becomes a plain, unconditional delete with nothing left to reassign.

**Notification fixes** (`frontend/src/components/NotificationBell.tsx`, `frontend/src/pages/NotificationsPage.tsx`, `frontend/src/styles/theme.css`, `frontend/src/api/types.ts`):
- **The actual "white on white" bug**: the notification bell's dropdown is a normal light `.card` popover, but it's rendered as a DOM descendant of `.app-header` (the bell icon lives in the header bar) — `.app-header` sets a header-tuned white `color` and overrides `.btn`/`.text-muted` for text sitting directly on its own dark bar, and the dropdown never reset any of that back to normal page colours despite visually being a light surface, not header chrome. Fixed with a `.notification-dropdown` class and three CSS rules (`.app-header .notification-dropdown` / `...text-muted` / `...btn`) specific enough to win over the header's own rules regardless of source order — confirmed visually via a screenshot, not just by reading the CSS, since a plain diff can't show what actually renders.
- **Click-through**: `notificationLink()` (`api/types.ts`) resolves a notification's `entity_type`/`entity_id`/`project_id` to a route (`requirement` → the requirement's page, `change_request` → the CR's page, `project_stage` → that project's admin page, otherwise falls back to the project overview if a `project_id` is present at all, else not navigable) — the same "resolve from what the activity feed already carries" approach `activityEntryLink` already used for the project-history feed, just applied to notifications for the first time. Clicking a notification now marks it read (as before) and navigates, in both the header dropdown and the full `/notifications` page.

**Verified for real**: full backend suite — 371 tests total (23 new: 17 stage/component/category rename+delete permission/validation/reassignment-correctness cases in a new `test_project_structure_admin.py`, plus 6 comment-editing/attachment-author-only cases added to `test_change_request_field_tracking.py`) — passing, `ruff check .` clean. `tsc --noEmit`, `eslint`, and `vite build` clean. Live-browser verification (Playwright MCP): posted a comment with a compose-time attachment, edited it (confirmed body change, attachment preservation, and the "(edited)" label all render correctly), renamed a stage then deleted it with reassignment (confirmed via the requirements list that every requirement previously targeting it now shows the reassignment target, with no leftover reference to the deleted stage anywhere), renamed then deleted a category with a *cross-component* reassignment target (confirmed the affected requirements' component/category badges updated to the new component, while their historical `unique_code` prefix stayed unchanged as designed), and confirmed the notification dropdown's text/buttons are legible (screenshot) and that clicking a change-request notification navigates straight to that change request.

## Container image publishing and version tagging

The `docker-build` CI job (see "Continuous integration" above) previously built and tagged the production images but deliberately never pushed them, per an earlier explicit instruction to hold off until the system had seen enough real-world usage to trust an automated publish. Per a later explicit instruction, that's now reversed: the job pushes `backend`, `frontend`, and `mcp-server` to `ghcr.io/<owner>/<repo>-{backend,frontend,mcp-server}` on every push to `main` — pull request runs still build all three (still proving the Dockerfiles work) but never push, since a same-repo PR's `GITHUB_TOKEN` could technically publish but a feature branch has no business overwriting `:latest` (or any tag) in the registry a real deployment pulls from. No new secret was needed: `GITHUB_TOKEN` already had `packages: write` at the job level from when the job was first written build-only.

**Version tagging via GitVersion.** Each image now gets three tags instead of two: the existing `<commit sha>` and `latest`, plus a SemVer computed by [GitVersion](https://gitversion.net/) (new `GitVersion.yml`, repo root) from the commit graph. The repo has no git tags and only one long-lived branch, so `mode: Mainline` was the only configuration that made sense here — every merge to `main` is treated as a release and the version increments automatically (starting at `0.1.0`), rather than requiring someone to push an annotated tag by hand for every release the way GitVersion's default `ContinuousDelivery` mode would. This gives deployments a stable, human-meaningful version to pin `IMAGE_TAG` to (see below) without adding any manual release-tagging step to the workflow. `actions/checkout` needed `fetch-depth: 0` added (full history, not the default single shallow commit) for GitVersion to have a commit graph to compute from at all.

**`docker-compose.yml` updated to match.** The root (production) compose file previously only had `build:` for `backend`/`frontend`/`mcp-server`, meaning a local Dockerfile build was the *only* way to run it — appropriate when nothing was ever published, wrong now that images exist. Each of the three now also declares `image: ${GHCR_IMAGE_PREFIX:-ghcr.io/ballardr/reqtrackmanager}-<service>:${IMAGE_TAG:-latest}` alongside the existing `build:`. Both were kept, deliberately, rather than replacing `build:` outright: `docker compose up --build -d` (or a plain `up`/`pull` before any image has ever been published) still works exactly as before with no registry involved, while `IMAGE_TAG=<semver-or-sha>` plus `docker compose pull && docker compose up -d` becomes the faster upgrade path — pull a specific released version and restart, no source checkout or local build needed on the deployment host at all. `GHCR_IMAGE_PREFIX` is separately overridable for anyone mirroring the images into a different registry/namespace.

Documented in `docs/deployment.md`'s new "Obtaining container images" section (tag meanings, the `IMAGE_TAG`/`GHCR_IMAGE_PREFIX` variables, the pull-instead-of-build upgrade path) and the README's "Continuous integration" section (which previously ended with "does not publish them yet — see the comments... for exactly what to uncomment when ready"; that instruction has now been followed).

**Verified**: `docker compose config` against the updated root `docker-compose.yml` (with the required secrets stubbed in) resolves cleanly and the three `image:` lines interpolate to the expected `ghcr.io/ballardr/reqtrackmanager-{backend,frontend,mcp-server}:latest` with no `IMAGE_TAG`/`GHCR_IMAGE_PREFIX` set, confirming the defaults are correct before any image has actually been pushed by the updated workflow.

## README restructure: product-facing content, dev workflow split out, nginx doubling investigated

The README had drifted into two audiences at once — a prospective user evaluating the product and a contributor setting up a dev environment — with the latter (full local backend/frontend/test/lint/CI setup) taking up roughly half the file. Per explicit instruction, restructured along that seam:

- **New `docs/development.md`**: the "Quick start — local development / evaluation" section (including Demo data) and the entire "Development workflow" section (backend/frontend setup, backend/E2E tests, linting, CI) moved there verbatim, since none of it is genuinely "what is this product" content. `CLAUDE.md`'s README Requirements section (which mandates a "Development workflow" section in the README) was updated to make explicit that a short section linking to `docs/development.md` satisfies this, rather than requiring the full instructions duplicated inline — otherwise this move would have contradicted the project's own checked-in documentation policy.
- **README rewritten around product content**: the intro was expanded from a single dense paragraph into three sections — the requirements workflow (authoring, versioning, stage baselining, change requests with tasks/voting, discussion threads, review scheduling), team-workflow features (RBAC, custom fields/terminology/templates, notifications), and an enterprise-features section (SSO/OIDC, PATs scoped per org/project, application-layer encryption at rest, the MCP server, observability, the SOC 2 policy package, and the SCIM/provisioning blueprint) — covering exactly the three areas asked for (requirements, workflows, enterprise features) without inventing capabilities the codebase doesn't have; every claim traces to an existing feature already documented elsewhere (`docs/requirements.md`, `docs/enterprise-integration.md`, `docs/mcp-server.md`).
- **`CLAUDE.md` documentation-requirements exception**: added an explicit carve-out that README screenshots don't need "what it shows / how to read it / why it matters" explanatory prose the way Mermaid diagrams do — a short caption is enough. Written to stop a future documentation pass from padding the screenshots table to satisfy the diagram rule, which was never the rule's intent.
- **Screenshots recaptured** (all 9, at the same 1440×900 viewport as before) against the live dev/test stack with `scripts/seed_demo_data.py`'s dataset — the existing ones predated the recent white-logo header change and a couple of UI states shown (e.g. an org-admin screenshot with a stray autofilled password visible in the "new user" form) weren't clean. Logged in as `demo.admin@example.com` and re-walked every page the original set covered; the org-admin capture additionally has its scratch "new user" form fields cleared first so nothing that looks like a credential appears in a published screenshot, even though the underlying values were fake bootstrap-only demo credentials.
- **Production deployment section expanded in the README itself** (not just deployment.md) per instruction to actually *discuss* what's needed rather than link past it: a concrete minimal `.env`, and a paragraph each on the three "extra" components — MinIO (default S3-compatible storage, swappable for real S3), per-organisation OIDC/SSO (no bundled IdP in production; Keycloak is dev/test-only), and the MCP server (optional, credential-less, forwards the caller's own token) — plus both reverse-proxy patterns with a working nginx snippet for the same-origin one.

**The `/api/api/v1` doubling investigation** (prompted by "I think the current nginx example would end up with the backend on `/api/api/v1`"): tested for real against the live dev/test stack rather than reasoned about from the config alone. Ran a throwaway nginx container on the same Docker network implementing `docs/deployment.md`'s exact same-origin subpath config (`location /api/ { proxy_pass http://backend:8000; }`), then compared `GET /api/v1/system/branding` through the proxy against the same request straight to the backend: identical status (`401`) and identical body, and the extra one-to-one `/api/docs`/`/api/openapi.json` blocks likewise matched their direct equivalents (`200`, byte-identical `openapi.json`). **The nginx config itself is correct** — `proxy_pass http://backend:8000;` with no URI component forwards the original request path unchanged, and that path already carries `/api/v1/...` from the frontend's own hardcoded route literals, so there is no doubling anywhere in the documented example.

The real doubling risk is a `PUBLIC_API_BASE_URL` misconfiguration, not the proxy: the frontend always prepends `/api/v1/...` (baked into its own request-building code, `frontend/src/api/client.ts`) onto whatever `PUBLIC_API_BASE_URL` resolves to, so the intuitive-but-wrong choice of `PUBLIC_API_BASE_URL=/api` (matching the proxy's own location prefix, which feels like it should be "the API's base URL") produces requests to `/api` + `/api/v1/...` = `/api/api/v1/...`, which 404s. Confirmed this directly too — ran the actual built frontend image with `VITE_API_BASE_URL=/api` and inspected its generated `env-config.js`, showing the literal wrong value that would be prepended. Deliberately not "fixed" in code: the doc already specified `PUBLIC_API_BASE_URL=` (empty) as correct, and adding logic to strip a redundant `/api` prefix would paper over a config mistake in a way that could silently break a deployment whose API genuinely lives under a different `/api`-containing path for unrelated reasons. Instead, `docs/deployment.md`'s same-origin subpath section gained an explicit "Common mistake" callout naming the wrong value, explaining the mechanism, and cross-referencing this verification, plus a note on the existing "verified against a real reverse proxy" line recording the re-verification and its scope.

**Verified**: all of the above — the proxy/doubling comparisons were live `curl` diffs against real containers (not just described), not synthetic. `tsc`/build not re-run for this pass (no frontend/backend code changed, only Markdown and the two throwaway nginx/frontend test containers, both torn down afterward).

## CI failure: three Playwright specs broken by the component/category rename UI and the comment-attachment redesign

CI failed on the merge of the batch above (`docker-build` never ran; `backend-and-e2e` failed with 3/23 Playwright specs red: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, all in the same "add component and category" step). Diagnosed against the real dev/test stack rather than guessed from the stack trace, since the failure mode (a button staying disabled after filling its inputs) had more than one plausible cause.

**Root cause #1 — stale selector, not a real bug.** All three specs asserted `getByText("Software")`/`getByText("Performance")` etc. right after creating a component/category, from before component/category names were rename-able. The rename feature (this same batch, see "Comment editing/attachment redesign, renaming and deleting..." above) changed those names from static text to always-editable `<input value={...}>` fields with no `placeholder`/label — so the name only ever exists as an input's *value*, never as a text node, and `getByText` correctly found nothing. Playwright has no `getByDisplayValue` (that's a Testing Library API, not Playwright's — confirmed by trying it: `TypeError: page.getByDisplayValue is not a function`); fixed with `page.locator('input[value="X"]')`, first verifying live that React does mirror the `value` prop onto the DOM attribute for these inputs (checked via `element.getAttribute('value')` against a real running page), so the CSS attribute selector is reliable here.

**Root cause #2 — a real, if narrow, app-level race, not just a test bug.** Fixing root cause #1 got further but still failed: the "New category" button stayed disabled even after filling its Name/Prefix fields immediately following component creation. Root-caused with a `MutationObserver` attached to the real running app (not guessed): `ProjectAdminPage.tsx`'s `reload()` (called after every mutation) fires 7 requests concurrently via `Promise.all`, then two *more* awaited **sequentially** afterwards (`setOrgUsers`, `setReportTemplates`) — unrelated to the Categories tab, but still state on the same component, so each still triggers a re-render. That second wave lands ~150-300ms after the new component itself first becomes visible, which is enough to reset whatever's mid-typed into its own newly-rendered "add category" form if a script (or a very fast user) starts typing immediately. Confirmed the exact timing via the observer before treating it as the explanation, not after.

Deliberately not "fixed" by making `reload()` a single atomic state update or debouncing it — that's a real app-level improvement worth doing at some point (two of `reload()`'s 9 requests are unrelated to most of what triggers it and don't need to block anything), but out of scope for a CI-red fix and risked introducing a real regression under time pressure to chase a real one. Fixed at the test layer instead: `await page.waitForLoadState("networkidle")` after the component becomes visible, before touching its form, rides past the whole settling window regardless of exactly how many re-renders cause it. Also scoped the subsequent `getByPlaceholder("Name"/"Prefix")` calls to the specific new component's own container (`input[value="X"].locator("xpath=../../..")`, three levels up per the actual JSX nesting) rather than a page-wide `.first()`, since with rename inputs now also using those placeholders, "first Name/Prefix pair on the page" is no longer an assumption safe to make.

**A third, unrelated latent bug found while fixing `pelion-v2.spec.ts`**: after both fixes above, that spec progressed further and failed at a completely different step — `input[type="file"]` resolved to 2 elements instead of 1. The comment-attachment redesign (same batch) added an always-present file input to the comment compose box (`CommentThread.tsx`), which now collides with the requirement's own "Attachments" card file input on the same page — a selector that was unambiguous before this batch. Fixed by scoping to the "Attachments" card specifically (`page.locator(".card", { has: page.getByRole("heading", { name: "Attachments" }) })`) rather than a page-wide file-input query. Checked the rest of the suite for the same pattern (`grep` across `tests/playwright/tests/`) — only this one site was affected; `report-image-attachment.spec.ts`'s own file-input query already scoped via `[accept="image/*"]`, which doesn't collide.

## Full-product QA pass: 15 new Playwright specs closing every remaining page/workflow gap, three real bugs found and fixed, ten cross-spec test-isolation races found and fixed

Asked to act as QA lead for a team new to the project: review the whole product UI end-to-end against `docs/requirements.md` and this log, and add Playwright coverage so every page and workflow is tested, with business logic verified as backend-enforced rather than just hidden client-side. Full detail (personas, workflow catalogue, every gap found) lives in `docs/e2e-workflows.md`, which is this suite's own living document; this entry records the decisions and the real bugs, per this log's usual scope.

**Gap inventory.** Cross-checked the full route list (`frontend/src/App.tsx`, 23 pages) against `docs/requirements.md` and this log's implementation history. Massif (v3) is fully built except SCIM/separate-port provisioning (out of scope per the existing decision above); several post-Massif rounds added self-signup/invites, report images, and CR field-tracking. Fourteen genuinely untested areas remained — entire pages or backend-enforced rules with zero Playwright coverage: review scheduling, change-request tasks/voting, stage review deadlines/completion, the structural/groups/fields/templates halves of Project and Org Admin, org-wide 2FA, the server-admin user directory and bans, notifications/favourites, project history, comment editing/attachments, the CSV import wizard, requirement/CR filters, and preferences/theme/landing-page. Closed with 15 new spec files under `tests/playwright/tests/e2e-workflows/`, one per cohesive area, following this suite's existing convention (real UI interaction paired with a direct API call proving server-side enforcement wherever a permission or workflow-state rule is involved). Two personas (`stakeholderAlpha2`, `orphan`) and their helpers were added to `seed_e2e_dataset.py`/`helpers.ts` alongside the existing five, idempotent-guarded the same way the script already was. AI-assisted requirement writing, IBM DOORS import, SCIM, and the separate provisioning port were skipped silently, per standing scope decisions above.

**Three real product bugs, found live and fixed** (identify → verify → remediate, since two touch workflow-state enforcement):
- **No UI path existed to put a project stage into `review` status at all.** `ProjectAdminPage.tsx`'s only stage action jumped `scoping → approved` directly; the review-deadline/stakeholder-response UI (C-R-05) only ever renders once a stage is *in* review, which nothing in the product could reach — flagged to the user before fixing, per this project's standing "query unexpected behaviour rather than silently work around it" instruction. Fixed with a new "Start review" action, plus real state-machine validation added to `POST /stages/{id}/transition` (`_ALLOWED_STAGE_TRANSITIONS` in `routers/projects.py`): a stage can no longer skip a step (`scoping → approved`) or move backwards (`approved → scoping`), which would otherwise silently unlock already-locked requirements outside the change-request process; `COMPLETED` stays reachable only via the dedicated `/complete` endpoint. New backend test `test_stage_transition_rejects_skipping_and_backwards_moves`; four pre-existing backend tests updated to go through `review` first, and `seed_demo_data.py` (missed in the same pass, caught later when a reseed broke — see below) updated the same way.
- **`GET /projects/{id}/report-config` was gated to manager-only, silently breaking the Reports and Project Admin pages for stakeholders/members.** Same failure class as the previously-documented "OrgAdminPage hangs forever" bug (see "UI/UX audit" above): one over-privileged fetch inside a page's single `Promise.all` reload fails the whole page load. C-U-03 lets stakeholders/members generate reports, so `ReportsPage.tsx` was already silently 403ing there too, independent of the admin-page symptom that surfaced it. Fixed by loosening the GET to `require_project_view` (the PUT stays manager-only); new test `test_report_config_is_readable_by_a_plain_stakeholder_not_just_a_manager`.
- **`ToggleSwitch` didn't stop click propagation.** `CollapsibleSection` wraps its title in a clickable header; the 2FA-enrollment toggle is the one place a `ToggleSwitch` renders inside that title, so enabling it also bubbled up and immediately collapsed the section, hiding the QR/confirmation UI the toggle had just revealed. Fixed with `e.stopPropagation()` in `ToggleSwitch.tsx` itself, not just the one call site, since the same nesting could recur.

**Verification methodology, and a real lesson in it.** Each new spec was written and passed individually against its own fresh reseed. Running the full suite together (38 specs, `workers: 1`, sequential) then surfaced a *second* class of failure individual runs can't catch: ten specs (eight of mine, one pre-existing seed script, one pre-existing spec's undocumented dependency) whose passing depended on assumptions that only hold on a database only one spec has touched. None were product bugs; full root-cause detail for each (a stale hard-coded requirement name a different spec legitimately mutates, four `Locator.count()`-doesn't-auto-wait races, an unscoped placeholder query colliding with a same-page SMTP field, and this doc's own reseed recipe missing a required seed script) is in `docs/e2e-workflows.md`'s "Full-suite hardening" section rather than duplicated here. The methodology lesson is worth recording here though: the first attempt to *confirm* the fixes by re-running the full suite again without a fresh reseed in between produced 9 failures in specs that had never failed before — an artifact of two prior runs plus interactive MCP-browser debugging having left the shared database in states those specs never anticipated, not a regression. The only valid verification turned out to be a full clean-slate reseed (`down -v && up --build -d`, backend pytest as the truncate step, restart, both seed scripts) followed by exactly one suite run with no manual interference — confirmed clean (38/38) on the fourth attempt, after the first three each surfaced genuine issues to fix.

**Verified for real**: backend pytest — 373 passed — after each of two clean-slate reseeds in this pass (confirming the stage-transition change didn't regress anything, and again after fixing `seed_demo_data.py` against it). Full Playwright suite (38 specs, old + new) — 38 passed — against a genuinely fresh reseed with zero interference, the only run of the four attempts that counts as real confirmation per the methodology note above.

**Verified**: all three affected specs re-run individually until green, then the full 23-spec Playwright suite (`23 passed`) and the full backend suite (`371 passed`) both run clean against the real dev/test stack — not just the three fixed specs in isolation, catching any other fallout from the same rename/attachment UI changes. Per the stack's known pytest-truncates-the-schema behavior, restarted `backend` and reseeded both `seed_demo_data.py` and `seed_e2e_dataset.py` afterward before considering the stack verified-clean again.

## CI: GitVersion 6 removed Mainline mode (real run failure), frontend test/coverage badges added, and a badge-publishing race fixed

The `docker-build` job's real run on `main` failed at "Determine version" with `GitVersion output is not valid JSON` and no further detail in the Actions log — the underlying `dotnet-gitversion` process produced no output at all, which the wrapper action then couldn't parse. Diagnosed by running the actual `gittools/gitversion` tool locally (via its official Docker image) against both a throwaway repo and the real repo checked out to `main`, rather than guessing from the truncated Actions log: `mode: Mainline` — valid in GitVersion 5.x, and what `GitVersion.yml` was still set to — was removed entirely in GitVersion 6.0 (only `ContinuousDelivery`/`ContinuousDeployment` remain), so the config file itself was rejected before GitVersion could even attempt a version calculation. This had never actually been exercised: the prior "Container image publishing" pass (see above) only verified `docker compose config`'s tag *interpolation* against hypothetical values, never ran GitVersion itself for real.

**Fixed** by switching to `mode: ContinuousDeployment` (GitVersion 6's replacement for Mainline's "every commit on the tracked branch is a release" behaviour — unlike `ContinuousDelivery`, it produces a clean `X.Y.Z` with no prerelease suffix on every commit, which is what a container tag needs) plus an explicit `next-version: 0.1.0`, since GitVersion's own default next-version starts a repo with no tags at `0.0.1`, not the `0.1.0` the file's comment already (aspirationally, and incorrectly until now) claimed. Verified against the real tool both ways: a throwaway repo with a minimal `mode: Mainline` config reproduces the exact same "Invalid enum scalar 'Mainline'" exception GitVersion itself throws (the Actions wrapper just swallows it), and the actual repo checked out to `main` with the fixed config computes `SemVer: "0.1.0"` correctly.

**Frontend coverage/test badges** (asked for alongside the CI fix): the `frontend` job previously only linted, type-checked, and built — no test step existed in CI at all, even though `npm run test-storybook` (Storybook stories run as real-Chromium Vitest browser tests) already worked locally and is documented in `docs/development.md`. Added coverage collection to `vite.config.ts` (`test.coverage`, root-level rather than per-project since Vitest's workspace/"projects" mode doesn't allow per-project coverage config) with a `json-summary` reporter for badge parsing, `test.reporters: ["default", "junit"]` for a JUnit report mirroring the backend's `pytest-report.xml`, and a CI step running `npm run test-storybook -- --coverage` with the same coverage-summary/job-summary/artifact-upload treatment the backend already gets. Frontend coverage starts low (~0.2%, honestly reported, not massaged) since only two component stories exist today and most page-level logic is instead covered by the Playwright E2E suite, which this number doesn't measure — noted explicitly in both the job summary and `development.md` so a future reader doesn't mistake it for "untested."

**A real, Vitest-specific gotcha found via local `act` runs** (see the new "Testing the pipeline locally with `act`" section in `development.md`): `npm run test-storybook -- --coverage` reliably completed its actual work — every test ran, the coverage report and JUnit XML were both written correctly — and then never exited, hanging the step (and, unbounded, the whole job) indefinitely. Two `act`-specific causes were ruled out and fixed along the way (a plain `docker run` container's tiny default `/dev/shm` crashing headless Chromium — `--shm-size=2gb`; no init process to reap the crashed browser's zombie child — `--init`), neither of which affects real GitHub-hosted runners (full VMs, not nested containers) and so aren't part of `ci.yml` itself. But even with both fixed and the browser no longer crashing, the parent `vitest` process still didn't return control — a real `@vitest/browser` + Playwright-provider quirk, reproduced consistently, not an artifact of the container. Fixed at the CI-step level: wrapped the command in `timeout 180`, and on a nonzero exit, parse `vitest-report.xml` directly rather than trusting the process's own exit code — a real per-test failure (any `failures`/`errors` in the JUnit output) still fails the step, but a hang-after-success is caught and treated as the pass it actually was. Chose this over chasing further `act`/Vitest configuration, since the actual test/coverage output was already provably correct both times it was reproduced.

**Badge-publishing race, found while wiring the frontend badges up, not by observing a real failure yet.** The backend's existing badge-publish step force-pushes a fresh, from-scratch git history straight to the `badges` branch from *inside* the `backend-and-e2e` job. Adding an equivalent step to the `frontend` job — which runs in parallel, not sequentially — would have meant both jobs independently force-pushing over the same branch with no knowledge of each other, so whichever finished last would silently wipe out the other's badge files depending purely on scheduling, not a change either side made. Fixed by moving badge publishing out of both jobs entirely into a new `publish-badges` job gated on `needs: [frontend, backend-and-e2e]`, which downloads both jobs' raw coverage/JUnit artifacts, generates all four badge files together, and — a further improvement over the original mechanism, not just a race fix — fetches whatever's already on `badges` first and only overwrites the files it has fresh data for, so a run where only one side produced a report (e.g. the other job failed before getting that far) updates just that side's badges instead of blanking the other side's most recent ones. Badge JSON filenames also gained explicit `backend-`/`frontend-` prefixes (`backend-coverage-badge.json` etc., previously unprefixed for the backend alone) for symmetry; the README's four badges were updated to match.

**Verified**: `GitVersion.yml`'s fix confirmed against the real GitVersion 6.8.2 tool (Docker image), both the failure reproduction and the fix's correct `0.1.0` output on `main`. The frontend test/coverage step confirmed locally (`npm run test-storybook -- --coverage`, correct coverage % and JUnit output) and via `act` (`act push -j frontend -P ubuntu-latest=catthehacker/ubuntu:act-latest --container-options "--shm-size=2gb --init"`), which is what surfaced and let this pass verify the exit-hang fix for real rather than only reasoning about it. `act -l` confirms the updated job graph (`frontend`/`backend-and-e2e` → `publish-badges`/`docker-build`) parses correctly. The `publish-badges` job's actual push mechanism (fetch-then-selectively-overwrite) was not run against the real `badges` branch as part of this pass — its logic was reasoned through carefully and matches the existing, already-proven force-push pattern closely enough to ship, but is worth watching on the next real `main` push.

## Bundle export/import: requirement CSV, project bundles, organisation bundles

Three related capabilities, requested directly rather than derived from `docs/requirements.md`: full-fidelity requirement CSV export/import, project export/import (structure + full history, for migrating a project between organisations), and organisation export/import (for backup, offboarding, and cross-deployment migration). None is mandated by an existing requirement ID — R-F-02 only requires CSV output of the *generated report table*, not round-trip fidelity or import, and no ID covers project/org-level portability at all — so this is flagged here explicitly as a capability beyond the documented milestones, per this project's governance (`docs/requirements.md` itself is unmodified).

**Scope, confirmed with the user before building:** full history (change requests with their versions/tasks/votes/comments, baselines, review outcomes, requirement version history, and structural audit events), not just current-state snapshots; file attachments embedded in the export bundle rather than deferred; all three built in one pass, with the org bundle reusing the project bundle's per-project logic (`services.project_export.collect_project_data`/`apply_project_data`, refactored out of `build_project_bundle`/`import_project_bundle` specifically so `services.org_export` could call them per-project without "zip one project, then unzip it back out").

**Security decisions** (flagged per the change-management policy's identify → verify → remediate requirement for anything touching secrets or multi-tenant isolation):

- **Restricted secrets are never exported.** `Organization.smtp_password`/`oidc_client_secret` (both `EncryptedString`) are excluded from every bundle entirely, per `docs/soc2/policies/data-classification-and-confidentiality-policy.md`'s rule 2 ("Restricted data is never returned in API responses or logs") — the cleanest option consistent with an already-audited control rather than a new bundle-level encryption scheme. Non-secret SMTP/OIDC *configuration* (host, client id, issuer URL) is still exported for the operator's reference; the secrets themselves must be re-entered manually post-import. `User.password_hash`/`totp_secret`, `PersonalAccessToken.token_hash`, and `PendingInvite.token` are likewise never carried over — a password hash is bound to a specific bcrypt work factor/salt but not to a tenant boundary, so reusing it cross-deployment would itself be a credential-leakage concern, not just a tenant-isolation one.
- **An imported organisation's `sso_enabled`/`sso_only` are always forced `False`**, regardless of the source bundle's values, precisely *because* the OIDC secret above is never carried over — importing `sso_only=True` verbatim with no working secret would leave the new org's login page unusable (no working SSO, native login form hidden) except via the existing narrow server-admin `join-as-admin` carve-out. The source values are still exported for reference when manually reconfiguring SSO; a warning is added at import time if `sso_only` was set.
- **Project-level group *membership* (`ProjectGroupMember`) is never recreated on import, in either bundle kind** — only the group *structure* (name/role) is. This was caught before shipping, not after: the initial design replayed member emails the same way every other reference is resolved (match an existing account by email, or invite), which would have let an account with no prior relationship to the target organisation gain project access purely because its email happened to match — a privilege-escalation-via-import path, most visibly for a cross-organisation project import. Member emails are still included in the export for a human to re-populate manually; the importing user is always guaranteed project-manager access instead (`apply_project_data`'s "ensure at least one manager" fallback).
- **Organisation-level membership (`UserOrgRole`, `OrgGroupMember`) *is* granted for real on org import** — deliberately different from the project-level decision above, because an org import always creates a brand-new organisation from scratch (never merges into an existing one): there is no existing tenant boundary being crossed by populating the new org's own initial membership, the same way `services.bootstrap` grants the very first server-admin org membership on deployment. A member with no existing account is invited (`services.invites.create_pending_invite`) rather than granted their original role directly, since invite acceptance only ever grants base membership — a warning records that their original role (e.g. `org_admin`) needs a manual re-grant once they sign up.
- **Org export requires genuine `org_admin` in that org — no server-admin bypass** (I-M-05's established "server admin doesn't get org-scoped data access for free" pattern). An operator backing up an org they don't belong to uses the existing self-service `POST /orgs/{id}/join-as-admin` first, rather than this feature adding a new bypass. Org *creation* (plain and from-bundle) stays server-admin-only, matching `POST /orgs`.

**A real bug found via end-to-end testing, not just reasoned about (identify → verify → remediate):** the "ensure at least one project manager" fallback (needed precisely because project group membership is never replayed, above) originally attributed to whichever user was performing the import. For a *project* import that's the actual importer — appropriate. For an *org* import, though, the person performing the import is a server admin acting as a platform operator (`POST /orgs/import` is server-admin-only) — attributing every project's manager fallback to them left the server admin as a standing project manager of every single imported project, an unintended and unnecessary access grant. **Identified** via a Playwright spec asserting the org's real (invited-then-matched) admin could see the imported project; the assertion instead showed the *server admin* holding all four project roles on it. **Verified** directly against the running API (`GET /projects` as the server admin, confirming the stray roles; `GET /orgs/{id}/users` as the server admin against both the source and imported orgs, confirming they held zero role in either once the fix was applied). **Remediated**: `import_org_bundle` now tracks the first successfully-matched `org_admin` member as an `acting_user`, and every per-project fallback (the manager guarantee, plus every unresolved-reference fallback the shared `UserResolver` applies) attributes to that person instead of `current_user` — falling back to the server admin only if literally no org_admin member could be matched (e.g. every member needed an invite). The org-level audit event (`"organization"`/`"imported_from_bundle"`) still correctly attributes the platform action itself to the real importer.

**Two more bugs caught by tests before merge, worth recording since they'd have silently broken every export with custom fields or attachments:** (1) `RequirementVersion.custom_fields`/`ChangeRequestVersion.custom_fields` are JSONB dicts keyed by the *stringified* `CustomFieldDefinition` id — the first export implementation built its id→name lookup keyed by the raw UUID object, so `key in lookup` silently never matched and every custom field value was dropped from every export. (2) Exported attachment `file_ref`s were written as the bare `FileAsset` id, but the zip entry itself is written as `{id}_{filename}` — import's dict lookup by bare id never found the corresponding zip entry, so every attachment would have silently failed to import. Both caught by the round-trip assertions in `backend/tests/test_project_export_import.py` before either shipped.

**New files**: `backend/app/services/csv_safety.py` and `backend/app/services/downloads.py` (small shared helpers, extracted from `services/reports.py`/`routers/reports.py` so the CSV-injection guard and `Content-Disposition` filename sanitizer aren't duplicated by the new export endpoints), `backend/app/services/requirement_csv.py`, `backend/app/services/bundle_common.py` (the `UserResolver`/`BundleImportWarnings`/file re-upload helpers shared by both bundle kinds), `backend/app/services/project_export.py`, `backend/app/services/org_export.py`.

**Verified**: full backend suite — 397 passed (380 pre-existing + 17 new: 7 in `test_requirement_csv_export_import.py`, 9 in `test_project_export_import.py`, 8 in `test_org_export_import.py`, some tests covering multiple assertions each) — `ruff check .` clean, `tsc -b`/`eslint` clean on every touched frontend file. Three new Playwright specs (`csv-import-wizard.spec.ts`'s new export test, `project-bundle-export-import.spec.ts`, `org-bundle-export-import.spec.ts`) plus the existing suite, each confirmed individually and in combination against a fresh two-script reseed (`seed_e2e_dataset.py` + `seed_demo_data.py`). The full 41-spec suite showed a small, *different* 2-3 specs red on each of several repeated full-batch runs (never the same spec twice in a row, and every one of them passed cleanly in isolation immediately after a fresh reseed) — consistent with this suite's own documented pre-existing characteristic (see "Full-product QA pass" above: "the first attempt to confirm the fixes by re-running the full suite again without a fresh reseed... produced 9 failures in specs that had never failed before — an artifact of prior runs having left the shared database in states those specs never anticipated, not a regression"), not a regression from this work. One genuine gap in this session's own testing process, not the product: `badge-filters.spec.ts` depends on `seed_demo_data.py` specifically (a separate script from the e2e dataset), which was initially missed when reseeding between runs.

**Follow-up, from the actual PR run on real GitHub Actions**: `frontend` and the new `publish-badges` job both passed for real, confirming the `act`-based verification above transferred correctly to the genuine runner environment. `backend-and-e2e` failed on one test — `report-image-attachment.spec.ts` (pre-existing, not part of this pass) — with the report intro's markdown textarea still empty 5 seconds after uploading an image, rather than containing the expected `![pixel.png](attachment:<id>)` insert. Passed locally (3.4s total), so not a deterministic bug: `fileInput.setInputFiles(...)` only waits for the browser's own file-selection event, not for `RichTextEditor.tsx`'s `uploadImage` (an awaited `api.postFile` network round trip) that has to resolve before the markdown insert happens — a real timing race, the same class this whole pass has been finding all day, just apparently marginal enough that it had never actually lost the race against Playwright's default 5s assertion timeout until running on a real (more resource-constrained, shared 2-vCPU) Actions runner instead of a local dev machine. Fixed the same way as everywhere else in this pass: `page.waitForResponse` on the upload's actual POST, wrapped around `setInputFiles`, so the value assertion only ever starts once the upload has genuinely completed.

### Hardening pass: zip-bomb / oversized-upload guard on bundle and CSV import

A follow-up hardening and validation pass over the merged bundle export/import work above, per the change-management policy's identify → verify → remediate practice for anything touching multi-tenant isolation or secrets (`docs/soc2/policies/change-management-and-secure-development-policy.md`). Re-read every new service/router file end to end rather than re-trusting the prior pass's own account of itself.

**Identified**: no deployment-wide request/upload body size limit exists anywhere in this codebase (confirmed absent from `app.main`'s middleware, every Docker/nginx config, and `services.files.upload_file`) — a pre-existing, previously-undocumented gap. `POST /orgs/import`, `POST /projects/import`, and `POST .../requirements/import` all read the entire upload into memory (`await file.read()`) with no cap, and the two bundle-import endpoints additionally decompress a `zipfile.ZipFile` entry (`zf.read("manifest.json")`/`zf.read("project.json")`/`zf.read("org.json")`, plus every `files/*` attachment) with no bound on the decompressed size either — DEFLATE's ~1032:1 worst-case ratio means a compressed upload well under any sane size cap could still expand to many gigabytes in memory before a single field is validated. Project import is reachable by any org admin or project creator (not just a server admin), so this is a real resource-exhaustion path for a multi-tenant deployment, not just a server-admin-only concern — worth fixing as part of this branch's own hardening rather than filing away as "matches the rest of the codebase."

**Verified**: read `services.bundle_common`/`services.project_export`/`services.org_export`/`services.requirement_csv` in full end to end (not just the diff) to confirm the zip-slip/path-traversal question was already closed correctly (attachments are looked up by dict key off `zf.namelist()`, never extracted to a filesystem path, so no zip-slip risk) and that every new endpoint's authorization matches its stated intent (org export: `require_org_role(ORG_ADMIN)`, no server-admin bypass, matching I-M-05; org import: `require_server_admin`; project export: `require_project_manage`; project import: mirrors `POST /projects`'s org-admin-or-project-creator check; CSV import: `require_project_view` dependency plus the same internal `_require_edit_role(db, current_user, project_id)` call `POST` (create) uses — not a gap, just this codebase's established two-step pattern for view-scoped-dependency-plus-manual-edit-check).

**Remediated**: `services.bundle_common` gained `enforce_upload_size_limit` (rejects a raw upload over 200MB, checked before the zip is even opened) and `enforce_zip_uncompressed_size_limit` (rejects a bundle whose `ZipInfo.file_size` total across every entry exceeds 1GB, read from the central directory — no decompression needed — checked immediately after opening, before `.read()` is called on any entry). Both are wired into `project_export._read_bundle`, `org_export.import_org_bundle`, and `routers.requirements.import_requirements` (the CSV path, which has no decompression amplification but was equally unbounded). Five new tests (`test_import_rejects_a_raw_upload_over_the_size_limit`/`test_import_rejects_a_bundle_whose_declared_uncompressed_size_exceeds_the_cap` in both `test_project_export_import.py` and `test_org_export_import.py`, plus `test_import_rejects_a_csv_over_the_size_limit`) confirm both layers reject a real (small, legitimately-exported) bundle once the limit is monkeypatched down, rather than needing an actual multi-gigabyte payload to exercise. The broader, pre-existing gap — every *other* upload endpoint (avatars, requirement/comment attachments, org logo/login background) still has no size cap — is out of scope for this branch (it predates the bundle feature and isn't specific to it) and is now recorded explicitly in `docs/soc2/policies/information-security-policy.md`'s Known Gaps rather than left undocumented.

**Verified (full re-run after the fix)**: full backend suite — 402 passed (397 pre-existing + 5 new) — `ruff check app tests` clean. `tsc -b` and `eslint .` clean on the frontend (2 pre-existing warnings in `BrandingContext.tsx`/`ThemeContext.tsx`, both unrelated to this branch). The four bundle/CSV-import-adjacent Playwright specs (`csv-import-wizard.spec.ts` ×2, `org-bundle-export-import.spec.ts`, `project-bundle-export-import.spec.ts`) pass against a fresh two-script reseed, including the org-import spec's own uncommitted fix (asserting the imported project is visible to the bundle's real, invited-then-matched org admin — not the importing server admin's own "Projects" tab — which is the correct way to confirm the earlier `acting_user` privilege-escalation fix actually landed on the right person).

### Real data leak found via manual `/metrics` inspection: project/org ids in CORS preflight label values

Reported directly by the user (`curl http://localhost:8000/metrics` showed real project/org ids), not surfaced by the hardening pass above — a reminder that a design intent verified by reading the code (`metrics_middleware` labels with the matched route's *template*, specifically to avoid exactly this) can still be defeated by a code path that was never exercised while reading it.

**Identified**: `main.metrics_middleware` computed `path = request.scope.get("route").path if request.scope.get("route") else request.url.path` — correct for routed traffic, but `request.scope["route"]` is never set for a CORS preflight `OPTIONS` request, because `CORSMiddleware` recognizes and short-circuits a preflight (any `OPTIONS` request carrying `Access-Control-Request-Method`) with a synthesized response *before* FastAPI's router runs, even when the underlying path would otherwise match a real, registered endpoint. Every non-simple cross-origin request the SPA makes (essentially every authenticated API call, since it sends an `Authorization` header) triggers exactly one such preflight, so the `else` branch's raw-path fallback fired constantly, embedding real path parameters — project ids, org ids, requirement ids, wherever they appear in a URL — as literal Prometheus label values. Confirmed on the running dev stack: 1220 distinct `OPTIONS`-method time series carrying real ids, versus zero across every routed (non-`OPTIONS`) series. `/metrics` itself has no authentication (deliberately — see `docs/deployment.md`'s "typically scraped over the internal Docker network, not exposed publicly" and the corresponding gap now recorded in `docs/soc2/policies/information-security-policy.md`), so this was live tenant-identifier exposure to anyone who could reach the endpoint, not just a metrics-quality problem — though it's that too: an in-process `Counter`/`Histogram` label set is never garbage collected, so those 1220 series would only ever grow, one new permanent time series per unique path ever hit by a preflight.

**Remediated**: unmatched-route requests (a preflight, or a genuine 404) are now labeled with a fixed `path="unmatched"` instead of falling back to the raw path — closing the id leak and the unbounded-cardinality growth in the same change, at the cost of no longer being able to tell *which* unmatched path was hit (an acceptable trade: those requests never reached application logic, so there's nothing project-specific to attribute them to). Regression test (`test_metrics_never_embeds_real_ids_from_a_cors_preflight` in `backend/tests/test_coverage_gaps.py`) issues a real preflight against a real org id and asserts it never appears in `/metrics`, plus that the request lands in the `path="unmatched"` bucket.

**Verified**: reproduced the leak live before fixing (curl against a fake org id, confirmed it appeared verbatim in `/metrics`'s `OPTIONS` series), then confirmed post-fix that the same fake id never appears and lands under `path="unmatched"` instead. Full backend suite re-run after the fix: 403 passed (402 prior + 1 new) — `ruff check app tests` clean.

## Two user-reported bugs: server admin's project-list org picker leaking every org in the deployment, and notifications firing for a user's own actions

Both reported directly by the user in the same session as the `/metrics` leak above, continuing the same identify → verify → remediate practice.

### Project list org filter/picker showed every organisation to a server admin, not just their own

**Identified**: `ProjectListPage.tsx` populated its org filter dropdown and "new project" org picker from the plain `GET /orgs`, which has one deliberate server-admin bypass (`routers/orgs.py::list_organizations`, I-M-05's documented carve-out) meant for the platform-level org directory (`ServerOrganisationsPage.tsx`) — a server admin sees every organisation in the deployment there by design. Reusing that same call for "which orgs can I create a project in / filter by" was wrong: a server admin with no real membership in most orgs could pick one they hold no role in at all, and the org filter leaked the full server-wide directory into what's meant to be "orgs I can act within." Not a bug for ordinary users — the bypass only applies to server admins, so this was invisible until a server admin actually looked.

**Remediated**: `list_organizations` gained a `mine: bool = False` query param that opts out of the server-admin bypass and always returns only the caller's own memberships; `ProjectListPage.tsx` now calls `GET /orgs?mine=true`. The existing `{orgs.length > 1 && ...}` conditionals that hide the filter/picker when there's only one org needed no change at all — they were already correct, just fed the wrong data. Every other caller of plain `GET /orgs` (`ServerOrganisationsPage.tsx`, `OrgListPage.tsx`, `PreferencesPage.tsx`) is unaffected, since the param defaults to off. (`OrgListPage.tsx`'s nav "Org Management" entry has the same latent bug — its own docstring already claims "lists organisations the user belongs to" — but the user didn't report it and it's out of scope for this pass; worth a follow-up.)

**Verified**: two new backend tests (`test_list_organizations_mine_scopes_to_membership_even_for_server_admin`, `test_list_organizations_mine_is_a_noop_for_non_server_admins` in `test_audit_fixes.py`) and a new Playwright spec (`project-list-org-scoping.spec.ts`) exercising the real UI: a server admin self-elevated into two disposable orgs sees exactly those two (not the shared Alpha/Beta/Gamma orgs) in both controls, intercepting the actual `GET /orgs?mine=true` request the page fires rather than a separately-issued one; dropping to one org (via org deletion — self-service leave 409s here, since these disposable orgs' only admin is the server admin who created them) hides both controls entirely. Caught two of my own test-authoring mistakes before this was reliable: (1) the frontend Docker image has to be rebuilt too, not just the backend — the first run showed the *old* unfixed dropdown because only the backend container had picked up the change; (2) the spec's own cleanup used self-service leave, which 409s for exactly the reason the spec exists (server admin is the org's only admin) — switched to server-admin-only hard delete instead.

### Notifications fired for a user's own actions

**Identified**: `services.notifications.notify()` had no concept of "who triggered this" — whether a self-notification happened depended entirely on each call site remembering to exclude the actor. Two call sites already did (`add_comment` on requirements and change requests, via `get_subscriber_ids(..., exclude_user_id=current_user.id)`); at least 13 others didn't: a project manager submitting their own change request got "change request submitted"/"your input is requested" alongside every other PM/stakeholder; approving/rejecting a CR notified the creator even when creator and approver were the same person, and broadcast "requirements updated" to the whole project including the approver; a stage transition broadcast to every project member included whoever triggered it; granting/revoking a project or org role could self-notify if the actor targeted their own account. Two existing tests had unknowingly codified the bug as expected behaviour — `test_stage_approval_notifies_project_members` and `test_change_request_submit_and_decide_notify` both used a single actor (`admin_token`, already a project manager by construction) and checked *their own* notifications, so the self-notification was indistinguishable from "the broadcast works at all."

**A genuinely ambiguous case, checked with the user rather than guessed**: accepting a pending invite grants org/project access as a side effect of the recipient's own login (`services.invites._grant_invite`), and sends them a "You've joined" notification — technically self-triggered, but with no other way for the user to discover the grant (unlike the other cases, where the actor already sees the result of their action on screen). The user confirmed this one should keep notifying. `auth.py`'s "your password was changed" (a deliberate security tripwire — must reach the account owner precisely so a change made with a stolen session is still caught) and the two scheduled/system sweeps (`services.reviews.send_due_review_reminders`, `services.stages.auto_approve_overdue_stage_reviews` — no live actor to exclude) were left alone for the same reason: not every recipient-equals-actor case is the bug being reported.

**Remediated**: `notify()` gained an opt-in `actor_id: UUID | None = None` parameter — when it matches the recipient, no `Notification` row is created (returns `None`; nothing previously read the return value). Passed from `current_user.id` at the 13 real call sites (`change_requests.py`'s submit/approve/reject/comment, `requirements.py`'s comment, `orgs.py`'s role grant, and `projects.py`'s stage-transition broadcast — threaded through `_notify_stage_transition` from both its callers — group-member-add, role-assign (direct and by-email), and role-revoke); left absent at the 4 deliberate exceptions above. Opt-in rather than an automatic heuristic specifically so those exceptions don't need special-casing — they simply never pass `actor_id`.

**Verified**: fixed the two existing tests that had encoded the bug (now use a second, non-actor recipient to confirm the broadcast still works, plus an explicit assertion the actor themselves doesn't receive it) and found a third, `test_new_stage_entering_scoping_notifies_members` in `test_audit_fixes.py`, the same way — caught only on a full suite re-run, not by reasoning alone. Added five new tests: revoke/grant self-suppression at both the project and org level, and a companion test confirming a *different* CR creator still gets notified when someone else decides their CR (proving the fix only suppresses the same-person case, not the notification generally). Full backend suite: 409 passed (403 prior + 6 new: 2 org-scoping, 4 notification). `ruff check app tests` clean; `tsc -b`/`eslint .` clean (same 2 pre-existing, unrelated warnings as every prior pass). Six Playwright specs re-run against a genuinely fresh reseed (`project-list-org-scoping.spec.ts`, `notifications-and-favourites.spec.ts`, `change-request-approval-separation.spec.ts`, `change-request-tasks-and-voting.spec.ts`, `stage-review-and-completion.spec.ts`, `multi-org-admin.spec.ts`) — all pass. One of those runs surfaced a pure test-process mistake rather than a product bug: reseeding via `docker compose restart backend` plus the seed scripts doesn't actually reset anything if the tables were never truncated first (the seed scripts are idempotent and silently skip when data already exists) — only a real pytest run truncates them; a spec re-run against a stack that was merely restarted, not truly reseeded, saw a previous run's already-completed stage and failed for reasons that had nothing to do with either fix.

### CI went red on the real PR run: the org-scoping fix's own side effect, plus one unrelated pre-existing collision, neither caught by the narrower local spec selection above

The user linked the actual failing `backend-and-e2e` job (PR #2, run 31550244289) and asked whether it might be a lock-file issue needing a regenerate step. It wasn't — both install steps (`npm ci` for frontend, pip for backend) succeeded cleanly; the job failed at "Run Playwright E2E suite" with 4 real test failures, none lock-file-related. This pass's own local validation had run 6 hand-picked specs, not the real full 42-spec suite CI actually runs — a real gap, since 3 of the 4 failures were a direct, entirely foreseeable consequence of the org-scoping fix that this same local validation had already exercised in isolation, just not against every other spec that happens to share the same assumption.

**Identified**: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, and `pelion-v2.spec.ts` (twice) all created their project via the bootstrap `admin@example.com` account and unconditionally selected "Default Organization" from what they assumed was always the first `<select>` on the "New project" panel — a leftover assumption from before this pass's fix, when a server admin's org picker showed every organisation in the deployment (including E2E seed orgs the admin was never a member of), so an explicit selection was needed to land on the right one. Post-fix, `admin@example.com` correctly has exactly one real org membership, so the picker is now correctly absent entirely (`orgs.length > 1` in `ProjectListPage.tsx`) — and the "first combobox" these specs blindly grabbed became the *role* filter instead, whose static "All roles..." text obviously never contains "Default Organization". A fourth, unrelated failure in `project-admin-groups-and-fields.spec.ts` turned out to be a pre-existing collision predating this session entirely: `csv-import-wizard.spec.ts` creates its own `E2E Priority <timestamp>` custom field on the same project (Beta-2), and `project-admin-groups-and-fields.spec.ts`'s plain `getByText("Priority")` substring-matches both when both specs land in the same batch run — a latent test-isolation gap in the already-merged bundle-import work, surfaced only because this run happened to schedule both specs together.

**Remediated**: all three "select the org explicitly" call sites now check for the picker's actual presence first (`select:has(option:text-is('Default Organization'))`.count() > 0) before selecting from it, rather than assuming it's always there — correct in both the single-org case (picker absent, org implicit) and a hypothetical future multi-org bootstrap admin (picker present, selects as before). `project-admin-groups-and-fields.spec.ts`'s two ambiguous locators became an anchored regex (`/^Priority\b/`) that matches its own field's label ("Priority *Select…LowMediumHigh") but not "E2E Priority", independent of what other specs have run before it in the same batch.

**Verified**: reproduced the exact CI failure signatures by reading the job's log directly (`gh run view --log-failed`) rather than guessing from the spec diffs alone. Full 42-spec suite run locally against a genuinely fresh reseed: 41 passed, 1 failed (`requirements-and-cr-filters.spec.ts`, an assertion on persisted view-mode state) — re-run in isolation against another fresh reseed, passed cleanly, matching this suite's own already-documented characteristic (see "Full-product QA pass" and the bundle-export/import entry above: a handful of specs going red only in a full shared-DB batch run, never in isolation, is expected here, not a regression). All 4 originally CI-reported failures confirmed fixed.

## QA team-lead pass: full-product review and Storybook coverage build-out

The user asked for a QA-team-lead-style pass over the entire frontend: review every page/component, build out Storybook coverage toward 95%, query anything unexpected, and confirm business logic is enforced in the backend rather than duplicated incorrectly in the frontend. An inventory pass first read every one of the 22 pages and 20 shared components plus the in-app help docs (`frontend/src/help/*.md`), rbac.py, and the two components that already had stories (`Spinner.tsx`, `CustomFieldsForm.tsx`) to establish the existing story conventions before writing new ones.

**Identified**: three genuine findings, none security-critical:
1. `OrgAdminPage.tsx`'s Advanced tab let an admin tick "Allow self-signup" while the same organisation was `sso_only`, with no inline warning — the backend already rejects the combination (`test_update_advanced_settings_rejects_self_signup_for_sso_only_org`, `test_update_sso_config_rejects_sso_only_while_self_signup_enabled`, and documented as mutually exclusive in `docs/soc2/policies/access-control-policy.md`'s Provisioning section), so this was a UX gap, not a compliance one — the admin only found out via a 422 after saving.
2. `frontend/src/help/06-user-access.md`'s external-user-provisioning mermaid diagram had an unescaped `"` inside a flowchart node label (`Email just says "sign in via SSO"`), which is malformed Mermaid syntax — invisible until a story actually exercised `mermaid.run()` in a real browser, since a broken diagram fails silently in production (no console-visible crash the way a React error would). Caught building `HelpPage.stories.tsx`, which is exactly the kind of regression `CLAUDE.md`'s "no malformed syntax" diagram-validation rule exists to catch, and confirms the value of real Chromium-backed Storybook tests over static review.
3. `CollapsibleSection.tsx`'s expanded-state header wrapped its (arbitrary ReactNode) `title` in a real `<button>`. `PreferencesPage.tsx`'s two-factor section nests a real interactive `ToggleSwitch` (itself a `<button role="switch">`) inside that title, producing an invalid `<button>`-inside-`<button>`, surfaced as a React `validateDOMNesting` console warning while exercising `PreferencesPage.stories.tsx`. Nested interactive elements are invalid HTML with unpredictable click/keyboard-focus behaviour across browsers — `ToggleSwitch.tsx` already documents having added `stopPropagation()` specifically to survive being nested this way, which was a workaround for symptoms rather than the underlying invalid markup.

Also confirmed, not a bug: four pages (`RequirementDetailPage.tsx`, `ChangeRequestDetailPage.tsx`, `RequirementsPage.tsx`, `PreferencesPage.tsx`) re-derive role-based UI visibility (`canEdit`/`canArchive`/`canDecide`/`canManageTasks`/`canVote`/`canManageProject`) in JS from `useMyProjectRoles`, mirroring `rbac.py`'s role hierarchy. This matches `useMyProjectRoles.ts`'s own documented rationale ("the backend always enforces this regardless, but a control that 403s on click isn't a working workflow") and is exactly the shape business-logic enforcement should take: the backend is the sole source of truth, the frontend only mirrors it for UX. The real risk is silent drift between the two if `rbac.py`'s hierarchy changes without the frontend copies changing to match — addressed below, not by removing the frontend checks. `ReportsPage.tsx`'s `chaptersToMarkdown`/`effectiveContentFor` similarly and deliberately mirror `services/reports.py::_chapters_markdown`/`resolve_report_config_with_template`, already documented as intentional in that file's own comments.

**Remediated**:
1. `OrgAdminPage.tsx` now computes `selfSignupConflict = allowSelfSignup && ssoOnly` and, when true, shows an inline warning (new `strings.orgAdmin.selfSignupSsoConflict` string) and disables the Advanced tab's Save button — surfacing the existing backend rule before the round trip instead of only after. No backend change; the existing rejection test coverage is unchanged.
2. The malformed mermaid node label was reworded to avoid embedded quotes (`Email just says: sign in via SSO`), fixing the diagram without changing its meaning.
3. `CollapsibleSection.tsx`'s expanded-state header now uses the same `<div role="button" tabIndex={0}>` + `onKeyDown` (Enter/Space) pattern its own collapsed-state branch already used, instead of a real `<button>` — eliminates the invalid nesting for any current or future `title` containing an interactive element, with identical keyboard/screen-reader behaviour.
4. Storybook coverage was built out from 2 files (`Spinner.tsx`, `CustomFieldsForm.tsx`) to all 42 pages/components, added in four batches (pure/presentational components → API/context-dependent components → simpler pages → the eleven largest/most complex pages, including role-permutation stories for the four convenience-permission pages above, pinning their button visibility against `rbac.py`'s hierarchy so drift between the two breaks a story rather than shipping silently). New shared test infrastructure: `frontend/src/testing/storybook-helpers.tsx` (fixture builders; `withAuth`/`withStatefulAuth`/`withTerminology`/`withRouter` decorators — `withStatefulAuth` specifically for stories exercising a `useUiPreference`-backed control, where a no-op `setUiPreference` would silently no-op the interaction under test) and `frontend/src/testing/StatefulAuthProvider.tsx` (split out purely to keep `storybook-helpers.tsx` free of a real component export, which would otherwise break Fast Refresh). `AuthContext`/`ProjectContext` (`TerminologyContext.tsx`'s internal context) were each split into a small dedicated `*Value.ts`/`*Value.tsx` file for the same Fast-Refresh reason, so stories can supply fixture values via `<Context.Provider>` directly.

**Verified**: `npm run lint` and `npm run build` clean throughout (no new warnings introduced — the two pre-existing `react-refresh/only-export-components` warnings on `BrandingContext.tsx`/`ThemeContext.tsx` are unchanged and were already documented as accepted). Final `npm run test-storybook -- --coverage` run: 254 tests across 43 story files, all passing; line coverage rose from a pre-pass baseline (2 files covered) to **62.58%** (statements 59.73%, branches 56.25%, functions 46.78%) — short of the 95% target. The shortfall is concentrated in the largest, most state-heavy pages (`OrgAdminPage.tsx` 43.65%, `ProjectAdminPage.tsx` 44.57%, `ChangeRequestDetailPage.tsx` 46.36%, `RequirementDetailPage.tsx` 53.5%), where each additional percentage point requires a dedicated story per edge case (every delete-confirmation dialog, every tab × role combination, every validation branch) rather than a handful of stories reaching most of the file the way the smaller pages did; `App.tsx` (routing/provider wiring) remains at 0%, which is a poor Storybook fit and is already covered by the Playwright E2E suite instead. Getting from here to 95% is a real, boundable follow-up (systematically enumerating the remaining branches file-by-file), not a fundamentally different exercise, and the existing 43 files establish the mocking patterns (`spyOn(api, ...)`, role-permutation fixtures, stateful-vs-stateless auth) the remaining coverage would reuse directly.

## Organisation rename, and a "send test email" action for both deployment-wide and per-org SMTP

User request: organisations should be renameable, and there should be a "test email" action for both server-wide SMTP and an organisation's own SMTP override. Two small, independent features, both org/deployment-property management in the same vein as the branding/advanced-settings endpoints already there.

- **Rename is its own endpoint (`PUT /orgs/{id}/name`), not folded into `update_org_branding` or `update_advanced_settings`.** `Organization.name` previously had no update path at all post-creation. A dedicated endpoint (payload: `OrganizationRename`, reusing `OrganizationCreate.name`'s exact non-blank/whitespace-trim validator, since a blank name would break `DELETE /orgs/{id}`'s "type the exact name to confirm" gate the same way `OrganizationCreate`'s own docstring already explains) keeps each org-property endpoint doing one thing, matching the existing `/branding`, `/default-template`, `/advanced-settings` split rather than growing one of them an unrelated field. `require_org_role(ORG_ADMIN)`, no server-admin bypass — same I-M-05 scoping as every other org-property endpoint; C-U-01's own clarification ("Organisational Admins can manage properties of the organisation") is the basis for org-admin (not server-admin) being the right authority here. The frontend inline-edit control (an `<input>` + "Rename" button next to the `<h1>`, appearing only once `GET /advanced-settings` has confirmed the caller is actually an org admin — the same "does the privileged GET succeed" gate the Advanced Settings section itself already uses to hide from non-admins) mirrors the existing rename pattern for stages/components/categories in `ProjectAdminPage.tsx` (edit the input in place, a pencil-icon button appears once the value differs from the saved one) rather than inventing a new interaction shape.

- **The SMTP-override storage-only seam (see this file's "Mockup UI alignment pass" entry above) is now partially wired, deliberately narrowly.** `services/email.py::send_email`/`send_email_async` gained an optional `smtp_override: SmtpOverride | None` keyword (a small dataclass: host/port/username/password/use_tls) — when given, the deployment-wide `Settings.smtp_*` are bypassed entirely for that one call; when omitted (every existing call site: `services/notifications.py`, the daily digest, `services/disk_monitor.py`), behaviour is byte-for-byte unchanged. The override is deliberately all-or-nothing rather than merged field-by-field with the deployment defaults — mixing an org's own host with the deployment's MailHog port would silently produce a nonsensical connection target, and `aiosmtplib.send` already picks a sensible default port from `use_tls`/`start_tls` when an override's own `port` is `None`, so there's nothing to gain from partial merging. The *only* caller that ever passes an override is the new `POST /orgs/{id}/test-email` (org-admin only, 400 if `Organization.smtp_host` isn't set yet, 502 with the underlying error text if the send itself fails — surfacing *why* is the entire point of a test-email action, unlike every other `send_email` call site, which already deliberately swallows delivery failures behind a broad `except Exception` so a downstream mail outage never breaks the triggering request). Ordinary notification email still always goes through the deployment-wide relay; this does not "finish" the seam for SSO group-sync or for routing real notifications through an org's own relay — those remain exactly as storage-only as before, and the seam's docstring plus the entry above were both updated to say so, so a future reader doesn't assume more got wired than actually did.

- **The deployment-wide equivalent (`POST /system/test-email`, server-admin only) is a separate endpoint in `routers/system.py`, not a `smtp_override=None` special case of the org one.** The two have different authorization (`require_server_admin` vs. `require_org_role(ORG_ADMIN)`) and different preconditions (the deployment SMTP config always exists via `Settings` defaults — MailHog in dev — so there's no "not configured yet" 400 case to share). Both share the same request shape (`schemas/email.py::TestEmailRequest`, an optional `to_email` defaulting to the caller's own account) and the same 502-with-reason failure handling, so the shared piece is factored into one schema rather than one endpoint trying to serve two different authorization/precondition stories.

**Verified**: 11 new backend tests (`backend/tests/test_org_rename_and_test_email.py`) covering rename (success, blank-name rejection, non-admin 403), and both test-email endpoints (default-to-caller and explicit-recipient success with `send_email` mocked and its `smtp_override` argument asserted, 403 for the wrong role, 400 for an org with no SMTP host, and 502-with-the-underlying-reason on a simulated send failure) — full backend suite re-run inside the real dev/test container (where the `mailhog` hostname the deployment-wide default points at actually resolves): 409 passed, 0 failed. `ruff check app tests` clean. Frontend: `tsc -b`, `eslint .`, and `npm run build` all clean; new Storybook stories (`RenameOrganization`, `AdvancedSettingsTestEmailNoSmtpConfigured`, `AdvancedSettingsTestEmailSend` in `OrgAdminPage.stories.tsx`; `TestEmailTab`, `TestEmailSendSuccess`, `TestEmailSendFailure` in `ServerManagementPage.stories.tsx`) pass alongside the existing suite. New Playwright spec `org-rename-and-test-email.spec.ts` verifies both features against a real, freshly rebuilt/reseeded dev stack — rename via the UI, persisting across reload; the org test-email button starting disabled with an explanatory hint until an SMTP host is saved; and, for both the org-scoped and deployment-wide test-email actions, real delivery confirmed through MailHog's HTTP API (`GET /api/v2/messages`, the same verification approach `external-project-invite.spec.ts` already established) rather than only asserting the UI shows a success message. One test-authoring mistake caught and fixed before this was reliable: the disposable org-admin persona's `display_name` ("Rename Org Admin") collided with `getByLabel("Rename")` against the nav rail's own "{name} — Preferences" link accessible name — renamed the fixture persona and added `exact: true` to the locator. A full batch re-run of six other org-related specs alongside the new one surfaced one pre-existing flake (`org-security-controls.spec.ts`, unrelated to this change — no file this pass touched) that passed cleanly both in isolation and on a second batch run, matching this suite's own already-documented "a handful of specs go red only in a full shared-DB batch run, never in isolation" characteristic (see the CI-failures entry above) rather than a new regression.

## MCP server write mode

The user requested write access for `mcp-server/` — specifically so requirements can be authored by an AI assistant, not read-only browsing only — with one explicit, non-negotiable exclusion: no tool may ever approve a requirement or decide a change request, because ReqTrackManager's approval workflow is only meaningful if every approval is a deliberate human action taken with real accountability, in the UI. The user also recorded, as a standing product-strategy decision (not just scope for this one change): **MCP-based tool access is this product's preferred, and currently only, mechanism for AI to interact with the system — deliberately instead of building an AI backend, model integration, or inference pipeline into the product itself.** That's now captured in [solution-architecture.md](solution-architecture.md)'s "AI Integration Layer" section as an explicit architecture decision, not left implicit in the fact that `mcp-server/` happens to be the only AI-shaped thing in the repo.

- **Off by default, opt-in per deployment (`MCP_WRITES_ENABLED`), and gated at tool-registration time, not at call time.** `create_requirement`/`update_requirement` are defined inside `if MCP_WRITES_ENABLED:` in `server.py` — when the flag is unset, those two tools don't exist in the MCP tool list at all, rather than existing and refusing every call. This is a stronger, simpler guarantee than a runtime check inside each tool, and it means an MCP client's own tool-discovery response already tells the truth about what this deployment allows before any call is attempted. The bundled dev/test stack (`tests/container/docker-compose.yml`) sets it to `true` by default so CI's `mcp-server` pytest suite actually exercises the write path; the production `docker-compose.yml` at the repo root defaults it to `false`, requiring an explicit operator decision to turn writing on at all — matching the user's own framing of this as "an option to support writing," not a default-on capability.

- **Approval is excluded structurally, not by convention.** `update_requirement` has no `status` parameter in its function signature at all — not "a `status` parameter that rejects `'approved'`," but no parameter for it whatsoever, so there is no way to smuggle a status transition through this server's MCP tool-call schema regardless of what the calling account's own project role could do directly via the API (a Project Manager's PAT used through this server still can't self-approve through it). This was a deliberate choice over relying on the backend's existing RBAC (`_require_edit_role`/the PM-only check in `update_requirement`, `backend/app/routers/requirements.py`) to gate it: RBAC answers "is this account allowed to approve," which is the right question for the UI and the REST API, but not the right question for *whether an AI assistant should ever be offered the button at all* — those are different concerns, and conflating them would mean a future backend RBAC change could silently reopen this specifically-excluded capability through the MCP server without anyone deciding that on purpose. `decide_change_request` (`backend/app/routers/change_requests.py`) and `record_review_outcome`/`complete_requirement` (`backend/app/routers/requirements.py`) are likewise never wrapped by any tool, in either mode — not "wrapped but blocked," simply absent. A new test, `test_no_tool_can_ever_approve_or_decide_anything` (`mcp-server/tests/test_server.py`), asserts no tool name in the discoverable set (in either mode) contains `approve`/`decide`/`complete`/`review_outcome`/`vote`, as a regression guard against this boundary eroding by accident in a future change rather than by deliberate revisiting.

- **Scope for this first cut is requirement content only — create and edit, not the full CRUD/workflow surface.** Considered and deliberately left out for now (each a natural, larger follow-up, not ruled out on principle the way approval is): submitting a change request (proposing an edit to a *locked* requirement — `update_requirement` already correctly 409s on a locked requirement with a message pointing at the UI's change-request flow instead of silently failing), archiving/deleting a requirement, adding comments, uploading attachments, and creating traceability links. Narrowing to exactly what the user asked for ("requirements can be written") kept the change reviewable and the guardrail surface small; broadening later is straightforward since the pass-through-auth design means each addition is "one more thin wrapper around an existing, already-tested REST endpoint," not new plumbing.

- **`update_requirement` is a partial update that fetches-then-merges, even though the underlying `PUT` is a full replace.** The REST endpoint (`RequirementUpdate`) requires every content field on every call — exactly what the existing frontend form already does, re-submitting its fully-loaded state each time. An AI caller doesn't have a form holding that state, and forcing it to always pass every field (having first called `get_requirement` to learn them) is both extra round-trips and a real risk: forgetting one field on a real edit would silently blank it. So `update_requirement` itself calls `GET .../requirements/{id}` first and only overwrites the arguments the caller actually passed, sending the rest through unchanged — `keywords`/`custom_fields` are the one deliberate exception (full-replace when given, matching `set_keywords`' own storage semantics, documented in the tool's own docstring) since "add one keyword" vs. "replace the whole set" is genuinely ambiguous and guessing wrong either way is worse than requiring the caller to send the full set. This also means `review_date`/`review_lead_days`/`reviewer_id` can't be explicitly *cleared* through this tool (a missing argument and "clear this" both look identical) — documented as a known gap in the tool's own docstring rather than solved with a sentinel-value scheme, since the UI already covers that case and it wasn't part of what was asked for.

- **`_call_backend` gained a `json` body parameter and clearer 400/409/422 surfacing (`_detail()`, extracting FastAPI's `{"detail": "..."}` shape instead of dumping raw response text).** Every prior tool was `GET`-only, so this plumbing didn't exist yet; both new tools need it, and the clearer error text specifically matters for the locked-requirement 409 case, where the AI assistant (and, through it, the user) needs to actually understand *why* the edit was rejected and what to do instead, not just see an opaque JSON blob.

**Verified**: `mcp-server/tests/test_server.py` — `test_tools_are_discoverable` now asserts the tool set conditionally on `MCP_WRITES_ENABLED` (mirroring `server.py`'s own env parsing rather than importing its constant, since the test process and the running server are separate processes/containers); five new tests cover write-mode's actual behaviour end to end against a real backend: `create_requirement` produces a genuine draft requirement with the fields it was given; `update_requirement`'s partial-update merge leaves untouched fields exactly as they were; attempting to pass `status` to `update_requirement` fails before reaching the backend; editing a requirement locked by directly approving it via the real REST API (proving no MCP tool did the approving) is rejected with a clear conflict message; and the approval-tool-name regression guard described above. `_create_test_project` (mirroring the existing `_create_test_org` helper's pattern) creates a disposable project/component/category per test via the real API so these don't depend on E2E/demo seed data being loaded. `ruff check .` clean in `mcp-server/`. One test-authoring mistake caught and fixed before this was reliable: the initial guardrail test flagged its own substring match too bluntly (`"vote" in name` tripped on the legitimate read tool `list_change_request_votes` — listing the advisory tally, not casting a vote) — narrowed to checking each tool name's *leading* verb (the part before the first `_`) against a forbidden set, plus a separate substring check for `review_outcome`, which correctly leaves every `list_*`/`get_*` read tool alone. Full mcp-server pytest suite rebuilt and run against the real dev/test stack (`tests/container/docker-compose.yml`, MailHog/Postgres/backend all real, no mocking) in both configurations: with `MCP_WRITES_ENABLED=true` (the stack's new default), 23 passed, 1 skipped (an existing, pre-written skip for a read tool needing pre-existing change-request seed data this run's throwaway projects don't have — unrelated to this change); with it explicitly set to `false`, the discoverability and approval-guardrail tests confirm `create_requirement`/`update_requirement` are genuinely absent from the tool list, not merely refusing calls. `docker compose exec mcp-server ruff check .` clean throughout.

## HTML email template system, with org/platform branding and a one-click unsubscribe link

User request: every outgoing email (instant notifications, the daily digest, the org/system "send test email" actions, the disk-usage alert) was plain text with zero branding. Wanted a real HTML template system per a supplied design brief (clean modern SaaS email, light/dark theme support, self-contained embedded images, one clear CTA, understated footer with legal/company info and an unsubscribe link), with the default header/logo area specifically "the platform icon, the header title on a black background", brandable per organisation the same way the UI already is, falling back to a platform default. Two explicit follow-up requirements: Jinja2 (the templating engine chosen for this) had to be used safely given its real CVE history, and every one of the footer/branding fields had to be overridable per-org, not just platform-wide.

- **Jinja2 pinned to 3.1.6, autoescape always on, and — the actual mitigation — templates are never built from a runtime string.** The user cited a specific CVE number that turned out not to exist in NVD/Snyk/any vendor advisory; rather than chase an unverifiable ID, this was designed against the real, confirmed Jinja2 CVE history instead (2024-22195/34064 `xmlattr`-filter XSS, 2024-56201/56326 sandbox escapes, 2025-27516 sandboxed `|attr` bypass — all fixed by 3.1.6). All four content templates load exclusively from static files on disk via `FileSystemLoader` (`backend/app/templates/email/`); nothing ever calls `Environment.from_string` with org-/user-supplied text, which is what actually rules out the server-side template injection class those sandbox-escape CVEs depend on (they all require the *template source* to be attacker-influenced, not just a variable's value) — so a `SandboxedEnvironment` isn't needed either, there's no path for a caller to supply template code at all. No `|safe`/`Markup(...)` is used on any org-/user-supplied value; the one legitimate near-miss — turning `email_footer_address`'s embedded newlines into `<br>` — is a custom `nl2br` filter (`services/email_templates.py`) that escapes the raw value *first* and only marks the already-escaped, safe result `Markup` afterwards, never the reverse. A dedicated regression test (`test_email_templates.py::test_notification_email_escapes_org_and_notification_supplied_content`) renders every user-controlled field (org header title, footer company name/address, notification title) with `<script>`/`onerror=` payloads and asserts the raw payload never appears unescaped in the output — verified passing before this was considered done.

- **Branding resolution reuses the existing org-overrides-platform-default two-tier shape verbatim, extended to three new fields, per the user's follow-up.** `resolve_email_branding` (`services/email_branding.py`) resolves the header logo/title/accent colour from `Organization.logo_file_id`/`header_title`/`accent_color_hex` falling back per-field to `ServerSettings` — exactly what `BrandingContext.tsx` already does client-side for UI chrome, mirrored server-side since email renders in the backend. Three new fields, `email_footer_company_name`/`email_footer_website`/`email_footer_address`, were added to *both* `Organization` and `ServerSettings` with identical null-means-inherit semantics (not platform-only, which was the first draft — the user corrected this explicitly: an org must be able to set its own footer identity, same path as every other branding field). `email_footer_company_name` falls back to a hardcoded "ReqTrackManager" when unset anywhere, same as `header_title`'s fallback to the built-in product name. Digest and the two platform-level alerts (system test email, disk-usage warning) always resolve with `organization_id=None` — deliberate, since a digest can span notifications from multiple orgs and there's no single "right" org to brand it with.

- **Header banner is a literal HTML block, not a generated raster image.** "The platform icon, header title on a black background" is a full-width `#000000` table row containing the resolved logo (`<img src="cid:brand_logo">`, omitted entirely — not a broken-image icon — when no logo is configured anywhere) and the header title in white, unthemed by design (only the body/footer surfaces switch with `prefers-color-scheme`). This keeps one visually consistent brand mark regardless of theme and avoids a whole raster-generation subsystem (Pillow, already a transitive dependency via `qrcode[pil]`, was available but unnecessary) for something table-based CSS does natively and more email-client-compatibly.

- **Images embedded via `multipart/related` `cid:`, never an external URL**, per the design brief. `services/email.py`'s `send_email`/`send_email_async` gained `html_body`/`inline_images` parameters; when a logo is present, `EmailMessage.add_related()` attaches it under the HTML part (verified structurally with a standalone MIME-tree dump before wiring it into the real send path, then again against real MailHog captures — see Verified below). The logo bytes are fetched via the existing `services/files.py::read_file`/`FileAsset.content_type` — no new upload or storage mechanism.

- **One-click unsubscribe is a stateless signed JWT that flips the exact same field the logged-in Preferences page controls, with every use audited.** Per the user's explicit follow-up ("please do both, one-click unsubscribe, but that updates the login preferences... log when it's been used"): `create_email_unsubscribe_token`/`decode_email_unsubscribe_token` (`security.py`) mirror the existing `purpose`-claim JWT pattern (`create_access_token`/`create_oidc_state_token`), with a 2-year expiry rather than the usual short session-token lifetime — deliberately, since the token grants no session and can only ever set `User.email_digest_mode = DigestMode.NONE` (the identical field `PUT /auth/me` already lets a logged-in user edit), so the usual "keep it short-lived" reasoning for a credential doesn't apply: the worst case of a leaked link is someone else disabling that user's email notifications, an availability/annoyance issue, not a confidentiality or integrity breach. The new public, unauthenticated `GET /notifications/unsubscribe` endpoint (`routers/notifications.py`) is a deliberate, narrow exception to normal authenticated-request handling — consulted against `docs/soc2/policies/access-control-policy.md` and `system-operations-monitoring-and-logging-policy.md` first, per this repo's required identify→verify→remediate step for new unauthenticated surfaces — mirroring the precedent the access-control policy already documents for "shared UI chrome" (the platform logo's own openly-readable endpoint): unauthenticated by necessity (a link clicked from a mail client has no session to present), narrowly scoped (one low-sensitivity, already-user-controlled preference, nothing else reachable), and every use — successful or a rejected/expired/tampered token — writes an `AuditEvent` (`email_unsubscribed_via_link` / `email_unsubscribe_link_rejected`), satisfying the logging requirement explicitly rather than leaving misuse invisible.

- **All five existing send-sites converted, not just new ones added alongside them**, per the user's "all of them" answer: `notify()`'s instant path, the daily digest, the org- and system-scoped test-email actions, and the disk-usage alert (`services/disk_monitor.py`, which gained a `db: Session` parameter to `check_disk_usage_once` purely to resolve platform branding — existing tests updated to pass one). Every plain-text `send_email`/`send_email_async` callsite in the codebase now also passes `html_body`/`inline_images`.

**Verified**: full backend pytest suite — `docker compose exec backend python -m pytest -q` (real MailHog, no mocking of the SMTP transport at the aiosmtplib level for the invite-email tests, which is why this must run inside the container rather than a host venv where the `mailhog` hostname doesn't resolve) — **433 passed**, including 4 new template-rendering tests (`test_email_templates.py`, one of them the XSS-escaping regression above), 8 new unsubscribe-token tests (`test_email_unsubscribe.py` — valid token flips state + audits, idempotent re-use, garbage/expired/wrong-purpose/tampered/unknown-user tokens all rejected without side effects), and extended assertions on the existing `test_digest.py`/`test_org_rename_and_test_email.py` confirming real HTML (not a bare string) is what's actually sent. `ruff check app tests` clean. Frontend: `tsc --noEmit`, `eslint .` (2 pre-existing, already-accepted warnings unrelated to this change), and `npm run build` all clean; the full Storybook interaction suite — **262 passed across all 43 story files** — including new/extended stories for the platform and per-org email-footer fields (`ServerManagementPage.stories.tsx`'s `PlatformBrandingEmailFooter`, `OrgAdminPage.stories.tsx`'s `BrandingSectionEmailFooterSave`). Manual verification against the real rebuilt dev/test Compose stack: a platform-default test email (no org context, no logo configured) rendered the black header/title, default accent-coloured CTA, and "© 2026 ReqTrackManager" footer fallback exactly as designed; a second test email after setting an organisation's own header title, accent colour, logo, and footer company/website/address (with the org's SMTP override pointed at the same MailHog instance) showed all of it correctly overriding the platform default, including a genuine `multipart/related` `image/png` MIME part referenced via `cid:brand_logo` in the header `<img>` tag; a real instant notification (a password-change) was confirmed via direct DB/audit-table inspection to both arrive with the footer's "Manage your email preferences" link and, on visiting that link, flip `email_digest_mode` from `instant` to `none` and write the `email_unsubscribed_via_link` audit row. Full Playwright suite (`npx playwright test`, 45 specs) passed, including 3 in the extended `org-rename-and-test-email.spec.ts` that now decode the actual MIME/quoted-printable HTML body captured by MailHog and assert on its content (dark-mode CSS present, org branding resolved, the unsubscribe link present with a real token) rather than only asserting delivery happened. `backend/scripts/seed_demo_data.py` and `seed_e2e_dataset.py` were both updated to set org-level `email_footer_*` values on the orgs they create (Solstice Robotics and E2E Alpha Robotics respectively) so the new fields are exercised by default rather than left null; platform-wide `ServerSettings.email_footer_*` was deliberately left unset by both scripts, since mutating global deployment settings from a per-org demo/E2E seeder felt like the wrong layer — the platform-default fallback path is already covered by the manual MailHog verification and `test_platform_branding_falls_back_to_built_in_product_name_when_unset`.

## Production incident: `email_footer_*` columns missing after the HTML template system deploy, and a regression test for the whole bug class

**Identify**: after pulling the latest image to the production server, every request touching `ServerSettings` (`GET /api/v1/system/test-email`, and by extension anything calling `resolve_email_branding`) failed with `psycopg2.errors.UndefinedColumn: column server_settings.email_footer_company_name does not exist`. The "HTML email template system" change above (`07570c1`) added `email_footer_company_name`/`email_footer_website`/`email_footer_address` to both `Organization` and `ServerSettings` in `app/models/organization.py`, but no Alembic migration was ever written to add those columns — an oversight that went undetected because the backend test suite's schema fixture and any dev workflow using `Base.metadata.create_all()` both build the schema straight from the current model classes, so they never exercise the gap between "the model has the field" and "a migration adds the column." A real deployment running `alembic upgrade head` (`app/migrations.py`, on every startup) does exercise exactly that gap, which is how this reached production without being caught locally or by the existing suite.

**Verify**: confirmed via `grep` across `backend/alembic/versions/` that no migration referenced `email_footer_*` anywhere, and reproduced the missing columns directly against the `tests/container` Postgres instance (`\d server_settings` / `\d organizations`) after rebuilding and recreating the `backend` container so it actually picked up the new migration file (Compose doesn't bind-mount source — a stale image otherwise silently tests against yesterday's code).

**Remediated**:
1. `backend/alembic/versions/0007_email_footer_branding.py` adds all six columns (`email_footer_company_name`/`_website`/`_address` on both `organizations` and `server_settings`) using the same `ADD COLUMN IF NOT EXISTS` pattern 0002–0006 already use, for the same reason: convergent on both a fresh `create_all()`'d database and an already-migrated one missing the columns.
2. A new regression test, `backend/tests/test_schema_migrations_match_models.py::test_models_and_migrations_have_not_drifted`, guards against this exact bug class recurring. Since `conftest.py`'s session-scoped `_schema` fixture already builds the test database via real `alembic upgrade head` rather than `create_all()`, the live migrated schema is directly comparable to `Base.metadata` — the test uses `alembic.autogenerate.compare_metadata` (the same diff engine `alembic revision --autogenerate` uses) and fails with the full diff if a model field, table, or index has no matching migration. Running it turned up one genuine pre-existing (harmless) drift unrelated to this incident: `comment_files.comment_id`'s index, created by raw SQL in migration `0004` (`CREATE INDEX IF NOT EXISTS ix_comment_files_comment_id ...`), was never declared on the `CommentFile` model, so autogenerate saw it as an index to drop. Fixed by adding `index=True` to that column (`app/models/file.py`) — SQLAlchemy's default naming convention (`ix_<table>_<column>`) matches the migration's hand-picked name exactly, so this is metadata-only and required no new migration.

**Verified**: `docker compose exec backend python -m alembic upgrade head` inside a freshly rebuilt `tests/container` backend applies `0006`→`0007` cleanly; `\d server_settings`/`\d organizations` confirm all six columns now exist. Full backend pytest suite passed inside the same container, including the new drift test with zero diffs after the `file.py` fix.

## Deployment-wide organisation label override

Requested directly by the user, not derived from `docs/requirements.md` (no requirement ID covers relabelling the "organisation" concept itself — C-C-03's per-project terminology override is a separate, fixed, closed set of nouns that deliberately excludes it, per `docs/decisions.md`'s existing terminology entry). Flagged here per this project's governance the same way the bundle export/import work above was.

**Scope, confirmed with the user before building:** a single deployment-wide setting (`ServerSettings.org_label_singular`/`org_label_plural`), not a per-organisation override — there's no single right answer to "whose branding" the word choice should follow the way accent colour/logo/header title already do, since the word is used to refer to organisations in general, not any one of them. And, per explicit follow-up feedback after an initial narrower draft: **every** string literal containing "organisation"/"Organisation(s)" in `frontend/src/i18n/strings.ts` was converted, not just the primary chrome (nav/page titles/column headers) originally proposed — full-sentence hints/confirmations included. Two exceptions, both deliberate: the platform-branding admin form's own "Organisation label (singular/plural)" field labels stay in English always (they describe the control, not content subject to it), and `ENTITY_TYPE_LABEL`'s `"organization"` entry in `api/types.ts` was replaced by an explicit branch in `activityEntityLabel` (it's a plain module-level `Record`, which can't call the `useOrgLabel` hook).

**Mechanism**: each affected `strings.ts` entry changed from a plain string to a function of the resolved label(s) (e.g. `notProvisioned: (org: string) => \`Your ${org} has not provisioned you access.\``), called at each site with `orgLabel`/`orgLabelPlural`/`orgLabelCapitalized` from three new hooks in `frontend/src/context/BrandingContext.tsx` (mirroring `useTerm`/`useTermPlural`'s existing shape for C-C-03). This type change is also the completeness mechanism: every call site still treating the value as a plain string (e.g. `{strings.auth.notProvisioned}` in JSX) became a `tsc` compile error, so the sweep across ~30 files was verified by the type checker rather than by grep/manual review alone — a plain `grep -rn "organisation"` pass afterward still caught a handful of genuinely-hardcoded literals `tsc` couldn't (a few raw JSX strings in `OrgAdminPage.tsx`/`ServerOrganisationsPage.tsx` that had never gone through `strings.ts` at all), which were fixed the same way.

**Backend**: two nullable `String(50)` columns on `ServerSettings`, `NULL` meaning "use the built-in English word" — same convention as `default_header_title`. Migration `0006_org_label.py` (see the incident entry above for why a migration is never optional here). Threaded through the existing `GET`/`PUT /system/branding` endpoint and `ServerSettingsOut`/`ServerSettingsUpdate` schemas rather than a new endpoint, since it's platform branding.

**Verified**: `backend/tests/test_org_export_import.py` and the wider suite pass unaffected; `tsc -b --noEmit` clean across the frontend after the full conversion; the Storybook interaction suite's new `PlatformBrandingOrgLabel` story (`ServerManagementPage.stories.tsx`) confirms the two new fields round-trip through `PUT /system/branding`; a new Playwright spec (`org-label.spec.ts`) sets a custom label via the real admin UI, confirms the nav rail and server-organisations page pick it up, and restores the default afterward (`test.afterAll`, the same global-setting reset pattern `self-signup.spec.ts` already uses for `ServerSettings.signup_mode`). `eslint .`: 0 errors; the three new hooks add three more `react-refresh/only-export-components` warnings to `BrandingContext.tsx` (same rule/file this repo's own "HTML email template system" verification note already accepted 1 pre-existing instance of, for `useBranding` — exporting a Provider component alongside its consumer hooks from one file is standard React context practice, not something worth restructuring around for this rule).

## Merge-import into an existing organisation

Requested directly by the user alongside the organisation-label work above, with two explicit scoping decisions made during review before implementation:

1. **No "pick two existing orgs and merge them" UI.** This is the existing org bundle export/import mechanism (`services.org_export`, see the "Bundle export/import" entry above), pointed at an organisation the caller already administers instead of always creating a brand-new one — an org admin exports (or receives) a bundle and imports it into their own existing organisation.
2. **Scoped to users, groups, projects, and report templates only** — the target organisation's own profile (branding, SMTP, SSO/OIDC config, logo, login background, default template project, `slug`, `is_active`, `pat_max_lifetime_days`) is never read from the bundle or written to. This was narrowed from an initial draft that also planned to let org-level settings be merged with a keep/override choice per field; the user cut that scope explicitly, which also removes most of the conflict surface a "some data already exists" merge would otherwise need to reason about.

**Security decisions** (identify → verify → remediate, per the change-management policy — same standard the original bundle work and its hardening pass above were held to, since this crosses an *existing* tenant boundary in a way the original "always brand-new organisation" design explicitly never had to):

- **Code reuse over duplication, per explicit review feedback.** `services.org_export` was refactored so `import_org_bundle` (unchanged behaviour, brand-new org) and the new `merge_org_bundle` (existing org) share the same parsing/import helpers — `_parse_org_bundle`, `_import_members`, `_import_org_groups`, `_import_report_templates`, `_import_projects` — parameterised by whether conflicts are possible (`resolutions`/`merge_by_name`), rather than a second near-identical implementation.
- **A project is never overwritten in place on conflict** — only skipped or imported as a renamed copy (`"<name> (imported)"`). A project carries real, potentially large requirement/change-request history; silently replacing that is a materially bigger and more destructive feature than this one, and wasn't asked for. A report template has no such history, so its conflict resolution *does* allow an in-place overwrite (`"use_import"`) — the asymmetry is deliberate, not an oversight.
- **Users and org groups are always additive, never a conflict.** An existing member keeps their current role (a bundle member who already holds it is a silent no-op, not a duplicate-row error); an existing group gains the bundle's members as a union, never loses one. Neither can be downgraded or removed by a merge, closing off a privilege-de-escalation-via-import surface before it existed.
- **A real bug found during test-writing, not just reasoned about**: `_import_members`'s "attribute the project-manager fallback to the first matched `org_admin` member, not the importer" logic (the same fix recorded in the original bundle-import bug entry above) is correct for a brand-new org with no established admin yet, but wrong for a merge — the *target* org's own admin (`current_user`, performing the merge) is already the obviously legitimate fallback, and reassigning it to some *other* organisation's admin who merely happens to also be a bundle member would be a regression, not a fix. Caught by `test_merge_with_no_conflicts_adds_users_groups_projects_and_templates` failing to find the merged project via the target admin's own project list. Remediated with a new `reassign_acting_user` flag on `_import_members`: `True` for `import_org_bundle`, `False` for `merge_org_bundle`.
- **Authorization**: `require_org_role(ORG_ADMIN)` on the target organisation for both `POST /orgs/{id}/import/preview` and `.../import/merge` — the same bar `GET /orgs/{id}/export` already uses, deliberately no server-admin bypass (I-M-05).
- **`resolutions` must cover every conflict exactly, or the whole import is refused (400)** rather than silently defaulting an unresolved conflict either way — `merge_org_bundle` recomputes the same conflict set the preview endpoint reported and diffs it against the supplied keys before writing anything. An invalid resolution value for a conflict's kind (e.g. `"keep_existing"` on a project conflict) is rejected the same way.

**New endpoints/schemas**: `POST /orgs/{id}/import/preview` (`OrgMergePreviewResult`/`MergeConflictOut`) and `POST /orgs/{id}/import/merge` (`OrgMergeResult`) in `routers/orgs.py`; `detect_merge_conflicts`/`merge_org_bundle` in `services/org_export.py`. Frontend: `ImportConflictPanel.tsx` (grouped by conflict kind, radio choice per item, defaulting to the non-destructive option — skip/keep-existing — so submitting without touching every choice can never surprise-overwrite or surprise-duplicate anything), wired into a new collapsible section on `OrgAdminPage.tsx`.

## Hardening pass on org merge-import and rename

Requested follow-up ("do a hardening pass") to the previous two entries (organisation-label override and merge-import). Identify → verify → remediate, per the change-management policy's practice for security-sensitive changes; scoped to the most recent commit's actual surface (`44ba1bf`: merge-import, org rename, deployment-wide org label, and the two backfill migrations).

**Found and fixed: a bundle-driven import could hand a banned account real access back, bypassing `User.is_banned` entirely.** That invariant is already enforced at two call sites — `assign_org_role` and `assign_project_role_by_email` (see the "Hardening pass on self-signup/invites/external-users" entry above) — specifically so a known-bad account flagged by a server admin "can't quietly be let back in through a different org's admin... without a server admin needing to intervene again per org" (`User.is_banned`'s own docstring). `_import_members` (shared by `import_org_bundle` and the new `merge_org_bundle`) matched a bundle member by email and granted `UserOrgRole` directly with no `is_banned` check at all — a third, unguarded path to the exact same grant. Worse, `_import_org_groups` had the same gap on a second, independent axis: `rbac.get_effective_project_roles` resolves project access through `OrgGroupMember` rows with no separate check that the user holds any `UserOrgRole` at all (an org group nested into a `ProjectGroup` grants access purely by group membership), so a banned account added to an org group via a bundle would gain real project access without ever touching `UserOrgRole` or being caught by the fix above on its own.

This gap technically predates this commit — `_import_members`'s logic (minus the `merge_org_bundle`-specific `reassign_acting_user` behaviour) is a straight refactor of `import_org_bundle`'s pre-existing member-import loop, confirmed by diffing against the parent commit — but `import_org_bundle` is `require_server_admin`-only, a small, highly-trusted population. `merge_org_bundle` exposes the same unguarded logic to *any* `org_admin` of *any* organisation in the deployment, and does so against an org's real, live membership rather than a freshly-created empty one — a materially larger and more realistic exploit surface than the pre-existing gap on its own, so this is treated as a regression this commit introduced, not merely an old gap left as-is. The realistic shape isn't even adversarial: export an org for backup/migration, have one of its members banned afterward, then merge that now-stale bundle back in later — without this fix, the ban would be silently undone.

Fixed by skipping (with a warning, same style as the existing "member has no account — invited instead" warning) an `is_banned` match in both `_import_members` and `_import_org_groups`, threaded through both `import_org_bundle` and `merge_org_bundle` alike — closing the gap for both paths, not just the newly-exposed one. New test: `test_merge_does_not_grant_a_role_or_group_membership_to_a_banned_member`, which captures a bundle with a real member + group membership, then has that account leave and get banned *after* export (the only order `ban`/`add_org_group_member`'s own membership preconditions allow), and asserts the merge into a fresh target org grants neither the org role nor the group membership, surfacing a warning instead.

**Reviewed and found adequate, no changes needed**: `rename_organization`'s authorization (`ORG_ADMIN`, no server-admin bypass, matching every other org-property endpoint) and validation (reuses `OrganizationCreate`'s own non-blank-name validator). `preview_organization_merge`/`merge_organization_bundle`'s upload handling (`_parse_org_bundle` already applies `enforce_upload_size_limit`/`enforce_zip_uncompressed_size_limit`, the same zip-bomb guards the original bundle-import hardening pass added) and `resolutions` parsing (malformed JSON and non-string values are both rejected with 400 before any write). `merge_org_bundle`'s "any org_admin can point at any bundle, including one exported from an org they have no relationship to" shape — confirmed this is the feature as scoped ("an org admin exports (or receives) a bundle and imports it into their own existing organisation"), not an unreviewed cross-tenant hole: it can only ever grant what the *target* org's own admin could already grant by hand (project creation, member invites via `assign_org_role`), gated behind that same `ORG_ADMIN` bar, and the additive-only/no-overwrite design (see the entry above) means it can't destroy the target's own data either. `org_group`/`report_template`/project name-collision detection being case-insensitive-by-`.lower()`-only (no Unicode normalisation) — consistent with every other name-uniqueness check already in this codebase (stages/components/categories), not a new relaxation. `0006_org_label.py`/`0007_email_footer_branding.py` migrations — columns match `ServerSettings`/`Organization` model fields exactly, `IF NOT EXISTS` pattern consistent with 0002-0005, and the new `test_schema_migrations_match_models.py` (added in the same commit) now pins the whole bug class the second migration was itself written to fix. `org_label_singular`/`org_label_plural` and `email_footer_*` rendering — used in ~40 frontend call sites, all plain JSX text interpolation (no `dangerouslySetInnerHTML` anywhere in the codebase), so React's default escaping applies; email rendering already has its own escaping test (`test_notification_email_escapes_org_and_notification_supplied_content`). The merge-preview/confirm frontend flow (`OrgAdminPage.tsx`) reuses the same in-memory `File` object for both calls rather than letting the user swap it between preview and confirm — moot as a server-side control regardless, since `merge_org_bundle` always recomputes conflicts from the bytes actually posted to `/import/merge`, never trusts the preview call's result.

**Follow-up ("look deeper"): traced invariants beyond the endpoint-level review above**, the same way the "Deeper hardening pass" entry earlier in this log followed its own first pass — looking at what the new code depends on and interacts with, not just re-reading the same lines.

**Found and fixed: `merge_org_bundle`'s audit event recorded *that* a merge happened but not *where the data came from*.** The system-operations-monitoring-and-logging-policy's first rule requires audit entries to record "who did it, when, and what changed — not reconstructed after the fact." `merged_from_bundle`'s `detail` had `actor_id` (who) and per-entity-kind counts (what), but nothing identifying which organisation's bundle was merged in, who exported it, or when — despite that being right there in the bundle's own `manifest.json` and already being parsed into a local variable (`_manifest`, underscore-prefixed specifically because it went unused). For most bundle-import paths in this codebase this is a pre-existing, lower-stakes gap (`import_org_bundle`, and `project_export.py`'s equivalent, both create a brand-new entity from scratch, so "what changed" is self-evident from the new org/project's own audit trail) — left as-is here rather than expanded into an unrelated refactor. `merge_org_bundle` is different: it's the one path in the whole bundle subsystem that writes into an organisation's *real, live* data, which is exactly why it was already held to a stricter authorization/isolation bar than the others in the first entry above — the same reasoning extends to its audit trail. Without the source recorded, "a project neither of us remembers creating just appeared in our org" has no way to be answered from the audit log alone. Fixed by adding `source_org_name`/`source_exported_by_email`/`source_exported_at` (the manifest's own self-reported fields — same trust level as the rest of the bundle's contents, not independently re-verified) to the log event's `detail`. New test: `test_merge_audit_event_records_the_source_bundles_provenance`, which queries `AuditEvent` directly (the established pattern from `test_email_unsubscribe.py`) and asserts all three fields are populated after a real merge.

**Reviewed and found adequate, no changes needed, on the deeper pass**: project-name uniqueness is not a database constraint anywhere in this codebase (`Project.name` has no `unique=True`/`UniqueConstraint`), so a TOCTOU race between two concurrent merges (or a manual create and a merge) producing two same-named projects is pre-existing accepted behaviour, not a new hole this feature opens. File/attachment scoping under merge (`apply_project_data` derives `organization_id` from the newly-created project's own row — the merge *target's* id — never from the bundle's own data), confirmed by reading `project_export.py` directly rather than assuming the existing project-bundle hardening still holds after this reuse. A banned user already holding the exact role/group membership `_import_members`/`_import_org_groups` would otherwise warn about skipping is not currently reachable — `ban_orphaned_user` only ever bans an account with zero org roles anywhere, and (post the fix above) every remaining grant path checks `is_banned` — so the warning's phrasing was left as unconditional rather than adding dead-code-guarding complexity for a state the invariant itself rules out.

**Verified for real**: new regression test passes in isolation and as part of the full suite; full backend suite — 443 passed, zero regressions, run against the real `tests/container` Postgres stack. `ruff check` clean on both touched files.

**Verified**: `backend/tests/test_org_import_merge.py` — 8 new tests (no-conflict merge; a project-name collision resolved both ways; a report-template-name collision resolved both ways; the target org's own profile fields provably unchanged after a merge; missing/invalid `resolutions` both rejected; a non-admin-of-the-target caller rejected; a wrong-kind bundle rejected) — plus the full pre-existing `test_org_export_import.py` suite confirming the refactor didn't change `import_org_bundle`'s own behaviour. `ruff check` clean. Frontend: `tsc -b --noEmit` clean; the Storybook interaction suite's new `ImportConflictPanel.stories.tsx` (5 tests) and the existing `OrgAdminPage.stories.tsx` (15 tests, unaffected by the new section) both pass. A new Playwright spec (`org-merge-import.spec.ts`) drives the real UI end to end: exports a source organisation with a project, uploads it into a disposable target organisation that already has a same-named project, resolves the conflict as "import as a new copy" through the actual conflict panel, and confirms both the resulting two projects and the target's untouched branding via the API afterward.

## User-reported bug: login-page background image "didn't persist"

User reported that a background image set for the "Server Management Platform" login page didn't persist. Identify → verify → remediate, per the change-management policy's practice for security-sensitive changes (this touches unauthenticated access to a file-download endpoint).

**Identified**: the image did persist — the bug was that it never rendered on the actual login page. Two stacked access-control gaps, both pre-existing:
1. `GET /api/v1/system/branding` (`routers/system.py`) required `get_current_user`, but `LoginPage.tsx` calls it on mount, before any session exists, to fetch `default_login_background_file_id`. The request 401'd, the `.then()` never ran, and `loginBackgroundFileId` state stayed `null` — so the plain `/login` page's `useEffect` fetch silently failed and no background was ever requested.
2. Even with (1) fixed, `GET /api/v1/files/{file_id}` (`routers/files.py::download_file`) unconditionally required a bearer token via `get_current_user_header_or_query`. This meant no anonymous visitor could load *any* login-page image, including the org logo already shown on the org-branded `/login/{slug}` page (`OrgLoginPage.tsx`) — an equally real instance of the same root cause, just not the one reported.

Confirmed `ServerSettingsOut` (the branding schema) carries no sensitive fields (no SMTP credentials, no OIDC secrets), so making it fully public mirrors the existing precedent of `orgs.py::get_org_login_info`, which already serves org branding (including `login_background_file_id`) with no authentication at all.

**Remediated**: `get_branding` dropped its `current_user` dependency entirely (now unauthenticated, matching `get_org_login_info`). `deps.py` gained `get_current_user_header_or_query_optional`, identical to `get_current_user_header_or_query` except it returns `None` instead of raising when no token is presented at all (a token that *is* present but invalid/expired still 401s as before — only the "no token" case is relaxed, so a broken session doesn't silently downgrade to anonymous). `download_file` now resolves the current user through that optional dependency and checks a `is_public_branding` bucket — `Organization.logo_file_id`, `Organization.login_background_file_id`, `ServerSettings.default_logo_file_id`, `ServerSettings.default_login_background_file_id` — before any auth check at all; only avatars, org shared resources, and requirement/comment attachments still require `current_user is not None`, unchanged from before. Avatars deliberately stayed in the "authenticated but not anonymous" bucket — there's no pre-login surface that renders one, unlike logos/login backgrounds.

**Verified**: five new backend tests in `test_files.py` — anonymous download succeeds for org logo, org login background, platform default logo, and platform default login background (all four independently, both upload sources); `GET /api/v1/system/branding` succeeds with no `Authorization` header; and a regression test confirms avatars and org shared resources still 401 anonymously, pinning the boundary of the carve-out. Full backend suite run inside the real `tests/container` stack (per this project's established Docker-test workflow, since `backend/tests/`'s `DATABASE_URL` guard requires it): 448 passed, 0 failed, 0 regressions. `docker compose restart backend` + both seed scripts re-run afterward to restore the dev/e2e data the suite run wipes.

## Groups/permissions overhaul, part 1: project visibility ("Only specified" vs "Org-wide visibility")

User request: a large groups/permissions overhaul (true org-group nesting, project visibility, OAuth/SAML group sync, unified user-management UI — see the four-feature plan discussed and approved in-session). Per the user's explicit direction, none of this is added to `docs/requirements.md` (which the agent may not edit) — it's recorded here in the decisions log instead, following this project's identify → verify → remediate practice for the parts that touch authorization. This entry covers part 1 only: project visibility. The user also explicitly descoped an earlier idea (a new org-wide "project admin of all projects" role) in favour of this simpler visibility toggle.

**What was built**: a `Project.visibility` field (`ProjectVisibility`: `only_specified` default, or `org_wide`). The grant is implemented inside `services.rbac.get_effective_project_roles` itself, not just `list_projects`'s bespoke accessible-id union — every `require_project_*` dependency funnels through that one function, so an org-wide project's content (not just its list-page entry) becomes visible uniformly. When `visibility == org_wide` and the caller holds any role at all in the project's own organisation, they get baseline `ProjectRole.MEMBER` — never manager/administrator/stakeholder, which still require an explicit assignment. Deliberately **not** folded into `get_project_member_user_ids` (broadcast notification targeting, C-N-01) — every org member getting notified for every org-wide project would be a worse regression than the gap it closes — nor into `can_manage_project_settings`, which still requires a real role or org-admin.

The toggle is exposed in two places: the "New project" creation form (`ProjectListPage.tsx`, always explicit — never inherited from a cloned template, so a template happening to be org-wide doesn't silently make every project cloned from it org-wide too) and `ProjectAdminPage.tsx`'s settings tab (existing `PATCH /projects/{id}`, audit-logged via the established `log_event` pattern).

**Verified**: four new backend tests in `test_rbac.py` (baseline MEMBER access granted with no explicit role, scoped strictly to the project's own organisation, never implies manage rights, and the visibility change is itself audit-logged) plus `test_schema_migrations_match_models.py` confirming the new migration (`0008_project_visibility.py`) matches the model. Full backend suite: 452 passed (448 + 4 new), 0 regressions, run inside the real `tests/container` stack. `ruff check` clean. Frontend: `tsc -b`, `eslint .` clean (same 5 pre-existing, already-accepted warnings); new Storybook story (`OverviewTabSetOrgWideVisibility`) and a new Playwright spec (`project-visibility.spec.ts`) exercising the real UI end to end — org-wide visibility set at creation, confirmed on the settings tab, a plain org member with zero explicit role granted access, then the toggle reversed and access revoked — using a disposable project created within the spec specifically so it can't interfere with `project-access-scope.spec.ts`'s existing assertion that a scoped member does *not* see other org projects by default.

One real regression caught by re-running the broader Playwright suite (not just the new spec): the "New project" form's new, unconditional Visibility `<select>` broke two existing tests that had encoded "no select elements when there's only one org to choose from" — their actual intent (per their own comments) was "no *org-picker* select," which happened to coincide with "no selects at all" only because the org picker and the template picker were previously the form's only conditional `<select>`s. Fixed `project-list-org-scoping.spec.ts`'s assertion to expect exactly the Visibility select (not zero), and `project-bundle-export-import.spec.ts`'s one-shot `isVisible()` check on `select.first()` — which could now latch onto the always-present Visibility select before the org picker had asynchronously mounted — with the same retrying wait-for-content pattern `multi-org-admin.spec.ts` already used for the identical race. Also cleaned up disposable orgs left behind by my own earlier failed local runs of `project-list-org-scoping.spec.ts` (it deletes its own scaffolding at the end, but only on the success path) before re-verifying.

## Groups/permissions overhaul, part 2: true org-group nesting

Continuation of the plan from part 1 above. An `OrgGroup` can now contain another `OrgGroup` as a member, transitively — a distinct, new relationship from the existing (and deliberately one-level-deep) org-group-in-*project*-group nesting (C-U-12).

**What was built**: `OrgGroupMember` gained a nullable, self-referential `member_org_group_id` FK (mirroring `ProjectGroupMember`'s existing user-or-org-group XOR shape one level up); `user_id` became nullable with a check constraint enforcing exactly one of the two is set. Cycle prevention (`would_create_org_group_cycle`, `services/rbac.py`) runs at write time in `add_org_group_member` — rejects self-nesting and any nesting that would close a longer loop. Transitive resolution uses two new iterative BFS closure helpers (`_ancestor_org_group_ids`/`_descendant_org_group_ids`) rather than a recursive SQL CTE, matching this file's existing preference for explicit Python-side set operations over SQL recursion; wired into `get_effective_project_roles` (a user in a sub-group now counts as a member of every ancestor group for project-role resolution) and `get_project_member_user_ids` (broadcast notifications now reach members of descendant groups too). A defensive iteration cap exists but should never trip, since cycles are already rejected at write time.

Found and fixed along the way, before it could ship as a silent gap: org bundle export/import (`services/org_export.py`) would have dropped nested-group structure entirely — `OrgGroupMember.user_id`-keyed queries naturally excluded the new nested-group rows (NULL never matches a join), so an exported bundle simply never carried `nested_group_names`. Fixed by adding that field to the export JSON and a second import pass (after every group in the bundle is created/resolved, since a group can reference another one appearing later in the same list) that wires up nesting edges by name — with the same cycle check and warn-and-skip behavior as the live endpoint, for a malformed or adversarial bundle.

**Verified**: `backend/tests/test_org_group_nesting.py` (new, 7 tests) — self-nesting rejected, a longer 2-hop cycle rejected, a 3-hop cycle rejected, cross-org nesting rejected, transitive resolution through a 3-level chain actually grants the project role, a nested-group member can be removed via the generic `member_id` delete endpoint, and `leave_organization`'s cleanup is unaffected by nesting edges being present. Full backend suite: 459 passed (452 + 7 new), 0 regressions, including `test_schema_migrations_match_models.py` (migration `0009_org_group_nesting.py` matches the model) and the full pre-existing `test_org_export_import.py`/`test_org_import_merge.py` suites (confirming the nested-group export/import addition didn't change existing bundle behaviour). `ruff check` clean. Frontend: `tsc -b`, `eslint .` clean (same 5 pre-existing warnings); `OrgAdminPage.tsx`'s groups section gained a nest/un-nest picker with cycle-rejection error surfaced inline; new Storybook story (`GroupsSectionNestGroup`) plus a fix to the pre-existing `GroupsSectionShowsMembers` story (a second group fixture, needed to exercise nesting, made its `getByText("Engineering")` query ambiguous against the new nesting `<option>` — scoped to the `<span>` specifically). New Playwright spec (`org-group-nesting.spec.ts`) drives the real UI: create two groups, nest one inside the other, confirm it's listed, attempt the reverse nesting and confirm the cycle error surfaces inline rather than failing silently, then remove the nested-group edge and confirm it's gone.

## Groups/permissions overhaul, part 3: IdP group-membership sync (OIDC extension + SCIM, replacing the originally-planned SAML)

Continuation of the plan from parts 1-2 above. Original scope was extending OIDC group-claims sync to `OrgGroup` membership, plus adding SAML as a second SSO login protocol. The user redirected mid-build on the second half: SAML only carries group information as a side effect of an active login (can't promptly deprovision someone who stops logging in), whereas SCIM is the purpose-built push-provisioning protocol for exactly this job (the IdP calls into this app whenever group membership changes, no login required) and is what Okta/Entra ID/Google Workspace actually use. SAML was dropped from scope entirely; SCIM replaced it.

**Part A — OIDC extension**: `OrgGroup.idp_synced_group_name` (nullable, unique per org via a partial index) marks a group as IdP-managed. New `services/oidc_provisioning.py::sync_org_groups_from_claims`, structurally identical to the existing `sync_org_roles_from_claims` — same vocabulary-scoping (only ever touches groups with the field set) and "no claim at all is unknown, not empty" rule — called alongside it in `auth_oidc.py::oidc_callback`. Configured per group via a new `PATCH /orgs/{id}/groups/{id}` endpoint and an inline input in `OrgAdminPage.tsx`'s groups section.

**Part B — SCIM 2.0 provisioning** (`backend/app/routers/scim.py`): inbound Users/Groups CRUD + group-membership PATCH, authenticated by a per-org bearer token (`Organization.scim_token_hash`/`scim_token_prefix`, hashed the same way as a Personal Access Token — `security.generate_scim_token`/`hash_pat`). A SCIM Group's `id` is this app's own `OrgGroup.id` and a SCIM User's `id` is this app's own `User.id` (no separate external-id mapping table needed — the IdP addresses whatever `id` a `POST` returns). `POST /Users` finds-or-creates by email and grants baseline `OrgRole.MEMBER`; deactivation never hard-deletes (C-U-04/C-U-05); a banned account can never be (re-)provisioned. Deliberately a pragmatic RFC 7643/7644 subset (single `attr eq "value"` filters, `active`/`displayName`/`members` PATCH paths only, no Bulk/ETags/schema extensions) — documented in the router's own module docstring and `docs/enterprise-integration.md`, not hidden. Both sync paths only ever touch `OrgGroupMember` rows with `user_id` set — never `member_org_group_id` (nested-group) edges, which stay structural/admin-managed.

Token management: `GET`/`POST`/`DELETE /orgs/{id}/scim-token` (org-admin only), surfaced in `OrgAdminPage.tsx`'s new "SCIM provisioning" section — the raw token is shown exactly once, at generation.

**A real regression found and fixed via the full Playwright suite, not the new spec itself**: the new per-group IdP-sync "Save" button used the codebase's generic `strings.common.save` ("Save") — inconsistent with this app's own established convention of specific button labels ("Save settings", "Save SSO settings", "Save branding") — which collided with a pre-existing, already-fragile *unscoped* `page.getByRole("button", { name: "Save", exact: true })` in `project-admin-templates-and-reports.spec.ts` (that spec's own comment already documented scoping most of its locators to `reportTemplatesSection` specifically because other sections can be left expanded server-side by another spec sharing the same Gamma persona — this one line had been missed). Whenever `org-group-nesting.spec.ts` (or any other spec) left the "Organisation groups" section expanded for that persona, each existing group's own "Save" button padded out the page-wide match to 3, breaking the strict-mode-sensitive locator. Fixed by giving the new button a specific label (`strings.orgAdmin.saveIdpSync`, "Save sync name") and scoping the pre-existing spec's line to `reportTemplatesSection` too, matching its own already-stated rationale.

**Verified**: `backend/tests/test_oidc_provisioning.py` — 5 new `sync_org_groups_from_claims` tests (grant, no-match, revoke-on-claim-disappearance, never touches an unsynced group or nested-group edges, no-op on a missing claim) plus 2 new endpoint tests (IdP-sync-target round trip via `PATCH`, two groups can't claim the same synced name). `backend/tests/test_scim.py` (new, 11 tests) — token lifecycle (generate/status/revoke, real bearer-auth request), unauthenticated/invalid-token rejection, user provisioning + idempotent re-provisioning + baseline role grant, PATCH deactivation, group create + PATCH add/remove membership (both list-value and single-member-filter path forms), PUT full-replace membership, group delete, cross-org isolation (404 on another org's resources), nested-group edges survive a membership PATCH untouched, and a banned account can't be (re-)provisioned. Full backend suite: 476 passed (459 + 17 new), 0 regressions, including `test_schema_migrations_match_models.py` (migrations `0010_oidc_group_sync.py`/`0011_scim_provisioning.py`, including the partial unique index, match the models exactly). `ruff check` clean. Frontend: `tsc -b`, `eslint .` clean (same 5 pre-existing warnings); new Storybook stories (`GroupsSectionNestGroup`'s sibling `ScimSectionGenerateToken`); new Playwright spec `scim-provisioning.spec.ts` (generates a token through the real UI, uses it for a real authenticated `GET /scim/v2/Users` request proving the whole loop works end to end — not just the UI round-trip — then revokes it and confirms the same token immediately 401s). Full 46-spec Playwright suite run three times against genuinely fresh reseeds while chasing the regression above and two pre-existing flakes (`requirements-and-cr-filters.spec.ts`, already documented elsewhere in this log as a full-batch-only flake; `project-admin-templates-and-reports.spec.ts`, the actual regression above) — final clean run: 46 passed, 0 failed.

## Groups/permissions overhaul, part 4: unified user-management surfaces

Final part of the plan from parts 1-3 above. Closes the original UX gap the user first reported ("org groups can be added as users to projects") plus the "unified user management" ask — extending existing pages rather than building a new monolithic one, per the plan's IA recommendation.

**Closed the original gap**: `ProjectAdminPage.tsx`'s groups tab gained an org-group picker (add/remove) next to the existing per-user `UserAutocomplete` — the backend (`add_project_group_member`) already fully supported `org_group_id`, but no frontend surface ever sent it; only `OrgAdminPage`'s "expanded project" panel could. `member_org_group_ids` is now rendered as an actual list with remove buttons instead of a bare count.

**New self-service `GET /auth/me/memberships`** (`routers/auth.py`): a user's full cross-org picture — org roles, org-group membership (direct and inherited via nesting, reusing the new `services.rbac.get_user_org_group_ids`), and per-project roles — scoped to orgs with a direct `UserOrgRole` (every project-accessible user already has one, C-U-02). Wired into `PreferencesPage.tsx`'s pre-existing "Your access" tab (added in an earlier pass, see that entry above) as a new "Groups" list per org, alongside the org roles/projects it already showed.

**Server-admin access-review directory** (`GET /system/users`) gained a `group_names` field — every org group a user effectively belongs to (direct + inherited) across every org, gated by the same `access_review_show_org_names` setting that already gates `organization_names`, for the identical reason (naming a specific team is at least as revealing of org-internal structure as an org name). Surfaced as a new "Groups" column in `ServerManagementPage.tsx`'s access-review table.

**Considered and explicitly reverted: a new org-admin-scoped nav-rail entry.** The plan called for one, but implementing it surfaced a documented, deliberate prior decision (see this log's "Preferences page: tabbed, plus a new 'Your access' tab" entry): the team had already removed `/orgs`'s own nav-rail link entirely, specifically *because* non-admin/org-admin reachability was moved to the Preferences "Your access" tab's per-org "Manage organisation" link instead of a persistent rail entry. Re-adding a rail entry for the same purpose would have quietly re-introduced the pattern that decision rejected. Reverted the nav-rail change (state, effect, JSX, icon import, strings, and the story exercising it) before it shipped; the "Your access" tab — now itself enhanced with group info by this same pass — already satisfies the underlying "can an org admin easily reach the org(s) they administer" goal.

**Verified**: `backend/tests/test_my_memberships.py` (new, 3 tests: org role + group + project role all present, transitively-inherited groups correctly marked `direct: false`, orgs the caller doesn't belong to are excluded) and one new test in `test_access_review.py` (`group_names` includes both direct and inherited membership). Full backend suite: 480 passed (476 + 4 new), 0 regressions. `ruff check` clean. Frontend: `tsc -b`, `eslint .` clean (same 5 pre-existing warnings); new/extended Storybook coverage (`ProjectAdminPage.stories.tsx`'s `GroupsTabAddOrgGroup` — including a genuine mock-path collision fix, since `/orgs/{id}/groups` also matched the pre-existing `/groups` substring check meant for project groups; `PreferencesPage.stories.tsx`'s `AccessTabShowsOrgsAndProjects` extended for direct/inherited groups; `ServerManagementPage.stories.tsx`'s new `AccessReviewShowsGroups`). New/extended Playwright coverage: a new step in `project-admin-groups-and-fields.spec.ts` (create an org group via API, reload so `ProjectAdminPage`'s once-on-mount org-groups fetch picks it up, nest it into a project group through the real picker, then remove it) and a new step in `preferences-and-theme.spec.ts` (a 3-level-deep nested-group setup via API, confirming the "Your access" tab shows both the direct and the inherited group with the "(via a nested group)" qualifier). Full 46-spec Playwright suite run against a genuinely fresh reseed: 46 passed, 0 failed — including both of the above specs, which had intermittently failed earlier in this pass due to accumulated state pollution (duplicate custom fields, a changed persona password) from dozens of manual re-runs against the same long-lived stack during development, not any product defect; each was independently reproduced as passing cleanly on fresh data before being counted as done.

This closes out the full four-part groups/permissions/visibility/provisioning overhaul from this session (see the "Groups/permissions overhaul" entries above, parts 1-4).

## Hardening pass on the groups/permissions overhaul

Requested follow-up ("please do a hardening pass") to the four parts above. Identify → verify → remediate, per the change-management policy's practice for security-sensitive changes; scoped to that overhaul's actual surface — project visibility, org-group nesting, OIDC/SCIM group sync, and the new unified user-management surfaces.

**Found and fixed: SCIM group deletion cascaded away project access with no record of what changed.** `DELETE /scim/v2/Groups/{id}` (`routers/scim.py::delete_group`) is the *only* org-group-deletion path anywhere in the app — no equivalent exists for a human org admin. `ProjectGroupMember.org_group_id` and `OrgGroupMember.member_org_group_id` both carry `ondelete="CASCADE"` back to `org_groups.id`, so deleting a group silently revokes whatever project access it granted through nesting into a `ProjectGroup`, and detaches it from any parent org group it was nested inside — an IdP routinely deleting/recreating one of its groups can do this with nobody in the app aware anything beyond "a group was deleted" happened. The audit event only ever recorded the group's own id/name. This is the same "record what changed, not just that something happened" gap the merge-import audit-provenance hardening pass (see above) fixed for `merge_org_bundle`'s `detail`, on a different endpoint. Fixed by capturing the affected `ProjectGroup` ids and parent-`OrgGroup` ids *before* the cascade runs and recording both in the `scim_deleted` event's `detail`, so "a project's access just changed and nobody knows why" is answerable from the audit log alone, without changing the deletion behaviour itself (blocking it would break legitimate SCIM deprovisioning). New test: `test_scim_delete_group_audit_event_records_the_access_it_cascaded_away`, which nests a SCIM-managed group both into a project group and inside a parent org group, deletes it, and asserts both ids land in the audit event's detail.

**Found and fixed: a defense-in-depth gap in the new group-membership read path, mirroring a pattern already applied elsewhere.** `services.rbac.get_effective_project_roles`'s existing org-group-in-project-group resolution re-joins on `OrgGroup.organization_id == Project.organization_id` as defense in depth, explicitly commented as such, even though `routers.orgs.add_org_group_member` already rejects cross-org nesting at write time — belt and braces, not redundancy for its own sake. The new `get_user_org_group_ids` (backing `GET /auth/me/memberships` and the access-review directory's `group_names`) computes its transitive ancestor closure via `_ancestor_org_group_ids` with no equivalent re-check, and neither of its two callers re-filtered the result by organisation either — so a cross-org nesting edge that ever got into the database by some other means (a bug, a future code path, direct DB access) would surface a foreign organisation's group name under the wrong org's membership entry, unlike the project-role path, which would simply resolve to nothing. Not independently exploitable today (the same write-time check the project-role path also relies on already blocks it), but inconsistent with this codebase's own established posture on this exact class of check. Fixed by re-scoping the ancestor closure to the target `organization_id` inside `get_user_org_group_ids` itself, matching the sibling function. New test: `test_my_memberships_ancestor_closure_never_leaks_a_cross_org_group_name`, which — since the real API cannot construct this state at all — inserts a cross-org nesting edge directly via the DB session (the only way to reach it) and confirms the foreign organisation's group name never appears under the caller's own org entry.

**Found and fixed (minor, robustness not security): a malformed member id in a SCIM Group payload 500ed instead of being rejected cleanly.** `create_group`, `replace_group`, and `patch_group` all called `UUID(...)` directly on caller-supplied `members[].value` strings; a real IdP only ever echoes back an id this app itself issued, so this was never expected to matter in practice, but the request body is untyped (`dict[str, Any]`), so a malformed or hand-crafted request hit an uncaught `ValueError` and surfaced as a bare 500 rather than a clean skip — inconsistent with `_add_group_member`'s own existing tolerance for an unrecognised-but-well-formed id ("silently skip a member id that isn't a real org member"). Fixed with a shared `_parse_uuid` helper used at all three call sites. New test: `test_scim_group_patch_and_create_reject_malformed_member_ids_without_500ing`.

**Reviewed and found adequate, no changes needed**: `get_effective_project_roles`'s `ProjectVisibility.ORG_WIDE` grant (never implies manager/administrator/stakeholder; correctly excluded from `get_project_member_user_ids` and `can_manage_project_settings`, matching its own module-docstring claims) and `list_projects`'s parallel `accessible_ids` leg (queries `UserOrgRole` directly, the same source `get_effective_org_roles` itself reads — no discrepancy between the two). `add_org_group_member`'s existing checks (`_get_org_group_in_org` for the target group, `get_effective_org_roles` for a user being added, `would_create_org_group_cycle` plus same-org resolution for a nested group) and `_import_org_groups`'s two-pass nested-group import (already reuses `would_create_org_group_cycle` against live DB state, including edges added earlier in the same import — verified this holds correctly because SQLAlchemy's default autoflush makes each cycle check see prior adds within the same pass). `sync_org_groups_from_claims` — confirmed it runs after `auth_oidc.py::oidc_callback`'s existing `is_active`/`is_archived` check (a deactivated/banned account never reaches it, so no path to a new grant while banned) and only ever touches `user_id`-keyed `OrgGroupMember` rows, never `member_org_group_id` nesting edges. `routers/scim.py::require_scim_org`'s bearer-token resolution (hashed lookup via the same `hash_pat` used for PATs, 403 on a disabled organisation, 401 on anything else) and every `Users`/`Groups` handler's org-scoping (`_get_org_user`/`_get_org_group`, both confirmed to 404 rather than leak existence across organisations — covered by the existing `test_scim_cannot_access_another_organisations_resources`). `SCIM Users` provisioning's `is_banned` check on an already-existing matched account (a sixth instance of the class of gap the "Hardening pass on self-signup/invites/external-users" and "org merge-import" entries above describe — this one was already closed at build time, not something this pass had to find). The `download_file`/`get_branding` changes behind the original login-background-image bug fix: org logos and login-background images becoming genuinely public (no auth at all) is the fix's entire point, matches the same public-branding reasoning `get_org_login_info` already established, and avatars remain authenticated-only as before; a PAT-scoped caller can still freely read public-branding content regardless of its own scope, which is not a boundary crossing since the content is already public to a fully anonymous caller.

**SOC 2 documentation**: added SCIM bearer tokens as a documented credential type in `access-control-policy.md`'s Authentication section (item 9, mirroring item 8's PAT treatment — a third bearer-credential type, but organisation-scoped rather than user-scoped, since it authenticates an IdP integration with no user behind the request) and a new Provisioning/deprovisioning item (2a) covering the claims-based-vs-push-based (SCIM) sync split and cross-referencing the `is_banned` check above.

**Also found and fixed, while verifying: three pre-existing, unrelated Playwright races**, surfaced only because this pass's repeated pytest/Playwright runs put the shared dev stack under more concurrent load than usual. None touch groups/permissions/SCIM, and all three predate this session's overhaul entirely, but the user was explicit that a found issue gets fixed as part of the same piece of work, not documented and left — including the third one below, which this entry originally (wrongly) wrote off as "ordinary flakiness" after it passed on a single isolated retry, before the user corrected that reflex and asked for it to actually be fixed too.

`requirements-and-cr-filters.spec.ts`'s view-mode-persistence step clicked "List view" and immediately called `page.reload()` with no wait in between; `AuthContext.tsx`'s `setUiPreference` (`useUiPreference`'s backing implementation) fires its `PATCH /auth/me/preferences` fire-and-forget, with no await, so a reload that wins the race observes the pre-save state. `project-history.spec.ts`'s date-range-filter step had the same shape one line earlier: it filled the date field and asserted on the resulting `/changes` fetch with no explicit wait, relying purely on the assertion's own timeout to outlast the debounce+network round trip — inconsistent with the very next line in the same test step, which already waits for that endpoint explicitly. `org-security-controls.spec.ts`'s org-wide-2FA-enforcement step had the same shape again: it clicked into a project (triggering a `GET /api/v1/projects/{id}` that resolves 403 and drives the "2FA required" UI) and asserted on that UI immediately, with no wait for the request the assertion actually depends on. All three fixed the same way, mirroring the pattern `preferences-and-theme.spec.ts`'s "pronouns save and persist" step already established for this exact class of race: wrap the triggering action in `Promise.all([page.waitForResponse(...), action])` so the test only proceeds once the server call it depends on has actually resolved. The `org-security-controls.spec.ts` fix was re-verified with three consecutive isolated runs (not just one) specifically because the earlier single-retry-then-move-on approach is what produced the wrong call the first time.

**Verified**: targeted run of the seven affected/new test files (`test_scim.py`, `test_my_memberships.py`, `test_org_group_nesting.py`, `test_rbac.py`, `test_access_review.py`, `test_oidc_provisioning.py`) — 88 passed. Full backend suite run four times independently against a freshly wiped test database each time (the first two attempts were run in parallel with each other by mistake, producing a large false-failure batch from the resulting DB collision — a self-inflicted artifact of this pass's own verification process, not a product defect; the remaining runs, each alone, were clean): 483 passed (480 + 3 new), 0 regressions, every time. `ruff check` clean. Full 50-spec Playwright suite, run against freshly reseeded data at each stage: 48 passed / 2 failed before any race fix (`project-history.spec.ts`, `requirements-and-cr-filters.spec.ts`); 49 passed / 1 failed after fixing those two (`org-security-controls.spec.ts`, root-caused rather than accepted as flaky per the correction above); **50 passed, 0 failed** on the final run after fixing all three.

## Project statuses, typed bidirectional requirement links, and requirement actions

User request (backend half of an approved plan; frontend is a separate follow-up pass): three new capabilities — org-definable project statuses, org-extensible bidirectional requirement links (replacing the previous fixed `RequirementLinkType` enum), and requirement actions (a review/test task with its own identity, shareable across multiple requirements). Per the user's explicit direction and `docs/requirements.md` being authoritative and off-limits to the agent, the design rationale lives here rather than there.

**Org-definable project statuses (`ProjectStatusDefinition`)**: every organisation is seeded with 4 defaults (Proposed/Active/Abandoned/Completed) at creation; `Project.status_id` is `NOT NULL` with no `ON DELETE` action (implicit `RESTRICT`). This is deliberate double coverage, not redundancy for its own sake: the app layer already refuses to delete an in-use status (409, naming the affected count) and refuses to leave an organisation with zero statuses (409, so a project's status picker/default can never end up empty) — the DB-level `RESTRICT` is a second backstop against any future code path that forgets the app-layer check, since silently orphaning a `NOT NULL` foreign key would corrupt data far worse than a delete simply failing.

**Typed, bidirectional, org-extensible requirement links (`RequirementLinkTypeDefinition`)**: replaces the fixed `RequirementLinkType` enum (`relates_to`/`depends_on`/`derived_from`) with an org-definable table storing a `forward_name`/`reverse_name` pair per type (e.g. "Derives from" / "Is the source of"; a symmetric relationship like "Related to" just repeats the same name in both columns), seeded with 12 defaults per organisation and freely extensible beyond that. A client-side naming-convention heuristic (e.g. stripping/negating a word to guess the reverse direction) was rejected in favour of storing both names explicitly and resolving direction server-side, per request, in `routers/requirements.py::_link_to_out` — the requirement being viewed is compared against the link's `source_requirement_id` to decide `direction` (`"outgoing"`/`"incoming"`) and pick `forward_name` vs. `reverse_name`; a client-side guess would be both fragile (no reliable general rule for turning "Depends on" into "Is a dependency of") and duplicated logic the server already has to get right for validation anyway. `RequirementLink.link_type_id` is `NOT NULL` (no `RESTRICT`-vs-`CASCADE` ambiguity, since deleting a link type follows the same reassign-or-block rule as project statuses, never a bare cascade that would silently reinterpret a link's meaning).

**The enum-to-FK migration (`0012_project_statuses_link_types_actions.py`)**: the trickiest backfill in this change. `requirement_links.link_type` (old, 3-valued VARCHAR enum) is mapped to the new per-organisation `requirement_link_type_definitions` row via a `CASE rl.link_type WHEN 'relates_to' THEN 'Related to' ... END` join against the source requirement's own project's organisation, guarded by an `information_schema.columns` existence check so the whole block is a genuine no-op on a brand-new database (whose `0001_initial.py` builds straight from the *current* models, which never had a `link_type` column at all). The old 3-part unique constraint is located and dropped by inspecting `pg_constraint`/`pg_attribute` for column membership rather than a literal `DROP CONSTRAINT <name>`, because Postgres silently truncates that constraint's default-generated name past its 63-byte `NAMEDATALEN` limit — not worth trying to reproduce by hand. Two of the *new* schema's own unique constraints hit the same 63-byte limit (`requirement_link_type_definitions(organization_id, forward_name)` and `requirement_links(source_requirement_id, target_requirement_id, link_type_id)`), so both models were given an explicit short `name=` in their `UniqueConstraint(...)` — the same fix this codebase already established for this exact class of problem in migration `0009` (org-group nesting) — rather than leaving Postgres to truncate an 78-/66-byte auto-generated name unpredictably, which would have made a legacy-database migration's constraint name a fragile guess.

**Requirement actions (`RequirementAction`)**: a required task (review, test, etc.) gets its own project-scoped identity — its own `unique_code`, generated the same way `Requirement.unique_code` is, via a new `Project.next_action_seq` counter — rather than belonging to exactly one requirement, so `RequirementActionLink` is a genuine many-to-many join: one action (e.g. a single wind-tunnel stability test) can satisfy several requirements at once, and unlinking it from one requirement never deletes it or affects any other requirement still linked to it. `RequirementActionOutcome` (pending/completed/failed) stays a small fixed enum rather than a fourth org/project-configurable definition table alongside statuses/link-types/action-types — nothing in the request asks this vocabulary to vary per org/project, and every comparable lifecycle vocabulary already in this codebase (`RequirementReviewOutcome`, `ChangeRequestStatus`) is a fixed enum too. `completed_at`/`completed_by` stamp the moment `outcome_status` first leaves `PENDING` (`services/actions.py::apply_outcome_transition`) and clear again if reverted back to `PENDING`, mirroring `uncomplete_requirement`'s existing "revert a mistake" pattern. An action's own discussion thread and file attachments reuse the existing generic `ReviewComment`/`CommentFile` machinery (`ReviewTargetType.ACTION`, a third target type alongside requirement and change request) and a `RequirementActionFile` table structurally identical to `RequirementFile`, rather than a fourth parallel comment/attachment system.

**Action types are project-scoped, not org-scoped** (`ActionTypeDefinition`, matching `CustomFieldDefinition`'s existing project-scoping) — an explicit product decision made with an eye toward a possible future where projects can nest. No nested-project mechanism exists today and none was built here; the only concession made for that possible future is keeping this a plain per-project FK rather than baking a flat org-wide list in anywhere, so a later "fall back to the parent project's action types" resolution could be layered on top of this shape without a schema rework.

**Shared rename/delete/reassignment rules across all three definition tables** (`services/definitions.py::delete_definition_with_reassignment`, used identically by `ProjectStatusDefinition`, `RequirementLinkTypeDefinition`, and `ActionTypeDefinition`): renaming is a plain name update with zero cascading effect (every reference points at the row's id, never its name); a scope (organisation for statuses/link-types, project for action-types) must always retain at least one row, since the referencing FK is `NOT NULL` and a creation flow needs something to default to; deleting a row currently referenced by other rows requires an explicit `reassign_to_id` (validated same-scope, not-self) rather than a flat block or a silent cascade — the admin gets to choose where affected rows land, and both the reassignment and the subsequent delete are logged as separate audit events in one transaction. This one shared implementation, rather than three near-identical ad-hoc ones, is also why `_move_ordered` was extracted out of `routers/projects.py` into `services/ordering.py::move_ordered` — the same up/down swap logic three more reorder endpoints now needed.

**Reuse over duplication, applied consistently**: `services/actions.py::get_requirement_action_in_project`/`action_to_out` and the `services/definitions.py` helpers above are the two clearest instances, but the same principle shaped several smaller choices — `routers/actions.py` has its own small `_require_edit_role`/`CAN_EDIT_ROLES` rather than importing `routers/requirements.py`'s private copies (this codebase has no precedent for one router importing another's private helpers; the two IDOR-guard/output-builder functions above went into a service module specifically because *they* are needed by two different routers, `routers.actions` and `routers.requirements`'s new requirement<->action linking section).

**Existing code updated for the schema change**: `services/project_export.py` (bundle export/import) now matches a requirement link's type by `forward_name` within the *target* organisation rather than by enum value — like every other cross-tenant reference in a bundle (users by email, custom fields by name), a source-deployment link-type id means nothing on import; a bundle link whose type has no match in the target organisation is skipped with a warning rather than guessed. `services/requirement_csv.py`'s CSV export renders a link's forward name the same way. `routers/projects.py::create_project`/`services/templates.py::clone_project` both now assign `status_id` (defaulting to the organisation's first status; a cloned project never inherits the *template's own current* status, since that's lifecycle state, not portable configuration — same treatment `clone_project` already gives stages/baselines) and seed default action types — freshly for a from-scratch project, copied from the template for a cloned one (matching how custom field definitions are already copied).

**Verified**: full backend suite, run against the real Postgres test database per `backend/tests/conftest.py`'s established convention — 531 passed, 0 failed (483 pre-existing + 48 new, across 6 new test files covering create/list/reorder/rename/delete for all three definition tables including the full §4.0 rename/reorder/minimum-count/reassignment/cross-scope-IDOR matrix, directional `display_name` resolution from both ends of a link, action CRUD/archive/outcome-transition-stamping/comments/file-attachments, and multi-requirement action sharing/unlink-doesn't-delete) plus 2 pre-existing tests updated for the new required `link_type_id` field. Also found and fixed along the way: two pre-existing tests in `test_invites_and_external_users.py` that constructed `Project(...)` rows directly via the ORM (bypassing the API) started failing a `NOT NULL` constraint on `status_id` once that column became mandatory — fixed by seeding default statuses and passing an explicit `status_id`, the same pattern every other direct-ORM `Project` construction in this change now follows. `test_schema_migrations_match_models.py` passes (the Alembic-produced schema matches the models exactly, including the two explicitly-named unique constraints above). `alembic upgrade head` verified clean against a genuinely fresh database. The tricky enum-to-FK backfill's SQL was additionally verified in isolation against a hand-built minimal schema (`CASE 'depends_on' -> 'Depends on'` correctly resolves to the matching `requirement_link_type_definitions` row), since no pre-existing production-like database exists yet to replay the legacy upgrade path against end-to-end (this is still pre-release, per `0001_initial.py`'s own docstring). `ruff check` clean. Both `backend/scripts/seed_demo_data.py` and `seed_e2e_dataset.py` updated and run end-to-end against a live server: the demo dataset gets a custom "Supersedes" link type, 3 requirement links (2 default-typed, 1 custom-typed), one project moved to "Active" status, and 2 requirement actions (one completed review, one failed test, the failed one shared across two requirements); the E2E dataset gets a fixed custom link type, one fixed requirement link, and two fixed-title requirement actions (one completed, one pending) for a follow-up Playwright pass to write specs against.

## Project statuses, typed bidirectional requirement links, and requirement actions — frontend

Frontend half of the approved plan whose backend half is the entry immediately above: admin UI for the two new org-scoped definition tables and the one project-scoped one, a project status picker, a greenfield requirement-links UI, and a new requirement-actions feature (its own project-level pages plus a linking card on the requirement detail page). No schema/API changes; this pass consumes what the backend entry above already shipped.

**Org admin UI (`OrgAdminPage.tsx`) — Project Statuses and Link Types tabs**: both are new `CollapsibleSection`s built as a direct copy of the existing custom-fields admin pattern already on this page (add-row, inline rename, up/down reorder), with link types needing two name inputs per row (`forward_name`/`reverse_name`) instead of one. **Project Admin UI (`ProjectAdminPage.tsx`) — Action Types tab**: identical pattern, project-scoped instead of org-scoped, consistent with action types themselves being project-scoped per the backend entry's own reasoning.

**Delete/reassign UX, shared across all three lists**: rather than a bespoke confirm dialog per table, all three follow one interaction shape that mirrors the backend's own shared `reassign_to_id` contract exactly — click delete, call `DELETE` with no `reassign_to_id`; a plain success just removes the row; a 409 opens an inline reassignment picker showing the server's own in-use count message verbatim (never a generic re-derived one, so the picker can't drift out of sync with whatever count logic the backend uses) with a `<select>` of the scope's other rows, and only resubmits with `reassign_to_id` once something is chosen (`Confirm delete` stays disabled until then). The delete button itself is disabled — with a `title` tooltip, not silently — whenever the list has exactly one row left, since the backend's own minimum-one-remaining rule (§4.0 of the plan) guarantees that delete would 409 anyway; sending a request the UI already knows will fail would just be a slower way to show the same error. This is one interaction pattern implemented three times with different field names, not three separate designs, matching the backend's own single shared `services/definitions.py` helper.

**Project status picker (`ProjectAdminPage.tsx`)**: a plain `<select>` next to the pre-existing `visibility` control on the project settings tab, populated from the parent organisation's `project-statuses` list and bound to `status_id` on the existing `ProjectUpdate` PATCH call — no new save path, since `status_id` is just one more field on a form that already had a save button.

**Requirement links UI (`RequirementDetailPage.tsx`) — greenfield, no prior frontend consumer existed**: a "Links" card listing every link sorted by `display_name` then the other requirement's unique code, each row a clickable link to that requirement plus a remove button, and an inline add-link form (a requirement picker plus a `link_type_id` select rendering each type's `forward_name`, since the requirement currently being viewed is always the link's source from this page's point of view — the backend resolves `direction`/`display_name` per request, per the backend entry's reasoning, so this page never has to compute or guess which name applies). Neither the list nor the add-link form is gated by `requirement.is_locked`: links are traceability metadata about the requirement, not its governed content, the same distinction the backend entry already draws and the same one this codebase already applies to `ReviewComment` discussion threads.

**`FileAttachmentList.tsx` extraction**: the file-list-plus-upload-plus-remove block that used to live inline in `RequirementDetailPage.tsx`'s Attachments card is now a small shared component (`files`, `onUpload?`, `onRemove?`, `disabled?`, `emptyHint?`), used unchanged by both the requirement's own Attachments card and the new `ActionDetailPage.tsx`. `disabled`/`emptyHint` exist specifically for the requirement side's "approved, use a change request instead" lock (C-G-12); actions have no such lock concept, so `ActionDetailPage.tsx` simply never passes `disabled`, rather than the component needing to know which kind of entity it's attached to.

**Requirement actions UI — two new pages plus a card on the requirement detail page**: `ProjectActionsPage.tsx` is the project-wide home for actions (table of unique code/title/type/outcome/assignee/due date, filters mirroring the backend's own `outcome_status`/`action_type_id`/`include_archived` query params, and a "New Action" form) — it exists because a `RequirementAction` has its own project-scoped identity independent of any one requirement, per the backend entry, so it needs a browsing home that isn't nested under a single requirement. `ActionDetailPage.tsx` is the single-action view: editable fields, an outcome-status control, the reverse list of every requirement this action is linked to (computed client-side by walking the project's requirements and calling each one's `/actions` endpoint, since no bulk "which requirements link this action" endpoint exists and one wasn't worth adding for a single detail page), a reused `FileAttachmentList`, and a reused `CommentThread`. The **Actions card** on `RequirementDetailPage.tsx` covers the requirement-side view of the same relationship: a linked-actions list with unlink, a "link existing action" picker, and a "create and link" inline form that posts to the backend's own `create-and-link` endpoint in one round trip rather than two separate requests. New routes `/projects/:projectId/actions` and `/projects/:projectId/actions/:actionId` in `App.tsx`, plus an "Actions" nav-rail entry in `Layout.tsx`.

**No new API-client abstraction**: per this codebase's existing convention (no per-entity wrapper functions in `api/client.ts`), every new page calls `api.get/post/patch/delete` directly with literal URL strings, matching every other page in the app — `api/client.ts` itself needed no changes for this pass.

**Verified**: `cd frontend && npm run build` (`tsc -b && vite build`) clean, 0 TypeScript errors. `npx eslint .` clean — 5 warnings, identical in count and location to the pre-existing baseline on `main` (all in untouched `context/BrandingContext.tsx`/`context/ThemeContext.tsx`, `react-refresh/only-export-components`; none in any new or changed file). `npx storybook build` succeeded; all 16 new story entries (`FileAttachmentList` x7, `ActionDetailPage` x5, `ProjectActionsPage` x5, plus the extended `OrgAdminPage`/`ProjectAdminPage`/`RequirementDetailPage` story sets covering delete-disabled-at-last-row, delete-in-use-opens-reassign-picker, populated Links/Actions cards, add-link, and create-and-link-action) are present in the built `index.json`. Docker Compose dev/test stack (`tests/container/`) rebuilt and recreated per this repo's "containers don't bind-mount source" rule; backend suite re-run against it — 531 passed, 0 failed, confirming the backend entry's own numbers still hold unchanged. Full Playwright suite run against a freshly reset stack (`docker compose down -v && up -d --build`, both `seed_e2e_dataset.py` and `seed_demo_data.py` re-run against the clean database) exactly once: **54 passed, 0 failed** — all 4 new specs (`org-admin-project-statuses-and-link-types.spec.ts`, `project-admin-action-types.spec.ts`, `requirement-actions.spec.ts`, `requirement-links.spec.ts`) plus the full 50-spec pre-existing suite, none regressed by the `RequirementLink`/`Project` type-shape changes touching `RequirementDetailPage.tsx`. (An earlier run of the full suite, done without resetting the database between the standalone new-spec run and the full-suite run, showed 4 failures; investigation traced every one to the mutating new specs — add/rename/delete operations — colliding with state they'd already mutated on the prior pass, not to any application defect, and the clean re-run above confirms it.) `README.md` updated with a short mention of links/actions/statuses in the existing requirements-workflow paragraph, not a rewrite. `docs/solution-architecture.md` was already updated by the backend pass (new ER diagram sections); no further architectural change here since this pass is UI-only.

## PR #8 CI failure: `npm ci` rejected a lockfile that installed fine locally, and closing the Node-version gap between local dev and CI/Docker for good

CI's `frontend` job failed on `npm ci` with `npm error Missing: vite@8.2.1 from lock file` (also `fsevents@2.3.3`), even though `frontend/package-lock.json` had been committed straight after a locally-successful `npm install`/`npm run build`. Reproduced exactly by running `npm ci` against the committed lockfile inside a `node:20` container (matching `ci.yml`'s then-`node-version: "20"`, npm 10.8.2) — it failed there too, while the same lockfile installed cleanly with the local machine's npm 11.16 (bundled with a locally-installed Node 24, via `nvm`). Root cause: npm 11 resolved `@vitest/browser`'s and `@vitest/browser-playwright`'s nested `vite`/`fsevents` optional dependencies differently than npm 10.8.2 does, and wrote a lockfile missing the nested `node_modules/@vitest/browser/node_modules/vite` (and `.../browser-playwright/...`) entries that npm 10.8.2 expects — a lockfile that was complete and correct for npm 11, not corrupt, just wrong for the npm version CI and `frontend/Dockerfile` (then `node:20-alpine`) actually used. The user reported this as a *recurring* issue specifically after agent sessions, meaning previous passes had almost certainly hit the same class of drift; they fixed the immediate break themselves (`11c6a6d`, `5c54c4a` — regenerating `package-lock.json`, byte-identical to this investigation's own repro/regeneration) while this investigation was still in progress, leaving the recurrence-prevention work below.

**First attempt — pin local dev down to CI's Node 20 — abandoned per user feedback.** Initial approach added `frontend/.nvmrc` (`20`) and a `frontend/scripts/docker-npm-install.sh` that regenerated the lockfile inside `node:20-alpine` via Docker, plus an enforced Docker-based pre-commit hook. The user objected to requiring Docker just to commit a change, and separately asked whether CI could instead move to a later Node version — Node 20 was at/near its LTS end-of-life anyway, while this machine's (and presumably other agent sessions') default `nvm` Node was already 24. Regenerating under a stale `node_modules` from an *earlier* Docker attempt (bind-mounting a temp dir that still had a prior run's `node_modules`) silently reused macOS-only `@rollup/rollup-darwin-x64` and produced a lockfile missing every Linux rollup variant — worth remembering if a Docker-based regeneration is ever revisited: always use an isolated dir with *no* pre-existing `node_modules`, never a bind-mounted host directory another platform's install has already touched.

**Final fix — raise CI/Docker to Node 24 to match the environment agents already default to, instead of forcing every environment down to an older pin.** `.github/workflows/ci.yml` (`frontend` and `backend-and-e2e` jobs) and `frontend/Dockerfile` now use Node 24; `frontend/.nvmrc` and `package.json`'s new `"engines": {"node": "24.x"}` pin the same for local use. This makes the *local* npm 11.x that caused the original break the version CI now runs too, closing the gap from the other direction and removing Docker from the loop entirely: `frontend/scripts/sync-lockfile.sh` (replacing the abandoned `docker-npm-install.sh`) is now the required way to add/remove/bump a frontend dependency — it refuses to run under the wrong Node major version (checked against `.nvmrc`), reinstalls from a clean `node_modules`, and re-verifies with `npm ci`, all locally, no container. A tracked `.githooks/pre-commit` (wired up automatically via a `postinstall` script in `frontend/package.json` that sets `core.hooksPath` — a no-op, swallowed by `2>/dev/null || true`, in `frontend/Dockerfile`'s build where `.git` doesn't exist) runs `npm ci --dry-run` — non-destructive, writes nothing to disk — whenever `frontend/package.json` or `package-lock.json` is staged, and blocks the commit if they're out of sync. [CLAUDE.md](../CLAUDE.md) ("Frontend Dependency Changes") and [docs/development.md](development.md) ("Changing frontend dependencies") both document this so it reproduces automatically for future agent sessions.

**Verified**: `npm ci`/`npm run lint`/`npm run build` all pass under a fresh `node:24-alpine` container (matching the updated Dockerfile/CI exactly) from a clean checkout — lint shows the same 5 pre-existing warnings as the baseline entry above, build clean; `tests/playwright/package-lock.json` (also now installed with Node 24 in CI) verified with a clean `npm ci` too. The `postinstall` wiring was confirmed to actually set `core.hooksPath` after a plain `npm install`, to no-op harmlessly under `node:24-alpine` with no `.git` present (Dockerfile build path), and the hook itself was exercised both directly and via a real `git commit` against a deliberately desynced `package.json` — blocked with exit 1 and no commit created, then passed cleanly once reverted. Files changed: `.github/workflows/ci.yml`, `frontend/Dockerfile`, `frontend/package.json` (`engines` + `postinstall`), `frontend/package-lock.json`, `frontend/.nvmrc`, `frontend/scripts/sync-lockfile.sh` (new), `.githooks/pre-commit` (new), `CLAUDE.md`, `docs/development.md` — no application code touched.

## Mobile nav-rail collapse toggle was inert (reported: "shown but does not do anything")

Root cause was a pre-existing, deliberate responsive rule in `frontend/src/styles/theme.css` (`@media (max-width: 860px)`, added when the rail/sidebar responsive layout was built): below 860px there isn't room for a full rail, content column, and the 240px filter/activity sidebar side by side, so the rule force-collapses `.nav-rail:not(.nav-rail-icons)` to icon-only width unconditionally. `Layout.tsx`'s expand/collapse button (`railCollapsed` state, persisted server-side via `useUiPreference`) still toggled correctly and re-rendered the class/icon/label, but the CSS override made both states render identically at that width — the button was fully functional, just visibly doing nothing, which reads as broken. This is in tension with `docs/requirements.md` U-P-02 ("fully responsive... including mobile devices").

**Fix**: added a `nav-rail-toggle-row` class to the toggle's wrapping `<div>` and hid it inside the existing 860px media query in `theme.css`, rather than changing the force-collapse behavior itself — the rail's forced icon-only layout below that width is intentional and still needed, only the now-inert control needed to go. A true off-canvas mobile drawer (so the rail can be fully hidden, not just icon-width, on phones) was scoped out as a larger redesign and flagged separately rather than folded into this fix.

**Verified**: new spec `tests/playwright/tests/e2e-workflows/mobile-nav-rail.spec.ts` — asserts the toggle is hidden at 700px width and visible again at 1280px, against a rebuilt/recreated `tests/container` frontend image. Passed. `golden-path.spec.ts` re-run clean (50/50 unaffected by the CSS-only change). Files changed: `frontend/src/components/Layout.tsx`, `frontend/src/styles/theme.css`, `tests/playwright/tests/e2e-workflows/mobile-nav-rail.spec.ts` (new).

## Two more bugs found during the same UX audit's deeper pass: a mobile card-grid overflow, and a dead-end 2FA login on org-branded pages

A follow-up, deeper round of the same UX audit (user asked to "go deeper" after the initial pass above) turned up two more small, self-contained bugs alongside a much larger set of design-consistency findings that stayed in the audit document rather than becoming code changes yet. Per this repo's own policy, a found bug gets fixed as part of the same pass rather than deferred — both were small and unambiguous enough to fix immediately, unlike the broader redesign work the audit is still gathering sign-off on.

**Mobile card-grid overflow**: `RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, and `FavouritesPage.tsx` all sized their tile/card views with a fixed `gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))"`. Below ~431px viewport width the collapsed nav rail (56px) plus `.app-content`'s own 24px-each-side padding leaves less than 280px for the grid itself (e.g. 375 − 56 − 24 − 24 = 271px), so the single column that still fit was forced wider than its container — a reproducible ~9px of horizontal overflow and a page-level horizontal scrollbar on an iPhone SE/mini-class (375px) viewport. Fixed by clamping the track minimum to `minmax(min(280px, 100%), 1fr)` in all four files, so it never exceeds whatever width is actually available — a one-line change per file, no visual difference at any normal desktop/tablet width. `ProjectOverviewPage.tsx`'s own card grid already used a smaller `minmax(240px, 1fr)` and was never affected, left unchanged.

**Org-branded login's 2FA branch was a silent dead end**: `OrgLoginPage.tsx`'s own code comment claimed a 2FA challenge on an org-branded (`/login/:slug`) page "falls through to the plain /login flow rather than duplicating the code-entry form here" — but `handleSubmit` only ever branched on the non-2FA case; when `login()` returned `requires2fa: true`, nothing happened at all. No redirect, no error, no code-entry form — the submit button just stopped showing its loading state and the same email/password form sat there with zero indication anything needed to happen next. Fixed by actually implementing the documented fallback: `OrgLoginPage.tsx` now calls `navigate("/login", { state: { challengeToken: result.challengeToken } })` on the 2FA branch, and `LoginPage.tsx` reads an optional `challengeToken` out of router state to initialise its own `challengeToken` state, so it opens straight on the code-entry step instead of asking for email/password a second time (the already-issued challenge token is a plain string good for one `verify2fa` call regardless of which page holds it, so no backend change was needed).

**Verified**: `cd frontend && npx tsc -b` clean. New specs: `tests/playwright/tests/e2e-workflows/mobile-card-grid-overflow.spec.ts` (asserts `document.documentElement.scrollWidth <= clientWidth` at a 375×900 viewport across all four affected tile/card views — all 4 passed) and `tests/playwright/tests/e2e-workflows/org-login-2fa-handoff.spec.ts` (gives an org a slug via API, enrols the zero-org-membership "orphan" persona in real TOTP 2FA — same persona/pattern `two-factor-auth.spec.ts` already uses so no shared persona is left in a 2FA-enabled state for other specs — then drives a real browser through the org-branded page to confirm it lands on `/login`'s code-entry step rather than the pre-fix dead end; disables 2FA again at the end). Both run against a rebuilt/recreated `tests/container` frontend image. `golden-path.spec.ts` and `mobile-nav-rail.spec.ts` re-run clean, confirming no regression from either change. One unrelated hiccup during verification: the shared "orphan" persona was found already deactivated (`is_active: false`) from an earlier, unrelated test run against this long-lived dev stack — reactivated via `POST /api/v1/system/users/{id}/reactivate` before the new 2FA spec would run; not a defect in this change, just the same "continuously-mutating shared database" caveat `docs/e2e-workflows.md` already documents. Files changed: `frontend/src/pages/RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, `FavouritesPage.tsx`, `OrgLoginPage.tsx`, `LoginPage.tsx`; new specs `mobile-card-grid-overflow.spec.ts`, `org-login-2fa-handoff.spec.ts`.

## UX audit and style guide: navigation, settings hierarchy, create flows, confirmation, terminology, and scale

The three bug fixes above were incidental discoveries from a broader, user-requested UX audit ("assume the role of a team manager for a team of UX designers"), run in four passes covering every page's navigation, tabs/accordions/modals, create-entity flows, destructive-action confirmation, platform-default overrides, multi-org navigation, auth screens, accessibility, copy/icon/badge consistency, terminology-override coverage, and list pagination against the app's own U-P-06 recommendation.

**New durable output — [docs/ux-style-guide.md](ux-style-guide.md)**: nine principles (settings-hierarchy depth model, override visibility, create-as-a-layer, one-component-per-pattern, one door for single/bulk create, two-tier confirmation, feedback-on-every-mutation, accessible-control-naming, wayfinding) plus five worked patterns (settings hierarchy, platform-default overrides, create panels/popovers/bulk, confirmation tiers + toast, an org-switcher wayfinding pattern modelled on Azure/Entra's tenant switcher) and a token reference documenting `frontend/src/styles/theme.css`'s existing colour/spacing system as the design system it already functions as, rather than proposing a new one. Normative for new/reworked frontend UI going forward.

**New point-in-time output — [docs/ux-audit-2026-08.md](ux-audit-2026-08.md)**: the findings that motivated each style-guide rule, plus an effort-sorted roadmap (24 items, S/M/L) tracked with a Status column. The two most severe findings, both surfaced only on the fourth pass: (1) the project-level terminology-override system (rename "requirement"/"change request"/"project"/"stage"/"component"/"category" per project) is consumed in only 5 call sites across 3 files — 4 of the 6 configurable terms have zero effect anywhere, and even the 2 partially-working ones leak the hardcoded English noun back in on the requirement detail page, comments, review-due lists, the CSV import wizard, and (most visibly) Project Admin's own custom-fields entity-kind dropdown, one tab over from the Terminology settings meant to rename those exact words; (2) two list views — a project's full change-history/audit timeline and the server-admin's cross-org user directory — have no `limit`/`offset` pagination on either the frontend request or the backend endpoint, in tension with `docs/requirements.md` U-P-06's (Recommended-priority) call for lazy-loading large data sets, while `ProjectListPage` already implements the same pattern correctly elsewhere in the app.

Neither finding was acted on in this pass — per the user's own explicit sequencing ("audit first, small fix now"), only the three small, unambiguous bugs above were fixed immediately; the structural findings (terminology coverage, list pagination, the org switcher, the Org Admin/Project Admin restructuring, and the new shared `Tabs`/`SidePanel`/`Popover`/`Modal`-confirm/`Toast` components) are scoped as roadmap items for a deliberate follow-up implementation pass, not silently deferred — see `docs/ux-audit-2026-08.md`'s Roadmap table for the full, current-as-of-this-entry list and status of each.

**Verified**: both new docs' Mermaid diagrams checked for balanced fences and valid node/edge syntax (reused, unmodified, from the diagrams already validated in the interactive review artifact this pass distilled from). `README.md` updated: the intro doc-links paragraph now points to both new files, and Known Limitations gets a one-sentence pointer to the audit's headline gaps (settings-hierarchy inconsistency, confirmation inconsistency, terminology coverage, unpaginated lists) rather than restating them. No application code changed in this entry — see the entry immediately above for the three code fixes this same audit pass produced.

## UX style guide: illustrated current-vs-proposed mockups, and phase 1 of the implementation roadmap

Two follow-ups to the UX audit/style guide entry above, same day.

**Illustrated current-vs-proposed mockups for `docs/ux-style-guide.md`'s five patterns.** The user asked for real before/after images in the style guide itself, not just ASCII wireframes — matching the polished visual mockups already built for the interactive review artifact, but explicitly as illustrative mocks rather than literal app screenshots. Reused that artifact's exact mock-shot HTML/CSS markup (styled with the product's own light-theme token values) in a standalone local HTML file (nine `current`/`proposed` pairs: settings hierarchy, platform-default override, new-group popover, bulk-entry-point split button, change-request creation, two confirmation tiers, post-mutation toast feedback, and the org switcher), then rendered each pair to a PNG via a short headless-Chromium Playwright script (`chromium.launch()` + per-element `.screenshot()`, 2x device-scale-factor) using the `playwright` package already installed for `tests/playwright/`. Saved to `docs/figures/ux-style-guide/` (new directory, following the existing `docs/figures/architecture/` convention) and embedded in `docs/ux-style-guide.md` via standard Markdown image syntax, each with descriptive alt text, replacing the ASCII-art placeholders from the first pass. Two of the nine were initially clipped (the new-group popover's Cancel/Create row and the type-to-confirm dialog's buttons, both cut off by their mock container's `overflow: hidden` combined with an undersized `min-height` for absolutely-positioned content) — fixed by widening the containers and, for the popover, switching to `overflow: visible`, then re-rendered and visually re-verified. Total image payload ~1.0MB across 9 files, reasonable for a git-committed doc. No source `.html`/`.cjs` scratch files were committed — both lived in the session scratchpad and were discarded after use, matching how the interactive review artifact itself was never checked into the repo. Per the user's follow-up correction, the prose immediately surrounding each image was then trimmed to only what isn't already visible in the picture itself (implementation implications, disambiguating a hypothetical panel from a real one) — these mockups get the same "screenshots don't need a full explainer" treatment `CLAUDE.md` already grants README images, since the current/proposed labels and captions baked into each image already carry the reading key a diagram would otherwise need spelled out in surrounding text.

**Implementation, phase 1: the roadmap's S-effort quick wins.** Started work against `docs/ux-audit-2026-08.md`'s roadmap, beginning with the smallest, self-contained, no-design-decision items first (before the larger shared-component/backend/architectural items, which get scoped with the user before starting): `ReportsPage.tsx` gained the `Spinner` loading guard every other list-driven page already had (`!project || !reportConfig` — it was the one page of those audited that rendered empty dropdowns/rich-text fields until its `Promise.all` resolved); `RequirementDetailPage.tsx`'s archive button now calls `window.confirm(strings.requirements.archiveConfirm)` before archiving, matching `ActionDetailPage.tsx`'s existing behaviour for the structurally identical action (new `archiveConfirm` string added next to the existing `archive` button-label string in `strings.ts`, confirmed against `backend/app/routers/requirements.py::delete_requirement`'s own docstring — "Archives (soft-deletes)... preserving its history" — so the confirm copy accurately describes it as reversible-via-history rather than a true delete).

**Verified**: `cd frontend && npx tsc -b` clean. New spec `tests/playwright/tests/e2e-workflows/requirement-archive-confirm.spec.ts` — creates a throwaway requirement (never touches seeded data, since archiving isn't reversible from the UI), confirms cancelling the dialog leaves it in place, confirms accepting it archives and returns to the list, and asserts the dialog's exact message text. Initial version raced `page.waitForEvent("dialog")` against the triggering `.click()` inside one `Promise.all` and hung — native `confirm()` blocks the page's JS thread until resolved, so `.click()` never settled; fixed by switching to this repo's own established convention (`page.once("dialog", ...)` registered before the click, seen already in `personal-access-tokens.spec.ts`/`server-org-lifecycle.spec.ts`/`user-directory-and-bans.spec.ts`) rather than inventing a new pattern. Existing report-generation coverage (`report-image-attachment.spec.ts`) re-run clean against the `ReportsPage.tsx` change. `golden-path.spec.ts`, `mobile-nav-rail.spec.ts`, and `mobile-card-grid-overflow.spec.ts` re-run clean, confirming no regression. Both changes rebuilt/recreated in the `tests/container` frontend image before testing, per this repo's "containers don't bind-mount source" convention. `docs/ux-audit-2026-08.md`'s roadmap table Status column updated to "Done" for both items. Files changed: `frontend/src/pages/ReportsPage.tsx`, `frontend/src/pages/RequirementDetailPage.tsx`, `frontend/src/i18n/strings.ts`; new spec `requirement-archive-confirm.spec.ts`.

**Correction, same day: the "org switcher" finding and its Principle 9/wayfinding pattern were wrong, not just imprecise.** The user pushed back immediately on reading it: `ProjectListPage` and everything scoped under a project already deliberately pool across every org a user belongs to (an org column/filter is surfaced only when there's more than one org to disambiguate), so an Azure/Entra-style tenant switcher — which exists specifically to scope an *entire portal* to one tenant at a time — was the wrong model to reach for. It would have suggested exclusive org-scoping the app doesn't have and, per the roadmap item it produced, would have needed a new `CurrentOrgContext` threaded through the whole frontend to solve a problem that didn't actually exist. Re-examined and corrected rather than left in place: the real, much narrower gap is that `/orgs` (the only path to org *administration*, as opposed to content browsing) has no nav-rail entry point for anyone but a server admin — a one-link fix, no new state or component. Updated: `docs/ux-style-guide.md`'s Principle 9 and its "Pattern: wayfinding" section (rewritten, no longer proposing a header switcher), `docs/ux-audit-2026-08.md`'s "at a glance" row (severity dropped High → Medium, description corrected) and its "Navigation & the orphan page" section (the incorrect framing left in place with an explicit note explaining what was wrong and why, rather than silently rewritten, so a reader following the document's own history can see the correction happened), and the roadmap row (L-effort "org switcher + CurrentOrgContext" replaced with S-effort "nav-rail entry point to `/orgs`"). The now-inaccurate `figures/ux-style-guide/wayfinding-org-switcher.png` mockup was deleted rather than left orphaned. No code was affected — the org-switcher item was never implemented, only documented, so this is a docs-only correction.

## Style guide binding on future work; implementation phase 1 completed (nav link, RichTextEditor Modal, aria-label sweep); three regressions found and fixed during full-suite verification

Two more pieces of the same day's work. First, per explicit user request, [CLAUDE.md](../CLAUDE.md) gained a new "Frontend UX Style Guide Adherence" section (mirroring the existing SOC 2 policy section's binding language) making `docs/ux-style-guide.md` binding on new/reworked frontend UI for this and future agent sessions, not just descriptive — including a pointer to check the audit's roadmap before building a shared component from scratch, so the "newest feature already reproduces the pattern" problem the audit found doesn't keep recurring.

Second, the remaining phase-1 (S-effort) roadmap items were implemented: the corrected nav-rail link to `/orgs` (`Layout.tsx`, new `strings.nav.myOrganizations(orgPlural)` terminology-aware label, `Building2` icon reused from the existing server-admin equivalent — placed unconditionally in the Global section rather than gated on org-admin role, since determining that client-side would need new data-fetching the corrected finding explicitly argued against); `RichTextEditor.tsx`'s link-insert `window.prompt()` replaced with the shared `Modal` (a `linkModalOpen`/`linkUrl` state pair, a small form with a disabled-until-non-empty Insert button, `exec("createLink", ...)` unchanged — the file already had no `strings.ts` usage for its sibling image-picker panel, so plain literals were used here too rather than introducing a new convention for one component); and the aria-label sweep, delegated to a background agent scoped to exactly six files (`OrgAdminPage.tsx`, `ProjectAdminPage.tsx`, `RequirementDetailPage.tsx`, `CommentThread.tsx`, `ReportChapterListEditor.tsx`, `FileAttachmentList.tsx`) with the established `title`+matching-`aria-label` convention, sourcing text from `strings.ts` where a suitable key already existed and adding six new ones (`admin.removeMember`, `admin.removeNestedGroup`, `admin.deleteCustomField`, `orgAdmin.deleteReportTemplate`, `orgAdmin.deleteResource`, `orgAdmin.removeSsoMapping`) where none did — reviewed by reading its diffs directly rather than trusting its summary alone, per this session's own practice.

**Verification surfaced three real regressions**, distinct from the pre-existing "full-suite ordering collision" flakiness `docs/e2e-workflows.md` already documents (confirmed as that separate, expected category by rerunning each flaky spec alone against a fresh reseed, where all of them passed) — these three did not:
1. `workflow-bypass-attempts.spec.ts` clicked the requirement Archive button and asserted immediate navigation, with no dialog handler — broken by this same day's earlier archive-confirm addition. Fixed by adding `page.once("dialog", (dialog) => void dialog.accept())` before the click, matching the pattern `requirement-archive-confirm.spec.ts` already established.
2. `org-label.spec.ts` asserted `getByRole("link", { name: "Tenants" })` (no `exact`) expecting one match; the new "My organisations" nav link, once the deployment's org label is overridden to "Tenants," renders as "My tenants" — a second element containing "Tenants" as a substring, so the non-exact locator now resolved to two elements. Fixed by adding `exact: true` to both affected assertions in that spec (a third, pre-existing assertion for the reverted-to-default case already had it, which is presumably why that one direction was never caught).
3. `mobile-card-grid-overflow.spec.ts`'s own two tile-view tests switched the shared `orgAdminAlphaBeta` persona's Requirements/Change-Requests view-mode preference to "tiles" — a server-persisted setting (`useViewMode`), not page-local state — and never switched it back, which later broke `project-history.spec.ts`'s shared `openRequirementByCode` helper (`.card, tr` combined selector, apparently not tile-view-safe) for any spec run afterward using that persona. Fixed by adding a "List view" click at the end of both tests, matching the restore-state convention this same file's own "Favourites card grid" test already used for its favourite toggle.

**Verified**: `cd frontend && npx tsc -b` clean throughout. Full Storybook suite (`npx vitest run --project=storybook`) — 47 files / 303 tests passed after merging the agent's changes with this session's own. Docker `tests/container` frontend image rebuilt/recreated. Per this repo's own documented methodology ("the only valid verification is a full clean-slate reseed followed by exactly one suite run"), the full Playwright suite was run three times against three separate `docker compose down -v && up --build -d` clean-slate resets: run 1 (63 specs, both `seed_e2e_dataset.py` and eventually `seed_demo_data.py`) surfaced the `workflow-bypass-attempts.spec.ts` regression plus 6 specs in the "ordering collision" category (one of which, `badge-filters.spec.ts`, turned out to be a self-inflicted gap in this run's own setup — it depends on `seed_demo_data.py`, which the first reset hadn't run, per `docs/e2e-workflows.md`'s own documented "how to run it" recipe); run 2 (both seed scripts run, regression #1 fixed) surfaced regressions #2 and #3; run 3, with all three regressions fixed and both seed scripts run: **62 passed, 1 failed** (`project-admin-templates-and-reports.spec.ts`, confirmed via an isolated rerun to pass cleanly alone — the same pre-existing ordering-collision category, not a fourth regression). `docs/ux-audit-2026-08.md`'s roadmap Status column updated to "Done" for the three items completed in this entry. Files changed: `frontend/src/components/Layout.tsx`, `RichTextEditor.tsx`, `RichTextEditor.stories.tsx`, `Layout.stories.tsx`, `i18n/strings.ts`, plus the six aria-label files listed above; `tests/playwright/tests/e2e-workflows/org-admin-nav-link.spec.ts` (new), `richtext-insert-link-modal.spec.ts` (new), `workflow-bypass-attempts.spec.ts`, `org-label.spec.ts`, `mobile-card-grid-overflow.spec.ts`; `CLAUDE.md`.

## `OverridePill`: the platform-default/override pattern applied to the org branding form's text fields

Next roadmap item after the previous entry's three: "Reset affordance for text-based branding overrides" (header title, the three email-footer fields — accent colour already had a working revert checkbox per the audit, and logo/login-background need a new backend `DELETE` endpoint, so both stayed out of this frontend-only pass).

**New shared `frontend/src/components/OverridePill.tsx`**, per the style guide's "Pattern: platform default vs. override" — one component, not four ad hoc implementations, since this exact field shape (header title, footer company/website/address) repeats four times on one form. Renders a `Platform default` badge, or a `Custom` badge plus an optional `onReset` button; `custom` is driven by the *saved* org value (`org.header_title != null`, etc.), not the in-progress input, so it can't flicker mid-edit or claim a state that isn't actually persisted yet — clicking Reset only clears the field locally, the existing shared "Save branding" button is still what persists it, matching how every other field on this form already batches edits rather than saving per-field.

**Restructured the four affected fields from an implicit `<label>` wrapping input+hint to an explicit `htmlFor`/`id` pair**, label and `OverridePill` as row siblings above the input. The implicit-wrap shape (still used elsewhere on this same form, e.g. the accent-colour checkbox) would have nested `OverridePill`'s own `<button>` inside the `<label>` associated with the *input*, which is invalid: a label's click-forwarding targets its associated control, and stacking an unrelated interactive `<button>` inside that same label is exactly the kind of nested-interactive-element structure accessibility tooling flags. Confirmed the restructure doesn't change accessible-name computation `getByLabelText` depends on (`explicit `htmlFor`/`id` computes the same way implicit wrapping does) by keeping the existing `BrandingSectionSave`/`BrandingSectionEmailFooterSave` stories' assertions (`getByLabelText(/^Header title/)`, `getByLabelText("Company name")`, `getByLabelText("Website")`) unchanged and green.

**Verified**: `npx tsc -b` clean. New `OverridePill.stories.tsx` (5 stories: platform-default state, custom-with-reset, custom-without-reset, both themes) plus a new `OrgAdminPage.stories.tsx` story (`BrandingSectionOverridePillAndReset`, using a new `org` override option added to the existing `mockOrgAdminApis` test helper) exercising both pill states and the reset-then-clear flow against a header-title-and-company-name-customised fixture — full Storybook suite 48 files / 309 tests, all green. New Playwright spec `tests/playwright/tests/e2e-workflows/org-branding-override-reset.spec.ts`, deliberately written against the seeded Alpha org rather than fixture data, since `seed_e2e_dataset.py` already customises Alpha's email-footer fields while leaving its header title on the platform default — exercises both pill states with zero extra setup, resets the company name field through the real UI, confirms the pill flips to Platform default after Save+reload, and restores the org's original seeded branding in `afterEach` (runs even on failure) so this shared org's data isn't left mutated for later spec runs — confirmed by re-running the same spec back-to-back, which only passes a second time if the first run's cleanup actually worked. One bug in the first draft of that spec: a second `.click()` on the "Branding section" toggle after `page.reload()` *collapsed* it instead of re-expanding it, since `CollapsibleSection`'s expand/collapse state is itself server-persisted per user (`useUiPreference`) and had stayed expanded across the reload — fixed by switching to this suite's own existing `ensureExpanded()` helper (checks `aria-expanded` before deciding whether to click) instead of an unconditional click, the same fix category `mobile-card-grid-overflow.spec.ts` needed for a different persisted-preference collision in the previous entry. Regression sweep (`golden-path`, `org-label`, `org-rename-and-test-email`, `org-security-controls`) re-run clean. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done". Files changed: `frontend/src/components/OverridePill.tsx` (new), `OverridePill.stories.tsx` (new), `frontend/src/pages/OrgAdminPage.tsx`, `OrgAdminPage.stories.tsx`, `frontend/src/i18n/strings.ts`; new spec `org-branding-override-reset.spec.ts`.

## `Modal`: focus trap and focus restoration

Next S-effort roadmap item: `Modal`'s ARIA roles were already correct (`role="dialog"`, `aria-modal`, `aria-label`, Escape-to-close), but it had neither of the two behavioural gaps a real focus trap needs — Tab could walk a keyboard user out of the dialog into the page behind it, and closing never returned focus to whatever had triggered it.

**Both fixed in `Modal.tsx` alone** (used everywhere `Modal` is used already, no call-site changes needed): a `dialogRef` captures the dialog element; on mount, `document.activeElement` is saved for restoration on unmount, and initial focus moves to the first focusable descendant *only if nothing inside already has focus* — the qualifier matters because `RichTextEditor`'s link-insert form (added earlier this same day) already sets its own `autoFocus` on the URL field, and a naive "always focus the first element" would have stolen focus back to the dialog's own Close button out from under it, since passive `useEffect`s run after React's native `autoFocus` handling during commit. The existing Escape-key listener gained Tab-trap logic in the same handler: on `Tab`/`Shift+Tab` at either end of the dialog's own focusable-element list, `preventDefault` and wrap to the other end, rather than letting focus leave the dialog.

**Verified**: `npx tsc -b` and `npx eslint` both clean. Four new `Modal.stories.tsx` stories: `FocusMovesIntoDialogOnOpen` (default children, no focusable content of their own → the Close button gets focus), `AutoFocusChildWins` (a child `<input autoFocus>` keeps focus, confirming the fallback correctly defers), `TabTrapCycles` (two extra buttons in children; Tab from the last wraps to Close, Shift+Tab from Close wraps to the last), `RestoresFocusOnClose` (a small stateful harness component that opens/closes the real `Modal`, confirming focus returns to the triggering button after Escape). Full Storybook suite: 48 files / 313 tests, all green. `tests/container` frontend image rebuilt/recreated; Playwright regression sweep (`richtext-insert-link-modal.spec.ts`, `change-request-tasks-and-voting.spec.ts`, `change-request-approval-separation.spec.ts`, `golden-path.spec.ts` — covering every real page that renders a `Modal` today) re-run clean, confirming the `autoFocus`-wins fallback actually holds in the live app, not just in an isolated story. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done". Files changed: `frontend/src/components/Modal.tsx`, `Modal.stories.tsx`.

## Shared `Tabs` component (migrated all three hand-rolled tab bars) and shared `Toast` component (piloted on three real mutations)

Two more M-effort roadmap items, worked in parallel — `Tabs` migration delegated to a background agent once the component itself and its tests were done, while `Toast` was built and piloted directly, since wiring it into real pages needed judgment calls (which mutations, what copy) a scripted sweep shouldn't make unsupervised.

**`Tabs`** (`frontend/src/components/Tabs.tsx`, new): WAI-ARIA APG "Tabs, automatic activation" — arrow keys both move focus and switch the active tab. Replaces `ProjectAdminPage.tsx`/`ServerManagementPage.tsx`/`PreferencesPage.tsx`'s three byte-for-byte-identical hand-rolled tab bars (confirmed identical by direct comparison before building anything — the audit's finding was exact, not approximate), none of which had `role="tablist"`/`"tab"` or keyboard nav beyond plain Tab-through. Renders the same markup/CSS as the hand-rolled version (a visual no-op), plus a `tabPanelProps(idPrefix, key)` helper callers spread onto each panel so `aria-controls`/`aria-labelledby` are real, not just the tablist half of the pattern. `idPrefix` is caller-supplied (not internally generated) specifically so the same string can be shared between the `Tabs` call and every `tabPanelProps` call without extra plumbing.

Initial implementation moved focus on arrow-key nav via `requestAnimationFrame` + `document.getElementById` after the state update — flaky in its own Storybook tests (a hardcoded `idPrefix="demo"` let one story's stale DOM answer another story's `getElementById` lookup) and, even after making `idPrefix` unique via `useId()`, still occasionally failed on the wrap-around case. Root-caused and replaced: every tab button is *always* rendered (only its panel is conditional), so the arrow-key target already exists in the DOM at keydown time — a `buttonRefs` ref array + a synchronous `.focus()` call inside the same event handler removed the whole class of timing bugs, and is also just more idiomatic React for this than an id-string round-trip.

`react-refresh/only-export-components` flagged `tabPanelProps` (a non-component export colocated with the `Tabs` component) — added to `eslint.config.js`'s `allowExportNames`, the project's own documented escape hatch for exactly this shape (already used for `resolveLandingPath`/`useViewMode`/etc.), not a new exception.

**Migration** (delegated to a background agent, reviewed after): `idPrefix`s `"project-admin-tabs"`/`"server-management-tabs"`/`"preferences-tabs"`. Also fixed, while touching the one file that had it: `ProjectAdminPage.tsx`'s `"reportSetup"` tab was the one label in the whole file hardcoded as a literal string rather than sourced from `strings.ts` (a separately-noted audit finding) — added `strings.admin.reportSetup` and switched the tabs array to use it. `Tabs` buttons now carry `role="tab"` (overriding the implicit `role="button"` every hand-rolled version had), which broke every Storybook/Playwright locator written as `getByRole("button", { name: "<tab label>" })` against one of these three pages — the agent fixed all affected Storybook selectors as part of its own verification; **the same class of breakage existed in 20 Playwright spec files** (not the agent's scope, found and fixed directly afterward): `getByRole("button", { name: "<label>" })` → `getByRole("tab", { name: "<label>" })` for the exact, exhaustively-enumerated set of 13 tab-label strings across ProjectAdminPage/ServerManagementPage/PreferencesPage, applied via a scoped `sed` sweep across the 20 confirmed files (every hit individually confirmed via `grep` first, not a blind regex) rather than by hand one at a time.

**`Toast`** (`frontend/src/context/ToastContext.tsx` + `.css` additions, new): a `ToastProvider`/`useToast()` pair, matching this codebase's existing context-module shape (`ThemeContext`, `AuthContext`) rather than an ad hoc singleton — mounted once in `main.tsx`, above `AuthProvider`. Success toasts reuse a solid dark chip (good contrast in both themes); error toasts follow `.btn-danger`'s own established convention of using the danger colour as an *accent*, never a solid fill (`border-left` stripe on a neutral surface) — matched to what's actually in this app already rather than the red-fill choice the review artifact's own illustrative mockups took a design liberty with. Auto-dismisses after 4s or on manual close; stacks multiple toasts; portalled to `document.body` like `Modal`/`Tooltip`. A `withToast()` Storybook decorator was added to `testing/storybook-helpers.tsx` alongside the existing `withAuth`/`withRouter` ones for any page whose stories need to assert a toast fired.

**Piloted on three real mutations**, not "every mutation" — the roadmap item's literal scope (dozens of call sites across ~20 files) is too large to do safely and reviewably in one pass; shipping the infrastructure plus a proven, tested pilot is the honest unit of work here, with the rest tracked as a follow-up rather than claimed done. Chosen pilots: `RequirementDetailPage.tsx`'s `archive()` (also gained a `try`/`catch` it never had before — one more of the audit's "no error handling at all" findings, fixed while already in this function), `ActionDetailPage.tsx`'s `archive()` (same treatment, for symmetry with its sibling), and `OrgAdminPage.tsx`'s `saveBranding()` (kept its existing inline `brandingError` display for field-level context *and* added a toast, rather than one replacing the other — the style guide's Principle 7 asks for both a shared toast and a shared inline error state, not one instead of the other).

**Verified**: `npx tsc -b` and a full-tree `npx eslint src/` both clean (the only warnings are the same 5-item pre-existing `BrandingContext.tsx`/`ThemeContext.tsx` baseline this repo already tracks — no new ones). Full Storybook suite: 50 files / 326 tests, including new stories for `Tabs` (click-switches-tab, tablist/tabpanel `aria-labelledby`/`aria-controls` cross-check, arrow-key nav with wraparound) and `Toast` (success, error, stacking, manual dismiss), plus toast assertions added to `RequirementDetailPage.stories.tsx`/`ActionDetailPage.stories.tsx`/`OrgAdminPage.stories.tsx`'s existing archive/save stories.

**Also found and fixed, while verifying against the real app: a genuine pre-existing bug in the shared Playwright helper `openRequirementByCode`** (`tests/playwright/tests/e2e-workflows/helpers.ts`), unrelated to anything built in this entry but surfaced by it — `project-history.spec.ts` failed in a full clean-slate suite run with a strict-mode violation resolving 9 links instead of 1. Root cause: the helper's `page.locator(".card, tr", { hasText: code }).first()` shape is broken whenever a project has enough requirements — `RequirementsPage.tsx`'s list view wraps its entire `<table>` in one outer `<div class="card">`, which also "has text" matching any code found in *any* row and sits before every `<tr>` in document order, so `.first()` silently resolved to that outer wrapper rather than the specific row; invisible with few requirements (the wrapper's one findable link and the correct row's link were the same element), a real failure once a full suite run's accumulated throwaway-requirement creation grew a project past that threshold. Fixed by locating the code's own exact text first, then walking up via XPath to its *nearest* `tr`/`.card` ancestor, rather than a flat multi-selector `hasText` match — eliminates the ambiguity structurally instead of coincidentally avoiding it. Re-verified: three full clean-slate-reseed (`docker compose down -v && up --build -d`, both `seed_e2e_dataset.py` and `seed_demo_data.py`) suite runs while chasing this and the two locator-migration sweeps above — first showed the `openRequirementByCode` failure alone (unrelated to Tabs/Toast), second (after the helper fix) showed it resolved with one unrelated later assertion failing from same-session data accumulation (confirmed by an isolated fresh-reseed rerun passing clean), **third and final: 64/64 passed.**

Files changed: `frontend/src/components/Tabs.tsx` (new), `Tabs.stories.tsx` (new), `frontend/src/context/ToastContext.tsx` (new), `ToastContext.stories.tsx` (new), `frontend/src/styles/theme.css`, `frontend/src/main.tsx`, `frontend/src/testing/storybook-helpers.tsx`, `frontend/eslint.config.js`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`), `ServerManagementPage.tsx` (+ `.stories.tsx`), `PreferencesPage.tsx` (+ `.stories.tsx`), `RequirementDetailPage.tsx` (+ `.stories.tsx`), `ActionDetailPage.tsx` (+ `.stories.tsx`), `OrgAdminPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/helpers.ts` and 20 spec files' tab-button locators. `docs/ux-audit-2026-08.md` roadmap Status updated: `Tabs` → Done; `Toast` → partial, noted as "component built, piloted on 3 mutations, broader sweep is follow-up work" rather than marked fully Done.

## Shared `SidePanel` and `Popover` components, and the first real popover usage (Org Admin's "New group")

Next M-effort roadmap item, explicitly called out as a prerequisite for several others still queued (the single/bulk create entry points, "New X" copy unification) — style guide "Pattern: create panels, popovers, and one door for bulk": a full-height right-anchored `SidePanel` for multi-field create/edit flows too heavy for a popover, and a small anchored `Popover` for one- or two-field flows, replacing the "always-visible inline form pinned below the list" pattern the audit found on every one of the app's 14+ "new X" flows.

**Extracted `dialogA11y.ts`** (`useDialogA11y`, a plain `.ts` hook, not a component) out of `Modal.tsx` before writing either new component: `SidePanel` needs the exact same focus-trap/focus-restoration/Escape-to-close behaviour `Modal` already had, and `Popover` needs the trap/restore half of it too (it has no backdrop or Escape-only affordance of its own). Rather than copy the ~50 lines a second time, `Modal.tsx` was refactored to call the shared hook, with no behavioural change (its own stories — `FocusMovesIntoDialogOnOpen`, `AutoFocusChildWins`, `TabTrapCycles`, `RestoresFocusOnClose` — all still pass unmodified, confirming the extraction was a pure refactor).

**`SidePanel.tsx`** (new): structurally `Modal` with a different backdrop layout — `justifyContent: "flex-end"` instead of centred, full-height, `borderRadius: 0` — same header+title+Close-button chrome, same backdrop-click-to-close. No new a11y surface beyond what `useDialogA11y` already provides.

**`Popover.tsx`** (new): positioned relative to a caller-supplied `anchorRef` on the same principle as `Tooltip` (portalled to `document.body`, measured via `getBoundingClientRect` so a scrollable ancestor like the nav rail can't clip it, flips above the anchor when there's no room below) but click-triggered rather than hover-triggered, interactive, and — unlike `Tooltip` — closes on outside click as well as Escape (a `mousedown` listener that no-ops when the target is inside the bubble or the anchor itself, so the trigger button's own toggle click isn't double-handled). Has no header chrome of its own; the caller supplies the whole body, typically ending in its own Cancel/submit row that calls `onClose`. First implementation positioned via React state (`setPosition` in a `useLayoutEffect`, same shape as `Tooltip`) and failed two of its own Storybook a11y stories (`FocusMovesIntoPopoverOnOpen`, `RestoresFocusOnClose`) — focus landed on `<body>` instead of the popover's first field. Root cause: the state update added an extra render pass between mount and `useDialogA11y`'s passive focus effect, which raced it. Fixed by applying the measured position directly to the DOM node inside the same `useLayoutEffect` (`bubble.style.top/left/visibility`) instead of through React state — single render, no race, and arguably more correct for a value that's read once per open rather than needing to trigger further re-renders.

**First real usage: Org Admin's "New group."** Exactly the flow the style guide's own mockup (`figures/ux-style-guide/create-group-popover.png`) illustrates. `OrgAdminPage.tsx`'s Organisation groups section previously had a permanently-visible `<input placeholder="Name">` + "New group" button pinned below the group list at all times; replaced with a "New group" button at the top of the section that opens a `Popover` with just the name field and a Cancel/Create row. `createGroup()` gained a `try`/`catch` it didn't have before, plus `showToast` on both success (`orgAdmin.groupCreated`, new string) and failure — Principle 7, applied here since the flow was already being touched, not a mechanical sweep of the rest of the app's create flows (`Toast`'s broader rollout is still tracked separately as partial).

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Full Storybook suite: 52 files / 343 tests, including new `SidePanel.stories.tsx` (mirrors `Modal.stories.tsx`'s open/close/focus-trap/restore coverage) and `Popover.stories.tsx` (a stateful harness with a real trigger button and anchor, since there's no meaningful way to render `Popover` without one; covers open, Escape-close, outside-click-close, click-inside-does-not-close, focus-on-open, tab-trap, and restore-on-close — the last one deliberately starts the harness closed and opens via a real click, since restoration is only a meaningful assertion when the "previously focused element" `useDialogA11y` captures on mount is genuinely the trigger, not `<body>`), plus a new `OrgAdminPage.stories.tsx` story (`GroupsSectionCreateGroupViaPopover`) exercising the full popover create flow including the toast. Rebuilt/recreated the `tests/container` frontend image. Updated the one existing Playwright spec that drove the old inline form directly (`org-group-nesting.spec.ts`, which creates two throwaway groups as setup for its actual nesting assertions) to open the popover and submit through it instead — re-run clean, along with `org-label.spec.ts`, `org-security-controls.spec.ts`, `org-rename-and-test-email.spec.ts`, `org-admin-project-statuses-and-link-types.spec.ts`, `org-branding-override-reset.spec.ts`, `org-admin-nav-link.spec.ts`. One unrelated failure surfaced first, on the (non-clean-slate) targeted rerun that included `org-admin-project-statuses-and-link-types.spec.ts`: the seeded Beta org had only 1 of its expected 4 project statuses, from state a prior spec run in this same long-lived dev stack had left behind — not caused by this change (confirmed by inspecting the failure's own accessibility snapshot, which showed the unrelated statuses missing, not a groups-section symptom). Per this repo's own documented methodology, ruled it out with a full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and exactly one full suite run rather than chasing it further: **64/64 passed.** `docs/ux-audit-2026-08.md` roadmap Status updated to "Done" for the `SidePanel`/`Popover` item. Files changed: `frontend/src/components/dialogA11y.ts` (new), `SidePanel.tsx` (new), `SidePanel.stories.tsx` (new), `Popover.tsx` (new), `Popover.stories.tsx` (new), `Modal.tsx`, `frontend/src/styles/theme.css`, `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/org-group-nesting.spec.ts`.

## Shared `ConfirmDialog` component (both confirmation tiers), piloted on three real call sites

Next M-effort roadmap item, and per the audit's own note the highest-severity one relative to its footprint: no shared confirmation component existed at all, so the app had accumulated four different patterns — nothing (most delete/archive buttons), `window.confirm` (~9 call sites, e.g. requirement/action archive, org disable, PAT revocation), and one ad hoc inline card with typed-name confirmation (organisation delete, the app's one existing irreversible/wide-blast-radius action) — instead of the style guide's two deliberate tiers.

**`ConfirmDialog.tsx`** (new): a single component for both tiers, not two — which tier renders is driven entirely by whether the caller passes `requireTypedText`, not a `variant` prop callers have to choose correctly. Wraps `Modal` (so it inherits its focus-trap/restore/Escape-to-close behaviour via `dialogA11y.ts` for free); Tier 1 renders a plain Cancel/confirm row, Tier 2 additionally renders a text input whose value must exactly match `requireTypedText` before the confirm button enables. `confirmLabel` is caller-supplied (e.g. "Archive", "Permanently delete") rather than a fixed "Confirm", matching how every other shared component in this codebase (`Modal`, `Popover`) takes its copy from the caller rather than baking in generic text.

**Piloted on three real call sites**, not a mechanical sweep of the ~12 remaining ones — same reasoning as `Toast`'s pilot (which mutation gets which exact copy, and which existing pattern it's replacing, is a judgement call per site):
- **Tier 1** — `RequirementDetailPage.tsx` and `ActionDetailPage.tsx`'s `archive()` actions, both previously `window.confirm`. `strings.requirements.archiveConfirm`/`strings.actions.archiveConfirm` were each split into a new short `archiveTitle` (the dialog's title/aria-label) and a trimmed `archiveConfirm` (now message-body text only, no longer prefixed with the question the title now carries).
- **Tier 2** — `ServerOrganisationsPage.tsx`'s organisation delete, previously a bespoke `.card` rendered inline below the table (its own `deleteConfirmText` state, a plain unlabelled `<input>`, manual `disabled={deleteConfirmText !== org.name}` wiring). Replaced with `<ConfirmDialog requireTypedText={org.name} .../>`; the page no longer needs its own `deleteConfirmText` state at all, since `ConfirmDialog` owns the typed value internally and only calls `onConfirm` once it already matches — the confirm handler just reads the already-known `org.name` rather than threading the typed string back out. The input also gained a real accessible name it never had before (`aria-label` from a new `strings.common.typeToConfirmLabel(name)` helper — `Type "Acme Corp" to confirm` — Principle 8), on top of the existing `placeholder`.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Full Storybook suite: 53 files / 350 tests — new `ConfirmDialog.stories.tsx` (Tier 1 confirm/cancel, Tier 2 disabled-until-exact-match), plus `RequirementDetailPage.stories.tsx`/`ActionDetailPage.stories.tsx`'s existing archive-toast stories rewritten to drive the in-app dialog instead of `spyOn(window, "confirm")` (each gained a paired cancel-leaves-it-untouched story), and `ServerOrganisationsPage.stories.tsx`'s `DeleteRequiresTypedConfirmation` rewritten to query `within(document.body)` for the dialog rather than `within(canvasElement)`, since `Modal` (and therefore `ConfirmDialog`) portals out of the page root. Rebuilt/recreated the `tests/container` frontend image. Updated the two Playwright specs that drove the old native/inline patterns directly: `requirement-archive-confirm.spec.ts` (rewritten from `page.once("dialog", ...)` native-dialog handling to `page.getByRole("dialog")` against the in-app one) and `workflow-bypass-attempts.spec.ts` (same swap, at the one other spot in the suite that archived a requirement past its confirmation step). `server-org-lifecycle.spec.ts`'s existing org-delete step needed no changes — it already located the confirm input by `getByPlaceholder(orgName)`, which still resolves correctly since `Modal`'s portal target is `document.body` and Playwright's `page.` locators aren't scoped to a container the way Storybook's `within(canvasElement)` is. One targeted (non-clean-slate) rerun surfaced `workflow-bypass-attempts.spec.ts` failing on an *earlier*, unrelated step than the one this change touches (its own seeded/renamed locked requirement no longer existed under its original name, from that same spec already having run — and permanently mutated it — earlier in the session); ruled out as pre-existing non-idempotency rather than a regression by re-running the full suite once against a fresh clean-slate reseed per this repo's own documented methodology: **64/64 passed.** `docs/ux-audit-2026-08.md` roadmap Status updated to "Partial" for this item (component built and proven on both tiers in production; broader rollout across the remaining ~12 call sites is follow-up work, tracked the same honest way `Toast` is). Files changed: `frontend/src/components/ConfirmDialog.tsx` (new), `ConfirmDialog.stories.tsx` (new), `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `ActionDetailPage.tsx` (+ `.stories.tsx`), `ServerOrganisationsPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/requirement-archive-confirm.spec.ts`, `workflow-bypass-attempts.spec.ts`.

## One entry point for single vs. bulk requirement creation

Next M-effort roadmap item — style guide "Pattern: create panels, popovers, and one door for bulk": `RequirementsPage.tsx` had the inline "New requirement" form (toggle-to-reveal) and `CsvImportWizard`'s own "Import CSV"/"Export CSV"/"Download template" row rendered **unconditionally**, competing as two separate, permanently-visible blocks above the list — the audit's own example of "zero of the fourteen create-entity flows use a modal, drawer, or popover."

**Retitled from the roadmap's original "...requirement/change-request creation."** Change Requests has no bulk/CSV creation path at all (confirmed against the audit's own "How things get created" table) — there is nothing to consolidate there under this item. Its inline form staying inline (not becoming a `SidePanel`) is an explicit scoping decision, not an oversight; converting it is a separate, not-yet-ticketed future step if the team wants it.

**`CsvImportWizard.tsx`**: converted to `forwardRef` exposing a new `CsvImportWizardHandle` (`{ openFilePicker: () => void }`, via `useImperativeHandle`) and a new `showImportTrigger?: boolean` prop (default `true`, so any future caller gets today's self-contained behaviour without opting in). When `false`, its own "Import CSV" label+button don't render, but the underlying `<input type="file">` still does (now always rendered once, referenced by `fileInputRef`, and conditionally wrapped in the visible label or left bare) — a caller can trigger the OS file picker via the ref without either component needing to reach into the other's state. Export/template stay visible unconditionally either way, in both callers — they're downloads, not part of the create flow this pattern is about, so folding them into the same "Add" menu as "Add one"/"Import from CSV" would have grouped semantically unrelated actions together.

**`RequirementsPage.tsx`**: the old unconditional `<CsvImportWizard/>` render now passes `showImportTrigger={false}` and a `ref`. The header's "New {requirement}" button now opens a `Popover` with two items — "Add one" (unchanged: sets `showNewForm(true)`, same inline form as before) and "Import from CSV" (`csvWizardRef.current?.openFilePicker()`). The deep-link path from Project Overview's own "New requirement" button (`?new=1` query param) is untouched — it still calls `setShowNewForm(true)` directly on mount, bypassing the popover entirely, since a user who already clicked "New requirement" on a different page shouldn't have to choose "Add one" again on arrival.

**Mid-task addition to `CLAUDE.md`, per explicit user request**: a new bullet in "Frontend UX Style Guide Adherence" generalising the principle this exact piece of work is an instance of — before building new frontend UI, check whether an equivalent already exists elsewhere in the codebase; if it does but isn't a shared component yet, extract it into one *and update the existing call site(s)*, not just the new one. Landing a new page on a shared component while leaving the original bespoke copy in place adds a second inconsistent implementation instead of removing the first.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Full Storybook suite: 53 files / 352 tests — new `CsvImportWizard.stories.tsx` story (`HiddenImportTriggerOpensViaRef`, a small ref-holding harness proving the file picker opens via the imperative handle with no visible "Import CSV" trigger, and that the rest of the import flow — upload, mapping, confirm — is otherwise identical) and new `RequirementsPage.stories.tsx` story (`NewRequirementMenuOffersOneOrBulk`); three existing `RequirementsPage.stories.tsx` stories that clicked "New" expecting the inline form to appear immediately updated to click "Add one" in the resulting popover first. Rebuilt/recreated the `tests/container` frontend image. Updated seven Playwright specs whose "New requirement"/"New Requirement" click previously opened the inline form directly: `golden-path.spec.ts`, `pelion-v2.spec.ts`, `mockup-engagement.spec.ts`, `requirement-archive-confirm.spec.ts`, `workflow-bypass-attempts.spec.ts`, `review-scheduling.spec.ts`, `project-admin-groups-and-fields.spec.ts` — each gained one `page.getByRole("button", { name: "Add one" }).click()` immediately after the existing trigger click; `csv-import-wizard.spec.ts` needed no changes, since its `page.locator('input[type="file"]...').setInputFiles(...)` targets the file input directly regardless of visibility, and `page.` locators aren't scoped to a rendering container the way Storybook's `within(canvasElement)` is. Two targeted (non-clean-slate) reruns surfaced failures entirely unrelated to this change and upstream of anything it touches — `project-admin-groups-and-fields.spec.ts` blocked on a disabled "New field" button before ever reaching its Requirements-page step (stale custom-field-name collision from that same non-idempotent spec's own earlier run), and `csv-import-wizard.spec.ts`'s second test failed creating a requirement via direct API call (`createResp.ok()` false, same non-idempotency category) — both consistent with this repo's documented "state-mutating specs need a fresh reseed to rerun cleanly" limitation, not a regression. Ruled out with a full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and one full suite run: **63/64 passed**, the one failure (`project-admin-templates-and-reports.spec.ts`, a Gamma-org report-template flow with zero relation to Requirements/CSV/Popover) confirmed via an isolated rerun to pass cleanly alone — the same pre-existing full-suite ordering-collision category `docs/e2e-workflows.md` already documents, and the exact same spec that landed in that category two entries above in this log. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done", retitled to drop the change-request half per the scoping note above. Files changed: `frontend/src/components/CsvImportWizard.tsx` (+ `.stories.tsx`), `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`, `CLAUDE.md`; `tests/playwright/tests/golden-path.spec.ts`, `pelion-v2.spec.ts`, `mockup-engagement.spec.ts`, `e2e-workflows/requirement-archive-confirm.spec.ts`, `e2e-workflows/workflow-bypass-attempts.spec.ts`, `e2e-workflows/review-scheduling.spec.ts`, `e2e-workflows/project-admin-groups-and-fields.spec.ts`.

## Consolidate filter UI onto `FilterPanel`: Project List, Project Reviews Due, Server Organisations, Notifications

Next M-effort roadmap item — the audit's own "Pattern inconsistency" finding: `RequirementsPage.tsx`/`ChangeRequestsPage.tsx` already established a `.side-grid` shell (list on the left, a right-hand `FilterPanel` sidebar of `FilterField`/`FilterCheckbox` controls) but four other list pages kept their filters as an inline row of buttons/selects above the list instead, a second, inconsistent pattern for the same job.

**User was explicitly asked first whether the separately-flagged, much larger "terminology overrides" item (the audit's other High-severity finding, needing `strings.ts` itself to become terminology-aware per the audit's own suggested end-state) should be tackled now or deferred — user said to do it last, as a full architectural fix, after every other roadmap item.** `docs/ux-audit-2026-08.md`'s terminology-overrides row updated to record that explicit ordering decision before moving on to this item.

**Convention split, matched exactly to what `RequirementsPage.tsx` already does**: search inputs, page-level actions ("New project", "Mark all read"), and the `ViewToggle` stay in the main content area's own header row — only controls that *narrow* the visible set move into `FilterPanel`. This is a judgment call made once against the reference pages rather than re-derived per page:
- **`ProjectListPage.tsx`**: the Active/Archived toggle (previously two buttons) became a `FilterField` select (`Status`: Active/Archived); organisation, role, and stage-status filters moved from their own inline `<select>`s into `FilterField`s unchanged otherwise. Search and `ViewToggle` stayed in the main row.
- **`ProjectReviewsDuePage.tsx`**: component and reviewer selects moved into two `FilterField`s. No search/actions existed on this page to begin with.
- **`ServerOrganisationsPage.tsx`**: the three-way Active/Disabled/All button group became a single `FilterField` select. Search, "New {org}", and the create-org form/import-warnings/action-error blocks stayed in the main content column, above the table.
- **`NotificationsPage.tsx`**: the "Unread only" checkbox moved from the inline row into `FilterPanel` via `FilterCheckbox`, unchanged otherwise. Search and "Mark all read" stayed in the main row.

All four pages now wrap their content in the same `.side-grid` CSS class `RequirementsPage.tsx` uses (defined once in `theme.css`: `1fr 240px` two-column grid, collapsing to one column under the existing narrow-viewport breakpoint) — no new CSS, reusing the existing shell exactly.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Storybook: all four pages' existing stories (23 combined) passed unmodified except one, confirming the refactor is behaviour-preserving — `ServerOrganisationsPage.stories.tsx`'s `ShowAllIncludesDisabled` clicked a `getByRole("button", { name: "All" })` that no longer exists now that status is a select; updated to `userEvent.selectOptions(canvas.getByLabelText("Status"), "All")`. `FilterPanel`/`FilterField`/`FilterCheckbox` already had their own dedicated component-level Storybook coverage from when `RequirementsPage.tsx` first introduced them, so no new stories were needed for the shared component itself — only the one page-level story broken by the button-to-select change. Rebuilt/recreated the `tests/container` frontend image. Grepped every Playwright spec for interactions with the four pages' filters before running anything: `server-org-lifecycle.spec.ts` had the same "All" button click as the broken story (fixed to `page.getByLabel("Status").selectOption("all")`); `notifications-and-favourites.spec.ts`'s `page.getByLabel("Unread only")` and `project-list-org-scoping.spec.ts`'s CSS locator against the organisation `<select>`'s own `<option>` text both needed no changes, since neither depends on where in the DOM the control sits. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and one full Playwright suite run: 63/64 passed, the one failure (`report-image-attachment.spec.ts`, a report intro image-upload/PDF-generation spec with zero relation to any of the four pages touched) confirmed via an isolated rerun to pass cleanly alone — a pre-existing timing flake, not a regression. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done." Files changed: `frontend/src/pages/ProjectListPage.tsx`, `ProjectReviewsDuePage.tsx`, `ServerOrganisationsPage.tsx` (+ `.stories.tsx`), `NotificationsPage.tsx`; `tests/playwright/tests/e2e-workflows/server-org-lifecycle.spec.ts`.

## A visible "Invite" entry point + keyboard navigation for `UserAutocomplete`

Next M-effort roadmap item — the audit's "fifteenth create flow with no visible trigger at all" finding: inviting an external, non-org-member user to a project is real and working (Project Admin's Groups tab), but there's no "Invite" button anywhere and no visual cue the field does anything beyond adding an existing member — the only entry point is typing an email and noticing what the dropdown does. The component itself also had zero keyboard support (no arrow keys, no Enter-to-select), the style guide's own Principle 4 example of "every interactive control has a name" / arrow-key navigation.

**Scope check first**: `UserAutocomplete` has 3 call sites (`OrgAdminPage.tsx` ×2, `ProjectAdminPage.tsx` ×1), but only the `ProjectAdminPage.tsx` one passes `organizationId`+`onSelectExternal` — the other two only add existing org members to a group, with no invite capability at all. So the "visible entry point" half of this fix is scoped to that one call site; the keyboard-navigation half applies to the component as a whole (used everywhere) — including making the invite-only affordances (the hint text, the invite option in the dropdown) opt-in based on whether the caller wired up `organizationId`/`onSelectExternal`, so the two non-invite-capable call sites are unaffected.

**`UserAutocomplete.tsx`**: rebuilt around the WAI-ARIA "Combobox with list autocomplete" pattern. The input gets `role="combobox"`, `aria-expanded`, `aria-controls`, `aria-autocomplete="list"`, `aria-activedescendant`; the dropdown is `role="listbox"`; each match (including the invite/add-existing result) is `role="option"` with a matching `id`/`aria-selected`, plus a highlighted-row background so the same state is visible, not just announced. Arrow Up/Down move a `highlightedIndex` through the combined `matches` + external-match option list (wrapping at the ends, opening the dropdown on the first Down if closed); Enter selects whatever's highlighted; Escape closes without selecting. A new `inviteCapable` flag (`organizationId && onSelectExternal` both set) drives a persistent hint paragraph — "Not a member yet? Type their email to invite them." — shown before any query is typed, so the invite path is visible without already knowing the trick; it steps aside once typing starts, replaced by the dropdown's own "Invite {email}"/"Add {email}" result once a query resolves one. Deliberately kept separate from `canInvite` (whether *this specific query* actually resolved an external match) — the hint's job is discoverability up front, not confirming a match exists.

**`ProjectAdminPage.tsx`**: the one invite-capable call site's placeholder changed from `strings.admin.addMemberPlaceholder` ("Type a name or email to add…") to a new `strings.admin.addOrInviteMemberPlaceholder` ("Type a name to add, or an email to invite…") — the other two (non-invite-capable) call sites keep the original placeholder, since "or an email to invite" would be actively misleading there.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. `UserAutocomplete.stories.tsx`: 3 existing stories pass unmodified (behaviour-preserving), plus 3 new ones — `KeyboardNavigationSelectsHighlightedOption` (Arrow-Arrow-Enter selects the second match, no mouse), `EscapeClosesTheDropdown`, `InviteHintVisibleBeforeTyping` (hint shown before typing, gone once a query is entered). Full Storybook suite: 54 files / 365 tests. Two page-level stories needed fixes for a real, expected side effect of making the input itself `role="combobox"`: `ProjectAdminPage.stories.tsx`'s `GroupsTabAddOrgGroup` had a bare `getByRole("combobox")` that was unambiguous before (only the org-group-nesting `<select>` had that role) and is now ambiguous (`UserAutocomplete`'s own input on the same tab matches too) — scoped to `.find((el) => el.tagName === "SELECT")`. Rebuilt/recreated the `tests/container` frontend image. Grepped the whole Playwright suite for anything touching `UserAutocomplete`'s old placeholder text, a bare `getByRole("combobox")` near a "Project groups"/"Organisation groups" tab, or a `getByRole("button", ...)` click on a dropdown match (now `role="option"`, not `"button"`, per the same WAI-ARIA pattern) before running anything — found and fixed the same class of break in `external-project-invite.spec.ts` (`getByRole("button", {name: /Invite .../})` → `getByRole("option", ...)`), `org-security-controls.spec.ts` and `project-admin-groups-and-fields.spec.ts` (old placeholder text), and `org-group-nesting.spec.ts` + `project-admin-groups-and-fields.spec.ts` (ambiguous `getByRole("combobox")`/`.last()` now also matching `UserAutocomplete`'s input on the same tab — scoped to `page.locator("select")` instead).

**A genuinely non-idempotent spec found and fixed along the way, per the user's explicit request** (this surfaced directly while chasing what looked like a regression): `project-admin-groups-and-fields.spec.ts` created its four custom fields with fixed names ("Verification method", "Detailed rationale", "Safety critical", "Priority") on the shared seeded Beta-2 project. Custom Fields has no server-side uniqueness constraint on name, so re-running this spec against an already-mutated Beta-2 (no reseed in between — exactly what happened during this task's own iterative debugging of the locator fixes above) doesn't fail outright on the *first* assertion, but leaves every earlier run's own same-named fields behind, and a later step or a different spec sharing Beta-2 can trip over the resulting duplicates in a way that's hard to distinguish from a real regression (a "New field" button observed stuck disabled, initially misread as a possible UI bug before being traced back to this). Fixed by giving all four field names a shared per-run `Date.now()` suffix, threaded through every step that references them, removing the fixed-name assumption entirely — verified by running the spec three times back-to-back against the same unseeded database (no failures), where it previously failed on the second run onward. This is the concrete case the `CLAUDE.md` idempotency rule (added earlier this session) was written for; no other spec was found to need the same fix in this pass.

`docs/ux-audit-2026-08.md` roadmap Status updated to "Done." Files changed: `frontend/src/components/UserAutocomplete.tsx` (+ `.stories.tsx`), `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`, `org-security-controls.spec.ts`, `org-group-nesting.spec.ts`, `project-admin-groups-and-fields.spec.ts`.

## Unify the three "aggregate" list pages: Notifications, Favourites, My Reviews Due

Next M-effort roadmap item — the audit's "Pattern inconsistency" finding: Notifications, Favourites, and My Reviews Due are all "your stuff, across every project" list pages, and each presented that list differently — a clickable-card list with pagination, a card grid with none, and a plain table with neither filters nor pagination. My Reviews Due's project-scoped sibling page (`ProjectReviewsDuePage`) even has component/reviewer filters the cross-project version lacked entirely, on the same underlying data.

**Scoped to structure, not row content.** Favourites (projects), Notifications (notification events), and My Reviews Due (requirements) are three genuinely different entity types — forcing one literal shared row/card component across all three would be a worse fit than three separate, purpose-built renderings (the same reasoning `DefinitionList` was scoped away from stages/components earlier this session). What's actionable and was actually inconsistent: pagination shape, and whether cross-project rows show which project they belong to.

**Backend**:
- **`routers/projects.py::list_projects`**: new `favorite_only: bool = False` query param, filtering the already-built `out` list before the existing sort/count/slice — the same paginated `GET /projects` endpoint `ProjectListPage` uses, not a separate favourites endpoint, so `FavouritesPage` gets search and pagination for free rather than reimplementing them.
- **`routers/reviews.py::list_my_due_reviews`** (`GET /me/reviews/due`): gained the same optional `limit`/`offset`/`X-Total-Count` contract as `list_requirements` (omitting both still returns everything), and each row now carries `project_name` — resolved via the same batch-lookup-by-id pattern the endpoint already used for `component_name`. `RequirementDueForReviewOut.project_name` is optional and only ever populated here; the project-scoped `/projects/{id}/requirements/reviews/due` sibling leaves it `None`, since that page already has the project in its own URL and showing its own name back would be redundant.

**Frontend**:
- **`FavouritesPage.tsx`**: converted from `api.get` + client-side `.filter(p => p.is_favorite)` (an unbounded fetch of *every* accessible project just to find the favourited ones) to `api.getPage` against `/projects?favorite_only=true`, plus a search input — matching `ProjectListPage`'s and `NotificationsPage`'s own `PAGE_SIZE`/`total`/`LoadMoreButton` convention exactly.
- **`MyReviewsDuePage.tsx`**: same `api.getPage`/`LoadMoreButton` conversion, plus a new Project column using the backend's new `project_name` field.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Backend: two new tests in `test_pagination.py` (`test_projects_list_favorite_only_filters_to_favourited_projects`, `test_my_reviews_due_paginates_reports_total_and_includes_project_name`) plus the existing `test_project_config.py`/`test_review_scheduling.py` suites re-run clean (the shared `RequirementDueForReviewOut` schema change is additive/optional, so its other consumer's behaviour is unchanged). Full backend suite: **535/535 passed** — rebuilt the backend image first (its source isn't bind-mounted; running new tests against the previous build's binary would have silently tested stale code), then ran via `docker compose exec backend python3 -m pytest tests/` — note `python3 -m pytest`, not a bare `pytest`, since this container's `tests/` has no `__init__.py` and a bare invocation left `/app` off `sys.path`, failing every test at collection with `ModuleNotFoundError: No module named 'app'` before any actually ran. Frontend Storybook: `FavouritesPage.stories.tsx` and `MyReviewsDuePage.stories.tsx` both converted from `spyOn(api, "get")` to `spyOn(api, "getPage")`, with new `LoadMoreAppendsTheNextPage` stories on both and a new `SearchNarrowsTheList` story on Favourites; full suite 54 files / 368 tests, all green. Rebuilt/recreated both the `tests/container` backend and frontend images. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and one full Playwright suite run: **64/64 passed** — no spec needed changes, since neither page's DOM contract (button roles/text, card/table shape) changed, only how the data behind it is fetched. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done." Files changed: `backend/app/routers/projects.py`, `backend/app/routers/reviews.py`, `backend/app/schemas/requirement.py`, `backend/tests/test_pagination.py`; `frontend/src/pages/FavouritesPage.tsx` (+ `.stories.tsx`), `MyReviewsDuePage.tsx` (+ `.stories.tsx`), `frontend/src/api/types.ts`.

## Logo & login-background reset

Next M-effort roadmap item, and the audit's other "Platform defaults & overrides" finding: accent colour and (as of the previous "Reset affordance for text-based branding overrides" item) the header title/email-footer fields all have a working revert-to-platform-default path, but the org logo and login-background image had none at all — in the UI or the API. Upload-only, with no `DELETE` route to clear the override. Explicitly flagged in the roadmap as touching org branding and needing the change-management policy's identify → verify → remediate review before being considered complete.

**Backend**: two new endpoints in `routers/orgs.py`, `DELETE /{organization_id}/logo` and `DELETE /{organization_id}/login-background`, both `require_org_role(OrgRole.ORG_ADMIN)`-gated (the exact same dependency the sibling upload endpoints already use). Each is a genuine no-op (200, unchanged, no audit event) when nothing is currently set — reverting a setting already at its default isn't an error, it's asserting an end state that may already hold — and otherwise nulls the FK, deletes the now-orphaned `FileAsset` via the existing `services.files.delete_file` (removing both the storage bytes and the row, not just unlinking it — the same cleanup discipline `delete_org_resource` already uses), and writes a `logo_removed`/`login_background_removed` audit event via `log_event` before committing.

**Frontend**: `OverridePill` gained an optional `disabled` prop (previously only used by the batched-edit text fields, which have no in-flight state of their own to disable against). The org logo and login-background image fields (`OrgAdminPage.tsx`, Branding and Single sign-on sections respectively) now each show an `OverridePill` next to their label, wired to a new `removeLogo`/`removeLoginBackground` handler that calls the new `DELETE` endpoint and reloads immediately — unlike the text fields' reset (which just clears the input locally and waits for the section's own Save button), these fire and persist immediately, matching how the upload itself already commits with no separate Save step. Both show a toast on success/failure, extending the `Toast` pilot (previously on requirement archive, action archive, and branding save) to two more mutations.

**Identify → verify → remediate review** (`docs/soc2/policies/change-management-and-secure-development-policy.md`):
- *Identify*: (1) authorization — must be scoped to the acting org, not any org; (2) multi-tenant isolation — must only ever touch the calling org's own FK/asset, never another org's; (3) audit trail — a mutating action must not bypass `services/audit.py`'s logging pattern; (4) data retention — a reverted image shouldn't linger as an orphaned, unreferenced blob in storage indefinitely; (5) the public, unauthenticated `GET /api/v1/files/{id}` download route (which serves org branding images with no auth, by design, for the pre-login page) must not go on serving a logo/background after it's been reset.
- *Verify*: (1)/(2) confirmed by reusing `require_org_role(OrgRole.ORG_ADMIN)` unmodified — identical to `upload_org_logo`/`upload_login_background`, and it already resolves `organization_id` from the path, so there's no way to reach another org's row through this endpoint; `test_org_member_cannot_delete_org_logo` pins (1) directly. (3) confirmed by the new `log_event` calls, only fired on an actual mutation (the no-op path deliberately doesn't log a no-op as if it were a real deletion). (4) confirmed by calling `delete_file` rather than only clearing the FK — `test_delete_org_logo_and_login_background_revert_to_the_platform_default` pins this by asserting the old file's `GET /files/{id}` 404s afterward, not just that the org's FK went null. (5) is the same assertion — a 404 on the download route is exactly "stopped serving it." One accepted, non-remediated edge case: two concurrent `DELETE` calls against the same still-set logo could both read a non-null `logo_file_id` and both attempt `delete_file` on the same already-removed storage key, a benign race (no privilege escalation, no data exposure) that `delete_org_resource` already carries under the same conditions — not a new risk class introduced by this change, so left as-is rather than adding locking disproportionate to the actual exposure.
- *Remediate*: no findings required a code change beyond what's described above.

**Verified**: `npx tsc -b` and a full-tree `npx eslint` both clean. Backend: four new tests in `test_files.py` (`test_delete_org_logo_and_login_background_revert_to_the_platform_default`, `test_delete_org_logo_is_a_noop_when_nothing_is_set`, `test_org_member_cannot_delete_org_logo`, plus the pre-existing upload tests re-run alongside them); full backend suite **535/535 passed**, rebuilt-image-first, `python3 -m pytest`. Storybook: `OverridePill.stories.tsx` gained `CustomWithResetDisabledWhileSaving`; `OrgAdminPage.stories.tsx` gained `BrandingSectionLogoAndLoginBackgroundReset` (resets both, asserts the `DELETE` call and the success toast for each); full suite 54 files / 370 tests, all green. Rebuilt/recreated both the `tests/container` backend and frontend images. New/extended Playwright coverage in the existing `org-branding-override-reset.spec.ts` (the spec the earlier text-field reset item created) rather than a new file — a second test alongside the existing one, covering upload-then-reset for both the logo and the login-background image, with `afterEach` extended to also `DELETE` both (a no-op when unset, so safe to run unconditionally) alongside its existing branding-field restore, keeping the spec idempotent per `CLAUDE.md`'s testing-idempotency rule; confirmed by running it three times back-to-back with no reseed in between, all passing. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and one full Playwright suite run: **64/65 passed** — the one failure (`project-history.spec.ts`, unrelated to org branding) confirmed via an isolated rerun to pass cleanly alone, a pre-existing timing flake matching the same pattern seen earlier this session with `report-image-attachment.spec.ts`, not a regression. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done." Files changed: `backend/app/routers/orgs.py`, `backend/tests/test_files.py`; `frontend/src/components/OverridePill.tsx` (+ `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/org-branding-override-reset.spec.ts`.

## Org Admin → resource-menu restructure, incl. splitting Advanced

Next L-effort roadmap item — the style guide's Principle 1 finding that Org Admin's 15 flat, all-collapsed `CollapsibleSection`s independently invented a different answer than Project Admin's 8-tab bar to the same "many settings, one page" problem. The style guide's own "Pattern: settings hierarchy" section already specifies the exact fix (resource-menu sub-pages, for a page with more than 5 groups opened rarely as a whole) and ships both the "before" (15 sections) and "after" (6 groups) diagrams to implement against, so this pass is a faithful migration against an already-agreed design, not a fresh design exercise.

**New shared component, not a page-local layout.** `ResourceMenu.tsx`: a left-hand vertical menu of route-addressable groups with a content pane on the right — modelled on `Tabs.tsx`'s WAI-ARIA-conscious keyboard support (arrow keys move focus and activate, `Home`/`End` jump to the ends) but deliberately *not* built on `Tabs`' `role="tablist"`/`"tab"` pattern, since each group here is a real URL segment, not client-only state: the menu is built from real `<Link>`s with `aria-current="page"` on the active one, and arrow-key movement calls `navigate()` (mirroring Tabs' own automatic-activation model, just adapted from internal state to routing) rather than only moving a highlighted-but-unselected item. Reuses the app's existing `.nav-link`/`.nav-link.active` classes for its items so the visual language matches the persistent nav rail exactly (Principle 4: one component per pattern), rather than inventing new styling for what's visually the same "list of links, one highlighted" shape.

**Routing**: `App.tsx`'s `/orgs/:orgId/admin` route became `/orgs/:orgId/admin/:group?` (react-router v6.30 supports optional path segments natively). Grepped every frontend call site that links to `/orgs/${orgId}/admin` before touching this (`Layout.tsx`'s nav rail — no, that's `/orgs`, not `/orgs/:id/admin` — `OrgListPage.tsx`'s auto-redirect and directory links, `PreferencesPage.tsx`'s "Manage organisation" link, `ServerOrganisationsPage.tsx`) and confirmed all of them use the bare path with no group segment, which still resolves (falls back to "overview") since the segment is optional — none needed updating.

**Content migration matched the style guide's "after" diagram exactly — regrouped, nothing deleted or invented.** The unsectioned top content (org name/rename, export, leave) plus the rare-use `Import/merge` and `Shared resources` sections became **Overview** (the only group keeping `defaultCollapsed` on those two sub-sections, per the diagram's own "rare, opened almost never" framing). **People** = Users + Groups. **Projects & workflow** = the Projects management section (group/membership matrix) + Project statuses + Link types. **Branding & defaults** = Branding + Default project template. **Templates & reports** = Report Defaults + Report templates. **Integrations & security** = SSO + SCIM + the old Advanced section's contents, split by what they actually govern rather than kept as one catch-all: SMTP/email fields and the test-email block became their own "SMTP & email" card; 2FA/self-signup/external-user-policy became a "Security" card; PAT max-lifetime moved into the existing Personal Access Tokens section; the `sso_group_mappings` role-mapping list moved into the existing SSO section (an OIDC/SSO concept, not a generic "Advanced" one). Login-background moved out of the SSO section (where it sat for no reason connected to SSO/OIDC) into Branding, alongside the org logo, per the diagram's own "Branding — logo, colour, footer" leaf. The SMTP & email and Security cards still share **one** `saveAdvanced` button (placed at the bottom of the Security card) — both remain one underlying `OrgAdvancedSettings` PUT regardless of which card the fields visually live in now; adding a second save endpoint or button was explicitly avoided as unnecessary. Every section moving into a non-Overview group had its `defaultCollapsed` flipped from `true` to `false` (kept as a `CollapsibleSection`, still user-collapsible) — leaving them collapsed-by-default under a resource menu that already narrows each group to 2–5 items would have reinvented the audit's own "one optional block inside a single view, never the whole page" problem one level down, just moved from page-scope to group-scope.

**Scope exclusion, named rather than silently dropped**: the style guide's G6a note describes moving IdP-group-name mapping "from Groups" into SSO. That note is about the *per-group* `idp_synced_group_name` field (state `idpSyncEdits`, handler `saveIdpSync`), rendered inline in each group's own row inside the Groups section — a fundamentally different, harder change than the other moves in this pass: it would need a new consolidated "all groups' IdP mappings" table/view under SSO to actually make sense there, not just relocating the existing per-row inline field (which would read as orphaned, disconnected from the group list it's about, if moved as-is). That's disproportionate scope for an otherwise pure regrouping pass, so it was left exactly where it is, inside Groups (which itself moved into People as described above) — the same kind of explicit, reasoned exclusion this session's earlier `DefinitionList` item made for stages/components (documented rather than silently dropped).

**A real bug found and fixed mid-pass, not deferred**: `ResourceMenu`'s root element was originally styled `className="row resource-menu"`, reusing the app's shared `.row` flex utility class for its own layout needs. `.row` is also relied on throughout the Playwright suite as a scoping selector for "one specific list row" (e.g. `page.locator(".row", { hasText: someRowsOwnText })`). Since `ResourceMenu`'s wrapper spans the *entire* content pane, giving it the `.row` class made it the new outermost `.row` match in DOM order for any text anywhere in the selected group — silently hijacking `project-admin-templates-and-reports.spec.ts`'s unscoped `page.locator(".row", { hasText: revisedName }).first()` delete-button lookup (a real Playwright failure caught by the full-suite run, not found by inspection). Fixed by giving the wrapper its own dedicated `.resource-menu` CSS class instead, moving the flex layout there rather than composing it from `.row` + inline overrides — the kind of naming collision worth flagging for any future component that visually resembles a `.row`/`.stack` shape but wraps a whole page's content, not one row of it.

**Two more bugs found incidentally during full-suite verification, unrelated to Org Admin but fixed per `CLAUDE.md`'s fix-don't-defer rule rather than logged as out of scope**, both in `pelion-v2.spec.ts` / `ProjectListPage.tsx` — neither file is otherwise part of this roadmap item:
1. `pelion-v2.spec.ts`'s "New project" org-picker locator (`page.locator("select:has(option:text-is('Default Organization'))")`) was unscoped/page-wide, written when the bootstrap admin account belonged to exactly one org. It's since started belonging to two (org-creation via the API grants the creator membership, and both `seed_e2e_dataset.py` and `seed_demo_data.py` create orgs through this same admin account) — at which point `ProjectListPage`'s own "Organisation" `FilterPanel` select (added by an earlier, already-landed but still-uncommitted pass in this same working tree) *also* lists every org name as an option, including "Default Organization," making the locator ambiguous between the two selects. Fixed by scoping to the "New project" form card specifically. A second, related bug surfaced once the first was fixed: the picker-presence check (`orgPicker.count() > 0`) ran immediately after `.click()`, but `.count()` takes an immediate, non-retrying snapshot rather than auto-waiting — invisible with a single org (the check always legitimately returned 0 either way) but a real race once a picker can actually be present, since the form's own re-render hadn't necessarily finished. Fixed by waiting for the Name input to actually attach first. A third, compounding bug: even with the picker reliably found, `ProjectListPage`'s own org-fetch is async and separate from the form's unconditionally-rendered Name/Summary inputs — clicking "New project" too soon after navigating to `/projects` could still see zero orgs loaded (picker legitimately absent at that instant). Fixed by waiting for network idle before opening the form, both at initial login and after navigating back to `/projects` later in the same spec.
2. Once the org-picker fixes correctly routed both projects to the same org, a fourth, independent bug surfaced in the *next* step: "Software"'s own "add category" form's `Name` field silently reverted to empty between being filled and the "New category" button being clicked, leaving it permanently disabled. Root-caused via a captured accessibility-tree snapshot at the moment of failure: `Prefix` (filled second, closer to the click) still read `"PERF"`, but `Name` (filled first) had reverted to empty — confirming a *second*, later re-render reset it, not a first-fill failure. The spec's own existing comment already documents why: `ProjectAdminPage`'s post-mutation `reload()` fires 9 requests, "7 concurrently, then two more awaited *sequentially* afterwards" — and the spec's existing single `waitForLoadState("networkidle")` call can resolve in the brief gap between those two waves, not only after both, letting the test fill the form mid-cascade. Fixed with a second `waitForLoadState("networkidle")` immediately before the click, defensively re-filling only whichever field (if either) actually got reset rather than assuming which — confirmed stable across two consecutive isolated reruns plus the final full-suite run.

**Collapse-default story fixes weren't just click-removal — two needed real disambiguation**, since sections that used to be mutually exclusive (only one accordion open at a time before a user expanded a second) are now simultaneously mounted within the same resource-menu group: `GroupsSectionShowsMembers` asserted `getByText(/Alex Morgan/)` unscoped, which became ambiguous once Users and Groups (both now open by default under People) both render that name (Users' own table row, Groups' own member list) — scoped to the Engineering group's own row. `ProjectStatusesDeleteDisabledAtLastRow` asserted the "only one left, delete disabled" title unscoped, which became ambiguous once Project statuses and Link types (both open by default under Projects & workflow) each independently satisfy "exactly one row" in their respective mocked fixtures — scoped to the Active row specifically.

**Verified**: `npx tsc -b` and a full-tree `npx eslint .` both clean throughout (re-checked after every fix, not just once at the end). New `frontend/src/components/ResourceMenu.stories.tsx` (5 stories: click-switches-group with unmounted-not-hidden non-active content, only-the-active-group carries `aria-current`, arrow-key navigate-and-activate with wraparound, light/dark theme). Full Storybook suite: **55 files / 375 tests, all green** — `OrgAdminPage.stories.tsx`'s pre-existing ~25 stories were read individually (not run-then-reactively-patched) and updated to select the correct resource-menu group before asserting on content that's no longer reachable from a bare page load, with two of them needing the disambiguation described above beyond a simple group-selection swap. Rebuilt/recreated the `tests/container` frontend image (twice — once for the initial page/component change, once more after the `.row`-collision CSS fix). Grepped the whole Playwright suite for Org Admin interactions before touching anything, beyond the files named in this task's own brief: found and fixed 7 spec files in total (`org-admin-project-statuses-and-link-types.spec.ts`, `org-group-nesting.spec.ts`, `org-branding-override-reset.spec.ts`, `org-security-controls.spec.ts`, `org-rename-and-test-email.spec.ts`, `scim-provisioning.spec.ts`, `project-admin-templates-and-reports.spec.ts`), each via a new `selectOrgAdminGroup` helper added to `helpers.ts` (idempotent guard on `aria-current`, mirroring `ensureExpanded`'s existing idempotent guard on `aria-expanded`) — `org-branding-override-reset.spec.ts`'s login-background step also lost its now-unnecessary SSO-section navigation entirely, since that field lives in the already-open Branding card now. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and full Playwright suite runs: the first came back **62/65 passed**, all three failures root-caused rather than assumed — one was the `.row`-collision bug above (real regression, fixed), the other two (`project-admin-groups-and-fields.spec.ts`'s terminology-override step, `pelion-v2.spec.ts`) don't touch Org Admin or `ResourceMenu` at all and were confirmed via isolated reruns: the terminology one passed cleanly alone (a timing flake, left alone — neither of this session's two named pre-existing flakes, but the same class of issue, and not caused by this change); `pelion-v2.spec.ts` failed the *same way* alone too, which is what led to root-causing and fixing the three unrelated bugs described above rather than dismissing it as flaky. A second full clean-slate run came back **64/65** (only the by-then-already-diagnosed `pelion-v2.spec.ts` category-form race left, mid-fix). The **third and final full clean-slate run, after all fixes: 65/65 passed.**

`docs/ux-audit-2026-08.md` roadmap Status updated to "Done," with the IdP-mapping scope exclusion named in the Notes column. Files changed: `frontend/src/components/ResourceMenu.tsx` (new, + `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/App.tsx`, `frontend/src/styles/theme.css`, `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/helpers.ts`, `org-admin-project-statuses-and-link-types.spec.ts`, `org-group-nesting.spec.ts`, `org-branding-override-reset.spec.ts`, `org-security-controls.spec.ts`, `org-rename-and-test-email.spec.ts`, `scim-provisioning.spec.ts`, `project-admin-templates-and-reports.spec.ts`; `tests/playwright/tests/pelion-v2.spec.ts` (unrelated-bug fixes, per above).

## Project Admin's 8-tab bar → 5 tabs

Next L-effort roadmap item, picked because the two shared components its own roadmap row named as prerequisites (`CollapsibleSection`, already in wide use; `Popover`, built for Org Admin's own "New group" flow) already existed from earlier passes this session, and `DefinitionList` (the "Shared 'definition-table CRUD' component" item, Partial) already covers Action Types here — the remaining work was purely tab count/organisation, not new shared infrastructure. Unlike Org Admin's restructure, this is **not** a resource-menu conversion: the style guide's own decision tree routes "5 or fewer views, all relevant together" (Project Admin's tabs, actively used inline while administering a project) to `Tabs`, and only "more than 5 groups, opened rarely as a whole" to a resource menu — Project Admin's tabs are the former, just one over the ceiling, so this is a consolidation, not a pattern change.

**Exact grouping, 8 → 5:**
- **Overview** — unchanged (name/summary/visibility/status/archive/export), plus Terminology's own JSX/state/handlers relocated underneath as a pure move, no logic touched — a `<hr>` divider and its own (unchanged) `<h2>Terminology</h2>` sub-heading separate it from Overview's settings, per the task's "at minimum a sub-heading" floor rather than a `CollapsibleSection` (a bare relocation didn't need the extra accordion machinery, and terminology-override *architecture* — making `strings.ts` itself terminology-aware, the actual high-value fix — stays deferred to last per direct prior user instruction; this item only moves where its existing, still-mostly-inert UI lives).
- **Structure** — Stages + the Components/Categories tree, each its own `CollapsibleSection`, both `defaultCollapsed={false}` (a freshly-merged tab shouldn't also hide half its own content by default, same reasoning the Org Admin restructure used for sections moving into a non-Overview resource-menu group). Fixes the audit's "tab whose own heading disagreed with its label" finding: the tree's own `<h2>` used to read bare "Components" under a "Categories" tab; it's now the `CollapsibleSection`'s own title, "Components & categories" — accurate to what the block actually is (a two-level component→category tree), and the redundant inner `<h2>` was deleted rather than left duplicating the new section header.
- **Fields & Actions** — Custom Fields + Action Types (the latter already on `DefinitionList`), same two-`CollapsibleSection`, both-open shape.
- **Groups** — unchanged membership UI, plus a new "New group" `Popover` — the audit's "a 'New X' flow with no create form" finding, the one tab that broke the app's otherwise-universal "every X has an inline create form" pattern. Copied Org Admin's own "New group" popover shape exactly (trigger ref, `newGroupPopoverOpen` state, Enter-to-submit on the name field, Cancel/Create row, a `useToast()`-fired "Group created" success toast per Principle 7) with one real difference: `ProjectGroupCreate` requires `role: ProjectRole` up front (a project group's role can't be changed after creation — no update endpoint exists for it), so this popover has a second field, a role `<select>` (same four options/order as `ProjectListPage`'s own role filter) defaulting to `member`. No backend change needed — `POST /projects/{id}/groups` and its schema already supported this; verified by reading `backend/app/schemas/project.py::ProjectGroupCreate` and the router before assuming otherwise.
- **Reports** — unchanged content, two fixes named in the audit: (a) the tab's own `<h2>` was a literal `"Report Setup"` string, the one heading in the file not pulled from `strings.ts` (the tab bar's own label already was) — now `{strings.admin.reportSetup}`; (b) its four settings groups, never wrapped in anything despite being exactly `CollapsibleSection`'s "optional block within a view" case, are now two sections — "Default template" (the template-selector dropdown, only rendered when the org has templates) and "Report content" (intro + body chapters + appendices, kept together rather than split three ways, mirroring `OrgAdminPage`'s own analogous "Report Defaults" section which already bundles the same trio under one heading — treating three fields that share one Save handler and one subject as one group, not an arbitrary three-way split for its own sake). The one shared `saveReportConfig`/"Save settings" button stays outside both sections, unaffected by which is currently collapsed.

**Deliberately left alone, named rather than silently dropped:**
- The four delete/reassign behaviours (Custom Fields, Stages, Categories/Components, Action Types) — already a considered, documented scope decision from the `DefinitionList` item earlier this session (see that entry's own reasoning: Stages'/Components'/Categories' delete flows carry extra non-generic behaviour a shared component would force awkwardly). This item is about tab *count and organisation*, not that inconsistency.
- Terminology-override architecture (`strings.ts` becoming terminology-aware, all 6 terms wired up) — deferred to last per direct prior user instruction; untouched beyond relocating Terminology's existing, unchanged UI as described above.

**A real accessible-naming collision found and fixed, not just a test inconvenience**: merging Terminology under Overview put two buttons both labelled "Save settings" (Overview's own project-settings save, and Terminology's) on one screen — a genuine Principle 8 violation (a screen reader or keyboard user tabbing through hears "Save settings… Save settings" with no way to tell them apart), not only a `getByRole` test ambiguity, so the fix went in the component, not just the tests: Terminology's button is now labelled "Save terminology" (`strings.admin.saveTerminology`, new). This is a deliberate, flagged deviation from the task's "pure relocation of Terminology's JSX, nothing else" instruction — the collision the merge itself introduced wasn't something a pure JSX move could avoid, and fixing it is a one-line text change, not a logic/state/handler change. A second, same-shape collision surfaced in Reports: the new "Default report template" `CollapsibleSection`'s own `aria-label` ("Default report template section") is a substring of the field's own `<label>` text, and Playwright/Testing-Library's `getByLabel` matches substrings by default — `getByLabel("Default report template")` resolved to both the section header and the actual `<select>`. Fixed the same way, by giving the section a distinct title ("Default template," `strings.admin.defaultTemplateSection`) rather than reusing the field's own label text, leaving the field's own label (needed for its own accessible name) untouched.

**Pre-existing test fragility surfaced, not caused, by the merge — fixed rather than dismissed**: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, and `pelion-v2.spec.ts` each compute a "this component's own row" locator as `input[value="Software"]` (or `"Web"`) → `xpath=../../..`, with no guard against also matching the *bottom* "add component" form's own Name field, which transiently still holds the just-typed value for one render before `setNewComponentName("")` clears it — a real, if narrow-window, race already implicitly relied on being harmless. Before this merge it was: the ancestor climb from either match stayed within the (then-standalone) Categories tab's own root. After merging Stages into the same "Structure" tab, `CollapsibleSection` adds one more wrapping level, so the *same* climb from the bottom field's transient match now escapes all the way up to the shared tab panel and picks up Stages' own "Name" field too — a real strict-mode violation, reproduced consistently (not a flake) in `pelion-v2.spec.ts`. Fixed with the `:not([placeholder])` guard `project-admin-structural.spec.ts` already established for exactly this shape (only a genuine rename input for an existing row is ever placeholder-less) in all three specs.

**Verified**: `npx tsc -b` and a full-tree `npx eslint .` clean throughout (re-checked after every fix). Storybook: `ProjectAdminPage.stories.tsx`'s existing 17 stories read individually and updated for the new tab names/`CollapsibleSection` scoping (not run-then-reactively-patched), plus one new story, `GroupsTabCreateGroupViaPopover` (mirrors Org Admin's own popover-create story: open, type a name, pick a role, submit, assert the `POST .../groups` call with `{name, role}` and the "Group created" toast). Full suite: **55 files / 376 tests, all green**. Rebuilt/recreated the `tests/container` frontend image (twice — once for the page/strings changes, once more after the two label-collision fixes). Grepped the whole Playwright suite for every old tab name (`Categories`, `Custom fields`, `Action types`, `Terminology`, `Project stages`) before considering this done, beyond the files anticipated in the task brief — found and fixed 9 spec files in total: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, `project-admin-structural.spec.ts`, `project-admin-groups-and-fields.spec.ts` (also gained the new "create group via popover" Playwright coverage, placed *after* the steps relying on "Members" being the last group in DOM order, since the new group would otherwise become the new last one and break those), `project-admin-action-types.spec.ts`, `project-admin-templates-and-reports.spec.ts` (no tab-name change needed — "Report Setup" kept its label — but exercised the "Default template" section-title fix), `stage-review-and-completion.spec.ts`, and `workflow-bypass-attempts.spec.ts` — the last two found only via the full-suite grep, not named in the task brief. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and Playwright suite runs: the first came back 62/65 (the three failures above — the terminology-tab-navigation gap, the two label collisions surfacing as `getByLabel`/`getByRole` ambiguity, and the pre-existing `softwareRow`/`webRow` race — each root-caused rather than assumed, per `CLAUDE.md`'s fix-don't-defer rule); the **second, full clean-slate run after all fixes: 65/65 passed**, with neither of this session's two named pre-existing flakes (`report-image-attachment.spec.ts`, `project-history.spec.ts`) appearing. Backend: no backend change was needed (`POST /projects/{id}/groups` and `ProjectGroupCreate` already supported the new popover's payload as-is, confirmed by reading the schema/router before assuming otherwise) — the full backend suite was still re-run against the rebuilt image as a sanity check per the change-management policy's practice for changes touching an existing authorization-adjacent endpoint: **538/538 passed**, `python3 -m pytest tests/`.

**An unrelated but serious bug found incidentally while drafting this entry**: this file (`docs/decisions.md`) had lost essentially its entire history in the working tree — the uncommitted copy this task started from had been silently reduced to 26 lines (just the tail of the "Logo & login-background reset" entry, decapitated of its own `##` heading in the process, plus the following "Org Admin → resource-menu restructure" entry) — almost certainly an earlier pass in this same session overwriting the whole file with `Write` instead of appending with `Edit`. The agent that found this restored the file back to `git show HEAD:docs/decisions.md` (1354 lines) plus the two salvaged entries plus this one — but HEAD itself was already stale by three whole entries authored earlier in this same session and never committed ("Consolidate filter UI onto `FilterPanel`", "A visible 'Invite' entry point + keyboard navigation for `UserAutocomplete`", "Unify the three 'aggregate' list pages"), and its guessed heading for the decapitated entry ("Org branding: logo & login-background reset") wasn't the real one. Caught and fully corrected in the same session, from the original text still held in that session's own context: the three missing entries reinserted verbatim in their correct chronological position, and the decapitated entry's real heading ("Logo & login-background reset") and its full lost body (intro paragraph, Backend/Frontend sections, and the identify → verify → remediate review write-up — only the final "Verified" paragraph had survived) restored in full. The file's `## ` heading list now matches this session's actual work item-for-item, with no inferred or guessed content remaining.

Files changed: `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-structural.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `project-admin-action-types.spec.ts`, `stage-review-and-completion.spec.ts`, `workflow-bypass-attempts.spec.ts`; `docs/ux-audit-2026-08.md`; `docs/decisions.md` (this entry, plus fully restoring the prior history lost earlier in the session, per above).

## Terminology overrides now cover their own surface: `strings.ts` becomes terminology-aware

Last item on the 2026-08 UX audit roadmap (L effort, deferred to last per direct prior user instruction — see that roadmap row and the "Terminology overrides are a fixed, closed set of keys" entry above). The audit's finding: of C-C-03's six overridable nouns (project, stage, component, category, requirement, change_request), only 2 (requirement, change_request) were consumed anywhere, and even those only on their own list page + nav label — the requirement detail page, its comments, review-due lists, the CSV import wizard, and (most visibly) Project Admin's own custom-fields entity-kind dropdown all hardcoded the English word one tab over from the Terminology settings meant to rename it.

**Architecture.** `frontend/src/i18n/terminology.ts` (new): `TerminologyKey`, `DEFAULT_TERMS`, `pluralize()` (fixes a real bug — a naive `+s` produced "categorys"; now handles the consonant+y→ies rule), and `applyTerminology`/`resolveTerminologyDeep` — a `{token}` substitution mechanism recognising a fixed `TOKEN_MAP` (`{project}/{Project}/{projects}/{Projects}`, and the same 4-way pattern for stage/component/category/requirement/changeRequest). `strings.ts`'s existing string table gets these tokens embedded directly in its English values (e.g. `newRequirement: "New {requirement}"`) rather than converting to a wrapper-function-per-string approach — chosen because it's a minimal diff over the existing `t()`/`Strings` shape everything already imports, preserves every existing key/call-site name, and keeps the "one string table, looked up by key" mental model intact; a function-per-string approach would have required touching every one of the ~800 entries' *shape*, not just tagging the ~150 that are genuinely one of the six concepts. This doesn't contradict the earlier "fixed, closed set of keys" decision above — that decision was about *not* opening the key set to arbitrary nouns, not about whether consumption should be fully templated; the six-key set stays exactly as fixed as before. `TerminologyContext.tsx` gained `useStrings()`: returns `strings.ts`'s whole table with every token substituted for the current project's resolved terminology (memoized on the `ProjectContext` terminology object), the terminology-aware replacement for calling `t()` directly.

**Consolidating the old ad hoc mechanism.** Before this, 5 call sites in 3 files (`Layout.tsx`, `RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectOverviewPage.tsx`) bypassed `strings.ts` for the two "working" terms, computing them directly via `useTerm`/`useTermPlural` and interpolating raw into JSX (e.g. `<Plus/> New {requirementTerm}`). Now that the corresponding `strings.ts` entries are tokenized, this was a second, redundant mechanism for the same thing — consolidated onto `useStrings()`, the ad hoc local variables deleted. `useTerm`/`useTermPlural` remain exported for the one legitimate remaining use case: a raw resolved term needed outside a string template (e.g. `CsvImportWizard.tsx`'s in-component `FIELDS` array, built with `useMemo` since it can't be computed at module scope like before — see below), plus one bare-fallback-noun spot in `ChangeRequestsPage.tsx` with no natural `strings.ts` key to route through.

**Frontend call-site conversion.** The dominant existing pattern was `const strings = t();` at *module scope* — outside the component function, which is exactly why plain `t()` could never be terminology-aware (not even inside a render). Converted every page/component confirmed to render at least one tokenized key to `const strings = useStrings();` *inside* the component body: `ProjectAdminPage`, `RequirementDetailPage`, `ChangeRequestDetailPage`, `ProjectListPage`, `OrgAdminPage`, `ServerOrganisationsPage`, `ProjectReviewsDuePage`, `MyReviewsDuePage`, `PreferencesPage`, `ReportsPage`, `ActionDetailPage`, `ProjectHistoryPage`, `FavouritesPage`, `ImportConflictPanel`, plus the 4 ad hoc-mechanism files above. Files confirmed to use zero tokenized keys (`BrandingContext`, `ConfirmDialog`, `SubscribeButton`, `CommentThread`, `SidePanel`, `Modal`, `LoadMoreButton`, `UserAutocomplete`, `DefinitionList`, `LoginPage`, `OrgListPage`, `OrgLoginPage`, `OidcCompletePage`, `NotificationBell`, `NotificationsPage`, `SignupPage`, `ServerManagementPage`, `ProjectActionsPage`) were deliberately left on plain `t()` — verified independently with a Python script that parses `strings.ts`'s actual nested structure, extracts every key whose value contains a real `TOKEN_MAP` token (as opposed to the pre-existing `{org}`/`{name}`/`{n}`/etc. call-site placeholder convention, which uses the same brace syntax but isn't a terminology token), and cross-references every `strings.*.*` call site in every `.tsx`/`.ts` file against that set — confirming zero files render a tokenized key while still calling `t()` at module scope.

**Leaks outside `strings.ts` entirely — the highest-value fixes, since a mechanical `t()`→`useStrings()` sweep could never catch these:**
- `frontend/src/api/types.ts`'s `CUSTOM_FIELD_ENTITY_KIND_LABEL` — a plain module-level `Record`, which can never be terminology-aware since it's a static export, not a hook — the audit's single most visible example (Project Admin's own custom-fields entity-kind dropdown reading "Requirement"/"Change request" literally, one tab over from Terminology). Deleted; `ProjectAdminPage.tsx`'s existing-field badge now derives from `strings.admin.entityKindRequirement`/`entityKindChangeRequest` (already tokenized, already used by the "New field" dropdown) via a small inline ternary instead.
- `frontend/src/components/CsvImportWizard.tsx`'s module-level `FIELDS` array (`label: "Component"`, `label: "Category"` hardcoded) — restructured into a `buildFields(componentTerm, categoryTerm)` function called inside the component via `useMemo`/`useTerm`, since a module-level array can't call hooks. The two fields' *hint* text ("Must exactly match an existing component's prefix...") was also tokenized, the identical leak class one line below the label. The `level` field's hint (`'"requirement" or "recommended"...'`) was deliberately left untouched — it names the literal `RequirementLevel` wire values, not the terminology noun, and tokenizing it would be actively wrong. `buildTemplateCsv`'s column *keys* and the download filename (`requirements-import-template.csv`) stay literal machine identifiers, same reasoning as `component_prefix` being an internal identifier rather than display text — only the *displayed* mapping-UI labels needed to become term-aware.
- `services/reports.py::generate_csv_report()`'s hardcoded `["ID", "Name", "Component", "Category", ...]` CSV header — gained an optional `terminology: dict[str, str] | None` parameter; the two affected cells now come from `(terminology.get(key) or default).capitalize()`. Its only caller, `routers/reports.py::generate_csv()`, already had `project` in scope and now passes `project.terminology`. Grepped `services/reports.py` and `services/labels.py` in full for any other hardcoded terminology-noun display text — found none: the PDF report's own table header is `["ID", "Name", "Reasoning", "Status"]` (component/category names are rendered as chapter/section *headings*, never the literal word "Component"/"Category"), and `labels.py` only maps `RequirementStatus` enum values, not a terminology noun.

**Two further raw leaks found during verification, not named in the original task brief, fixed as the same class of bug (an existing tokenized `strings.ts` key that was simply never wired to this particular call site) rather than left for later:** `ProjectListPage.tsx`'s list-view table had a bare `<th>Stage</th>` never routed through `strings.ts` at all (now `{strings.projects.stage}`, "Current {stage}"); `ProjectReviewsDuePage.tsx` and `RequirementsPage.tsx` each had a `FilterField label="Component"`/`label="Category"` hardcoded despite the same file already using tokenized keys two lines away (now `strings.requirements.component`/`.category`, reusing the exact same `{Component}`/`{Category}` keys `RequirementsPage.tsx`'s own create-form labels already used). Found by grepping the whole frontend for bare `>Noun<` and `label="Noun"`/`title="Noun"` JSX patterns for all six nouns after the named files were done, rather than assuming the task brief's list was exhaustive.

**Flagged by the implementing agent for my own review rather than fixed unilaterally, then reviewed and fixed directly** (each needed a new key/token added to `strings.ts`, which the task brief reserved for me specifically: "do not re-tag or re-scan `strings.ts`... flag it explicitly... rather than silently adding more tokens"). I checked each against real surrounding code before adding anything:
- `Layout.tsx`'s `<div className="nav-section-label">Project</div>` was completely raw, zero `strings.ts` routing — a genuine leak the original tagging pass didn't reach, since it's a section label rather than an existing `nav.*` entry. Fixed with a new `nav.projectSectionLabel: "{Project}"` key. (Its sibling section labels "Global"/"Administration" are not terminology nouns at all and were correctly left alone — not part of this fix's scope.)
- `ProjectListPage.tsx`'s `<FilterField label="Stage status">` was similarly raw — `projects.allStages` ("All {stage} statuses") only covered the dropdown's own empty option, not the field's own label. Fixed with a new `projects.stageStatusLabel: "{Stage} status"` key.
- `RequirementsPage.tsx`'s `<option value="">All categories</option>` was raw despite the same file already using `strings.requirements.category` two lines above. Fixed with a new `requirements.allCategories: "All {categories}"` key (kept local to the `requirements` section rather than reusing the existing but unrelated-section `reports.allCategories`, matching this file's own established per-section key convention). Its sibling `<option value="">All versions</option>` on the "Target version" filter (bound to the `stage` entity under the hood, per `target_stage_id`) was deliberately left as plain English, same reasoning `strings.ts`'s own `targetVersion: "Target version"` entry was never tagged in the first place — "version" reads as a distinct, pre-existing UI-copy choice for this one field rather than the "stage" concept itself (the CSV wizard's `target_version` hint text treats it the same way), not a leak of the terminology system as designed.

All three fixes re-verified after adding: `npx tsc -b` clean, full Storybook suite still 55/380 green (no story asserted the old raw text in a way that would break), full clean-slate Playwright suite (`down -v && up --build -d`, both seed scripts, `npx playwright test` unfiltered) **66/66 passed** again, including `terminology-coverage.spec.ts` and both named pre-existing flakes absent.

**Seed data.** Neither seed script set any project's `terminology` field before this (checked, zero hits) — per CLAUDE.md, a feature with no seeded demo value reads as absent. `seed_demo_data.py`: new `set_project_terminology()` helper, applied `{"stage": "Phase", "requirement": "Spec", "change_request": "ECR"}` to the Falcon-3 Inspection Drone project (aerospace-engineering vocabulary reads naturally for a hardware programme; deliberately partial — project/component/category left at defaults — to also demonstrate overrides don't have to be all-or-nothing). `seed_e2e_dataset.py`: rather than mutate one of the 6 existing named projects (all reused across many other specs, several of which assert exact default-English nav/button text that an override would break), added a 7th, dedicated project (`Delta-1 Terminology Demo`, `TERMINOLOGY_PROJECT_NAME`/`TERMINOLOGY_OVERRIDE` constants, same fixed override) reserved solely for the new terminology spec — documented in the script and in `docs/e2e-workflows.md`'s persona/project table and a new numbered workflow entry (#23).

**Tests.**
- *Storybook*: existing suite re-verified unchanged under default terminology first (a token with no override resolves to the same English word — any change would have signalled a wiring mistake, not a story needing an update). New stories using the existing `withTerminology()` decorator: `RequirementDetailPage.stories.tsx`'s `TerminologyOverrideAppliesThroughoutPage` (the "Make a change request" link and the archive-confirm dialog title); `ProjectAdminPage.stories.tsx`'s `CustomFieldsTabEntityKindDropdownReflectsTerminology` (the fix's headline example — both the existing-field badge and the "New field" dropdown's two options) and `StructureTabPluralisesCategoryCorrectly` (proves the irregular-plural fix against *default* terminology — "Components & categories", not "& categorys" — deliberately with no `withTerminology` decorator, since the bug being guarded is in the default-term pluralisation path itself); `ProjectReviewsDuePage.stories.tsx`'s `TerminologyOverrideAppliesToHeadingAndFilters` (the review-due heading + component filter column/option).
- *Playwright*: `project-admin-groups-and-fields.spec.ts` already exercised the terminology *save flow* itself (set "requirement"→"Spec" on Beta-2, confirm the nav relabels, revert) — left untouched per the task's explicit "don't touch the save flow" boundary. Added a new, separate spec, `terminology-coverage.spec.ts`, using the dedicated Delta-1 project (no set/revert dance needed, since its override is permanent and no other spec depends on Delta-1's terminology staying at any particular value): nav rail ("Specs"/"ECRs"), the requirement detail page's "Make ECR" link, Project Admin's custom-fields entity-kind dropdown (default "Spec", switched to confirm "ECR"), and the review-due list heading ("Specs due for review").

**Scope boundaries respected**: `TERMINOLOGY_KEYS` (backend, fixed/closed set) untouched; the terminology *save* UI/flow (Project Admin's Terminology tab, `PUT /projects/{id}/terminology`) untouched; `strings.ts` itself not re-scanned/re-tagged beyond what was already tagged going in.

**Verified**: `npx tsc -b` and a full-tree `npx eslint .` clean throughout (0 errors; re-checked after every batch of changes; the 5 pre-existing `react-refresh/only-export-components` warnings in `BrandingContext.tsx`/`ThemeContext.tsx` are unrelated and untouched). One genuine new lint finding fixed, not suppressed: converting `ProjectOverviewPage.tsx` to `useStrings()` surfaced a real `react-hooks/exhaustive-deps` warning on its data-loading `useEffect` (it closes over `strings.common.error`, which is now a reactive value rather than a module constant) — added to the effect's dependency array rather than disabled inline. Storybook: **55 files / 380 tests, all green**. Rebuilt/recreated the `tests/container` backend and frontend images before any live verification (neither bind-mounts source). Backend: pytest run first per the change-management policy's sequencing rule (never concurrent with Playwright) — **541/541 passed** (538 pre-existing + 3 new: `generate_csv_report`'s override/default-fallback behaviour, plus a router-level test proving `generate_csv`'s endpoint actually passes the project's *persisted* terminology through, not just that the service function accepts the parameter in isolation), `python3 -m pytest tests/`. Backend container restarted (pytest truncates all tables) and both seed scripts re-run before Playwright, per the standard sequence. Playwright: full clean-slate suite, no filter — **66/66 passed** (65 pre-existing + 1 new, `terminology-coverage.spec.ts`), including neither of this session's two named pre-existing flakes (`report-image-attachment.spec.ts`, `project-history.spec.ts`) appearing. Also grepped the whole Playwright suite for any spec asserting an exact default-English word on a project whose terminology this change touched — none found, since Delta-1 is dedicated and new, and Beta-2's terminology (used by `project-admin-groups-and-fields.spec.ts`) was already reset to default at that spec's own end before this change and remains untouched by anything here.

Files changed: `frontend/src/i18n/terminology.ts` (new), `frontend/src/context/TerminologyContext.tsx`, `frontend/src/i18n/strings.ts` (tokenized ahead of this implementation pass, plus 3 more keys added afterward for the flagged-and-fixed leaks above); `frontend/src/components/Layout.tsx`, `CsvImportWizard.tsx`, `ImportConflictPanel.tsx`; `frontend/src/pages/{RequirementsPage,ChangeRequestsPage,ProjectOverviewPage,ProjectAdminPage,RequirementDetailPage,ChangeRequestDetailPage,ProjectListPage,OrgAdminPage,ServerOrganisationsPage,ProjectReviewsDuePage,MyReviewsDuePage,PreferencesPage,ReportsPage,ActionDetailPage,ProjectHistoryPage,FavouritesPage}.tsx` (+ `.stories.tsx` for `RequirementDetailPage`, `ProjectAdminPage`, `ProjectReviewsDuePage`); `frontend/src/api/types.ts`; `backend/app/services/reports.py`, `backend/app/routers/reports.py`, `backend/tests/test_reports.py`; `backend/scripts/seed_demo_data.py`, `backend/scripts/seed_e2e_dataset.py`; `tests/playwright/tests/e2e-workflows/helpers.ts` (+ new `terminology-coverage.spec.ts`); `docs/e2e-workflows.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## First-pass UX review: "My organisations" leaked server-admin scope, no build-identity surface, resource menus with no title

Three issues from the user's own first-pass review of this session's UX-audit work, not roadmap items — logged here per this project's own convention of recording implementation decisions regardless of where the request originated.

**"My organisations" showed every org on the server to a server admin.** `OrgListPage.tsx` (the nav rail's "My organisations" link, `/orgs`) called the bare `GET /orgs`, which deliberately returns every org on the deployment for a server admin — I-M-05's platform-wide bypass, correct for `ServerOrganisationsPage`'s own directory but wrong here: this is a *personal* membership list, the same distinction `PreferencesPage.tsx`'s own "Access" tab had already worked around with an N+1 membership-probe (its own code comment names the exact same bug). The backend already had the right tool for this, unused by this one call site: `GET /orgs?mine=true`, an explicit opt-out of the server-admin bypass added for `ProjectListPage`'s org filter and already documented in `orgs.py::list_organizations`'s own docstring as existing for exactly this "organisations I can actually act within" case. Fixed with a one-line switch to that query param — no backend change needed, the endpoint already supported this.

*A real regression found during full-suite verification, not shipped silently*: `server-admin-scope.spec.ts` navigated to `/orgs` directly (`page.goto`, not the nav link) specifically to exploit the old unfiltered behaviour, reaching Alpha's degraded "not a member" admin view (I-M-05's other carve-out: a server admin can join/bootstrap any org's initial admin) via a page that was never the intended path for that — `ServerOrganisationsPage.tsx` already has its own "Edit" link straight to `/orgs/{id}/admin` per row, the real nav-linked route for this. The `mine=true` fix correctly broke the test's incidental shortcut without removing any real capability. Fixed by retargeting that test step through `/server/organisations` → its own "Edit" link, scoped to Alpha's row (`getByRole("row", { name: new RegExp(ORG_NAMES.alpha) })`, the same scoping pattern `server-org-lifecycle.spec.ts` already uses) — the test's stale comment ("/orgs isn't linked from the nav any more") was also inaccurate against current `Layout.tsx` (the "My organisations" link renders for every user, server admin included) and is no longer present now that the step doesn't depend on it.

**No way to see the running frontend/backend build version from the UI.** CI (`docker-build` job) already computes a GitVersion SemVer and tags every image with it plus the commit SHA, but nothing baked that identity *into* the image — a running container had no way to report its own version, only the tag it happened to be pulled under. Fixed by threading `APP_VERSION`/`GIT_SHA`/`BUILD_DATE` through as Docker build ARGs (both `backend/Dockerfile` and `frontend/Dockerfile`, defaulting to `dev`/`unknown` for a plain local build with no `--build-arg`) and wiring the CI job's existing `steps.gitversion.outputs.semVer`/`github.sha` (plus one new `Determine build date` step) into `build-args:` on both image builds. Backend: `app/version.py` reads them from the environment once at import time; new `GET /api/v1/system/version` (any authenticated user, not server-admin-gated — build metadata, not org content, same reasoning `/health` already uses for staying unauthenticated) returns them. Frontend: Vite's own `import.meta.env.VITE_*` build-time substitution picks up the same values (set as `VITE_`-prefixed `ENV` in the Dockerfile, distinct from `VITE_API_BASE_URL`'s *runtime* injection via `env-config-entrypoint.sh` — version is a property of the image itself, not the deployment target, so it belongs with Vite's build-time constants, not that mechanism) via a new `frontend/src/version.ts`. Surfaced as a small always-visible footer at the bottom of the nav rail ("App vX.Y.Z" / "API vX.Y.Z", full commit+build-date in each line's `title`), hidden when the rail is collapsed to icons-only like every other rail label — not a new Preferences tab, since Preferences already sits at the style guide's own 5-tab ceiling (Principle 1) and this is glanceable status, not a setting.

**The new resource menu had no title showing which org/project it was editing.** Two related but separate gaps, both from the audit's own earlier restructure passes: Org Admin's resource-menu conversion (see "Org Admin -> resource-menu restructure" above) moved the organisation's own `<h1>{org.name}</h1>` into the Overview group's content specifically — every other group (People, Projects & workflow, Branding & defaults, Templates & reports, Integrations & security) had no page title at all once you navigated off Overview, since `ResourceMenu` itself rendered nothing but the menu+content grid. Project Admin's 8-tabs-to-5 consolidation left its own `<h1>` as the generic `strings.nav.admin` ("{Project} admin") label, no project name anywhere on the page. Fixed by giving `ResourceMenu` an optional `title`/`subtitle`, rendered once above the menu+content grid so it persists across every group switch (unlike the per-group content it wraps) — `OrgAdminPage` passes `org.name` + "{OrgCap} admin" as subtitle, and the now-redundant duplicate `<h1>{org.name}</h1>` inside Overview's own content was removed rather than left duplicating text the page now shows universally. `ProjectAdminPage` (not a `ResourceMenu` consumer — it's `Tabs`, per the style guide's own 5-or-fewer-views routing) got the same fix directly: its `<h1>` now reads the project's own name, with the old generic label demoted to a muted subtitle underneath, matching `ProjectOverviewPage`'s own established `<h1>{project.name}</h1>` pattern elsewhere in the app.

**Verified**: `npx tsc -b` and a full-tree `npx eslint .` clean throughout (0 errors; the same 5 pre-existing `react-refresh/only-export-components` warnings, unrelated and untouched). Storybook: **55 files / 382 tests, all green** (new: `ResourceMenu.stories.tsx`'s `ShowsTitleAndSubtitle`, proving the title persists across a group switch; `Layout.stories.tsx`'s `ShowsVersionFooter`; `OrgListPage.stories.tsx`'s three existing stories tightened to assert the exact `mine=true` path rather than accepting any `api.get` call, so a regression back to the bare endpoint fails the story instead of silently passing). Backend: **543/543 passed** (541 pre-existing + 2 new, `test_system_version.py` — reachable by any authenticated user, 401 unauthenticated), `python -m pytest tests/ -q`. Rebuilt/recreated the `tests/container` backend and frontend images before any live verification (neither bind-mounts source; confirmed both build cleanly with no `--build-arg`s, reporting the documented `dev`/`unknown` placeholders). Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and Playwright: first run came back 69/70 (the `server-admin-scope.spec.ts` regression above, root-caused rather than assumed, then fixed); **second full clean-slate run after the fix: 70/70 passed** (68 pre-existing + 2 new: `admin-page-titles-and-version-footer.spec.ts`, and one new test added to `org-admin-nav-link.spec.ts` proving the zero-membership server-admin persona sees the empty state), including neither of this session's two named pre-existing flakes (`report-image-attachment.spec.ts`, `project-history.spec.ts`) appearing.

Files changed: `frontend/src/pages/OrgListPage.tsx` (+ `.stories.tsx`); `.github/workflows/ci.yml`, `backend/Dockerfile`, `frontend/Dockerfile`; `backend/app/version.py` (new), `backend/app/routers/system.py`, `backend/tests/test_system_version.py` (new); `frontend/src/version.ts` (new), `frontend/src/vite-env.d.ts`, `frontend/src/api/types.ts`, `frontend/src/components/Layout.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `frontend/src/components/ResourceMenu.tsx` (+ `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.tsx`; `tests/playwright/tests/e2e-workflows/org-admin-nav-link.spec.ts`, `server-admin-scope.spec.ts`, `admin-page-titles-and-version-footer.spec.ts` (new); `docs/deployment.md`, `docs/e2e-workflows.md`, `docs/decisions.md` (this entry).

**A CI-only failure, root-caused rather than assumed.** The PR built on this work (`main...feature/ui-improvements`) failed CI's `Backend tests, coverage & E2E` job on `terminology-coverage.spec.ts`, which had passed on every local run in this session (including this entry's own full clean-slate re-verification). The actual failure: a strict-mode violation on `page.getByText("Must respond to input within 50ms")` — Playwright's default substring match is case-insensitive, and HW-FN-001's own seeded `reasoning` field restates its name in lowercase prose ("Reasoning: must respond to input within 50ms."), so the bare `getByText` resolved to both that paragraph and the actual requirement link, non-deterministically depending on render/viewport timing (present in CI's run, absent in every local run here). Fixed by scoping to `getByRole("link", { name: "Must respond to input within 50ms" })`, the only one of the two that's actually the intended, clickable target — re-verified locally (this spec alone, then a full clean-slate 70/70 run) before pushing.

## PR #9 CI run hung 18+ minutes with no output, had to be manually cancelled — no timeout anywhere in the workflow, and no ceiling on it

[Run 32209526768](https://github.com/ballardr/reqtrackmanager/actions/runs/32209526768), `backend-and-e2e`'s "Install Playwright browsers" step (`npx playwright install --with-deps chromium`, `tests/playwright`), hung with zero output from `02:54:52Z` to `03:12:45Z` — user reported it as "stuck," cancelled it manually, and asked whether something needed fixing rather than assuming a fluke. Root-caused from the raw job log (`gh run view --job=<id> --log`), not assumed: the step's own `npm ci` and initial `apt-get update` output ran normally through `Get:5 https://archive.ubuntu.com/ubuntu noble-security InRelease`, then produced nothing further until the cancellation — a stall inside `apt-get update` against `archive.ubuntu.com`/`security.ubuntu.com`, a known flaky/slow-mirror issue on GitHub-hosted Ubuntu runners (whose apt sources mix the fast in-datacenter `azure.archive.ubuntu.com` mirror with these slower direct-to-Canonical ones), not a code or dependency regression. The real gap the incident exposed: `ci.yml` had no `timeout-minutes` anywhere — no step, no job — so a stall like this had nothing to stop it short of GitHub's 6-hour default job ceiling, and the cascading failures the user saw after cancelling (`docker-build` failing, "No test report files were found," missing `playwright-report.xml`) were all just downstream consequences of the cancellation (`docker-build` has `needs: [frontend, backend-and-e2e]`), not separate bugs.

Separately confirmed via each job's `startedAt` (`gh run view --json jobs`) that `frontend` and `backend-and-e2e` already start within the same second on every run checked, both this one and prior successful runs — the two jobs have no `needs:` between them and already run in parallel exactly as intended; the visible duration gap (frontend ~2 min vs. backend-and-e2e ~15-20 min normally) is inherent to backend-and-e2e's much longer serial step chain (Docker Compose build, two health-waits, pytest, two ruff lints, MCP tests, reseed, full Playwright suite), not a scheduling problem. No change made here.

**Fix**: `timeout-minutes` added to all four jobs (`frontend`: 15, `backend-and-e2e`: 45, `publish-badges`: 10, `docker-build`: 20 — each roughly 2-3x its normal observed duration) plus `timeout-minutes: 5` on both "Install Playwright browsers" steps specifically (frontend's and backend-and-e2e's), so a repeat of this mirror stall fails in 5 minutes instead of running until manually noticed and cancelled. Alongside this, both jobs' Playwright browser caches (`~/.cache/ms-playwright`, keyed on `${{ runner.os }}-playwright-${{ hashFiles(<that job's package-lock.json>) }}` — the two jobs pin different Playwright versions, `frontend/package.json`'s `playwright` vs. `tests/playwright/package.json`'s `@playwright/test`, so each needs its own key) are now cached via `actions/cache@v4`, cutting the ~300MB Chromium re-download on every run; this doesn't remove the `apt-get update` exposure itself (`--with-deps` runs it regardless of whether the browser binary was already cached, since OS-level packages don't persist across the fresh VM each job gets), which is what the timeout is for.

**Verified**: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` clean. Not re-run against a live GitHub Actions run as part of this change (no code path to reproduce the mirror stall on demand) — the timeout values were sized against this run's own real step/job durations and prior successful runs' `gh run view --json jobs` timings, not guessed.

Files changed: `.github/workflows/ci.yml`, `docs/decisions.md` (this entry).

## UX audit, fifth pass: `FilterBadge` and grouped downloads written up as rules, entity lists at scale, "view a user" gap, and a real bug fix (Requirements' create form never actually reached `SidePanel`)

Requested as a fresh top-to-bottom UI review from the perspective of a UX design team lead, rather than a specific bug report — re-checked every page against the style guide the first four passes produced (not against the pre-audit UI), which surfaced gaps the earlier passes' own roadmap items had left half-closed. Full findings and roadmap in `docs/ux-audit-2026-08.md`'s new "fifth pass" intro paragraph and the sections it links (`#directories-at-scale-users-and-groups`, `#no-way-to-view-a-users-access`, `#badges-as-filter-shortcuts-real-but-not-everywhere`); two new style guide principles (10, 11) and two new pattern sections ("directories at scale," "entity detail panel") in `docs/ux-style-guide.md`. Per this project's own fix-don't-defer convention, three of the findings were fixed in the same pass rather than left on the roadmap:

**Requirement single-create converted onto `SidePanel` (a real bug, not just a style-guide gap).** The "One entry point for single vs. bulk requirement creation" item (this log, above) built the Popover chooser ("Add one"/"Import from CSV") but left "Add one" opening the same pre-audit inline card it always had — `SidePanel` existed (built for the fourth pass) but had never actually been wired to a real create flow anywhere in the app. Project Overview's own "+ New Requirement" link deep-links to `?new=1`, which set `showNewForm` directly, skipping the Popover — so from Overview the (identical, still-unconverted) form read as "a different, older form" than the one reached via the Popover on the Requirements page itself. Fixed by moving both branches of the create form (the main fields, and the "no components/categories yet" quick-setup variant) into `SidePanel`; both entry points now open the literal same layer, and the list underneath stays visible and untouched while it's open. `RequirementsPage.tsx`: two-column field rows (`component`/`category`, `target version`/`level`) collapsed to single-column `<label>` stacks to fit `SidePanel`'s 420px width. No behaviour change to any handler — `createRequirement`/`createInlineComponent`/`createInlineCategory` and their state are untouched, only the JSX container around them moved.

**CSV "Export CSV"/"Download template" combined behind one "Export" `Popover`.** Same shape as Principle 5's "one door for add one and add many," just for two downloads instead of a create/import pair — the style guide didn't say the rule extended past creates, so nothing flagged it. `CsvImportWizard.tsx` gained `exportMenuOpen`/`exportTriggerRef` state and one "Export" trigger button opening a `Popover` with the two existing handlers (`exportCsv`, the template-download `onClick`) unchanged, just relocated. Import CSV's own trigger/location (`showImportTrigger`) is untouched.

**`ProjectActionsPage`'s outcome badge wired to `FilterBadge`.** Its results table showed `outcome_status` as a plain `<span className="badge">`, one column over from a `FilterPanel` `<select>` driven by the identical value — `FilterBadge` (built fourth pass, already correct on `RequirementsPage`/`ChangeRequestsPage`/`ProjectListPage`/`ServerOrganisationsPage`) had never been written up as a rule, so a fifth page reproduced the gap it should have prevented. New `toggleOutcomeFilter` helper, same shape as the other four pages' own toggle functions.

**Deliberately left alone, on the roadmap, not fixed here:** search + pagination for Org Admin's Users/Groups and Project Admin's Groups tab, and a read-only "view user" detail panel — both real M-effort features (a new search param + `limit`/`offset` + `LoadMoreButton` wiring for the former; a new `SidePanel`-based read-only view, possibly a new aggregate read endpoint, for the latter), not S-effort UI-only fixes like the three above. Named explicitly in the audit roadmap rather than silently dropped.

**A non-idempotent test found and fixed while verifying the `SidePanel` change, per this repo's own convention (not deferred):** `workflow-bypass-attempts.spec.ts` archived the seeded requirement "Must log all state transitions" and recreated it under a derived name — not reversible from the UI, so a second run against the same database (which is exactly what happened verifying this change, before a deliberate clean-slate reseed made the gap visible) failed at the step expecting the original seeded name to still exist, unarchived. Fixed by having the test create its own dynamically-named throwaway requirement (`E2E Bypass Target ${Date.now()}`) at the start of the run instead of depending on the fixed seeded name surviving from a prior run; re-verified by running the spec twice back-to-back against the same database with no reseed in between.

**A second, unrelated locator fix, same root cause, surfaced by the `SidePanel` change itself:** `review-scheduling.spec.ts` and `workflow-bypass-attempts.spec.ts` both used an unscoped `page.getByRole("combobox").first()` immediately after opening the requirement create form, relying on it being the first `<select>` in DOM order. Once the form became a `SidePanel` (portalled to the end of `document.body`), the filter sidebar's own Status `<select>` — still inline, earlier in the page — became the new "first" combobox instead. Fixed by scoping both to `page.getByRole("dialog", { name: "New Requirement" })` first. Checked every other `getByRole("combobox").first()` in the suite (`golden-path.spec.ts`, `mockup-engagement.spec.ts`, `change-request-tasks-and-voting.spec.ts`, `change-request-approval-separation.spec.ts`, `project-visibility.spec.ts`, `multi-org-admin.spec.ts`, `terminology-coverage.spec.ts`) — none of the others target the Requirements create form, so none needed the same fix; confirmed by running each affected file.

**Verified**: `npx tsc -b` and a full-tree `npx eslint .` both clean. Storybook (`vitest run --project=storybook`) on the three changed story files: 25/25 passed, including new stories `ExportMenuOffersCsvAndTemplate` (`CsvImportWizard.stories.tsx`) and `FilterByOutcomeBadge` (`ProjectActionsPage.stories.tsx`), and the existing `RequirementsPage.stories.tsx` create-flow stories updated to query `within(document.body)` for the now-portalled panel. Rebuilt/recreated the `tests/container` frontend image. Full clean-slate reseed (`docker compose down -v && up --build -d`, `seed_e2e_dataset.py`) before final verification, specifically to rule out the same "reused a mutated seeded fixture" false failures a mid-session non-clean rerun surfaced (and which led to the `workflow-bypass-attempts.spec.ts` fix above). Playwright, all currently green against the fresh dataset: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, `e2e-workflows/requirement-archive-confirm.spec.ts`, `e2e-workflows/review-scheduling.spec.ts`, `e2e-workflows/workflow-bypass-attempts.spec.ts` (run twice back-to-back, no reseed between, to confirm the idempotency fix), `e2e-workflows/csv-import-wizard.spec.ts`, `e2e-workflows/project-admin-groups-and-fields.spec.ts`, and the new `e2e-workflows/overview-new-requirement-side-panel.spec.ts` (deep-links from Project Overview, asserts the `SidePanel` dialog and the untouched list are both visible at once, creates a requirement through it). Not run: a full-suite pass across every spec in `tests/playwright/tests/e2e-workflows/` — the targeted set above covers every spec that actually interacts with the requirement create form, CSV export/template, or `ProjectActionsPage`'s outcome badge, confirmed by grepping the whole suite for each of those three surfaces before deciding the targeted set was complete, rather than run-everything-and-see.

Files changed: `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `frontend/src/components/CsvImportWizard.tsx` (+ `.stories.tsx`), `frontend/src/pages/ProjectActionsPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/golden-path.spec.ts`, `tests/playwright/tests/e2e-workflows/csv-import-wizard.spec.ts`, `review-scheduling.spec.ts`, `workflow-bypass-attempts.spec.ts`, and new `overview-new-requirement-side-panel.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/ux-style-guide.md`, `docs/decisions.md` (this entry).

## Search + pagination for Org Admin's Users/Groups and Project Admin's Groups tab

Sixth-pass roadmap item — the audit's own "Directories at scale" finding: at real scale (the user's own example: ~100 groups, ~1,000 users) these three admin lists had no search and no pagination, and both group lists rendered every group's full member list expanded, unconditionally, all the time.

**Backend, three endpoints, same contract as `list_requirements`**: `list_org_users` (`routers/orgs.py`), `list_org_groups`, and `list_project_groups` (`routers/projects.py`) each gained optional `search` (name/email substring, case-insensitive) and `limit`/`offset`, with the pre-slice total returned via `X-Total-Count`. Omitting `limit` still returns every match, unpaginated — deliberately, since existing unfiltered callers (Project Admin's own org-group nesting picker, calling `GET /orgs/{id}/groups` with no query params at all) needed to keep working unchanged. `list_org_users`' `search`/`limit`/`offset` are open to any caller who can already reach the endpoint, unlike the existing access-review filters (`stale_since_days`, `has_2fa`, etc.), which stay org-admin-gated — narrowing the same directory a plain member can already browse isn't an access-review signal. Added a stable `display_name`/`name` sort to all three (previously arbitrary dict/query order), required for offset-based pagination to actually behave — without it, "page 2" isn't a well-defined concept.

**Frontend — `OrgAdminPage.tsx` (Users and Groups sections)**: a search box + `LoadMoreButton` on Users, wired through a rebuilt `loadUsers(filter, search, offset, append)` (previously `loadUsers(filter)`, a single unpaginated `api.get`). Groups got the same search + `LoadMoreButton`, plus a real interaction-model change: each group now renders behind its own `CollapsibleSection` (`variant="plain"`, `defaultCollapsed`, title = `"<name> (<member count>)"`), collapsed by default, instead of an always-expanded `<div>`. One genuine complication, not just a rename: Org Groups can nest inside each other, and the nested-group name resolution (`nestedGroups`) and the "add nested group" `<select>`'s option list (`nestableGroups`) both need *every* group in the org, not whatever the Groups section's own search/page currently shows — resolving them against the paginated/searched `groups` state would silently drop nesting candidates that fall outside the current search term or page. Fixed with a second, deliberately-unpaginated `allGroups` fetch (the same `GET /orgs/{id}/groups` call with no query params, now doubling as this) — `groups` drives which groups render as cards, `allGroups` drives nesting resolution, kept as two separate pieces of state rather than one.

**Frontend — `ProjectAdminPage.tsx` (Groups tab)**: same search + `LoadMoreButton` + `CollapsibleSection`-per-group treatment, pulled out of the tab's big `reload()` `Promise.all` bundle into its own `loadGroups`. No `allGroups`-equivalent needed here — checked first, and confirmed a project group only ever nests *org* groups (resolved from the page's existing, separate, still-unpaginated `orgGroups` fetch), never another project group, so there's no self-referential nesting concern the way Org Admin's own groups have.

**A non-obvious a11y consequence, checked rather than assumed**: `CollapsibleSection`'s own docs already flag that a non-string `title` gets no `aria-label` fallback — this change is the first caller to actually pass a *computed* string (`` `${g.name} (...)` ``) rather than a static one, so it was worth confirming it stays a plain string (not JSX) specifically to avoid tripping that known, previously-dormant gap; it does.

**Verified**: New backend tests in `tests/test_pagination.py` (`test_org_users_search_and_paginate`, `test_org_groups_search_and_paginate`, `test_project_groups_search_and_paginate`) — pagination, total count, and search-by-name/email, plus (for groups) confirming an unpaginated call still returns everything. Full backend suite: 546/546 passed. `ruff check` clean (the pre-existing `ruff format` drift across 121 unrelated files, including these two routers, predates this change and wasn't introduced by it — not touched). `npx tsc -b` and `npx eslint .` both clean. Full Storybook suite: 55 files / 384 tests passed, including `OrgAdminPage.stories.tsx`'s and `ProjectAdminPage.stories.tsx`'s own group-section stories, updated for the new collapsed-by-default `CollapsibleSection` shape (`GroupsSectionShowsMembers`/`GroupsSectionNestGroup`/`GroupsTabAddMember`/`GroupsTabAddOrgGroup` each now expand the relevant group's card before asserting on its contents) and a new `spyOn(api, "getPage")` mock branch for `/users`/`/groups` in both files' shared mock helpers. Rebuilt/recreated both `tests/container` images; full clean-slate reseed before Playwright verification. New shared helper `openGroupCard()` (`tests/e2e-workflows/helpers.ts`, same idempotent-only-click-if-collapsed shape as the existing `ensureExpanded`) — four existing specs relying on a group's member list/add-member input/nesting picker being reachable without expanding anything (`org-group-nesting.spec.ts`, `org-security-controls.spec.ts`, `external-project-invite.spec.ts`, `project-admin-groups-and-fields.spec.ts`) updated to call it first; all four, plus nine further admin-surface specs identified by grep as plausibly touching these sections (`multi-org-admin`, `org-admin-nav-link`, `admin-page-titles-and-version-footer`, `single-org-admin`, `user-directory-and-bans`, `server-admin-scope`), pass. Not run: a full-suite pass across every remaining spec — the fifteen run above cover every spec found (by grepping the whole suite for the Users/Groups sections' own placeholder text, section labels, and interaction points) to actually touch the changed surface.

Files changed: `backend/app/routers/orgs.py`, `backend/app/routers/projects.py`, `backend/tests/test_pagination.py`; `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/helpers.ts`, `org-group-nesting.spec.ts`, `org-security-controls.spec.ts`, `external-project-invite.spec.ts`, `project-admin-groups-and-fields.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## A read-only "view user" access panel

Last of the three Users/Groups roadmap items from this session — the audit's "No way to view a user's access" finding: an org admin auditing what one person can actually reach had to open every project's own membership list by hand, one at a time.

**Backend**: new `GET /orgs/{organization_id}/users/{user_id}/access` (org-admin gated), returning `{ org_groups, projects }` — `org_groups` is the user's *direct* org-group memberships; `projects` is every project in the org where `rbac.get_effective_project_roles(db, user_id, project_id)` resolves to a non-empty role set, each with that role set and which of the project's own groups the user directly belongs to. Deliberately calls the existing `get_effective_project_roles` rather than re-deriving role resolution (direct assignment, direct/nested project-group membership, org-wide-visibility grants) a second time — one algorithm serving both the actual permission checks and this read-only summary means they can't quietly drift apart. Iterates every project in the org and calls it once each, server-side, rather than exposing an N-round-trip client-side fan-out. Projects with zero access are omitted from the response entirely (this answers "what can they reach," not "here's every project with an empty role list"). New schemas `UserAccessGroupRef`/`UserAccessProject`/`UserAccessOut` in `schemas/org.py`.

**Frontend**: `OrgAdminPage.tsx`'s Users table gained a per-row "View access" (eye icon) button, opening a read-only `SidePanel` — org groups as a plain list, each accessible project as a card (name linking to the project, a `.badge` per role, and a "via project group(s)" line when applicable). Fetched on demand when the panel opens, not preloaded per row.

**Verified**: New backend tests, `test_user_access.py` — a user with a direct org-group membership, a direct project role, and role-via-project-group all show up correctly; a project with zero access is excluded; non-admin gets 403; unknown user id gets 404. Full backend suite: 546/546 (unchanged from the prior entry — no other endpoint touched). `ruff check` clean. `npx tsc -b`/`npx eslint .` clean. New Storybook story `ViewUserAccessPanel` (`OrgAdminPage.stories.tsx`) plus a new `getPage`-adjacent `/access` branch in the shared mock helper; full Storybook suite 55/55 files, 385/385 tests. New Playwright spec `user-access-panel.spec.ts` — views the logged-in org admin's own row (rather than depending on a second seeded persona's exact org/project membership shape) and confirms both of Alpha's projects and the "Project manager" role appear, plus Escape-to-close; re-ran alongside the six specs from the prior entry plus five more admin-surface specs, all pass. One process note worth recording since it cost real time: running `pytest` a second time mid-session (for these new tests) truncated the e2e dataset again, same as the first pagination pass — the `docker compose restart backend` + reseed step is required after *every* pytest run against `tests/container`, not just the first, and was momentarily missed here before Playwright verification caught it via a login failure.

Files changed: `backend/app/routers/orgs.py`, `backend/app/schemas/org.py`, `backend/tests/test_user_access.py`; `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`; new `tests/playwright/tests/e2e-workflows/user-access-panel.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## PR #10 CI hit the same apt-mirror stall as PR #9, same day — removed the `apt-get install` step entirely instead of just timing it out faster

The 5-minute `timeout-minutes` added after the PR #9 incident (previous entry) did its job — [run 32224889835](https://github.com/ballardr/reqtrackmanager/actions/runs/32224889835)'s `frontend` job failed its "Install Playwright browsers" step at exactly 5 minutes instead of hanging — but the underlying `archive.ubuntu.com`/`security.ubuntu.com` mirror stall recurred, a second time in one day, and cascaded into a spurious "Frontend coverage summary" failure (`coverage/coverage-summary.json` never got written, since the step that writes it never ran). Root-caused from the job log (`gh api repos/.../actions/jobs/<id>/logs`), same signature as before: `npm ci`/lint/build/`apt-get update` all completed normally, then nothing until `##[error]The action 'Install Playwright browsers' has timed out after 5 minutes.`

**Decided against**: leaving the timeout as the only mitigation (fails faster, but doesn't stop it recurring) or lowering/raising the timeout value (doesn't address the actual cause — a flaky Canonical mirror GitHub's own runner images depend on, entirely outside this repo's control).

**Fix**: both jobs' `npx playwright install --with-deps chromium` step (the one that runs `apt-get install` for Chromium's OS deps and is what actually stalls — installing the browser binary itself is fast and was never the problem) is gone. Confirmed via the MCR registry API (`curl https://mcr.microsoft.com/v2/playwright/tags/list`) that `v1.62.1-noble` exists and matches both `frontend/package-lock.json` and `tests/playwright/package-lock.json`'s exactly-resolved `playwright`/`@playwright/test` version (`1.62.1` in both, despite `tests/playwright/package.json` still declaring `^1.47.2` — a valid range, not a bug, npm just resolved to the newest version satisfying it).

- `frontend` job now runs entirely inside `container: mcr.microsoft.com/playwright:v1.62.1-noble` — Chromium and its OS deps are already in the image, so the "Cache Playwright browsers" and "Install Playwright browsers" steps are deleted outright, not just sped up. `actions/setup-node@v7` and every other existing action still runs fine inside a job container (the GitHub Actions runner mounts what JS-based actions need into the container automatically); no other step changed.
- `backend-and-e2e` can't take the same whole-job-container approach — it spends most of its steps running `docker compose`/`docker compose exec` directly against the runner's own Docker daemon to drive the dev/test stack, and putting that inside a job container would mean Docker-in-Docker, a materially bigger and riskier change than this incident warrants. Instead only the Playwright test run itself moves into a throwaway `docker run --network host mcr.microsoft.com/playwright:v1.62.1-noble` container against that same host daemon — `--network host` reaches the compose stack's services on `localhost` exactly like the native process did (every service in `tests/container/docker-compose.yml` publishes a host port; `playwright.config.ts`'s `baseURL` is `http://localhost:3000`), and the working directory (already `npm ci`'d on the host in a separate step first) is bind-mounted in, so it's the same test files/config/`node_modules` as before — `@playwright/test` has no native/compiled dependencies, so a host-installed `node_modules` mounted into another Linux/glibc container is safe.
- Both `container:`/image references carry a comment that the tag's version must be bumped in lockstep with any future `playwright`/`@playwright/test` npm version bump (same lockstep discipline CLAUDE.md already requires for Node's version across `ci.yml`/`Dockerfile`/`.nvmrc`) — a mismatch would break browser launches with a clear version-mismatch error rather than silently misbehaving, but there's no automated check for it yet.

**Verified**: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` clean. Tag existence and exact version match confirmed against the live MCR registry API and both lockfiles, as above. **Not verified end-to-end against a live GitHub Actions run** — no way to reproduce the apt-mirror stall on demand, and this needs a real PR run to confirm both `container:` jobs actually execute (in particular: `actions/setup-node` behavior inside the frontend job's container, and file ownership of report files written by `docker run`'s default-root process and then read back by `dorny/test-reporter`/`actions/upload-artifact` on the host in `backend-and-e2e` — root-owned bind-mounted files are normally world-readable so this is expected to be fine, but untested here). Flagging this explicitly rather than claiming it's proven: watch the next PR #10 CI run closely and adjust if either job surfaces a problem this reasoning didn't anticipate.

Files changed: `.github/workflows/ci.yml`, `docs/decisions.md` (this entry).

## The Docker-image CI fix above worked and surfaced a real, pre-existing `.side-grid` overflow bug on ProjectListPage's tile view

The previous entry's open questions resolved cleanly on the next real run: [run 32228099145](https://github.com/ballardr/reqtrackmanager/actions/runs/32228099145) (`main`, post-merge) — `frontend` passed in ~2 minutes with no `apt-get` step at all, and `backend-and-e2e`'s `docker run --network host` reached the compose stack fine and its root-owned report files were read back without issue by `dorny/test-reporter`/`actions/upload-artifact`. But `backend-and-e2e` still failed, on a genuine test failure this time, not infrastructure: `tests/e2e-workflows/mobile-card-grid-overflow.spec.ts`'s "Project List tile view" — `expect(overflow).toBeLessThanOrEqual(0)` got `49`. 71/72 other tests passed, including the other three tests in that same spec file.

**Root-caused, not assumed** — reproduced live against the local `tests/container` stack via the Playwright MCP browser tools (44px locally, close enough to CI's 49px; the small difference is an unrelated leftover throwaway project from earlier local testing padding out the min-content calculation below) at a 375×900 viewport as `e2e-orgadmin-ab@example.com`. Walking the DOM (`getBoundingClientRect()` on every element, `getComputedStyle()` up the ancestor chain from `.grid`) found `.side-grid`'s own box correctly at 271px (its `@media (max-width: 860px) { .side-grid { grid-template-columns: 1fr; } }` collapse rule, added when this class was first introduced, **was** applying), but its single grid-item child was rendering at 339.633px — wider than its own parent. Cause: `1fr` is shorthand for `minmax(auto, 1fr)`, and `auto`'s automatic minimum is the column's content's min-content size. ProjectListPage's tile view nests another `repeat(auto-fill, minmax(min(280px, 100%), 1fr))` grid inside that column (the project-card grid itself), and that inner grid's `100%` can't resolve while the outer `.side-grid` column is being asked for ITS min-content contribution — the container size the `100%` would resolve against is exactly what's being computed at that point — so per spec it falls back to the literal `280px` floor instead of the intended container-clamped value. On content wide enough to actually need that clamp — the seeded `orgAdminAlphaBeta` persona's role text, "Your roles: Member, Project administrator, Project manager, Stakeholder" — that forced the *entire* `.side-grid` column wider than the viewport, not just the nested grid, defeating the 860px collapse this rule exists to do. Confirmed by live-patching the stylesheet in the browser (`document.head.appendChild` a `<style>` override): overflow went from 44px to exactly 0px changing only `1fr` → `minmax(0, 1fr)`.

This is a second, more subtle instance of the exact same root problem the spec file's own docstring already documents being fixed once before (percentage-based track sizing not surviving a size-indeterminate computation context) — the first fix (`minmax(280px, 1fr)` → `minmax(min(280px, 100%), 1fr)` on the inner grid) handled the *inner* grid being asked for its own final layout size; this one is the *outer* `.side-grid` column being asked for its min-content contribution, a different computation context the same class of fix doesn't reach. Only "Project List tile view" hit it among the four tests in that spec (Requirements/Change Requests/Favourites tile views also nest a `repeat(auto-fill, ...)` grid inside a `.side-grid` column) because it's the only one of the four whose card content includes this specific long, comma-joined role list — the other three's card content stays under the ~280px floor this bug degrades to, so the mis-sized minimum never exceeds the viewport for them. Not a regression from the CI change above; it was already broken, just never previously observed because CI hadn't run this suite to completion (or wasn't run against this exact seeded role data) since `.side-grid` was introduced.

**Fix**: `.side-grid`'s collapsed-state rule (`frontend/src/styles/theme.css`, inside the `@media (max-width: 860px)` block) is now `grid-template-columns: minmax(0, 1fr)` instead of bare `1fr` — explicitly overriding the automatic minimum to `0` so no descendant's intrinsic content size can force the column wider than the space actually available, which is the standard fix for this category of CSS Grid bug. The base (non-collapsed, ≥861px) two-column rule is untouched — this only affects the narrow-viewport single-column case the failing test actually exercises, and there's no evidence (or test coverage) of the same issue at wider viewports where there's normally ample room.

**Verified**: `npx tsc -b` clean (CSS-only change). Rebuilt/recreated the `tests/container` frontend image (containers don't bind-mount source) and re-confirmed via the browser tools against the *real* rebuilt CSS, not just the live-patched override: 0px overflow, screenshot showing all card content fitting cleanly. `mobile-card-grid-overflow.spec.ts` alone: 4/4 passed (previously 3/4). Full clean-slate verification per this repo's own documented methodology (`docker compose down -v && up --build`, both seed scripts, exactly one suite run): **72/72 passed** — the 7 failures seen on an initial run against the pre-existing, hours-old local stack (`badge-filters`, `csv-import-wizard`, `org-login-2fa-handoff`, `project-admin-templates-and-reports`, `report-image-attachment`, `two-factor-auth`, `user-directory-and-bans`) were the known "continuously-mutating shared database" noise this doc has flagged before (two of the seven, `report-image-attachment.spec.ts` and `project-admin-templates-and-reports.spec.ts`, are already-documented pre-existing flakes above), not caused by this change — none touch `.side-grid` or any page this fix affects, and all seven passed clean on the reseeded run.

Files changed: `frontend/src/styles/theme.css`, `docs/decisions.md` (this entry).

## `RequirementDetailPage`'s Links/Actions cards: create rows onto `Popover`/`SidePanel`, remove/unlink onto `ConfirmDialog`

Roadmap item 1 of 6 from the sixth-pass audit — [Links and linked actions: the predicted debt is still there](ux-audit-2026-08.md#links-and-linked-actions-the-predicted-debt-is-still-there). This was the newest feature in the app (requirement-links/"traceability," merged the same day as the fourth-pass audit) and had already reproduced the exact always-inline, no-confirmation pattern the rest of the app was mid-fix on — predicted as accruing debt at the time, never actually roadmapped until this pass named it explicitly. Direct precedent copied from the same page rather than inventing a new shape: the Archive button's existing `ConfirmDialog` usage (Tier 1), and `RequirementsPage`'s "+ New requirement" `Popover` and `SidePanel`.

**Links card**: "Add link" (the `newLinkTargetId`/`newLinkTypeId` selects) now opens a `Popover` anchored to a new "+ Add link" trigger button, instead of rendering permanently below the list. Two fields (target requirement, link type) plus Cancel/"Add link" — same shape as Org Admin's "New group" popover. `addLink()` now closes the popover on success (`setAddLinkPopoverOpen(false)`) but leaves it open with `linkError` visible on failure, so a rejected submission doesn't silently vanish. Removing a link (`removeLink`) no longer fires straight from the row's trash-icon `onClick` — it now sets a `linkToRemove: RequirementLink | null` state and renders one `ConfirmDialog` (Tier 1, `requireTypedText` omitted) when set, reusing the existing `removeLink` string as the confirm button's label.

**Actions card**: split across two triggers rather than one, chosen per the style guide's Popover-vs-SidePanel decision tree by field count, not forced into a single shape — "Link existing action" (one field: an existing-action select) opens a `Popover`; "Create and link a new action" (five fields: title, description, type, assignee, due date) opens a `SidePanel`. The `showCreateAction` boolean already existed as the inline-reveal toggle and needed no renaming — it now gates a `SidePanel` instead of a `<div style={{background: "var(--color-surface-alt)"}}>` block, and `createAndLinkAction()` already closed it and reset its fields on success, so that function needed no changes at all. `linkExistingAction()` gained the same close-on-success/stay-open-on-error behaviour as `addLink()` above. Unlinking (`unlinkAction`) converts the same way as removing a link: an `actionToUnlink: RequirementAction | null` state plus one `ConfirmDialog`.

**New strings** (`frontend/src/i18n/strings.ts`, `requirements` namespace): `removeLinkTitle`/`removeLinkConfirm`/`unlinkActionTitle`/`unlinkActionConfirm` — generic (not per-row-instance) confirmation copy, matching how the existing `archiveTitle`/`archiveConfirm` pair on the same page doesn't embed the specific requirement's name either; the row you just clicked the trash/unlink icon on is the implicit subject. Both `*Confirm` messages use the `{requirement}`/`{requirements}` terminology tokens.

**Found and fixed along the way, not deferred**: `requirement-actions.spec.ts` created its throwaway action with a fixed title, `"E2E Action Flow Test"` — no timestamp. Re-running the updated spec against this repo's long-lived `tests/container` stack (4 days old, per this doc's own repeated "continuously-mutating shared database" notes) failed on a strict-mode locator violation: two actions already named exactly that, left behind by earlier unrelated runs, both linked to SW-PERF-006. This is precisely the non-idempotent-test class this repo's own testing rules call out — a spec must pass standalone or run repeated back-to-back against the same database, not only once against a freshly seeded one. Fixed by timestamping the title (`` `E2E Action Flow Test ${Date.now()}` ``), the same pattern `requirement-archive-confirm.spec.ts` already uses for its own throwaway requirement name; re-ran twice back-to-back afterward to confirm it no longer collides with itself.

**Verified**: No backend files touched — this feature's endpoints (`POST/DELETE .../links`, `POST .../actions`, `POST .../actions/create-and-link`, `DELETE .../actions/{id}`) were already correct and unchanged, so no backend test was needed; confirmed via `git status` before starting Docker verification. `npx tsc -b` and a full-tree `npx eslint .` both clean. Full Storybook suite: 55 files / 388 tests, including `RequirementDetailPage.stories.tsx`'s existing `AddLink`/`ActionsCardPopulated`/`CreateAndLinkAction` stories updated to drive the popover/panel (scoped to `within(document.body).getByRole("dialog", ...)`, since both portal out of the page root the same way `Modal`/`ConfirmDialog` do) plus four new stories: `RemoveLinkConfirms`, `LinkExistingAction`, `UnlinkActionConfirms`, and `AddLink`'s own assertion that the target-requirement select isn't in the document until the popover opens. Rebuilt/recreated the `tests/container` frontend image (no bind-mounted source). Updated `requirement-links.spec.ts` and `requirement-actions.spec.ts` for the new interaction shape (trigger click → scoped lookups inside the opened `dialog`, confirm-dialog step before each delete/unlink) plus the idempotency fix above; both, along with `requirement-archive-confirm.spec.ts` and `overview-new-requirement-side-panel.spec.ts` (the other two specs already exercising `ConfirmDialog`/`SidePanel` on adjacent surfaces), pass repeated back-to-back. Also re-ran every other spec found (by grepping the whole suite for `openRequirementByCode`/direct requirement-URL navigation) to plausibly touch this page: `mobile-card-grid-overflow.spec.ts`, `comment-editing-and-attachments.spec.ts`, `project-history.spec.ts`, `workflow-bypass-attempts.spec.ts`, `review-scheduling.spec.ts`, `terminology-coverage.spec.ts` — 9/9 passed. Not run: a full clean-slate whole-suite pass — the specs above cover every file identified as touching the changed cards or this page more broadly.

Files changed: `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/requirement-links.spec.ts`, `requirement-actions.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## `CommentThread.tsx`: swap `X` for `Trash2` on the three attachment-delete buttons

Roadmap item 2 of 6 from the sixth-pass audit — [A style-guide-documented inconsistency, still there](ux-audit-2026-08.md#a-style-guide-documented-inconsistency-still-there). The style guide's own Tokens section had named this exact inconsistency (`X` used for "permanently delete a file" in `CommentThread.tsx` where `Trash2` is used everywhere else, e.g. `FileAttachmentList`) as far back as the fourth pass, but it never had its own roadmap line and was never picked up — closing it out here.

A one-icon-import, three-call-site swap, exactly as scoped: `CommentThread.tsx`'s import switched from `X` to `Trash2` (`lucide-react`, same library, no new dependency), and its three attachment-removal buttons — the posted-attachment remove button, the pending-upload-during-edit remove button, and the pending-upload-during-compose remove button — now render `<Trash2 size={12} />` instead of `<X size={12} />`. Grepped the file first to confirm these were the only three `X` usages present (there was no other `X` — e.g. no dismiss/close control — to accidentally touch or leave behind).

No backend change: these buttons already called the existing `onRemoveAttachment` prop / `DELETE` endpoint unchanged, confirmed via `git status` showing no `backend/` files touched. No new Storybook coverage needed — `CommentThread.stories.tsx` already exercises these buttons via `onRemoveAttachment`/`onUploadAttachment` callback props and asserts on text/title, never on the icon glyph itself, so nothing there was pinned to `X` and nothing needed updating. Same for Playwright: `comment-editing-and-attachments.spec.ts` locates the button via `getByRole("button", { name: "Remove attachment" })` — the shared `removeAttachment` i18n string used as both `title` and `aria-label` — not by icon, so no locator changes were needed either.

Style guide (`docs/ux-style-guide.md`, Tokens section) updated: removed `CommentThread.tsx`/`X` from the "known exceptions worth fixing rather than copying" list (only the report-template Edit-button-with-no-icon exception remains there), with a short note that the `X`/`Trash2` exception was fixed in this pass.

**Verified**: `npx tsc --noEmit` and `npx eslint` on the changed file both clean, no new warnings. Grep confirms zero remaining `X` icon usage in `CommentThread.tsx` and `Trash2` used consistently at all three sites. Rebuilt/recreated the `tests/container` frontend image (no bind-mounted source) and ran the specs that exercise comment-attachment deletion against it: `comment-editing-and-attachments.spec.ts` (author-only edit/attach/remove flow), plus `pelion-v2.spec.ts`, `requirement-actions.spec.ts`, and `report-image-attachment.spec.ts` as adjacent attachment-touching coverage — all passed.

Files changed: `frontend/src/components/CommentThread.tsx`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## `window.confirm` → `ConfirmDialog`, and `Toast` wiring, across the remaining audit-enumerated list

Roadmap item 3 of 6 from the sixth-pass audit — [Confirmation and feedback rollout, precisely](ux-audit-2026-08.md#confirmation-and-feedback-rollout-precisely). Unlike items 1/2, this one was pre-scoped as two flat lists (11 `window.confirm` call sites across 4 files; 11 pages with real mutations and no `useToast`) rather than a single feature — worked through both lists file by file, same piecemeal approach the fourth pass used for its own three pilot sites each, copying the pilot's exact patterns (Tier 1 `ConfirmDialog`, `useToast`/`toErrorMessage`) rather than inventing new ones.

**Part A — `window.confirm` → `ConfirmDialog` (all 11 call sites, all Tier 1)**: `OrgAdminPage.tsx` (PAT descope-all, revoke-all, revoke-one), `ServerManagementPage.tsx` (deactivate, ban, grant/revoke server-admin, PAT revoke-all — consolidated into one `confirmAction: { kind, userId } | null` state plus a `confirmActionCopy` lookup table, rather than four near-identical dialog blocks), `PreferencesPage.tsx` (PAT revoke-all, revoke-one), `ServerOrganisationsPage.tsx` (disable — delete already used `ConfirmDialog` Tier 2 from the fourth pass, untouched). Each conversion follows the archive-button precedent exactly: a `*ToOpen`/`*ToRevoke`-style state variable set by the trigger button, the mutation function stripped of its `if (!window.confirm(...)) return` guard, and one `<ConfirmDialog>` rendered conditionally. `strings.ts` gained a parallel `*Title` key next to each existing `*Confirm` key for every converted site (the pilot's own `archiveTitle`/`archiveConfirm` split) — where the old single string combined a question and its rationale in one sentence, the question moved to the new `*Title` and the rationale stayed as the (now-trimmed) `*Confirm` message; three of `orgAdmin`'s PAT confirm functions lost their now-redundant `org` parameter as part of that split (each was used at exactly one call site, confirmed via grep before changing the signature).

**Part B — `Toast` wiring (all 11 pages)**: `ChangeRequestDetailPage.tsx`, `ChangeRequestsPage.tsx`, `FavouritesPage.tsx`, `NotificationsPage.tsx`, `PreferencesPage.tsx`, `ProjectActionsPage.tsx`, `ProjectListPage.tsx`, `ReportsPage.tsx`, `RequirementsPage.tsx`, `ServerManagementPage.tsx`, `ServerOrganisationsPage.tsx` — every one imports `useToast`/`toErrorMessage` and wires at least one real mutation. Per-mutation judgement (the fourth pass's own reasoning for not sweeping mechanically): a success toast where no other success feedback existed (`RequirementsPage`'s create, `ProjectActionsPage`'s create — both named specifically in the audit as having picked up real interaction-model work without Toast — plus `ChangeRequestsPage`'s create, `ChangeRequestDetailPage`'s submit/withdraw/approve/reject, `ProjectListPage`'s create, `ServerOrganisationsPage`'s create/disable/enable/delete, `NotificationsPage`'s mark-all-read); an error toast (via the shared `toErrorMessage` fallback) where a mutation had literally zero error handling before this pass — several were unhandled promise rejections, not just missing UI: `RequirementsPage`'s move/inline-component/inline-category-create/CSV-import-catch, `FavouritesPage`'s unfavorite, `ProjectListPage`'s toggle-favorite, `ReportsPage`'s `generate()` (a bare `try`/`finally` with no `catch` at all — a failed report generation previously had zero visible feedback), `PreferencesPage`'s revoke-pat/revoke-all-pats/save-notification-prefs, `ServerManagementPage`'s platform-wide PAT revoke-all. `PreferencesPage`'s avatar upload gained a `catch` too, but reports inline (`avatarError`, matching the page's own per-section inline-error convention) rather than via toast, since it sits in a form full of inline error text already.

**Deliberate skips, named explicitly rather than silently dropped** (the "adequate inline UI already" exception): `ServerManagementPage.tsx`'s `PlatformBrandingTab` (save, logo upload, login-background upload), `SignupModeTab` (save), and `TestEmailTab` (send) all already show inline "Saved."/"uploaded"/"Test email sent" success text and inline red error text right next to their own buttons — adding a toast on top would double up the same information rather than close a real gap, so these three sub-tabs were left as they were. `PreferencesPage.tsx`'s profile save, password change, and 2FA enrol/confirm/disable were left alone for the same reason (each already has its own adequate inline success/error path — password change and 2FA-disable both redirect to a re-auth login screen, which is itself the success signal). `OrgAdminPage.tsx`'s three PAT actions (Part A) were *not* given new toasts — that page wasn't on the Part B list (it already imports `useToast` for other mutations), and adding toast coverage to mutations outside the audit's enumerated 22 would have been exactly the mechanical sweep the fourth pass's own reasoning argues against.

**Also fixed while converting, not deferred**: `system.revokeServerAdminConfirm` previously had no elaboration at all ("Revoke server admin from this user?" with nothing else) — `ConfirmDialog`'s message can't be empty, so it now reads "They will lose access to server-wide management immediately," matching every sibling confirm's shape rather than being a bare repeat of the title.

**Playwright**: grepped the whole suite for `on("dialog"`/`once("dialog"` — exactly 4 native-dialog call sites across 3 files, all now converted to the in-app `ConfirmDialog`, same swap `requirement-archive-confirm.spec.ts` made in the fourth pass: `personal-access-tokens.spec.ts` (PAT revoke via Preferences), `user-directory-and-bans.spec.ts` (deactivate, ban — two sites), `server-org-lifecycle.spec.ts` (disable). No spec exercised `grantServerAdmin`/`revokeServerAdmin`/`OrgAdminPage`'s three PAT confirms/`PreferencesPage`'s PAT revoke-all — a pre-existing coverage gap, not something this pass introduced or was asked to close.

**Verified**: `npx tsc -b` and a full-tree `npx eslint src/` both clean — zero errors, the same 5 pre-existing `react-refresh/only-export-components` warnings this doc already tracks (`BrandingContext.tsx`/`ThemeContext.tsx`), no new ones. Grep confirms zero remaining `window.confirm` in the 4 Part-A files and a `useToast` import in all 11 Part-B pages. Storybook: every touched page's existing `.stories.tsx` updated in place — `withToast()` added to each `meta.decorators`, existing stories exercising a converted `window.confirm` site rewritten to drive `ConfirmDialog` (`OrgAdminPage`, `ServerManagementPage`, `ServerOrganisationsPage`), new stories added for sites with no prior coverage (`OrgAdminPage`'s PAT revoke confirm+cancel, `PreferencesPage`'s PAT revoke confirm+cancel, `ServerManagementPage`'s deactivate-cancelled) and for each new toast/error path (`ChangeRequestDetailPage`'s submit/reject toasts, `ChangeRequestsPage`'s create toast, `FavouritesPage`'s unfavorite-error toast, `NotificationsPage`'s mark-all-read toast, `ProjectActionsPage`'s create toast, `ProjectListPage`'s create toast, `ReportsPage`'s generate-failure toast, `RequirementsPage`'s create toast and move-failure toast) — full suite **55 files / 401 tests, all green** (previously 388). Rebuilt/recreated the `tests/container` frontend image. Full clean-slate reseed (`docker compose down -v && up --build -d`, both seed scripts) and one full Playwright suite run: **71/72 passed**, the one failure (`report-image-attachment.spec.ts`) is this doc's own previously-documented pre-existing timing flake (see the mobile-grid entry above and others), confirmed via an isolated rerun to pass cleanly alone — not a regression, and unrelated to any of the 15 files this entry touches. `git status` confirmed no `backend/` files touched throughout. `docs/ux-audit-2026-08.md` roadmap Status updated to "Done," noting the two deliberate-skip groups above.

Files changed: `frontend/src/i18n/strings.ts`; `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `ServerManagementPage.tsx` (+ `.stories.tsx`), `PreferencesPage.tsx` (+ `.stories.tsx`), `ServerOrganisationsPage.tsx` (+ `.stories.tsx`), `ChangeRequestDetailPage.tsx` (+ `.stories.tsx`), `ChangeRequestsPage.tsx` (+ `.stories.tsx`), `FavouritesPage.tsx` (+ `.stories.tsx`), `NotificationsPage.tsx` (+ `.stories.tsx`), `ProjectActionsPage.tsx` (+ `.stories.tsx`), `ProjectListPage.tsx` (+ `.stories.tsx`), `ReportsPage.tsx` (+ `.stories.tsx`), `RequirementsPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/personal-access-tokens.spec.ts`, `user-directory-and-bans.spec.ts`, `server-org-lifecycle.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## `/unarchive` endpoint + "Restore" button for requirements and actions

Roadmap item 4 of 6 from the sixth-pass audit — [Archive is one-way for requirements and actions, not projects](ux-audit-2026-08.md#archive-is-one-way-for-requirements-and-actions-not-projects). `projects.py` already had both `POST /{id}/archive` and `POST /{id}/unarchive`, with a working (toggling) unarchive button on `ProjectAdminPage.tsx`'s settings tab (label "Unarchive {project}", no `ConfirmDialog`); `requirements.py` and `actions.py` had only `/archive`. This pass added the missing counterpart to both, mirroring the project version's shape exactly rather than inventing a new one, and — per the change-management policy's identify → verify → remediate practice for security-sensitive changes (this touches mutating, audit-logged, RBAC-gated endpoints) — reviewed the result below.

**Backend**: `services/requirements.py::unarchive_requirement` (new, sits next to the existing `archive_requirement`) resets `is_archived`/`archived_at`/`archived_by`; `routers/requirements.py::restore_requirement` (`POST /requirements/{id}/unarchive`, named to avoid colliding with the service function of the same underlying verb) uses the exact same permission gate as `delete_requirement` (the requirement archive endpoint) — `PROJECT_MANAGER`/`PROJECT_ADMINISTRATOR` via `get_effective_project_roles` — and the same `log_event`/`pubsub.notify` shape, with `action="unarchived"`. `routers/actions.py::unarchive_action` (`POST /actions/{id}/unarchive`) mirrors `archive_action` just as directly: same `require_project_manage` dependency (project managers/administrators, plus organisation admins via `can_manage_project_settings` — a broader gate than the requirement side's, since that's `archive_action`'s own existing gate too, not something this pass introduced or changed), same `get_requirement_action_in_project` IDOR guard, same audit-log shape. No service-layer function was extracted for the action side since `archive_action` itself has no service-layer counterpart to mirror — inline in the router, matching its own existing shape rather than introducing a new abstraction asymmetrically.

**Idempotency, deliberately copied from `unarchive_project`, not from `archive_action`**: `archive_action` 409s if the action is already archived; `unarchive_project` has no equivalent guard — calling it on an already-active project is a harmless no-op. Both new endpoints copy the *project's* shape: calling `/unarchive` on an item that isn't archived just re-asserts "not archived" and logs `action="unarchived"` again, no error. Pinned by `test_unarchive_requirement_restores_it_and_is_idempotent`/`test_unarchive_action_restores_it_and_is_idempotent`, each calling `/unarchive` twice and asserting `200` both times.

**Frontend**: `RequirementDetailPage.tsx` and `ActionDetailPage.tsx` each gained a "Restore" button (`ArchiveRestore` icon) shown exactly when the existing Archive button isn't (`requirement.is_archived`/`action.is_archived`), plus a plain "Archived" badge next to the heading so the state is visible without inferring it from button presence alone. No `ConfirmDialog` on Restore — checked `ProjectAdminPage.tsx`'s own unarchive button first per the task's instruction to verify rather than assume, and confirmed it fires immediately with no confirmation of any kind; Restore is the same shape (reversible again — archiving it right back costs nothing), unlike Archive itself, which stays Tier-1-confirmed. `RequirementDetailPage.tsx`'s Archive button is now also gated on `!requirement.is_archived` (previously unconditional on `canArchive` alone, a latent gap `ActionDetailPage.tsx` didn't have — its own Archive button was already gated on `!action.is_archived`).

**Found while wiring the frontend, fixed in the same pass (not deferred): `RequirementsPage.tsx` had no way to reach an archived requirement at all.** `list_requirements` already excluded archived rows unless `include_archived=true`, and `ProjectActionsPage.tsx` already exposed that as an "Include archived" filter checkbox with an "Archived" badge per row — but `RequirementsPage.tsx` never sent the parameter and had no such checkbox, so an archived requirement dropped out of the UI's requirements list with no path back to its own detail page (not even a stale link — nothing on the page shows it at all). The new Restore button would have been unreachable dead code for requirements specifically: the whole feature this roadmap item exists to add depends on being able to get to the archived item's page in the first place. Fixed by adding the same `includeArchived` state, `include_archived` query param, `FilterCheckbox`, and per-row "Archived" badge `ProjectActionsPage.tsx` already has (both tile and list view).

**Security review (identify → verify → remediate)**:
- *Authorization*: both endpoints require the same role set their sibling archive endpoint already requires, verified by new `test_unarchive_requirement_requires_manager_or_admin_role`/`test_unarchive_action_requires_manage_role` (a `stakeholder`-only user gets 403 on both). Neither endpoint accepts an `organization_id`/cross-project parameter from the caller; the target's `project_id` is resolved server-side and cross-checked (`requirement.project_id != project_id` 404s; `get_requirement_action_in_project` does the equivalent for actions) — no IDOR surface beyond what `delete_requirement`/`archive_action` already carry.
- *Audit trail*: both endpoints call `log_event` unconditionally before commit, `action="unarchived"`, same `entity_type`/`entity_id`/`actor_id`/`project_id` (and `organization_id` for actions) shape as every sibling mutation in these two routers — no gap versus the existing pattern `services/audit.py` establishes.
- *Noted, not changed*: the requirement side's permission check (`get_effective_project_roles(...) & {PROJECT_MANAGER, PROJECT_ADMINISTRATOR}`, no org-admin bypass) is narrower than the action side's (`require_project_manage`, which does include org admins via `can_manage_project_settings`) and narrower than the project side's own `/archive`/`/unarchive` (also `require_project_manage`). This asymmetry pre-dates this pass — it's `delete_requirement`'s own existing gate, faithfully mirrored rather than widened — and is out of scope for a `/unarchive`-endpoint-addition pass to silently change; flagging it here rather than either fixing it unasked or leaving it undocumented.
- *No new secret, credential, or Restricted-classified field is logged, returned, or stored* — both endpoints return the same `RequirementOut`/`RequirementActionOut` shapes their sibling `GET` endpoints already return.

**Demo data**: neither seed script archived anything before this pass. Added to `seed_demo_data.py` only (not `seed_e2e_dataset.py` — a fixed pre-archived fixture there would risk exactly the non-idempotent-test trap this repo's own testing rule warns about, since a spec that restores it would leave a later run's assumptions wrong; the new Playwright spec below creates and archives its own throwaway fixtures instead, per that same rule): Falcon-3's "Support geofencing with a configurable no-fly boundary" requirement is archived (not restored) after seeding, and the completed "Review remote-ID firmware module against FAA rule text" action is archived after its outcome/comment — both left archived so a manual reviewer has a concrete "Include archived" + "Restore" example to click through, via new `archive_requirement`/`archive_action` script helpers.

**A verification methodology mistake, caught and corrected before trusting any number from it**: the first attempt rebuilt the `tests/container` images (`docker compose build backend frontend`) and then ran `docker compose restart backend` before reseeding and testing — but `restart` only restarts the *existing* container from whatever image it was already created from; it does not pick up a freshly built image the way `up -d --force-recreate` (or a plain `up -d --build`) does. That first backend pytest run (host- and container-invoked alike) and both seed-script runs therefore executed against the pre-session backend image, missing this pass's endpoints, service function, and tests entirely — confirmed after the fact by `docker compose exec backend grep`ping for `unarchive_requirement` and the new seed-script print line and finding neither, and by the demo dataset coming back with zero archived items despite the script supposedly archiving two. Corrected by a full clean-slate reset (`docker compose down -v && up -d --build`) rather than patching around it, then redoing every verification step below against the confirmed-current containers — the same "don't trust a number from a run whose preconditions turned out to be wrong" standard this doc's other entries hold Playwright reruns to.

**Verified** (all against the clean-reset stack): backend — full suite inside the backend container, per `backend/tests/conftest.py`'s convention — **553 passed (549 pre-existing + 4 new), 0 failed**, 0 regressions (running from the host instead reproduces 7 unrelated `SMTPConnectError` failures in `test_invites_and_external_users.py`/`test_oidc_provisioning.py`/`test_org_export_import.py`, traced to `mailhog`'s hostname only resolving inside the Compose network — confirmed environmental, not a regression, by re-running just those files inside the container where all 51 passed). `ruff check` clean on `app/`, `tests/`, and both seed scripts. Frontend: `npx tsc -b` and `npx eslint src/` both clean (same 5 pre-existing `react-refresh/only-export-components` warnings this doc already tracks, no new ones). `npx storybook build` succeeded; all 4 new/extended story entries (`RequirementDetailPage`'s `ArchivedRequirementShowsRestoreButton`, `ActionDetailPage`'s extended `ArchivedActionIsReadOnly` plus new `RestoreActionShowsToast`, `RequirementsPage`'s `IncludeArchivedFilterShowsArchivedRequirement`) present in the built `index.json`. Both seed scripts re-run end to end with no errors, this time confirmed via direct API check to actually leave the intended archived fixtures behind (`GET .../requirements?include_archived=true` and `.../actions?include_archived=true` both return the expected archived item). New Playwright spec `requirement-and-action-restore.spec.ts` found and fixed one real bug in itself during this process: its "Archived badge" assertion used a bare `.card, tr` selector which, in list view, also matches the outer `<div className="card">` wrapping the *entire* table (not just one row), so under the full suite's parallel workers it collided with `requirement-archive-confirm.spec.ts`'s own leftover archived fixture and failed a strict-mode check — fixed by scoping through the same xpath-nearest-row-or-card-ancestor pattern `helpers.ts::openRequirementByCode` already uses, then re-verified with `--workers=2 --repeat-each=2` alongside `requirement-archive-confirm.spec.ts` specifically to reproduce the original collision scenario. Full Playwright suite, one clean run against the reset-and-reseeded stack: **74/74 passed**, including the new spec and the four pre-existing archive/filter-adjacent specs (`requirement-archive-confirm.spec.ts`, `requirement-actions.spec.ts`, `requirements-and-cr-filters.spec.ts`, `requirement-links.spec.ts`) confirming the Archive-button gating change (`!requirement.is_archived`) and the new `RequirementsPage.tsx` filter didn't regress anything nearby. `docs/solution-architecture.md` left unchanged: checked how the project archive/unarchive pair was documented there (it isn't — no endpoint-level documentation exists for either), so no architecturally-significant change to record.

Files changed: `backend/app/services/requirements.py`, `backend/app/routers/requirements.py`, `backend/app/routers/actions.py`; `backend/tests/test_requirement_lifecycle.py`, `backend/tests/test_requirement_actions.py`; `backend/scripts/seed_demo_data.py`; `frontend/src/i18n/strings.ts`, `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ActionDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/requirement-and-action-restore.spec.ts` (new); `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Bulk operations on `RequirementsPage.tsx`'s list view: checkbox column, contextual toolbar, no new backend endpoint

Roadmap item 5 of 6 from the sixth-pass audit — [Bulk operations: none exist](ux-audit-2026-08.md#bulk-operations-none-exist). Unlike the other five items in that pass, this one wasn't a straight build: the audit named it explicitly as having no existing pattern anywhere in the app to copy and needing a design decision first, rather than piecemeal conversion of an already-established shape (the way `ConfirmDialog`/`Toast`/`Popover` rollouts in items 1–4 all were). The design was made up front (checkbox column + header "select all loaded" + contextual toolbar, reusing `ConfirmDialog`/`Toast`/`Popover` rather than inventing bulk-specific versions of any of them) and is now also written up as `docs/ux-style-guide.md`'s new "Pattern: bulk operations on a list," the same way `FilterBadge`/`SidePanel`-for-viewing were documented when *they* were introduced — so a second page adopting this pattern later has a reference shape rather than the nearest inconsistent example to copy.

**Scope, deliberately narrow**: `RequirementsPage.tsx`'s table/list view only — not the tile view (checkboxes only render there; the audit itself framed this as "most naturally table-shaped"), and not any other list page in the app. This is a first pilot, not a whole-app sweep; the style guide section above exists precisely so a second page can adopt the same shape later without re-deriving it, rather than this pass trying to roll it out everywhere at once.

**Selection**: `selectedIds: Set<string>`, plus a header checkbox whose `indeterminate` state is set imperatively via a ref (not a settable React prop). "Select all" means all *currently loaded* rows, never "everything matching the filter" — `RequirementsPage` already paginates via `LoadMoreButton` rather than loading a project's full requirement set up front, so promising "every row matching the filter, including ones not yet loaded" from a single checkbox would be misleading; selecting more just means loading more first. Selection survives a "load more" append (the Set is keyed by id, not array position) but is cleared inside `reload()` — i.e. on any filter/search change, and after a bulk action completes — since a fresh fetch can drop previously-selected rows from the result set entirely.

**Toolbar**: renders directly above the table once `selectedIds.size > 0` (list view only), gated on `canManageProject` — the same PM/PA-or-org-admin gate `RequirementDetailPage.tsx`'s own single-row Archive button already requires. Both bulk actions are administrative in the same way archive already is, and gating the whole checkbox column + toolbar on one boolean avoids a "you can select rows but neither action is available" dead end for a plain stakeholder/member.

**Two bulk actions, both looping over existing single-row endpoints — no new backend endpoint**:
- **Archive selected** loops `DELETE /projects/{id}/requirements/{id}`, the same endpoint the single-row Archive button already calls.
- **Move to stage** opens a `Popover` (stage `<select>`, sourced from the same `stages` state the create form's own "Target version" field already uses) then loops `PUT /projects/{id}/requirements/{id}` — the same direct-edit endpoint `RequirementDetailPage.tsx`'s `save()` already calls, and its full `RequirementUpdate` payload shape, not a new partial-patch endpoint. Every field on that payload except `target_stage_id` carries the row's own current value forward unchanged (`RequirementsPage` already has every field it needs from the list response — `RequirementOut`/list-endpoint shape matches the detail endpoint's exactly), so the only effective change per row is the stage.

Both loops run sequentially (simpler than bounded concurrency, and fine for the list sizes a first pilot targets) and tolerate individual row failures rather than aborting the batch — one locked or already-archived row shouldn't block the rest. This was a genuine design choice, not a default: a new bulk endpoint would let the backend do this in one transaction, but a first pilot on one page didn't justify a new endpoint plus its own authorization/audit-logging review (see the change-management policy note below) when the existing single-row endpoints, already correctly gated and already audit-logging every mutation via `log_event`, do the job. Revisiting that trade-off is the natural trigger for a second page adopting this pattern with materially larger expected selections.

**Confirmation and feedback**: both actions confirm via the same `ConfirmDialog` (Tier 1, no typed text) the single-row Archive button already uses — copy pluralised and count-bearing (`archiveTitle`'s "Archive this {requirement}?" becomes `bulkArchiveTitle`'s "Archive N {requirements}?"), same tier, since a bulk archive/move is exactly as reversible per row as its single-row equivalent (archive un-archives; a stage move can be moved again) — the count changes the wording, not the tier. After the loop completes (success or partial failure), a summary `Toast` reports `"N updated"` or `"N updated, M failed"`, phrasing modelled directly on `CsvImportWizard.tsx`'s existing "N created, M error(s)" import-summary card, adapted from a persistent card to the transient shared `Toast`. Selection is cleared and the list reloaded regardless of outcome.

**Accessibility**: every row checkbox has its own `aria-label` (`"Select {unique_code} {name}"`) and the header checkbox is labelled `"Select all"` — direct `aria-label` on the `<input>`, the same convention `RequirementsPage.tsx`'s existing reorder buttons already use (the audit's own named-good accessibility example on this page), rather than a wrapping `<label>` element.

**New strings** (`frontend/src/i18n/strings.ts`, `requirements` namespace): `bulkSelectAll`, `bulkSelectRow`, `bulkSelectedCount`, `bulkClearSelection`, `bulkArchiveSelected`, `bulkArchiveTitle`, `bulkArchiveConfirm`, `bulkMoveToStage`, `bulkMoveApply`, `bulkMoveTitle`, `bulkMoveConfirm`, `bulkResultSuccess`, `bulkResultPartial` — the `bulk*Title`/`bulk*Confirm` pairs use the same `{requirement}`/`{requirements}`/`{stage}` terminology tokens every other string on this page already does, so a project with terminology overrides (C-C-03) sees correctly-substituted bulk-action copy too, not just the single-row copy.

**Confirmed no backend change was needed** before starting, per this task's own instruction to verify rather than assume: `RequirementOut` (the list endpoint's response shape) already carries every field `RequirementUpdate` (the `PUT` payload) requires — `name`, `reasoning`, `clarification`, `description`, `component_id`, `category_id`, `owner_id`, `level`, `keywords`, `custom_fields`, `review_date`, `review_lead_days`, `reviewer_id` — so the move-to-stage loop never needs a second round-trip per row to fetch fields the list response doesn't already have.

**Security review (change-management policy's identify → verify → remediate practice, since this touches mutating, audit-logged, RBAC-gated endpoints)**: no *new* authorization surface — both loops call endpoints whose existing role gates (`delete_requirement`: PM/PA via `get_effective_project_roles`; `update_requirement`: any of the three `CAN_EDIT_ROLES`, further narrowed here by the frontend's own `canManageProject` gate on the toolbar) are unchanged and already reviewed when those endpoints were built (`/unarchive` entry above; the original requirement-update endpoint predates this audit). No new field is logged or returned — the `PUT` loop's `change_note` is the literal string `"Bulk move to stage."`, not user-supplied free text, and the archive loop calls the existing endpoint with no body at all, so no new Restricted-classified value enters any log line. Each row's own `log_event` call (already present in both existing endpoints, unconditional before commit) fires once per row exactly as it would for N separate single-row actions — a bulk action doesn't collapse N audit-log entries into one, which is intentional: the audit trail should show what actually happened row by row, not a single opaque "bulk operation" entry.

**Found and fixed along the way, not deferred**: writing the new Playwright spec surfaced that Alpha-1's default `PAGE_SIZE=30` requirement list, after several early iterations of the spec left behind ~15 leftover throwaway "E2E Bulk Req …"/"E2E Bulk Target …" fixtures from debugging the search-box/header-checkbox timing issue below, pushed the project past 30 requirements — which then broke `requirement-and-action-restore.spec.ts`'s own throwaway item off the first page ("Load more (30/39)" in the failure trace), since that spec's own `getByText`/`getByRole("link")` assertions never call `LoadMoreButton`. This wasn't a regression in either page's code — a full clean-slate `docker compose down -v && up -d --build` plus both seed scripts reset it — but it's recorded here as a caution for the next agent debugging a spec iteratively against this same long-lived stack: a failed spec run's own dynamically-named fixtures accumulate exactly like a passing one's do, and enough of them from repeated debugging can push an unrelated spec past a page-size boundary it was implicitly relying on staying under.

**A real, pre-existing race hit while writing the spec, not caused by this change**: `RequirementsPage.tsx`'s fetch effect has no request-cancellation guard (already known — `requirements-and-cr-filters.spec.ts`'s own docstring names it, and its own `settled()` helper works around it). The first version of the new e2e spec typed a search-box filter immediately after a bulk-archive step's own trailing `reload()`, and the archive step's own unfiltered response landed *after* the newly-filtered one and silently overwrote it. Worked around by selecting the second batch's two rows via their own checkboxes (the same approach the archive step already uses) instead of the search box + header "Select all" combination, sidestepping the race entirely rather than adding another `settled()`-style wait — the header checkbox's "select all *loaded* rows" behaviour already has its own dedicated Storybook coverage (`SelectAllHeaderCheckbox`), so the e2e spec didn't need to re-prove it under a race-prone path.

**A real cross-spec regression, found via a full-suite run and fixed, not deferred**: the new spec's throwaway project stage (created via Project Admin so a "move to stage" target exists — a fresh project only seeds one "Scoping" stage) was left behind uncleaned after the first version of the spec finished. A full clean-slate suite run surfaced the consequence: `workflow-bypass-attempts.spec.ts` — unrelated to this feature, on the same project — failed with a strict-mode violation on `getByRole("button", { name: "Start review" })` resolving to two elements, because that spec assumes exactly one "scoping"-status stage on Alpha-1. A project stage isn't a safe fixture to leave behind the way the four dynamically-named throwaway *requirements* in the same spec are (the precedent `project-admin-groups-and-fields.spec.ts`'s own custom fields already established as tolerable) — it's a small, shared, enumerable resource other specs query by role/count, not by name. Fixed by adding a final cleanup step: after the move-to-stage step, the spec deletes its own stage via Project Admin's existing delete-with-reassign flow (`DELETE .../stages/{id}?reassign_to=<Scoping's id>`), moving its two fixture requirements back to Scoping first. Re-verified: the bulk-operations spec twice back-to-back (with cleanup, still idempotent), then `workflow-bypass-attempts.spec.ts` run immediately afterward against the same database — passes, where it failed before the fix.

**Verified**: no backend files touched — confirmed via `git status` before starting, and again after finishing frontend work, since the design spec called for exactly this (existing single-row endpoints, unchanged). `npx tsc -b` and a full-tree `npx eslint .` both clean — zero errors, the same 5 pre-existing `react-refresh/only-export-components` warnings this doc already tracks, no new ones. Full Storybook suite: **55 files / 410 tests, all green** (401 after item 3's own pass, 404 after item 4's — this pass adds 6 new stories on `RequirementsPage.stories.tsx`: `BulkSelectShowsToolbar`, `SelectAllHeaderCheckbox`, `MemberSeesNoBulkControls`, `BulkArchiveConfirmAndToast`, `BulkMoveToStageConfirmAndToast`, `BulkArchivePartialFailureShowsToast` — 404 + 6 = 410; that file's `stages` fixture also gained a second entry so the move-to-stage stories have somewhere real to move to, and the file now has 19 stories total). Rebuilt/recreated the `tests/container` frontend image (no bind-mounted source) — actually a full `docker compose down -v && up -d --build` plus both seed scripts, per the "found and fixed along the way" note above; backend was rebuilt too as part of that reset even though unchanged, since it was the simplest way back to a known-clean stack rather than reasoning about which of several already-modified-and-rebuilt-earlier-this-session containers still matched their images. New spec `requirements-bulk-operations.spec.ts` covers the golden path for both bulk actions against Alpha-1 (creates its own throwaway project stage and four throwaway requirements, all timestamped — no seeded/shared data mutated) and passed standalone, repeated back-to-back (`--repeat-each=2`), including its cleanup step. Full Playwright suite, one clean run against the reset-and-reseeded stack: **74/75 passed**. The one failure, `project-admin-templates-and-reports.spec.ts` (Gamma-1's report-template default select racing under full-suite load), is unrelated to this change — it touches neither `RequirementsPage.tsx` nor any file this pass modified, is one of this doc's own already-documented pre-existing flakes (see the fourth-pass entry above: "two of the seven \[…\] are already-documented pre-existing flakes"), and was confirmed to pass standalone on rerun. A first full run (before the fix below) also caught a real self-inflicted regression, described next; the 74/75 number above is from the *second*, corrected full run, after that fix — not the first.

Files changed: `frontend/src/i18n/strings.ts`, `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/requirements-bulk-operations.spec.ts` (new); `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Column-header sorting on data tables

Roadmap item 6 of 6, the last item from the sixth-pass audit — [Column-header sorting on data tables](ux-audit-2026-08.md#table-sorting-manual-reorder-only). The audit deliberately left the backend-vs-client-side scope decision open ("depends on how large these lists are actually expected to get in practice"), to be settled once the pagination work above (Directories at scale, two unbounded lists, plus the Org Admin/Project Admin directories item) had shipped and made clear which tables actually page.

**New shared component, not four hand-rolled sorts**: `frontend/src/components/SortableHeader.tsx` — a `<th>` containing a real `<button>` (keyboard-operable, not a bare `<th onClick>`), cycling unsorted → ascending → descending → unsorted, with `aria-sort` on the `<th>` itself rather than an `aria-label` on the button. That last point is deliberate, not cosmetic: an `aria-label` on the button would substitute for the button's own text in the `<th>`'s computed accessible name (accessible-name-from-content rules), silently renaming the columnheader from e.g. "ID" to "Sort by ID" for every screen-reader user and every `getByRole("columnheader", { name })` query in the existing test suite alike — caught by `RequirementsPage.stories.tsx`'s pre-existing `ListView` story, which asserts `getByRole("columnheader", { name: "ID" })`, before it ever reached Playwright. Matches the WAI-ARIA APG "sortable table" pattern rather than inventing a bespoke one. A plain `cycleSort()` helper is exported alongside it so every page's click handler shares the exact same state-transition logic. New `.th-sort` CSS in `theme.css` (button fills the `<th>`'s own padding so a sortable header lines up with a plain-text one beside it). Documented as new style guide "Pattern: sortable column header," including the backend-vs-client-side decision tree below, per Principle 4 ("one component per pattern") — the audit named this risk explicitly (four independent implementations would repeat the exact "no shared component" mistake already found in this app's three tab bars).

**Per-table backend-vs-client-side split, decided against actual current code, not the audit's fourth-pass description (which had drifted — five other roadmap items shipped earlier this same session)**:

| Table | Loads via | Sort location |
|---|---|---|
| `RequirementsPage` (`routers/requirements.py::list_requirements`) | `limit`/`offset` + `LoadMoreButton`, `PAGE_SIZE=30` | Backend `sort`/`order` |
| `ChangeRequestsPage` (`routers/change_requests.py::list_change_requests`) | Same, `PAGE_SIZE=30` | Backend `sort`/`order` |
| Org Admin's Users table (`routers/orgs.py::list_org_users`) | Same, `USERS_PAGE_SIZE=30` (shipped in the "Search + pagination for Org Admin's Users and Groups" item above) | Backend `sort`/`order` |
| `ProjectActionsPage` (`routers/actions.py::list_actions`) | Always `db.scalars(query...).all()` — no `limit`/`offset` at all, never has | Client-side `useMemo` |
| Org Admin's Groups section / Project Admin's Groups tab | `limit`/`offset` + `LoadMoreButton` (same earlier item) | **Neither — out of scope** |

The last row is a real finding, not an oversight: both render each group behind its own `CollapsibleSection` card (name, resolved member/nested-group lists, an "add member" row) — an accordion-of-cards, not a `<th>`-based table. There are no column headers to make sortable; "column-header sorting" doesn't structurally apply, regardless of the fact that both lists are otherwise exactly the kind of backend-paginated directory the rule above would route to a backend param. Named explicitly here rather than silently skipped, since the task brief flagged both as needing verification.

**Backend**: `sort`/`order` (not an existing convention to match — grepped the whole `routers/` tree first, found no prior partial-sort support anywhere) added as optional query params, default `None`/`"asc"`, to the three endpoints above. Omitting `sort` leaves each endpoint's existing default ordering completely unchanged — `list_requirements` still applies its component/category/`sort_order` sort (C-G-04) by default, `list_change_requests` still returns query order, `list_org_users` still sorts by `display_name` — so this is additive, matching the same "optional, non-breaking" contract `limit`/`offset` already established. Allowed `sort` values are constrained via a `Query(..., pattern=...)` regex per endpoint (422 on an unknown field) rather than trusting arbitrary client input as a `getattr()` key: `unique_code`/`name`/`status`/`created_at` for requirements, `proposed_name`/`status`/`created_at` for change requests, `display_name`/`email`/`last_login_at` for org users. Two nullability edge cases handled explicitly and documented on the endpoint docstrings: `ChangeRequestOut.proposed_name` is nullable (a MODIFY_REQUIREMENT change request that didn't touch the name — the list view falls back to the linked requirement's own name for display via `crTitle()`, which the backend sort can't see without an extra join) — null rows sort as empty string, a documented, acceptable approximation rather than a join-per-row fix; `OrgUserOut.last_login_at` is nullable (never logged in) — those rows always sort last regardless of `order`, so "sort by last login, descending" surfaces the most-recently-active users first rather than "never logged in" accounts jumping to the top.

**Frontend**: `RequirementsPage.tsx`/`ChangeRequestsPage.tsx`/`OrgAdminPage.tsx` each gained a `SortState` (typed to that page's own allowed sort keys), added `sort`/`order` to their existing `listParams()`/query-builder function, and added `sort` to the `useEffect` dependency array that already triggers `reload()` on filter/search changes — a header click reuses the exact same refetch-from-offset-0 path a filter change already takes, no new plumbing. `ProjectActionsPage.tsx` (no backend pagination) instead sorts the already-loaded array in a `useMemo`, with a small per-column comparator (`due_date` nulls sort last, same reasoning as the backend's `last_login_at` handling). Sortable columns, per the audit's own "obvious ones only" guidance: `RequirementsPage` — ID, Name, Status (Target·level and Component·category left alone, no natural single-column order); `ChangeRequestsPage` — Name, Status, Created (Target/Level left alone); `ProjectActionsPage` — ID, Title, Outcome, Due date (Type and Assignee left alone); Org Admin Users — Email, Name, Last login (Roles and 2FA left alone — badge-only/boolean columns with no natural order, matching the audit's own example).

**A real accessible-name interaction bug found and fixed before it ever reached Playwright**: caught entirely by the pre-existing Storybook `ListView` story on `RequirementsPage.stories.tsx` (`getByRole("columnheader", { name: "ID" })`), which started failing the moment `SortableHeader`'s button carried its own `aria-label` — see the accessible-name-from-content explanation above. Fixed by dropping the `aria-label` entirely (the WAI-ARIA APG pattern doesn't use one) rather than patching the story's query, since the story's assertion was correct and the component's first draft wasn't.

**A second interaction bug, this time in a new spec, not the app**: `requirements-and-cr-filters.spec.ts`'s new "sort by name" step for change requests initially compared row order immediately after `aria-sort` flipped to the new value. `aria-sort` updates synchronously on the click (a plain React state change), *before* the header-triggered refetch's response has actually landed — so the row-order check could read the *previous* sort's data. Fixed by waiting on the actual network response (`page.waitForResponse` matching `sort=...&order=...`) before asserting order, and wrapping the order check itself in `expect(...).toPass()` so it retries rather than reading the DOM exactly once right after the response promise resolves (a render tick still has to happen). The same fix was applied to the pre-existing Requirements ID-sort step for consistency, even though it happened to pass without it. `ProjectActionsPage`'s client-side sort has no such race (the `useMemo` recomputes synchronously in the same render as the state update), so its Playwright step didn't need this treatment.

**A locator collision found and fixed**: the new Change Requests spec's `createChangeRequest()` helper clicked `getByRole("button", { name: "Create" })` (no `exact: true`) to submit the create form — which, once the new "Created" sortable column header existed on the same page, ambiguously substring-matched both the submit button and the header's button, throwing a strict-mode violation. Fixed by adding `exact: true`. Grepped the whole suite for other non-exact `"Create"` button locators reachable from `ChangeRequestsPage`; none of the others found (`change-request-approval-separation.spec.ts`, `change-request-tasks-and-voting.spec.ts`, `workflow-bypass-attempts.spec.ts`) were affected — all three already used `exact: true`.

**Verified**: Backend — 3 new tests in `backend/tests/test_sorting.py` (`test_requirements_list_sorts_by_name_and_status_both_directions`, `test_change_requests_list_sorts_by_proposed_name`, `test_org_users_list_sorts_by_email`), each confirming ascending, descending, the unfiltered default order is unchanged when `sort` is omitted, and a 422 on an invalid `sort` value. `ruff check` clean. Full backend suite run twice: once from the host (556 total, 7 failed — the same `test_invites_and_external_users.py`/`test_oidc_provisioning.py`/`test_org_export_import.py` `SMTPConnectError`s this doc already documents as a host-vs-container `mailhog`-hostname artifact, confirmed non-regressions since they're the same named tests), then for real inside the rebuilt `tests/container` backend image (`docker compose exec backend python3 -m pytest tests/ -q`): **556 passed, 0 failed**. `npx tsc -b` and a full-tree `npx eslint .` both clean — the same pre-existing `react-refresh/only-export-components` warnings this doc already tracks (including a new one on `SortableHeader.tsx` itself, from co-exporting `cycleSort` alongside the component — the same accepted pattern `BrandingContext.tsx`/`ThemeContext.tsx` already use), no new warning classes. Full Storybook suite: **56 files / 421 tests, all green**, including new `SortableHeader.stories.tsx` (7 stories: unsorted, ascending/descending/back-to-unsorted after 1/2/3 clicks, a second column active, light/dark) and one new sorted-state story per table (`RequirementsPage`'s `SortByNameRefetchesWithSortParams`, `ChangeRequestsPage`'s `SortByCreatedRefetchesWithSortParams`, `ProjectActionsPage`'s `SortedByTitleAscending`, `OrgAdminPage`'s `UsersSectionSortByEmail`).

Rebuilt/recreated both `tests/container` backend and frontend images. Full clean-slate reset (`docker compose down -v && up -d --build`, both seed scripts) before Playwright verification, per this doc's own established methodology ("the only valid verification is a full clean-slate reseed followed by exactly one suite run") — a first attempt at verification ran a 26-spec targeted regression batch (all passed) immediately followed by a full-suite run *without* reseeding between them, which surfaced 6 failures across specs this pass never touched (`csv-import-wizard.spec.ts`, `org-login-2fa-handoff.spec.ts`, `project-admin-action-types.spec.ts`, `two-factor-auth.spec.ts`, `user-directory-and-bans.spec.ts`, plus `requirements-and-cr-filters.spec.ts` itself on an unrelated pre-existing step) — all six vanished on a genuinely clean single run, confirming they were an artifact of running two suites back-to-back against a database the first run had already mutated (e.g. `project-admin-action-types.spec.ts` renaming/deleting its own "Review"/"Test" seed fixtures on its first pass, then asserting they still exist on an unreseeded second pass — exactly the non-idempotent-test failure mode this repo's own convention warns about, here self-inflicted by the verification process rather than a bug in any spec), not a regression from this change. **Final clean run: 78/78 passed**, including the 4 new/extended sort specs (`requirements-and-cr-filters.spec.ts`'s two sort steps/describe block, new `project-actions-list-sorting.spec.ts`, `user-access-panel.spec.ts`'s new sort describe block) and the full pre-existing suite — no regression in any of the items 1-5 surfaces (bulk-select checkboxes, reorder buttons, archive/restore filtering, `useToast` wiring) from adding a sortable header row on top of them.

Files changed: `backend/app/routers/requirements.py`, `backend/app/routers/change_requests.py`, `backend/app/routers/orgs.py`; `backend/tests/test_sorting.py` (new); `frontend/src/components/SortableHeader.tsx` (new, + `.stories.tsx`), `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ChangeRequestsPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ProjectActionsPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/styles/theme.css`; `tests/playwright/tests/e2e-workflows/requirements-and-cr-filters.spec.ts`, `project-actions-list-sorting.spec.ts` (new), `user-access-panel.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

This closes the sixth-pass audit's roadmap in full — every item in `docs/ux-audit-2026-08.md`'s Roadmap table now reads "Done" except three pre-existing, independently-scoped "Partial" rows (`ConfirmDialog`/`Toast`'s broader rollout beyond their original pilot call sites, and the "definition-table CRUD" component's deliberate Stages/Components-tree exclusion) — checked against the task's own instruction to look for stale "N items remain" framing now that this was the last open item, and found none: the doc has no running open-item counter to update, and those three Partial rows are honest, independently-documented partial states unrelated to sorting, not something this pass's completion invalidates.

## File upload triggers: one shared component

Roadmap item — [Extract a shared file-upload-trigger component from `CommentThread.tsx`'s own duplicated inline pattern; convert the other 8–11 file inputs onto it](ux-audit-2026-08.md#file-upload-triggers-three-different-visual-treatments), user-requested directly after confirming this was the right roadmap item to pick up.

**New shared component**: `frontend/src/components/FileUploadTrigger.tsx` — a `.btn`-styled `<label>` wrapping a hidden `<input type="file">`, extracted from `CommentThread.tsx`'s own hand-rolled version of exactly this shape. `onSelect(file)` fires on selection and the input's value is reset immediately after, so choosing the same filename twice in a row still fires a change event. Supports a forwarded `ref` to the underlying input and a `showTrigger={false}` mode that mounts the input without a visible label — both needed by `CsvImportWizard`'s existing `openFilePicker()` imperative handle, used when `RequirementsPage`'s split-button trigger opens the picker itself rather than `CsvImportWizard` rendering its own visible "Import CSV" button a second time on the same screen. Documented as new style guide `Pattern: file-upload trigger`.

**Converted all 11 call sites the finding named, not just the 8 Pattern B ones**: the finding's own text ("extract `CommentThread.tsx`'s own inline shape into a shared component ... closing all three treatments to one") called for converting Pattern A's two existing "good" call sites too, not leaving them as a second hand-rolled copy of the now-shared shape:

- **Pattern A → shared component**: `CommentThread.tsx`'s two attach-file triggers (edit and compose), `CsvImportWizard.tsx`'s own separate hand-rolled copy (its `fileInputRef`/`showImportTrigger` plumbing is now the component's own `ref`/`showTrigger` props).
- **Pattern B → shared component** (8 call sites, previously a bare unstyled native file input): `FileAttachmentList.tsx` (requirement/action attachments), `RichTextEditor.tsx` (report image insert), `PreferencesPage.tsx` (avatar), `ServerManagementPage.tsx` (platform logo, platform login background), `OrgAdminPage.tsx` (shared resources, org logo, org login background). Several of these previously wrapped both a caption *and* the input in one implicit `<label>` — nesting the new component's own internal `<label>` inside that would be invalid HTML, so those were restructured to an explicit `<label htmlFor>` caption (or a plain `<span>`/`<div>` wrapper where no caption existed) sitting beside the new component, matching the pattern `OrgAdminPage.tsx`'s org-logo/login-background fields already used. `#org-logo-input`/`#org-login-background-input` ids were preserved exactly (an existing Playwright spec locates them directly by id) by passing `id` through to `FileUploadTrigger`. Three now-unused input refs (`logoInputRef`/`resourceInputRef`/`loginBackgroundInputRef` in `OrgAdminPage.tsx`, `logoInputRef` in `ServerManagementPage.tsx`) were dead even before this change — grepped for any `.current` read and found none — and were deleted rather than left pointing at a ref prop the new component doesn't need for these call sites.
- **Pattern C — left unchanged, an explicit call, not an oversight**: the finding flagged this as "worth a quick, explicit call" rather than assuming every non-A call site converts the same way. `ServerOrganisationsPage.tsx`, `OrgAdminPage.tsx`'s import/merge section, and `ProjectListPage.tsx`'s bundle-import forms all pair the `.zip` file field with a visible name/text field and a separate submit step ("Preview"/"Create") — genuinely a form field to fill in before submitting, not a one-click trigger that acts immediately the way every other converted call site is. Converting these to a hidden-input-behind-a-button shape would also drop the browser's own "which file is currently selected" affordance, which these three forms rely on as visible state between selecting a file and clicking submit. Left as `.input`-classed native file inputs.

**Found and fixed along the way, not deferred**:
- `CommentThread.tsx`'s two attach-file triggers had `title` but no `aria-label` — an icon-only control with no accessible name beyond a hover tooltip (Principle 8). Both now carry the same string as both `title` and `aria-label`.
- `csv-import-wizard.spec.ts`'s first test asserted `getByText(new RegExp("E2E CSV Import Row One"))` — an unanchored substring that a second run's own leftover row (from an earlier session against this same long-lived dev stack) could also match, throwing a strict-mode violation on 2 matches rather than 1. The row's own already-`Date.now()`-suffixed title is now captured in a variable and asserted on exactly (`{ exact: true }`), so the test is idempotent regardless of what a previous run left behind.
- `project-admin-groups-and-fields.spec.ts` creates a `required: true` list-type custom field (`priorityField`) on Beta-2, a shared, persistent seeded project, to exercise the "Required" checkbox — and never deleted it. Left behind, it permanently blocks *any other* spec's requirement-creation calls on that project (UI or direct API) that don't happen to supply a value for it, since Beta-2 is reused by several other specs. Reproduced directly: `csv-import-wizard.spec.ts`'s second test failed with `400 'Priority <ts>' is required.` on a plain `POST .../requirements` call that has nothing to do with custom fields. Fixed by deleting `priorityField` in the same "delete a custom field" step that already deletes `safetyCriticalField` via the UI. The stray field already present in the live dev stack from an earlier run was deleted directly via the API to unblock verification immediately.

**Verified**: `npx tsc --noEmit` and a full-tree `npx eslint .` both clean — the same pre-existing 6 `react-refresh/only-export-components` warnings this doc already tracks, no new ones. `npm run build` succeeds. Full Storybook suite: **57 files / 427 tests, all green**, including new `FileUploadTrigger.stories.tsx` (6 stories: icon+text rendering, selecting a file calls `onSelect` and resets the input's value, `disabled`, `showTrigger={false}` mounts the input without a visible label, light/dark) and an extended `FileAttachmentList.stories.tsx` (`WithFilesAndUpload` now also exercises an actual file upload via `userEvent.upload`).

Playwright: after a full clean-slate reset (`docker compose down -v && up -d --build`, both seed scripts — the previously-running dev stack had accumulated several hours of prior sessions' leftover test data, which surfaced as 11 unrelated failures — 2FA, stage review, project statuses, user bans, none in any file this change touched — on a first un-reset full run; all 11 vanished on a genuinely clean run, confirming they were pre-existing pollution, not a regression), **77/78 passed** in one clean run. The one failure, `report-image-attachment.spec.ts` (its "generate a PDF report" step, asserting the downloaded PDF's bytes contain `/Subtype /Image`), was investigated rather than dismissed since it's the one spec directly exercising a file this change modified (`RichTextEditor.tsx`'s image-insert trigger): isolated reruns showed a genuine, pre-existing intermittent flake (2/5 and 1/3 failures across repeated isolated runs) in the PDF-image-embedding pipeline itself, not in the upload/insert step — and, decisively, `git stash`-ing this change entirely and rebuilding the frontend against the original, unmodified code reproduced the identical flake at the identical assertion (1/3 failed the same way). This confirms the flake predates and is independent of this change; it was not investigated further or fixed here, since root-causing it is a backend/report-generation timing question unrelated to file-upload button styling — flagged here as a real, reproducible, ~30-40%-failure-rate pre-existing issue for a future session, not silently left unmentioned. The dev stack was reset and reseeded a second time after this investigation (which itself left throwaway orgs/projects/nested-group state behind from repeated isolated reruns) to hand back a clean stack.

Files changed: `frontend/src/components/FileUploadTrigger.tsx` (new, + `.stories.tsx`), `frontend/src/components/CommentThread.tsx`, `frontend/src/components/CsvImportWizard.tsx`, `frontend/src/components/FileAttachmentList.tsx` (+ `.stories.tsx`), `frontend/src/components/RichTextEditor.tsx`, `frontend/src/pages/PreferencesPage.tsx`, `frontend/src/pages/ServerManagementPage.tsx`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/csv-import-wizard.spec.ts`, `project-admin-groups-and-fields.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Change Requests active-only default, raw-enum label-map fixes, OpenAPI version constant, and `Pattern: FilterPanel`

Four roadmap items from the seventh pass's audit-only findings ([docs/ux-audit-2026-08.md](ux-audit-2026-08.md) roadmap rows ~502–504 and 527), picked up in one session since three are small and independently scoped and the fourth is docs-only.

**1. Default Change Requests to an active-only status filter.** `ChangeRequestsPage.tsx:107`'s `statusFilter` previously initialised to `""` (every status, including `approved`/`rejected`/`withdrawn`, loaded by default) — see [Change Request and Requirement status filters](ux-audit-2026-08.md#change-request-and-requirement-status-filters-show-raw-enum-values). The list is backend-paginated (`PAGE_SIZE=30`/`LoadMoreButton`), so a client-side-only default filter would have silently broken pagination — it would filter only the currently-loaded page, not the true result set — so this needed a backend contract, not just a frontend default.

**Design decision — tri-state filter, not a binary toggle:** `list_change_requests` (`backend/app/routers/change_requests.py:275-323`) gained a new `active_only: bool = False` query param, filtering `ChangeRequest.status.in_([DRAFT, SUBMITTED, IN_REVIEW])` when true and `cr_status` is absent — an explicit `cr_status` still wins over `active_only` if both are somehow sent, since a request for one specific status is more specific than "any active status." On the frontend, `statusFilter`'s type widened from `ChangeRequestStatus | ""` to `ChangeRequestStatus | "" | "active"` (`CrStatusFilterValue`, `ChangeRequestsPage.tsx:43-55`), defaulting to `"active"`. The `<select>` now offers three states: "Active" (sends `active_only=true`, no `cr_status` — the new default), "All statuses" (sends neither param — same as the old default), and each individual status (sends `cr_status`, unchanged from before). A binary "just hide terminal statuses with no way back to true unfiltered" was rejected: the existing individual-status options and the row `FilterBadge`s (`:698`/`:719`) already gave users a way to see exactly one status, and collapsing "Active" and "All statuses" into one option would have removed the previously-default "everything" view entirely rather than just changing what loads first. A sentinel string (`"active"`) distinct from both `""` and any real `ChangeRequestStatus` value was necessary so the three states are distinguishable in both the `<select>`'s value and `listParams()`'s branching (`if (statusFilter === "active") ... else if (statusFilter) ...`) — `toggleStatusFilter` (row-badge click-to-filter) is unchanged: clicking a badge still sets that one status, clicking it again still reverts to `""` (all), not back to `"active"`, matching the roadmap brief's "keep working exactly as today when a specific status is picked."

**2. Fix raw-enum filter/table text to use the existing label maps.** Style guide Principle 12 exists specifically for this. All four instances the audit named, re-verified against current line numbers (code had moved since the audit was written):
- `ChangeRequestsPage.tsx`'s status `<select>` (`:745-753`, touched in the same pass as item 1 above) now renders `CHANGE_REQUEST_STATUS_LABEL[s]` instead of the raw `{s}`.
- `RequirementsPage.tsx:889-896`'s equivalent status `<select>` now renders `REQUIREMENT_STATUS_LABEL[s]`.
- `RequirementDetailPage.tsx:649`'s version-history table now renders `REQUIREMENT_STATUS_LABEL[h.status]` instead of raw `{h.status}` — `REQUIREMENT_STATUS_LABEL` was already imported on this file for the page's own status badge (`:467`), so this was a one-line fix.
- `OrgAdminPage.tsx:1615`'s project-group role badge (under Projects & workflow → Manage users) now renders `PROJECT_ROLE_LABEL[g.role]` instead of raw `{g.role}` — `PROJECT_ROLE_LABEL` was likewise already imported and used two lines away for a different badge on the same page.

Checked whether any of the four `Record<Enum, string>` maps could return `undefined` for a value in scope here: all four are exhaustive over their respective TypeScript union types (`RequirementStatus`, `ChangeRequestStatus`, `ProjectRole`) and the fields being indexed (`h.status: RequirementStatus`, `g.role: ProjectRole`) are typed to those same unions, not `string` — so this is a compile-time guarantee, not just a runtime observation, and no latent gap was found.

**3. Fix the hardcoded `FastAPI(version="1.0.0")` OpenAPI metadata constant.** `backend/app/main.py`'s `FastAPI(...)` constructor now passes `version=APP_VERSION` (imported from `app.version`, the same module `GET /api/v1/system/version` already reads) instead of a literal `"1.0.0"` that was never synced with a real release — see [App/API version: investigated, working as designed](ux-audit-2026-08.md#appapi-version-investigated-working-as-designed). This only feeds `/openapi.json`/`/docs` metadata; the real, already-correct version display (`frontend/src/version.ts`, the nav-rail footer) was untouched, per the audit's own finding that it wasn't the reported bug. New test `test_openapi_metadata_version_matches_app_version` (`backend/tests/test_system_version.py`) pins both surfaces — the live `FastAPI` app's own `.version` attribute and the `/openapi.json` response's `info.version` field — against `APP_VERSION`, so a regression back to a literal constant would fail a test immediately rather than silently drifting again.

**4. Document `FilterPanel` as its own style guide `Pattern:`.** Docs-only. New `## Pattern: FilterPanel` section in `docs/ux-style-guide.md`, following the same structure as the other `Pattern:` sections (`Pattern: view toggle`, `Pattern: file-upload trigger`): what the component is (`FilterPanel`/`FilterField`/`FilterCheckbox`, `frontend/src/components/FilterPanel.tsx`), when to reach for it (narrowing filters only — not primary actions, search, or `ViewToggle`, which stay in the main content area), its real call sites verified by grep rather than assumed from the audit's own hedged list of candidates — six pages today, not the "three" the audit's stale side-note said: `RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, `ProjectReviewsDuePage.tsx`, `ProjectActionsPage.tsx`, `ServerOrganisationsPage.tsx`, plus `NotificationsPage.tsx` (a single `FilterCheckbox`, no `FilterField` — confirms the panel doesn't need multiple controls to earn its place) — and its relationship to `FilterBadge` (Principle 10): a `FilterPanel` control is the only entry point for a value with no row badge, while a value that has both should use `FilterBadge` so the badge and the panel's own select drive the identical filter state, not two independently-tracked mechanisms for the same thing. Two stale cross-references fixed at the same time, per the roadmap item's own note that it was "found while fixing a stale cross-reference": Principle 3's parenthetical side-note (previously "worth remembering next time that list grows") now points at the new section instead of describing the gap it just closed, and the "Missing components identified this pass" table's `FilterPanel` row (previously "Not yet written... used on three pages") now links to it and reflects the real six-page count.

**Found and fixed along the way, not deferred:** none in the touched files beyond the four items themselves — `REQUIREMENT_STATUS_LABEL`/`CHANGE_REQUEST_STATUS_LABEL`/`PROJECT_ROLE_LABEL` were all already correctly exhaustive (see item 2's undefined-check above), so there was no fifth raw-enum instance or map gap to fix. One process note, not a code bug: verifying against the `tests/container` stack, running the full backend pytest suite (which manages its own schema state against that same `reqtrack_test` database, per this doc's existing "test suite manages its own DB" convention) left the database with zero users afterward, and a plain `docker compose restart backend` did not immediately re-run the startup bootstrap that recreates the server-admin user — a direct Python invocation of `services.bootstrap.run_bootstrap` did, and a subsequent full `down -v && up -d --build` was not needed on top of that to get both seed scripts and the full Playwright suite running cleanly. Flagged here since it cost real time diagnosing, not because it's a code defect: `run_bootstrap` itself is idempotent and correct (confirmed by manual invocation), and the sequence "pytest full suite, then reseed" already reset the DB in every prior session's methodology — worth a plain container restart (not just relying on the process already running) between "ran the backend test suite" and "ran the seed scripts" in future sessions, so lifespan's bootstrap step has an unambiguous chance to re-run against the post-pytest schema state.

**Verified**: Backend — 2 new tests in `backend/tests/test_change_request_filters.py` (`test_active_only_hides_terminal_statuses`, `test_explicit_cr_status_wins_over_active_only`) and 1 new test in `backend/tests/test_system_version.py` (`test_openapi_metadata_version_matches_app_version`). `ruff check .` clean (one import-order fix needed in `main.py` itself — `app.version` sorts after `app.services.*`, not before `app.routers`, alphabetically — fixed before this was considered done, not left for a linter warning). Full backend suite inside the rebuilt `tests/container` backend image (`docker compose exec backend python3 -m pytest tests/ -q`): **559 passed, 0 failed** (556 pre-existing + 3 new). `npx tsc --noEmit` and a full-tree `npx eslint .` both clean — the same pre-existing 6 `react-refresh/only-export-components` warnings this doc already tracks, no new ones. `npm run build` succeeds. Full Storybook suite: **57 files / 432 tests, all green** — 5 new stories: `ChangeRequestsPage.stories.tsx`'s `StatusFilterDefaultsToActiveAndWidensToAll` (asserts the default `active_only=true` request, that a terminal-status CR is hidden by default, and that "All statuses" both requests without `active_only` and reveals it) and `StatusFilterOptionsUseLabelMap`; `RequirementsPage.stories.tsx`'s `StatusFilterOptionsUseLabelMap`; `RequirementDetailPage.stories.tsx`'s `VersionHistoryShowsLabelledStatus` (required adding a `history` override to `mockRequirementDetailApis`'s `extra` param, previously hardcoded to always return `[]`); `OrgAdminPage.stories.tsx`'s `ProjectGroupRoleBadgeUsesLabelMap`.

Playwright: after a full clean-slate reset (`docker compose down -v && up -d --build`, both seed scripts — see the process note above for why an extra manual bootstrap step was needed this one time), **79/79 passed**, including the new `requirements-and-cr-filters.spec.ts` describe block ("change requests default to an active-only status filter") — creates its own dynamically-named CR (`Active Filter Test CR ${Date.now()}`) and withdraws it via the UI rather than touching shared seed data, per this repo's idempotent-test convention, then confirms it's hidden under the default "Active" filter, reappears under "All statuses," and is findable directly via the "Withdrawn" specific-status option.

Files changed: `backend/app/routers/change_requests.py`, `backend/app/main.py`, `backend/tests/test_system_version.py`, `backend/tests/test_change_request_filters.py` (new); `frontend/src/pages/ChangeRequestsPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/requirements-and-cr-filters.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## New Requirement split-button, CSV wizard in a Modal with per-field fixed values, and the last two "New requirement form" fixes

Five roadmap items from the seventh pass's audit-only findings ([docs/ux-audit-2026-08.md](ux-audit-2026-08.md) roadmap rows 505/506/507/524/525), all under one "New Requirement creation flow" umbrella: the split-button trigger pattern, moving the CSV import wizard into a `Modal`, a per-field fixed-value toggle on CSV import, and the last two small fixes (labels, auto-grow) on the new-requirement form itself.

**1. `Pattern: split-button trigger`, applied to "New Requirement."** New `SplitButtonTrigger` (`frontend/src/components/SplitButtonTrigger.tsx`) — two adjacent `.btn`s in one `.split-button` group (new CSS in `theme.css`, joined borders/radii so they read as one control): the left button calls `onDefaultAction` directly on click; the right, narrower chevron button (`aria-label="More options"`, a new `strings.common.moreOptions` string) reveals `alternatives` in a `Popover`, reusing that component's positioning/outside-click-close logic rather than a second implementation. Wired up on `RequirementsPage.tsx:434-442`, replacing the previous `addMenuOpen`/`Popover`-based two-option menu that made *every* click (including "Add one," the common case) stop at a menu first: a plain click on "New requirement" now opens the `SidePanel` create form immediately; the chevron reveals "Import from CSV," which calls `csvWizardRef.current?.openFilePicker()` exactly as the old menu's "Import from CSV" option did. `strings.requirements.addOne` was deleted as dead code — its only caller was the now-removed "Add one" menu row.

**Judgment call, not resolved by the audit/style-guide text:** the style guide's own "Pattern: split-button trigger" description (written the pass before this one) allows either "a chevron, or hovering the control" for the reveal affordance. Implemented as click-to-reveal on the chevron, not hover, for two reasons: hover has no equivalent on a touchscreen, and this app explicitly supports mobile viewports (U-P-02) — a hover-only path to "Import from CSV" would make it unreachable there; and every other disclosure control already in the app (`CollapsibleSection`, `Popover` itself) opens on click, not hover, so click-to-reveal keeps this component consistent with the app's existing interaction vocabulary rather than introducing its first hover-triggered interactive menu (which would also need its own touch/keyboard fallback story if it were the *only* path in). Scope, per the item's own "you don't need to find/convert other call sites in this pass" note: only `RequirementsPage.tsx`'s trigger was converted; the component itself is generic and reusable, ready for a future pass to pick up the other menu-hides-a-common-case call sites the audit didn't enumerate.

**2. Move `CsvImportWizard`'s body into a `Modal`.** The column-mapping/preview step (`CsvImportWizard.tsx:398` onward, `headers && (...)`) now opens in a `Modal` instead of the previous always-inline `<div className="card stack">` once a file is picked — the trigger row (Import CSV / Export) is unchanged and stays visible on the page underneath. This needed `Modal.tsx` extended first, resolving its own previously-documented "known implementation dependency" (style guide `Pattern: modal dialog for entity create/rename`): a new `size?: "md" | "lg"` prop (`Modal.tsx:19,42`, `MODAL_MAX_WIDTH_PX`) — `"md"` (default) keeps the original fixed 560px for every existing caller (the vote-comments viewer, `ConfirmDialog`) unchanged; `"lg"` widens to 900px, which the CSV wizard's field/column/hint table needs and every other current `Modal` caller doesn't. Container move only, as scoped — the wizard's own column-mapping/preview logic and both backend endpoints (`GET`/`POST .../requirements/export`/`import`) are unchanged. The Modal's own title ("Map your CSV columns") replaces the previous redundant `<strong>Map your CSV columns</strong>` heading inside the card, since the Modal already renders it as an `<h2>` — no duplicate heading, and every existing `getByText("Map your CSV columns")` assertion (Storybook and Playwright alike) still resolves against the Modal's own title element.

**3. Per-field "map from column" vs. "fixed value for every row" toggle on CSV import.** `CsvImportWizard.tsx:47`'s new `FIXED_VALUE_FIELDS` set names exactly the four candidate fields the audit called out: `component_prefix`, `category_prefix`, `level`, `target_version`. Each gains a checkbox ("Same value for every row," its own per-field `aria-label` since all four would otherwise share one ambiguous name) that swaps that field's column-mapping `<select>` for a fixed-value input matching its real type, defaulting to column-mapped (today's only prior behaviour) so an existing CSV/workflow is unaffected:
- `level` — a `<select>` of the three `RequirementLevel` values, rendered through `REQUIREMENT_LEVEL_LABEL` (style guide Principle 12 — the option text is the human label, the option's own `value` is the raw enum wire string, matching how `RequirementsPage.tsx`'s own per-row create-form level select already does this).
- `target_version` — a `<select>` of the project's real `ProjectStage`s, value = stage name (the canonical CSV column's own expected format — a stage *name* string, not an id), with a "— Project's default stage —" blank option matching the backend's own blank-defaults-to-earliest-stage behaviour.
- `component_prefix`/`category_prefix` — `<select>`s sourced from the project's real `components`/`categories` (not free text), so a fixed value can't produce an unknown-prefix row error the way a typo could; `category_prefix`'s own options narrow to the fixed component's categories once `component_prefix` is also fixed (`CsvImportWizard.tsx:313-320`), falling back to every category (with its owning component's name prefixed for disambiguation) when component isn't itself fixed.

`resolveFieldValue` (`CsvImportWizard.tsx:236`) is the one shared resolution function both `confirmImport` (the real upload) and the preview table call, so the preview can never show a different answer than what's actually uploaded. `missingRequired` was extended to treat a required field (`component_prefix`/`category_prefix`) satisfied by either a mapped column or, if its toggle is on, a non-empty fixed value — `level`/`target_version` are optional either way, unchanged.

**Backend: confirmed no endpoint/schema change was needed or made, correcting an assumption in this item's own original scope note** — per this project's own validation rule ("challenge assumptions made by other agents where relevant"), the audit's phrasing ("verify how the backend import endpoint consumes the mapping and extend it correctly to accept a fixed value") implied the backend has some notion of a column-to-field mapping to extend. It doesn't: `POST .../requirements/import` (`backend/app/routers/requirements.py:291`, `import_requirements`) takes only an `UploadFile` and reads it with a plain `csv.DictReader` against fixed canonical column names — no request-schema field for a "mapping" exists or ever did. The "mapping" concept lives entirely client-side: `CsvImportWizard.tsx` always resolves every field to its final per-row value (mapped-column lookup or fixed constant) *before* building the canonical CSV it uploads (`confirmImport`), so from the backend's perspective a "fixed value" column is indistinguishable from a column that simply happens to repeat the same value on every row — a wire contract that already worked, not new backend behaviour. Pinned explicitly anyway rather than left as an unstated assumption: `test_fixed_value_columns_apply_uniformly_across_every_row` (`backend/tests/test_requirement_csv_export_import.py`) posts a CSV where `component_prefix`/`category_prefix`/`level`/`target_version` repeat one value across three rows (only `name` varies), confirming all three import with the shared field values intact.

**4/5. Add `<label>`s to Name/Reasoning/Description; shared auto-growing textarea.** `RequirementsPage.tsx:555-573`: Name, Reasoning, and Description each now wrap their control in `<label className="stack" style={{ gap: "0.25rem" }}>{text}<input/AutoGrowTextarea .../></label>`, the identical shape Component/Category/Target version/Level already used — style guide Principle 13, trivial, no new component for the label half. Reasoning/Description also swap their fixed `rows={2}` `<textarea>` for the new `AutoGrowTextarea` (`frontend/src/components/AutoGrowTextarea.tsx`) — a controlled `<textarea>` that grows via `scrollHeight` up to a cap computed from `getComputedStyle(...).lineHeight` (theme.css's real `15px`/`1.5` typography) × `maxVisibleLines` (default 8, per the audit's own "roughly 8 lines" suggestion) plus padding/border, so the cap tracks the stylesheet rather than being a magic pixel number re-derived per caller.

**Found and fixed along the way, not deferred: a real auto-grow sizing bug, caught by a Storybook interaction test before it ever reached Playwright.** `theme.css` sets a global `box-sizing: border-box` (`theme.css:60`). `scrollHeight` is always a content+padding measurement and never includes border width, regardless of box-sizing — so assigning `el.style.height = scrollHeight` (a border-box *total*) directly shrank the textarea's actual visible content area by its own border width (2px) on every resize. `AutoGrowTextarea.stories.tsx`'s `GrowsToFitContentWithinTheCap` story caught this immediately (`scrollHeight` 70 vs. `clientHeight` 69, i.e. the element was reporting overflow it shouldn't have had) — fixed by adding `verticalBorder` back to the assigned height (`AutoGrowTextarea.tsx`'s effect), matching how the cap's own `maxHeight` calculation already accounted for border. A second, unrelated story bug in the same file (an over-tight `+1`px tolerance for legitimate sub-pixel rounding between the two measurements) was loosened to `+2` once the real bug above was fixed and the assertion still occasionally failed on rounding alone.

**A second found-and-fixed issue, this time in the CSV wizard's own Storybook stories, not the app:** every story that opens the mapping Modal originally grabbed `within(document.body).getByRole("dialog", { name: "Map your CSV columns" })` immediately after `userEvent.upload(...)`, with no wait — since `Papa.parse`'s `complete` callback (and the resulting `setHeaders` re-render) isn't synchronous with the upload event, this occasionally raced and failed with "component failed to render properly." Fixed by wrapping the first dialog lookup in every affected story in `waitFor(...)`, matching the pattern the file's own pre-existing stories already used for the Export popover.

**Seed data:** checked whether `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` needed updates, per this repo's testing-requirements rule — not needed. None of the five items changed a model field, table, enum value, or the backend's request/response shape (the CSV fixed-value item, confirmed above, is a pure frontend request-shape change against an already-existing wire contract); both scripts were re-run end to end against the reset stack as part of verification below and completed with no errors, unrelated to this pass.

**Verified**: Backend — 1 new test (`test_fixed_value_columns_apply_uniformly_across_every_row`, `backend/tests/test_requirement_csv_export_import.py`). Full backend suite inside the `tests/container` backend image: **560 passed, 0 failed** (559 pre-existing + 1 new). `ruff check app/ tests/` clean. Frontend: `npx tsc -b` and a full-tree `npx eslint src/` both clean — the same 6 pre-existing `react-refresh/only-export-components` warnings this doc already tracks (`SortableHeader.tsx`, `BrandingContext.tsx` ×4, `ThemeContext.tsx`), no new ones. `npm run build` and `npx storybook build` both succeed. Full Storybook suite: **59 files / 449 tests, all green** — new `SplitButtonTrigger.stories.tsx` (6 stories: click-performs-default, chevron-reveals-without-firing-default, outside-click-closes, disabled, light/dark) and `AutoGrowTextarea.stories.tsx` (6 stories: accepts input, grows within the cap, caps and scrolls past `maxVisibleLines`, `maxVisibleLines` controls the cap, light/dark); `CsvImportWizard.stories.tsx` extended with `MappingStepOpensAsAModal` and `FixedValueToggleAppliesToEveryRow` (asserts the preview reflects the fixed values on *every* row, then reads the actual uploaded `File`'s text to confirm every data line carries them, not just the first), plus every existing upload-driven story updated for the Modal wrap; `RequirementsPage.stories.tsx`'s split-button story renamed/rewritten (`NewRequirementSplitButtonOpensCreateFormDirectly`, plus a new `NewRequirementChevronRevealsImportFromCsv`) and four other existing stories simplified from a two-click "New requirement" → "Add one" sequence down to one click, plus two new stories (`CreateFormFieldsAllHaveVisibleLabels`, `ReasoningFieldGrowsWithContent`).

Playwright: rebuilt/recreated both `tests/container` backend and frontend images (the `AutoGrowTextarea.tsx` border-box fix above was a real app-code change made *after* the first build, so a second rebuild was needed before this counted as verified against current code — caught by re-checking `git status`/the edit timeline rather than trusting the first build). Nine existing specs updated for the split-button's one-click behaviour (deleting the now-obsolete second `"Add one"` click): `golden-path.spec.ts`, `pelion-v2.spec.ts`, `mockup-engagement.spec.ts`, `requirement-archive-confirm.spec.ts`, `requirement-and-action-restore.spec.ts`, `requirements-bulk-operations.spec.ts`, `review-scheduling.spec.ts`, `workflow-bypass-attempts.spec.ts` (2 call sites), `project-admin-groups-and-fields.spec.ts`; `overview-new-requirement-side-panel.spec.ts`'s own doc comment updated to describe the new interaction (its actual test steps were already unaffected, since the deep-link path it covers bypasses the trigger entirely). New spec `requirement-create-split-button-and-csv-fixed-values.spec.ts` covers all three explicitly-requested pieces: the split button's click-vs-chevron behaviour (a plain click opens the form directly with no menu ever appearing; the chevron reveals "Import from CSV" without itself opening the form), the CSV wizard's mapping step as a real `role="dialog"` Modal with the page underneath still present, and the fixed-value toggle's actual import result (uploads a CSV with only a `name` column, fixes all four toggleable fields, then reads the created requirements back via the API to confirm every row — not just the first — carries the fixed component/category/level/target-stage) — passed standalone and via `--repeat-each=2`. Full clean-slate reset (`docker compose down -v && up -d --build`, both seed scripts) and one full suite run: **81/81 passed** (79 pre-existing + 2 new), including every spec this pass touched and the full pre-existing suite alongside it, confirming no regression from the split-button/Modal/fixed-value changes.

Files changed: `frontend/src/components/Modal.tsx`, `CsvImportWizard.tsx` (+ `.stories.tsx`), `SplitButtonTrigger.tsx` (new, + `.stories.tsx`), `AutoGrowTextarea.tsx` (new, + `.stories.tsx`); `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`); `frontend/src/i18n/strings.ts`, `frontend/src/styles/theme.css`; `backend/tests/test_requirement_csv_export_import.py`; `tests/playwright/tests/golden-path.spec.ts`, `pelion-v2.spec.ts`, `mockup-engagement.spec.ts`, `e2e-workflows/requirement-archive-confirm.spec.ts`, `e2e-workflows/requirement-and-action-restore.spec.ts`, `e2e-workflows/requirements-bulk-operations.spec.ts`, `e2e-workflows/review-scheduling.spec.ts`, `e2e-workflows/workflow-bypass-attempts.spec.ts`, `e2e-workflows/project-admin-groups-and-fields.spec.ts`, `e2e-workflows/overview-new-requirement-side-panel.spec.ts`, `e2e-workflows/requirement-create-split-button-and-csv-fixed-values.spec.ts` (new); `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Role-display collapsing, the Favourites cluster, `ProjectHistoryPage` onto `ActivityPanel`, and the resource picker dialog

Six roadmap items picked up in one session, all from `docs/ux-audit-2026-08.md`'s open (blank-Status) rows: `Pattern: role display` (510), the three-item Favourites cluster (511/512/513), `ProjectHistoryPage` → `ActivityPanel` (515), and `Pattern: resource picker dialog` (508). Rows 509/514/516/517-523/526 were explicitly out of scope for this pass (design-blocked or reserved to avoid `OrgAdminPage.tsx` edit collisions with other batches).

**510. `Pattern: role display`.** New `collapseProjectRoles()` (`frontend/src/api/types.ts`, next to `PROJECT_ROLE_LABEL`) implements the precedence the style guide's pattern section already specified: `project_manager` alone if held; else whichever of `project_administrator`/`stakeholder` are held (both, if both — they're tied, not ordered); else `member`. Applied at every real compact `ProjectRole`-badge-list call site, found by grepping every `PROJECT_ROLE_LABEL` usage rather than trusting the roadmap item's own worked example: `ProjectListPage.tsx` (tile view `:296` and list view `:352`), `FavouritesPage.tsx:110`, `PreferencesPage.tsx:573` ("my organisations" project list). Left deliberately uncollapsed: Org Admin's "View access" `SidePanel` (`OrgAdminPage.tsx:~1416`), per the pattern's own access-audit exception.

**Scope correction, found by grep rather than assumed — same category of finding row 507's own scope-correction was:** this roadmap row's title reads "collapse project/org role badges," but the style guide's actual `Pattern: role display` section (and the audit finding it cites) defines a precedence only for `ProjectRole`'s real four-tier structure. Checked whether `OrgAdminPage.tsx`'s Users table `Roles` column — the audit's own "The Users table itself shows org roles the same un-collapsed way, comma-joined" aside — was actually in scope: it renders `OrgRole` (`org_admin`/`project_creator`/`member`, `u.roles.map((r) => ORG_ROLE_LABEL[r])`), a different, unordered three-value enum with no precedence defined anywhere in the style guide's pattern text. Collapsing it would mean inventing an unspecified rule, not applying the documented one — left untouched, and flagged here explicitly rather than silently decided either way.

**511. Favourites-only filter on `ProjectListPage`.** A new `FilterCheckbox` ("Favourites only") in the page's existing `FilterPanel`, setting `favorite_only=true` on the same `GET /projects` query param `FavouritesPage.tsx` already used — confirmed against `list_projects` (`backend/app/routers/projects.py`) that it composes with every other existing filter (role/stage/org/search/archived) server-side rather than needing a client-side-only filter. No backend change.

**512. Reactive nav-rail "Favourites" entry.** New `FavouritesContext`/`FavouritesProvider` (`frontend/src/context/FavouritesContext.tsx`), mounted in `Layout.tsx` alongside `TerminologyProvider`/`BrandingProvider`. `hasFavourites` moved out of `LayoutShell`'s own local state (previously recomputed only on mount and on landing at `/projects`/`/favourites`, "the only two places a favourite can be toggled" per the old code's own comment) into the shared context; `ProjectListPage.tsx`'s and `FavouritesPage.tsx`'s toggle handlers both call the new `refreshFavourites()` immediately after their mutating API call resolves. **Judgment call**, since the audit left the exact wiring open ("not just special-casing more routes"): an event-driven refetch via context (toggle succeeds → call `refreshFavourites()` → context re-fetches the full project list once) rather than (a) a global favourites-count field returned from every API response, which would mean touching every endpoint's response shape for one UI affordance, or (b) short-interval polling, which fires the same request repeatedly with no actual signal of change. The context's default value (`{ hasFavourites: false, refreshFavourites: () => {} }`, not `undefined`) is deliberately a real, harmless no-op rather than a value that throws outside a provider — this matters for Storybook coverage below, where `ProjectListPage`/`FavouritesPage` stories render standalone with no `FavouritesProvider` wrapped around them and still work.

**513. `Pattern: view toggle` wired onto `FavouritesPage`.** `FavouritesPage.tsx` now uses `useViewMode("favourites")`/`ViewToggle`, matching `ProjectListPage.tsx`/`RequirementsPage.tsx` exactly — a new list/table view (star/unfavourite, name+summary, organisation, stage badge, roles, requirement count) sits alongside the existing tile grid, replacing the page's previous fixed CSS-grid-only layout. Style guide's `Pattern: view toggle` section marked applied.

**515. `ProjectHistoryPage` converted onto the shared `ActivityPanel` component.** `ProjectHistoryPage.tsx`'s hand-rolled list markup (badge + `describeActivityEntry` + link + timestamp, previously `:92-109`) replaced with `<ActivityPanel entries={changes} getLink={(c) => activityEntryLink(c, projectId)} fullWidth />`. `ActivityPanel` itself gained two new optional props rather than being forked: `getLink` (a project-wide list spans many different requirements/change requests, so — unlike the single-entity detail-page side panels `ActivityPanel` already served — each row needs its own link saying which item it's about; `RequirementDetailPage.tsx`'s/`ChangeRequestDetailPage.tsx`'s existing calls simply omit it, unchanged) and `fullWidth` (renders at natural width instead of the sidebar's `minWidth: 220`/`alignSelf: flex-start`, since this is now the page's main content, not a narrow companion panel). `RequirementDetailPage.tsx`'s separate `RequirementVersion`-backed History card (a different data source entirely, per the audit's own explicit distinction) was not touched.

**Judgment call — `ProjectOverviewPage.tsx`'s own activity card was deliberately left alone, not converted too, despite rendering the identical shape (badge + `describeActivityEntry` + link + timestamp, its own `:107-129`).** The audit's "Project activity and requirement activity: same data, duplicated rendering" finding names `ProjectHistoryPage.tsx` as the one with duplicated *markup* needing the fix ("The actual duplication is on the frontend: `ProjectHistoryPage.tsx:92-109` hand-rolls its own list markup... instead of reusing `ActivityPanel`") — it does not make the same claim about `ProjectOverviewPage.tsx`, which already shares the underlying `describeActivityEntry`/`activityEntityLabel` text-generation functions (the dedup an earlier pass already did) even though its row *layout* independently duplicates the same shape. Converting it too was not part of this roadmap row's scope and risked second-guessing a distinction the audit itself drew deliberately (a dashboard summary card embedded next to stage-progress charts vs. a full-page timeline) rather than an oversight — left alone rather than silently expanding scope; noted here rather than silently decided.

**508. `Pattern: resource picker dialog`.** New `ResourcePickerModal` (`frontend/src/components/ResourcePickerModal.tsx`) — a `Modal` (`size="lg"`) with a source list on the left (`sources: ResourcePickerSource[]`, each just `{ id, label, loadFiles() }`) and the active source's files on the right as a checkbox multi-select, one "Attach selected" action at the bottom. Built pluggable per the style guide's own instruction: today exactly one source is wired up ("{Org} shared resources"), but adding a second (a project's own uploads, a report chapter's existing image pool) is a new array entry for the caller, not a rewrite of the component. **Confirmed via grep before wiring anything up, per this project's own verify-before-building rule**, that `POST /{requirement_id}/files/link` (`backend/app/routers/requirements.py:1187-1217`) really had zero frontend callers anywhere in `frontend/src/` — the audit's own finding, re-checked rather than assumed. Wired to `RequirementDetailPage.tsx`'s Attachments card: a new "Link from shared resources" button (hidden once the requirement is locked, matching `FileAttachmentList`'s own direct-upload trigger gating — the backend endpoint itself 409s on a locked requirement, so this is UI parity with an existing rule, not a new one) opens the dialog; `onAttach` calls the endpoint once per selected file id (it only accepts one id per call), then reloads the card and shows a toast (`strings.resourcePicker.attachedToast`).

Multi-select-then-attach (a checkbox per file, one "Attach selected" action) was chosen over attach-on-click-per-row: it matches the existing shared-resource checkbox picker on `ChangeRequestsPage.tsx` (proposed-attachment selection) more closely than a single-click-and-close flow would, and avoids a chain of individual network round trips each closing and reopening the dialog for a multi-file attach. Per the item's own explicit scope note, the report-chapter image picker the style guide names as a plausible future consumer of this same component was **not** wired up in this pass — `reports.py`/`ReportsPage.tsx`'s own bespoke resource-selection logic is untouched; the component was just built generic enough that doing so later doesn't need a rewrite.

**Principle 12 applied throughout, not just at the label-map call sites already covered:** `ResourcePickerModal`'s and the Attachments card's new UI text all route through new `strings.resourcePicker.*` keys (`orgResourcesSource`, `noFiles`, `attachSelected`, `linkFromSharedResources`, `attachedToast`) rather than inline English, matching how `strings.changeRequests.selectAttachment`/`noAttachmentsAvailable` already handle the sibling shared-resource picker on `ChangeRequestsPage.tsx`.

**No backend change for row 508.** The endpoint already existed and already had a pinning test (`test_org_shared_resource_can_be_linked_to_requirement`, `backend/tests/test_files.py`) — confirmed by grep before assuming a gap, rather than adding a duplicate test for already-covered behaviour.

**Seed data:** checked `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` against this pass's testing-requirements rule — no updates needed. None of the six items changed a model field, table, enum value, or backend request/response shape; all six are frontend display/UI logic (`collapseProjectRoles`) or frontend wiring onto an already-existing, already-tested backend endpoint (`files/link`). Both scripts were re-run end to end against a freshly reset `tests/container` stack as part of verification below and completed with no errors.

**Verified**: Backend — no new tests needed (no backend behaviour changed; `files/link`'s existing coverage confirmed above). Full backend suite inside the `tests/container` backend image (`docker compose exec backend python3 -m pytest tests/ -q`): **560 passed, 0 failed** (unchanged from before this pass, as expected for a frontend-only change set). `npx tsc -b` clean. Full-tree `npx eslint src/`: **0 errors**, 7 `react-refresh/only-export-components` warnings — the same 6 this doc already tracks, plus one new instance on `FavouritesContext.tsx` (co-exporting `useFavourites()` alongside `FavouritesProvider`, the identical accepted pattern every other context file in the app already uses — `AuthContext.tsx`, `BrandingContext.tsx`, `ThemeContext.tsx` — not a new warning class). `npm run build` succeeds. Full Storybook suite: **60 files / 461 tests, all green** — new `ResourcePickerModal.stories.tsx` (6 stories: select-and-attach, multiple sources switch the right pane, empty source, a failed attach stays open with an inline error, light/dark); `ActivityPanel.stories.tsx` extended with `WithLinksToRelatedEntities` (`getLink`/`fullWidth`); `FavouritesPage.stories.tsx` extended with `ListView`; `ProjectListPage.stories.tsx` extended with `FavouritesOnlyFilterNarrowsTheList` and `RoleBadgesCollapseToTheEffectiveHighest`; `RequirementDetailPage.stories.tsx` extended with `LinkFromSharedResourcesAttachesFile` and `LinkFromSharedResourcesHiddenWhenLocked`; `ProjectHistoryPage.stories.tsx`'s existing stories updated for the `ActivityPanel` markup change (a real RTL gotcha caught here, see below).

**Found and fixed along the way, in this pass's own test code, not the app:** `ProjectHistoryPage.stories.tsx`'s pre-existing assertions (`getByText("Alex Morgan updated")`) initially got rewritten to include the new link's label text (`getByText("Alex Morgan updated requirement-1")`) on the assumption that `ActivityPanel`'s description `<div>` and its child `<Link>` would concatenate into one matchable string — they don't: Testing Library's default `getByText` only considers a node's own direct text-node children, not descendant elements' text, so a div containing a text node followed by a sibling `<a>` never matches a string spanning both. Reverted to asserting the description text alone (`"Alex Morgan updated"`) and the link separately by role/name/href, which is both correct and a strictly stronger assertion than the original single-string match.

**Judgment calls recap** (per this project's own "flag a deviation/open decision explicitly" rule): the `OrgRole` scope correction on row 510 (above); event-driven `FavouritesContext` refetch over polling or a response-shape change (row 512, above); `ProjectOverviewPage.tsx`'s activity card deliberately left unconverted (row 515, above); multi-select-then-attach shape and out-of-scope report-image-picker reuse (row 508, above).

Playwright: full clean-slate reset (`docker compose down -v && up -d --build`, both seed scripts, matching this doc's own established "the only valid verification is a full reseed followed by exactly one suite run" methodology) before the final run. New/modified specs: `role-display-collapsing.spec.ts` (new — builds a dedicated throwaway user on Gamma via the UI, granted `project_administrator`+`stakeholder` via two different project groups then `project_manager` via a third, rather than mutating any shared seeded persona's effective permissions for the rest of a suite run, which risks changing behaviour other specs depend on, e.g. a "stakeholder can't archive" assertion elsewhere — proves the tied-tier-2 badge pair, the manager-collapses-everything case, and the uncollapsed "View access" panel, all in one flow); `resource-picker-link-requirement-attachment.spec.ts` (new — builds its own throwaway org/admin/project/requirement via the API, same pattern `report-image-attachment.spec.ts` already established, rather than reusing a seeded requirement, since Alpha-1's own first requirement is deliberately locked by the seed script and this spec specifically needs an unlocked one; proves select-and-attach, the unselected file staying unattached, and the trigger disappearing once locked while the already-linked file stays visible); `notifications-and-favourites.spec.ts` extended (the favourites-only filter, and the reactive nav-rail assertion replacing the spec's own previous work-around comment — it used to `page.goto("/favourites")` directly specifically because the rail link might not have appeared yet, now asserts the rail link directly instead); `project-history.spec.ts` extended (an activity row's link, scoped to the entity-type filter rather than the unfiltered default view, since the latter's page-1 contents depend on how much unrelated activity the shared Alpha org has accumulated by the time this spec happens to run in a full suite). Two bugs found and fixed in this pass's own new specs before they passed, not in the app: `resource-picker-link-requirement-attachment.spec.ts`'s lock-the-requirement step originally sent `owner_id: null` in the PUT payload, but `RequirementUpdate.owner_id` is a required (non-nullable) `UUID` in the schema — the PUT silently 422'd and the requirement never actually locked, which the test's own next assertion (button still visible) caught; fixed by capturing the org admin's own user id from the login response and reusing it. `role-display-collapsing.spec.ts` hit two strict-mode violations from scoping too broadly: `getByPlaceholder("Email")` (non-exact) also matched the Users section's "Search by name or email" search box; and, once more than one of this spec's own project groups had a member, a page-wide `li`/`getByText(email)` match became ambiguous across them — both fixed by scoping to the specific section/group card (`{ exact: true }` on the first; the group's own `<strong>` title, two levels up, matching `org-group-nesting.spec.ts`'s existing pattern, on the second).

Ran the new/modified specs plus a regression sample of adjacent specs first (`user-access-panel.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `org-group-nesting.spec.ts`, `report-image-attachment.spec.ts`, `requirement-actions.spec.ts`, `comment-editing-and-attachments.spec.ts` — chosen for touching `OrgAdminPage`'s Users/Groups sections, project groups, or a requirement's Attachments card, the areas this pass's changes are nearest to): **11/11 passed**. Full suite, one clean run against the freshly reseeded stack: **83/83 passed** (81 pre-existing + 2 new spec files), 5.1 minutes — no regression in any spec this pass didn't touch.

Files changed: `frontend/src/api/types.ts`, `frontend/src/components/ActivityPanel.tsx` (+ `.stories.tsx`), `frontend/src/components/Layout.tsx`, `frontend/src/context/FavouritesContext.tsx` (new), `frontend/src/components/ResourcePickerModal.tsx` (new, + `.stories.tsx`), `frontend/src/components/FilterPanel.tsx` (unchanged, reused), `frontend/src/i18n/strings.ts`; `frontend/src/pages/ProjectListPage.tsx` (+ `.stories.tsx`), `FavouritesPage.tsx` (+ `.stories.tsx`), `PreferencesPage.tsx`, `ProjectHistoryPage.tsx` (+ `.stories.tsx`), `RequirementDetailPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/role-display-collapsing.spec.ts` (new), `resource-picker-link-requirement-attachment.spec.ts` (new), `notifications-and-favourites.spec.ts`, `project-history.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## UX audit, seventh-pass roadmap: Org Admin's Users/Groups promoted to top-level groups (517)

First of a seven-item batch implementing the seventh pass's remaining open roadmap rows (517-521, 523, 516) — the SSO-group-mapping schema migration (522) and the two explicitly-deferred `RequirementStatus`/`ChangeRequestKind` design items (509/514) stay out of scope, as directed. Items 517/519/520/523 all touch the same `orgAdminGroups`/Overview region of `OrgAdminPage.tsx`, so they're implemented in the roadmap's own listed order (517 → 519 → 520 → 523) to avoid fighting overlapping edits; 518, 521, and 516 are interleaved/appended around them as they touch other regions of the same file or other files entirely.

**517.** `OrgAdminGroupKey` (`OrgAdminPage.tsx`) replaced its single `"people"` key with `"users"`/`"groups"` — a flat regroup, matching the style guide's settings-hierarchy addendum ("this doesn't need a new navigation capability... `ResourceMenu` already renders any number of flat top-level groups"). The one `activeGroup === "people"` JSX block split cleanly into two: the Users `CollapsibleSection` + "View access" `SidePanel` under `"users"`, the Groups `CollapsibleSection` under `"groups"`. `ORG_ADMIN_GROUP_KEYS` and the `orgAdminGroups` route-def array both updated; `App.tsx`'s existing `/orgs/:orgId/admin/:group?` route needed no change, since it already resolves any recognised flat key. New strings `groupUsers`/`groupGroups` added to `strings.ts`; `groupPeople` removed (grepped first to confirm no other call site used it — none did).

**Verification.** `npx tsc --noEmit` clean; `npx eslint` clean on the three touched files. Storybook: all 30 `OrgAdminPage.stories.tsx` tests pass, including 6 stories whose `getByRole("link", { name: "People" })` calls were updated to "Users" or "Groups" depending on which section each story actually exercises (`UsersSectionAndCreateUser`, `UsersSectionSortByEmail`, `ViewUserAccessPanel` → Users; `GroupsSectionShowsMembers`, `GroupsSectionNestGroup`, `GroupsSectionCreateGroupViaPopover` → Groups). `GroupsSectionShowsMembers`'s own comment about "Alex Morgan appearing twice" no longer applies now that Users and Groups render on separate pages — updated rather than left stale. Playwright specs updated to match (grepped for `selectOrgAdminGroup(page, "People")` and `admin/people` across `tests/playwright`): `role-display-collapsing.spec.ts` (2 call sites, both → "Users" — creates a user, then later opens "View access"), `org-group-nesting.spec.ts` (→ "Groups"), `user-access-panel.spec.ts` (2 call sites → "Users"), `org-security-controls.spec.ts` (→ "Users"). Playwright itself deferred to a consolidated run once items 1-4 (all sharing this file/region) are in, rather than a full `docker compose down -v && up -d --build` cycle after every single-file S-effort item — noted here as a deliberate pacing call, not a skipped verification step.

## UX audit, seventh-pass roadmap: "New user" onto Modal, org action menu, Leave organisation relocated (518-520)

Continuing the same batch (see the 517 entry above). Items 518-520 all touch `OrgAdminPage.tsx`'s Overview/Users region, implemented in roadmap order.

**518.** `OrgAdminPage.tsx`'s "New user" form (email/name/password + submit, permanently visible below the Users table) converted to a `Modal` (default `size="md"`) opened by a "New user" button — the first real usage of the revised Principle 3 in this codebase, setting the shape items 519/521 in this same batch follow. `createUser()` previously had no `try`/`catch` at all (a failed create threw uncaught); it now matches `createGroup()`'s existing pattern — closes the modal and shows a success `Toast` (new `strings.orgAdmin.userCreated`) on success, shows an error `Toast` on failure without closing the modal.

**519.** New `ActionMenu` (`frontend/src/components/ActionMenu.tsx`) — a `MoreVertical` kebab trigger opening a `Popover`-backed menu (`role="menu"`/`"menuitem"`, reusing `Popover`'s positioning/focus-trap/outside-click-close rather than a new implementation, the same reuse `SplitButtonTrigger` already makes). Combines Overview's rename and export actions behind one trigger (`triggerLabel={strings.orgAdmin.orgActions(orgLabelCap)}`, e.g. "Organisation actions"). Rename opens a `Modal` (replacing the always-visible inline input + conditional "Rename" button); export calls the existing `exportOrg()` handler directly and closes the menu, per the item's own scope note that a relocated action isn't a create/rename flow and doesn't need a dialog. **Permission-preserving judgment call, not incidental**: the *old* code only gated the rename input behind `advanced != null` (org-admin-only, since a non-admin 403s on advanced-settings) — export and leave were both offered to every member unconditionally. Collapsing rename+export into one menu component made it tempting to gate the whole menu behind `advanced`, which would have silently taken Export away from ordinary members — instead the menu always renders, and only the "Rename" *item* is conditionally included in its `items` array. `ActionMenu` also gained a `disabled` prop so the trigger can be disabled while `exportingOrg` is true, preserving the old button's in-flight-disable behaviour that would otherwise have been lost when the label ("Exporting…") stopped being visible once the menu closes on click. New `docs/ux-style-guide.md` "## Pattern: action menu" section (between "Pattern: modal dialog for entity create/rename" and "Pattern: resource picker dialog") plus a `MoreVertical` row in the Tokens icon table and an `ActionMenu` mention in "Framework & primitives".

**520.** Destination decided: Preferences' existing "Your access" tab, not a per-org card on `/orgs` — that tab already lists every org a user belongs to (role badges, projects, groups), so a "Leave" button per row extends an existing list rather than building a second, independent one (this project's own shared-component-reuse rule; a *new* org-list-with-leave-buttons on `/orgs` would have been a second, slightly-different implementation of the same "list my orgs" shape `PreferencesPage.tsx` and `OrgListPage.tsx` both already have). **A real gap found and fixed while moving it, not just relocated**: `OrgAdminPage.tsx`'s old "Leave {org}" button fired `DELETE /orgs/{id}/membership` immediately, with zero confirmation of any kind — the exact pattern the style guide's "Pattern: confirmation, in two tiers" was written against. Now Tier 1 `ConfirmDialog` ("Leave {org.name}?" — reversible, an admin can re-add the user) plus a success `Toast`. `PreferencesPage.tsx`'s mount-time org/project/group-loading `useEffect` body was extracted into a standalone `loadMyAccess()` so `leaveOrg()` can call it again after a successful leave — the left org's own projects have to disappear from the picker too, not just the org row itself. Accessible naming: the button's visible label is the static "Leave" (the row already shows the specific org's name via `<strong>{org.name}</strong>`), but its `aria-label` is `` `Leave ${org.name}` `` — short and per-row-distinguishable, separate from the longer `title` hover text (`Remove your own membership in ${org.name}`), so the accessible name doesn't repeat the full sentence for a screen-reader user while still disambiguating multiple "Leave" buttons on the same page (caught by a first draft of the Storybook story failing on an over-long aria-label). `OrgAdminPage.tsx`'s `leaveOrg()`/`leaveError`/the inline `btn-danger` button removed entirely, along with the then-unused `navigate`/`useNavigate` and `LogOut` icon import — not left as a second, dead copy per this item's own explicit instruction.

**Verification.** `npx tsc --noEmit` clean; full-tree `npx eslint src/` clean (0 errors, the same 7 pre-existing `react-refresh/only-export-components` warnings, no new ones); `npm run build` succeeds. Storybook: new `ActionMenu.stories.tsx` (6 stories) plus updates to `OrgAdminPage.stories.tsx`/`PreferencesPage.stories.tsx` — full suite **472/472 passing across 61 files** (up from 461/60 before this batch).

**Two pre-existing, unrelated bugs found while verifying against the live stack and fixed per this project's "fix, don't defer" rule, not logged and left**:
- `preferences-and-theme.spec.ts`'s own "access tab lists direct and inherited org-group membership" step asserted a bare, page-wide `getByText("(via a nested group)")` — correct the first time this spec ever ran against a given database, but ambiguous (strict-mode violation) on any later run, since it creates a fresh nested-group pair for the same shared `orgAdminAlphaBeta` persona every time and never cleans the old ones up, so each repeat run adds one more "(via a nested group)" match on screen. Scoped to the specific `<li>` containing that run's own uniquely-suffixed parent-group name instead.
- `org-admin-project-statuses-and-link-types.spec.ts` permanently mutated the shared "E2E Beta Software" org's seeded `DEFAULT_PROJECT_STATUSES` (deleted 3 of the 4 down to a single renamed "Active (E2E)") with no cleanup — since `seed_e2e_dataset.py` only seeds once, this spec could only ever pass the *first* time it ran against a given database, exactly the non-idempotent-test failure mode this project's own conventions call out. Rewritten to create its own disposable org (which gets the same four default statuses and default link types automatically, on org creation, same as every org) and two throwaway projects (which default to the org's lowest-sort-order status, "Proposed", the same way Beta-1/Beta-2 did) instead of touching Beta's shared fixture data at all — confirmed idempotent by running it twice in a row.

Playwright: ran the full set of specs touching `OrgAdminPage.tsx`'s Overview/Users/Groups regions or Preferences' Access tab (`admin-page-titles-and-version-footer`, `role-display-collapsing`, `org-group-nesting`, `user-access-panel`, `org-security-controls`, `org-rename-and-test-email`, `leave-organisation` (new), `preferences-and-theme`, `project-list-org-scoping`, `org-bundle-export-import`, `org-merge-import`, `org-admin-project-statuses-and-link-types`) against the rebuilt Docker stack (`docker compose -f tests/container/docker-compose.yml build frontend backend && up -d --force-recreate frontend backend`; `seed_e2e_dataset.py` reported already-seeded, no reseed needed for a frontend-only + test-hygiene change set): **17/17 passing** on the settled run. One transient flake seen mid-pass (`org-merge-import.spec.ts`, a download-event race when run back-to-back with `org-bundle-export-import.spec.ts` under the full batch's sequential load) — reproduced as green both standalone and paired with its neighbour on every retry, so treated as pre-existing suite timing noise, not a regression from this batch's own changes.

Files changed: `frontend/src/components/ActionMenu.tsx` (new, + `.stories.tsx`), `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/PreferencesPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/leave-organisation.spec.ts` (new), `org-rename-and-test-email.spec.ts`, `org-bundle-export-import.spec.ts`, `org-merge-import.spec.ts`, `preferences-and-theme.spec.ts`, `org-admin-project-statuses-and-link-types.spec.ts`, `project-list-org-scoping.spec.ts` (comment only), `role-display-collapsing.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## UX audit, seventh-pass roadmap: "New Project"/"New Organisation" onto Modal (521)

Fifth of the seven-item batch (see the 517/518-520 entries above). Same "Pattern: modal dialog for entity create/rename" shape items 518/519 already established in this batch — the third and fourth real usages, proving the pattern generalises beyond `OrgAdminPage.tsx`.

**`ProjectListPage.tsx`.** The always-visible `<div className="card stack">` (org picker, name, summary, template picker, visibility + hint, bundle-import file field, submit) is now a `Modal` (default `size="md"`) opened by the existing "New project" button. Two small, in-scope improvements made while rebuilding the form, not separate items: (1) Name/Summary/the org picker gained real `<label>`s — they were placeholder-only before, the same gap the new-requirement form had before an earlier roadmap pass fixed it (Principle 13); (2) the audit's own "How things get created" finding that picking an import file silently hides the template picker with no explanation now shows a small note (`strings.projects.importIgnoresTemplate`) in its place instead of the field just vanishing — a genuinely small fix, not a redesign of the two fields' relationship, per this item's own "don't scope-creep" note.

**`ServerOrganisationsPage.tsx`.** Same container swap for its simpler two-field form (name + bundle-import file), the name field gaining a real `<label>` as well. The two pages' forms were kept independent, as scoped — no shared component was forced between Project and Organisation creation beyond `Modal` itself, since they don't otherwise share meaningful structure (org creation has no template/visibility/org-picker concepts at all).

Neither page's underlying create logic or backend endpoint changed — a pure container swap, matching the item's own scope note.

**Verification.** `npx tsc --noEmit` clean; `npx eslint` clean on both pages, their stories, and `strings.ts`. Storybook: `ProjectListPage.stories.tsx` (12/12) and `ServerOrganisationsPage.stories.tsx` (9/9) both pass — new `CreateProjectModalCancel`, `CreateProjectImportFileHidesTemplateWithNote` (asserts the note appears and the template picker disappears once a file is chosen), `CreateOrganisationModalCancel`; the existing create stories on both files rewritten to scope into the portalled `role="dialog"` and query fields by label instead of placeholder.

Playwright: grepped for every `getByRole("button", { name: "New project" })`/`"New organisation"` click site across the suite rather than assuming a fixed list — found 9 across 8 files (`multi-org-admin.spec.ts` ×2, `project-bundle-export-import.spec.ts`, `project-admin-templates-and-reports.spec.ts`, `project-visibility.spec.ts`, `project-access-scope.spec.ts`, `project-list-org-scoping.spec.ts` ×2, `single-org-admin.spec.ts`, `org-bundle-export-import.spec.ts`, `server-org-lifecycle.spec.ts`). All updated the same way the earlier `SidePanel` rollout (roadmap item 489) already established for a portalled layer: scope every subsequent locator to `page.getByRole("dialog", { name: "New project" | "New organisation" })` rather than a bare `.card`/page-wide query, and swap `getByPlaceholder` for `getByLabel` now that the fields are real `<label>`s. Full rebuilt-frontend run against the live stack: **10/10 passing**.

Files changed: `frontend/src/pages/ProjectListPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ServerOrganisationsPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/multi-org-admin.spec.ts`, `project-bundle-export-import.spec.ts`, `project-admin-templates-and-reports.spec.ts`, `project-visibility.spec.ts`, `project-access-scope.spec.ts`, `project-list-org-scoping.spec.ts`, `single-org-admin.spec.ts`, `org-bundle-export-import.spec.ts`, `server-org-lifecycle.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## UX audit, seventh-pass roadmap: "Integrations & Security" split into OAuth/SSO, Email, Security (523)

Sixth of the seven-item batch (see the 517/518-520/521 entries above). Same flat-regroup shape as items 517's Users/Groups split — no new `ResourceMenu` capability, per the style guide's own settings-hierarchy addendum, which already named this exact split as the anticipated next step.

**Regroup.** `OrgAdminGroupKey`'s `"integrations-security"` replaced with `"oauth-sso"` | `"email"` | `"security"`. The single `activeGroup === "integrations-security"` block (SSO/OIDC + its group-mappings sub-block, SCIM, SMTP & email, Security, Personal Access Tokens — 5 `CollapsibleSection`s) split cleanly into three: OAuth/SSO (SSO/OIDC + SCIM), Email (SMTP & email), Security (Security + PATs, unchanged from where an earlier pass already put PAT lifetime).

**SCIM placement — the one non-obvious call, per this item's own "use judgment" note.** Grouped with SSO/OIDC under OAuth/SSO rather than folded into Security. Reasoning: SCIM and SSO/OIDC are both, fundamentally, *identity-provisioning integrations* — mechanisms by which an external system (an IdP) pushes or pulls user/group state into this org — whereas Security's other members (2FA requirement, self-signup, external-user policy) are policy toggles this org's own admins set directly, with no external system in the loop. SCIM doesn't touch anything more "security-sensitive" than SSO/OIDC does (both handle credentials/tokens), so severity wasn't the deciding factor — the mechanism (provisioning vs. policy) was. Recorded here per this project's own "flag a non-obvious call explicitly" rule, exactly as the roadmap item asked.

**A real design snag found while splitting, not just a mechanical JSX move.** The SSO group-mappings sub-block and the SMTP & email card both persisted via a single shared "Save advanced settings" button that used to live further down the *same* combined page (their own inline notes literally said "saved with the Security settings below"). Splitting into three separate top-level pages would have silently broken this: a user editing SSO mappings on the new OAuth/SSO page would have had no visible control left to actually save them, since the button that used to do it moved to a different page entirely. Fixed by giving OAuth/SSO's mappings sub-block and Email's SMTP card each their own explicit save button (new strings `saveMappings`/`saveEmailSettings`) — both still call the exact same `saveAdvanced()` function Security's own button calls. This is safe, not a duplicate-endpoint hack: `OrgAdminPage` is one React component whose state (`smtpHost`, `advanced.sso_group_mappings`, `require2fa`, `patMaxLifetimeDays`, ...) all lives at the top level regardless of which resource-menu group is currently rendered, so calling `saveAdvanced()` from any of the three pages submits the complete, correct `OrgAdvancedSettings` payload — the same reasoning `SMTP & email` and `Personal Access Tokens` already relied on when they shared one button on one page, just now applying across three pages instead of one.

**Copy cleanup.** The pre-existing shared button (now Security-only) relabelled `strings.orgAdmin.saveAdvanced` from "Save advanced settings" to "Save security settings" — "Advanced" is the retired UI concept this whole roadmap batch (and the fourth/fifth passes before it) has been dismantling; leaving the label behind on the one surviving button would have quietly reintroduced it. 4 Playwright call sites and 2 Storybook assertions updated for the rename.

**Verification.** `npx tsc --noEmit` clean; full-tree `npx eslint src/` clean (0 errors, same 7 pre-existing warnings). Storybook: full suite **477/477 passing across 61 files** (up from 472 before this item — 2 new stories, `EmailSettingsSave`/`SsoMappingsSave`, proving the new per-group save buttons actually persist). `npm run build` succeeds.

Playwright: full rebuild (`docker compose -f tests/container/docker-compose.yml build frontend && up -d --force-recreate frontend`) and a run of every spec touching this region — `org-security-controls.spec.ts` (2FA/external-user-policy, now on "Security"), `org-rename-and-test-email.spec.ts` (SMTP override + test email, now on "Email"), `scim-provisioning.spec.ts` (now on "OAuth/SSO"), `sso.spec.ts` (unaffected — configures SSO via direct API calls, not the Org Admin UI form, confirmed by grep before assuming it needed changes): **10/10 passing**.

Files changed: `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/org-security-controls.spec.ts`, `org-rename-and-test-email.spec.ts`, `scim-provisioning.spec.ts`; `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## UX audit, seventh-pass roadmap: RequirementDetailPage's History and Activity cards merged (516)

Last of the seven-item batch (see the 517/518-520/521/523 entries above). Unlike the other six, this roadmap row's own scope note said "merge or clearly re-label" — a design call, not a mechanical fix. The product decision, stated explicitly in the batch's own brief: merge, not relabel-only, keeping both renderings.

**What "History" and "Activity" actually were.** `RequirementDetailPage.tsx`'s "Change log" card (`strings.requirements.history`, despite the audit's own shorthand calling it "History") rendered `RequirementVersionEntry[]` — the requirement's own approved version/status-transition ledger, one row per version, via `GET .../requirements/{id}/history`. The `ActivityPanel` sidebar rendered `ChangeEntry[]` — the general audit-log feed (comments excluded from *this* particular feed per the file's own C-A-09 note... correction, checked directly: comments ARE included in `ChangeEntry`/`get_project_changes`, the file's C-A-09 note is about the version-history table specifically never showing comments, which is obviously true of a version ledger) via `GET .../requirements/{id}/activity`, itself a filter over `get_project_changes` — two genuinely different data sources with real overlap (both show version/status changes) but neither a strict subset of the other (only Activity shows comments/links/attachments/archiving; only Version history shows the change-note text an editor typed for that specific version).

**Merge shape.** One card, a two-button `aria-pressed` toggle (`Table`/`Activity` lucide icons — the Tokens table's icon-collision rule ruled out reusing `LayoutGrid`/`List`, which specifically mean tile vs. list elsewhere in the app), both renderings kept intact — selecting a view swaps which one is on screen, nothing was dropped. New copy: "Version history" for the table (replacing the old, arguably-mismatched "Change log" heading — the table was never actually labelled "History" in the UI despite that being this whole item's own name for it), "Activity" unchanged for the feed. State: `useUiPreference<string>("requirement_detail_history_view", "activity")` called directly, **not** a new `useHistoryView`-style wrapper analogous to `useViewMode`. Judgment call, per this item's own "your call" note: `useViewMode`'s own `ViewMode` type is hardcoded to `"tiles" | "list"`, so wrapping it (or writing a same-shaped sibling) for a `"versions" | "activity"` pair would either force a misleading type or duplicate `useViewMode`'s five-line body for no real reuse — calling `useUiPreference` directly, narrowing the loosely-typed stored string the same way `useViewMode` itself does inline, was simpler and no less discoverable for a pattern used in exactly one place today.

**`ActivityPanel` gained a `bare` prop** (skip the outer `.card` and its own hardcoded `<h2>Activity</h2>`) rather than forking a second component or duplicating its entry-list JSX inline in `RequirementDetailPage.tsx` — the merged card supplies its own card and its own toggled heading (which needs to read "Version history" or "Activity" depending on state, not always "Activity"), so nesting the full `ActivityPanel` unchanged would have produced a card-in-a-card with a stuck, wrong heading. Every other `ActivityPanel` caller (`ChangeRequestDetailPage.tsx`'s side panel, `ProjectHistoryPage.tsx`'s full-width list) is unaffected — `bare` defaults `false`.

**Layout consequence, not just a card swap.** The page's `.side-grid` (a fixed `1fr 240px` two-column grid: everything else in the main column, `ActivityPanel` alone in the narrow second column) collapsed to a single `.stack` — once Activity's content moved into the merged card in the main column, nothing was left to occupy the second column, and a `.side-grid` with an empty second slot would have been a dead, meaningless wrapper. Confirmed no other page-level styling depended on this specific `.side-grid` instance (the CSS rule itself is shared and untouched for every other page still using it).

**Style guide.** Added a note under the existing "Pattern: view toggle (tile vs. list)" section rather than a whole new `## Pattern:` — this is the same underlying idea (a small `aria-pressed` two-button toggle, `useUiPreference`-backed, remembered per-user) applied to a second, differently-shaped pair of views, not a structurally new pattern. Explicitly named as *not* literally reusing the shared `ViewToggle` component (different icon/label pair, `ViewToggle`'s own type is tile/list-specific) — a future third case with the same tile/list semantics should still use `ViewToggle` directly; this note exists so a *fourth* similarly-shaped-but-different pair doesn't have to re-derive the "small toggle + `useUiPreference`, doesn't have to be the literal shared component" reasoning from scratch.

**Verification.** `npx tsc --noEmit` clean; `npx eslint` clean on all touched files. Storybook: full suite **480/480 passing across 61 files** (up from 477 before this item) — `RequirementDetailPage.stories.tsx` gained `HistoryActivityToggleSwitchesViews` (default-Activity, toggle to Version history, toggle back, `aria-pressed` state on both buttons throughout) and `NoDuplicateHistoryOrActivityCard` (exactly one "Activity"/"Version history" `<h2>` on the page, not the old two-card duplication); `ActivityPanel.stories.tsx` gained `Bare`. The pre-existing `VersionHistoryShowsLabelledStatus` story needed a decorator change (`withAuth` → `withStatefulAuth`) since its play function now has to actually click the toggle before the table is reachable — a no-op `setUiPreference` would have left the toggle click silently inert.

Playwright: new `requirement-history-activity-toggle.spec.ts` — creates a throwaway requirement (not seeded data, since editing it to produce a real version/activity entry is the point), edits it to generate one version-history row + one activity entry, confirms Activity is the default view, toggles to Version history and confirms the *same* edit appears as a table row (not a second copy), and — the one thing Storybook can't exercise — confirms the chosen view survives a full page reload via the server-synced `ui_preferences` bag, not just in-memory component state. Also ran the full existing set of specs touching `RequirementDetailPage` to catch any layout-removal regression from dropping `.side-grid`: `comment-editing-and-attachments`, `resource-picker-link-requirement-attachment`, `requirement-and-action-restore`, `requirement-actions`, `review-scheduling`, `requirement-archive-confirm`, `workflow-bypass-attempts` — **9/9 passing** (including the 2 new toggle-spec tests).

Files changed: `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/components/ActivityPanel.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/requirement-history-activity-toggle.spec.ts` (new); `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

---

## UX audit roadmap: explicit requirement approval action + change-request lock guard (509)

The last of the roadmap's "needs a design pass first" items before this pass: no standalone way to approve a requirement existed, and nothing stopped a change request from targeting a still-draft one. Touches authorization (who can lock a requirement out of direct editing) and the change-request workflow, so this went through the identify → verify → remediate review `change-management-and-secure-development-policy.md` calls for on security-sensitive changes, same as this project's other hardening passes.

**Design, resolved by extending existing precedent rather than inventing a new one.** The roadmap item itself pointed at the answer: `LOCKED_STATUSES = {APPROVED, COMPLETED}` (`services/requirements.py:25`) already draws the line between "editable directly" and "editable only via an approved change request" (C-G-12) — `update_requirement`'s own lock check already enforces it for direct edits. The fix extends that one existing line to change-request creation too, rather than introducing a second, parallel notion of "which statuses need a CR."

**`create_change_request` guard.** A `MODIFY_REQUIREMENT` change request now requires the target requirement's current version to be locked (`is_locked()`, reusing the exact helper `update_requirement` already calls) — a 400 otherwise, with a message pointing the caller at editing directly instead. `NEW_REQUIREMENT` change requests are unaffected (there's no existing requirement to lock-check).

**New `POST .../requirements/{id}/approve`.** The actual, previously-missing standalone path out of `draft`/`reviewed` — added alongside `complete`/`uncomplete` in `routers/requirements.py`, same shape (a precondition-checked single-purpose action, not a detour through the general `PUT` edit endpoint, which already supported `status=approved` but only as one field among a whole edit payload). Gate: project manager only — not the broader `require_project_manage` (which also admits project administrators and org admins) — matching `decide_change_request`'s own explicit check and `update_requirement`'s existing inline comment on its `status=approved` branch, both citing C-U-03's clarification that names requirement approval as a *Project Manager* privilege specifically, layered on top of what administrators/stakeholders can otherwise do.

**The bug found alongside it, fixed in the same pass, not deferred.** `decide_change_request`'s `MODIFY_REQUIREMENT` approval branch unconditionally set `status_value=RequirementStatus.APPROVED` — regardless of the requirement's status *before* the change request was approved. With no creation-time guard, a change request could previously be opened against a `draft` requirement and, on approval, silently promote it straight to `approved` bypassing any real review; worse, one opened against an already-`COMPLETED` requirement would, on approval, silently revert it back down to `approved` — the requirement's "this shipped" state disappearing as a side effect of an unrelated field edit, with no message that it happened. Now that `create_change_request`'s own guard guarantees the target is already locked (`approved` or `completed`) at creation time, and status transitions monotonically upward otherwise, the current status at decision time is always one of those two — so `status_value=None` (the existing `apply_new_version` convention for "carry the current value forward unchanged") is the correct fix, not a different forced value. `approval_authority_id` is unaffected either way, since it's only reassigned when `status_value == RequirementStatus.APPROVED` is explicitly passed — a modify-CR against already-approved content isn't a fresh approval, so the original approver of record correctly stays attributed.

**Frontend.** `RequirementDetailPage.tsx` gained an "Approve" button (`btn-primary`, `CheckCircle` icon) shown exactly when `requirement.requires_approval` is true (an existing, previously-unconsumed field — `draft`/`reviewed`) and the viewer holds `project_manager`; "Make change request" now only renders once `requirement.is_locked` is true, matching the new backend guard instead of always being offered. `markCompleted`/`unmarkCompleted` — found lacking try/catch and toast feedback while touching this exact button row, and fixed in the same pass per this project's own "fix, don't defer" rule — gained both, matching the shape `archive`/`restore` already had on the same page. `ChangeRequestsPage.tsx`'s "Select a requirement" dropdown (the modify-CR creation form) now filters to `is_locked` requirements only, with an explanatory hint (`noLockableRequirements`) when none exist yet, and its default-selection logic picks the first *locked* requirement rather than blindly the first one in the list.

**Test-suite blast radius, found and fixed rather than left broken.** ~20 call sites across 10 backend test files previously created a `MODIFY_REQUIREMENT` change request against a requirement that was still `draft` at that point — exactly the gap this item closes, so every one of them now correctly 400s under the new guard. Each was updated to approve the requirement first (either via the new `/approve` endpoint directly, or relying on an existing `_approve_current_stage` call already present earlier in the same test): `test_change_request_field_tracking.py` (7 of 9 call sites; the other 2 already locked via stage approval), `test_requirement_lifecycle.py`, `test_reporting_and_changes.py`, `test_notifications.py`, `test_change_request_tasks_and_votes.py` (a shared `_submitted_cr` helper), `test_org_lifecycle.py`, `test_project_export_import.py` (also required bumping a version-history-length assertion from 2 to 3, since the new explicit approve step is itself a real version), `test_project_structure_admin.py`, `test_custom_fields.py`, `test_security_hardening.py`. Both seed scripts had one CR-creation site each affected by the same gap: `seed_e2e_dataset.py`'s "pre-existing change requests for volume" loop now approves each target requirement first; `seed_demo_data.py`'s Solstice Cloud Platform change request was initially assumed to need the same fix, but investigating first (rather than adding it unconditionally) showed that requirement's own seed spec already set its status to `"approved"` directly — the added approve call 409'd as "already approved," confirming the assumption was wrong, and was removed rather than left in as a redundant no-op.

**A second-order bug found while fixing the first**: `frontend/src/testing/storybook-helpers.tsx`'s `buildRequirement()` previously defaulted `is_locked`/`requires_approval` to fixed `false` values regardless of the `status` override a story passed in — so a story built with `status: "approved"` (locked, in the real app) was silently mocked as unlocked. This had been latent and harmless until this pass made `is_locked`/`requires_approval` drive real UI (the "Make change request" link, the new "Approve" button); fixed at the root by deriving both fields from `status` by the same rule the backend uses (`LOCKED_STATUSES`/`REQUIRES_APPROVAL_STATUSES`), with an explicit override still winning — rather than patching every affected call site's overrides individually. This surfaced one further pre-existing story inconsistency once fixed: `RequirementDetailPage.stories.tsx`'s `StakeholderCanEditNotArchive` used `status: "approved"` while asserting the edit form was reachable — status "approved" is locked for every role including a stakeholder, so that assertion was only ever passing because of the mock bug being fixed here; corrected to `status: "draft"`, which is what the story actually intends to demonstrate.

**Verification.** Backend: full suite, **565/565 passing** (including 5 new tests: `test_approve_requirement_transitions_draft_to_approved`, `test_approve_requirement_requires_project_manager_role`, `test_approve_requirement_rejects_an_already_locked_requirement`, `test_create_change_request_rejects_a_still_draft_requirement`, and the regression test for the `decide_change_request` bug, `test_decide_change_request_preserves_completed_status_instead_of_reverting_to_approved`). `npx tsc --noEmit` clean. Storybook: full suite **483/483 passing across 61 files** (up from 480) — new stories `ManagerCanApproveDraftRequirement` (clicks Approve, asserts the toast), `StakeholderDoesNotSeeApproveOnDraftRequirement`, and `NoLockableRequirementsShowsHint` on `ChangeRequestsPage.stories.tsx`; `ManagerCanArchiveAndComplete` gained assertions that "Make change request" now shows and "Approve" doesn't, once locked. One story-authoring bug found and fixed along the way: the "no lockable requirements" hint paragraph, originally nested inside the `<label>` wrapping the select, made `getByLabelText("Select a requirement")` fail (the hint's own text became part of the label's accessible name) — moved to a sibling `<p>` outside the `<label>`, which is also the more correct a11y shape (a select's accessible name shouldn't absorb unrelated help text below it).

**Playwright.** Full rebuild of the `tests/container` stack, then a clean-slate reseed (`seed_e2e_dataset.py` + `seed_demo_data.py`) — necessary because the backend pytest suite's own session-scoped `DROP SCHEMA public CASCADE` fixture runs against the same `reqtrack_test` database this stack's E2E/demo data lives in, per `docs/e2e-workflows.md`'s own documented gotcha. New `requirement-approval.spec.ts` (a throwaway requirement, not seeded data, since approving isn't reversible from the UI — mutating a shared seeded requirement would corrupt it for every other spec and for a repeat run of this one): a stakeholder sees neither "Approve" nor "Make change request" on it and a direct API approve call 403s; the project manager sees and uses "Approve," after which "Make change request" appears and "Approve" doesn't. `seed_e2e_dataset.py` also gained a lock step for Delta-1 Terminology Demo's first requirement, needed for `terminology-coverage.spec.ts`'s existing "Make {changeRequest}" assertion to have anything to find post-guard.

**Four pre-existing, unrelated spec breakages found and fixed along the way, per this project's own "fix, don't defer" rule**, surfaced only by running the *entire* suite rather than just the specs this item touches:
- `golden-path.spec.ts`, `mockup-engagement.spec.ts`, and `pelion-v2.spec.ts` (the three broad top-level walkthrough specs, as opposed to `tests/e2e-workflows/`'s narrower specs) still drove the "New project" form via `getByPlaceholder("Name"/"Summary")` and an xpath ancestor-climb to a `.card` — stale since an earlier, already-`Done` roadmap item (521) converted that form to a `Modal` with real `<label>`s, which apparently only updated the `e2e-workflows/` specs its own decisions.md entry named, not these three. All three now scope to `page.getByRole("dialog", { name: "New project" })` and use `getByLabel`, matching the convention already established in `project-list-org-scoping.spec.ts` and friends.
- `mockup-engagement.spec.ts` separately needed its own new requirement approved before raising a change request against it (this item's own guard) — added an explicit "approve the requirement" step via the UI, since this spec never baselines a stage the way `golden-path.spec.ts` does.
- `terminology-coverage.spec.ts`'s "Make {changeRequest}" assertion needed Delta-1's target requirement locked (see the seed-script fix above).

Full suite (after all of the above): **85/86 passing**, `tests/e2e-workflows/requirement-and-action-restore.spec.ts` the one failure — re-ran in isolation immediately after, **2/2 passing**, confirming a transient/resource-contention flake from the full-suite run rather than a real regression (matches the same pattern already seen from `org-merge-import.spec.ts` earlier in this same verification pass, which also failed once under full-suite load and passed cleanly both standalone and on a second full run).

Files changed: `backend/app/routers/requirements.py`, `backend/app/routers/change_requests.py`; `backend/tests/test_requirement_lifecycle.py`, `test_change_request_field_tracking.py`, `test_reporting_and_changes.py`, `test_notifications.py`, `test_change_request_tasks_and_votes.py`, `test_org_lifecycle.py`, `test_project_export_import.py`, `test_project_structure_admin.py`, `test_custom_fields.py`, `test_security_hardening.py`; `backend/scripts/seed_e2e_dataset.py`, `seed_demo_data.py`; `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ChangeRequestsPage.tsx` (+ `.stories.tsx`), `frontend/src/i18n/strings.ts`, `frontend/src/testing/storybook-helpers.tsx`; `tests/playwright/tests/e2e-workflows/requirement-approval.spec.ts` (new); `tests/playwright/tests/golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts` (pre-existing, unrelated `getByPlaceholder`-vs-Modal breakage fixed along the way); `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

---

**Batch summary (items 517/518/519/520/521/523/516, all seven Done).** All items from this pass's brief are implemented and verified — none partial. Two items outside this batch's scope, per the brief's own instruction, were left untouched: 522 (`OrgGroup.granted_org_role` SSO schema migration — a real data migration, not a UI move) and the two `RequirementStatus`/`ChangeRequestKind`-design-dependent rows (509, 514). Cumulative verification across the whole batch: full frontend `npx tsc --noEmit`/`npx eslint src/` both clean throughout; Storybook grew from 461/60 (pre-batch) to 480/61 tests/files, all green on the final run; `npm run build` succeeds. Two genuinely pre-existing, unrelated test-hygiene bugs were found while verifying against the live stack and fixed in place rather than deferred (per this project's own "fix, don't defer" rule) — both logged under their own roadmap items' entries above: `preferences-and-theme.spec.ts`'s page-wide "(via a nested group)" text match, and `org-admin-project-statuses-and-link-types.spec.ts`'s permanent mutation of Beta org's shared seeded project statuses (rewritten onto a disposable org, confirmed idempotent by running it twice in a row). No backend changes were needed for any of the seven items — confirmed per item, not assumed, since the batch's own instructions asked that any backend need be flagged explicitly rather than silently worked around.

---

## UX audit roadmap: Modal conversion sweep, first batch (526, partial)

Item 526 ("convert every entity-create/rename flow currently on `Popover`/inline onto `Modal`, per the revised Principle 3") is explicitly the broadest item left on the roadmap, described in its own row as cutting across nearly everything above. Rather than attempt the whole surface in one pass, this batch scoped tightly to Principle 3's own named entity list (`docs/ux-style-guide.md`: "a project, an organisation, a user, a group, a requirement") cross-referenced against what was actually still Done for each: project/organisation/user creation and org rename were already converted (items 518/519/521); **group** creation and the **requirement** create form were not, despite both being named explicitly. A fifth flow, the Change Requests page's own "New change request" form, was added to this batch even though "change request" isn't one of Principle 3's five named nouns — it's the single most field-heavy inline create form left anywhere in the app (a kind selector plus a per-field checkbox-revealed editor, explicitly called out as such in this same document's historical "Pattern: create panels" mockup section) and had never received *any* layer conversion at all, unlike every other entity create flow in the app, which had at least reached `Popover`/`SidePanel` before this pass.

**Four flows converted, all straight `Popover`/inline → `Modal` container swaps with no field-layout changes**, since `Modal`/`SidePanel`/`Popover` all share the same `card stack` dialog chrome via `dialogA11y.ts` — confirmed by diffing `Modal.tsx` against `SidePanel.tsx` before touching either call site, rather than assuming:
- `RequirementsPage.tsx`'s "New requirement" form (`SidePanel` → `Modal`, `size="lg"` given its field count — name/reasoning/description/component/category/target version/level plus custom fields plus the empty-component/category quick-setup variant).
- `ChangeRequestsPage.tsx`'s "New change request" form (bare inline `<div className="card stack">` → `Modal`, `size="lg"`) — the trigger button changed from a toggle (`setShowForm((v) => !v)`) to a plain open (`setShowForm(true)`), matching every other Modal-fronted create button in the app; closing is `Modal`'s own Escape/backdrop/× affordances instead of a second click on the same button.
- `OrgAdminPage.tsx`'s "New group" (`Popover` → `Modal`; state renamed `newGroupPopoverOpen`→`newGroupModalOpen` to match the `newUserModalOpen` naming convention item 518 already established, and its now-unused `anchorRef`/`useRef` import removed — `Modal` doesn't anchor to a trigger the way `Popover` does).
- `ProjectAdminPage.tsx`'s "New group" (same conversion, project-scoped endpoint + the required role select item 488 added).

**Why not a Popover for these:** the decision tree's own leaf split is "create or rename an entity" → `Modal` vs. "one or two fields, a single quick decision" → `Popover` — group creation is 1-2 fields but is creating a new entity, which the tree resolves by entity-vs-quick-action, not by field count (the "Add link" `Popover` on the same requirement-detail page, by contrast, is correctly left alone below — it's a quick decision *about* an existing requirement/action, not creating either).

**Verified, not assumed, that both flows still have the DOM-order fragility their earlier `SidePanel` conversions already surfaced.** `Modal`/`SidePanel`/`Popover` all portal to the end of `document.body`, so any Playwright/Storybook query that resolves "first combobox on the page" unscoped will silently start resolving to whatever inline control (typically a `FilterPanel` status select) precedes the portal in DOM order once a form moves from inline to a portalled layer — this exact class of breakage was already fixed once for `RequirementsPage`'s own conversion (`review-scheduling.spec.ts`/`workflow-bypass-attempts.spec.ts`, roadmap item 489) and reproduces identically for `ChangeRequestsPage`: `golden-path.spec.ts`, `mockup-engagement.spec.ts`, `change-request-tasks-and-voting.spec.ts`, and `change-request-approval-separation.spec.ts` all used `page.getByRole("combobox").first()` immediately after opening the form. All four now scope to `page.getByRole("dialog", { name: "New change request" })` first, matching the established fix.

**Test updates were near-zero-diff because `role="dialog"`/`aria-label={title}` is identical across `Modal`, `SidePanel`, and `Popover`** — every existing `getByRole("dialog", { name: ... })` query kept working unchanged for all four flows; only the DOM-order-dependent unscoped queries above needed a code change, and a handful of stale comments/story names referencing "SidePanel"/"popover" for these specific flows were corrected for accuracy (`RequirementsPage.stories.tsx`, `overview-new-requirement-side-panel.spec.ts`'s own doc comment, `review-scheduling.spec.ts`, `workflow-bypass-attempts.spec.ts`, `org-group-nesting.spec.ts`; `OrgAdminPage.stories.tsx`'s `GroupsSectionCreateGroupViaPopover` → `GroupsSectionCreateGroupViaModal`, `ProjectAdminPage.stories.tsx`'s `GroupsTabCreateGroupViaPopover` → `GroupsTabCreateGroupViaModal` — both renames checked against the whole repo for other references first; only `docs/decisions.md`'s own historical entries named the old export names, left as-is since they're a past-tense record). `ChangeRequestsPage.stories.tsx`'s five create-flow stories (`ModifyRequirementFieldTogglePrefills`, `ModifyRequirementCreateDisabledUntilFieldSelected`, `NewRequirementKindShowsPlainCreateForm`, `SubmitCreatesChangeRequest`, `NoLockableRequirementsShowsHint`) had no prior Modal/SidePanel/Popover conversion to inherit a scoping pattern from at all (this form had never left inline), so each was rescoped from bare `canvas.*` queries to `within(dialog).*` directly against the new `Modal`.

**Deliberately not touched this pass, named rather than silently dropped — the remainder of item 526's own scope:** the new-custom-field inline row and the Stages/Components/Categories/Action-Types one-step "New X" rows in Project Admin (these are the `DefinitionList`/bespoke definition-table shape items 478/480 already reviewed and deliberately left alone as a different, intentional pattern — a flat table row-add, not a freestanding entity-create form); the org report-template nested-accordion create flow; the three duplicated "New PAT" nested-accordion forms (Org Admin, Server Management, Preferences); and `RequirementDetailPage.tsx`'s "Create and link a new action" `SidePanel` (built in item 496, before this revision of Principle 3 existed — a real remaining case of the pattern the revision targets, since a brand-new `Action` is exactly the "not detail about the current view" shape the revision's reasoning applies to regardless of `Action` not being one of Principle 3's five example nouns). `Add link`/`Link existing action` on the same page are correctly left on `Popover` — they're a quick 1-2-field decision about an existing entity, not a create flow, per the decision tree above.

**Verified.** `npx tsc -b` and a full-tree `npx eslint .` both clean. Storybook: the four touched story files (91 tests) plus the full suite (**483/483 passing across 61 files**) both green — no regression outside the touched files. Rebuilt/recreated the `tests/container` frontend image (no bind-mounted source; backend untouched, no rebuild needed). Playwright: one failure on first run (`requirements-and-cr-filters.spec.ts`'s status-filter test, expecting a seeded draft requirement to disappear under an "approved" filter) turned out to be leftover state from an earlier, unrelated run against the same long-lived dev database rather than a regression from this change — confirmed by a full clean-slate reseed (`docker compose down -v && up -d --build`, both seed scripts) and a clean rerun, per this project's own documented methodology for exactly this failure shape (see the "Extend terminology overrides" and "explicit requirement approval action" entries above, both of which hit and diagnosed the identical pattern). Final clean-slate run, the 22 specs identified as touching any of the four converted flows (grepped for `"New requirement"`/`"New Requirement"`/`"New change request"`/`"New group"` across the whole suite, not assumed): **22/22 passing** — `golden-path.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`, `overview-new-requirement-side-panel.spec.ts`, `change-request-tasks-and-voting.spec.ts`, `change-request-approval-separation.spec.ts`, `review-scheduling.spec.ts`, `workflow-bypass-attempts.spec.ts`, `org-group-nesting.spec.ts`, `role-display-collapsing.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `requirement-and-action-restore.spec.ts`, `requirement-approval.spec.ts`, `requirement-archive-confirm.spec.ts`, `requirement-create-split-button-and-csv-fixed-values.spec.ts`, `requirement-history-activity-toggle.spec.ts`, `requirements-and-cr-filters.spec.ts` (3 tests), `requirements-bulk-operations.spec.ts`.

Files changed: `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `ChangeRequestsPage.tsx` (+ `.stories.tsx`), `OrgAdminPage.tsx` (+ `.stories.tsx`), `ProjectAdminPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/golden-path.spec.ts`, `mockup-engagement.spec.ts`, `e2e-workflows/change-request-tasks-and-voting.spec.ts`, `e2e-workflows/change-request-approval-separation.spec.ts`, `e2e-workflows/review-scheduling.spec.ts`, `e2e-workflows/workflow-bypass-attempts.spec.ts`, `e2e-workflows/org-group-nesting.spec.ts`, `e2e-workflows/overview-new-requirement-side-panel.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

---

## UX audit roadmap: Modal conversion sweep, second batch (526, continued)

Picked up the three cases the first batch named as deliberately not touched: the org report-template create/edit form, "New personal access token," and `RequirementDetailPage.tsx`'s "Create and link a new action" — closing out item 526 to just the two genuinely different-pattern exclusions (the `DefinitionList`/bespoke definition-table rows, and the org report-template/PAT/"New PAT" forms' own remaining scope, both already named and left alone deliberately in the first batch).

**Scope correction, found by grep rather than trusting the roadmap row's own wording:** item 526's own text describes "the three duplicated 'New PAT' nested-accordion forms (Org Admin, Server Management, Preferences)." Checked directly rather than assumed, per this project's own "validate rather than take a claim at face value" rule: only **Preferences** has an actual PAT *creation* form (`createPat`/`newPatName` etc.) — `OrgAdminPage.tsx`'s and `ServerManagementPage.tsx`'s own PAT sections are list-and-revoke UI only (an org/server admin managing *other users'* tokens), with no create form of any kind to convert. What the three files actually share is the nested-`CollapsibleSection` (`variant="plain"`) *idiom*, not three copies of a create flow. Only Preferences' own "New personal access token" form was converted; Org Admin's and Server Management's PAT sections are untouched, correctly — there was nothing there in scope.

**Three flows converted, same straight container-swap shape as the first batch — no field-layout changes:**
- `RequirementDetailPage.tsx`'s "Create and link a new action" (`SidePanel` → `Modal`, default `size="md"`) — built in item 496, before the Principle 3 revision existed; a brand-new `Action` has no "what came before it" in the app's reading order (per the pattern's own reasoning) regardless of `Action` not being one of Principle 3's five named example nouns. "Link existing action" and "Add link" on the same page are correctly left on `Popover` — quick 1-2-field decisions about an existing entity, not creates.
- `OrgAdminPage.tsx`'s org report-template create/edit form (nested `CollapsibleSection` → `Modal`, `size="lg"` given its field count — name/accent colour/cover-page/logo toggles/footer text/intro/chapters/appendices/chapters-per-component). This form is also reused for *editing* an existing template (`startEditTemplate`), not just creating — kept as the same single form/`Modal`, title switching between "New template"/"Edit template" exactly as the pre-conversion accordion already did; still one `Modal` state (`templateFormOpen`), not two. A "New template" button was added next to the template list (there was previously no separate open step — the form was simply always rendered, collapsed by the accordion itself) plus a resulting `openNewTemplateForm`/`closeTemplateForm` pair; `startEditTemplate` now also opens the modal, and delete/save both funnel through `closeTemplateForm` so the modal can't be left open pointing at a template that was just saved or removed.
- `PreferencesPage.tsx`'s "New personal access token" (nested `CollapsibleSection` → `Modal`, default size). Same shape: a "New personal access token" trigger button added next to the PATs tab's heading (previously the form had no open/close step, just an always-rendered accordion), new `strings.preferences.patNew` string, `openPatForm`/`closePatForm`/`resetPatForm` helpers factored out of `createPat`'s previous inline field-clearing. The post-create "copy your token now" card is unchanged and still renders on the page itself (not inside the modal, which closes on success) — the same place it always has.

**Why `Modal` and not `Popover` for the report-template/PAT forms, despite the group-creation precedent the first batch already established:** both are multi-field forms (7+ distinct inputs), squarely on the `Modal` side of the decision tree's field-count split, not the 1-2-field `Popover` side "New group" used.

**Verified.** `npx tsc --noEmit` and a full-tree `npx eslint src/` both clean. Storybook: the three touched story files (`PreferencesPage.stories.tsx`, `OrgAdminPage.stories.tsx`, `RequirementDetailPage.stories.tsx`, 79 tests) plus the full suite (**486/486 passing across 61 files**, up from the first batch's 483/61 — three new stories: `NewReportTemplateOpensModalAndCreates`, `NewReportTemplateModalCancel`, `PatsTabCreateModalCancel`) both green. `npm run build` succeeds. Rebuilt/recreated the `tests/container` frontend image (backend untouched, no rebuild needed) and ran the three Playwright specs that actually exercise these flows against the live rebuilt stack (grepped the whole suite for "Personal Access Tokens"/`patNamePlaceholder`/"report-templates"/"Report templates"/"Create and link a new action" rather than assumed which specs touch this): `personal-access-tokens.spec.ts`, `project-admin-templates-and-reports.spec.ts`, `requirement-actions.spec.ts` — **3/3 passing**. Not re-run: the full 60-file Playwright suite, since this batch's changes are narrowly confined to the three flows above with no shared-component or DOM-order-dependent change of the kind that broke unrelated specs in the first batch (`Modal`/`SidePanel` share identical `role="dialog"`/`aria-label` semantics, so no locator in any other spec depends on which of the two a given trigger opens).

Files changed: `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `OrgAdminPage.tsx` (+ `.stories.tsx`), `PreferencesPage.tsx` (+ `.stories.tsx`); `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/requirement-actions.spec.ts`, `project-admin-templates-and-reports.spec.ts`, `personal-access-tokens.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

**Documentation bug found and fixed while auditing the roadmap table for other remaining work, not part of this batch's own scope:** two other rows ("Shared Modal-based confirm + type-to-confirm variant," "Shared Toast component, wired into every mutation") were still marked "Partial" describing ~12 remaining call sites as open follow-up work — checked against "Work through the remaining `window.confirm`/no-`Toast` list" (sixth pass, already `Done`) and confirmed that row closed out exactly those call sites already. Both stale rows corrected to `Done` with a pointer to the row that actually did the work, rather than leaving the roadmap table internally contradicting itself.

---

## UX audit roadmap item 514: actions gated behind a draft/change-request rule once a requirement is locked

**A real design conflict, found and resolved before implementation, not silently picked a side on.** The roadmap item's own text framed this as needing a decision — "actions should follow the same draft/change-request-gated workflow as requirement fields" — but `RequirementDetailPage.tsx`'s own module docstring stated the *opposite* as a deliberate prior decision: "an Actions card for requirement actions... — neither of these is gated by `is_locked`, since links and actions are metadata about the requirement rather than its own governed content." Flagged explicitly to the user rather than resolved unilaterally, per this project's own "flag a deviation/open decision explicitly" rule — the user chose to gate actions via a new change-request kind (mirroring `MODIFY_REQUIREMENT`'s own gate) over the two alternatives (folding it into `MODIFY_REQUIREMENT` itself, or leaving the existing behaviour and marking the finding superseded). The docstring is updated to record this as a deliberate reversal, not a silent drift.

**Design, extending existing precedent rather than inventing a new mechanism:** a new `ChangeRequestKind.ADD_ACTION` (`backend/app/models/enums.py`) and six new nullable columns on `ChangeRequestVersion` (migration `0013_add_action_change_request_kind.py`, additive/backfill-free — every existing version row is unaffected): `proposed_action_link_id` (link an existing `RequirementAction`) or `proposed_action_title`/`proposed_action_description`/`proposed_action_type_id`/`proposed_action_assignee_id`/`proposed_action_due_date` (create a new one), mutually exclusive — mirrors `RequirementDetailPage.tsx`'s own existing "Link existing action"/"Create and link a new action" split rather than inventing a third shape. `create_change_request` (`routers/change_requests.py`) requires the target requirement already be locked, the identical guard `MODIFY_REQUIREMENT` already enforces (item 509) — a still-draft/reviewed requirement is edited/added-to directly instead. `create_and_link_action`/`link_action` (`routers/requirements.py`) gained the same lock check `upload_requirement_attachment`/`link_org_resource` already carry, rejecting the direct path with a 409 once locked. `decide_change_request`'s approval branch creates-and-links or links-existing on approval, re-validating the referenced action/action-type at approval time (mirrors the "attachments" re-check just above it in the same function, since either could have changed between submission and approval) rather than trusting creation-time validation alone.

**Judgment call, recorded here per this project's own rule:** the created action's `creator_id`/`RequirementActionLink.linked_by` attribute to `cr.creator_id` (the original submitter), matching `RequirementFile.linked_by`'s own convention for CR-approved attachments — not `apply_new_version`'s "the approving PM becomes the actor" convention used for a requirement's own field changes. Reasoning: an added action is closer in kind to a proposed attachment (a new artifact the submitter is contributing) than to a new requirement version (whose authorship follows the approver, since approval is what actually changes the requirement's own governed content).

**A pre-existing bug found and fixed alongside this, not deferred:** every notification title/body across `create_change_request`'s submit step and `decide_change_request`'s decision step interpolated `version.proposed_name` directly — always `None` for `ADD_ACTION` (which never sets it), and *already* `None` today for any `MODIFY_REQUIREMENT` change request that doesn't propose renaming anything (silently rendering "Change request submitted: None" in production, pre-existing and unrelated to this item, surfaced only because `ADD_ACTION` would have hit it on every single notification). New `_display_title(db, cr, version)` helper: `proposed_name` if set, else the proposed action's own title for `ADD_ACTION`, else the target requirement's current name, else a generic fallback — used at all four notification call sites.

**Two smaller findings, fixed along the way, not left as follow-up:** `ChangeRequestDetailPage.tsx` rendered a MODIFY_REQUIREMENT-vs-else ternary with no `ADD_ACTION` branch at all — extended to a three-way switch showing the proposed action's content (or, link-existing mode, a resolved preview of the action being linked, fetched via the already-existing-but-previously-uncalled-from-here `GET .../actions/{id}`) and hiding the Target/Level badges, which aren't meaningful for an action proposal. `backend/scripts/seed_demo_data.py`'s own pre-existing Falcon-3 action-seeding step called the direct create-and-link endpoint against `remote_id_req`, a requirement the same script seeds directly into `"complete"` status — this used to work only because the gate didn't exist yet; discovered when the reseed run below hit the new 409 for real. Fixed by converting that one call to the new `create_add_action_change_request` helper (create → submit → approve → re-fetch the created action by title), and used the same helper for a second, new demonstration on Solstice Cloud Platform's already-locked latency requirement — not just patched around the failure, since a demo script silently relying on the exact bug this item closes would be worth fixing regardless of which script hit it first.

**Deliberately not touched, named rather than silently dropped:** `backend/scripts/seed_e2e_dataset.py` — its own existing action fixtures (`E2E Review Action`/`E2E Test Action`) target still-draft requirements, unaffected by the new gate, and no spec asserts an exact change-request count on any project this script seeds, so no fixture was added there; the new Playwright spec below creates its own throwaway locked requirement instead, matching this suite's own established "don't mutate shared seed data for approval flows" convention. `ChangeRequestsPage.tsx`'s list-row Target/Level badges (`crTargetLabel`/`crLevelLabel`) still resolve to the target requirement's current stage/level for an `ADD_ACTION` row rather than being hidden the way the detail page's badges now are — lower-value real estate on a list row, not wrong, left alone rather than expanding scope to a second page's badge logic.

**Verified.** Backend: `python -m pytest` (local, against `reqtrack_test`) — full suite **568 passed, 7 failed**, all 7 failures confirmed pre-existing and unrelated (`aiosmtplib.errors.SMTPConnectError: ... connecting to mailhog ...` — a local-machine-only DNS gap, `mailhog` only resolves inside the Docker Compose network; reproduced identically on `test_invites_and_external_users.py`/`test_oidc_provisioning.py`/`test_org_export_import.py` run in isolation, none of which this item's diff touches). New `backend/tests/test_requirement_action_change_requests.py` (10 tests: direct-path 409 once locked for both create-and-link and link-existing, direct path still works while unlocked, CR rejected against a still-draft requirement, CR rejects both/neither of link-vs-create, both approval paths (create-new and link-existing) actually create/link on approval and not before, duplicate-link rejected, cross-project action-type and cross-project action-link both rejected) — **10/10 passing**. `test_schema_migrations_match_models.py` (compares live migrated schema against model metadata) passing, confirming the new migration is complete. Frontend: `npx tsc --noEmit`/`npx eslint src/` clean; Storybook — 4 new stories (`RequirementDetailPage.stories.tsx`: `CreateAndLinkActionRoutesThroughChangeRequestOnceLocked`, `LinkExistingActionRoutesThroughChangeRequestOnceLocked`; `ChangeRequestDetailPage.stories.tsx`: `AddActionShowsProposedActionContent`, `AddActionShowsLinkedActionPreview`), full suite green (486/486, unchanged file count). New `tests/playwright/tests/e2e-workflows/requirement-action-change-request.spec.ts` (create-and-link path only — the link-existing path is covered by the backend test and Storybook interactions instead, not duplicated as a third real-browser round trip) proves the actual end-to-end flow: direct creation on a freshly-approved requirement is blocked, the reason field gates the submit button, the action doesn't exist until the change request is approved, and it appears on the requirement immediately afterward. Found and fixed in the same pass: the spec's own first draft had a Playwright strict-mode violation (`getByText("Proposed action")` case-insensitively substring-matching the generated action title too) — not an app bug, but fixed rather than left flaky.

**Two genuine, pre-existing test-independence bugs found and fixed while verifying, neither caused by this item's diff but both only actually triggered by it — per this project's own "fix, don't defer" rule, fixed here rather than filed separately.** First full Playwright run (against a database a targeted 6-spec run had already touched, not a fresh reseed) surfaced 2 failures: `requirement-actions.spec.ts` and `requirements-and-cr-filters.spec.ts`. Root cause, confirmed by reading each failing seeded requirement's own version history via the API rather than assumed: `workflow-bypass-attempts.spec.ts` approves Alpha-1's whole "Scoping" stage baseline as part of its own scenario (a real, pre-existing, irreversible feature — no "unapprove stage" action exists) — this locks *every* requirement already in that stage at the time, including several other specs' seeded fixtures (`HW-FN-005`, `SW-PERF-006`, etc.) that were never touched by name and whose own doc comments called them "untouched by any other spec." That assumption was always fragile, but harmless before this item: direct action creation had no lock check at all, so a coincidentally-locked fixture didn't matter. Item 514's new 409 gate is what turns that latent fragility into a real, reproducible failure — first when a stage-locking spec happens to run before them in the same suite invocation, and (confirmed separately, since the project's own test-independence rule specifically requires passing "repeated back-to-back") on a second full-suite run against the same database even from a single clean invocation. Fixed at the two actually-broken specs, not at `workflow-bypass-attempts.spec.ts` itself (a much larger, riskier rewrite — moving it onto a fully throwaway project would also need its own role/persona setup for the several later steps that rely on Alpha-1's existing stakeholder/member personas — considered and deliberately not done this pass): `requirement-actions.spec.ts` now creates two throwaway, freshly-created requirements instead of relying on `HW-FN-005`/`SW-PERF-006` staying unlocked; `requirements-and-cr-filters.spec.ts`'s "status filter narrows the list" step now asserts against a throwaway draft requirement instead of `HW-FN-005` for the "not approved" side of the check (the "approved" side already used the seed script's own directly-and-permanently-locked `HW-FN-001`, unaffected either way). Both re-verified passing **against the already-stage-locked database** (the exact scenario that broke them) before the final clean-slate run below.

**Final full-suite verification, clean slate.** `docker compose down -v && up -d --build` (backend, and a `--no-cache` frontend rebuild after Docker's own layer cache incorrectly reported the `COPY . .` step unchanged despite genuinely modified source — verified by comparing output bundle hashes before/after), then both seed scripts run fresh (`seed_demo_data.py` genuinely exercised the new ADD_ACTION path for real, not just against mocks — see the seed-script fix below), then one full Playwright run: **87/87 passing**, up from the pre-existing 85/87 baseline (both new failures were the two bugs above, both real and now fixed, not flakes).

Files changed: `backend/app/models/enums.py`, `backend/app/models/change_request.py`, `backend/app/schemas/change_request.py`, `backend/app/routers/change_requests.py`, `backend/app/routers/requirements.py`; `backend/alembic/versions/0013_add_action_change_request_kind.py` (new); `backend/tests/test_requirement_action_change_requests.py` (new); `backend/scripts/seed_demo_data.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/RequirementDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ChangeRequestDetailPage.tsx` (+ `.stories.tsx`), `frontend/src/testing/storybook-helpers.tsx`; `tests/playwright/tests/e2e-workflows/requirement-action-change-request.spec.ts` (new), `requirement-actions.spec.ts`, `requirements-and-cr-filters.spec.ts` (both pre-existing, unrelated test-independence bugs found and fixed); `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

---

## UX audit roadmap item 522: `OrgGroup.granted_org_role` — SSO group→role mapping becomes a per-group property

**A real design conflict, flagged rather than resolved unilaterally, same as item 514 above.** The roadmap item's own text already named the open question — "needs a decision on migration handling for existing `sso_group_mappings` data" — so this was surfaced explicitly rather than picked silently: the user chose to proceed with the real schema change and migration, as scoped by the audit's own corrected finding (see [SSO group mappings, and Integrations and Security's own growing wall](ux-audit-2026-08.md#sso-group-mappings-and-integrations-and-securitys-own-growing-wall)).

**Design, extending existing precedent (`OrgGroup.idp_synced_group_name`'s own membership-sync mechanism) rather than inventing a new one:** a new nullable `OrgGroup.granted_org_role` column, meaningful only alongside `idp_synced_group_name` — enforced at the router layer (`_require_granted_role_has_sync_target`, both `create_org_group` and `update_org_group`), not a DB constraint, matching how `OrgGroup`'s existing IdP-sync validation is already done there rather than in the schema. `Organization.sso_group_mappings` (a flat JSONB list of `{sso_group, org_role}` with no relationship to any real `OrgGroup`) is retired entirely — model column, schema fields (`SsoGroupMapping`, `OrgAdvancedSettingsOut/Update.sso_group_mappings`), and the advanced-settings GET/PUT handling all removed. `services/oidc_provisioning.py::sync_org_roles_from_claims` rewritten to query `OrgGroup`s with both `idp_synced_group_name` and `granted_org_role` set, matched against the IdP's `groups`/`roles` claim — the same "sync down" (revoke once the matching claim disappears) and "vocabulary scoping" (never touch a role outside what these groups could grant) behaviour preserved unchanged, just reading from the new source.

**Migration decision, made explicit rather than assumed:** `granted_org_role` is a single nullable column, not a list — an org that (unusually, confirmed by grep not exercised by any existing test or seed data) mapped the same `sso_group` string to two different roles across two separate `sso_group_mappings` entries can only carry one forward on migration; the later entry in stored array order wins deterministically. Backfill (migration `0014_org_group_granted_role.py`, guarded the same way 0012/0013's backfills are — only a pre-0014 database has `sso_group_mappings` to read): for each existing mapping entry, reuse a matching `OrgGroup` (same `idp_synced_group_name`) if one already exists, else create a new one named after the SSO group string.

**Frontend:** the old flat "SSO group mappings" list/add/remove UI in the OAuth/SSO resource-menu group is retired in favour of the granted-role field sitting right next to `idp_synced_group_name` in the Groups section's own per-group form (matching the audit's own "surfaced right next to the existing field" instruction) — one combined "Save sync settings" button now saves both together (a role only makes sense alongside a sync name, so splitting them into two separate save actions would let the UI momentarily represent the invalid "role with no sync target" state the backend itself rejects). The role select only renders once SSO is actually configured for the org (`ssoConfig.oidc_issuer_url && ssoConfig.oidc_client_id`, not merely the settings payload having loaded — the audit's own correction to the pre-existing, too-loose gate), showing an explanatory hint instead when it isn't.

**A real, opportunistic fidelity improvement made while touching `org_export.py` for the mandatory `sso_group_mappings` removal, not a scope expansion for its own sake:** org bundle export/import's `org_groups` entries never carried `idp_synced_group_name` at all before this change (a separate, pre-existing gap — the old `sso_group_mappings` export/import was the *only* half of this feature that survived a bundle round trip, and the half that actually identifies which `OrgGroup` a mapping belongs to didn't). Retiring `sso_group_mappings` without also exporting `idp_synced_group_name`/`granted_org_role` on `org_groups` itself would have made SSO group→role mapping information disappear from bundle export/import entirely, a real regression, not neutral. Both fields now round-trip through `org_groups_json`; the merge-into-existing-org path (`merge_org_bundle`) skips (with a warning, matching this same function's existing pattern for a banned member or a cycle-forming nested-group edge) a bundle group whose IdP-sync name is already claimed by a different group in the target org, rather than letting the partial-unique-index `INSERT` fail outright.

**Two smaller, pre-existing doc/comment inaccuracies found and fixed while touching this code, not left as drive-by findings:** `Organization.sso_group_mappings`'s own model docstring described its JSON shape as `{"claim_value": ..., "org_role": ...}` — the actual, always-correct key used everywhere at runtime (`oidc_provisioning.py`, `orgs.py`) is `"sso_group"`, confirmed by grep before relying on the docstring. `User.ui_preferences`'s own docstring cited `sso_group_mappings` as its "structured but open-ended, rarely queried" precedent — repointed at `ReportTemplate.chapters`, an equivalent JSONB example that still exists, rather than left dangling after the field it referenced was deleted.

**Deliberately not touched, named rather than silently dropped:** a genuine "one IdP group grants multiple org roles" case has no representation in the new single-column model — flagged in the migration's own docstring as a real, narrow behaviour change for that (unexercised) edge case, not a bug being papered over.

**Verified.** Backend: `python -m pytest` — full suite **568 passed, 7 failed**, the same 7 pre-existing, mailhog-DNS-only failures confirmed throughout this session, none touching anything this item changed. `test_schema_migrations_match_models.py` passing, confirming migration `0014` matches the models exactly. Rewrote all 5 of `test_oidc_provisioning.py`'s `sync_org_roles_from_claims` tests onto the new `OrgGroup`-based fixture (mirroring `sync_org_groups_from_claims`'s own existing test pattern) — grant/no-match/sync-down/vocabulary-scoping/no-claim-at-all behaviour all still pass unchanged. Cleaned up 5 other test files' now-dead `"sso_group_mappings": []` payload keys and one real assertion rewrite (`test_engagement.py`'s advanced-settings roundtrip test). Frontend: `npx tsc --noEmit`/`npx eslint .` clean; Storybook — `SsoMappingsSave` replaced with `GroupsSectionSaveIdpSyncAndGrantedRole` (asserts the combined PATCH) and a new `GroupsSectionGrantedRoleHiddenWithoutSso` (the visibility-gate case) — both new stories plus the existing OrgAdminPage/ProjectAdminPage suites (58 tests) green.

**Full clean-slate Playwright verification, including the real Keycloak SSO flow this item's own core logic runs through.** `docker compose down -v && up -d --build` (+ a `--no-cache` frontend rebuild — Docker's layer cache incorrectly reported `COPY . .` unchanged a second time this session despite genuinely modified source; always verify by comparing output bundle hashes, don't trust the cache-hit alone), both seed scripts, then Playwright. `sso.spec.ts` (`tests/e2e-workflows/sso.spec.ts:61`, the group→role setup step) rewritten from a raw `PUT .../advanced-settings` with `sso_group_mappings` to a `POST .../groups` with `idp_synced_group_name`/`granted_org_role` together — the real authorization-code flow against the actual Keycloak instance in `tests/container` then provisions the role through the new mechanism for real, not against a mock. A first run (against a database a prior *targeted, non-reseeded* 6-spec batch had already touched, not this item's own fault) surfaced two failures in *other* specs entirely unrelated to this item's diff — both investigated, confirmed genuine and pre-existing, and fixed rather than deferred, per this project's own rule:
- `requirement-actions.spec.ts` and `requirements-and-cr-filters.spec.ts` (already covered under item 514's own entry above, discovered in that item's verification pass) — not re-caused here, just re-confirmed still fixed.
- `ProjectHistoryPage.tsx`'s date-range filter had no request-cancellation guard: an out-of-order `/changes` response could resolve after a newer one and silently overwrite it (`project-history.spec.ts`'s "filtering to a future date range" step, ~25% flaky in isolation before the fix, 0/9 flaky after). Fixed with a `requestGeneration` ref bumped on every `reload()` (a real filter/project change, not a `LoadMoreButton` append) — a response is only applied if it's still answering the latest generation by the time it resolves. No existing `AbortController`/cancellation pattern existed anywhere in the codebase to reuse; this is the first one, scoped to this one page rather than introduced as a new cross-app pattern in the same pass.
- `project-admin-templates-and-reports.spec.ts`'s "select it as Gamma-1's default report template" step called `page.reload()` immediately after clicking "Save settings" with no wait for the save's own `PUT .../report-config` to land — confirmed the backend persists correctly via a direct API call, so this was a pure test-side race (the same shape `preferences-and-theme.spec.ts` and others in this suite already guard against elsewhere), fixed the same way.

One further failure, investigated and *not* fixed, named rather than silently ignored: `report-image-attachment.spec.ts` (RichTextEditor's image-insert flow — untouched by this item's diff) failed twice, both times only as part of a long, multi-run full-suite context late in this session; **4/4 passing** when run in isolation immediately after, at normal speed (~3.5s vs. the failing runs' much longer ones). Treated as load-sensitive flakiness from an unusually long single session (many consecutive Docker rebuilds and 6–47-minute full-suite runs), not a reproducible bug — its own backend-side embed-into-PDF-bytes assertion is independently covered by `backend/tests/test_report_images.py` regardless. Final single-reseed, single-run verification: **86/87 passing** (only this one flake); the eight-spec failure batch seen in an intermediate, non-reseeded rerun was confirmed to be state pollution from this session's own repeated isolated-spec invocations against the same long-lived database, not real bugs — gone entirely once reseeded fresh.

Files changed: `backend/app/models/organization.py`, `backend/app/models/user.py` (stale docstring reference only), `backend/app/schemas/org.py`, `backend/app/routers/orgs.py`, `backend/app/services/oidc_provisioning.py`, `backend/app/services/org_export.py`; `backend/alembic/versions/0014_org_group_granted_role.py` (new); `backend/tests/test_oidc_provisioning.py`, `test_engagement.py`, `test_org_require_2fa.py`, `test_personal_access_tokens.py`, `test_org_rename_and_test_email.py`, `test_org_export_import.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/OrgAdminPage.tsx` (+ `.stories.tsx`), `frontend/src/pages/ProjectAdminPage.stories.tsx`, `frontend/src/pages/ProjectHistoryPage.tsx`; `tests/playwright/tests/e2e-workflows/sso.spec.ts`, `project-history.spec.ts`, `project-admin-templates-and-reports.spec.ts`; `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Deep hardening pass on Stage3 ("do a deep hardening pass")

Requested explicitly against the whole `feature/ui-improvement-stage-3` branch (commit `7c656c4` vs. `main`'s `4b1c556`, 132 files) rather than a single preceding item — scoped to that commit's actual new surface, per this project's established practice: `ADD_ACTION` change requests (item 514), the standalone `POST .../approve` endpoint, `active_only`/`sort` filtering on change requests, and `OrgGroup.granted_org_role` replacing `Organization.sso_group_mappings` (item 522, previous entry above). Identify → verify → remediate: read every changed backend router/model/service/schema line directly (not delegated) for authorization/tenant-isolation and injection/secrets/robustness issues, then a subagent covered the frontend lane (new `ResourcePickerModal`/`ActionMenu`/`FileUploadTrigger` components, `OrgAdminPage`/`PreferencesPage`/`ProjectListPage`'s large diffs, client-side-only authorization assumptions) — its one finding was independently re-verified by reading the actual backend endpoint before being trusted, per this project's standing rule never to act on a subagent's word alone.

**1. Found and fixed: a malformed `granted_org_role` in an org export bundle 500'd the whole import instead of degrading gracefully.** `_import_org_groups` (`backend/app/services/org_export.py`) parsed a bundle group's `granted_org_role` with a bare `OrgRole(granted_org_role)` call — unlike every other piece of untrusted bundle content in the same function (an `idp_synced_group_name` collision, a cycle-forming nested edge, a banned member — all skipped with a warning). Since an org bundle is just a zip file the calling org admin constructs (or received from elsewhere), a bad value there — hand-edited, corrupted in transit, or produced by a future version this one doesn't understand — raised an uncaught `ValueError` past the point where `_parse_org_bundle`'s own malformed-bundle handling could catch it, 500ing `POST /orgs/import` and `POST .../import/merge` instead of reporting a row-level warning like the sibling cases. Fixed by wrapping the parse in `try`/`except ValueError`, adding a warning and dropping just that group's role (same pattern as the sibling cases). New test: `test_org_import_merge.py::test_merge_skips_an_invalid_granted_org_role_with_a_warning_instead_of_500ing`.

**2. Found and fixed: `granted_org_role` was readable by any org member, not just admins — a real narrowing regression from the previous entry's own refactor.** `GET /orgs/{id}/groups` (`list_org_groups`) is authorized for `ORG_ADMIN, PROJECT_CREATOR, MEMBER` — deliberately broad, since a plain member needs group names/ids for the org-group nesting picker elsewhere in the app — and the previous entry added `granted_org_role` to every group in its response without reconsidering that authorization boundary. Before that refactor, the equivalent data (`Organization.sso_group_mappings` — which SSO group grants which org role) lived only behind `GET .../advanced-settings`, `ORG_ADMIN`-only. The result: any org member could call this endpoint directly (or, in principle, a future UI element could render it — the one existing call site, `OrgAdminPage.tsx`'s edit `<select>`, happens to also be gated behind an `ORG_ADMIN`-only `sso-config` fetch, which is incidental UI behavior, not a security control) and learn exactly which SSO group auto-grants `org_admin` on sync — privilege-configuration reconnaissance a plain member shouldn't have. Read-only: mutating still correctly requires `ORG_ADMIN` on both `create_org_group` and `update_org_group`, unaffected. Fixed by masking `granted_org_role` to `None` in the response for a non-`ORG_ADMIN` caller (`is_admin = OrgRole.ORG_ADMIN in get_effective_org_roles(...)`), the same field-masking pattern this router already uses elsewhere (`search_org_users`'s `may_see_existing` gate on external-user-match results) — a field mask on an intentionally-broad endpoint, not a new authorization tier. New test: `test_oidc_provisioning.py::test_granted_org_role_is_masked_from_a_plain_member_but_visible_to_an_org_admin`.

**Reviewed and found adequate, no changes needed:** `approve_requirement`'s new standalone endpoint (project-manager gate matches `decide_change_request`'s own identical check exactly; `REQUIRES_APPROVAL_STATUSES = {DRAFT, REVIEWED}` is the exact complement of `LOCKED_STATUSES`, so the two guards can't disagree). `ADD_ACTION` change-request creation and decision (`create_change_request`/`decide_change_request` in `routers/change_requests.py`) — `proposed_action_link_id`/`proposed_action_type_id` validated project-scoped at both creation (`get_requirement_action_in_project`, a 404-on-cross-project IDOR guard) and re-validated independently at approval time (mirroring the existing attachments re-check just above it), matching this codebase's established "don't trust submission-time state at decision time" pattern. `link_action`/`create_and_link_action` in `routers/requirements.py` now correctly reject once a requirement is locked (previously a genuine bypass of the change-request-only rule, closed as part of item 514 itself, not this pass). `list_change_requests`'s new `active_only`/`sort` params — `sort` is regex-constrained (`^(proposed_name|status|created_at)$`), no injection surface. `OrgGroupCreate`/`OrgGroupUpdate`'s `granted_org_role` requiring `idp_synced_group_name` is enforced identically on both create and update via the shared `_require_granted_role_has_sync_target` helper; only an existing `ORG_ADMIN` can ever set it, and the role vocabulary (`OrgRole`) tops out at `org_admin` within org scope — no escalation beyond what that admin could already grant by hand via `assign_org_role`, consistent with the "bundle import can only grant what the target org's own admin could already grant" acceptance established in the org-merge hardening pass above. `sync_org_roles_from_claims`'s replacement plumbing (role-granting groups queried fresh per call, `is_banned`/`is_active` still checked upstream in `auth_oidc.py` before it ever runs) — no change in invariant from the previous entry's own verification. Migrations `0013`/`0014` — additive/nullable columns, backfill logic's "later mapping wins" tradeoff is pre-existing and explicitly documented in the migration's own docstring, not something this pass needed to revisit. Frontend: `ResourcePickerModal`'s resource search and requirement-attachment linking (server-side `organization_id`/`project_id` checks confirmed on both the search and link endpoints); `FavouritesContext`, `ActivityPanel`, `ProjectHistoryPage`, `ProjectListPage`'s `favorite_only` filter (all reuse pre-existing, already-scoped endpoints, no new surface); `PreferencesPage`'s "Leave organisation" (self-service only, `current_user`-scoped server-side); `FileUploadTrigger` (a thin styled-button wrapper with no client-side type/size logic to bypass — `backend/app/services/files.py` has no type/size enforcement either, but that's a pre-existing gap the new component doesn't worsen, out of this pass's scope).

**Verified.** Backend: full suite **577 passed**, 0 failed, against a fresh rebuild with both fixes and their new tests in place. Targeted Playwright (not the full suite, given this pass's changes are backend-only and narrowly scoped): `org-group-nesting.spec.ts`, all 4 real-Keycloak `sso.spec.ts` specs, `org-admin-project-statuses-and-link-types.spec.ts`, `project-admin-groups-and-fields.spec.ts` — 8/8 passing against a freshly reseeded database.

Files changed: `backend/app/routers/orgs.py`, `backend/app/services/org_export.py`; `backend/tests/test_org_import_merge.py`, `test_oidc_provisioning.py`; `docs/decisions.md` (this entry).

## PR #12 CI failures: a real ruff lint error, then a real OrgAdminPage.tsx race (not a test-timing issue)

PR #12 (`feature/ui-improvement-stage-3`) failed CI three times in a row; each investigated rather than just re-run blind.

**1. Fixed: `backend/tests/test_org_import_merge.py:326`, an `E501` (line too long, 161 > 150) in the new `test_merge_skips_an_invalid_granted_org_role_with_a_warning_instead_of_500ing` test's manifest dict** (added in the Stage3 hardening-pass entry above). Every backend pytest test passed (100%) — this was pure lint, but `ci.yml`'s `Lint backend (ruff)` step has no `continue-on-error`, so failing it skipped every later step (mcp-server lint, dataset reseed, mcp-server tests, and the entire Playwright E2E suite) straight to the `if: always()` reporting steps; `Publish E2E test results` then failed too, but only as a secondary symptom of `playwright-report.xml` never having been generated. Fixed by wrapping the dict onto multiple lines; re-verified with `ruff check .` and the module's own tests against the live dev/test stack.

**2. First attempt, wrong diagnosis: a CI-only "render timing" theory for `org-security-controls.spec.ts`'s org-wide-2FA-enforcement assertion, mitigated by bumping its timeout — disproven by a third CI failure.** After two identical CI failures at the same assertion, a direct backend API call and an instrumented Playwright run both confirmed the 403/"2FA required" UI worked correctly when reproduced natively on this Mac (6+ passing attempts, including two full 87-spec runs), so this was provisionally attributed to a residual gap between `page.waitForResponse` observing the network response and the SPA actually repainting, mitigated by widening that one assertion's timeout to 15000ms. **This was wrong.** A third CI run failed at the *same* assertion after 17.6s — the full 15000ms timeout was consumed, which is far more time than a repaint could plausibly need, ruling out a rendering-speed explanation and pointing at a real, deterministic bug instead.

**3. Actual root cause, found by reproducing CI's exact execution path locally instead of native Playwright.** The one variable the "6+ passing" local runs hadn't controlled for was *how* Playwright itself runs — CI drives the suite from inside a throwaway `docker run --rm --network host ... mcr.microsoft.com/playwright` container (see `ci.yml`'s "Run Playwright E2E suite" comment), not a native process. Running the exact same spec that way, against a freshly reseeded stack, reproduced the CI failure locally on the first try — and reproduced it again with instrumentation added. Logging every `/advanced-settings` and `/projects/*` request showed the actual bug: the PUT that's supposed to save `require_2fa: true` was sending **`require_2fa: false`** — the toggle's own click had registered (`aria-checked` genuinely flipped to `"true"`), but flipped back to `"false"` again about 50ms later, before Save was clicked.

The cause is `OrgAdminPage.tsx`'s `reload()` — a single giant function, called after *every* mutating action anywhere on this page (including `toggleDisplayNameLock`, exercised by this same spec's immediately-preceding "lock then unlock a display name" step), that unconditionally re-fetches and overwrites all nine advanced-settings-derived fields (`smtp_*`, `pat_max_lifetime_days`, `require_2fa`, `allow_self_signup`, `auto_accept_email_domain`, `external_user_policy`), including from calls the calling action never awaits. `toggleDisplayNameLock`'s own `reload()` (itself doing ~10 sequential/parallel fetches: users, groups, advanced-settings, sso-config, scim-status, projects, pats, ...) can still be in flight by the time the *next* test step edits and saves that same form — its `advanced-settings` GET, still carrying the pre-edit value, resolves after the toggle click but before Save, clobbering the user's unsaved edit back to the last-saved value. This is architecturally the same class of bug as `ProjectHistoryPage.tsx`'s out-of-order-response race fixed earlier in this project's history (see that entry above) — a slow background refresh overwriting newer local state — except here the "newer state" is an *unsaved form edit*, not a second fetch's own result, and the race window is wide enough (spans an entire preceding test step, not one field's debounce) that it only shows up under CI's slower container-to-container networking, never on this Mac's fast native runs.

Fixed with `advancedDirtyRef`, a ref set by every one of the nine fields' own `onChange` handlers and checked by `reload()`'s advanced-settings block: if the user has an edit pending, a `reload()` in flight from an unrelated action skips re-populating those fields entirely rather than overwriting them. Cleared once `saveAdvanced()` succeeds, so a later, legitimate `reload()` resumes populating the form normally. `org-security-controls.spec.ts`'s assertion itself needed no change once the real race was closed — its 15000ms timeout (from the disproven attempt above) was reverted back to the default.

*Aside, not this item's bug but discovered and fixed while investigating it:* manually driving the Gamma org admin persona through this same enable/disable-org-2FA sequence via direct API calls (to reproduce independently of the test harness) left that persona locked out of its own org-admin endpoints partway through — `require_org_role` calls `_require_org_2fa` for *every* org-scoped endpoint including `advanced-settings` itself, so an admin without personal 2FA who enables the org-wide requirement immediately loses the ability to turn it back off via the API too, exactly as the real spec's own "self-service way out" step (enrol personal 2FA first, `/auth/2fa/enroll` isn't org-scoped) already accounts for. Not a bug — confirmed this is the documented, intended design — but a real trap for any future manual/API investigation of this feature that skips that step, and one that cost real time here since it wasn't recognised on the first two encounters (each read as "database got left in a broken state," requiring the same enrol → verify → disable-org-requirement → disable-personal-2FA recovery sequence the spec itself performs, before local reproduction could proceed); recorded here so it doesn't cost someone else the same detour.

**Verified.** `ruff check .`: clean. `pytest tests/test_org_import_merge.py`: 11 passed. `npx tsc --noEmit` / `npx eslint src/pages/OrgAdminPage.tsx`: clean. `org-security-controls.spec.ts` run via the exact `docker run --network host` mechanism CI uses — the only reproduction that had ever reliably failed pre-fix — **3/3 passing** against three independent fresh reseeds, plus native-Playwright confirmation. Broader regression check across `OrgAdminPage.tsx`'s other consumers (`org-rename-and-test-email.spec.ts`, `org-admin-project-statuses-and-link-types.spec.ts`, `org-branding-override-reset.spec.ts`, `user-directory-and-bans.spec.ts`, `user-access-panel.spec.ts`, `org-group-nesting.spec.ts`, `single-org-admin.spec.ts`, `multi-org-admin.spec.ts`) — one unrelated failure (`user-directory-and-bans.spec.ts`, the seeded orphan persona found `is_active: false`) traced to this same session's own earlier full-suite runs leaving mutated state behind (the `_e2e_dataset.py`/`seed_demo_data.py` scripts create-if-missing rather than resetting existing rows — harmless in CI's always-fresh database, a local-only trap from reusing one long-lived dev Postgres volume across many manual cycles in one long session); fixed by reactivating the persona directly, re-ran green, not a regression from this fix.

Files changed: `backend/tests/test_org_import_merge.py` (line-length fix); `frontend/src/pages/OrgAdminPage.tsx` (`advancedDirtyRef` guard — the real fix); `tests/playwright/tests/e2e-workflows/org-security-controls.spec.ts` (comment only, timeout reverted to default); `docs/decisions.md` (this entry).

**Follow-up, prompted by "could this same style of race be an issue anywhere else?": audited every other page with the same giant-`reload()`-called-from-everywhere shape, and it was.** Grepped every page for a `reload()`/`function reload` and checked whether any of its unconditional `setXxx` calls fed an editable input rather than a read-only list/display, and whether `reload()` was reachable from an action other than that same field's own save (the precondition for the race to be possible at all, not just theoretical). Four more confirmed, all fixed with the identical `xxxDirtyRef` pattern (set on the field's own `onChange`, checked by `reload()`'s assignment, cleared on that field group's own successful save):

- **`ProjectAdminPage.tsx`** — the single largest exposure: **33** `reload()` call sites (stage/component/category CRUD, custom-field CRUD, action-type CRUD, group membership, ...), unconditionally overwriting three independently-saved field groups: project settings (name, summary, allow-member-CRs, is-template, visibility, status), terminology overrides, and report config (intro, chapters, appendices, default template). Three separate refs (`settingsDirtyRef`, `terminologyDirtyRef`, `reportConfigDirtyRef`), matching the three independent save actions, rather than one shared guard that would block an unrelated group's save.
- **`RequirementDetailPage.tsx`** — **21** `reload()` call sites (posting a comment, uploading a file, linking an action, ...), unconditionally overwriting the entire requirement edit form (name, reasoning, clarification, description, target stage, level, review date/lead/reviewer) plus its custom-field values, both submitted together by the same `save()`. Arguably the highest real-world-likelihood instance: a user mid-edit on a long description while a comment or file upload elsewhere on the same page resolves in the background would have silently lost that edit, with no error surfaced. `form`'s five-plus onChange call sites consolidated behind one `updateForm()` wrapper (sets the ref, then calls `setForm`) rather than repeating the two-line guard at each call site, given the volume; the custom-field-values onChange (submitted in the same `save()` PUT) shares the same ref.
- **`ActionDetailPage.tsx`** — **12** `reload()` call sites, same shape as the requirement page but for an action's title/description/type/assignee/due-date; `setOutcome()` (the outcome-transition action, which also PATCHes the current form fields alongside `outcome_status`) clears the ref on success too, since it's effectively a second save path for the same fields.
- **`ServerManagementPage.tsx`**'s branding tab — narrower exposure (only `uploadLogo`/`uploadLoginBackground` reach its `reload()` from outside the tab's own save), but the same shape: accent color, header title, org label singular/plural, email-footer fields.

Deliberately **not** treated as instances of this bug: the many other `reload()`-shaped functions across `ChangeRequestsPage.tsx`, `ChangeRequestDetailPage.tsx`, `ProjectActionsPage.tsx`, `RequirementsPage.tsx`, `ProjectListPage.tsx`, `ServerOrganisationsPage.tsx`, and `ProjectHistoryPage.tsx` (already fixed for an out-of-order-response variant of this same underlying class, see that entry above) — checked and confirmed their `reload()`-equivalents only overwrite read-only lists/detail-display state, not fields bound to an editable input a user could have an unsaved edit pending on.

**Verified.** `npx tsc --noEmit` / `npx eslint` on all five touched pages: clean. Regression batch — `org-security-controls.spec.ts`, `project-admin-templates-and-reports.spec.ts`, `project-admin-structural.spec.ts`, `project-admin-action-types.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `requirement-actions.spec.ts`, `requirement-approval.spec.ts`, `requirement-and-action-restore.spec.ts` (×2), `requirement-action-change-request.spec.ts`, `comment-editing-and-attachments.spec.ts`, `org-label.spec.ts`, `server-org-lifecycle.spec.ts`, `server-admin-scope.spec.ts` — **14/14 passing** against a fully wiped-and-rebuilt stack (`docker compose down -v`, matching CI's always-fresh volume exactly). An intermediate run of this same batch against this session's long-lived, heavily-reused local Postgres volume showed 6 unrelated failures (all "create an entity, then find it by name in the default list view" timeouts, including on `RequirementsPage.tsx` — a file this pass never touched); the clean run after the volume wipe confirmed these were this session's own accumulated test-data pollution (many hours, dozens of full-suite runs, one shared dev database never reset), not a regression — consistent with the `is_active`/`require_2fa` seed-script non-idempotency already noted above in this same investigation. `ActionDetailPage.tsx`'s own edit-form fields have no dedicated Playwright coverage of the specific field values (only its `reload()`-triggering side effects — comment/attach — are exercised, and pass); verified by code review against the identical, already-proven pattern instead.

Files changed (this follow-up): `frontend/src/pages/ProjectAdminPage.tsx`, `RequirementDetailPage.tsx`, `ActionDetailPage.tsx`, `ServerManagementPage.tsx`; `docs/decisions.md` (this entry).

## Org Users role picker: checkboxes → multi-select dropdown, and a real reload-race bug

Raised directly: "it was set so you could change a user's role in org users and was done as a set of checkboxes... this should be a dropdown list selector, as a) there are errors when you try to select another level, and b) it looks disgusting."

**Checked before changing anything: the checkbox grid was a deliberate design decision, not an oversight** — see decision 14 in the "Hierarchical projects" entry above: "per-row grant/revoke checkboxes on the Users table (independent toggles, not a single-select, since `UserOrgRole` allows a user to hold more than one org role simultaneously...)". Converting straight to a single-value `<select>` would have silently reversed that decision and made it impossible to grant a user more than one `OrgRole` at once through this screen — confirmed still true today: `UserOrgRole`'s unique constraint is `(user_id, organization_id, role)`, `get_effective_org_roles` (`rbac.py:201-208`) returns a plain `set[OrgRole]` with no normalisation (unlike `ProjectRole`'s own `_normalize`), and `assign_org_role`/`revoke_org_role` (`orgs.py:842-927`) operate on one role row at a time with no exclusivity check anywhere. Flagged to the user rather than assumed; asked directly whether to force single-choice or keep multi-select in a proper dropdown shape — chose the latter.

**Fix: `MultiSelectDropdown`** (`frontend/src/components/MultiSelectDropdown.tsx`, new) — a trigger button styled like every other `<select className="input">` in the app, showing checked roles as its summary text, that opens a `Popover`-hosted checkbox list on click. Reuses `Popover`'s existing positioning/focus-trap/outside-click logic, the same reuse `ActionMenu` and `SplitButtonTrigger` already make, rather than a fourth anchored-menu implementation. See `docs/ux-style-guide.md`'s new "Pattern: multi-select dropdown" for the full write-up and the "don't use this to fake a checkbox group into looking prettier when the field is actually exclusive" caveat. Wired into `OrgAdminPage.tsx`'s Users table Roles column in place of the always-visible three-checkbox stack; the self-row "can't revoke your own role" guard is unchanged in behaviour, now surfaced as a `title` tooltip on the disabled checkbox instead of a silent `aria-label`-only disable.

**The actual root cause of "errors when you try to select another level," found while investigating rather than assumed to be a backend validation rule (there is none — `assign_org_role`/`revoke_org_role` have no cross-role rule beyond the self-targeting block):** `grantOrgRole`/`revokeOrgRole` called the page's own giant `reload()` on every toggle — the exact same "one function re-fetches ~10 endpoints, called from everywhere" shape already fixed once in this file for a different symptom (see the "PR #12 CI failures" entry above, `advancedDirtyRef`). Here the consequence isn't a clobbered form edit, it's two-fold: `reload()`'s own `loadUsers(..., 0, false)` always re-fetches from offset 0, so toggling a role for any user past the first 30 (`USERS_PAGE_SIZE`) made that row vanish from the list immediately after a "successful" change; and nothing serialised two toggles fired close together (the natural motion of switching a role — check the new one, uncheck the old), so two overlapping ~10-request reload bundles could race, and a transient failure in either one replaced the whole page with a load-error state via `reload()`'s own `catch`. Not flagged by the earlier audit of this same pattern because that pass's own criterion was specifically "does this `reload()` overwrite an *editable form field*" — the Users table's `roles` column is read-only display state, not a form field, so it correctly fell outside that fix's scope; it needed its own, different fix.

Fixed by having `grantOrgRole`/`revokeOrgRole` update just the affected row's own `roles` array in local `users` state on success, instead of calling `reload()` at all — a single grant/revoke never changes anything else on the page, so there's nothing else to refresh.

**Tests updated to match:** `frontend/src/pages/OrgAdminPage.stories.tsx`'s `UsersSectionGrantAndRevokeRole`/`UsersSectionCannotRevokeOwnRole` stories now open each row's dropdown before asserting on its checkboxes (queried via `within(document.body)`, since `Popover` portals there); `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`'s "once granted org-level rights..." step now opens the target row's roles dropdown before clicking the grant checkbox. New `frontend/src/components/MultiSelectDropdown.stories.tsx` covers the component itself: closed-state summary text, opening to reveal all options, per-option toggle callbacks, outside-click close, a disabled option with its `title` tooltip, and the empty-selection label.

**Verified.** `npx tsc --noEmit` and `npx eslint` on all touched frontend files: clean. No backend changes, so no backend test run was needed. Full Storybook interaction suite (`npx vitest run --project=storybook`) — **540/540 passing**, including the new `MultiSelectDropdown.stories.tsx` (8/8) and the updated `OrgAdminPage.stories.tsx` role stories. Rebuilt and recreated the `frontend` container against the already-running local dev/test stack (`tests/container/`), then ran `project-hierarchy.spec.ts` directly (not the full suite) — **4/4 passing**, including the step that opens the new roles dropdown and clicks the grant checkbox inside it against the real backend.

Files changed: `frontend/src/components/MultiSelectDropdown.tsx` (new), `MultiSelectDropdown.stories.tsx` (new); `frontend/src/pages/OrgAdminPage.tsx`, `OrgAdminPage.stories.tsx`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`; `docs/ux-style-guide.md`, `docs/decisions.md` (this entry).

## Hierarchical (parent/child) projects

Requested directly: "enable the ability for a project to be a child of another project," for large programmes where the overall initiative has its own requirements but sub-components need their own, while still being manageable as a whole. `docs/requirements.md` is silent (not prohibitive) on project nesting — this is recorded here as an interpretation, not a requirements.md edit (which this project cannot make). Two existing entries above (`ActionTypeDefinition`'s project-scoping, and the "Requirement actions" ERD section) already flagged project-scoping as a deliberate seam left open for exactly this; this entry is what fills it in.

### Confirmed design decisions

1. **Unlimited tree depth**, cycle prevention at the application layer only (`would_create_project_cycle`) — no DB constraint can express "no cycle anywhere in this table's transitive closure," the same tradeoff `OrgGroupMember.member_org_group_id`'s nested-group self-join already accepted.
2. **Forward inheritance (parent → child) is a per-project mode, not a boolean**: `ProjectRoleInheritanceMode.NONE` (default) / `MIRROR_ALL` (every role a user holds on the parent, via the parent's own *direct* resolution, mirrors onto the child) / `MIRROR_ROLE` (+ `role_inheritance_filter_role`, one of `STAKEHOLDER`/`PROJECT_ADMINISTRATOR`/`PROJECT_MANAGER` — only that exact role mirrors) / `MEMBER_ONLY` (any role on the parent grants baseline `MEMBER` on the child). The chain walks upward through ancestors and breaks at the first ancestor whose own mode is `NONE` (or that has no parent). Entirely controlled by the **child's** own manager — the child's access boundary is what expands, matching the existing precondition for changing any other project setting.
3. **Reverse inheritance (child → parent) is controlled entirely by the parent**, as an explicit `ProjectMemberSource` list the parent maintains (`POST/DELETE /projects/{parent_id}/member-sources`), not a flag on the child. For each listed child, anyone holding *any* direct role there gets baseline `MEMBER` on the parent. **This is a deliberate correction from an earlier draft**, which had the equivalent as a boolean on the child (`share_members_with_parent`) gated by the child's own manage rights. The user caught this directly: "share_members_with_parent is incorrect as it would allow a child project admin to gain access to a parent when it maybe shouldn't be able to do this. it should be set on the parent, eg consume members from child projects in association with a list of projects." That shape let a low-privileged actor who merely manages some child grant that child's entire membership read access into a potentially confidential parent, with zero involvement from the parent's own manager. Moving the setting to a parent-managed list closes this structurally: no code path lets a child add itself to a parent's list — only someone who already holds `require_project_manage` on the *parent* can populate it. This generalizes cleanly to multiple levels: a grandchild's members reach the grandparent only if the intermediate child is in the grandparent's own list *and* the grandchild is in the intermediate child's own list — every hop up requires that hop's own consent, not just the first.
4. **Action-type fallback is always on**, independent of both RBAC mechanisms: a child with zero of its own `ActionTypeDefinition` rows resolves through its nearest ancestor that has any (`resolve_effective_action_types`, `services/project_hierarchy.py`). Accepted, recorded trade-off: this is a pure structural walk with no visibility check of its own, so a confidential parent's action-type *names* (not content) are visible to a broader-visibility child's audience. Action type names are low-sensitivity metadata; the always-on behaviour was confirmed, not reversed, once this was raised.
5. **Inherited access — from either direction — counts as real membership** for broadcast notification targeting (`get_project_member_user_ids`), consistent with the existing principle that an inherited grant is real access, unlike `ProjectVisibility.ORG_WIDE`'s baseline (which stays excluded, as before).
6. **Tree view is a third `ViewToggle` mode** (tiles/list/tree), shown only when the current page's visible project set actually contains a parent/child relationship (`showTreeOption`) — applied to both `ProjectListPage` and the Favourites page.
7. **C-U-08 ("a project must always have a manager") accounts for forward-inherited managers.** Only `MIRROR_ALL` and `MIRROR_ROLE`-filtered-to-`PROJECT_MANAGER` can ever contribute a manager to a child — `MEMBER_ONLY` and the member-source mechanism both cap at `MEMBER` by construction, so they can never affect this floor. Changing a project's mode away from one of those two, or reparenting/clearing its parent, is blocked if it would leave the project with zero effective managers — computed inside the same row lock the existing last-manager-removal guard already uses (`lock_project_for_update`), closing the immediate-transaction TOCTOU window; a concurrent change further up the ancestor chain in a separate transaction is an accepted, narrow residual risk, not fully closed.
8. **Creating a project supports setting its parent directly** in the create flow, and a project's own page has a discoverable "Add sub-project" entry point, not only a dropdown on the generic list-page create modal — raised directly: "need to make sure when adding a project you can add as a nested project otherwise the workflow may be difficult."
9. **Disabling/reducing inheritance offers converting currently-inherited access to direct roles first** (`POST /projects/{id}/materialize-inherited-access`), so people relying on an inherited grant don't silently lose access the moment a mode is turned off, a project is reparented away, or a parent stops consuming from a child — raised directly: "when inherritence is turned off, there should be a way to convert the current members to be direct prior to disabling." Idempotent (skips a user who already holds an equal-or-higher direct role), a one-time snapshot rather than an ongoing sync.
10. **Project admin views show, per user, whether access is direct or inherited, and how** — raised directly: "Project admin user views, should also note when users are inherrited and how they are inherrited." `GET /projects/{id}/effective-members` returns full provenance (`kind: direct | group | org_wide | forward_inherited | member_source_inherited`, plus `via_project_id`/`via_mode` where applicable) per user, since a user can have more than one source simultaneously (e.g. a direct `MEMBER` grant plus a forward-inherited `PROJECT_MANAGER`) — all sources are returned, not collapsed to one.
11. **Creating a child project doesn't require the org-level `ORG_ADMIN`/`PROJECT_CREATOR` role** — managing the intended parent is sufficient on its own — **but this must not become a way to bypass that gate entirely.** Raised directly, working through the actual attack: "is there a security issue where roles are inherrited from parent to child... Also when inherritence is turned off, there should be a way to convert..." led into asking directly whether a naive "project-manage is enough" design was safe, and the answer is no: create a child under a project you manage, then detach it (clear its own parent) to end up with an ordinary root project you were never actually permitted to create standalone. Closed with a `parent_required` flag: a project created via the relaxed parent-manage-only path is marked `parent_required=True` server-side (never client-settable) and can never have `parent_project_id` cleared to null unless the actor performing that specific detach currently holds `ORG_ADMIN`/`PROJECT_CREATOR` at the org level — checked live against the actor, not just the flag, so someone who later earns real org rights isn't permanently stuck. Reparenting to a *different* real parent is unaffected; only the "become parentless" transition is gated, since that's the only operation that can produce an unrestricted root project.
12. **Both sides can detach a relationship** — a child can already clear its own `parent_project_id` (managing itself is sufficient, unchanged); the parent also gets `DELETE /projects/{parent_id}/children/{child_id}` to detach a specific child from its own side (gated on managing the parent, not the child) — both paths funnel through the same `parent_required` check from decision 11. **Attaching to a new parent** (create or reparent) requires managing *both* the project being attached and the target parent — tightened from an earlier draft that only checked the actor could *view* the target parent, too weak given it exposes that parent's own displayed structure (tree view, "Parent of:" list) to a relationship its manager never agreed to.
13. **The relaxed child-creation path is an org-level toggle**, `Organization.allow_relaxed_child_project_creation`, default enabled — an org admin who wants the stricter "all project creation needs `ORG_ADMIN`/`PROJECT_CREATOR`" status quo can opt back into it. Surfaced as a checkbox on `OrgAdminPage.tsx`'s Security group.
14. **The org role-management UI/API gap this toggle exposes as a hard blocker was closed, not deferred.** `POST /orgs/{id}/users/{user_id}/roles` (`assign_org_role`, grants any `OrgRole`, `ORG_ADMIN`-gated) and `create_org_user`'s optional `role` field already existed in the backend — confirmed by reading `routers/orgs.py` in full before assuming a bigger gap existed, correcting an earlier draft of this plan that claimed a new org role needed to be invented. What was actually missing, confirmed by grepping the whole frontend for zero references to the grant endpoint: no UI ever called it, `createUser()` hardcoded `role: "member"` into a field the backend already varied, and there was no revoke counterpart at all. Turning decision 13's toggle *off* with no way to grant `PROJECT_CREATOR` through the UI would have been a dead end, not a real stricter option. Closed with a new `DELETE /orgs/{organization_id}/users/{user_id}/roles/{role}` (mirrors `assign_org_role`'s shape; blocks the caller from targeting their own account, the same invariant `deactivate_org_user` already uses — since the caller must already hold `ORG_ADMIN` and can't touch their own role via this endpoint, an org can never reach zero admins through it, by construction, with no separate count check needed) plus frontend wiring: a role `<select>` on the "New user" modal (member/project creator/org admin, via the existing `ORG_ROLE_LABEL` map), and per-row grant/revoke checkboxes on the Users table (independent toggles, not a single-select, since `UserOrgRole` allows a user to hold more than one org role simultaneously — its unique constraint is `(user_id, organization_id, role)`, not one role per user) — each row's own checkbox disables unchecking (not checking) for the logged-in admin's own row, mirroring the backend's self-targeting block.

### Design principle carried through the whole plan

Forward inheritance and the member-source mechanism are kept fully decoupled, and each is authorized entirely by the side whose access boundary actually expands:
- Forward (`role_inheritance_mode`): the **child's** boundary expands → authorized by `require_project_manage` on the **child** (unchanged existing precondition).
- Member-source (reverse): the **parent's** boundary expands → authorized by `require_project_manage` on the **parent** (decision 3's correction — the opposite side from the flawed first draft).

Both directions resolve using only a project's *direct* roles at each hop (`UserProjectRole` + `ProjectGroup` + nested-org-group + org-wide-visibility — the existing 4-source resolution, extracted as `_direct_effective_project_roles`/`_direct_project_member_ids`), never another project's already-inherited results. Walking the forward chain up only reads each ancestor's *direct* roles; walking the member-source chain down only reads each listed child's *direct* roles (plus, recursively, whatever that child has itself separately consumed). This prevents a sibling-project leak — e.g. child C1 listed in P's member-source list, then P forward-mirroring "all roles" into unrelated sibling C2, accidentally exposing C1's members to C2 — and avoids any mutual-recursion hazard between the two mechanisms. A dedicated regression test (`test_member_source_does_not_leak_into_forward_mirroring_sibling` and its inverse) pins this down; it is exactly the kind of thing a future "improvement" could reintroduce if this weren't written down.

**What a project's own manager can see about its parent, worked through explicitly because the answer isn't obvious:** a direct, non-inherited `PROJECT_MANAGER` of child C with `MIRROR_ALL`/`MIRROR_ROLE` enabled from parent P will see P's name (needed to sensibly manage C's own inheritance setting) and, via the effective-members provenance view, the identities/roles of P's role-holders currently mirrored onto C. This is not new leakage introduced by who added them — it is the direct, already-priced-in consequence of what `MIRROR_ALL`/`MIRROR_ROLE` mean, identical to what *any* manager of C would see, inherited-in or not, and it's exactly the fact the create/edit-time confirmation exists to make an informed, deliberate choice about. What this manager cannot get, regardless of how they came to manage C: P's actual content (requirements, change requests, files), any capability on P itself, or any way to add themselves to P — all independently gated by `require_project_view`/`require_project_manage` on P. The member-source case is symmetric and equally "live," not a snapshot: a brand-new direct member added to a project listed in a parent's member-source list immediately gains `MEMBER` on the parent with no further action from the parent's side, by design — matching the same "live, not a snapshot" resolution as forward inheritance.

### Privilege-exposure friction

Per the blast-radius analysis above: `MIRROR_ALL`/`MIRROR_ROLE` require confirmation before taking effect (create and edit flows), naming the parent and stating both consequences (the access grant, and the provenance visibility) — `MEMBER_ONLY` and adding a member-source entry need none, since both cap at baseline `MEMBER`, the same risk profile `visibility=org_wide` already has with no confirmation. This uses `ConfirmDialog` in its plain (not typed-confirmation) tier — the shipped shape is a real confirmation step but not the typed-name "tier 2" this plan's own drafting language called it; a reasonable simplification given the action is reversible (the mode can be turned back off) and not destructive the way a delete is.

### Corrections made during implementation, beyond what planning anticipated

Each caught by the new test suite or a later verification pass, not a change in the design's intent:

- **`add_member_source`'s `source_project_id` should not be restricted to the caller's own accessible-project set.** The anti-oracle reasoning that motivated restricting the parent-selector similarly didn't hold here: the value space is already constrained to `project_id`'s real structural children (verified server-side), and the caller already manages `project_id` itself — requiring them to *also* independently hold some role on the specific child defeats the mechanism's own purpose (a parent's manager must be able to consume members from any real child, including a brand-new one with no role structure yet). Fixed to rely solely on `require_project_manage(project_id)` + the structural child check.
- **`revoke_project_role`/`remove_project_group_member`'s C-U-08 check needed an `is_inherited_manager` exception.** Once `get_effective_project_managers` became inheritance-aware, `managers == {user_id}` could no longer safely mean "removing this direct role leaves zero managers" — a user who is *both* a direct manager of a project *and* an inherited manager of it (e.g. also manages its parent, which the project mirrors) would be incorrectly blocked from having their redundant direct role removed. Added `is_inherited_manager(db, user_id, project_id)`, gating both checks on `managers == {user_id} and not is_inherited_manager(...)`.
- **`deactivate_org_user`'s C-U-09 fallback loop had a real ordering bug once inheritance existed.** It used to delete-then-check one project at a time; since a project's effective managers can now be pulled from a *different*, not-yet-processed project in the same org, interleaving deletes with checks could let a project silently end up with zero managers if its only inherited manager's own role hadn't been deleted yet at check time. Fixed to a two-phase pass: snapshot managers-before across every project in the org first, delete all this user's roles, then run the fallback check per project.
- **`remove_child_project`'s route used `{parent_id}` as its path parameter name**, breaking `Depends(require_project_manage)` — that dependency looks for a path parameter literally named `project_id`; with no such name FastAPI treated it as a required query parameter (422 on every call). Renamed to `{project_id}` (documented inline as unintuitive but required).

### SOC 2 identify → verify → remediate checkpoint

Run via this repo's `/code-review` skill at high effort, scoped to the diff, after backend implementation and before frontend work — per this project's change-management policy for authorization-affecting changes. Six findings, verified against the actual shipped code:

1. **CONFIRMED, fixed — `ProjectOut` leaked `parent_project_id` unredacted from six endpoints** (`GET`/`PATCH /{id}`, `POST ""`, `PUT .../terminology`, `POST .../archive`, `POST .../unarchive`). `GET /{project_id}` is view-gated (any role), not manage-gated, so any project viewer — not just its manager — could learn a hidden parent's identity by fetching the project directly, even though `list_projects`/`/ancestors` correctly redacted it. The redaction rule had only ever been applied to `ProjectListItemOut`. Fixed with `_project_out_with_redacted_parent`, applied at all six return points, plus a new `parent_project_name` field on `ProjectOut`.
2. **CONFIRMED, fixed — `get_project_users_by_role` (change-request `PROJECT_MANAGER`/`STAKEHOLDER` notification targeting) wasn't inheritance-aware**, so a user who is a real, effective manager purely via `role_inheritance_mode` would silently miss change-request notifications despite genuine approval authority — the original implementation's own docstring conceded this gap rather than closing it, a fix-don't-defer violation per this project's standing rule. Extended with the same forward-chain (+ member-source, for `MEMBER`) walk `get_project_member_user_ids` already had; this also widened the base (own-project) resolution to include nested-org-group holders, correcting an inconsistency with `get_effective_project_roles`'s own resolution.
3. **CONFIRMED, fixed — `_has_member_source_access`/`_member_source_contributed_user_ids` recursed with no depth cap**, unlike every other unlimited-depth walk in `rbac.py` (all others iterative with `_PROJECT_INHERITANCE_ITERATION_CAP`). Since both mechanisms support unlimited tree depth and this walk runs on nearly every project-scoped request, a sufficiently deep member-source chain could raise `RecursionError` (500) instead of degrading gracefully. Rewritten as iterative BFS via a new shared `_member_source_frontier` helper, same cap as everywhere else.
4. **CONFIRMED, fixed — `get_effective_project_managers`'s `filter_role_override` was gated on `mode_override is not None`** instead of its own presence, harmless only because both current call sites always pass both together — a latent bug for any future caller wanting to override just the filter role. Added an explicit `filter_role_override_set` flag, mirroring the existing `parent_override_set` pattern.
5. **CORRECT AS DESIGNED, no change — this decisions.md entry didn't exist yet at review time.** Code comments throughout the change cited it in advance; intentional per this plan's own sequencing (docs land last, folding in the review's own outcome), not a defect.
6. **DEFERRED then closed — seed scripts didn't yet exercise hierarchy fields.** Valid per this project's seed-script-sync rule, deliberately sequenced to after the frontend existed; closed in the seed-script phase below, not silently forgotten.

### Frontend

`ProjectRoleInheritanceMode` type + `PROJECT_ROLE_INHERITANCE_MODE_LABEL` (per the style guide's Principle 12 — never render the raw enum string); new `ProjectAncestor`/`ProjectTreeNode`/`ProjectMemberSource`/`EffectiveMember`/`MaterializeResult` types. New shared components: `ProjectTree.tsx` (presentational, no fetch logic of its own, per-node "+" to add a child), `ProjectHierarchyLabels.tsx` ("Child of:"/"Parent of:" labels, shared between `ProjectListPage` and the Favourites page rather than duplicated, capped at 3 inline children + "view all"). `ViewToggle`'s `ViewMode` extended to include `"tree"`, gated by a new `showTreeOption` prop that hides the third button's rendering entirely (not just disables it) when the page's visible project set has no hierarchy relationship.

`ProjectListPage.tsx`/`ProjectAdminPage.tsx`: parent-project select + inheritance-mode select (+ filter-role select for `MIRROR_ROLE`) in the create modal and the Overview/"Project settings" tab respectively, both routed through the same confirmation flow for `MIRROR_ALL`/`MIRROR_ROLE`; `ProjectAdminPage.tsx` shows the current parent's name plainly (unlike the list/tile "Child of:" label, which redacts a parent the viewer can't see — a project's own manager already holds the highest level of authority over this relationship and needs to see it to do their job) and gained a "Member sources" section (list + add-dropdown, sourced from the project's own `GET .../children`) and an "Add sub-project" button navigating to `/projects?parentProjectId=X&organizationId=Y` (`ProjectListPage.tsx` auto-opens the create modal with both pre-filled via a `useSearchParams` effect, cleared immediately after so a later reload/back-navigation doesn't silently reopen it). `ProjectAdminPage.tsx`'s "Project groups" tab gained an "Effective members" `CollapsibleSection` (lazy-loaded, not part of the page's main `reload()`, since it iterates every org member server-side): shows each user's effective role plus every provenance source, and a "Convert all inherited access to direct roles" button.

### Real bugs found only by running the finished feature end to end, not by static review

All caught during a full Playwright verification pass against the real `tests/container` stack — none surfaced by `tsc`/`eslint`/Storybook, which is exactly why this project's testing policy requires running the actual stack rather than trusting type-checking and unit-level tests alone for a UI feature:

- **`ProjectListPage.tsx`'s create-modal parent-selector excluded the very project being selected as the parent.** `parentOptions = allProjects.filter((p) => p.organization_id === newOrgId && !p.is_archived && p.id !== parentProjectId)` — the `p.id !== parentProjectId` clause is `ProjectAdminPage.tsx`'s own "exclude the project currently being edited from its own parent-picker" pattern, copied without adjusting for the create form's different context: on create there is no "self" yet to exclude, and `parentProjectId` here is simply the *selected* parent's own id, not a self-reference to avoid. The effect: once "Add sub-project" pre-filled a parent (decision 8), that parent's own `<option>` never existed in the list at all, so the select could never actually display it as chosen — a real, user-facing bug in the flagship entry point this whole feature added, invisible to every prior verification pass because none of them checked the select's *displayed* value after a pre-fill, only that the field existed. Fixed by dropping the exclusion entirely on this form.
- **The new "Effective members" section's title collided with every project's own default "Members" group.** `DEFAULT_GROUPS` (`routers/projects.py`) seeds a `ProjectGroup` named "Members" on every project — with the new section also titled "Members" on the same "Project groups" tab, `openGroupCard`'s prefix-matching regex helper (`tests/playwright/tests/e2e-workflows/helpers.ts`) started matching both, breaking `org-security-controls.spec.ts`, an entirely unrelated pre-existing spec. Renamed the section to "Effective members" — also just a clearer label on its own merits, not solely a test-fixing move.
- **Two pre-existing Playwright specs assumed the "New project" modal had exactly one/a specific count of `<select>` elements** (`project-list-org-scoping.spec.ts`, `project-admin-templates-and-reports.spec.ts`) — both broke once the new "Parent project" select joined Visibility (and, when present, the template picker) in the same modal. Fixed by updating the expected counts and scoping the template-picker's option-count assertion specifically to that select (`getByLabel("Create from template")`) rather than an unscoped dialog-wide option match, since "Parent project" also lists every project in the org as an option and can now collide on shared project names.
- **`getByLabel`-based locators for "Visibility" became newly fragile, not from a code bug but from an interaction with existing test-data hygiene.** Once "Parent project" lists every non-archived project in the org as `<option>` text, an org that has accumulated a leftover project whose name happens to contain a field's own label word (here, debris from a prior interrupted run of `project-visibility.spec.ts` itself, never cleaned up because the run failed before reaching its own cleanup step) makes a non-exact `getByLabel("Visibility")` match two elements. Not treated as a locator bug to work around — treated as debris to clean up, consistent with how this project's own "cross-spec state pollution" findings elsewhere in this document are always resolved by reseeding rather than by hardening every locator against arbitrary leftover data. A reproduction of this ambiguity is a signal to reseed, documented inline at the one call site most likely to hit it again.

### Seed scripts

`backend/scripts/seed_e2e_dataset.py`: `create_project`/new `add_member_source` helper gained the hierarchy kwargs; a fixed **Gamma-3 Hierarchy Parent / Gamma-4 Hierarchy Child** pair demonstrates both directions from one small fixture (Gamma-4 mirror-all-inherits from Gamma-3; Gamma-3 also consumes members from Gamma-4) — dedicated solely to `project-hierarchy.spec.ts`, per this suite's own "don't mutate shared named seed data, use a dedicated fixture" convention (the same shape `TERMINOLOGY_PROJECT_NAME`/`delta1` already established). A new persona, `e2e-projectmgr-g@example.com` (Gamma member only, no org-level role, direct `PROJECT_MANAGER` on Gamma-1 and direct `STAKEHOLDER` on Gamma-3), exercises the relaxed parent-manage-only creation path and the `parent_required` bypass-closure block end to end, and demonstrates genuine forward-inherited (not creator-direct) provenance on Gamma-4, since `orgAdminGamma` is a direct manager of both Gamma-3 and Gamma-4 already via creation-time seeding and so can't demonstrate the "inherited, not direct" case itself.

`backend/scripts/seed_demo_data.py`: **Falcon-3 Avionics Subsystem**, a real sub-project of Falcon-3 Inspection Drone (`MIRROR_ALL` forward, plus Falcon-3 consuming members from it in reverse), with its own Sensors component/categories and three requirements — `demo.engineer` gets a *direct* `PROJECT_MANAGER` grant there specifically (distinct from their existing stakeholder role on the two root projects) so the demo shows a real, direct-vs-inherited distinction rather than only ever-inherited access.

### Verified

Backend: full suite **610 passed, 0 failed** inside `tests/container` (the 7 failures seen when run on the bare host are the pre-existing, already-documented `mailhog`-DNS-only gap — confirmed clean in-container, where DNS resolves). `test_project_hierarchy.py` (25 tests) covers cycle prevention, cross-org rejection, all three forward modes, chain-breaking, the member-source mechanism (including multi-level, the authorization-boundary regression test, stale-row behaviour after reparenting), `parent_required` bypass closure (including the granted-org-rights retry succeeding), the org toggle, and the SOC2-review regression tests; `test_action_types.py` extended with 5 fallback/inheritance tests; `test_coverage_gaps.py` extended with 3 tests for the new `revoke_org_role` endpoint (success + no-op re-revoke, self-targeting block, non-admin 403). `ruff check` clean throughout, including a cleanup of two lint findings (`test_project_hierarchy.py`'s unused variable and an unsorted function-local import block) caught by a final full-repo `ruff check` pass rather than left from incremental development.

Frontend: `npx tsc -b`/`npx eslint src/` clean (only the 7 pre-existing warnings in untouched files); Storybook **527/527** across 63 files (up from 461/60 at the start of this session's UX-audit-batch baseline), including new coverage for `ProjectTree`, `ProjectHierarchyLabels`, the tree-mode `ViewToggle` states, the parent/inheritance fields and member-sources/effective-members sections on `ProjectAdminPage`, and the org role-management UI on `OrgAdminPage`.

Playwright: new `project-hierarchy.spec.ts` (4 tests — create-via-modal with the `MIRROR_ALL` confirmation and cancel path, hierarchy labels + tree view, member-sources visible only from the parent's side with effective-members provenance and materialize, the relaxed-creation-path + `parent_required` detach block + org-rights-retry flow, and the org-toggle block) passes **4/4 repeated twice back-to-back against the same database with no reseed between runs** — the second run surfaced (and led to fixing) a real idempotency gap in the spec itself: `materialize-inherited-access` is a one-way, permanent grant, so the spec now reverts it via a direct API call at the end of its own test step, restoring the fixture to how it found it. A targeted regression batch of 25 tests across specs that share the touched surface (project list/admin, org admin, mobile card grids, favourites, org group nesting, `golden-path.spec.ts`, `pelion-v2.spec.ts`, and the two specs whose "New project" modal assumptions this feature changed) — **25/25 passing** from a genuinely fresh `docker compose down -v && up --build` reseed of both `seed_e2e_dataset.py` and `seed_demo_data.py`, the only verification standard this project's own methodology note (see the "Full-suite hardening" entry above) accepts as conclusive.

Files changed: `backend/app/models/project.py`, `organization.py`, `enums.py`, `__init__.py`; `backend/alembic/versions/0015_project_hierarchy.py` (new); `backend/app/schemas/project.py`, `org.py`; `backend/app/routers/projects.py`, `orgs.py`, `action_types.py`, `actions.py`; `backend/app/services/rbac.py`, `project_hierarchy.py` (new), `definitions.py`, `templates.py`; `backend/tests/test_project_hierarchy.py` (new), `test_action_types.py`, `test_coverage_gaps.py`, `conftest.py`; `backend/scripts/seed_e2e_dataset.py`, `seed_demo_data.py`; `frontend/src/api/types.ts`, `i18n/strings.ts`, `testing/storybook-helpers.tsx`; `frontend/src/components/ProjectTree.tsx` (+ `.stories.tsx`, new), `ProjectHierarchyLabels.tsx` (+ `.stories.tsx`, new), `ViewToggle.tsx` (+ `.stories.tsx`); `frontend/src/pages/ProjectListPage.tsx` (+ `.stories.tsx`), `ProjectAdminPage.tsx` (+ `.stories.tsx`), `FavouritesPage.tsx` (+ `.stories.tsx`), `OrgAdminPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts` (new), `helpers.ts`, `project-visibility.spec.ts`, `project-list-org-scoping.spec.ts`, `project-admin-templates-and-reports.spec.ts`; `docs/solution-architecture.md`, `docs/e2e-workflows.md`, `docs/decisions.md` (this entry).

## Deep hardening pass on `feature/multi-level-projects` ("please do a deep hardening pass")

Requested explicitly against the whole `feature/multi-level-projects` branch (commit `7403f85` "Nested Projects Compatibility" vs. `main`, 50 files) — the entire hierarchical-projects feature described in the "Hierarchical projects" entry above, landed as one branch. That entry's own "SOC 2 identify → verify → remediate checkpoint" ran *during* implementation, before the frontend existed, scoped to the backend diff at that point in time; this pass is a second, independent look at the finished branch as a whole, per this project's standing practice of treating a hardening-pass request as its own scoped review rather than assuming an earlier in-flight review already covered everything. Identify → verify → remediate: every changed backend router/model/service/schema line read directly (not delegated) for authorization/tenant-isolation/injection/robustness issues; a subagent covered the frontend lane (client-side-only authorization assumptions, raw-enum rendering, redaction correctness, confirmation-dialog coverage, the known reload-clobbers-unsaved-edit race class) — both its findings were independently re-verified against the actual current code before being trusted, per this project's standing rule never to act on a subagent's word alone.

**1. CONFIRMED, fixed — a real RBAC escalation: listing an `ORG_WIDE`-visible child as a member source granted every user in the organisation real `MEMBER` access to the parent, not just a read grant on the child.** `_has_member_source_access` (`backend/app/services/rbac.py`) — the per-user check backing `get_effective_project_roles`, the live authorization gate behind `require_project_view` and everywhere else project access is decided — resolved each candidate source project via `_direct_effective_project_roles`, which (correctly, for its own purpose) includes source 4, `ProjectVisibility.ORG_WIDE`'s baseline grant. But `_direct_project_member_ids`, the sibling helper every *other* member-source/forward-inheritance bulk resolution in this same module deliberately uses instead, excludes `ORG_WIDE` specifically to prevent exactly this — its own docstring already warned "if this included `ORG_WIDE`, an org-wide-visible ancestor would flow its *entire organisation's* membership through forward inheritance or the member-source mechanism into every notification and every 'does this descendant have a real member' check". `_has_member_source_access` was the one path in the module that still read through the wrong helper, so that warned-against side channel was live: an org's own docstring for `ProjectMemberSource` frames the mechanism as "consume members from this real child" — a project manager listing a merely-`ORG_WIDE`-visible child (a common, ordinary visibility choice, not a contrived edge case) would unknowingly grant baseline access to their entire organisation on a parent that might be far more sensitive, with no indication anywhere in the UI that this was happening. Fixed to use `_direct_project_member_ids` (mirroring `_member_source_contributed_user_ids`'s already-correct bulk resolution). New test: `test_project_hierarchy.py::test_member_source_does_not_leak_org_wide_visibility_as_membership` — confirmed to fail against the pre-fix code (temporarily reverted via `git stash` of just this one file, rebuilt, reran) before being confirmed to pass post-fix, not just written and trusted.

**2. CONFIRMED, fixed — a project manager with no independent view access to the true parent could silently detach their project from it on the next unrelated "Save settings" click.** Two compounding gaps, found together by the frontend subagent lane and independently verified against the backend:
   - `_project_out_with_redacted_parent` (`backend/app/routers/projects.py`) redacted `parent_project_id`/`parent_project_name` for *any* caller lacking independent view access to the parent, with no exemption for a caller who manages the child itself — contradicting `ProjectAdminPage.tsx`'s own inline comment and this very document's "Hierarchical projects" entry above ("a project's own manager already holds the highest level of authority over this relationship and needs to see it to do their job"), both of which describe the manager-sees-the-true-parent behaviour as already shipped. It wasn't: nothing ties managing a child to holding any role on its parent, so this was a realistic, ordinary case, not a contrived one.
   - `ProjectAdminPage.tsx`'s `saveSettings()` unconditionally includes `parent_project_id: parentProjectId || null` in every PATCH regardless of whether the field was touched — defeating the very protection `ProjectUpdate`'s `model_fields_set` distinction (documented in its own docstring) exists to provide.
   
   Combined: a manager who can't see their project's real parent loads the settings tab (sees "No parent" — an artifact of redaction, not truth), edits something unrelated, saves, and the real parent relationship is silently severed — no error, no confirmation, since decision 12 already permits a child to detach by managing itself alone, and `parent_required` doesn't apply to an ordinarily-created project. Fixed at the root: `_project_out_with_redacted_parent` now exempts a caller who manages the project itself (`can_manage_project_settings`), matching the documented intent — they already hold unilateral authority to detach or reparent this exact relationship via this same endpoint, so showing them the true value they can already change is not a new disclosure. This closes the practical risk without needing a second, frontend-side fix: `saveSettings()`'s payload becomes safe again once the value it reads is always the true one for anyone who can actually reach it (every caller of this endpoint is already `require_project_manage`-gated). New test: `test_project_hierarchy.py::test_get_project_shows_true_parent_to_a_manager_with_no_independent_parent_access`.

**3. CONFIRMED, fixed — reparenting an existing MIRROR_ALL/MIRROR_ROLE-configured project to a *different* parent did not re-trigger the required confirmation, on both the create and edit forms.** The parent `<select>`'s `onChange` in both `ProjectAdminPage.tsx` and `ProjectListPage.tsx` only ever called `setParentProjectId` — confirmation is wired solely to the inheritance-*mode* select's own `onChange`. Reproduction: a project already has parent P1 with `role_inheritance_mode=mirror_all` (confirmed and saved earlier, the dialog having named P1); an edit later changes only the parent select to P2, leaving the mode select untouched — no `ConfirmDialog` fires, and on save the project now mirrors *P2's* role-holders, a materially different access grant than what was ever confirmed, violating this document's own decision 4/"Privilege-exposure friction" requirement that MIRROR_ALL/MIRROR_ROLE always show a confirmation naming the actual parent. The create form has the identical gap (pick P1, get the P1 confirmation, switch to P2 before submitting — the create request grants access sourced from P2 with no confirmation ever having named it). Fixed conservatively rather than by extending the confirm-dialog state machine to track a second pending field: changing the parent select while an elevated mode is set resets `role_inheritance_mode` back to `none` (the safe default), so re-selecting MIRROR_ALL/MIRROR_ROLE for the new parent goes through the existing, already-correct `requestInheritModeChange` confirmation flow, which then genuinely names the new parent. No backend change needed — `update_project`/`create_project` were never the gap, only the frontend's failure to prompt before submitting an already-valid, already-accepted request shape.

**Reviewed and found adequate, no changes needed (backend):** `_accessible_project_ids`/`build_project_tree`/`get_project_ancestors`/`get_project_children` (all correctly re-verify each candidate's actual `get_effective_project_roles` before including it, rather than trusting structural adjacency — inherits finding 1's fix automatically once merged); `add_member_source`'s deliberate "no independent accessible-set check on the child" design (re-confirmed correct: the value space is already constrained to real structural children, and requiring independent access would defeat the mechanism's purpose); `remove_child_project`/`update_project`'s `parent_required` and C-U-08 guards (`get_effective_project_managers` override parameters) — re-verified the `filter_role_override_set`-gated-on-its-own-flag fix from the previous hardening pass is genuinely in place, not regressed; `_validate_action_type`'s cross-project `ActionTypeDefinition` FK reference (structurally scoped to the real ancestor chain only, no cross-tenant surface); `revoke_org_role`'s self-targeting block; the `0015` migration (additive/nullable, no backfill risk).

**Reviewed and found adequate, no changes needed (frontend, per the subagent lane, independently re-verified):** every `role_inheritance_mode`/`ProjectRole`/`OrgRole` display routes through its label map, no raw enum strings reach the UI; `ProjectHierarchyLabels`/`ProjectTree`/tree-mode `ViewToggle` all rely on server-redacted fields rather than re-deriving visibility client-side; the org role grant/revoke UI and the `allow_relaxed_child_project_creation` toggle are correctly backed by independent server-side checks, client gating is UX-only; the new hierarchy fields (`parentProjectId`/`roleInheritanceMode`/`roleInheritanceFilterRole`) are correctly guarded by `settingsDirtyRef` against the known reload-clobbers-unsaved-edit race class already fixed elsewhere on this page — finding 2 above is a different bug (wrong *initial* value from redaction, not a clobbered in-progress edit) — and MEMBER_ONLY/member-source add correctly need no confirmation, matching the documented risk parity with `visibility=org_wide`.

**Found and fixed, incidental to verification (not a security finding, but a real bug per this project's own "fix, don't defer" rule):** `org-admin-project-statuses-and-link-types.spec.ts`'s `getByLabel("Status")` assertion became ambiguous once this branch added a "Parent project" `<select>` to the same `ProjectAdminPage.tsx` tab. A wrapping `<label>{text}<select>...</select></label>`'s computed accessible name flattens in every descendant `<option>`'s text, not just the label's own words — this spec's own fixture creates two same-org sibling projects (`E2E Status Project A`/`B`), and `B`'s name legitimately appears as a `<option>` inside "Parent project", making that select's accessible name contain the substring "Status" too. Unlike the already-documented, already-accepted "Visibility" locator fragility elsewhere in this suite (a cross-run-debris case, whose own inline comment explicitly declines to fix it via locator complication, treating a reproduction as "a signal to reseed" instead — re-confirmed still accurate: a clean-slate rerun of `project-visibility.spec.ts` alone passed), this one reproduces from the spec's *own* legitimate, single-run fixture data, so reseeding cannot fix it. Fixed by locating the Status `<select>` structurally (the one whose own `<option>` list contains a status name) instead of via `getByLabel`, avoiding the ambiguity outright rather than complicating it further.

**Verified.** Backend: `ruff check app tests` clean; full suite green both before and after the fix (confirmed via a deliberate revert-rebuild-retest cycle isolating finding 1's fix, not just "tests pass" trusted at face value), and again green after finding 2's fix, against a freshly wiped+rebuilt `tests/container` database. Frontend: `npx tsc --noEmit`/`npx eslint src/` clean (only the same 7 pre-existing warnings in untouched files). Playwright: `project-hierarchy.spec.ts` (4/4), plus a regression batch across the surfaces this pass touched (`project-list-org-scoping`, `project-admin-templates-and-reports`, `project-admin-structural`, `project-visibility`, `favourites`, `org-admin-project-statuses-and-link-types`) — 9/9 passing from a genuinely fresh `seed_demo_data.py`/`seed_e2e_dataset.py` reseed after a full schema wipe, the only verification standard this project's own methodology accepts as conclusive (an earlier, non-reseeded repeat run of the same batch showed 2 unrelated failures, both confirmed to be this session's own repeated-run self-pollution — the same already-documented class of shared-database test fragility noted throughout this file — not regressions from this pass's changes).

Files changed: `backend/app/services/rbac.py`, `backend/app/routers/projects.py`; `backend/tests/test_project_hierarchy.py`; `frontend/src/pages/ProjectAdminPage.tsx`, `ProjectListPage.tsx`; `tests/playwright/tests/e2e-workflows/org-admin-project-statuses-and-link-types.spec.ts`; `docs/decisions.md` (this entry).

## PR #13 CI failure, a shared-admin-password test hazard, and the `can_be_parent` eligibility gate

Three pieces of follow-up work in one session, each starting from a direct report against PR #13 (`feature/multi-level-projects`).

### 1. CI E2E failure: an unrelated pre-existing spec broken by a new same-named element

`badge-filters.spec.ts` (untouched by this branch's own diff) does `getByRole("link", { name: "Falcon-3 Inspection Drone" }).click()` on the projects list. `seed_demo_data.py`'s new "Falcon-3 Avionics Subsystem" sub-project renders a "Child of: Falcon-3 Inspection Drone" link (`ProjectHierarchyLabels`) with the exact same accessible name as the parent's own tile heading link. Fixed with `.first()` — both links resolve to the identical `/projects/{id}` destination (the child's "Child of" link target *is* the parent's id), so either is a correct click for this test's purpose. Confirmed no other spec references "Falcon-3 Inspection Drone" or the Gamma-3/Gamma-4 fixture pair in a way that collides the same way.

**Two more pre-existing failures found and fixed while verifying the above, not deferred** (both were self-inflicted local-session pollution *and* real, separately-worth-fixing non-idempotency, confirmed by reproducing on a genuinely fresh reseed, not just re-running):
- `project-visibility.spec.ts`: `getByLabel("Visibility")` on `ProjectAdminPage.tsx` becomes ambiguous once "Parent project" shares the same page/modal and a leftover project's name happens to contain "visibility" — the spec's own existing comment already named this exact risk and explicitly declined to fix the locator, treating a reproduction as "a signal to reseed." Given this project's standing rule that a test "must pass whether it runs alone, first, last, or repeated back-to-back against the same database" (not only after a fresh reseed), fixed properly this time: a `visibilitySelect()` helper locates the `<select>` structurally (`.filter({ hasText: "Only specified" })`, a phrase that can never appear as a project name) instead of by label, closing the fragility at the root rather than deferring to "reseed when it happens." The identical `.filter({ has: page.locator("option", {...}) })` construct tried first for this timed out unreliably against a `<select>` inside a modal `Locator` (not `Page`) scope even though the exact same shape worked fine page-rooted elsewhere in the suite; switched to the simpler, more robust `.filter({ hasText })` form everywhere it's used, including retrofitting `org-admin-project-statuses-and-link-types.spec.ts`'s earlier `has`-based fix from the same session.
- `project-admin-structural.spec.ts` mutated the shared "Beta-2" fixture in place (renaming its default "Scoping" stage, deleting/recreating its components) — a second run without an intervening reseed can no longer find "Scoping" at all. Converted to a disposable, uniquely-named project it creates and seeds itself via direct API calls (mirroring `seed_e2e_dataset.py`'s own `seed_project_content` shape: Hardware/Software components, Functional/Performance categories), the same remedy `org-admin-project-statuses-and-link-types.spec.ts` had already applied to itself for the identical anti-pattern.

All three (plus the original CI failure) verified passing **twice back-to-back with no reseed between runs**, not just once.

### 2. `pelion-v2.spec.ts` was changing the real bootstrap admin's password

Raised directly: "there is a test that changes the password, can that not use the admin user, as I frequently can't login as I don't know what the password is." `pelion-v2.spec.ts` logged in through the UI as `admin@example.com`/`ChangeMe123!` — the actual shared account used for manual local testing — then changed its password mid-test to exercise the change-password-triggers-a-notification flow, wrapped in a try/finally that reverted it. Two real problems, not just the one reported:
1. The try/finally boundary was drawn *after* the forward change (`await page.waitForURL(/\/login$/)` sat outside the `try`) — once `changeResponse.ok()` is true the mutation has already committed server-side, so a flake in that very next line would skip the revert entirely despite the real account's password already having changed. This is almost certainly the actual mechanism behind "frequently can't login," not just theoretical.
2. Even a gap-free try/finally still leaves the same shared credential hostage to a browser crash or worker kill — no amount of finally-block correctness removes that risk.

Fixed by removing the dependency entirely rather than hardening the revert further: the spec now provisions its own disposable org + org-admin persona via direct API calls (`admin@example.com` is used exactly once, read-only, to obtain a bearer token for that provisioning — never through the UI login form, so this spec can no longer touch that account's real password under any failure mode) and runs its whole walkthrough, including the password-change exercise, against that throwaway persona. The try/finally boundary was also corrected to start before the forward change, as defense in depth for the persona itself. Verified: full local Playwright suite green, and `admin@example.com`/`ChangeMe123!` confirmed to still authenticate via a direct API call after the run.

### 3. `can_be_parent`: an opt-in eligibility gate for the "Parent project" picker

Raised directly, two complaints in one: "when creating a new project, you get the field 'Parent project' when there are none that can be seen by the user. This makes no sense and should not be shown if there are none eligible. I think projects should also have a setting that allows them to be eligible to be parents, which is disabled by default." Asked directly whether eligibility should be a real backend-enforced gate or just a picker-side filter, given the size difference — the user chose full enforcement, matching this codebase's standing "never trust client-side filtering alone" posture.

**`Project.can_be_parent`** (new column, migration `0016`, default `False`) gates whether `parent_project_id` on an *other* project may point at this one — checked on both `create_project` and `update_project` (400 if the target parent hasn't opted in), not just filtered out of the frontend picker. Existing parent/child relationships are grandfathered by the migration's own backfill (`can_be_parent = true` for any project already referenced by another row's `parent_project_id`), so this cannot retroactively break an already-established hierarchy. Turning it back off on a project that already has children does not detach them — the same "changes apply going forward, not retroactively" principle `role_inheritance_mode` already follows (decision 9 in the "Hierarchical projects" entry above) — it only blocks *new* attachments.

**Frontend**: a new "Allow this project to be a parent" checkbox on `ProjectAdminPage.tsx`'s Project settings tab (always visible, independent of whether *this* project currently has, or could have, a parent of its own — it's the opt-in mechanism *other* projects' managers need). The "Parent project" field itself (and its dependent inheritance-mode sub-fields) is hidden entirely on both `ProjectAdminPage.tsx` and `ProjectListPage.tsx`'s create modal when there's nothing eligible to pick and no parent currently set — directly closing the reported UX complaint — while a project's *already-attached* parent always stays selectable even if that parent's own eligibility was since revoked, so an established relationship never renders as a blank/unselectable value. "Add sub-project" is disabled (with an explanatory `title`) until the project itself has opted in, since it leads straight into a create flow the backend would otherwise reject.

**A real bug found only by the Playwright verification, not by static review or the backend test suite:** `GET /projects` (`list_projects`) and `GET /{id}/children` both construct `ProjectListItemOut` field-by-field rather than via `ProjectOut.model_validate(project)` (the pattern the single-project `GET /{id}` correctly uses) — adding `can_be_parent` to the Pydantic schema without also adding it to those two explicit constructor calls meant it silently fell back to the schema's own default (`False`) for every project, regardless of the real database value, with no error anywhere (Pydantic only defaults missing fields, it doesn't reject them). Confirmed directly: the database correctly held `can_be_parent = true` for the seeded parent projects while the live API response for both listing endpoints reported `false` for all of them. Backend unit tests didn't catch this because `test_project_hierarchy.py`'s own assertions read the single-project `GET /{id}` response, never the list/children endpoints — closed with a dedicated regression test (`test_can_be_parent_is_reported_correctly_by_list_projects_and_children`) that reads both. This is the exact class of bug this project's testing policy runs a real browser for rather than trusting `tsc`/unit tests alone.

**Backend test ripple, mechanical but necessary given full enforcement:** every existing test across `test_project_hierarchy.py` (~25 call sites) and `test_action_types.py` (5 hierarchy-fallback tests, a separate file this session initially missed on the first pass — caught by the full suite, not assumed clean) that creates a parent-then-child pair now passes `can_be_parent=True` at the parent's own creation; the shared `conftest.py::create_project` helper gained the matching kwarg. `seed_demo_data.py` (Falcon-3 Inspection Drone) and `seed_e2e_dataset.py` (Gamma-1, Gamma-3 Hierarchy Parent) set it at seed time on every project used as a live parent target, including the one `project-hierarchy.spec.ts`'s relaxed-creation-path test attaches a child under; `project-hierarchy.spec.ts`'s own first test (which creates a brand-new parent live, not from a seeded fixture) gained an explicit opt-in step before its "Add sub-project" click. `project-list-org-scoping.spec.ts`'s single-org-picker assertions were updated for the new hidden-when-nothing-eligible baseline (a fresh org has zero projects, so nothing can ever be eligible there) rather than left asserting stale "always renders" behaviour.

**Verified.** Backend: `ruff check` clean; full suite **616 passed, 0 failed** (a first clean run surfaced 5 real failures in `test_action_types.py`, fixed and reconfirmed green — not silently left for a later pass). A `sqlalchemy`-connection-error batch seen on one intermediate run (`102 failed, 116 passed, 399 errors`) was traced to this session's own many-hours-long shared local Postgres connection exhaustion — never reproduced again after a schema wipe + backend restart, consistent with this project's own documented DB-pool-wedge pattern, not a code regression. Frontend: `tsc --noEmit`/`eslint` clean; Storybook **532/532** (up from 527), including new coverage for the checkbox (`CanBeParentToggleSaves`), the disabled/enabled "Add sub-project" states, and the hidden-vs-offered "Parent project" field on both pages. Playwright: full suite **91/91 passing** from a genuinely fresh reseed after a full schema wipe — the only verification standard this project's own methodology accepts as conclusive — including `project-hierarchy.spec.ts` and the fixed `project-list-org-scoping.spec.ts`.

Files changed: `backend/app/models/project.py`; `backend/alembic/versions/0016_project_can_be_parent.py` (new); `backend/app/schemas/project.py`; `backend/app/routers/projects.py`; `backend/tests/conftest.py`, `test_project_hierarchy.py`, `test_action_types.py`; `backend/scripts/seed_demo_data.py`, `seed_e2e_dataset.py`; `frontend/src/api/types.ts`, `i18n/strings.ts`, `testing/storybook-helpers.tsx`; `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`), `ProjectListPage.tsx` (+ `.stories.tsx`); `frontend/src/components/ProjectHierarchyLabels.stories.tsx`; `tests/playwright/tests/badge-filters.spec.ts`, `pelion-v2.spec.ts`; `tests/playwright/tests/e2e-workflows/project-visibility.spec.ts`, `project-admin-structural.spec.ts`, `org-admin-project-statuses-and-link-types.spec.ts`, `project-hierarchy.spec.ts`, `project-list-org-scoping.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Human UI/UX review batch: seven fixes (attach-on-create, activity-log duplication, dashboard navigation, list styling, login branding, responsive overflow, project users parity)

A second human (non-audit) UI/UX pass produced seven separate pieces of feedback, all implemented in one batch. The one genuine architectural decision among them — the activity-log deduplication — is recorded in detail below; the rest are noted briefly for the file-change record.

### Activity-log duplication: root cause and fix

`services/changes.py::get_project_changes` merges `AuditEvent` rows with `RequirementVersion`/`ChangeRequestVersion` rows into one feed (by design, per its own docstring — three/four sources, one timeline). The bug: every mutation that bumps a version *also* logs an `AuditEvent` for the exact same transition in the same request handler, and both were surfacing as separate rows — confirmed by reading every call site, not assumed: requirement `created`/`updated`/`approved`/`completed`/`uncompleted` (`routers/requirements.py`) each sit immediately after `apply_new_version`/`create_requirement`, and change-request `created` sits after the version-1 `ChangeRequestVersion` insert (`routers/change_requests.py`). `archived`/`unarchived`/`review_recorded` (requirement) and `submitted`/`withdrawn`/approve-reject (change request) do **not** pair with a version bump and were never duplicated.

**Decision: keep the version-history-sourced entry, suppress the redundant `AuditEvent` row**, via a `(entity_type, action)` filter applied only to the audit-sourced list — not a timestamp-matching heuristic, and not a change to what gets logged (both sources still get written; only which ones the *feed* surfaces changed). Chosen over the reverse (keep `AuditEvent`, suppress version-history) because the version-history entry already carries `change_note` (e.g. "Approved directly.", "Marked completed.") in its `detail`, which reads as more informative than the generic `AuditEvent`'s "approved"/"completed" action label alone, and because version-history entries exist for *every* version bump regardless of caller (baseline approval, CSV import, a future code path nobody's written yet) whereas hardcoding around specific `AuditEvent` call sites would only catch pairs known today. Verified the filter is a strict subset by grepping every `log_event(..., entity_type="requirement"/"change_request", ...)` call site against every version-bump call site, including `services/baseline.py` (bumps versions, never logs a paired event — already never duplicated, filter doesn't touch it) and the CSV import path (logs at `entity_type="project"`, not `"requirement"` — also untouched).

Pinned with `test_project_changes_has_no_duplicate_entries_for_version_bumping_actions` (`test_reporting_and_changes.py`): create/edit/approve/complete/uncomplete a requirement and create a change request, assert exactly one feed entry per transition (five for the requirement — one `created` plus four `updated`, since `apply_new_version` only ever synthesizes `created`/`updated`, not the more specific verb — and one for the CR).

### The other six, briefly

- **Attach files / add links during requirement creation**: the create `Modal` on `RequirementsPage.tsx` now advances to a second step (reusing `FileAttachmentList` and the same add-link `Popover` pattern `RequirementDetailPage.tsx` already had) against the just-created requirement's real id, rather than closing — the backend's file/link endpoints require an existing id, so this reuses them rather than building a pre-create staging flow. Also fixed a pre-existing bug found in the same area: Add Link was never disabled even with zero eligible targets, on both the create step and the detail page.
- **Dashboard widgets/charts didn't navigate**: `ProjectOverviewPage.tsx`'s glance tiles, status/CR pie charts, and stage-progress bars now link to the Requirements/Change Requests list pre-filtered to match. The hand-rolled CSS-conic-gradient `DonutChart` was retired in favour of **Recharts** (new dependency) — the user's explicit call, made with a future custom-dashboard-builder feature in mind, wrapped in a new `StatusPieChart` component rather than used ad hoc.
- **Project list styling**: `.side-grid`'s base rule (not just its `@media (max-width: 860px)` override, which already had this fix) now uses `minmax(0, 1fr)` instead of a bare `1fr`, so the `FilterPanel` column can no longer be pushed off-screen by wide content at moderate widths; project descriptions now clamp to a fixed number of lines (`-webkit-line-clamp`) instead of an unbounded single line.
- **Login page branding**: `OrgLoginPage.tsx` already had a logo+title block; the plain `LoginPage.tsx` didn't. Extracted into a shared `LoginBrandHeader` component used by both, rather than adding a second inline copy.
- **Responsive table overflow**: several tables (`OrgAdminPage.tsx`'s Users tab, `PreferencesPage.tsx`'s PATs/notifications tables, `RequirementDetailPage.tsx`'s version-history table) were missing the `overflowX: auto` wrapper the rest of the app's tables already use, letting them overflow their card at narrow widths. The nav-rail collapse itself already existed (`theme.css`'s 860px breakpoint) — nothing to fix there.
- **Project Users page vs. Org Users page**: `ProjectAdminPage.tsx`'s "Effective members" list (a bare `<ul>`) is now a searchable, sortable table matching Org Admin's Users table's structural pattern. Deliberate deviation from a literal reading of "look similar": `EffectiveMemberOut` has no `last_login_at`/`is_2fa_enabled`/`is_active` (org-account attributes, not meaningful at project scope) — a **Source** column (direct vs. inherited, and via what) takes their place instead of being force-fit. `docs/ux-style-guide.md` updated to state this pattern explicitly.

**Verified**: backend `pytest` (new dedup test plus the existing `test_reporting_and_changes.py`/`test_coverage_gaps.py`/`test_audit_fixes.py`/`test_pagination.py` suites, all of which touch the `/changes` feed); frontend `tsc -b`/`eslint` clean; full Storybook suite (updated `RequirementsPage.stories.tsx`, `ProjectAdminPage.stories.tsx`, new `StatusPieChart`/`LoginBrandHeader` stories) passing; Playwright specs added for each item and run against the dev/test stack.

Files changed: `backend/app/services/changes.py`; `backend/tests/test_reporting_and_changes.py`; `frontend/package.json`/`package-lock.json` (recharts); `frontend/src/components/StatusPieChart.tsx` (new, replaces deleted `DonutChart.tsx`/`.stories.tsx`), `LoginBrandHeader.tsx` (new); `frontend/src/pages/RequirementsPage.tsx` (+ `.stories.tsx`), `RequirementDetailPage.tsx`, `ProjectOverviewPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, `LoginPage.tsx`, `OrgLoginPage.tsx`, `OrgAdminPage.tsx`, `PreferencesPage.tsx`, `ProjectAdminPage.tsx` (+ `.stories.tsx`); `frontend/src/styles/theme.css`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/dashboard-navigation.spec.ts`, `login-branding.spec.ts`, `requirement-create-attach.spec.ts`, `project-users.spec.ts` (all new); `docs/ux-style-guide.md`, `docs/decisions.md` (this entry).

## Deep hardening pass on `feature/ui-improvement-stage-4` ("Please do a deep hardening pass")

Requested explicitly against the whole `feature/ui-improvement-stage-4` branch (commit `0046e21` "UX Stage 4 works" vs. `main`'s `4c71a2c`, 31 files) — the entire "Human UI/UX review batch" landed in the entry directly above, treated as its own scoped review rather than assumed already covered by that entry's own "Verified" section, per this project's standing hardening-pass practice. Identify → verify → remediate: the one changed backend file (`services/changes.py`'s activity-feed dedup filter) read and traced line-by-line directly — every suppressed `(entity_type, action)` pair's `AuditEvent.detail` compared against its corresponding `RequirementVersion`/`ChangeRequestVersion` synthesized entry to confirm no information loss, not just that the dedup filter's set matches the entry's own claimed list. A subagent covered the frontend lane (13 changed files: the new create→attach/link step, dashboard-navigation deep links, `StatusPieChart`/Recharts, `LoginBrandHeader`, the `ProjectAdminPage.tsx` members table, and the four responsive/overflow/styling-only files) against this project's standard checklist (client-side-only authorization, raw-enum rendering, the `reload()`-clobbers-unsaved-edit race class, confirmation-dialog tiers) — its five highest-stakes claims (the Add-Link disabled-with-zero-eligible-targets fix on both `RequirementsPage.tsx` and `RequirementDetailPage.tsx`, `OrgAdminPage.tsx`'s diff being CSS-only, `ProjectAdminPage.tsx`'s new member search/sort being read-only local state untouched by any `reload()`, and the dashboard tiles/charts linking only to already-authorized destinations) were independently re-verified by reading the actual diff hunks directly, not trusted from the subagent's summary alone, per this project's standing rule.

**No findings.** Unlike the two prior deep hardening passes on this project (Stage3, `feature/multi-level-projects`), this one surfaced nothing to fix — this batch's diff is almost entirely additive UI (a new create-flow step, new navigation links, a component swap, CSS wrappers) built exclusively on pre-existing, already-authorized endpoints, plus one small, well-scoped backend display filter with no authorization surface of its own. Recorded as its own entry regardless of the null result, matching this project's practice of always documenting a requested hardening pass's scope and outcome rather than only recording passes that found something.

**Reviewed and found adequate:**
- **`get_project_changes`'s new `VERSION_HISTORY_COVERED_ACTIONS` filter** (backend, the only non-frontend change in this diff): confirmed the suppressed `AuditEvent` rows for `requirement`/`{created, updated, approved, completed, uncompleted}` and `change_request`/`{created}` carry no data the corresponding version-history entry doesn't already have — traced `update_requirement`'s `log_event(..., detail={"change_note": payload.change_note})` specifically, since `payload.change_note` can be `None` while `apply_new_version` defaults it to `"Direct edit during scoping."` before storing it on the `RequirementVersion`; the surviving version-history entry is strictly *more* complete, never less, so suppressing the audit-side duplicate loses nothing. Confirmed `change_request`'s `approved`/`rejected` actions (logged at `change_requests.py:718`) are correctly *excluded* from the filter — unlike `created`, neither transition inserts a new `ChangeRequestVersion` row, so there is no duplicate to suppress there and the filter's own inline comment is accurate. The underlying `AuditEvent` rows are never deleted, only excluded from this one merged feed's display — the raw audit trail a compliance review would query directly is untouched, so this doesn't regress the change-management/secure-development policy's logging-integrity expectations.
- **Frontend, per the subagent lane, independently re-verified**: no new UI affordance in this diff gates on client state with no server-side backing (the create→attach/link step, the dashboard deep links, and the members-table search/sort all read/write through pre-existing, unmodified, already-authorized endpoints); every rendered enum value routes through its existing label map, no new raw-enum call site introduced; `StatusPieChart`'s only third-party-library input is pre-mapped label strings and numeric counts, no `dangerouslySetInnerHTML` or equivalent; `LoginBrandHeader` adds no new pre-auth network call on either login page (both already fetched the branding response this diff just reads two more fields off of); the previously-documented "Add Link never disabled with zero eligible targets" bug is genuinely fixed on both surfaces that needed it, not just claimed fixed in the commit message.
- **The four new Playwright specs** (`dashboard-navigation.spec.ts`, `login-branding.spec.ts`, `project-users.spec.ts`, `requirement-create-attach.spec.ts`), checked against this project's own repeated non-idempotent-test failure mode: none mutate shared seeded fixtures — `dashboard-navigation.spec.ts`/`login-branding.spec.ts`/`project-users.spec.ts` are read-only (the members-table search/sort is client-side-only state, confirmed above), and `requirement-create-attach.spec.ts`'s two tests each create their own uniquely-timestamped disposable project rather than touching a named shared fixture, matching this suite's established convention.

**Verified.** Backend: full suite **618 passed, 0 failed**, against a fresh `DROP SCHEMA public CASCADE` + `docker compose exec backend pytest` (no restart in between, per this project's own documented correct sequencing) — remaining warnings are pre-existing third-party `datetime.utcnow()` deprecations (`python-jose`, `botocore`), unrelated to this diff and outside files it touches. Frontend: `npx tsc --noEmit`/`npx eslint src/` clean (only the same 7 pre-existing warnings in untouched files); Storybook **550/550** across 65 files. Playwright, against a stack rebootstrapped and reseeded (both `seed_demo_data.py` and `seed_e2e_dataset.py`, both ran clean with no schema-sync gaps) after the pytest run per this project's documented DB-wedge-avoidance sequencing: the four new specs plus `project-hierarchy.spec.ts` (whose two-line `<li>`→`<tr>` locator update, needed once `ProjectAdminPage.tsx`'s members list became a table, was re-confirmed correct) — **11/11**; a broader regression batch across every other surface this diff's shared components/pages touch (`golden-path.spec.ts`, `requirement-actions.spec.ts`, `requirement-links.spec.ts`, `comment-editing-and-attachments.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `project-list-org-scoping.spec.ts`, `project-visibility.spec.ts`, `org-security-controls.spec.ts`, `requirement-approval.spec.ts`, `requirement-history-activity-toggle.spec.ts` — the last one exercising the exact activity feed `services/changes.py`'s dedup change modified, `badge-filters.spec.ts`) — **11/11**. **22/22 total**, the only verification standard this project's own methodology accepts as conclusive.

Files changed: `docs/decisions.md` (this entry only — no code changes were needed).

## Admin-tier ResourceMenu consistency: Server Admin and Preferences converted from Tabs, reversing Project Admin's earlier "stays on Tabs" call

Product owner-directed reversal of a recorded style-guide decision, not a new finding from an audit pass. `docs/ux-style-guide.md` Principle 1 and its "Pattern: settings hierarchy" decision tree say Resource menu is for more than five setting groups opened rarely as a whole, Tabs for five or fewer views all relevant together. Server Admin (4 groups), Preferences (5 groups), and Project Admin (5 groups) each sit at or under that threshold, and the 2026-08 UX audit roadmap ("Revisit Project Admin's 8-tab bar against the 5-tab ceiling") explicitly recorded a *deliberate* prior decision to keep Project Admin on `Tabs` specifically because those tabs are "all relevant together" and actively used inline, not "opened rarely as a whole." Shown that tension directly, the product owner decided **cross-page consistency across the app's whole admin tier** (Org Admin, Server Admin, Preferences, Project Admin — Org Admin already converted to `ResourceMenu` in the "Org Admin → resource-menu restructure" entry above) now outweighs the original per-page ≤5-groups reasoning: a page that's part of a recognisable set of admin-tier pages shouldn't use a different navigation shape from its siblings just because its own group count happens to clear the threshold the others exceeded.

**Scope: navigation shell only, not content.** All three pages kept their existing group/tab keys, labels, and panel content unchanged — only the container swapped from `Tabs`/`tabPanelProps` to `ResourceMenu`, copying `OrgAdminPage.tsx`'s already-established shape exactly rather than reinventing it: a flat `ResourceMenuGroupDef<K>[]` array, a `useParams<{ group?: string }>()` read with an `ORG_ADMIN_GROUP_KEYS`-style whitelist-and-fallback-to-default pattern, and a new `:group?` optional route segment so a bare link into the page (used everywhere today) still resolves.

- **`ServerManagementPage.tsx`**: new `/server/management/:group?` route (`App.tsx`), 4 groups (`accessReview`/`branding`/`signup`/`email`), default `accessReview`. `title`/`ariaLabel` now come from `ResourceMenu`'s own props (`strings.nav.serverManagement`, new `strings.system.sectionsNav`) instead of a bare `<h1>` + `Tabs`.
- **`PreferencesPage.tsx`**: new `/preferences/:group?` route, 5 groups (`profile`/`security`/`access`/`pats`/`notifications`), default `profile`. New `strings.preferences.sectionsNav`.
- **`ProjectAdminPage.tsx`**: new `/projects/:projectId/admin/:group?` route, 5 groups (`overview`/`structure`/`fieldsActions`/`groups`/`reportSetup`), default `overview`. Unlike the other two, this page keeps its own custom header row (`<h1>{project.name}</h1>` + the "Add sub-project" button) above `ResourceMenu` rather than passing `title` into the component — `ResourceMenu`'s own `title` prop would have duplicated the project name a second time next to a button that has nowhere else to live in that layout. New `strings.admin.sectionsNav` (tokenized: `"{Project} admin sections"`). The stale roadmap-referencing comment above the old tab-state `useState` ("per the style guide's own 5-tab ceiling for the Tabs pattern") was removed since it's the exact reasoning this reversal supersedes; the surrounding "8 tabs -> 5 groups" consolidation history comment was kept and reworded to note the later Tabs→ResourceMenu conversion rather than deleted, since that regroup (Terminology folded into Overview, Stages+Components/Categories merged into Structure, etc.) is still the current shape and still worth explaining.
- **`Tabs.tsx`/`tabPanelProps` themselves are untouched.** Grepping the frontend confirms these three pages were `Tabs`' only production call sites (its own file-level comment already said so: "Replaces three byte-for-byte-identical hand-rolled tab bars"), so it now has none — deliberately left in place regardless, per the task's own instruction and Principle 1's continued documented case for `Tabs` (a genuinely ≤5-co-relevant-view surface outside the admin tier), rather than deleted as dead code.

**Docs updated to reflect the reversal explicitly, not silently:**
- `docs/ux-style-guide.md` Principle 1 gained a sentence naming cross-page consistency with an already-converted admin-tier sibling as itself a valid reason to choose `ResourceMenu` even at or under the 5-group threshold.
- `docs/ux-audit-2026-08.md`'s roadmap: the "Shared, accessible `Tabs` component" row (473) got a "**Superseded 2026-08-27**" note; the "Revisit Project Admin's 8-tab bar" row (488) got a "**Reversed 2026-08-27, by direct product decision**" note explaining the old reasoning is still true in isolation but was outweighed; a new row records the Server Admin/Preferences conversions.

**Playwright**: every spec that drove these three pages via `page.getByRole("tab", { name: ... })` (Tabs' own ARIA role) now instead uses the new route segments, the same idempotent, `aria-current="page"`-checking pattern `helpers.ts`'s existing `selectOrgAdminGroup` already established for Org Admin — generalised into a new `selectResourceMenuGroup(page, groupLabel)`, with `selectOrgAdminGroup`/`selectServerManagementGroup`/`selectPreferencesGroup`/`selectProjectAdminGroup` as thin, page-named wrappers around it (kept as separate exported names rather than one generic call at every site, matching how the existing call sites already read as self-documenting about which page's group menu they operate on). 26 spec files updated across `tests/playwright/tests/e2e-workflows/` and the top-level `tests/playwright/tests/` directory — every file that had a `role="tab"` locator on Server Admin, Preferences, or Project Admin, found by grepping the whole suite (confirmed zero remaining `getByRole("tab"` hits against any of the three afterward). One stale comment in `project-admin-structural.spec.ts` ("ProjectAdminPage's tab state resets to the default (Overview) on a full page reload") was corrected in the same pass — it described `Tabs`' client-only-state behaviour, which no longer applies now that the group is a real route segment; a full reload now *preserves* the selected group, so the spec's own re-select-after-reload call is now a harmless, already-idempotent no-op, not a required step.

**Storybook**: no new stories, per the task's own scope call — `ResourceMenu` is reused as-is, not extended. The three pages' *existing* `.stories.tsx` files did need updating, though, since their `play` functions also drove the old `role="tab"` interaction and their `withRouter` decorators didn't yet declare the new `:group?` route pattern: all `getByRole("tab", { name: ... })` calls became `getByRole("link", { name: ... })` (37 call sites across the three files), and each `withRouter(initialPath)` call gained its route's `:group?` pattern as a second argument, matching `OrgAdminPage.stories.tsx`'s own established decorator shape.

**Verified.** Frontend: `npx tsc -b` and `npm run build` (typecheck + Vite production build) both clean — no new errors introduced by any of the three page conversions, the Storybook updates, or the new `strings.ts` keys; `npm run lint` clean (only the same 7 pre-existing `react-refresh/only-export-components` warnings in untouched files). Playwright: the full set of specs touched by this change (26 files, 32 tests spanning both `tests/e2e-workflows/` and the top-level directory) — **32 passed, 0 failed** (3.1 minutes), from a single, uninterrupted run against a freshly `DROP SCHEMA public CASCADE`-reset and reseeded (`seed_e2e_dataset.py`) `tests/container` stack. Getting to that clean run took several discarded attempts, worth recording since it's a real instance of this project's own documented DB-wedge/shared-stack class of problem, not a code issue: this session ran concurrently with other agents' unrelated work in the same working tree and against the same shared dev stack (a separate dashboard/result-counts workstream, confirmed directly via `docker top`/`ps aux` mid-session), and two earlier attempts were separately invalidated — once by a live `ObjectDeletedError`/`500`-producing schema wipe from another process's `pytest` run caught mid-flight in `docker compose logs backend`, and once self-inflicted, by accidentally launching two overlapping copies of the same Playwright invocation against the same login/browser state after a session interruption. Every failure from those contaminated runs traced back to either a login/"Sign out button not found" symptom (this project's own documented DB-wedge signature) or stale data from a previous run killed mid-test (e.g. Beta-1's seeded "Review" action type already renamed by an interrupted earlier attempt) — none were `ResourceMenu`/routing/locator failures, and the final clean run's 32/32 confirms none of the three conversions have a real defect.

Files changed: `frontend/src/pages/ServerManagementPage.tsx`, `PreferencesPage.tsx`, `ProjectAdminPage.tsx` (+ `.stories.tsx` for all three); `frontend/src/App.tsx`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/helpers.ts` and 21 spec files in that directory (`project-admin-structural`, `project-hierarchy`, `org-login-2fa-handoff`, `role-display-collapsing`, `two-factor-auth`, `report-image-attachment`, `stage-review-and-completion`, `richtext-insert-link-modal`, `org-rename-and-test-email`, `preferences-and-theme`, `org-label`, `project-admin-groups-and-fields`, `terminology-coverage`, `personal-access-tokens`, `external-project-invite`, `workflow-bypass-attempts`, `project-admin-templates-and-reports`, `project-admin-action-types`, `requirements-bulk-operations`, `org-security-controls`, `leave-organisation`); 5 top-level spec files (`golden-path`, `requirement-create-attach`, `mockup-engagement`, `project-users`, `pelion-v2`); `docs/ux-style-guide.md`, `docs/ux-audit-2026-08.md`, `docs/decisions.md` (this entry).

## Project-wide file browser, and a latent access-control bug found while building it

New feature: "a way to view all files in a project." `FileAsset` has no `project_id` of its own — it's only reachable via a join through whichever requirement/action/comment it's attached to (`RequirementFile`/`RequirementActionFile`/`CommentFile`), and `docs/requirements.md` U-P-05 only asked for a project overview metric ("number of files in project, if files are implemented"), never a browsable list. `GET /projects/{project_id}/files` (`backend/app/routers/projects.py::list_project_files`, `ProjectFileOut` in `backend/app/schemas/file.py`) fills that gap: three separate joins (direct attachment via `Requirement.project_id`, action attachment via `RequirementAction.project_id` directly — an action isn't owned by exactly one requirement, `RequirementActionLink` is many-to-many, so there's no single requirement to attribute a file to — and comment attachment via `ReviewComment.target_id` where `target_type == REQUIREMENT`, joined to `Requirement.project_id`), merged, sorted newest-first, and paginated with the same `limit`/`offset`/`X-Total-Count` convention every other list endpoint in this router uses. Gated by `require_project_view` (member-level content access), matching every other requirement-content-adjacent endpoint. Frontend: `frontend/src/pages/ProjectFilesPage.tsx` (+ `.stories.tsx`), a flat read-only table reusing `fileUrl()`'s existing download-link pattern from `FileAttachmentList.tsx` — no upload control, since uploads stay per-requirement/action/comment. Wired up at `/projects/:projectId/files` (`App.tsx`), a new "Files" nav-rail entry (`Layout.tsx`, between Actions and Reports), and `ProjectOverviewPage.tsx`'s "Files" metric tile now links here instead of the unfiltered requirements list it previously (incorrectly) pointed at — a separately-flagged bug ("the files glance shouldn't go to requirements"), fixed as part of the same change since the new destination is what the tile always should have pointed to.

**A real, pre-existing access-control bug found while tracing the three join tables this endpoint needed to combine, fixed in the same change rather than deferred** (per this project's standing "fix, don't defer" rule): `GET /api/v1/files/{id}` (`backend/app/routers/files.py::download_file`) resolves which project a non-org-resource, non-avatar file belongs to by checking `RequirementFile` then falling back to a `CommentFile`/`ReviewComment` lookup — but had no branch for `RequirementActionFile` at all. A file attached only via a requirement action (`POST /projects/{id}/actions/{action_id}/files`) therefore always resolved `project_id = None` and fell through to an unconditional 403, for *everyone*, including the uploader and the project's own manager — upload/list/delete all worked, but the file could never actually be downloaded. Confirmed via `test_action_direct_file_attachment` (`backend/tests/test_requirement_actions.py`), which uploaded and listed an action attachment but never called the download endpoint — the gap was invisible to the existing suite. Fixed by adding the missing branch, resolving via `RequirementAction.project_id` directly (no join through a requirement needed, for the same many-to-many reason `list_project_files` above attributes action files to the action itself). This is a fail-closed bug (over-restrictive, not over-permissive) so it doesn't regress the SOC 2 access-control policy's authorization posture, but per CLAUDE.md's guidance on changes touching authorization resolution logic, it's called out explicitly here rather than folded silently into the feature commit.

**Docs and seed data kept in sync:** `docs/solution-architecture.md` gained an implementation note under "Application Layer" explaining the three-way join, matching the existing bundle-export/import notes' style. `backend/scripts/seed_e2e_dataset.py` now seeds one file via each of the three origins on Alpha-1 (a direct attachment, an attachment on the existing "E2E Review Action" action, and a comment attachment) specifically so `project-files.spec.ts` has a realistic multi-source project to assert against — `docs/e2e-workflows.md` gained workflow entry #26 for it. `backend/scripts/seed_demo_data.py` gained the same three-origin file seeding on Falcon-3 (direct attachment on the still-draft "Complete pre-flight diagnostic checks" requirement — not the already-approved remote-ID requirement, which 409s on a direct attachment per C-G-12 — an action attachment on the completed firmware-review action, and a comment attachment on the flight-time discussion), so the demo dataset now actually demonstrates the feature it previously left implicit in the file-count metric alone.

**Tests added:** `backend/tests/test_files.py::test_project_files_endpoint_combines_sources_and_scopes_by_project` — seeds all three origins across two requirements in one project plus a direct attachment in a second, unrelated project, and asserts the endpoint returns exactly the three same-project rows with correct origin context, and critically that the second project's file never appears (same-target-scoped resolution, not "has access somewhere" — the hard access-control-policy requirement called out in this task). `test_project_files_endpoint_requires_project_membership` — a user with no role on the project gets 403, matching every other `require_project_view`-gated endpoint. `test_requirement_actions.py::test_action_direct_file_attachment` extended to assert the download actually succeeds (and 404s post-delete), pinning the `download_file` fix; `test_non_member_cannot_download_action_attachment` added alongside it, mirroring the existing requirement-attachment non-member test. Frontend: `ProjectFilesPage.stories.tsx` (mixed-sources rendering with origin links, `LoadMoreButton` pagination, empty state) and a new Playwright spec `tests/playwright/tests/e2e-workflows/project-files.spec.ts` (files from all three origins listed with correct origin links, the overview tile navigating here instead of to requirements, the nav-rail entry, and an actual download round-trip against the seeded Alpha-1 fixture).

**A messy, heavily-contested shared test environment during verification, not a code problem:** this session's Docker host ports were initially squatted by an unrelated, already-orphaned worktree's stack from an earlier task in the same environment (stopped once identified, freeing the ports); the shared `reqtrack_test` database was then found mid-verification in a partially-seeded, inconsistent state (an org existing with none of its persona users, `httpx.ConnectError: [Errno 99] Cannot assign requested address` transients under concurrent load, and at one point the schema itself was fully dropped by a concurrent process) — consistent with multiple other agents running pytest/seed/Playwright against the same stack concurrently, exactly as flagged separately. Resolved each time by re-running this project's own documented recovery recipe (`DROP SCHEMA public CASCADE` + backend restart to re-run migrations, then re-seed both scripts) rather than attempting a partial, riskier row-level patch-up.

**Verified.** Backend: full suite **624 passed, 0 failed** (includes the two new endpoint tests plus the two new action-download tests, run alongside a separate, unrelated in-flight workstream's own changes to `change_requests.py`/`requirements.py`/`test_pagination.py` in the same working tree — no interference either direction). Frontend: `npm run build` (`tsc -b && vite build`) clean; `eslint src/` clean (only the same 7 pre-existing warnings in files this change doesn't touch); full Storybook suite **67 files / 561 tests passed**, including `ProjectFilesPage.stories.tsx`'s 5. Playwright: `project-files.spec.ts` **1/1 passing** against a freshly reseeded stack; a small regression batch (`dashboard-navigation.spec.ts`, `requirement-actions.spec.ts`, `comment-editing-and-attachments.spec.ts`) run alongside it turned up 2 pre-existing failures, both in files this change never touches (`RequirementsPage.tsx`'s first-paint status-filter race, `RequirementDetailPage.tsx`'s create-and-link action dialog) and both reproducing consistently on retry — consistent with the separately-flagged, still-in-progress parallel "result-counts/dashboard-race" workstream, not a regression introduced here; left for that workstream to resolve rather than fixed blind, per the coordination note not to clobber concurrent work outside this task's scope.

Files changed: `backend/app/schemas/file.py`, `backend/app/routers/projects.py`, `backend/app/routers/files.py`; `backend/tests/test_files.py`, `test_requirement_actions.py`; `backend/scripts/seed_e2e_dataset.py`, `seed_demo_data.py`; `frontend/src/pages/ProjectFilesPage.tsx` (new, + `.stories.tsx`), `App.tsx`, `ProjectOverviewPage.tsx`; `frontend/src/components/Layout.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/strings.ts`; `frontend/src/testing/storybook-helpers.tsx`; `tests/playwright/tests/e2e-workflows/project-files.spec.ts` (new); `docs/solution-architecture.md`, `docs/e2e-workflows.md`, `docs/decisions.md` (this entry).

## Persistent "showing X of Y" result counts, and the dashboard glance-tile filter race

The "result-counts/dashboard-race" workstream referenced (as still in progress) in the entry directly above. Two related fixes across the three list pages (`RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`):

**Part A — persistent result count.** `X-Total-Count` (already returned by the three list endpoints) reflects the *filtered* count, but there was never a second, *unfiltered* total to compare it against — `LoadMoreButton`'s own "(loaded/total)" text is the only place `total` was ever shown, and it disappears once every page has loaded. Each of `requirements.py`/`change_requests.py`/`projects.py`'s list endpoints now also returns `X-Total-Unfiltered-Count`: one extra `COUNT(*)`-style query against the same base scope query (project/org + default archived-visibility), taken *before* search/status/stage/category filters narrow it further. `client.ts`'s `Page<T>` gained an optional `totalUnfiltered` field read from the new header (optional, not required — many existing `getPage` mocks across other pages' stories predate this field and don't need updating).

`FilterPanel` (`frontend/src/components/FilterPanel.tsx`) was restructured into a header (a new `ResultCount` component — "57 total" with no filter active, "Showing 12 matching · 57 total" once one is — plus, on `RequirementsPage`/`ProjectListPage` only, the free-text search box relocated here from the main content column) and a collapsible body (the existing `FilterField`/`FilterCheckbox` controls): fully expanded with no toggle on desktop, defaulting to collapsed with a `CollapsibleSection`-driven toggle below the same 860px breakpoint `.side-grid` itself already collapses at (`theme.css`). `ChangeRequestsPage` has no search box (unchanged, per the brief). Four other existing `FilterPanel` consumers (`NotificationsPage`, `ProjectActionsPage`, `ProjectReviewsDuePage`, `ServerOrganisationsPage`) needed a `sectionKey` prop added for the new required collapsible-body key, since `matching`/`total` are optional but `sectionKey` isn't — none of the other three had a comparable "unfiltered total" figure to wire up, so their headers show no `ResultCount`, unchanged in appearance from before this restructure. `docs/ux-style-guide.md`'s existing `Pattern: FilterPanel` write-up gained a dedicated subsection documenting this shape, plus corrections to two sentences in the original write-up that the restructure made inaccurate (the "never collapsed behind a menu" claim, and "free-text search box... stays in the main content area" — no longer true for the two pages whose search box moved).

**Part B — dashboard glance-tile filter race.** Root cause: `RequirementsPage.tsx`/`ChangeRequestsPage.tsx` each ran two effects on mount — a reload effect (deps include the filter state) and a separate deep-link effect that read `?status=`/`?stage=` from the URL and called `setStatusFilter(...)`. The reload effect fired first, with the *still-empty default* filter, kicking off an unfiltered fetch; the deep-link effect's `setStatusFilter` call then re-triggered the reload effect with the real filter, kicking off a *second* fetch. Neither `loadRequirements`/`loadChangeRequests` sequenced requests at all, so whichever response resolved last simply overwrote state — usually the correctly-filtered one, since it was kicked off second and (for a narrower query) often finishes faster, but not always: a dashboard tile click landing on `ProjectOverviewPage` → `RequirementsPage`/`ChangeRequestsPage` could show a correct filter dropdown next to an unfiltered list, "fixed" only by the user re-toggling the filter (which forces a fresh, single, correctly-sequenced fetch) — the exact manual workaround that had been masking this.

Fixed by seeding `statusFilter`/`targetStageFilter` (and the change-request tri-state equivalent) directly from `searchParams` in the `useState` initializer, rather than in a separate post-mount effect — the deep-link effect now only strips the URL params, never calls the filter setters itself, so exactly one reload effect run (and one fetch) happens per mount+filter combination. Belt-and-suspenders: `loadRequirements`/`loadChangeRequests`/`loadProjects` (all three list pages, for consistency) each gained a monotonic request-id `useRef` guard that discards a response if a newer request has since been kicked off, covering any other overlapping-fetch path (e.g. rapid filter changes) beyond the specific mount-time race the seeding fix eliminates.

**A CORS bug caught only by a real browser, not curl or pytest:** the new `X-Total-Unfiltered-Count` header was set correctly by the backend (confirmed directly with `curl`) and pytest's `TestClient` (which bypasses CORS entirely) saw it fine, but the frontend's `fetch().headers.get("x-total-unfiltered-count")` silently returned `null` in a real browser — `main.py`'s `CORSMiddleware` only listed `X-Total-Count` in `expose_headers`, and browsers hide any response header not on that allowlist from JS regardless of `allow_headers`. `ResultCount` fell back to `total` in both slots ("N total" only, never "Showing N matching"), which read as a plausible-looking but wrong UI state rather than an outright error — the kind of bug this project's own testing policy runs a real browser for specifically because unit/API-level tests structurally can't see it. Fixed by adding `X-Total-Unfiltered-Count` to `expose_headers`.

**A second, unrelated bug found and fixed to unblock verification, per this project's standing "fix, don't defer" rule:** `seed_demo_data.py`'s newly-added (by the parallel `project-files` workstream referenced above) direct-requirement-file-attachment call targeted `remote_id_req`, which `DRONE_REQUIREMENTS` seeds directly into `complete` status — already locked, so the direct-attachment endpoint 409s every time, deterministically, on a fresh database (confirmed: not a stale-data artifact). This blocked seeding "Solstice Cloud Platform" entirely, which every dashboard-navigation/result-count Playwright spec (including the pre-existing ones, not just this workstream's new ones) depends on. Retargeted to `preflight_req` (`draft` at that point in the script) instead — both this session and, independently, the parallel `project-files` workstream (see its own entry above, written concurrently) arrived at the identical fix.

**Verified.** Backend: `test_pagination.py` — 3 new tests (`test_requirements_list_reports_unfiltered_count_alongside_filtered_total`, `..._projects_list...`, `..._change_requests_list...`, each asserting `X-Total-Unfiltered-Count` stays fixed while `X-Total-Count` narrows under a filter/search term) plus the existing 10, **13/13**; full router-adjacent suite (`-k "requirement or change_request or project"`) **286/286**; a final targeted rerun (`test_pagination.py`, `test_requirement_lifecycle.py`, `test_reporting_and_changes.py`) after the CORS/seed-script fixes, **37/37**. Frontend: `tsc -b && vite build` clean; `eslint` clean (same 7 pre-existing unrelated warnings, no new ones); Storybook — new `ResultCount.stories.tsx` (2 states) and `FilterPanel.stories.tsx` (desktop-always-expanded, matching-count-shown, mobile-collapsed-with-toggle, using `page.viewport()` from `vitest/browser` to drive the responsive breakpoint in a real Chromium instance) plus the three target pages' and four incidentally-touched pages' existing stories, **66+83 passing across all reruns, 0 failing**. Playwright, against a live Docker Compose stack reseeded via both `seed_demo_data.py` and `seed_e2e_dataset.py`: `dashboard-navigation.spec.ts` — a new fourth test (clicking a "Requirements by status" chart segment with a network-delay-instrumented route to force the pre-fix race deterministically, then asserting every rendered status badge and the `ResultCount` text match the filter on first paint, not just the dropdown) verified to **fail** against the reverted pre-fix two-effect code and **pass** against the fix, alongside the pre-existing 3; new `result-count-and-filter-panel.spec.ts` (3 tests: `ResultCount`'s two states via both a status-badge click and the relocated search box, and the mobile-collapsed-by-default FilterPanel body with its own `ui_preferences`-reset-at-start-and-end for idempotency against the shared demo account) — **4+3 passing**, rerun back-to-back and in mixed order with `badge-filters.spec.ts` to confirm no ordering/leftover-state dependency; a broader regression batch (`golden-path.spec.ts`, `requirement-create-attach.spec.ts`, `project-users.spec.ts`, and five `e2e-workflows` specs touching the three target pages plus `notifications-and-favourites.spec.ts`/`project-actions-list-sorting.spec.ts` for the four incidentally-touched pages) — **16/16**.

Verification ran against a shared Docker Compose stack under genuine concurrent load from the parallel `project-files` workstream (see its own entry's "messy, heavily-contested shared test environment" note) — several `DROP SCHEMA public CASCADE` + reseed cycles were needed after the shared demo dataset was found repeatedly wiped/mutated by that concurrent activity mid-verification; no destructive action was taken beyond that documented recovery recipe, and no other workstream's files were edited.

Files changed: `backend/app/main.py`, `backend/app/routers/requirements.py`, `change_requests.py`, `projects.py`; `backend/tests/test_pagination.py`; `backend/scripts/seed_demo_data.py`; `frontend/src/api/client.ts`; `frontend/src/components/FilterPanel.tsx` (+ `.stories.tsx`), `ResultCount.tsx` (new, + `.stories.tsx`); `frontend/src/hooks/useNarrowViewport.ts` (new); `frontend/src/i18n/strings.ts`; `frontend/src/pages/RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, `NotificationsPage.tsx`, `ProjectActionsPage.tsx`, `ProjectReviewsDuePage.tsx`, `ServerOrganisationsPage.tsx`; `tests/playwright/tests/dashboard-navigation.spec.ts`, `result-count-and-filter-panel.spec.ts` (new); `docs/ux-style-guide.md`, `docs/decisions.md` (this entry).

## Generalized cross-project group permissions: any same-organisation project as a member source, plus project-referencing group members

Requested directly: "It would be good if you could use other projects/groups to give permission to a project. Especially useful for nested projects where an external party may be working in one project but they need to be given access to another project for understanding, and you want to use groups... to add them as members." Investigation found this was already largely built — `ProjectGroup` can nest an `OrgGroup` (C-U-12), and `ProjectMemberSource` already let a project consume baseline `MEMBER` access from a direct child (see this log's "Hierarchical projects" entry) — but two real gaps remained: `ProjectMemberSource` was restricted to a strict parent/child pair and could only ever grant bare `MEMBER`, and there was no way for a project group to be defined as "the members of some other project" directly. This entry covers both, plus the security review performed given this touches `services/rbac.py` (SOC2-governed, per `docs/soc2/policies/access-control-policy.md`).

**Scope decision, made with the user up front:** both gaps closed in one piece of work, not either/or — (a) generalize `ProjectMemberSource` beyond parent/child and beyond `MEMBER`-only, and (b) add a `ProjectGroupMember.source_project_id` reference type. Both remain **authorized entirely from the receiving project's side**, matching the existing parent/child design's asymmetry rationale exactly (a source project's manager is never consulted or notified — the same reasoning `ProjectMemberSource`'s own docstring already gives for why authorization isn't split the other way).

### 7a. `ProjectMemberSource` generalization

- **Schema** (`backend/app/models/project.py`): two new columns, `mirror_mode: ProjectRoleInheritanceMode` (default `MEMBER_ONLY` — byte-identical to every row's original behavior) and `mirror_filter_role: ProjectRole | None` (same `MIRROR_ROLE`-pairing validation as `Project.role_inheritance_filter_role`, reusing `_validate_role_inheritance_filter` in `schemas/project.py`, plus a new check rejecting `mirror_mode = NONE` — unlike the forward mechanism, `NONE` makes no sense here since the row's own existence already means "grant something").
- **Relaxed constraint**: the live revalidation in `services/rbac.py` (`_member_source_rows`, formerly `_member_source_frontier`) changed from `source_project.parent_project_id == project_id` to `source_project.organization_id == project.organization_id` plus a self-reference guard — any project in the same organisation, not just a structural child.
- **RBAC resolution rewritten to be mode-aware, per hop**, mirroring how `_forward_inherited_roles` already applies `role_inheritance_mode` for the forward direction: new `_member_source_derived_roles` (replaces the old boolean `_has_member_source_access`, used directly in `get_effective_project_roles`), `_member_source_role_holder_ids` (role-scoped, replaces the `role == MEMBER`-only special case in `get_project_users_by_role`), and a rewritten member-source section of `get_effective_project_members_with_provenance` that now names the actual contributing hop (`via_project_id`/`via_mode`) instead of always `None`/always `MEMBER`. A new `_direct_project_roles_excluding_org_wide` helper (the full-role-granularity counterpart to `_direct_project_member_ids`'s existing boolean exclusion) is what lets `MIRROR_ALL` mirror a source's *actual* direct roles without reintroducing the `ORG_WIDE`-baseline leak that helper's own docstring already warns against — the same class of bug a prior hardening pass on this exact mechanism found and fixed (see this log's "Hierarchical projects" entry).
- **Migration**: `alembic/versions/0017_generalized_cross_project_permissions.py` — `ADD COLUMN IF NOT EXISTS` for both, backfilling existing rows to `mirror_mode = 'member_only'` (identical behavior, no re-review of existing configuration needed).
- **Router** (`routers/projects.py::add_member_source`): accepts `mirror_mode`/`mirror_filter_role`, validates same-org instead of parent/child, still gated by `require_project_manage` on the receiving project only. **A second, easy-to-miss spot needed the same relaxation**: `_accessible_project_ids`'s own structural-visibility expansion walk (used to decide which projects show up in a user's project list at all) had its own hardcoded `Project.parent_project_id == ProjectMemberSource.project_id` check — generalizing only the grant-resolution side and missing this would have left a real, working grant invisible in the UI (the user could open the project by URL but it would never appear in their list). Fixed the same way, same-org join instead of parent/child.
- **Frontend** (`ProjectAdminPage.tsx`): the member-sources section's picker now offers any same-org project (`orgProjects`, already fetched for the parent-project picker — the now-dead `childProjects`/`GET .../children` fetch was removed rather than left as a backwards-compat shim), plus mode/filter-role selects mirroring the existing forward-inheritance controls. Each listed source shows its mode as a badge.

### 7b. `ProjectGroupMember.source_project_id`

- **Schema**: nullable `source_project_id` FK to `projects.id` (`ondelete="CASCADE"`), and the existing two-way "exactly one of user_id/org_group_id" check constraint widened to three-way.
- **Deliberately non-recursive (one hop only)** — this is the load-bearing design decision, not an implementation detail: "members of project X" means X's *direct* members only, resolved via a new `_direct_project_member_ids_base` helper that the existing `_direct_project_member_ids`/`_direct_project_role_holder_ids`/`_direct_effective_project_roles` all gained a third arm calling into (never into themselves, never into each other's full/recursive versions). This is what makes two projects referencing each other's rosters structurally incapable of causing unbounded recursion — a real scenario checked directly by a new test (`test_project_group_source_reference_is_one_hop_only`, a three-project X→Y→Z chain proving Z's members reach Y but not X). This mirrors, and is more conservative than, `ProjectMemberSource`'s own multi-hop chaining (source 6 deliberately *does* chain through a source's own further member-source list) — the two mechanisms made different tradeoffs here on purpose: `ProjectMemberSource` chaining was an existing, already-reviewed design property being preserved; a *new* recursive-by-default mechanism would have needed its own from-scratch cycle analysis this entry didn't want to take on when "one hop, no recursion possible at all" already satisfies the actual use case (referencing one other project's roster directly).
- Same-organisation constraint, same defense-in-depth shape as the existing nested-org-group arm (`OrgGroup.organization_id == Project.organization_id`, re-checked live at read time even though also enforced at write time).
- **Router** (`routers/projects.py::add_project_group_member`): the existing "exactly one of user_id/org_group_id" 400 check widened to three-way; a `source_project_id == project_id` self-reference guard added (structurally harmless given the one-hop design, but confusing/pointless to allow). `remove_project_group_member`'s delete `WHERE` clause gained the third match arm.
- **Frontend**: each project group's card gained a "reference another project's members…" picker alongside the existing user-autocomplete and org-group-nesting controls, listing referenced projects as `"<name>'s members"` bullets.

### Security review (identify → verify → remediate, per the change-management policy)

**Identify** — what could this weaken: (1) the same-org boundary being the *only* remaining constraint on `ProjectMemberSource`, replacing structural parent/child — could this let a project pull access from an org project its manager has no relationship to at all? (2) `ProjectGroupMember.source_project_id` reading a source project's members with zero consent from that project's own manager — is that a new escalation path? (3) does either mechanism reintroduce the `ORG_WIDE`-baseline leak a prior hardening pass already found and fixed once for this exact mechanism? (4) can the two mechanisms, or either mechanism against itself, be chained into unbounded recursion or a sibling-project leak the way source 5/6 were already deliberately decoupled to prevent?

**Verify**:
1. Same-org-only, not org-unrestricted — confirmed by `_member_source_rows`'/the new group arm's live re-join on `organization_id`, tested directly (`test_member_source_rejects_cross_org_source`, `test_project_group_source_reference_rejects_cross_org`, both asserting 400 at creation time against a genuinely separate organisation).
2. No consent required from the source side is **not a new escalation path** — it's the same authorization asymmetry `ProjectMemberSource`'s original design already established and documented the rationale for (a receiving project's manager already has the standing authority to grant any role to any user in their own project directly; sourcing that grant from another project's roster instead of typing emails one at a time doesn't grant anything the manager couldn't already grant by hand). Project names/existence are not confidential within an organisation (already visible via the ordinary project list to anyone with a role there), so this isn't a new information-disclosure path either. Confirmed both mechanisms remain unreachable from the source project's own management surface (`test_child_manager_cannot_add_itself_as_a_member_source`, pre-existing, still passes unchanged; `test_project_group_source_reference_rejects_self_reference` covers the group side's degenerate case).
3. `ORG_WIDE` leak: the new `_direct_project_roles_excluding_org_wide` helper (for `MIRROR_ALL`'s full-role mirroring) deliberately omits the `ORG_WIDE` visibility branch, matching `_direct_project_member_ids`'s existing exclusion — confirmed via `test_member_source_does_not_leak_org_wide_visibility_as_membership` (pre-existing, still passes unchanged; the generalization didn't touch this code path's exclusion, only added a new, separately-exclusion-respecting one alongside it).
4. Recursion/leak: `ProjectGroupMember.source_project_id` is structurally one-hop-only (see 7b above) — no BFS, no cycle possible by construction, verified directly. `ProjectMemberSource`'s existing multi-hop chaining and its decoupling from forward inheritance were already covered by `test_member_source_multilevel_requires_both_hops`/`test_member_source_listed_sibling_does_not_leak_via_mirror_all` (pre-existing, both still pass unchanged, confirming the generalization didn't disturb that property).

**Remediate**: no gap found beyond the two implementation spots already fixed as part of doing the generalization correctly (the `_accessible_project_ids` visibility-walk relaxation noted in 7a, and building the non-recursive `_direct_project_member_ids_base` split rather than a naive third arm on the existing recursive-capable function).

### Two existing Playwright tests updated for the behavior change

`project-hierarchy.spec.ts`'s `test_member_source_must_be_an_actual_direct_child` (backend) and its two UI-level equivalents no longer hold — replaced with `test_member_source_can_be_any_same_org_project_not_just_a_child` (backend, positive case) and the cross-org/self-reference rejection tests above. `test_stale_member_source_grants_nothing_after_child_reparented` assumed reparenting invalidates the grant (true only because the *old* constraint was structural parent/child); replaced with `test_member_source_survives_reparenting_within_same_org` (same-org reparenting no longer breaks the grant — a deliberate behavior change, not a regression) plus `test_member_source_stale_after_cross_org_move` (exercising the live-revalidation directly via a DB-level org move, since no product feature moves a project cross-org today to trigger it through the API). On the Playwright side, `project-hierarchy.spec.ts`'s existing "Gamma-4 has no way to manage that relationship" step asserted the old empty-state string (`"No direct sub-{projects} available to add."`, now `"No other {projects} available to add."`) and relied on Gamma-4 genuinely having zero candidates (true only under the old children-only restriction) — updated to assert the new, correct behavior (a picker now *does* show other same-org projects, proving the generalization end to end) rather than the coincidental old emptiness.

### Tests

Backend (`backend/tests/test_project_hierarchy.py`): 12 new tests covering both mechanisms — same-org acceptance/cross-org rejection/self-reference rejection for both, reparenting survival, the defensive stale-after-cross-org-move check, `MIRROR_ALL`/`MIRROR_ROLE`/default-`MEMBER_ONLY` mode behavior for `ProjectMemberSource`, and the one-hop-only non-recursion proof for the group mechanism. All existing tests in this file continue to pass unchanged except the two whose premises the generalization deliberately supersedes (see above).

Playwright: a new test in `project-hierarchy.spec.ts` (`generalized member-source (any same-org project, mirror modes) and project-referencing group members work end to end`) exercises both mechanisms through the real UI against Gamma-1/Gamma-2 (unrelated same-org projects) — self-healing against its own prior partial runs via a `cleanup()` helper called both defensively up front and in a `finally` block, since a mid-test failure would otherwise leave the shared Gamma fixture mutated for the next run. The existing "member sources are managed from the parent's admin page only" test's stale assertions were corrected in place (see above).

Storybook: `ProjectAdminPage.stories.tsx` gained mode-badge coverage in the existing `MemberSourcesListAndAdd` story (now also exercising the add-with-mirror-mode flow end to end against a mocked `api.post`) and a new `ProjectGroupCanReferenceAnotherProjectsMembers` story exercising the group-reference picker and its POST payload. `mockProjectAdminApis` gained a `groups` override (previously module-level-constant only) to support the new story without disturbing any existing one.

### Seed data

`backend/scripts/seed_demo_data.py`: Falcon-3 (an existing project, previously only demonstrating the parent/child member-source pair with its own Avionics sub-project) now additionally consumes stakeholders (`mirror_role`) from the *unrelated* Solstice Cloud Platform project, and Solstice Cloud Platform's own default "Stakeholders" group is defined as "Falcon-3's direct members" — one demonstration of each generalized mechanism, deliberately placed in the manual-demo dataset rather than the fixed E2E persona dataset so it can't collide with the new Playwright test's own dynamic Gamma-1/Gamma-2 fixture management. `add_member_source`'s helper signature gained optional `mirror_mode`/`mirror_filter_role` parameters (default to the original behavior when omitted, so every other existing call site — the Falcon-3/Avionics parent/child pair — is untouched); a new `get_project_group_id`/`add_project_group_source_reference` helper pair supports the group-reference demonstration.

### Docs

`services/rbac.py`'s module docstring updated from "six sources" to seven, documenting the generalized source 6 and the new source 7, with the same decoupling-rationale prose style as the existing five. `docs/soc2/policies/access-control-policy.md`'s item 2 (target-scoped resolution) gained a paragraph recording that both mechanisms were reviewed against that exact principle before being generalized, not exempted from it. `docs/solution-architecture.md`'s access-control ER diagram gained the `PROJECT |o--o{ PROJECT_GROUP_MEMBER : is` edge and updated prose for both mechanisms' generalized shape.

### Verified

Backend: the full suite, **634 passed, 0 failed**, run with `SMTP_HOST=localhost` (the suite's hardcoded `mailhog` default only resolves inside the Docker Compose network — an environment-invocation detail, not a code issue, that produced 7 spurious failures on an earlier host-run attempt, all confirmed unrelated and passing once corrected). `test_project_hierarchy.py` specifically: 42/42 (30 pre-existing + 12 new), rerun individually and as part of the full suite. Frontend: `tsc -b` and `npm run build` clean; `eslint` clean; the full Storybook suite, **562/562**, including the two updated/new `ProjectAdminPage.stories.tsx` interaction tests. Playwright: `project-hierarchy.spec.ts` — **5/5**, rerun twice back-to-back to confirm the new test's self-healing cleanup is genuinely idempotent (not just "passes once"); a regression batch (`project-admin-groups-and-fields.spec.ts`, `project-admin-structural.spec.ts`, `project-users.spec.ts`) — **3/3**, confirming the `ProjectGroup`/`ProjectMemberSource` type changes (new required fields) didn't disturb any other page consuming those types.

Migration applied cleanly to a freshly-reset schema (`DROP SCHEMA public CASCADE` + restart, confirming `alembic_version` lands on `0017`) and both seed scripts (`seed_e2e_dataset.py`, `seed_demo_data.py`) ran end to end against it without error, including the new demonstration calls.

Files changed: `backend/app/models/project.py`, `backend/app/services/rbac.py`, `backend/app/schemas/project.py`, `backend/app/routers/projects.py`; `backend/alembic/versions/0017_generalized_cross_project_permissions.py` (new); `backend/tests/test_project_hierarchy.py`; `backend/scripts/seed_demo_data.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/ProjectAdminPage.tsx` (+ `.stories.tsx`); `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`; `docs/solution-architecture.md`, `docs/soc2/policies/access-control-policy.md`, `docs/decisions.md` (this entry).

## Security uplift: full Dependabot + code-scanning remediation ("First stage of dependabot updates")

Requested as a review-and-fix pass across every open GitHub Dependabot alert (32) and code-scanning alert (7) on `feature/security-uplift-August-2026`. Touches authentication (OIDC ID-token verification) and a multi-tenant-adjacent search endpoint, so both fall under the change-management policy's identify → verify → remediate gate below, not just a routine dependency bump.

### Dependency upgrades (32 Dependabot alerts, all closed)

`backend/requirements.txt`: `python-jose[cryptography]` 3.3.0 → 3.5.0, `cryptography` 49.0.0 → 50.0.1, `python-multipart` 0.0.20 → 0.0.32, `python-dotenv` 1.0.1 → 1.2.3, `aiosmtplib` 3.0.2 → 5.1.2, `pytest`/`pytest-asyncio` 8.3.3/0.24.0 → 9.1.1/1.4.0 (pytest-asyncio bumped to stay compatible with pytest 9, though not itself flagged). `mcp-server/requirements.txt`: the same `pytest`/`pytest-asyncio` bump.

**`authlib` removed entirely** rather than bumped — 10 of the 32 alerts, including the critical JWS/JWK-header-injection signature-verification bypass (GHSA-wvwj-cvrp-7pv5), were against it. Its only use in the codebase was `authlib.jose` in `services/oidc_client.py::verify_id_token`, which authlib itself deprecated in favor of `joserfc` (now a direct dependency, `joserfc==1.7.4`, rather than pulled in transitively). Dropping the dependency closes all 10 alerts outright instead of chasing point-releases, and moves the OIDC ID-token verification path onto the library authlib's own maintainers now recommend for exactly this — see the security review below for what the migration itself changed.

`frontend/package.json`: `react-router-dom` 6.26.2 → 7.18.2 (the only real fix for GHSA-jjmj-jmhj-qwj2 — no patched release exists in the 6.x line; the app's usage is entirely "library mode" `BrowserRouter`/`Routes`/`Route`/hooks, none of the v7 data-router APIs, so the migration is a version bump with no code changes needed). `vite` 5.4.6 → 6.4.3 (the minimum fixing all three vite/esbuild CVEs; deliberately not 7.x or 8.x — `@vitejs/plugin-react`'s Babel-based transform triggers an upstream `optimizeDeps.esbuildOptions`-is-deprecated warning under vite 7/8's newer Rolldown-based optimizer regardless of which plugin-react version pairs with it, confirmed pre-existing and unrelated to this branch by reproducing it against the pre-upgrade `main` tree too — 6.4.3 avoids it entirely while still closing every CVE). `@vitejs/plugin-react` 4.3.1 → 4.7.0 (matching vite 6). `dompurify` added as a new direct dependency for the RichTextEditor fix below. `npm audit`: 0 vulnerabilities after `scripts/sync-lockfile.sh`.

### Code-scanning alerts (7, all fixed at the source, not dismissed)

1. **`orgs.py`/`scim.py` polynomial ReDoS (2 alerts).** `_EMAIL_LIKE` (a `^[^@\s]+@[^@\s]+\.[^@\s]+$`-style regex used to decide whether a project/org user-search query "looks like an email") and `_SIMPLE_EQ_FILTER` (parsing SCIM's `attr eq "value"` filter syntax) both had quadratic-or-worse backtracking on crafted input, and both take attacker-controlled strings (`q` on an org-member-visible search endpoint; `filter` on any SCIM-bearer-token holder's request) with no length cap. Rewritten as plain string operations (`str.partition`/`rfind`/slicing) instead of a single backtracking regex — genuinely linear, not just fast on the inputs a hand-written test happens to try. The first pass at the SCIM fix (anchoring a `\s+eq\s+"..."` regex to the end via `.search()`) was *itself* still polynomial on a long run of whitespace containing no "eq" at all — caught by deliberately stress-testing the fix against a 200k-character all-whitespace attack string before considering it done, not just re-testing the original crafted input the alert named.
2. **`local.py` path traversal (3 alerts, same underlying finding).** The existing guard (`pathlib.Path.resolve()` + `is_relative_to()`) was already correct, but CodeQL's `py/path-injection` sanitizer-recognition doesn't credit that idiom as a barrier. Rewritten using the exact `os.path.realpath()` + `str.startswith()` shape CodeQL's own rule documentation gives as the safe pattern, with identical confinement semantics — `test_local_storage_backend.py`'s existing traversal-rejection test still passes unchanged.
3. **RichTextEditor DOM XSS.** `editableRef.current.innerHTML = renderMarkdown(value)` reinterprets Markdown-rendered HTML built from a requirement/report field (attacker-influenceable in a multi-tenant app) without a sanitization barrier CodeQL recognizes. Added `DOMPurify.sanitize()` with an explicit tag/attribute allowlist matching exactly what `renderMarkdown` ever produces, applied at both this assignment and a second raw-HTML-injection spot found in the same component while fixing the first: `insertImage`'s `document.execCommand("insertHTML", ...)` interpolated an uploaded file's own `filename` into an `alt` attribute unescaped — a `"` in a filename breaks out of the attribute, a real (if narrow) stored-XSS vector via a crafted upload name, fixed by routing that fragment through the same sanitizer.
4. **Incomplete URL substring sanitization (test file).** `test_org_export_import.py` asserted `"smtp.example.com" in org_json_text` to confirm non-secret config survives export — CodeQL's heuristic reads a bare `"host" in text` as a URL/host-allowlist check a crafted value could spoof. Not a real security decision, but the test is more precise anyway asserting against the parsed field (`json.loads(org_json_text)["smtp_host"] == "smtp.example.com"`) rather than raw substring containment against the whole document.

### Security review (identify → verify → remediate, per the change-management policy)

**Identify** — what could removing `authlib` and rewriting `verify_id_token` on `joserfc` weaken: (1) does the new library actually verify the signature against the fetched JWKS, or could it be tricked into trusting an attacker-embedded key/algorithm the way the authlib CVE being removed allowed? (2) are `iss`/`aud`/`exp` still enforced with the same specificity (per-organisation issuer and client id, not just "some issuer")? (3) does the rewrite preserve `_assert_safe_external_url`'s SSRF guard on the JWKS fetch, untouched? (4) is there now *less* test coverage of this path than before, given the fix touches auth-critical code.

**Verify**:
1. `joserfc.jwt.decode(id_token, key_set, algorithms=_ALLOWED_ID_TOKEN_ALGORITHMS)` is called with an explicit allowlist of asymmetric algorithms only (`RS256/384/512`, `PS256/384/512`, `ES256/384/512` — no `none`, no `HS*`) — closing the specific "JWKS public key reused as an HMAC secret" algorithm-confusion class, the same root-cause family as this branch's own python-jose and Authlib CVEs. Proved, not just asserted: `test_verify_id_token_rejects_hs256_algorithm_confusion` signs a token with `HS256` using the RSA key's own public modulus as the HMAC secret and confirms it's rejected.
2. `iss`/`aud`/`exp` are checked twice — once via `JWTClaimsRegistry(iss=..., aud=..., exp=..., essential=True)` enforcing *presence* (OIDC Core 1.0 §2 requires all three), and again via the function's own existing explicit comparison against the specific per-organisation `issuer_url`/`client_id` (unchanged from before the migration, since that logic never touched authlib). Both halves are independently covered: `test_verify_id_token_rejects_wrong_issuer`, `..._wrong_audience`, `..._a_token_missing_exp`.
3. `_assert_safe_external_url` and `_internal_url` are untouched — the migration only replaced the decode/validate call, not anything upstream of it in `verify_id_token` or `discover`/`exchange_code_for_tokens`. Confirmed by diff, not just by assumption.
4. Test coverage *increased*, not decreased: before this change, `verify_id_token`'s actual cryptographic logic had **zero direct tests** — every existing caller-side test (`test_oidc_provisioning.py`, the OIDC callback tests) stubs it out via `monkeypatch.setattr`. Seven new tests in `test_oidc_client.py` exercise it directly with a locally-generated RSA key and real `joserfc`-signed tokens: valid-token acceptance, wrong-signing-key rejection, expiry, wrong issuer, wrong audience, missing `exp`, and the HS256-confusion attack above. Additionally verified against a **real IdP**, not just synthetic tokens: `tests/playwright/tests/e2e-workflows/sso.spec.ts`'s 5 tests drive the actual authorization-code flow against the Keycloak container and passed unchanged.

**Remediate**: no gap found — the migration is a net hardening (explicit algorithm allowlist and required-claim presence checks that authlib's call site never had) with materially better test coverage than what it replaced, not just a like-for-like swap.

### A regression the react-router 7 migration exposed in two existing Playwright tests, and a pre-existing non-idempotent test found while verifying it

React Router 7 wraps navigation state updates in React's `startTransition` by default (an opt-in future flag in 6, now the default) — this can keep a just-left page's content painted for one extra frame alongside the new page settling in underneath it. `golden-path.spec.ts`'s final "raise and approve a change request" step hit exactly this: a page-wide `getByText("Boot in under 3 seconds")` transiently matched both the change-request detail view's own "Name: Boot in under 3 seconds" changed-field summary and the new requirements list's card link during the transition, a strict-mode violation Playwright doesn't retry past. Confirmed as an upgrade-induced timing change, not a pre-existing flake, by reproducing the failure against this branch and then re-running the identical test against the pre-upgrade tree (stashing every dependency/source change) where it passed cleanly. Fixed by scoping the assertion to `getByRole("link", { name: "Boot in under 3 seconds" })` — the specific element that's actually unambiguous — matching this same spec file's own established pattern for exactly this class of ambiguity elsewhere.

Separately, running `sso.spec.ts` repeatedly against the same live dev stack while verifying the `joserfc` migration (see above) surfaced a genuine pre-existing non-idempotency: its setup step unconditionally `POST`s a new `OrgGroup` named `reqtrack-org-admins` onto the shared "Default Organization" fixture, which 400s ("idp sync target already in use") on any second run against a database that hasn't been reseeded from scratch — a real bug by this project's own testing-idempotency rule (must pass "whether it runs alone, first, last, or repeated back-to-back"), not something introduced by this branch, but found incidentally while working in this file and fixed per the project's standing "fix it, don't just note it" rule rather than left for someone else. There's no delete-group endpoint (`update_org_group`'s own docstring: "no rename endpoint exists for this... today"), so the fix looks up an existing group with that `idp_synced_group_name` first and `PATCH`es it back to the expected `granted_org_role` instead of always `POST`ing fresh — verified idempotent by running the same test four times back-to-back against the same unreset database.

### Tests

Backend: full suite, **646 passed, 0 failed**, run with `SMTP_HOST=localhost` (same pre-existing host-vs-Docker-network mailhog-resolution quirk noted in earlier entries — 7 spurious failures on a first host-run attempt, confirmed unrelated and passing once corrected). Includes 5 new tests: `test_looks_like_email_matches_the_original_regexs_semantics`/`..._is_not_vulnerable_to_polynomial_redos` (`test_invites_and_external_users.py`), `test_parse_simple_eq_filter_handles_the_bracket_filter_shape`/`..._rejects_malformed_expressions`/`..._is_not_vulnerable_to_polynomial_redos` (`test_scim.py`), plus 7 new `verify_id_token` tests in `test_oidc_client.py` (see Security review above). `ruff check` clean on every touched file.

Frontend: `tsc -b` and `npm run build` clean (react-router 7 + vite 6.4.3 + the new `dompurify` dependency); `eslint .` clean (same 7 pre-existing unrelated `react-refresh/only-export-components` warnings, confirmed pre-existing by reproducing against the pre-upgrade tree, no new ones); full Storybook suite, **67 files / 562 tests passed**, including `RichTextEditor.stories.tsx`'s interaction tests exercising the new `DOMPurify.sanitize()` call in a real Chromium instance (via `@vitest/browser-playwright`), not jsdom.

Playwright, against the real dev/test Docker Compose stack rebuilt from this branch: `sso.spec.ts` (**5/5**, real Keycloak authorization-code flow — the strongest available proof the `joserfc` migration verifies real-world RS256 ID tokens correctly, not just synthetic ones), `richtext-insert-link-modal.spec.ts` and `report-image-attachment.spec.ts` (**2/2**, the DOMPurify-sanitized code paths), `golden-path.spec.ts` (**1/1**, rerun 4 times back-to-back after the fix above), `dashboard-navigation.spec.ts`/`login-branding.spec.ts`/`org-login-2fa-handoff.spec.ts` (**9/9**, general navigation/routing regression coverage for the react-router 7 bump) — **17/17** run together in one batch to rule out cross-spec interference, plus the `sso.spec.ts` idempotency fix specifically reproduced-then-verified-fixed by running it 4 times back-to-back against the same unreset database.

### Docs

This entry. No SOC 2 policy gap closed (the authlib/algorithm-confusion risk removed here was never itself a documented "Known Gaps" entry, so there's nothing to update off `docs/soc2/policies/`'s control matrix) and no architecture change (the OIDC client's external shape — inputs, outputs, SSRF guard — is unchanged; only its internal JWT library is different).

Files changed: `backend/app/routers/orgs.py`, `backend/app/routers/scim.py`, `backend/app/services/oidc_client.py`, `backend/app/storage_backends/local.py`, `backend/requirements.txt`, `mcp-server/requirements.txt`; `backend/tests/test_invites_and_external_users.py`, `backend/tests/test_oidc_client.py`, `backend/tests/test_org_export_import.py`, `backend/tests/test_scim.py`; `frontend/package.json`, `frontend/package-lock.json`, `frontend/src/components/RichTextEditor.tsx`; `tests/playwright/tests/golden-path.spec.ts`, `tests/playwright/tests/e2e-workflows/sso.spec.ts`; `docs/decisions.md` (this entry).

## UI/UX feedback batch, Phase 1 + Phase 4: mobile filter panel ordering, and Org Admin's "View access" panel collapsed by default

Two independent, small, frontend-only phases from a larger seven-item UI/UX feedback batch (the other phases — "Completed" as an overlay marker, resend a pending invite, and the project membership/groups redesign — are separate, larger pieces of work tracked and implemented independently). Both phases here touch only frontend code/docs — no backend, migration, or API change.

### Phase 1: `FilterPanel` renders first on mobile, not after the whole list

`FilterPanel` (`frontend/src/components/FilterPanel.tsx`) was always the second DOM child of the shared `.side-grid` wrapper used by seven pages (`RequirementsPage.tsx`, `ChangeRequestsPage.tsx`, `ProjectListPage.tsx`, `ProjectReviewsDuePage.tsx`, `NotificationsPage.tsx`, `ProjectActionsPage.tsx`, `ServerOrganisationsPage.tsx`). `.side-grid`'s existing `@media (max-width: 860px)` collapse (`theme.css`) only reduced the grid to one column — it never reordered — so on a phone-width viewport a user had to scroll past the entire list/table to reach the filters.

Fixed with a stable `filter-panel` class added to `FilterPanel`'s root `<div>`, plus one new rule inside the *same* existing mobile-only media query: `.side-grid > .filter-panel { order: -1; }`. This only changes visual/paint order on narrow viewports (CSS `order`, not a DOM move) — desktop's two-column layout, `FilterPanel`'s own header/collapsible-body split, and its `useNarrowViewport`/`CollapsibleSection` accordion behaviour are all untouched.

### Phase 4: Org Admin's "View access" panel — collapsed roles by default, expand per row on demand

**Reverses a decision recorded 2026-08-24/25** in `docs/ux-style-guide.md`'s "Pattern: role display — effective highest role only" section, which explicitly carved this panel out as an access-audit exception that should "keep showing the full, uncollapsed role set." The user's new request reverses that — flagged back to the user during planning and resolved via an explicit choice (AskUserQuestion in the planning conversation): default to the same collapsed summary compact lists already use, with the full set available per project row on demand, so the audit detail isn't lost, just not the default view.

Implemented in `OrgAdminPage.tsx`'s `viewingUser` `SidePanel`: each project row now computes `collapseProjectRoles(p.roles)` (existing helper, `frontend/src/api/types.ts`) and shows that by default; a small "Show all N roles"/"Show fewer" `<button>` per row (local `expandedAccessProjectIds: Set<string>` state, reset whenever a different user's access is opened) toggles between the collapsed and full role list. Deliberately **not** built on `CollapsibleSection` — that component's collapse state is a per-user, cross-device *persisted* preference (`ui_preferences`), which is the wrong shape for a per-row, per-viewed-user, session-local disclosure that would otherwise leak state between different users' access panels or bloat the preferences bag with one key per project ever viewed.

`collapseProjectRoles()`'s own doc comment (`frontend/src/api/types.ts`) is updated to drop the now-stale "NOT applied to any access-audit view" carve-out and describe the new default-collapsed-with-expand behaviour. `docs/ux-style-guide.md`'s "Pattern: role display" section is updated in place (not replaced) to record the reversal and its date/reasoning, matching how earlier entries in that section already layer revisions rather than rewriting history.

A pre-existing Playwright assertion (`role-display-collapsing.spec.ts`'s last step, previously titled "Org Admin's 'View access' panel deliberately keeps showing every role uncollapsed") pinned the exact behaviour being reversed — updated to assert the new collapsed-default/expand-reveals-full-set behaviour instead. Verifying it also surfaced that the test's own fixture user ends up holding **4** effective roles, not the 3 a naive count of directly-granted group roles would suggest: `get_effective_project_roles`'s `_normalize` step (`backend/app/services/rbac.py`) implicitly adds `project_administrator`+`stakeholder`+`member` alongside a directly-granted `project_manager`, so "Show all N roles" reads N=4 there — the test comment now explains this rather than leaving a magic number.

### Tests

Frontend: `npx tsc -b` clean; `npm run lint` clean (7 pre-existing `react-refresh/only-export-components` warnings remain, all in files untouched by this work — `SortableHeader.tsx`, `BrandingContext.tsx`, `FavouritesContext.tsx`, `ThemeContext.tsx` — confirmed unrelated, no new warnings introduced). Full Storybook suite, **67 files / 563 tests passed**, including two new `OrgAdminPage.stories.tsx` stories: `ViewUserAccessPanel` (updated to also pin the collapsed default) and `ViewUserAccessPanelExpandRoles` (new — exercises expand then collapse-back).

Playwright, against the dev/test Docker Compose stack (frontend rebuilt + force-recreated first, since it doesn't bind-mount source): `result-count-and-filter-panel.spec.ts` — 5/5, including two new tests ("FilterPanel renders first on narrow viewports, unaffected on desktop" for Requirements and Change Requests pages, asserting rendered bounding-box position, not DOM order), rerun standalone a second time to confirm idempotency. `role-display-collapsing.spec.ts` and `user-access-panel.spec.ts` — 3/3, the former rerun standalone a second time to confirm its unique-per-run fixture data doesn't collide with itself. A broader regression batch of 12 files/22 tests touching `.side-grid` pages, mobile viewports, and Org Admin (`mobile-nav-rail.spec.ts`, `mobile-card-grid-overflow.spec.ts`, `multi-org-admin.spec.ts`, `single-org-admin.spec.ts`, `org-admin-nav-link.spec.ts`, `badge-filters.spec.ts`, `dashboard-navigation.spec.ts`, `pelion-v2.spec.ts`, `login-branding.spec.ts`, `mockup-engagement.spec.ts`, `golden-path.spec.ts`, `project-users.spec.ts`) — 22/22, confirming no regression from the shared `theme.css`/`OrgAdminPage.tsx` changes.

No backend changes, so the backend pytest suite wasn't re-run for this entry.

### Docs

This entry; `docs/ux-style-guide.md`'s "Pattern: role display" section (reversal recorded in place) and `collapseProjectRoles()`'s doc comment in `frontend/src/api/types.ts`.

Files changed: `frontend/src/components/FilterPanel.tsx`, `frontend/src/styles/theme.css`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`, `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`; `tests/playwright/tests/result-count-and-filter-panel.spec.ts`, `tests/playwright/tests/e2e-workflows/role-display-collapsing.spec.ts`; `docs/ux-style-guide.md`, `docs/decisions.md` (this entry).

## UI/UX feedback batch, Phase 2: "Completed" becomes an overlay marker, not a lifecycle status (C-G-11)

Third of the same seven-item UI/UX feedback batch (Phase 1 + Phase 4 above; Phase 3 "resend a pending invite" and Phase 5 "project membership/groups redesign" are separate, larger pieces of work tracked and implemented independently). Unlike Phases 1/4, this one is a correctness fix against `docs/requirements.md`, not a UI-opinion change: C-G-11 says plainly that "independently of lifecycle state, a requirement may be marked completed... as an overlay marker subject to the potential of periodic review where it may later be reversed to non-compliant." The pre-existing implementation (Massif v3, "Completion tracking (C-P-02, C-P-03)" entry above) baked `COMPLETED` into the same `RequirementStatus` enum as `draft`/`reviewed`/`approved`/`archived`, reachable only from `approved` — the opposite of "independently of lifecycle state." That entry is marked superseded in place above, for the `Requirement` half only; `ProjectStage`'s own completion tracking (`completed_at`/`completed_by`, `cascade_to_requirements`) was always correctly modelled and is unaffected.

### Data model

`Requirement` gains `is_completed: bool`, `completed_at: datetime | None`, `completed_by: UUID | None` — mirroring the existing `is_archived`/`archived_at`/`archived_by` overlay already on the same table. `RequirementStatus.COMPLETED` is removed, leaving `DRAFT`/`REVIEWED`/`APPROVED`/`ARCHIVED`, matching C-G-11's own list exactly. `StageStatus.COMPLETED` is untouched — stage completion is a separate, legitimate terminal status (C-P-02) that was never the problem.

Migration `0018_completion_overlay_marker.py` adds the three columns and backfills them from whatever was true before: every `Requirement` whose current `RequirementVersion.status == 'completed'` gets `is_completed=true`/`completed_at`/`completed_by` copied from that version's own `created_at`/`created_by` (who/when it was actually marked completed, since completing used to bump the version), then *every* `requirement_versions` row referencing `'completed'` — current and historical alike — is rewritten to `'approved'`. No Postgres `ALTER TYPE`/enum-recreation step was needed, unlike the usual pattern for dropping a native-Postgres-enum value: `RequirementStatus` is stored via `models.base.str_enum` (`native_enum=False`), a plain `VARCHAR` with no Postgres `CREATE TYPE`/`CHECK` constraint backing it at all — removing `COMPLETED` from the Python enum is purely an application-level validation change, so the migration only had to touch data. (This is a deviation from what the original implementation plan anticipated — it expected to need the standard create-new-type/migrate-column/drop-old-type dance and asked to check for precedent; there was none, because this codebase doesn't use native enums for exactly this reason.)

### Behaviour changes

- `services/requirements.py`'s `LOCKED_STATUSES` becomes `{APPROVED}` only — a completed requirement's `status` stays `APPROVED`, so `is_locked()` keyed on status alone is unaffected.
- `complete_requirement`/`uncomplete_requirement` (`routers/requirements.py`) now set/clear the three overlay fields directly on the `Requirement` row instead of calling `apply_new_version` — completing doesn't change content, so (unlike the old implementation) neither endpoint bumps the version number any more. `complete_requirement` also now explicitly rejects double-completing (409), not just a non-`APPROVED` status.
- `update_requirement`'s old "reject `status=completed` in the payload" special case is deleted outright — now that `COMPLETED` isn't a valid `RequirementStatus` value at all, Pydantic/enum request validation rejects it on the way in (422, not the old handler's 400).
- `services/stages.py`'s `complete_stage` cascade sets `is_completed`/`completed_at`/`completed_by` directly on each targeting `Requirement` instead of looping `apply_new_version` — same "don't bump the version" reasoning, still gated on current `status == APPROVED` and the existing `cascade_to_requirements` flag (and now also skips requirements already completed, to avoid clobbering an existing `completed_at`/`completed_by` on re-run).
- `record_review_outcome`: a `FAILED` outcome recorded against a currently-completed requirement now clears the completion overlay as part of recording that outcome — this is the concrete mechanism C-G-11's "may later be reversed to non-compliant... when a review/audit occurs" describes. Logged as a distinct audit event (`completion_cleared_by_review`) from `review_recorded`, not folded into its detail, so the reversal's own cause is independently traceable in the activity feed.
- `services/changes.py`'s `VERSION_HISTORY_COVERED_ACTIONS` (the dedup list that suppresses a generic `AuditEvent` in the project activity feed when a version-history entry already covers the same transition) had `completed`/`uncompleted` removed — they used to belong there because completing/uncompleting used to bump the version; now that neither does, their `AuditEvent` is the *only* record of the transition, and leaving them in that set would have silently dropped both actions from the activity feed entirely. Caught by `test_reporting_and_changes.py`'s existing duplicate-entry regression test failing during verification, not anticipated by the original plan — see "Deviations" below.

### Change-request decision: explicit "Approve and clear completion" choice

The specific design point flagged in the implementation plan and confirmed with the user before building it: a plain "Approve" on a `MODIFY_REQUIREMENT` change request must keep leaving `is_completed` exactly as it was (the existing, already-fixed carry-forward behaviour — `decide_change_request`'s `status_value=None` on the version-apply call was never responsible for the completion overlay either way, since that's a separate field entirely; its own comment is updated to say so rather than reference the now-removed `COMPLETED` status value). What was missing is a way for the approving project manager to say "this particular change is substantial enough that completion needs re-verifying" — added as an explicit, opt-in field rather than an automatic side effect of any edit:

- `ChangeRequestDecision` (`schemas/change_request.py`) gains `clear_completion: bool = False` — meaningful only when `approve=True`, `cr.kind == MODIFY_REQUIREMENT`, and the target requirement currently has `is_completed=True`; a no-op (not an error) otherwise, e.g. on a `NEW_REQUIREMENT`-kind CR or a target that isn't completed.
- `decide_change_request` applies it as a separate step *after* the existing version-apply logic (which is completely unchanged), clearing `is_completed`/`completed_at`/`completed_by` and logging a distinct `completion_cleared_via_change_request` audit event — so it's clear in the activity feed that the approver made that call deliberately, not that it happened as a side effect of approving the CR.
- Frontend (`ChangeRequestDetailPage.tsx`): when the target requirement is completed and the CR is `modify_requirement`-kind, a second "Approve and clear completion" button appears alongside the existing single "Approve" (unchanged everywhere else) — a typo-level CR gets plain Approve, a substantive one gets the second button, which drops the requirement back onto the due-for-review path.

### Frontend

`api/types.ts`: `RequirementStatus` drops `"completed"`; `Requirement` gains `is_completed`/`completed_at`/`completed_by`. `RequirementsPage.tsx`'s `STATUS_OPTIONS` drops `"completed"`; a new, independent `FilterCheckbox` ("Completed") sends `is_completed=true` to the list endpoint's new query param (`GET .../requirements` gained `is_completed: bool | None`, same filter-param pattern as the rest of that endpoint). Status badges (`RequirementsPage.tsx` tile/list views, `RequirementDetailPage.tsx`) keep showing lifecycle status unchanged, plus a separate small "Completed" badge shown alongside when `is_completed` is true — visually distinct, not merged into the status badge, since a completed requirement's status badge now always reads "Approved". `ProjectOverviewPage.tsx`'s "% complete" dashboard tile links to the requirements list's new `?completed=1` deep link instead of the old `?status=completed`. `ReportsPage.tsx`'s status filter drops its "Completed" `<option>` (the U-P-05 percentage-complete metric already covers this on the dashboard; reports don't gain a parallel `is_completed` filter in this pass). `backend/app/services/labels.py`'s `REQUIREMENT_STATUS_LABEL` (the PDF/CSV report label map, kept in sync by hand with the frontend's own map per that module's docstring) drops its now-dead `"completed"` entry too.

Dashboard/metrics: `routers/projects.py`'s U-P-05 percentage-complete metric (both the project-wide figure and the per-stage baseline breakdown) now queries `Requirement.is_completed.is_(True)` directly instead of `RequirementVersion.status == RequirementStatus.COMPLETED`.

### Deviations from the plan / judgment calls

- **No Postgres enum-type recreation** (covered above) — the plan anticipated needing it and asked to check for precedent; there was none, because `str_enum`'s `native_enum=False` means there's no Postgres enum type to alter in the first place. Simpler migration than planned.
- **`services/changes.py`'s `VERSION_HISTORY_COVERED_ACTIONS` fix** — not called out in the plan at all, found only because `test_reporting_and_changes.py::test_project_changes_has_no_duplicate_entries_for_version_bumping_actions` failed during the verification pass. Fixed immediately rather than deferred, per this repo's "fix, don't defer, found issues" rule; the test itself was updated to assert the new (correct) action-count breakdown rather than the old one.
- **Bundle export/import (`project_export.py`/`org_export.py`) is not backward-compatible with a bundle exported before this migration** — a bundle whose `requirement_versions[].status == "completed"` (from an older export) will fail import with a `ValueError` from `RequirementStatus("completed")`, rather than being silently coerced to `approved`+`is_completed=true`. No back-compat shim was added: this codebase's own stated pre-release posture (`0001_initial.py`'s docstring — "reasonable pre-release (no real deployments yet to preserve an incremental path for)") and the absence of any precedent for handling a stale enum value in either export module made a shim feel like unrequested scope; flagging it here explicitly rather than silently deciding it doesn't matter.
- **`requirement_csv.py` needed no changes** — despite being called out in the plan as a file to check, its `status` column already round-trips generically via `version.status.value` (which will simply never emit `"completed"` again), and it doesn't export `is_archived` either, so not exporting `is_completed` as a column keeps it consistent with the existing overlay-field precedent rather than a gap.
- **`seed_e2e_dataset.py` needed no changes** — confirmed by direct search that it has no `COMPLETED`/completion references of any kind (the Playwright suite's completed-requirement fixtures are all created dynamically within the specs that need them, per this repo's idempotent-test convention, rather than relying on seeded state).
- **`stage-review-and-completion.spec.ts` had a pre-existing non-idempotency bug, unrelated to this phase's own changes, found and fixed during verification.** The rewritten completion assertions (status stays "Approved", separate "Completed" badge) were a same-file, in-place edit at first — running the suite twice in a row (this repo's own standalone/repeat-safe convention) surfaced that the *original* test already couldn't be rerun against the same database without a reseed: it drove Alpha-2's single seeded stage through its entire one-way scoping→review→approved→completed lifecycle (no "reopen" endpoint exists), so a second run couldn't find "Start review" any more. Not a regression introduced by this phase, but found while working on this exact file, so fixed per this repo's "fix, don't defer" rule rather than left as a pre-existing flake: the spec now creates its own dynamically-named stage and two dynamically-named requirements targeting it each run, scoping every subsequent UI interaction to that specific stage's row rather than assuming it's the project's only one. Verified by running it twice back-to-back with no reseed in between.

### Tests

Backend pytest, all in `backend/tests/`: `test_stage_review_and_completion.py` gained `test_complete_and_uncomplete_do_not_bump_the_version_number`, `test_double_complete_is_rejected`, `test_uncomplete_rejects_a_requirement_that_is_not_completed`, `test_cascade_completion_is_still_gated_on_approved_status`, `test_completing_stage_without_cascade_leaves_requirements_uncompleted`, `test_failed_review_outcome_clears_completion`, `test_met_review_outcome_does_not_clear_completion`, and its existing completion tests updated for the new field shape. `test_requirement_lifecycle.py` gained `test_list_requirements_filters_by_is_completed`, `test_decide_change_request_with_clear_completion_clears_the_overlay`, `test_clear_completion_is_a_no_op_on_a_non_completed_requirement`, `test_clear_completion_is_a_no_op_on_a_new_requirement_kind_change_request`, and its `COMPLETED`-carry-forward regression test renamed/updated to `test_decide_change_request_plain_approve_preserves_completed_overlay`. `test_coverage_gaps.py` gained `test_project_metrics_completed_percent_reflects_is_completed_flag`. `test_project_export_import.py`'s rich-project fixture now also completes a requirement, with a new assertion that `is_completed`/`completed_at`/`completed_by` round-trip through export/import. `test_permission_matrix.py::test_direct_edit_cannot_jump_straight_to_completed` updated to expect 422 (schema validation) instead of the old handler's 400. `test_reporting_and_changes.py`'s duplicate-activity-entry regression test updated per the deviation above.

Full suite: `cd backend && pytest` (run inside the dev/test Docker Compose stack's own `backend` container, rebuilt from this branch first) — **658 passed, 0 failed** (72 pre-existing warnings, none new). `ruff check .` (backend) — all checks passed.

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as the Phase 1/4 entry above, none in files this phase touched). `npm run test-storybook` (full Storybook suite) — **67 files / 566 tests passed, 0 failed** — including new/updated stories: `RequirementDetailPage.stories.tsx`'s `CompletedRequirementShowsRevert` reworked for the new field shape and now also asserts the separate "Completed" badge; `RequirementsPage.stories.tsx`'s new `CompletedBadgeAndFilterCheckbox`; `ChangeRequestDetailPage.stories.tsx`'s new `SubmittedManagerSeesClearCompletionOptionForCompletedTarget` and `ApproveAndClearCompletionSendsFlag`; `ProjectOverviewPage.stories.tsx`'s fixture rebalanced to drop the `completed` bucket while keeping its sum-to-24 invariant.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, then `scripts/seed_demo_data.py` and `scripts/seed_e2e_dataset.py` re-run after the backend pytest run truncated all tables): `stage-review-and-completion.spec.ts` (rewritten completion assertions: status badge stays "Approved", separate "Completed" badge appears/disappears — see "Deviations" below for why this file also needed a standalone-idempotency fix, unrelated to the assertion rework itself), `requirements-and-cr-filters.spec.ts` (new "completed filter" step, confirms "Completed" is no longer a status `<select>` option), `change-request-approval-separation.spec.ts` (new test: a project manager sees and can use "Approve and clear completion" on a CR targeting a completed requirement, created as its own throwaway fixture rather than reusing shared seed data) — **all passed**, run both individually and in a combined regression batch alongside `requirement-approval.spec.ts`, `requirement-actions.spec.ts`, `requirement-links.spec.ts`, `badge-filters.spec.ts`, `result-count-and-filter-panel.spec.ts` (Phase 1/4's own, confirming no cross-phase regression), and `golden-path.spec.ts` — **16/16 passed**. `stage-review-and-completion.spec.ts` specifically was also run twice back-to-back with no reseed in between, to directly confirm the idempotency fix.

### Docs

This entry (plus the supersession note added to the "Completion tracking (C-P-02, C-P-03)" entry above); `docs/solution-architecture.md`'s core-requirements-domain section gained a paragraph on the `is_completed` overlay and a pointer back to this entry.

Files changed: `backend/app/models/requirement.py`, `backend/app/models/enums.py`, `backend/app/schemas/requirement.py`, `backend/app/schemas/change_request.py`, `backend/app/routers/requirements.py`, `backend/app/routers/change_requests.py`, `backend/app/routers/projects.py`, `backend/app/services/requirements.py`, `backend/app/services/stages.py`, `backend/app/services/changes.py`, `backend/app/services/labels.py`, `backend/app/services/project_export.py`, `backend/alembic/versions/0018_completion_overlay_marker.py` (new), `backend/scripts/seed_demo_data.py`; `backend/tests/test_stage_review_and_completion.py`, `backend/tests/test_requirement_lifecycle.py`, `backend/tests/test_coverage_gaps.py`, `backend/tests/test_project_export_import.py`, `backend/tests/test_permission_matrix.py`, `backend/tests/test_reporting_and_changes.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/RequirementsPage.tsx`, `frontend/src/pages/RequirementDetailPage.tsx`, `frontend/src/pages/ChangeRequestDetailPage.tsx`, `frontend/src/pages/ProjectOverviewPage.tsx`, `frontend/src/pages/ReportsPage.tsx`, `frontend/src/testing/storybook-helpers.tsx`, `frontend/src/pages/RequirementDetailPage.stories.tsx`, `frontend/src/pages/RequirementsPage.stories.tsx`, `frontend/src/pages/ChangeRequestDetailPage.stories.tsx`, `frontend/src/pages/ProjectOverviewPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/stage-review-and-completion.spec.ts`, `tests/playwright/tests/e2e-workflows/requirements-and-cr-filters.spec.ts`, `tests/playwright/tests/e2e-workflows/change-request-approval-separation.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry, plus the supersession note on the earlier Massif-v3 entry).

## UI/UX feedback batch, Phase 3: resend a pending invite

Fourth of the same seven-item UI/UX feedback batch (Phases 1/2/4 above; Phase 5 "project membership/groups redesign" is a separate, larger piece of work tracked and implemented independently, by a different agent, immediately after this one). Addresses the concrete complaint that motivated it: an invited-by-email user's "prod server didn't send one" had no recovery path — `services/invites.py`'s `create_pending_invite` was (and remains) the only way to trigger the invite email, and calling it again from the existing by-email "add member" flow creates a second `PendingInvite` row with a second, separate token rather than fixing the first one — and no endpoint listed a project's outstanding invites at all, so there was nothing to even confirm was stuck.

### Scope decision

Built for the standard (non-SSO) `PendingInvite` flow only, per the plan's own explicit scope cut: an `sso_only` organisation's by-email invites are provisioned immediately (`services.invites.provision_sso_invite`, no token, no `PendingInvite` row, access already granted) — there is nothing to "resend" there in the same sense, and the user's actual complaint (a stuck signup-link email) doesn't apply to that path. A parallel "resend the welcome email" for `sso_only`-org targets was deliberately not built this pass.

### Backend

- `GET /projects/{project_id}/pending-invites` (`routers/projects.py::list_pending_project_invites`) — lists a project's unaccepted `PendingInvite` rows (`project_id` matches, `accepted_at IS NULL`), newest first, including already-expired ones (an expired invite is exactly the case resending exists to fix, so it stays listed rather than disappearing). `status` (`"pending" | "expired"`) is computed at read time from `expires_at` vs. now, not stored. Gated by `require_project_manage` — the same dependency `assign_project_role_by_email` (the only endpoint that creates these rows) already uses, on the reasoning that listing/resending is a continuation of that same capability, not a new one.
- `POST /projects/{project_id}/pending-invites/{invite_id}/resend` (`resend_pending_project_invite`) — rotates the invite's token and `expires_at` and re-sends the signup-link email via `services.invites.resend_pending_invite` (new). A fresh token is issued rather than re-sending the stale one: the original may already have been exposed (logged, forwarded, sitting in an inbox nobody checks), so rotation is the safer default. Works whether the invite is currently pending or already expired; only an already-*accepted* invite is rejected (409), since there's nothing left to resend once someone's redeemed it. A cross-project `invite_id` (belonging to a different project than the one in the URL) 404s via a new `_get_pending_invite_in_project` helper, mirroring the existing `_get_group_in_project` guard against the same privilege-escalation shape (a manager of *some* project passing another project's id). Logs an `invite_resent` audit event (`services/audit.py::log_event`) with the invite's email in `detail`, following this codebase's existing per-mutating-endpoint audit-logging convention.
- `services/invites.py`: `create_pending_invite`'s and the new `resend_pending_invite`'s signup-link email body/subject were duplicated between the two initially; factored into a shared private `_send_invite_email` helper both call, rather than leaving two copies of the same wording to drift.
- Schema: `PendingInviteOut` (`backend/app/schemas/project.py`, next to `AssignByEmailOut`) — `id`, `email`, `role: ProjectRole`, `status: Literal["pending", "expired"]`, `created_at`, `expires_at`.
- SOC2: consulted `docs/soc2/policies/access-control-policy.md` before implementing, per this repo's CLAUDE.md requirement for anything touching invite/provisioning. Its Known Gaps item 4 (unverified-email-at-invite-time) already covers the original send; resending doesn't widen it — same caller-typed address, same single already-targeted grant, no new address reachable — so a short note was added to that item recording the review rather than opening a new gap entry.

### Frontend

New, deliberately self-contained component `frontend/src/components/PendingInvitesSection.tsx` — its own state and fetch (`GET .../pending-invites` on mount), a table (email/role/sent date/status/Resend button per row), toast feedback on resend success/failure (Principle 7, "every mutation ends with feedback"), and the label-map convention for the `pending`/`expired` status (`PENDING_INVITE_STATUS_LABEL`, `frontend/src/api/types.ts`) rather than the raw enum string. Deliberately built as a standalone component rather than inline JSX inside `ProjectAdminPage.tsx` — it's rendered from that page's "Project groups" tab (inside a `CollapsibleSection`, collapsed by default, alongside the existing "Effective members" section, both being member-directory concerns distinct from the groups list below) purely because that's where the equivalent Phase 5 "Members" page doesn't exist yet; per the plan's explicit sequencing note, the next agent (Phase 5, membership/groups redesign) is expected to fold this into a new Members page it creates, and moving a self-contained component's one usage + import is a much smaller diff for that agent than untangling inline JSX would have been. No cancel/revoke action — not asked for, not added speculatively.

No new endpoints were added under `/orgs` or any org-scoped path — this is purely a project-scoped addition, matching where the by-email invite flow that creates these rows already lives.

### Tests

Backend pytest (`backend/tests/test_invites_and_external_users.py`, extended in place — same file the rest of the by-email/invite flow is already tested in): `test_list_pending_invites_computes_pending_and_expired_status`, `test_list_pending_invites_excludes_already_accepted`, `test_resend_pending_invite_rotates_token_and_resends_email` (old token rejected at signup, new token redeems — same redemption pattern the existing invite-flow test already uses; `send_email` mocked/captured via `patch.object(invites_module, "send_email", ...)`, the same style `test_org_rename_and_test_email.py` already uses for the other email-sending endpoints), `test_resend_pending_invite_works_on_an_already_expired_invite`, `test_resend_pending_invite_rejects_an_already_accepted_invite` (409), `test_resend_pending_invite_404s_for_an_invite_belonging_to_a_different_project` (cross-project guard), `test_pending_invites_endpoints_are_gated_at_project_manage_tier` (403 for a plain project member on both new endpoints).

Playwright (`tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`): the existing signup-completion test's org/project setup was factored into a shared `setupInviteOrgAndProject` helper (each test needs its own isolated org/project so dynamically-named invites can't collide) and its MailHog-link extraction into `extractInviteLinksFromMailHog`, without changing its own assertions. A new second test sends an invite, confirms it's listed with "Pending" status (`ensureExpanded(page, "Pending invites")` + `getByRole("row", ...)`), clicks Resend, confirms the toast feedback and that a second, different signup link actually lands in MailHog (order-independent — asserts the link set contains something other than the original, rather than indexing into MailHog's own unspecified message ordering, which an earlier draft of this assertion got backwards and had to fix). Both tests run/pass standalone and rerun back-to-back with no reseed in between (idempotency).

Storybook (`frontend/src/pages/ProjectAdminPage.stories.tsx`): `mockProjectAdminApis` gained a `pendingInvites` override and a `/pending-invites` branch in its `api.get` mock; three new stories — `GroupsTabPendingInvitesEmpty`, `GroupsTabPendingInvitesShowsPendingAndExpired` (both status labels, not raw enum strings), `GroupsTabPendingInvitesResend` (asserts the exact `api.post` call and the resulting toast).

### Verification

Backend: full suite via the dev/test Docker Compose stack (backend rebuilt + force-recreated first) — **665 passed, 0 failed**, 72 pre-existing warnings (same count as the Phase 2 entry above, none new). `ruff check .` — all checks passed.

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as the Phase 1/2/4 entries, all in files untouched by this work — confirmed, not just assumed). `npm run test-storybook` (full suite) — **67 files / 569 tests passed, 0 failed**, including the three new stories above.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first): `external-project-invite.spec.ts` — **2/2 passed**, run twice back-to-back with no reseed in between to confirm idempotency; `project-admin-groups-and-fields.spec.ts` (regression check on `ProjectAdminPage.tsx`'s Groups tab, which this phase's new section shares a tab with) — **1/1 passed**.

Rebuilding+recreating the backend container for this verification pass surfaced, again, the shared-test-DB wedge this repo's memory notes already describe (`backend`'s `DATABASE_URL` in `tests/container/docker-compose.yml` points at the same `reqtrack_test` database `pytest` truncates between/after every test) — running the full backend pytest suite wiped the demo/e2e seed data the just-rebuilt frontend/backend needed for the Playwright run, including the bootstrap server-admin account, which required restarting the backend container (to re-run bootstrap) and re-running both `seed_demo_data.py` and `seed_e2e_dataset.py` before Playwright could log in at all. Not a bug introduced by this phase — a pre-existing environment characteristic — but recorded here since it's exactly the sequencing this repo's own testing conventions call for after touching both suites in one verification pass.

### Docs

This entry; `docs/solution-architecture.md` gained an "Implementation note (resending a pending invite)" paragraph in the Application Layer section; `docs/soc2/policies/access-control-policy.md`'s Known Gaps item 4 gained a short note recording that this addition was reviewed against it and doesn't widen it.

Files changed: `backend/app/schemas/project.py`, `backend/app/services/invites.py`, `backend/app/routers/projects.py`; `backend/tests/test_invites_and_external_users.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/components/PendingInvitesSection.tsx` (new), `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`; `docs/solution-architecture.md`, `docs/soc2/policies/access-control-policy.md`, `docs/decisions.md` (this entry).

## UI/UX feedback batch, Phase 5: project membership/groups redesign

Fifth and last of the same seven-item UI/UX feedback batch (Phases 1/2/4 in this log's earlier entries, Phase 3 immediately above). Addresses the three remaining items together, since all three converge on the same shared component: **project groups' role was fixed at creation** (no way to add members first and assign a role later); **Org Admin's per-project "Manage users" duplicated a worse, bespoke version of the project admin page's own membership UI**; and **Members and Groups were one combined "Project groups" tab**, making a mixed directory of two structurally different row shapes (individual users, project groups) harder to scan and impossible to deep-link into.

### Backend (`backend/app/routers/projects.py`, `backend/app/schemas/project.py`)

- **`PATCH /{project_id}/groups/{group_id}`** (`ProjectGroupUpdate{role: ProjectRole}`, modeled directly on `orgs.py::update_org_group`/`OrgGroupUpdate`) makes a `ProjectGroup`'s role editable after creation. No data migration needed — `services/rbac.py`'s effective-role resolution already reads `ProjectGroup.role` live at query time, confirmed by reading `get_effective_project_roles`/`get_effective_project_members_with_provenance` before assuming it, per the plan's own instruction. Gated `require_project_manage` (same tier `create_project_group`/`add_project_group_member` already use). Guard (C-U-08): moving a group's role *away from* `PROJECT_MANAGER` locks the project row (`lock_project_for_update`), tentatively sets the new role and flushes, then re-checks `get_effective_project_managers` is non-empty before commit — raising instead aborts via the request-scoped session's own close-without-commit (no explicit rollback needed), the same idiom `remove_project_group_member`/`revoke_project_role` already use. Because the check re-derives *actual* effective managers post-flush rather than pre-checking "was this specific group the target," it correctly allows the change whenever a backup manager source exists (a second group, or a direct grant) — pinned by `test_update_group_role_away_from_manager_allowed_with_backup_manager`.
- **`DELETE /{project_id}/groups/{group_id}`** (new — grep confirmed no such route existed). 400s for one of the four `is_default=True` standard groups (C-U-10) regardless of C-U-08 (checked first, since deleting a default group is never allowed at all, independent of whether it happens to also be the sole manager source). Otherwise, same lock-then-flush-then-recheck C-U-08 guard as the `PATCH` above, shaped for a whole-group removal: `ProjectGroupMember` rows cascade via the FK's existing `ondelete="CASCADE"`, so the check has to run *after* the delete is flushed (a pre-check the way `remove_project_group_member`'s single-membership check works would have to separately reproduce "what would managers look like without every membership this group grants," rather than just asking the ordinary live-state question afterward).
- **`GET /{project_id}/direct-members`** (`DirectMemberOut{user_id, display_name, email, roles: list[ProjectRole]}`, new) lists every user holding at least one direct `UserProjectRole` grant, one row per user with a real multi-valued `roles` array — confirmed by reading the model that `UserProjectRole`'s unique constraint is `(user_id, project_id, role)`, not one row per user, so a user can hold e.g. both `stakeholder` and `member` as simultaneous direct grants; force-collapsing to one role per row would have been a real regression against that. Gated `require_project_view_or_manage` (same tier `list_project_groups` uses — this exposes role *structure*, not requirement/change-request content, so any genuine project member can see it, not just managers). `GET /{project_id}/effective-members` was deliberately left untouched — confirmed it answers a different question (resolved-per-user-with-provenance for audit) from this new direct-grants listing, and continues to power the pre-existing "Effective members" table unchanged.
- **SOC2 review (identify → verify → remediate, per `docs/soc2/policies/change-management-and-secure-development-policy.md`'s practice for security-sensitive changes, since this is RBAC-adjacent — consulted `access-control-policy.md` first):**
  - *Identify:* the two new mutating endpoints could in principle let a lower-privileged caller reach `PROJECT_MANAGER` for themselves by repurposing an *existing* group's role rather than creating a new one.
  - *Verify:* `create_project_group` + `add_project_group_member` were already gated at `require_project_manage` before this phase — a `project_administrator` (below manager tier but still passing that gate) could already create a brand-new group with `role=project_manager` and add themselves to it. `PATCH .../groups/{group_id}` at the same gate tier adds an *alternate path to a capability that already existed*, not a new privilege boundary crossing — consistent with the plan's explicit choice to gate it identically. Cross-project IDOR: both new mutating endpoints route through the existing `_get_group_in_project` helper (404 unless the group belongs to the `project_id` in the URL), the same guard `add_project_group_member`/`remove_project_group_member` already rely on. `GET /direct-members`'s gate tier matches `list_project_groups` exactly, so no new disclosure beyond what that endpoint (and the by-role/by-email grant endpoints, which already imply the same information) already exposes.
  - *Remediate:* no gap found requiring a code change beyond what was already built; nothing added to `access-control-policy.md`'s Known Gaps, since this phase closes a product/UX gap (group role immutability) without opening or widening a documented security exception.

### Frontend

- **New shared component, `frontend/src/components/MemberRoleTable.tsx`** — the "add a user or group, then assign a role" table, built from existing primitives only (`SortableHeader`/`cycleSort`, `MultiSelectDropdown`, `LoadMoreButton`, `react-router-dom`'s `Link`). Rows are a `kind: "user" | "group"` discriminated union (`MemberRoleRow`); a `Type` badge column plus `SortableHeader`-sortable Name/Email columns match the existing "Effective members" table shape and Org Admin's Users table, per the style guide's already-recorded "project-scoped member lists match Org Admin's Users table" pattern. The Role cell deliberately differs by row kind — a user row gets `MultiSelectDropdown` (`UserProjectRole` is genuinely multi-valued, the same treatment `OrgRole` already gets on `OrgAdminPage.tsx`'s Users table); a group row gets a plain `<select className="input">` (`ProjectGroup.role` is one column, genuinely single-valued) — this is intentional, not an inconsistency the component should "fix." Both wire a client-side "last manager" disabled+`title` hint (computed from how many rows in the table currently grant `project_manager` at all), mirroring `MultiSelectDropdown`'s existing self-role-revoke treatment on `OrgAdminPage.tsx` — an approximation only (it can't see e.g. a forward-inherited manager from a parent project), with the backend guard above remaining authoritative regardless. `search` plus a client-side "load more" window (not a fresh network request per page) since both call sites merge two already-fully-loaded, unpaginated fetches (`GET .../groups` with no `limit`, `GET .../direct-members`) rather than paginating server-side — the same "small enough to load whole, search/paginate client-side" precedent `GET .../effective-members` already established, extended here to a second, newly-added source. `addControl` is a caller-supplied `ReactNode` slot (composing `UserAutocomplete` + a role `<select>`), since what can be added differs slightly per call site. Final prop shape: `{ rows: MemberRoleRow[]; onToggleUserRole(userId, role, checked); onChangeGroupRole(groupId, role); groupHref?(groupId): string; addControl?: ReactNode; ariaLabel: string }`.
  - **Deviation from the plan's literal wording:** the plan described the group-row navigation prop as `onGroupClick`; built instead as `groupHref?: (groupId: string) => string`, rendering a real `<Link>` rather than a button+`onClick` — matching this codebase's own stated "a real URL, not client-only state" bookmarkability principle (`ResourceMenu`'s own framing, which the plan itself invokes for the `?openGroup=` deep link one paragraph later) more directly than a callback would have. The `aria-label` originally planned for that link was dropped after a Storybook story caught it silently replacing the link's own visible name (the same "accessible-name-from-content" pitfall `SortableHeader.tsx`'s own code comment already documents) — replaced with a `title` tooltip instead, which doesn't have that effect.
- **`frontend/src/pages/ProjectAdminPage.tsx`:** `ProjectAdminGroupKey` gains `"members"`, inserted before `"groups"` in `projectAdminGroups` — no routing change needed. **Members** section: `MemberRoleTable` fed by `memberTableGroups` (new state, `GET .../groups` with no `limit`) merged with `directMembers` (new state, `GET .../direct-members`) into `MemberRoleRow[]`; an add-control (`UserAutocomplete` + role `<select>`) calling `POST .../roles` for an existing org member or reusing the existing `addExternalMember(email, role)` for the by-email/invite branch; `PendingInvitesSection` (Phase 3) and the pre-existing "Effective members" `CollapsibleSection` both relocated here unchanged, below the new table (a different question — resolved-with-provenance audit data — from the new table's direct-grants editing, so not merged into it). Role-toggle/role-change handlers (`toggleDirectMemberRole`, `changeGroupRole`) update `directMembers`/`memberTableGroups` (and, for `changeGroupRole`, also the Groups section's own `groups` state) locally on success rather than calling the page's giant `reload()` — the same fix the style guide's "Pattern: multi-select dropdown" section already recorded for `OrgAdminPage.tsx`'s analogous `grantOrgRole`/`revokeOrgRole`, both to avoid an unnecessary full-page refetch and to avoid the exact stale-reload race that fix was written to close. **Groups** section: unchanged `GET .../groups` + `LoadMoreButton` + search + "+ New group" `Modal` (`ProjectGroupCreate` still requires a role at creation — deliberately *not* made optional even though `PATCH` now makes it correctable afterward, matching the plan's explicit scope boundary). Each group row is now a plain button opening a `SidePanel` (`openGroupId` state) instead of an always-expanded `CollapsibleSection` accordion, containing the pre-existing 3-way composition editor (direct users/nested org groups/nested source-project refs — same handlers, relocated unchanged) plus a `Group role` `<select>` at the top (`changeGroupRole`) and a `Delete group` button (disabled + explanatory `title` for a default group, otherwise opening a Tier-1 `ConfirmDialog` before `DELETE .../groups/{group_id}`). A `member_source_project_ids` reference now also renders a `Link` to that project's own new Members page plus a clarifying "this is live, not a fixed list" hint line, extending the pre-existing `viaProjectMembers` labelled-line treatment (which already correctly never let one of these be edited/removed member-by-member) rather than replacing it. **`?openGroup={groupId}` deep link:** a `useSearchParams()` effect, scoped to the Groups section being active, opens that group's `SidePanel` on load and clears the param via `setSearchParams(..., { replace: true })` so browser-back doesn't reopen it — resolved first against `memberTableGroups` (the Members section's unpaginated fetch) rather than the Groups section's own paginated `groups`, so a deep-linked group not on the currently-loaded page is still found. Incidental fix while in this code: the org-group-nesting `<select>` inside the group panel had no `aria-label` (unlike its sibling source-project-reference `<select>`, which already had one) — added, since leaving it out was both a real accessibility gap and made the new `SidePanel`'s two `<select>`s ambiguous to `getByRole` queries once a `Group role` select was added alongside it.
- **`frontend/src/pages/OrgAdminPage.tsx`:** the inline "Manage users" expand-in-place (`expandedProjectId`/`expandedProjectGroups`/`toggleExpandedProject`/`addExpandedProjectGroupMember`/`removeExpandedProjectGroupMember`) is deleted outright, replaced by a `Modal` (`size="lg"`) opened by the existing "Manage users" button, wrapping the same `MemberRoleTable` — fed by the same two endpoints (`manageUsersGroups`/`manageUsersDirectMembers` state, fetched fresh on open via `openManageUsers`), with its own add-control and its own local-state-update-on-success role handlers mirroring `ProjectAdminPage.tsx`'s. `groupHref` wires to that project's own `/projects/{id}/admin/groups?openGroup={groupId}` page — a small extra beyond the plan's literal ask, since it was a one-line reuse of a prop the component already needed for the other call site, and gives an org admin a genuine way to jump from "manage users" straight into a group's full composition editor rather than a dead end.

### Tests

Backend pytest (new file, `backend/tests/test_project_group_role_editing.py`): `test_update_group_role_reflected_live_with_no_membership_row_change`, `test_update_group_role_no_op_when_role_unchanged`, `test_update_group_role_away_from_manager_blocked_when_sole_manager`, `test_update_group_role_away_from_manager_allowed_with_backup_manager`, `test_delete_default_group_is_rejected`, `test_delete_custom_manager_group_blocked_when_sole_manager`, `test_delete_custom_manager_group_allowed_with_backup_manager`, `test_update_and_delete_group_gated_at_manage_tier` (403 below manage tier; success for an org admin holding no project role at all, per `can_manage_project_settings`'s existing carve-out), `test_direct_members_gated_at_view_or_manage_tier`, `test_direct_members_returns_multi_role_array_per_user`. `test_permission_matrix.py` was checked and does **not** track a route inventory to extend (it's a flat set of discrete behavioural tests, not an enumerated endpoint table) — the plan's own instruction to extend it was conditional ("if that file tracks a route inventory"), so this was a no-op by design, not an oversight.

Storybook: `frontend/src/components/MemberRoleTable.stories.tsx` (new) — `MixedUserAndGroupRows`, `EmptyState`, `AtScalePaginated`, `LastManagerRoleControlDisabled`, `SearchNarrowed`, `WithAddControl`. `frontend/src/pages/ProjectAdminPage.stories.tsx` — the old `GroupsTabAddMember`/`GroupsTabAddOrgGroup` rewritten as `GroupsTabOpensSidePanelWithRoleSelectAndDelete`/`GroupsTabAddMemberViaSidePanel`/`GroupsTabAddOrgGroupViaSidePanel`, plus new `GroupsTabDeleteDefaultGroupDisabled`/`GroupsTabDeleteGroupRequiresConfirmation`; `GroupsTabPendingInvites*` and `EffectiveMembers*`/`MaterializeButtonConvertsInheritedAccess` retargeted to click the new "Members" nav link; `ProjectGroupCanReferenceAnotherProjectsMembers` extended for the new `Link`/hint; new `MembersTabShowsUsersAndGroups`, `MembersTabToggleDirectMemberRole`, `MembersTabChangeGroupRoleReflectsOnGroupsTab`, `MembersTabAddDirectMember`. `frontend/src/pages/OrgAdminPage.stories.tsx` — `ProjectGroupRoleBadgeUsesLabelMap` rewritten against the `Modal`+`MemberRoleTable` shape (a fresh `mockProjectsWorkflowWithOneProject` helper, needed because `mockOrgAdminApis`'s own generic `api.get` mock resolves any `"/groups"`-containing path to the *org*-scoped fixture before a project-scoped path can be reached — the same reason the pre-Phase-5 version of this exact story already needed a full override); new `ManageUsersModalShowsMemberRoleTable`, `ManageUsersModalChangeGroupRole`, `ManageUsersModalAddControlRenders`.
- **Deviation:** a `GroupsTabOpenGroupDeepLink` Storybook story was attempted and dropped — `ProjectAdminPage.stories.tsx`'s `meta` already fixes one `withRouter` initial path for every story in the file, and react-router refuses to mount a second `<Router>` nested inside another, so no per-story initial-URL override is possible without restructuring every other story's decorators. Covered by Playwright instead (below), which has no such constraint.

Playwright: **new** `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts` (create a group on the Groups page, change its role from the Members table, confirm it reflects on the Groups page's `SidePanel` with no refetch, add a direct member with a role via the Members page, the C-U-08 last-manager guard surfacing as a disabled role control, and the `?openGroup=` deep-link round trip — all against a dedicated throwaway project created via the API, not a shared seeded one, since the last-manager scenario needs a project whose only manager source is known and can't have been mutated by another spec sharing the suite's run) and `tests/playwright/tests/e2e-workflows/org-admin-manage-users-modal.spec.ts` (the Modal opens the same `MemberRoleTable`, its role controls work, and closing it returns to the plain project row — zero prior Playwright coverage existed for "Manage users" at all, confirmed by grep before writing this). **Updated** for the Groups-tab SidePanel restructuring (a new `openProjectGroupPanel` helper added to `helpers.ts`, replacing `openGroupCard` at every call site that targeted `ProjectAdminPage`'s own groups — `openGroupCard` itself is untouched and still correct for `OrgAdminPage`'s own group cards, which kept their `CollapsibleSection` accordion): `project-admin-groups-and-fields.spec.ts`, `project-hierarchy.spec.ts` (two steps — the "Members" group's project-reference add, and effective-members retargeted from "Project groups" to the new "Members" section), `org-security-controls.spec.ts`, `role-display-collapsing.spec.ts` (also fixed a now-stale "no delete-project-group endpoint exists" code comment), `external-project-invite.spec.ts` (both invite-by-email tests, plus its pending-invites-list step retargeted to "Members"), and `tests/project-users.spec.ts` (retargeted to "Members"; its own table/search-box locators scoped to the "Effective members" section specifically, since the new `MemberRoleTable` on the same section now also has an "Email" column header and a search box whose placeholder is a superset match of the old unscoped `getByPlaceholder("Search members")` query).

### Verification

Backend: full suite via the dev/test Docker Compose stack (backend rebuilt + force-recreated first) — **675 passed, 0 failed**, 72 pre-existing warnings (same count/content as every earlier phase's own entry, none new). `ruff check .` — all checks passed. New `test_project_group_role_editing.py` (10 tests) was also run standalone against a local Postgres directly (bypassing the container) during development — 10/10 passed there first, confirming the endpoint logic itself before the full-suite/container run. (A first container run produced a wall of spurious `E`/`F` results starting partway through — traced to two `pytest` invocations having been started concurrently against the same shared test database, the exact wedge shape this repo's own memory notes describe; not a real regression. Fixed by killing both, restarting the `backend` container, and re-running exactly once.)

Frontend: `npx tsc -b --force` clean; `npx eslint .` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier phase's own entry, none in any file this phase touched). `npx vitest run --project=storybook` (full suite) — **68 files / 587 tests passed, 0 failed**, run twice back-to-back with zero unhandled errors on either run (see below for a flake that appeared, and was fixed, before this final pair of clean runs). Every story touched or added by this phase was additionally run in isolation first and confirmed passing before the full-suite run (`MemberRoleTable.stories.tsx`: 8/8; `ProjectAdminPage.stories.tsx`: 43/43; `OrgAdminPage.stories.tsx`: 46/46).

Four real bugs were caught during this verification pass and fixed, not deferred:
1. A `getByRole("combobox")` in a Groups `SidePanel` story became ambiguous once the panel gained a second `<select>` (the new `Group role` one) — fixed by scoping to the specific input.
2. `EffectiveMembersSortByEmail`'s `getAllByRole("row")` picked up the new `MemberRoleTable`'s own rows once it started sharing a page with the Effective Members table — fixed by scoping to the Effective Members `CollapsibleSection`'s own card.
3. `MemberRoleTable`'s group-row `Link` originally carried an `aria-label` that silently replaced its visible name as the accessible name (see the component's own deviation note above) — a `Members Tab Shows Users And Groups` story assertion caught it (`getByRole("cell", { name: "Stakeholders" })` failed since the cell's computed name had become "View Stakeholders's members").
4. **A genuine app bug, found only once the full Storybook suite ran repeatedly:** `externalAddResult` (the by-email invite/add outcome message) was only rendered inside the new Members section — the Groups section's own per-group `SidePanel` invite-by-email flow (`addExternalMember`, unchanged, still reachable from there) sets the exact same state but had nowhere left to show it, since that rendering call site moved with the rest of the Members-section JSX during the split. Silent on the Groups tab: an admin inviting someone by email from a group's own panel got no on-screen confirmation their invite was sent at all (Principle 7 violation) — caught by three separate `external-project-invite.spec.ts`/`org-security-controls.spec.ts` Playwright failures (below), not by Storybook, since no existing story asserted on that specific banner from the Groups tab. Fixed by rendering `externalAddResult` a second time, inside the group `SidePanel`, next to its own `UserAutocomplete`.
5. **A genuine, reproducible test-timing bug**, not a real app defect: `GroupsTabAddMemberViaSidePanel` typed into the panel's `UserAutocomplete` (armed with `organizationId`, so a 250ms debounced search fetch) and ended without closing the panel, leaving that debounce timer's effect cleanup to run only whenever the Storybook/vitest test runner itself got around to tearing down that story's tree — which, empirically, did not always happen before the timer fired, producing an intermittent "Cannot read properties of undefined (reading 'map')" in the *unrelated, untouched* `UserAutocomplete.tsx`, caught by vitest as an "Unhandled Error" (not a failed assertion — all 587 tests passed even on the run(s) where this fired). Root-caused by noting it was 100% reproducible at the same point across multiple full-suite runs, and that no other story left an armed debounce timer un-torn-down at story end. Fixed by having the story explicitly close the panel (a real `SidePanel`/`onClose` unmount within this app's own lifecycle, not a hope that the test harness tears the tree down first) before ending — confirmed clean (zero unhandled errors) on two consecutive full-suite runs afterward.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first; `seed_demo_data.py`/`seed_e2e_dataset.py` both ran clean with no errors, confirming they're still in sync with the current schema/endpoints per this repo's testing conventions — no changes needed to either): every spec this phase touched or added, run together — **19/19 passed** (`project-admin-members.spec.ts` (new, 1), `org-admin-manage-users-modal.spec.ts` (new, 1), `project-admin-groups-and-fields.spec.ts` (1), `project-users.spec.ts` (1), `project-hierarchy.spec.ts` (5), `org-security-controls.spec.ts` (1), `role-display-collapsing.spec.ts` (1), `external-project-invite.spec.ts` (2)), plus a broader same-run regression sweep of specs exercising `OrgAdminPage`/`ProjectAdminPage`/shared components this phase didn't directly touch — **6/6 passed** (`golden-path.spec.ts`, `org-group-nesting.spec.ts`, `single-org-admin.spec.ts`, `multi-org-admin.spec.ts`, `mockup-engagement.spec.ts`, `pelion-v2.spec.ts`). Two genuine bugs in this phase's own new spec files were found and fixed during this run: `project-admin-members.spec.ts` and `org-admin-manage-users-modal.spec.ts` each asserted `getByRole("cell", { name: PERSONAS.<x>.name })` without `exact: true` — Playwright's role-name matching defaults to substring (unlike Testing Library's default-exact, which is why the equivalent Storybook assertions never hit this), so the assertion also matched that same row's Role-cell `MultiSelectDropdown` trigger button, whose own accessible name ("`<name>`'s roles") contains the plain name as a substring; fixed by adding `exact: true` to both.

### Judgment calls and scope notes

- `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` needed **no changes** for this phase: both already seed `ProjectGroup` rows with a `role` and `UserProjectRole` grants the new endpoints operate on directly, with no new field/table/enum this phase adds that either script would need to populate — confirmed by grep before concluding this rather than assuming it.
- The plan's "Storybook: updates to `ProjectAdminPage.stories.tsx` and `OrgAdminPage.stories.tsx`" was interpreted to include both *fixing* stories the restructuring broke (nav-label/DOM-shape changes) and *adding* new ones for the new behaviour — not just the latter; every pre-existing story that clicked into the old combined "Project groups" tab for content that moved to "Members" was found via grep and retargeted, not left to fail.

### Docs

This entry; `docs/solution-architecture.md` gained an "Implementation note (project membership/groups redesign, Phase 5)" paragraph in the Application Layer section (and its prior Phase 3 paragraph was corrected to say `PendingInvitesSection` now lives on the Members section, not "Project groups").

Files changed: `backend/app/schemas/project.py`, `backend/app/routers/projects.py`; `backend/tests/test_project_group_role_editing.py` (new); `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/components/MemberRoleTable.tsx` (new), `frontend/src/components/MemberRoleTable.stories.tsx` (new), `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts` (new), `tests/playwright/tests/e2e-workflows/org-admin-manage-users-modal.spec.ts` (new), `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`, `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`, `tests/playwright/tests/e2e-workflows/org-security-controls.spec.ts`, `tests/playwright/tests/e2e-workflows/role-display-collapsing.spec.ts`, `tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`, `tests/playwright/tests/e2e-workflows/helpers.ts`, `tests/project-users.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Follow-up UX batch, Phase A: org-level invites, and telling "new user" apart from "invite" in the UI

First of a follow-up five-phase batch (Phase 0 — the shared `DirectoryTable` component — landed first and separately; Phases B/C/D/E are separate, larger pieces of work tracked and implemented independently, by other agents). Prompted by two pieces of direct user feedback on the previous batch: (1) Org Admin's "New user" sets a password immediately, which read as surprising — "I expected a single-use code"; (2) a freshly-created user shows "Active" immediately, with no visible "invited/pending" state anywhere at the org level. Investigation confirmed both are real, distinct gaps rather than one: `create_org_user` (I-M-05: "users must have email, a display name and a password") and the token-link invite flow (`services/invites.py`, built in the prior batch's Phase 3) are two genuinely separate, both-legitimate provisioning paths — but the invite flow was, before this phase, only reachable from *inside a project* (a project's by-email "add member" flow); there was no org-level entry point into it at all, even though `services.invites.create_pending_invite` already supported an org-only invite (`project=None`). `is_active` correctly means "not deactivated," not "has completed onboarding" — but with no invited/pending state visible anywhere at the org level, an invited-but-unregistered person was simply invisible in Org Admin.

### Backend

Mirrors the project-level pending-invite pattern (Phase 3 above) almost exactly, scoped to `project_id IS NULL` rows:

- `GET /orgs/{organization_id}/pending-invites` (`routers/orgs.py::list_org_pending_invites`) — lists an organisation's unaccepted, **org-only** `PendingInvite` rows (`organization_id` matches, `project_id IS NULL`, `accepted_at IS NULL`), newest first, including already-expired ones. Project-scoped invites (created via a project's by-email flow) are never double-listed here — they stay owned by `list_pending_project_invites`. Gated by `require_org_admin_or_server_admin`, the same dependency `create_org_user` uses (the only other endpoint that creates these rows).
- `POST /orgs/{organization_id}/pending-invites` (`create_org_pending_invite`) — the actual new capability: a thin wrapper over `services.invites.create_pending_invite(db, email=..., organization=org, project=None, project_role=None, invited_by=current_user.id)`. Rejects with 400 for an `sso_only` organisation, mirroring `create_org_user`'s existing guard, and deliberately *not* falling back to `provision_sso_invite` the way `assign_project_role_by_email`'s sso_only branch does — that branch has a concrete project/role to provision ahead of a real SSO login; a bare org-only "invite" has no such target, and introducing a second, immediate-provision response shape just for this case was judged not worth the complexity for what's already reachable another way (configure the person's IdP group/role claim to match on their first real SSO login). Also 409s if an account with that email already exists anywhere (same check `create_org_user` makes), since an invite to an existing account would never be consumed (`consume_pending_invites` only ever fires from native signup or first OIDC login, neither of which an existing account goes through again).
- `POST /orgs/{organization_id}/pending-invites/{invite_id}/resend` (`resend_org_pending_invite`) — same token-rotation-and-resend behaviour as the project-level endpoint, via the same `services.invites.resend_pending_invite`, which already accepted `project: Project | None` and needed no signature change. Cross-org-boundary guard (`_get_org_only_pending_invite`, mirroring `_get_pending_invite_in_project`) 404s an invite belonging to a different organisation or a project-scoped invite passed against this org-level endpoint.
- Schema: `OrgPendingInviteOut` (`backend/app/schemas/org.py`) — `id`, `email`, `status: Literal["pending", "expired"]`, `created_at`, `expires_at`, and (new, not on the project-level `PendingInviteOut`) `invited_by_display_name`, since the Org Users table surfaces who sent each invite. `OrgPendingInviteCreate` — `email` only.
- `GET /orgs/{id}/users`'s `org_role` filter param (`backend/app/routers/orgs.py`) already existed and worked correctly server-side — confirmed by reading `list_org_users` directly (`if org_role is not None: results = [r for r in results if org_role in r.roles]`) and pinned with a new test (`test_list_org_users_filters_by_org_role`); it had simply never had a frontend control wired up to it. Not a bug to fix, just an unused capability to finally expose.
- SOC2: consulted `docs/soc2/policies/access-control-policy.md` before implementing, per this repo's CLAUDE.md requirement for anything touching invite/provisioning. Same reasoning as the Phase 3 entry above applies unchanged (org-only invite, same caller-typed address, same single grant, no new address reachable) — a short note was added to that entry's SOC2 review recording that this org-level addition was also reviewed and doesn't widen the existing unverified-email known gap.

### Frontend (`OrgAdminPage.tsx`)

- **"Invite user" button** next to "New user," opening its own small `Modal` (email only — no password/name fields, since the invitee sets those at signup). A hint line under both buttons spells out the difference (`newUserVsInviteHint`) directly answering the user's "I expected a single-use code" feedback.
- **Users table rebuilt on the shared `DirectoryTable`** (Phase 0), replacing the old hand-rolled `<table>`+`SortableHeader` markup, wrapped in the standard `.side-grid` + `FilterPanel` layout every other list page in this app already uses (`RequirementsPage.tsx` etc.) instead of a bare table with inline controls. `GET /orgs/{id}/pending-invites` is fetched alongside `GET /orgs/{id}/users` and merged client-side into one row list, `kind: "user" | "invited"` — the same discriminated-union pattern `MemberRoleTable`'s `kind: "user" | "group"` established. An invited row repurposes three column slots rather than adding new ones: **Name** shows "Invited by {name}" instead of a display name (none exists yet), **Last login** shows "Sent {date}" instead of a login date, and **Status** shows a Pending/Expired badge (`PENDING_INVITE_STATUS_LABEL`, never a raw string) plus a Resend button in place of the active/deactivated text. **Roles** and **2FA** go blank/em-dash (neither concept applies pre-signup), and **Actions** (View access/lock) is user-only. Invited rows aren't part of `users`' own server-side sort/pagination (they come from a separate, unpaginated fetch), so they're always rendered ahead of the sorted/paginated user rows rather than interleaved into that order — documented as a deliberate simplification, not an oversight.
- **Filters migrated into the shared `FilterPanel`**: the three existing ad-hoc toggle `<button>`s ("stale," "no 2FA," "no project access") were a single-select `"" | "stale" | "no2fa" | "noaccess"` state — mutually exclusive by construction, even though the three backend params (`stale_since_days`/`has_2fa`/`has_project_access`) were always independent. Migrated to three separate `FilterCheckbox`es (`userFilterStale`/`userFilterNo2fa`/`userFilterNoAccess`), each now genuinely independent — checking "stale" no longer silently clears "no 2FA." A new `FilterField` role filter (`<select>` over `OrgRole`, through `ORG_ROLE_LABEL`) wires up the previously-unused `org_role` param. A new "Show invited" `FilterCheckbox`, defaulting on, hides/shows the merged invited rows. The existing name/email search moved onto `FilterPanel`'s own `search`/`onSearchChange` header prop, replacing the separate `<input>` that used to sit above the table.
- **Clarity pass on "Active"**: wrapped in a `title` tooltip (`statusActiveHint`) spelling out "not deactivated, not necessarily logged in" — a one-line addition, not a schema change, directly answering the second piece of feedback.
- Label-text collision found and fixed while building the role filter: the New User modal's own "Role" field label and a bare "Role" `FilterField` label would both resolve to the same accessible name "Role" while the modal is open (it renders via a portal to `document.body`, so the underlying page's filter select stays mounted behind it) — an ambiguity that would only surface at test-authoring time, not runtime, but was still real. Fixed by using the already-defined-but-previously-unused `strings.orgAdmin.orgRole(orgCap)` ("Organisation role") for the filter's label instead of reusing `strings.orgAdmin.role` ("Role").

### Tests

Backend pytest (new file, `backend/tests/test_org_pending_invites.py`, mirroring `test_invites_and_external_users.py`'s project-level coverage): token rotation on resend (old token rejected at signup, new one redeemable), resend on an already-expired invite, resend rejects an already-accepted invite (409), org-only invite creation rejected for an `sso_only` org (400) and for an already-existing email (409), `GET /pending-invites` never includes project-scoped invites (checked both directions — an org-only invite doesn't leak into the project-level list either), 403 gating on all three new endpoints below `require_org_admin_or_server_admin`, and `test_list_org_users_filters_by_org_role` pinning the previously-untested-but-working `org_role` filter.

Storybook (`frontend/src/pages/OrgAdminPage.stories.tsx`): `mockOrgAdminApis` gained an `orgInvites` override, a `/pending-invites` branch in its `api.get` mock (also added to the separate `mockProjectsWorkflowWithOneProject` mock used by the "Manage users" modal stories, which replaces rather than composes with the shared mock), and its `/users` `getPage` mock now honours an `org_role` query param so a story can exercise the new filter end to end. New stories: `InviteUserOpensModalAndCreates`, `InviteUserModalCancel`, `UsersSectionShowsInvitedRowWithResend`, `UsersSectionHideInvitedFilter`, `UsersSectionRoleFilterNarrowsResults`, `UsersSectionMigratedFilterCheckboxesWorkIndependently`. One pre-existing story (`UsersSectionAndCreateUser`, plus its `LightTheme`/`DarkTheme` aliases) broke on this change — its `canvas.getByText("Org admin")` assertion became ambiguous once the new role filter's own "Org admin" `<option>` existed on the same page — fixed by scoping the assertion to the roles-dropdown trigger button's own text content instead of a page-wide text search.

Playwright (new file, `tests/playwright/tests/e2e-workflows/org-level-invite.spec.ts`, three tests, each creating its own fresh organisation the same way `external-project-invite.spec.ts` does): invite-then-signup confirms org access without project access; invite-then-resend confirms a rotated, different link via MailHog (split into a separate test from signup, mirroring the project-level spec's own two-test shape, rather than chaining signup after resend in one test — redeeming an already-resent invite's *original* link 400s, and MailHog's own message ordering isn't a documented "newest-last" contract, so picking "the latest link" to redeem after a resend would have been non-deterministic); filtering by organisation role narrows results. Two pre-existing specs needed updates for the migrated filter checkboxes and the new invited-row merge: `org-security-controls.spec.ts`'s filter-button clicks became checkbox clicks (uncheck instead of a since-removed "Clear filters" button); `user-access-panel.spec.ts`'s column-sort test (`visibleEmails()`, which asserts every visible row's first cell forms a sorted sequence) now unchecks "Show invited" first, since an invited row merged in ahead of the sorted user rows isn't part of that sort and would otherwise corrupt the assertion the moment any org it runs against ever has a pending invite.

Seed scripts: not extended to demonstrate this feature, matching the precedent already set by the project-level invite flow (Phase 3 above), which was also never added to `seed_demo_data.py`/`seed_e2e_dataset.py` — an invite is inherently transient/no-account-yet, not the kind of steady-state fixture those scripts otherwise model.

### Verification

Backend: full suite via the dev/test Docker Compose stack (backend rebuilt + force-recreated first) — **684 passed, 0 failed**, 72 pre-existing warnings (same count as the Phase 2/3 entries above, none new). `ruff check app tests` — all checks passed. (A first pass run from the host venv against `localhost` showed 14 failures, all `aiosmtplib.errors.SMTPConnectError: ... nodename nor servname provided` on the `mailhog` hostname — a host-vs-container DNS artifact, not a real regression: `mailhog` only resolves inside the compose network. Confirmed by rerunning the same 14 tests, then the full suite, via `docker compose exec backend python -m pytest` — 27/27 and then 684/684 passed.)

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier entry, none new, none in files touched by this work). `npm run test-storybook` (full suite) — **603/603 passed**, including the 6 new stories and the one fixed pre-existing one.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, `seed_e2e_dataset.py` re-run after): `org-level-invite.spec.ts` — 3/3 passed; `org-security-controls.spec.ts`, `user-access-panel.spec.ts`, `role-display-collapsing.spec.ts`, `project-hierarchy.spec.ts`, `external-project-invite.spec.ts`, `admin-page-titles-and-version-footer.spec.ts` (every spec found via grep to touch Org Admin's Users section or the project-level invite flow it mirrors) — all passed.

### Docs

This entry; `docs/soc2/policies/access-control-policy.md`'s Phase 3 SOC2 note extended with a short line covering this org-level addition; `docs/solution-architecture.md` gained an "Implementation note (org-level invites, follow-up UX batch Phase A)" paragraph right after the existing "resending a pending invite" note.

Files changed: `backend/app/schemas/org.py`, `backend/app/routers/orgs.py`; `backend/tests/test_org_pending_invites.py` (new); `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/org-level-invite.spec.ts` (new), `tests/playwright/tests/e2e-workflows/org-security-controls.spec.ts`, `tests/playwright/tests/e2e-workflows/user-access-panel.spec.ts`; `docs/soc2/policies/access-control-policy.md`, `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Follow-up UX batch, Phase B: Org Groups and Project Groups both rebuilt on `DirectoryTable`

Second phase of the same follow-up batch as Phase A above, independent of it (both build on Phase 0's shared `DirectoryTable`, neither depends on the other). Widened from the original "make Org Groups a table like Users" ask after Phase 0's own design-review pass found a second, adjacent gap: Project Admin's existing Groups tab (built in the prior batch's Phase 5) wasn't a real `<table>` either — a `<button style={{width:"100%"}}>` per row, no `SortableHeader`. Both "groups" directories moved onto `DirectoryTable` together in this phase rather than Org Groups alone.

### A real bug, found and fixed, not just a refactor

Reading `OrgAdminPage.tsx`'s old accordion body carefully (rather than assuming the SSO-sync fields were already gated, as the original plan draft did) found that only the `granted_org_role` `<select>` was actually conditioned on `ssoConfig?.oidc_issuer_url && ssoConfig?.oidc_client_id` — the `idp_synced_group_name` text `<input>` and the "Save sync settings" button rendered **unconditionally**, regardless of whether SSO was configured for the org at all. An org admin could type a sync name and click Save with no SSO ever set up; the backend would accept and persist it (`update_org_group` has no SSO-configured precondition of its own — only `_require_granted_role_has_sync_target`, which is unaffected either way), leaving a dormant `idp_synced_group_name` waiting for SSO to eventually be configured. Not a privilege-escalation bug (the role-granting field was correctly gated the whole time, so no role could actually be granted via this path without SSO), but a real UI-gating inconsistency the user asked to have fixed, not merely refactored around. Fixed by gating the entire sync sub-section — checkbox, name input, role select, Save action — behind the same `ssoConfigured` check; with SSO unconfigured, only the existing muted hint renders, nothing else.

On top of the fix, added the requested explicit toggle: a "Sync membership and role from an SSO/IdP group" checkbox inside the now-fully-gated section, checked by default only when `idp_synced_group_name` is already non-null, showing/hiding the name+role fields together. Unchecking clears both immediately via the existing `PATCH /orgs/{id}/groups/{id}` (`{idp_synced_group_name: null, granted_org_role: null}`) — no backend schema change, since both fields already existed and were already validated together. The field's label was renamed from "IdP-synced group name" to "SSO group name," matching this org's existing "Required SSO group" (`oidcRequiredGroup`) terminology rather than the more technical "IdP" term this codebase's own SSO copy doesn't otherwise use. The Save action previously had no success feedback at all (only the inline error state on failure) — added a "Sync settings updated" toast, closing that Principle 7 gap in the same pass since it's the same code path the toggle's own auto-clear now also calls.

### Frontend — Org Groups (`OrgAdminPage.tsx`)

Replaced the `CollapsibleSection`-of-`CollapsibleSection`s accordion (which rendered every group's full member list open and inline, unconditionally) with `DirectoryTable` (Name sortable, Members count not sortable) inside the standard `.side-grid` + `FilterPanel` layout, matching Phase A's Users table composition exactly — search via `FilterPanel`'s own header prop, no additional filter fields exist for Groups so its body is empty besides the search box. Each row opens a `SidePanel` (style guide "Pattern: entity detail panel") containing, relocated unchanged in logic: the member-users editor (`UserAutocomplete` add + remove), the nested-org-groups editor (`<select>` + remove), and the now-fully-gated SSO-sync sub-section described above. The old accordion rendering is deleted entirely — this table + panel is now the only way org groups are shown.

### Frontend — Project Groups retrofit (`ProjectAdminPage.tsx`)

Replaced the `<button>`-row list with `DirectoryTable`: Name (sortable), Role (badge via `PROJECT_ROLE_LABEL`, not sortable), Members (count, not sortable). The Name column's render function still embeds the `is_default` "Default" badge alongside the name (unchanged from the old list) — Phase C is what removes `is_default` and this badge entirely, out of scope here.

**`onRowClick`, not `rowHref` — a real ambiguity found during verification, not assumed either way.** The original plan preferred `rowHref` (a real, deep-linkable `<Link>`) if it composed naturally with the existing `?openGroup=`/`useSearchParams` handling, since a `<Link>` is strictly more capable than a click handler. It does compose mechanically, but a first implementation with `rowHref` broke two existing Playwright specs: every standard project seeds a default group literally named **"Members"** (`DEFAULT_GROUPS`, `routers/projects.py`) — the exact same accessible name as this page's own `ResourceMenu` "Members" nav link. With `rowHref`, both render as `role="link"` elements sharing one accessible name on the same page, which is a genuine ambiguity (the same confusion a screen-reader user navigating by link text would hit, not just a test-selector artifact) rather than a Playwright-only concern. Reverted to `onRowClick` (a `role="button"` trigger, which can't collide with a nav `<Link>`) — the existing manual `openGroupId`/`useSearchParams` `useEffect` (unchanged since Phase 5) still owns the `?openGroup=` deep link either way; `onRowClick` just calls the same `setOpenGroupId` a URL-driven open already used. This is exactly the plan's own documented fallback ("otherwise keep the existing manual handling and just call it from `onRowClick`"), landed on after finding the concrete reason to take it rather than assuming `rowHref` up front.

Existing search/`LoadMoreButton`/"+ New group" `Modal` behaviour is unchanged, just re-hosted on `DirectoryTable` inside the same `.side-grid` + `FilterPanel` shell.

### Backend — a small, deliberate addition beyond the original plan

Neither `list_org_groups` nor `list_project_groups` had a `sort`/`order` param — both always returned name-ascending. The original plan didn't call for adding one (it only used the absence of a sort param to justify *not* sorting the Members column). Implementing Name-column sort as a client-side re-sort of only the currently-loaded page would have reproduced exactly the anti-pattern `docs/ux-style-guide.md`'s own "Pattern: sortable column header" warns against: both lists already page via `limit`/`offset`, and "sorted by name, descending" has to mean sorted across every match, not just whatever page happened to be loaded first. Added a minimal `order: "asc" | "desc"` query param (default `asc`, unchanged behaviour) to both endpoints instead — a one-line `ORDER BY` direction flip each, no `sort` field param needed since Name is each list's only sortable column. `applyGroupSort` in both `OrgAdminPage.tsx`/`ProjectAdminPage.tsx` mirrors `applyUserSort`'s existing shape exactly: refetch with the new sort rather than re-sort in place.

### Docs

`docs/ux-style-guide.md`: "Pattern: directories at scale" and "Pattern: sortable column header" both updated — the first no longer describes Org/Project Groups' always-expanded-accordion problem as current (both are `DirectoryTable`+`SidePanel` now); the second no longer carves Groups out as "no column headers to make sortable," and its own paragraph also had a stale mention of Org Users' pre-Phase-A state that hadn't been updated when Phase A shipped — fixed in the same pass since it was the same paragraph. Added a new "Pattern: directory table" section documenting `DirectoryTable` itself (Phase 0 built it, no phase had written it up yet) — what it deliberately excludes and why, and which directories share it as of this phase (three: Org Users, Org Groups, Project Groups — Project Members and Server Admin's Access Review are later phases of the same batch, not yet landed as of this entry).

`docs/solution-architecture.md` gained an "Implementation note (Org/Project Groups rebuilt on `DirectoryTable`, follow-up UX batch Phase B)" paragraph, right after the Phase A one.

SOC2: consulted `docs/soc2/policies/access-control-policy.md` before starting, since the SSO-sync fix touches SSO-driven role granting. Concluded no policy update was needed — the actual authorization/validation logic (`require_org_role(ORG_ADMIN)` on the endpoint, `_require_granted_role_has_sync_target`'s server-side check) is completely unchanged; the bug and its fix are both frontend-only visibility, not a backend control. The policy's existing item 2/2a already documents the `granted_org_role`/`idp_synced_group_name` mechanism at the level that matters (backend enforcement), unaffected by this phase.

### Tests

Backend pytest: `backend/tests/test_pagination.py`'s existing `test_org_groups_search_and_paginate`/`test_project_groups_search_and_paginate` extended with `order=asc`/`order=desc` assertions for the new param.

Storybook (`OrgAdminPage.stories.tsx`): the Groups-section stories rewritten for the table+panel shape — `GroupsSectionOpensSidePanel` (new), `GroupsSectionSaveIdpSyncAndGrantedRole`, `GroupsSectionSyncToggleDefaultsCheckedWhenAlreadySynced` (new), `GroupsSectionToggleSyncOffClearsFields` (new), `GroupsSectionSsoSyncSectionHiddenWithoutSso` (renamed/widened from `GroupsSectionGrantedRoleHiddenWithoutSso` — now pins the *entire* sub-section absent, including the name input and Save button, not just the role select, per the regression this phase fixes), `GroupsSectionShowsMembers`, `GroupsSectionNestGroup`. `mockOrgAdminApis` gained a `groups` override so a story can exercise an already-synced group.

Storybook (`ProjectAdminPage.stories.tsx`): the existing Groups-tab stories (`GroupsTabOpensSidePanelWithRoleSelectAndDelete`, `GroupsTabAddMemberViaSidePanel`, `GroupsTabAddOrgGroupViaSidePanel`, `GroupsTabDeleteDefaultGroupDisabled`, `GroupsTabDeleteGroupRequiresConfirmation`) updated for the new `DirectoryTable` row shape (role/member-count column assertions added), row-trigger role unchanged (`button`, per the `onRowClick`-not-`rowHref` decision above) so most of these needed only a light touch.

Playwright: `org-group-nesting.spec.ts` rewritten for the new table+panel shape (`helpers.ts`'s old `openGroupCard`, built for the retired accordion, replaced by `openOrgGroupPanel`, mirroring the existing `openProjectGroupPanel`). New file `org-groups-sso-sync.spec.ts` — the regression-pin test the plan specifically asked for: gating absent-without-SSO (name field, role select, toggle, and Save button all genuinely absent, not just the role select), then the toggle's on/off round trip against a real SSO-configured org, verified both through the UI and by re-fetching the group via the API. Runs against a dedicated, freshly created throwaway org/admin (same pattern `org-rename-and-test-email.spec.ts` already uses) rather than a shared seeded persona, since it mutates `PUT /orgs/{id}/sso-config` — doing that against a persona shared with other specs in this suite's single-worker run would leak SSO state into specs that don't expect it. Two real issues found while first running this new spec, both fixed: (1) the org-scoped `PUT /sso-config` call needs the *newly created org admin's own* token, not the platform admin's — `require_org_role(ORG_ADMIN)` deliberately does not let a server admin bypass it (I-M-05); (2) the spec must close a `SidePanel` before reopening it in a later step, since its backdrop (`role="presentation"`) otherwise intercepts the next click on the underlying row. `project-admin-groups-and-fields.spec.ts` and `project-admin-members.spec.ts` (both already covering Project Groups' row-open flow) needed no shape changes since the row stayed a `<button>` — confirmed by running them, not assumed. `docs/e2e-workflows.md` not updated — like `org-rename-and-test-email.spec.ts`, the new spec uses a dedicated throwaway org rather than the fixed persona/project catalogue that doc indexes.

### Verification

Backend: full suite via the dev/test Docker Compose stack (`tests/container`, backend rebuilt + force-recreated first) — **684 passed, 0 failed**, 72 pre-existing warnings (same count as every earlier entry, none new). `ruff check app tests` — all checks passed.

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier entry, none new, none in files touched by this work). `npm run test-storybook` (full suite) — **606/606 passed**.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, `seed_e2e_dataset.py` re-run after the backend pytest run truncated it): `org-group-nesting.spec.ts`, `org-groups-sso-sync.spec.ts` (new), `project-admin-groups-and-fields.spec.ts`, `project-admin-members.spec.ts` (covers the `?openGroup=` deep-link round trip specifically), `org-security-controls.spec.ts`, `role-display-collapsing.spec.ts`, `project-hierarchy.spec.ts`, `external-project-invite.spec.ts`, `scim-provisioning.spec.ts`, `org-level-invite.spec.ts`, `user-access-panel.spec.ts` (every spec found via grep to touch either Groups section, or `openProjectGroupPanel`/`openGroupCard`) — all passed.

### Judgment calls and deviations from the plan

- **`onRowClick` instead of `rowHref` for Project Groups** — see "Frontend — Project Groups retrofit" above. The plan explicitly anticipated this exact fallback if `rowHref` didn't compose cleanly; it didn't, for a concrete, verified reason (the "Members" default-group-name/nav-link collision), not a preference.
- **Added `order` params to `list_org_groups`/`list_project_groups`**, beyond what the plan's Phase B text specified, to keep Name-column sort backend-driven rather than client-side-only — see "Backend" above for the reasoning; this follows the style guide's own existing decision rule rather than introducing a new one.
- **Fixed a stale sentence in `docs/ux-style-guide.md`'s "Pattern: directories at scale"** left over from Phase A (it still described Org Users' pre-Phase-A state as current even though Phase A had already fixed it) while touching the same paragraph for Groups — per this repo's standing "fix issues found, don't defer" rule.
- **Added a success toast to `saveIdpSync`** (previously silent on success) while fixing the gating bug in the same function, closing a small pre-existing Principle 7 gap rather than leaving it once the toggle's own auto-clear path started calling the same function.

### Files changed

`backend/app/routers/orgs.py`, `backend/app/routers/projects.py`; `backend/tests/test_pagination.py`; `frontend/src/i18n/strings.ts`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`, `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/helpers.ts`, `tests/playwright/tests/e2e-workflows/org-group-nesting.spec.ts`, `tests/playwright/tests/e2e-workflows/org-groups-sso-sync.spec.ts` (new), `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts`; `docs/ux-style-guide.md`, `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Follow-up UX batch, Phase E: Server Admin's Access Review moved onto `DirectoryTable`/`FilterPanel`, matching Org Admin's Users table

Fifth phase of the same follow-up batch as Phases A/B/C/D above, independent of all of them (builds only on Phase 0's shared `DirectoryTable`). Server Admin's "Access Review" tab (`ServerManagementPage.tsx`'s `AccessReviewTab`) already had real filters (a `view` dropdown for orphaned/server-admins/all, an `includeDeactivated` checkbox) and backend-paginated `LoadMoreButton`, but — confirmed by reading both the frontend and `list_system_users` directly — no free-text search, no sortable columns (a plain static `<table>`/`<th>`, not `SortableHeader`-based like Org Admin's Users table), and its `view`/`includeDeactivated` filters were plain inline controls, not `FilterPanel`.

### Backend (`backend/app/routers/system.py`)

- **`search: str | None = None`** on `list_system_users` — name/email substring, case-insensitive, applied as the exact same Python-side `needle in x.display_name.lower() or needle in x.email.lower()` check `list_org_users` (`backend/app/routers/orgs.py`) already uses, reused verbatim rather than reinvented as a SQL `ilike`, per the plan's own instruction.
- **`sort`/`order` — a deliberate addition beyond the plan's literal text, following the same reasoning Phase B already established for `list_org_groups`/`list_project_groups`'s `order` param.** The plan's Phase E section named only `search` as a backend change, but also asked for "Email/Name/Last login/Created sortable" columns on a table that's backend-paginated (`limit`/`offset`, `LoadMoreButton`). Without a backend sort, a `DirectoryTable` "sortable" column can only mean a client-side sort of whatever page happens to be currently loaded — exactly the anti-pattern `docs/ux-style-guide.md`'s "Pattern: sortable column header" warns against, and the same one Phase B's own entry above already declined to reproduce for Org/Project Groups. Added `sort: str | None` (`display_name|email|last_login_at|created_at`, `created_at` being the one addition beyond `list_org_users`'s own three sortable fields — Org Users has no "Created" column) and `order: "asc"|"desc"`, mirroring `list_org_users`'s contract exactly, including its null-handling for `last_login_at` (never-logged-in accounts always sort last, regardless of direction).
- `search` and `sort`/`order` both apply to the already-materialized `list(db.scalars(query).all())` before the `X-Total-Count` header is computed and before `limit`/`offset` slicing — same ordering as the existing filters, so total counts and pagination stay correct against the narrowed/sorted set.

### Frontend (`ServerManagementPage.tsx`)

`AccessReviewTab` wrapped in the same `.side-grid` + `FilterPanel` layout as Org Admin's Users table (the direct composition template): `view` became a `FilterField` `<select>` (identical options/labels), `includeDeactivated` became a `FilterCheckbox` (identical label, so existing `getByLabel("Include deactivated accounts")` call sites needed no change), and the new search box uses `FilterPanel`'s own `search`/`onSearchChange` header prop. The table itself moved onto `DirectoryTable`: Email/Name/Last login/Created sortable (backed by the new `sort`/`order` params), Organizations/Groups not sortable (comma-joined names, no natural order — style guide "Pattern: sortable column header"), Actions unchanged (deactivate/ban/grant-or-revoke-server-admin via the existing `ConfirmDialog`s). No `onRowClick`/`rowHref` — this directory has no per-row detail panel, so both props are simply omitted, per the plan.

**A real bug found and fixed during Storybook verification, not assumed away:** the old implementation's `reload()` called `setUsers(null)` before every refetch (filter change, search keystroke, sort click, or post-action reload), which — when the search/filter controls lived *outside* the `!users`-gated block — was harmless (a brief `<Spinner>` swap, nothing the user was actively interacting with disappeared). Migrating the search box *inside* the same `.side-grid`/`FilterPanel` this list renders changed that: nulling `users` on the first keystroke unmounted the whole `FilterPanel` (search input included) in favour of the `<Spinner>`, dropping focus and losing every keystroke after the first. The new `AccessReviewFilterPanelSearchAndFilters` Storybook story caught this directly — `userEvent.type`ing "orphan" into the search box only ever produced a `getPage` call for `search=o`, since the input vanished after the first character. Fixed by removing the `setUsers(null)` reset from `reload()` entirely (rows now simply stay as-is, stale, until the new page resolves) — the same behaviour Org Admin's own `loadUsers` already has (its `users` state is never nullable in the first place). `!users` is now only ever true on the very first, pre-any-fetch mount, exactly matching Org Admin's `Spinner`-then-populate shape.

### Tests

Backend pytest (`backend/tests/test_access_review.py`): `test_system_users_search_matches_name_or_email_case_insensitively` (name and email substring, case-insensitive, mirroring `test_pagination.py`'s existing `test_org_users_search_and_paginate`), `test_system_users_search_combines_with_view_and_active_filters` (search ANDs with, doesn't override, `no_org_membership`/`is_active` — the params `view`/`includeDeactivated` map to), `test_system_users_sort_by_email_and_created_at` (pins the new `sort`/`order` params, ascending and descending).

Storybook (`ServerManagementPage.stories.tsx`): `AccessReviewFilterPanelSearchAndFilters` (pins the migrated `view`/`includeDeactivated` controls keep their accessible names, and the new search box narrows the request via `search=`), `AccessReviewSortByEmail` (clicking the Email header cycles `sort=email&order=asc` → `sort=email&order=desc`). All pre-existing `AccessReview*` stories (`AccessReviewOrphanedAccounts`, `AccessReviewShowsGroups`, `AccessReviewBannedAndAdminBadges`, `AccessReviewGrantServerAdmin`, `AccessReviewDeactivateCancelled`, `AccessReviewLoadMoreAppendsTheNextPage`) needed no changes — the Actions column's buttons and their accessible names are unchanged, just re-hosted inside `DirectoryTable`.

Playwright (`tests/playwright/tests/e2e-workflows/user-directory-and-bans.spec.ts`): three new `test.step`s added to the existing spec — search narrows the list by both a name and an email fragment (and an empty search restores the full list), the Email column cycles through `aria-sort` `none → ascending → descending → none` via its `SortableHeader` button, and the migrated "Show"/"Include deactivated" controls still narrow results identically to before. Inserted before the spec's existing deactivate/ban steps, ending back at the default "orphaned" view with an empty search so those unchanged steps still find the orphan persona's row.

### Docs

This entry; `docs/solution-architecture.md` gained an "Implementation note (Server Admin's Access Review moved onto `DirectoryTable`/`FilterPanel`, follow-up UX batch Phase E)" paragraph after the Phase B one.

### Verification

Backend: full suite via the dev/test Docker Compose stack (`tests/container`, backend rebuilt + force-recreated first) — **687 passed, 0 failed**, 72 pre-existing warnings (same count as every earlier entry in this batch, none new).

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier entry, none new, none in files touched by this work). `npm run test-storybook` (full suite) — **608/608 passed**, including the 2 new `AccessReview*` stories (the search-race bug above was caught and fixed during this run, not after).

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, `seed_demo_data.py`/`seed_e2e_dataset.py` re-run after the backend pytest run truncated the DB): `user-directory-and-bans.spec.ts` (the spec this phase extended) — passed, including the three new `test.step`s; `org-rename-and-test-email.spec.ts`, `org-label.spec.ts` (both found via grep to navigate through `/server/management`, Access Review's own route, before exercising other tabs) — all passed, confirming the page still mounts and hands off correctly with the rebuilt tab.

### Judgment calls and deviations from the plan

- **Added `sort`/`order` to `list_system_users`**, beyond what the plan's Phase E text specified (only `search`) — see "Backend" above. Necessary for the "sortable" columns the plan's own frontend section asks for to be backend-driven rather than a misleading client-side sort of only the loaded page, following the precedent Phase B already set for `list_org_groups`/`list_project_groups`.
- **Removed `reload()`'s `setUsers(null)` reset** — not something the plan called out, but a genuine regression the new `FilterPanel`+search composition would have introduced (search box unmounting mid-keystroke); found via the Storybook story the plan itself asked for, fixed rather than left in place per this repo's standing "fix issues found, don't defer" rule.

### Files changed

`backend/app/routers/system.py`; `backend/tests/test_access_review.py`; `frontend/src/i18n/strings.ts`, `frontend/src/pages/ServerManagementPage.tsx`, `frontend/src/pages/ServerManagementPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/user-directory-and-bans.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Follow-up UX batch, Phase C: default project groups removed; migrated to direct grants

Third phase of the same follow-up batch (Phases A/B/E above), and the highest-risk one — a real data migration affecting every existing project's manager-of-record, so this entry documents the identify→verify→remediate security review performed alongside the change itself, per the change-management policy's practice for RBAC-adjacent work.

**Why default groups existed, and why they're being removed now.** `create_project`'s non-template path (`backend/app/routers/projects.py`) used to auto-create four "standard" `ProjectGroup` rows on every project (`is_default=True` — "Project Managers"/"Project Administrators"/"Stakeholders"/"Members", `DEFAULT_GROUPS`) and grant the creator's initial `PROJECT_MANAGER` role by adding them to the manager-role one — the only way a fresh project ever got its first manager. Those four groups could never be deleted (`delete_project_group` special-cased `is_default`) and existed on every project whether wanted or not, even though a direct `UserProjectRole` grant was always available as a simpler, equally-supported mechanism for a single person, and Project Admin's own Groups tab already lets an admin create exactly this shape of group by hand when they actually want one. The user's ask that started this batch — "Members should just be effective members; the four default groups are redundant" — is what surfaces this cleanly: with pending invites now folding into the Members table (Phase D, dependent on this phase) and direct-grant-with-inline-role-picker already a first-class flow, the four auto-created groups add structural noise (and undeletable rows) without giving anything a direct grant doesn't.

### Backend — `create_project`'s non-template path (`backend/app/routers/projects.py`)

Stops creating `DEFAULT_GROUPS` entirely (the constant itself is deleted). The creator's initial `PROJECT_MANAGER` role is granted via the exact same fallback the template-clone branch already used when a cloned project ended up with no manager — rather than duplicate that logic, both branches now call a single new shared helper, `_ensure_project_has_a_manager(db, project, fallback_user_id)`: if `get_effective_project_managers` already finds one, it's a no-op; otherwise it prefers adding the fallback user to an existing manager-role `ProjectGroup` if one exists (only reachable via a template clone that copied a custom, user-created manager-role group — a from-scratch project has zero groups at this point, so this branch is structurally unreachable there), falling through to a direct `UserProjectRole` grant otherwise. The template-clone path itself is otherwise unaffected — it still clones whatever groups a template actually has, all non-default, user-created ones.

### Data migration (`backend/alembic/versions/0019_remove_default_project_groups.py`)

For every existing `is_default=True` `ProjectGroup`: every direct user member (`project_group_members.user_id IS NOT NULL`) is materialized into a direct `UserProjectRole(user_id, project_id, role)` grant, idempotently (`ON CONFLICT (user_id, project_id, role) DO NOTHING` against the existing unique constraint — a user who already independently held the same direct role ends up with exactly one row, not a duplicate). Then: if the group has *only* plain direct members (no nested org-group ref, no cross-project member-source ref), it's deleted outright (`project_group_members` rows cascade via the existing `ON DELETE CASCADE` FK); if it has composition beyond plain direct members — confirmed this happens for real, not just hypothetically: `seed_demo_data.py` attaches a `source_project_id` reference to the default "Stakeholders" group — it's *not* deleted, only demoted (`is_default = false`), so it survives as an ordinary, fully-manageable custom group with its nested composition intact. Written as a single `DO $$ ... $$` block (`alembic`'s established style in this repo for data-shape migrations, see 0012/0014's precedent), guarded by an `information_schema.columns` existence check the same way 0014/0017/0018 guard their own backfills — a brand-new database's `0001_initial.py` runs `Base.metadata.create_all()` against the *current* models, which no longer define `is_default` at all, so the whole backfill is a no-op there; only a database migrated from before this revision has real rows to convert. `ProjectGroup.is_default` is dropped from the column set at the end of the same migration.

C-U-08 ("every project must retain at least one manager") is preserved exactly by construction: every direct member of a manager-role default group ends up holding that same role directly before the group is ever touched, and a group is only ever deleted (never silently emptied) once every one of its direct members has already been carried forward; a group whose manager contribution comes from a nested org-group or cross-project reference is demoted, not deleted, so that contribution is completely unaffected. Pinned by `backend/tests/test_default_group_migration.py`, which runs the *real* migration (not a reimplementation of its logic) — downgrades the live test database to revision 0018, seeds pre-migration-shaped rows directly via raw SQL (project/user setup goes through the normal API; only the `is_default` group rows need raw SQL, since that column no longer exists on the ORM model to insert through at all), upgrades back to head, and asserts against the result via the ordinary API — covering both the plain-group (materialize-then-delete) and extra-composition (materialize-then-demote, manager coverage preserved) cases.

### Cleanup

- **`delete_project_group`**'s `is_default`-based delete rejection is removed entirely; its docstring (previously "the four standard groups... cannot be deleted") now describes the current, single guard — C-U-08, which already applied to every group regardless of `is_default` and needed no change of its own.
- **`is_default` dropped everywhere**, not just the DB column: `ProjectGroup` model, `ProjectGroupOut` schema (`ProjectGroupCreate`/`ProjectGroupUpdate` never had it), `project_export.py`'s bundle format (stops writing `"is_default"` into exported group JSON; import tolerates but ignores an old bundle's `"is_default"` key, same "retired field, silently ignored on import" treatment any other dropped field gets), and the frontend: `ProjectAdminPage.tsx`'s "Default" badge on the Groups tab's Name column and the disabled-delete-button-with-explanatory-title branch on the group `SidePanel`'s Delete button — both removed, confirmed non-trivial as the design review flagged (not already clean). `frontend/src/api/types.ts`'s `ProjectGroup` interface loses the field; every Storybook fixture/story literal constructing a `ProjectGroup` across `ProjectAdminPage.stories.tsx`/`OrgAdminPage.stories.tsx` updated to match. `MemberRoleTable.tsx`'s own `MemberRoleGroupRow.isDefault` field is untouched here (confirmed genuinely unused beyond being stored) — it disappears with that component's own retirement in Phase D, not this phase; the two call sites that used to populate it from `g.is_default` (`ProjectAdminPage.tsx`, `OrgAdminPage.tsx`'s "Manage users" modal) now pass a literal `false`, since no group is ever default again.
- **`clone_project` gap, found in design review before this phase was finalized** (`backend/app/services/templates.py`): it previously only copied `ProjectGroup`/`ProjectGroupMember` rows from a template, never `UserProjectRole` rows — harmless while every grant went through a group, but a real, silent regression once direct grants become the *normal* mechanism (this phase): a template's directly-granted managers/admins/stakeholders/members would vanish on clone for everyone except whoever happened to be doing the cloning (covered by the C-U-08 fallback, but nobody else the template creator intended). Fixed: `clone_project` now also copies the source project's `UserProjectRole` rows verbatim. **A second, smaller gap found while making that fix, not in the original plan**: the existing `ProjectGroupMember` copy loop already only carried `user_id`/`org_group_id` across, silently dropping any `source_project_id` ("this group's members = that project's own direct members") reference a template group had — fixed in the same pass, copying all three member-target kinds now, per this repo's "fix issues found, don't defer" rule. `source_project_id` itself is copied verbatim (pointing at the original referenced project, never remapped to the new project even if it happens to reference the template being cloned) — a deliberate, load-bearing choice, not an oversight.
- **`backend/scripts/seed_demo_data.py`**: since projects no longer auto-create a "Stakeholders" group, the script now explicitly creates a plain (non-default) group of that name via a new `create_project_group` helper before attaching the `source_project_id` reference to it — same end demo state (Solstice Cloud Platform's "Stakeholders" group still resolves to Falcon-3's own direct members), just an explicit creation step. `get_project_group_id` (name-lookup-by-default-group helper) is deleted along with it.
- **Tests updated across the backend suite** (grepped for `is_default`/`DEFAULT_GROUPS`/"Project Managers" across `backend/tests/`, not assumed to be a short list): `test_rbac.py`, `test_project_group_role_editing.py` (rewritten most heavily — its `_default_manager_group` helper no longer has anything to find, replaced with `_make_group_the_sole_manager`, which grants a *second* person the role via a brand-new group then revokes the creator's own direct grant, since `revoke_project_role`'s own C-U-08 guard only allows removing a direct grant when the grantee isn't left as the *sole* effective manager — a fresh project's creator always starts as exactly that, so a real second manager source has to exist first), `test_project_hierarchy.py`, `test_audit_fixes.py`, `test_security_hardening.py`, `test_coverage_gaps.py`, `test_pagination.py`, `test_templates.py` — every one of these previously reached a project's post-creation groups list assuming at least a manager-role group would be there; each now either creates the group it needs explicitly or asserts against the creator's new direct grant instead.

### Tests

Migration test (`test_default_group_migration.py`, described above). Pytest: `test_project_creator_becomes_project_manager` (a freshly created project's creator has a direct `UserProjectRole(PROJECT_MANAGER)` row and zero `ProjectGroup` rows); `test_group_response_has_no_is_default_field_and_delete_never_special_cases_it` (`delete_project_group` no longer special-cases `is_default` — its only remaining guard is C-U-08 — and the field is genuinely gone from the response body, not just unused); `test_cloning_a_template_preserves_a_directly_granted_non_manager_role` and `test_cloning_a_template_with_zero_managers_still_produces_a_valid_manager` (the two review-found `clone_project` gaps — the latter reaches the zero-manager state via a direct DB delete of the template creator's `UserProjectRole` row, since `revoke_project_role`'s own C-U-08 guard makes that state genuinely unreachable through the live API once a project has exactly one manager, the same reason `test_project_group_role_editing.py`'s helper needed a second manager source above).

### Security review (identify → verify → remediate, per the change-management policy's practice for RBAC-adjacent changes)

Consulted `docs/soc2/policies/access-control-policy.md` and `change-management-and-secure-development-policy.md` before starting, since this touches authorization scaffolding directly (how a project's first manager grant is expressed, a data migration converting group membership into direct grants at scale, and a relaxed delete guard).

**Identified** five candidate risks, each independently verified against the actual code/behaviour rather than reasoned about in the abstract:

1. *Does the migration ever silently drop a manager?* No — verified by reading the migration's own logic and by `test_default_group_migration.py::test_plain_default_group_materializes_members_then_is_deleted` and `test_default_group_with_extra_composition_materializes_members_and_survives_demoted`, which run the *real* SQL (not a reimplementation) against seeded pre-migration-shaped data and assert the exact same people remain managers afterward, now via direct grants. Every direct member is materialized *before* a group is ever deleted; a group is only deleted once fully materialized, never while it still carries composition (nested/cross-project) the migration can't reproduce as a direct grant.
2. *Does removing `is_default`'s delete-rejection weaken any real protection?* No — verified that `is_default` was a purely structural "this specific group can never be deleted" rule with no relationship to actual access; the substantive protection (C-U-08, "a project must always retain at least one manager") is unconditional on `is_default` today and was already the *only* guard `delete_project_group` needed for the manager-role case specifically — removing the redundant structural check doesn't touch it, confirmed by the full existing `test_delete_custom_manager_group_blocked_when_sole_manager`/`test_delete_custom_manager_group_allowed_with_backup_manager` coverage passing unchanged.
3. *Does `clone_project` now copying `UserProjectRole` rows cross any tenant boundary?* No — `clone_project` only ever runs within `create_project`'s existing same-organization validation (the template and the new project share `organization_id` by construction), so copying direct grants moves access between two projects in the *same* organization, to users who already held that exact role on the template — no new person gains access, no cross-org exposure.
4. *Does the now-copied `source_project_id` on `ProjectGroupMember` (the second, smaller `clone_project` fix above) introduce any new cross-project reference risk?* No — `source_project_id` was already a same-organization-only, receiving-side-authorized mechanism before this fix (see `ProjectGroupMember`'s own docstring and the "generalized cross-project permissions" review this repo already performed on it); copying it verbatim during a clone doesn't create a new reference, it just stops silently dropping an existing one the template's manager had already explicitly set up and consented to.
5. *Does the migration's idempotent `ON CONFLICT DO NOTHING` insert ever mask a real error rather than a genuine already-exists case?* No — the only way a conflicting row can exist is a user who already independently held the identical `(user_id, project_id, role)` direct grant, which is exactly the case where inserting a duplicate would be wrong; verified by reading the unique constraint definition and by the migration test's own assertions.

**Verified**: every finding above was checked against the real, current code (`rbac.py`'s `_direct_project_managers`/`get_effective_project_managers`, `revoke_project_role`/`delete_project_group`'s guards, `templates.py::clone_project`, the migration's actual SQL) and against a live run of the migration itself, not taken on description alone. The full backend pytest suite (690 tests) was run twice against the real dev/test Docker Compose stack to confirm no regression and rule out a one-off flake — see Verification below.

**Remediated**: no new gap was found requiring a user decision or a policy update — this phase *removes* a structural restriction (`is_default`) that had no independent security value beyond what C-U-08 already provided, and *adds* a fallback path (`_ensure_project_has_a_manager`) that is strictly the same guarantee the template-clone branch already gave, now shared rather than duplicated. `docs/soc2/policies/access-control-policy.md` was read in full and needed no update: nothing in its Known Gaps list references default project groups or this creation-time mechanism, and the actual authorization surface it documents (project role resolution, C-U-08's manager-retention guarantee) is unchanged in substance, only in which underlying rows express it.

### Two small incidental fixes, found while verifying this phase's own Playwright coverage

**A real, pre-existing accessibility bug: Escape inside a nested `Popover` closed its enclosing `Modal`/`SidePanel` too.** Found while writing the "Manage users" modal's own Playwright coverage above — opening a row's role `MultiSelectDropdown` (a `Popover`) *inside* the "Manage users" `Modal` and pressing Escape closed both, not just the popover. Root cause: `Modal`/`SidePanel`/`Popover` all share `useDialogA11y` (`frontend/src/components/dialogA11y.ts`), and each independently registers its own `window` "keydown" listener with no coordination between instances — `addEventListener` has no built-in "only the topmost dialog" concept, so a single Escape press fired every currently-mounted dialog's `onClose` at once. This bug pre-dates this phase (`OrgAdminPage.tsx`'s "Manage users" modal has wrapped `MemberRoleTable`, itself already using `MultiSelectDropdown`/`Popover` for user rows, since Phase 5) — it was simply never exercised by any existing spec until this phase's new test tried to close a row's dropdown from inside that modal. Fixed with a containment check already implicit in the same function's Tab-trap logic just below it: Escape now only closes a dialog whose own `dialogRef` currently contains `document.activeElement` — since `Modal`/`SidePanel`/`Popover` are each portalled to `document.body` as independent DOM subtrees (never actually nested in the DOM even when opened "inside" one another logically), and each already moves focus into itself on open, this correctly identifies the innermost/topmost currently-open dialog as the only valid target, with no new state or dialog stack needed. Verified safe for every other Escape-to-close flow in the app: the full Storybook suite (607/607, exercising `Modal`/`SidePanel`/`Popover` across many isolated scenarios) and the full set of this phase's own Playwright specs were re-run after the fix and stayed green — see Verification below.

**Enum/derived-string rendering gap, flagged directly by the user mid-pass:** `ProjectAdminPage.tsx`'s Terminology section (Overview tab) rendered each configurable term's row label by deriving it from the raw key via CSS `text-transform: capitalize` + `.replace("_", " ")` (`change_request` → "Change request") instead of a real label map — exactly the pattern this repo's Principle 12 forbids, with a sibling role/status value two lines away already doing this correctly. Fixed: added `strings.admin.terminologyKeyLabel` (`frontend/src/i18n/strings.ts`), a plain literal label map for the six `TERMINOLOGY_KEYS` ("Project", "Stage", "Component", "Category", "Requirement", "Change Request") — deliberately *not* run through the `{project}`-style terminology-substitution system, since these rows are the canonical default term names being configured, not yet-possibly-overridden usage elsewhere in the UI (substituting them through the override they themselves name would be circular). `ProjectAdminPage.tsx` now reads from it instead of the CSS/replace hack.

### Docs

This entry; `docs/solution-architecture.md` gained an "Implementation note (default project groups removed; migrated to direct grants, follow-up UX batch Phase C)" paragraph, and the pre-existing Phase 5 note's description of `DELETE /projects/{id}/groups/{group_id}` was corrected in place (it described the now-removed `is_default` 400 as current behaviour, with a forward pointer added to the new Phase C note instead of silently leaving the stale sentence).

### Verification

Backend: full suite via the dev/test Docker Compose stack (`tests/container`, backend rebuilt + force-recreated first), run **twice** in full given this migration's blast radius — first run: 689 passed, 1 failed (`test_signup.py::test_signup_rejects_duplicate_email`, an unrelated code path — signup/`ServerSettings.signup_mode`, nothing this phase touches); re-ran that single test in isolation and it passed; re-ran the entire suite a second time in full (rather than assume a one-off flake) — **690 passed, 0 failed**, 72 pre-existing warnings (same count as every earlier entry in this batch, none new), confirming the first run's failure was a genuine, non-reproducing flake unrelated to this phase's changes. `ruff check app tests` — all checks passed.

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier entry, none new, none in files touched by this work). `npm run test-storybook` (full suite) run **twice** — once before, once after the `dialogA11y.ts` fix above — **607/607 passed both times**, including the updated `GroupsTabOpensSidePanelWithRoleSelectAndDelete`/`GroupsTabDeleteGroupRequiresConfirmation` stories and confirming `GroupsTabDeleteDefaultGroupDisabled` (retired — its premise no longer exists) is gone.

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, `seed_e2e_dataset.py`/`seed_demo_data.py` re-run after the backend pytest runs truncated the DB — both completed cleanly end to end, confirming the `seed_demo_data.py` change above works in practice, and a direct API check confirmed Solstice Cloud Platform's explicitly-created "Stakeholders" group correctly carries its `source_project_id` reference with no `is_default` field in the response): every spec touched by this phase, plus every spec found via grep to interact with project-group creation/membership near a fresh `create_project` call — `external-project-invite.spec.ts`, `org-admin-manage-users-modal.spec.ts`, `org-security-controls.spec.ts`, `project-admin-groups-and-fields.spec.ts`, `project-admin-members.spec.ts`, `project-hierarchy.spec.ts` (all 4 tests), `org-group-nesting.spec.ts`, `role-display-collapsing.spec.ts` — **13/13 passed** on the final run (two real issues found and fixed during this sweep, both described below).

### Judgment calls and deviations from the plan

- **Both `clone_project` gaps were fixed together** — the plan's own review only named the `UserProjectRole`-copy gap; the `source_project_id`-member-copy gap was found while reading the same function to fix the first one, and fixed in the same pass per this repo's "fix issues found, don't defer" rule rather than filed separately.
- **`test_project_group_role_editing.py`'s `_default_manager_group` helper became `_make_group_the_sole_manager`, not a simple rename** — a fresh project's creator now always starts as the sole *direct* manager (not group-derived), and `revoke_project_role`'s own C-U-08 guard refuses to remove a direct grant that would leave its holder as the sole effective manager (even though they'd remain one via a group they're also in) — so every test that needs "a group is the project's only manager source" has to first establish a *second* manager (a different person, via the new group) before revoking the creator's own direct grant. This is a real behavioural consequence of the phase, not a test-only inconvenience: the same asymmetry now exists for real project managers reorganizing their own project's structure.
- **Chose `_ensure_project_has_a_manager` as a shared helper rather than inlining the fallback in the non-template branch** — the plan explicitly asked for "reuse that same code path/helper," and a from-scratch project's own call into it always takes the direct-grant branch in practice (zero groups exist at that point), but sharing the one function keeps that guaranteed by construction rather than by two independently-maintained copies staying in sync.
- **Two real Playwright locator/behaviour bugs found and fixed while writing this phase's own new coverage, not scoped in the plan**: (1) `PROJECT_ROLE_LABEL.project_manager` renders as "Project manager" (lowercase "m"), not "Project Manager" — two new specs' Escape-guard assertions initially used the wrong casing in their `getByRole("checkbox", { name: /Revoke Project Manager from .../ })` locators and found nothing; fixed to match the real label. (2) The `dialogA11y.ts` Escape-nesting bug described above, found by the same new coverage, fixed at the shared-component level rather than worked around per-spec.

### Files changed

`backend/app/models/project.py`, `backend/app/routers/projects.py`, `backend/app/schemas/project.py`, `backend/app/services/project_export.py`, `backend/app/services/templates.py`, `backend/alembic/versions/0019_remove_default_project_groups.py` (new); `backend/scripts/seed_demo_data.py`; `backend/tests/test_default_group_migration.py` (new), `backend/tests/test_rbac.py`, `backend/tests/test_project_group_role_editing.py`, `backend/tests/test_project_hierarchy.py`, `backend/tests/test_audit_fixes.py`, `backend/tests/test_security_hardening.py`, `backend/tests/test_coverage_gaps.py`, `backend/tests/test_pagination.py`, `backend/tests/test_templates.py`; `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/components/dialogA11y.ts`, `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `tests/playwright/tests/e2e-workflows/helpers.ts`, `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts`, `tests/playwright/tests/e2e-workflows/org-admin-manage-users-modal.spec.ts`, `tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`, `tests/playwright/tests/e2e-workflows/org-security-controls.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`, `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Follow-up UX batch, Phase D: unified Members table — `MemberRoleTable`/`PendingInvitesSection` retired, `kind` split for safe inline role-toggling

Sixth and final phase of the follow-up batch (Phases 0/A/B/C/E above), depending on Phase C (default project groups had to be gone first — otherwise this phase would be building the new table against a group shape that was about to disappear). With that phase landed, groups stopped being the common way to grant a role, which is what made the previous batch's split of Project Admin's Members section into an editable `MemberRoleTable` (direct users + groups) *plus* a separate effective-members audit table *plus* a separate `PendingInvitesSection` look like three sections doing one job rather than three genuinely different concerns. This phase folds all three into one: `components/ProjectMembersTable.tsx`, built on `GET /{project_id}/effective-members` (now editable and paginated) with pending invites merged in as a per-row status.

### Backend — splitting the collapsed `"direct"` provenance kind (`backend/app/services/rbac.py`)

The highest-risk change in this phase, per the plan's own design-review finding: `get_effective_project_members_with_provenance`'s `kind: "direct"` was **not** "directly-granted-role only" — `_direct_effective_project_roles` already internally distinguished five sources (a genuine `UserProjectRole` row, same-project `ProjectGroup` membership, a nested org group, a project-referencing group, and `ProjectVisibility.ORG_WIDE`) before collapsing all five into one bucket. Building "checked + kind is `direct` ⇒ freely toggle-off-able" directly on that collapsed value would have been actively wrong: a role held only via a surviving custom group or org-wide visibility would show as `"direct"` and *look* editable, but `DELETE /{project_id}/roles/{user_id}/{role}` only ever deletes `UserProjectRole` rows — unchecking it in the UI would 204 as a silent no-op while the person kept the role.

Fixed by adding `_direct_effective_project_roles_by_kind` (new function, `rbac.py`) — the same five-source resolution `_direct_effective_project_roles` already did internally, but returning a `dict[str, set[ProjectRole]]` keyed by `direct_role`/`direct_group`/`direct_org_group`/`direct_project_ref`/`direct_org_wide` instead of one unioned set. `_direct_effective_project_roles` itself is now just this new function's five sets unioned together (byte-identical behaviour for every one of its many existing callers — RBAC resolution, notification targeting, C-U-08 checks — none of which needed the finer granularity). `get_effective_project_members_with_provenance`'s own direct-sources loop now emits one provenance entry per `(kind, role)` pair from the breakdown instead of one collapsed `"direct"` entry per role. `forward_inherited`/`member_source_inherited` are unaffected — only the direct-sources bucket needed splitting.

Only `direct_role` is ever safe to present as toggle-off-able: it's the sole kind that corresponds to a real `UserProjectRole` row `DELETE .../roles/{user_id}/{role}` can act on. The other four are shown checked-but-disabled in the frontend, with a title explaining the role must be changed at its actual source (the group's own membership/role, or the project's visibility setting).

### Backend — `search`/`limit`/`offset` on `GET /{project_id}/effective-members`

`get_effective_project_members_with_provenance` still resolves every candidate user's full provenance in Python before any sorting/filtering — a full rewrite to real DB-level pagination was out of scope for this batch (higher risk, no evidence current data volumes need it). Instead, the *endpoint* (`get_effective_members`, `routers/projects.py`) sorts the fully-materialized `EffectiveMemberOut` list by `display_name` (case-insensitive), applies an optional `search` substring filter (name or email) to that sorted list, sets `X-Total-Count` to the post-search count, then slices by `limit`/`offset` — the same `list_project_groups`/`list_org_groups` contract (`X-Total-Count` header, omitting `limit` returns everything). A code comment on the endpoint spells out explicitly that this is application-level, not database-level, pagination, so a future pass doesn't assume it scales indefinitely.

**Deviation from the plan's literal wording**: the plan described this sort/filter/slice as living inside `get_effective_project_members_with_provenance` itself. It's implemented in the router instead — that function returns `dict[UUID, list[dict]]` with no `display_name`/`email` (the router builds those from `User` rows afterward, and also feeds `POST /materialize-inherited-access`, which needs the *complete*, unsliced set). Sorting/filtering/slicing where the named fields already exist, without changing the shared service function's contract for its other caller, is the same outcome with less risk.

**The new frontend component doesn't actually use `search`/`limit`/`offset` for its primary fetch** — see the frontend section below for why, and why that's a deliberate choice, not an oversight; the backend support is still real, pytested, and available to any caller that does need it.

### Frontend — `components/ProjectMembersTable.tsx` (new), replacing `MemberRoleTable.tsx`/`PendingInvitesSection.tsx` (both deleted, with their Storybook stories)

One `DirectoryTable` row per effective member — Name/Email (sortable, client-side re-sort of an in-memory array), Role (`MultiSelectDropdown` over the four `ProjectRole` values; checked options come from `sources`; an option whose only source is `direct_role` is freely togglable via `POST`/`DELETE /roles`, reusing `MemberRoleTable`'s proven "last manager" disabled+title treatment, now sourced from `direct_role`-kind entries only so it can't misreport for a manager whose role actually comes from a group or org-wide visibility; any other kind is checked-but-disabled with an explanatory title), Source (the existing per-role provenance text, one line per kind). Pending invites (`GET .../pending-invites`, unchanged endpoint) are merged in client-side as `kind: "invited"` rows — Role cell becomes a status badge + Resend button. Filters live inside the component itself (not page-composed, unlike every other `DirectoryTable` call site) specifically so the two call sites below render and behave *identically*, not just similarly: a `FilterField` role filter (checks `sources`, not just the collapsed `effective_role`), a `FilterCheckbox` "Show invited" (default on), and `FilterPanel`'s own search box.

**Why the component still loads `members`/`invites` fully and paginates/filters/searches entirely client-side, despite the new backend support existing**: role-filtering and the pending-invite merge both need the *complete* loaded set to stay correct. A `LoadMoreButton` backed by real server-side offset pagination would make the role filter silently incomplete for any member not yet loaded (a page 2 stakeholder simply wouldn't appear when filtering to "Stakeholder" if page 2 hadn't been fetched yet) — a strictly worse failure mode than the existing "this doesn't scale indefinitely" caveat the backend endpoint's own docstring already carries. This mirrors the precedent `MemberRoleTable`'s two data sources already set (small enough to load whole, search/filter/paginate client-side) rather than introducing a new one.

`ProjectAdminGroupKey`'s `"members"` section now renders only `ProjectMembersTable`; effective-members/pending-invites are fetched eagerly in the page's main `reload()` (not gated behind a lazy "Show members" button any more, since this is now the primary content of the tab, not a secondary audit view) — and eagerly for a second reason: the Groups section's own per-group `SidePanel` role `<select>` still needs an equivalent "how many project-manager sources exist" count for its own last-manager disabled state (`groupManagerSourceCount`, recomputed from `memberTableGroups` + `effectiveMembers`' `direct_role`-kind entries — the same two sources the retired `memberRoleRows` used to merge, just read separately now that Members no longer merges them into one row list). Org Admin's "Manage users" modal switches from `MemberRoleTable` to the same `ProjectMembersTable`, fed by the same per-project endpoints — one real shared implementation, which is what actually satisfies the original "org admin's view should look like project admin's" ask precisely.

`GET /{project_id}/direct-members` (`DirectMemberOut` schema, `list_direct_members` endpoint) — added last batch specifically to feed `MemberRoleTable`'s user rows — had no remaining caller once this landed, so it was removed entirely (endpoint, schema, its two dedicated pytest tests) rather than left as unused surface area. Several existing backend tests used it purely as a convenient "does this user hold this direct grant" assertion (`test_templates.py`, `test_default_group_migration.py`, `test_rbac.py`) — these were switched to a new `tests.conftest.direct_project_roles(project_id)` helper that reads `UserProjectRole` straight from the database, the same underlying data the removed endpoint exposed.

### Tests

Backend pytest (`backend/tests/test_effective_members_provenance.py`, new): `test_all_five_direct_kinds_distinguished_for_one_user_one_role` (a user holding the *same* role via all five direct sources simultaneously shows all five kinds separately, not collapsed — the core regression the split guards against), `test_different_roles_via_different_sources_not_cross_contaminated` (a direct `project_manager` grant and a group-sourced `stakeholder` grant on the same user don't leak kinds across roles), `test_revoking_a_direct_role_kind_role_actually_removes_it`, `test_revoking_a_group_sourced_role_leaves_it_untouched` (the backend-side half of "must not silently no-op" — `DELETE /roles` 204s against a role with no matching `UserProjectRole` row, but the group-derived access is still in effect afterward; the frontend's own disabled+title treatment is what actually stops a caller from trying), `test_effective_members_search_narrows_results`, `test_effective_members_limit_offset_paginate_deterministically` (two same-sized pages from the same sorted set never overlap and together reconstruct the full set). Existing tests updated: `test_project_hierarchy.py` (`kind == "direct"` → `"direct_role"`), `test_project_group_role_editing.py` (its two dedicated `/direct-members` tests removed with the endpoint; one gate-tier assertion mid-test switched to `GET /effective-members`, the same manage-tier endpoint at the same gate).

Storybook (`ProjectMembersTable.stories.tsx`, new): mixed member/invited rows, empty state, paginated (`LoadMoreButton`), a group-sourced role showing disabled with its explanatory title, the last-manager hint, search-narrowed, role-filtered, "Show invited" toggled off, and the `addControl` slot. `ProjectAdminPage.stories.tsx`/`OrgAdminPage.stories.tsx` updated: the old `MemberRoleTable`-fed stories (users-and-groups mixed rows, group-role-change-from-Members) removed since groups are no longer rows in this table at all; new stories cover add-direct-member, toggle-a-direct-role-off, and pending-invite-resend at the page-integration level (the component's own stories already cover the UI behaviours themselves in isolation, so these just confirm the real endpoint/callback wiring).

Playwright: `project-admin-members.spec.ts` rewritten — add-member-with-role, toggle a direct role off, the C-U-08 last-manager guard, a group-sourced role showing checked-and-disabled with its explanatory title, and the role filter. The old group-role-change-from-Members and `?openGroup=` deep-link steps were dropped from this spec (groups aren't rows here any more) — the deep link itself is still live, bookmarkable behaviour, so its own round-trip test moved to `project-admin-groups-and-fields.spec.ts` instead of being lost. `org-admin-manage-users-modal.spec.ts` rewritten to match (shows effective members with provenance, toggles a direct role, same last-manager guard). `external-project-invite.spec.ts` updated: the pending-invite-appears/resend test no longer expands a separate "Pending invites" section (folded into the merged table directly), and gained a new "Show invited" toggle step in the same scenario — this spec already sets up an org that permits external invites, so it's the natural home for that coverage rather than a third org-setup in `project-admin-members.spec.ts`. `project-hierarchy.spec.ts`'s own effective-members step no longer clicks the retired "Show members" button. `project-users.spec.ts` deleted outright — its "the members table is searchable and sortable" job is now covered by the rewritten specs above plus `ProjectMembersTable.stories.tsx`'s own component-level coverage, and its own locator assumptions (a two-table layout with a collapsed "Effective members section") no longer match the merged table's shape.

### Docs

This entry; `docs/solution-architecture.md` gained an "Implementation note (unified Members table, follow-up UX batch Phase D)" paragraph recording the `kind` split and the `MemberRoleTable`/`PendingInvitesSection` supersession.

### Judgment calls and deviations from the plan

- **`search`/`limit`/`offset` sort/filter/slice logic lives in the router (`get_effective_members`), not inside `get_effective_project_members_with_provenance`** — see the backend section above; the shared service function's contract for its other caller (`materialize-inherited-access`, which needs the complete set) is unaffected, and the display fields the sort/filter need don't exist on that function's own return shape.
- **The new frontend component doesn't actually issue paginated requests using the new backend params** — a deliberate choice, not a partial implementation: role-filtering and the pending-invite merge both need the complete set to stay correct, and real server-side offset pagination combined with a client-only role filter would silently hide legitimate members on a later page. The backend support is still real and pytested, for future consumers.
- **`GET /{project_id}/direct-members` removed entirely, per the plan's own "check whether it still has any caller" instruction** — several backend tests used it as a convenient assertion helper rather than exercising it as a feature; those were migrated onto a direct DB-read helper (`tests.conftest.direct_project_roles`) rather than treated as "still needs this endpoint to exist."
- **`groupManagerSourceCount` (Groups section's own last-manager hint) recomputed from two separately-fetched arrays instead of one merged row list** — the retired `memberRoleRows` used to combine `memberTableGroups` and direct-member data into one array specifically so both the Members table and the Groups section's own hint could read from it; now that Members no longer needs that merge, the Groups section's hint reads the same two underlying sources directly instead, preserving the exact same count/semantics it always had.

### An incidental fix, found while verifying this phase's own Playwright coverage: `ServerSettings` singleton race

Unrelated to anything this phase otherwise touches, but a real, previously-undiscovered bug — fixed here per this repo's "fix issues found, don't defer" rule rather than filed separately. While chasing an inconsistently-failing full Playwright run (`self-signup.spec.ts` and `org-bundle-export-import.spec.ts` failing with different symptoms on different runs — a classic non-deterministic-bug signature, not a deterministic UI regression), a live inspection of the test database found **two** real `server_settings` rows, with different `signup_mode` values. `ServerSettings` (`services/branding.py::get_server_settings`) was a "lazily created singleton row" by application convention only — `settings = db.scalar(select(ServerSettings)); if settings is None: create one` — with no DB-level constraint preventing two concurrent requests from both observing "no row yet" and both inserting: a textbook TOCTOU race, previously *documented* as a deliberate omission in the model's own docstring, not flagged as a live risk. Once two rows exist, `db.scalar(select(ServerSettings))`'s "first row" (no `ORDER BY`) is not deterministic — different requests can transiently see different rows, which explains both this run's inconsistent failures and, in hindsight, an earlier "genuine, non-reproducing flake" the Phase C entry above already logged against `test_signup.py` without root-causing it.

Fixed with a real DB-level constraint, not just a stricter application check (an application-level fix alone can't close a genuine two-concurrent-transactions race): migration `0020_server_settings_singleton_guard.py` deduplicates any existing extra rows (keeps the most recently updated one — the one most likely to reflect the latest real admin action, since either row could have "won" any given write unpredictably, so there's no principled way to *merge* two independently-diverged rows' field values) and adds `singleton_guard` (`BOOLEAN NOT NULL DEFAULT true`, `UNIQUE`) — a second `INSERT` now fails with an `IntegrityError` instead of silently succeeding as a duplicate. `get_server_settings` catches that `IntegrityError` on the losing side of the race and re-reads instead of letting it propagate, so the race becomes "the loser retries and gets the winner's row" rather than "the loser also succeeds." Pinned by two new tests (`test_server_settings_singleton.py`): the DB constraint itself rejects a manually-attempted second row, and a monkeypatched `db.scalar` forces `get_server_settings`'s own initial read to lie about "no row yet" (simulating exactly the race window) while a real row already exists, confirming the recovery path returns that row rather than raising.

### Verification

Backend: full suite via the dev/test Docker Compose stack (`tests/container`, backend rebuilt + force-recreated first), run to completion twice — once before the `ServerSettings` singleton fix above (**694 passed, 0 failed**) and once after, including its own 2 new tests (**696 passed, 0 failed**) — 72 pre-existing warnings both times (same count as every earlier entry in this batch, none new). `ruff check app tests` clean on both runs.

Frontend: `npx tsc -b` clean; `npm run lint` clean (same 7 pre-existing `react-refresh/only-export-components` warnings as every earlier entry in this batch, none new, none in files touched by this work); `npm run test-storybook` (full suite, 69 files) — **604 passed, 0 failed**, including the new `ProjectMembersTable.stories.tsx` (11 tests).

Playwright, against the dev/test Docker Compose stack (backend + frontend rebuilt and force-recreated first, `seed_demo_data.py`/`seed_e2e_dataset.py` re-run after each backend pytest run truncated the DB): the full suite (`npx playwright test`, every spec, 116 tests) — **116 passed, 0 failed** on the final run, after the `ServerSettings` fix above. A first full run (before that fix) showed 8 inconsistent failures across unrelated areas (`self-signup.spec.ts`, `org-bundle-export-import.spec.ts`, `server-admin-scope.spec.ts`, `server-org-lifecycle.spec.ts`, `user-access-panel.spec.ts`) — investigating why is what surfaced the `ServerSettings` singleton race documented above; 5 of the 8 were confirmed as ordinary flakes on isolated re-run, but `self-signup.spec.ts`'s two mode-dependent tests reproduced consistently across three separate attempts (including live manual reproduction via direct browser automation, which is what led to inspecting the database directly and finding the duplicate row) until the singleton fix landed, after which every previously-flaky spec passed cleanly and repeatably.

**This is the final phase of the follow-up UX batch** (Phases 0, A, B, C, D, E) — with this phase's own verification green, the batch as a whole is complete: `DirectoryTable` is now the shared shell behind all six directories (Org Users, Org Groups, Project Groups, Project Members, Server Admin's Access Review), org-level invites exist alongside password-set "New user," Org/Project Groups both render as real tables with a `SidePanel` per row, default project groups are gone in favour of direct grants, and Project Admin's Members section — now shared verbatim with Org Admin's "Manage users" modal — is a single unified, editable, filterable table over effective membership with pending invites folded in as a status rather than a parallel section.

### Files changed

`backend/app/services/rbac.py`, `backend/app/routers/projects.py`, `backend/app/schemas/project.py`; `backend/tests/test_effective_members_provenance.py` (new), `backend/tests/conftest.py`, `backend/tests/test_project_hierarchy.py`, `backend/tests/test_rbac.py`, `backend/tests/test_templates.py`, `backend/tests/test_default_group_migration.py`, `backend/tests/test_project_group_role_editing.py`; `frontend/src/components/ProjectMembersTable.tsx` (new), `frontend/src/components/ProjectMembersTable.stories.tsx` (new), `frontend/src/components/MemberRoleTable.tsx` (deleted), `frontend/src/components/MemberRoleTable.stories.tsx` (deleted), `frontend/src/components/PendingInvitesSection.tsx` (deleted), `frontend/src/api/types.ts`, `frontend/src/i18n/strings.ts`, `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `tests/playwright/tests/project-users.spec.ts` (deleted), `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts`, `tests/playwright/tests/e2e-workflows/org-admin-manage-users-modal.spec.ts`, `tests/playwright/tests/e2e-workflows/external-project-invite.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`, `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`; incidental `ServerSettings` singleton fix: `backend/app/models/organization.py`, `backend/app/services/branding.py`, `backend/alembic/versions/0020_server_settings_singleton_guard.py` (new), `backend/tests/test_server_settings_singleton.py` (new); `docs/ux-style-guide.md`, `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Project hierarchy on Project Overview + org-admin bypass (PR2 of the follow-up UX batch, 2026-09)

Second PR of the batch, the first of two flagged for identify → verify → remediate rigor (the plan's own PR2 section — see the "Sequencing" note at the top of the plan document): shows parent/child hierarchy on `ProjectOverviewPage` (previously shown on list pages only, never on the single-project dashboard) and gives an org admin visibility into the true hierarchy of any project in their own organisation, regardless of their own role/membership on that project.

### Identify

Read `docs/decisions.md`'s existing "Hierarchical projects" entry and the current code (not the plan's line-number guesses, which had drifted somewhat) before changing anything. Three claims from the plan needed independent verification:

1. **"`_project_out_with_redacted_parent` needs no change — it already calls `can_manage_project_settings`, which already checks `is_org_admin`."** Confirmed true by reading both functions (`backend/app/routers/projects.py:225-266`, `backend/app/services/rbac.py:282-291`): `can_manage_project_settings` returns `True` for `is_org_admin(db, user.id, project.organization_id)` before ever checking the caller's own project role, and `_project_out_with_redacted_parent` already ORs that check alongside `_accessible_project_ids` membership. No change needed here, exactly as the plan said.
2. **Parent/child are same-organisation at write time**, so an admin bypass keyed off the *child's* (or anchor project's) own organisation is safe for every ancestor/descendant in the chain. Confirmed by grep: `create_project` (`projects.py:365-366`) and `update_project` (`:940-941`) both reject a cross-organisation `parent_project_id` with "parent_project_id must be a project in this organisation."
3. **The gap is narrow — just `list_projects`, `get_project_ancestors`, `get_project_children` need an explicit `is_org_admin(...)` OR-condition at their internal `_accessible_project_ids` checks, no change to that shared function's own meaning.** Confirmed by reading `_accessible_project_ids` in full (a genuinely complex, multi-hop, iteratively-expanded set used by several other callers — the plan was right that changing its own return value would be the wrong, broad-blast-radius fix) and grepping every one of its callers (`create_project`'s tree endpoint, `list_projects`, `get_project_ancestors`, `get_project_children`, and `_project_out_with_redacted_parent` itself) to confirm none of the *other* callers needed touching.

**A fourth issue, not in the plan, found while verifying claim 1 above:** the plan's reasoning for "no change needed" implicitly assumes the org admin can already *reach* `GET /{project_id}` in the first place. That endpoint (and `get_project_ancestors`/`get_project_children`) were gated by `require_project_view`, which requires a genuine effective project role via `get_effective_project_roles` — org-admin status alone does **not** satisfy it (per the existing C-U-01 clarification baked into that dependency's own docstring: "org admin only guarantees the ability to manage project settings, not general content access"). Default project visibility is `ONLY_SPECIFIED`, not `ORG_WIDE`, so an org admin with zero direct/group role on a given project gets 403 "Not a member of this project" before the request body — and `_project_out_with_redacted_parent`'s already-correct exemption inside it — is ever reached. This directly contradicts the product requirement ("regardless of their own role/membership on that specific project") and the exact positive test scenario specified for this PR ("an org admin with NO role/group membership on either the parent or child project").

### Verify

Wrote a throwaway probe test (`tests/test_zzz_probe.py`, deleted before finishing — never committed) reproducing the scenario directly: an org admin created via `create_org_user(..., role="org_admin")`, granted no project role at all, calling `GET /{child_id}`, `GET /{parent_id}/children`, and `GET /{child_id}/ancestors`. All three returned `403 {"detail": "Not a member of this project."}` against the real backend and test database — confirmed empirically, not just reasoned about.

Checked whether `require_project_view_or_manage` (an existing dependency, already used by `list_project_groups` for the identical "structure, not content" reasoning — its own docstring: "so an org admin can see... a project they otherwise can't open, without this becoming a general content-access grant") was the right, precedented fix rather than inventing new logic. Also checked whether switching `get_project`'s auth dependency from `require_project_view` to `require_project_view_or_manage` was a genuine authorization *widening* or just closing an inconsistency: `update_project` (`PATCH /{project_id}`) already uses `require_project_manage`, which has the identical `can_manage_project_settings`/`is_org_admin` bypass — so an org admin with zero role could already unilaterally *change* every field `GET /{project_id}` returns, just not *read* them first. Letting the same admin read what they can already write is not a new privilege.

Grepped the full backend test suite for every bare `GET /{project_id}` call (not a sub-resource) to find any test whose 403 expectation this dependency swap could flip to 200: `test_org_group_nesting.py:91`, `test_org_lifecycle.py:71`, `test_org_require_2fa.py:41`, `test_rbac.py:432`, and every `test_project_hierarchy.py` assertion. Read each one's setup: the two "require_project_view-gated" 403 assertions in `test_org_lifecycle.py`/`test_org_require_2fa.py` are for an org admin who created the project themselves (holds a direct `PROJECT_MANAGER` role, so `require_project_view` already passed for them) hitting a *disabled org*/*2FA-required* wall — both of those checks (`_require_org_active`/`_require_org_2fa`) run identically inside `require_project_view_or_manage`, before the role/manage check, so unaffected. `test_rbac.py:432`'s outsider is a member of a *different* organisation entirely — `is_org_admin(db, user, project.organization_id)` is false for them regardless, so unaffected. None of the existing hierarchy tests use an org-admin token with zero project role against a bare `GET /{project_id}`.

### Remediate

**Backend** (`backend/app/routers/projects.py`):
- `get_project`, `get_project_ancestors`, `get_project_children` switched from `require_project_view` to `require_project_view_or_manage` — the fourth finding's fix, not in the original plan text, but required to make the plan's own stated positive test scenario reachable at all.
- `list_projects`: the parent/children population (`visible_parent_ids`/`children_by_parent`) now also includes any project whose *own* `organization_id` the caller is `is_org_admin` of, computed once per distinct organisation present in the result set (not per row) and unioned with the existing `_accessible_project_ids`-based check — `list_projects` itself still only *lists* rows the caller is independently accessible on (unchanged; the endpoint's own "No server-admin bypass (I-M-05)" comment and behaviour stand), only the parent/children *fields on an already-visible row* gained the bypass.
- `get_project_ancestors`: an `admin_bypass` flag (`is_org_admin` on the anchor `project_id`'s own organisation) short-circuits the truncate-at-first-inaccessible-ancestor loop.
- `get_project_children`: the same `admin_bypass` flag widens the `WHERE` filter from `Project.id.in_(accessible_ids)` to unconditional (all children of `project_id`), since children are guaranteed same-organisation as their parent.
- No change to `_accessible_project_ids` itself or to `_project_out_with_redacted_parent` — both confirmed correct in Identify above.

**Frontend**:
- `frontend/src/api/types.ts`: new `ProjectHierarchySummary` interface — the narrow `{id, parent_project_id, parent_project_name, children}` subset `ProjectHierarchyLabels` actually renders, rather than the full `ProjectListItem` it was previously typed against.
- `frontend/src/components/ProjectHierarchyLabels.tsx`: retyped to accept `ProjectHierarchySummary` instead of `ProjectListItem` — `ProjectListItem` still satisfies it structurally, so `ProjectListPage`/`FavouritesPage` needed no changes at their call sites.
- `frontend/src/pages/ProjectOverviewPage.tsx`: the existing load `useEffect` gained two more parallel fetches (`GET /{id}/ancestors`, `GET /{id}/children}` — the latter confirmed via grep to have zero other frontend callers before this, matching the plan). A root-first breadcrumb (ancestors, then the current project's own un-linked name) renders above the header when any ancestors are visible; `ProjectHierarchyLabels`, fed a `ProjectHierarchySummary` assembled from the `Project` GET response plus the fetched `children`, renders inline under the summary line — same plain `.stack`/`text-muted` styling as the rest of the page, no new component pattern introduced.

### Tests

Backend (`backend/tests/test_project_hierarchy.py`, new section "Org-admin hierarchy-visibility bypass"): `test_org_admin_sees_true_parent_children_and_ancestors_with_zero_project_role` (the plan's exact positive scenario — zero role on either project, all three endpoints reachable and truthful), `test_org_admin_of_a_different_organisation_gets_no_bypass` (an org admin of an unrelated organisation gets ordinary 403s — proves the bypass is scoped to *that project's* organisation, not "any org admin anywhere"), `test_non_admin_with_role_on_child_only_still_gets_redacted_parent_and_truncated_ancestors` (the negative/regression case — an ordinary `stakeholder` with a role on the child only still gets `parent_project_id: null`, an empty ancestors chain, and a 403 on the parent's children endpoint), `test_list_projects_shows_true_parent_and_children_to_org_admin_across_the_visibility_boundary` (the `list_projects` field-population case specifically, including confirming the *listing itself* stays unchanged — the parent row still isn't listed for an admin with no independent access to it).

Frontend: `frontend/src/pages/ProjectOverviewPage.stories.tsx` gained `WithHierarchy` (a parent + two children fixture, asserting the breadcrumb, the "Child of"/"Parent of" labels, and — since "Atlas Platform"/"Atlas Core" each legitimately render twice across the breadcrumb and the header/labels — using `getAllByRole`/`getAllByText` with explicit counts rather than the ambiguous singular query). The pre-existing `Dashboard`/`LoadError` stories' `api.get` mocks were extended to answer `/ancestors`/`/children` explicitly (previously falling through to a `buildProject(...)` default that would have handed `ProjectHierarchyLabels` a non-array `children` value and crashed on `.slice()`).

### Verified

Backend: `pytest tests/test_project_hierarchy.py` — **46 passed** (42 pre-existing + 4 new), confirming no regression in the file this PR edits most heavily. A broader regression sweep of every other file found (via the grep above) to call a bare `GET /{project_id}` with a token whose access level could plausibly interact with the dependency swap — `test_org_group_nesting.py`, `test_org_lifecycle.py`, `test_org_require_2fa.py`, `test_rbac.py`, `test_permission_matrix.py`, `test_project_statuses.py` — **71 passed**. `ruff check app/routers/projects.py` clean. Per this task's own scope (new/edited files in isolation, not the full suite — that happens once, after all six PRs land), the full backend suite was not run here.

Frontend: `npx tsc -b` clean. `npx eslint` on every touched file (`ProjectOverviewPage.tsx`, `ProjectOverviewPage.stories.tsx`, `ProjectHierarchyLabels.tsx`, `api/types.ts`) clean. Storybook, targeted rather than full-suite (same reasoning as backend): `ProjectOverviewPage.stories.tsx` alone — **5 passed** (`Dashboard`, `WithHierarchy`, `LoadError`, `LightTheme`, `DarkTheme`); `ProjectHierarchyLabels.stories.tsx` + `ProjectListPage.stories.tsx` + `FavouritesPage.stories.tsx` (the three other consumers of the retyped component/its callers) — **38 passed**, confirming the `ProjectListItem` → `ProjectHierarchySummary` retype didn't break either existing call site.

### Judgment calls and deviations from the plan

- **The dependency swap on `get_project`/`get_project_ancestors`/`get_project_children` (`require_project_view` → `require_project_view_or_manage`) is not in the plan's PR2 section**, which described only the three internal `_accessible_project_ids` call sites. It's a necessary consequence of the plan's own stated requirement and test scenario, found during Identify/Verify above and confirmed empirically before fixing (never assumed) — documented explicitly here per this repo's "flag an authorization change explicitly, don't silently diverge" rule rather than silently expanding scope. Chosen over inventing new logic because it reuses an existing, already-precedented dependency (`list_project_groups`'s own use of it) rather than adding a fourth pattern for the same "org admin sees structure without content access" need.
- **`GET /{project_id}` now also returns full project settings (not just hierarchy fields) to an org admin with zero role**, as a side effect of the dependency swap — judged acceptable and consistent (not a new escalation) because `PATCH /{project_id}` already grants that same admin unilateral write access to every one of those same fields via the identical `can_manage_project_settings` bypass; GET was the outlier, not PATCH.
- **`docs/soc2/policies/access-control-policy.md` was read before making this change** (per the change-management policy's practice for anything touching authorization) — its item 1 ("Organization role... scoped to one organization" implying org-wide authority over that organization's own projects) and item 2 (per-request re-resolution of the caller's *effective* role from the specific target's own organisation id, never "has this role anywhere") both support this shape of check; no wording update was needed there, since it already documents org-admin authority in general terms without enumerating which specific endpoints implement it.

### Files changed

`backend/app/routers/projects.py`; `backend/tests/test_project_hierarchy.py`; `frontend/src/api/types.ts`, `frontend/src/components/ProjectHierarchyLabels.tsx`, `frontend/src/pages/ProjectOverviewPage.tsx`, `frontend/src/pages/ProjectOverviewPage.stories.tsx`; `docs/decisions.md` (this entry).

## Direct org-group project role grants (PR4 of the follow-up UX batch, 2026-09)

Fourth PR of the batch, the second flagged for identify → verify → remediate rigor (the plan's own PR4 section — see the "Sequencing" note at the top of the plan document, `/Users/rballard/.claude/plans/eventual-sleeping-pony.md`). Unlike PR2 (a redaction-bypass fix), this is a genuinely **new** access-grant pathway: an organisation group can now hold a project role *directly*, as its own independently-revocable record (`OrgGroupProjectRole`), the same way `UserProjectRole` already lets a single user do — additive alongside, not a replacement for, nesting an org group inside a `ProjectGroup` (C-U-12). Both mechanisms coexist by explicit product decision (see the plan's "Background" section).

### Identify

Read `backend/app/services/rbac.py`'s whole provenance-resolution area, `backend/app/models/project.py`, `backend/app/routers/projects.py`'s `assign_project_role`/`revoke_project_role`/`add_project_group_member`, the Alembic version history, and the frontend's `MemberSourceProvenanceKind`/`isDirectRoleKind` (post-PR1) before changing anything — the plan's own line numbers had drifted (PR1/PR2/PR3 already landed) and PR1 in particular had already reshaped `_direct_effective_project_roles_by_kind`'s `direct_group`/`direct_org_group` kinds from `set[ProjectRole]` to `set[tuple[ProjectRole, UUID, str]]` (role, group_id, group_name), with `via_group_id`/`via_group_name` already threaded through `get_effective_project_members_with_provenance`. Five things needed independent verification before writing any code:

1. **Where a new kind needs to plug in.** `_direct_effective_project_roles_by_kind` is the single place all five (now six) direct sources are resolved per-user; `_direct_effective_project_roles` (the flat union used by `_forward_inherited_roles`'s `MIRROR_ALL` case and `get_effective_project_members_with_provenance`'s own `MIRROR_ALL` forward-inherited loop) is *just that function's sets unioned together* — confirmed by reading it line by line. This means adding a `direct_org_group_role` kind to the dict makes it participate in `MIRROR_ALL` forward inheritance **automatically**, with no separate code change to the inheritance walk itself.
2. **Which helpers `MIRROR_ROLE`/`MEMBER_ONLY` forward inheritance and all three member-source modes actually go through**, since they do *not* call `_direct_effective_project_roles_by_kind` — traced every call site: `_direct_project_role_holder_ids` (used by `MIRROR_ROLE` forward inheritance, `get_project_users_by_role`, and two of the three member-source helpers), `_direct_project_member_ids_base` (used, via `_direct_project_member_ids`, by `MEMBER_ONLY`/`MIRROR_ALL` member-counting and the project-reference arm), and `_direct_project_roles_excluding_org_wide` (used by `_member_source_derived_roles`'s own `MIRROR_ALL` case). All three needed their own extension; nothing else did. Confirmed by reading `get_effective_project_members_with_provenance` end to end and checking every one of its forward/member-source branches resolves through one of these three functions plus the flat union above — no fourth path exists.
3. **The exact shape to mirror for the new model.** `UserProjectRole` (`backend/app/models/project.py:334-342`): `UUIDPKMixin`/`TimestampMixin`, a bare FK to `users.id` (no `ondelete`, since users aren't hard-deleted), an `ondelete="CASCADE"` FK to `projects.id`, and a three-column unique constraint. The plan's own instruction to cascade-delete both FKs needed one adjustment: `ProjectGroupMember.org_group_id` (not `UserProjectRole.user_id`) is the correct precedent for the org-group side, since org groups *are* hard-deletable (`ondelete="CASCADE"` on `org_groups.id`), unlike users.
4. **The actual current migration head.** `alembic history` (not a line-number guess): `0020` ("Enforce ServerSettings singleton semantics at the DB level"), so the new migration chains as `0021`.
5. **Whether `revoke_project_role`'s C-U-08 guard has a group-scoped equivalent already, and whether one is even meaningful here.** Read `_direct_project_managers`/`get_effective_project_managers` in full: they deliberately count only direct `UserProjectRole` grants and a `ProjectGroup`'s *direct user* members towards the "at least one manager" floor — **no group-derived manager has ever counted**, not even the pre-existing nested-org-group mechanism (`ProjectGroupMember.org_group_id`). This is load-bearing for the guard design below.

### Verify

- **Traced every downstream cascade path a second time**, forward from the three extended helpers rather than backward from the provenance function, to confirm no fifth call site was missed: `_forward_contributed_member_ids` (notification membership), `get_project_users_by_role` (change-request/notification role targeting), `_member_source_contributed_user_ids`, `_member_source_role_holder_ids`. All resolve through `_direct_project_role_holder_ids`/`_direct_project_member_ids_base`, confirming the three-function extension is complete, not partial.
- **Confirmed empirically, not just by reading code, that a group's direct grant on a *parent* project cascades to a *child*** under `MIRROR_ALL`, `MIRROR_ROLE`, and `MEMBER_ONLY`, and via the member-source mechanism — four separate tests (`test_org_group_project_roles.py`, "Inheritance cascade" section below), each asserting both the actual access (a real HTTP call against the child/receiving project) and the provenance shown (`kind="forward_inherited"`/`"member_source_inherited"`, correct `via_project_id`/`via_mode`).
- **Confirmed the C-U-08 guard's practical scope by constructing the edge case directly**, rather than assuming: a project's real (individually-accountable) manager count is unaffected by any group-role grant or revoke, in both directions — revoking a group's `project_manager` grant while a real direct manager still exists succeeds normally even though it removes manager-level access from every group member at once (`test_revoking_a_groups_manager_role_succeeds_when_a_real_manager_remains`); and — since that state can't normally be reached through the API (every existing manager-count-reducing endpoint already guards against reaching zero) — a direct database write bypassing every API-level guard was used to construct an already-zero-real-manager state and confirm the new endpoint's own post-check still catches it (`test_revoking_a_groups_manager_role_is_blocked_if_no_real_manager_remains`), the same defense-in-depth behaviour `delete_project_group`/`update_project_group` already rely on for the identical reason.
- **Read `docs/soc2/policies/access-control-policy.md`** before writing the endpoints (per the change-management policy's practice for anything touching authorization). Its Authorization item 1 defines project role as one of three independent scopes "scoped to one project," and item 2 requires every check to resolve from the specific target's own ID, never "has this role anywhere" — the new endpoints satisfy both unchanged: `require_project_manage` (the same dependency `assign_project_role`/`revoke_project_role` already use) and a live re-check that the org group belongs to `project.organization_id` (mirroring `add_project_group_member`'s identical check for its own `org_group_id` target) are the only two gates, no new scope or bypass introduced. No policy wording update was needed — like PR2's conclusion, it already documents project-role authorization in general terms without enumerating every specific grant mechanism (nesting isn't named either).
- **Checked for escalation beyond what nesting already allowed**, the specific question the plan called out: the new mechanism grants exactly the same fixed `ProjectRole` vocabulary (`member`/`stakeholder`/`project_administrator`/`project_manager`) that `ProjectGroup` nesting and `UserProjectRole` already grant, through the identical `require_project_manage` gate and the identical cross-tenant check — there is no new role, no new bypass, and no path from this mechanism to any capability nesting couldn't already reach. **Checked that revocation cleanly removes the cascade**: resolution is entirely query-time (no cached/materialized row anywhere in the chain — `_direct_effective_project_roles_by_kind` and the three extended helpers all query `OrgGroupProjectRole` fresh on every call), so deleting the row removes it from every consumer's very next query, including both inheritance mechanisms; `test_revoke_removes_the_grant` and the cascade tests' own revoke-then-recheck steps confirm this empirically, not just by construction.

### Remediate

**Backend**
- New model `OrgGroupProjectRole` (`backend/app/models/project.py`, placed immediately after `UserProjectRole`) — `org_group_id` (FK `org_groups.id`, `ondelete="CASCADE"`), `project_id` (FK `projects.id`, `ondelete="CASCADE"`), `role` (`ProjectRole`), unique on `(org_group_id, project_id, role)`. Registered in `backend/app/models/__init__.py`.
- New migration `backend/alembic/versions/0021_org_group_project_roles.py` (`0020` → `0021`), raw-SQL `CREATE TABLE IF NOT EXISTS` + two indexes, matching this codebase's post-0001 migration style (see `0015_project_hierarchy.py`'s `project_member_sources` table for the closest precedent) — additive only, the new table starts empty.
- New schema `OrgGroupProjectRoleAssign` (`backend/app/schemas/project.py`) and `MemberSourceProvenanceOut`'s docstring updated to include the new kind.
- New endpoints in `backend/app/routers/projects.py`: `POST /{project_id}/group-roles` (`assign_group_project_role`) and `DELETE /{project_id}/group-roles/{org_group_id}/{role}` (`revoke_group_project_role`), placed immediately after their user-level counterparts. Both gated by `require_project_manage`; the POST re-validates the org group's organisation matches the project's, mirroring `add_project_group_member`'s identical check; the POST is idempotent (a duplicate grant is a silent no-op, no 409, matching `assign_project_role`'s exact behaviour — verified no second audit event is written either); neither sends a per-member notification, matching `add_project_group_member`/`remove_project_group_member`'s own group-target behaviour (not `assign_project_role`/`revoke_project_role`'s single-user notify) since a group grant can affect many users at once and this codebase's existing group-composition endpoints don't notify on that basis either. The DELETE's engagement (subscription/favourite) cleanup loops every member of the group — direct or via a nested subgroup, using the same descendant-expansion `_direct_project_role_holder_ids` uses to resolve the mechanism — checking each one's remaining effective access individually, rather than the single-`user_id` cleanup `revoke_project_role` does (see "An incidental fix considered and deliberately not made" below for a related pre-existing gap found, not fixed, in `remove_project_group_member`'s own equivalent line).
- `backend/app/services/rbac.py`: `DIRECT_ROLE_KINDS` gains `"direct_org_group_role"`; `_direct_effective_project_roles_by_kind` gains a new branch (reusing the same `user_org_group_ids` — direct + transitively-nested org groups — already computed for the `direct_org_group` branch) that queries `OrgGroupProjectRole` with the same cross-tenant re-check every sibling branch applies; `_direct_project_role_holder_ids`, `_direct_project_member_ids_base`, and `_direct_project_roles_excluding_org_wide` each gain the equivalent extension (Identify finding 2 above) — this is what makes both inheritance mechanisms cascade the new source automatically, without either walk needing its own group-aware branch. `get_effective_project_members_with_provenance`'s per-kind tuple-unpacking check gains the new kind. Module and function docstrings updated throughout (module docstring's source count 7 → 8, with a new numbered source-8 entry; two stale docstring references — "six sources" that should have read the already-correct seven, and a comment in `_direct_project_role_holder_ids` referring to an *earlier*, since-widened version of `get_project_users_by_role` — corrected in passing, since both were directly adjacent to text this change already had to touch).

**Frontend (type-level only, per this PR's explicit scope)**
- `frontend/src/api/types.ts`: `MemberSourceProvenanceKind` gains `"direct_org_group_role"`. `isDirectRoleKind` is **not** extended to return `true` for it — see "Judgment calls" below for the reasoning, since the plan flagged this as a decision to make and document either way.

### Tests

New `backend/tests/test_org_group_project_roles.py` (16 tests): grant + provenance (`kind`/`via_group_id`/`via_group_name`), revoke removes it (API response and a direct DB check), idempotent grant (no duplicate row, no second audit event), a no-op revoke of a never-granted role 204s harmlessly, two groups granting the same role produce two source entries (matching the existing `direct_group`/`direct_org_group` precedent in `test_effective_members_provenance.py`), descendant-expansion through a nested subgroup, cross-tenant grant rejection (400), 403 for both a bare org-role user and a genuine-but-insufficient (`stakeholder`) project role on both grant and revoke, audit log entries for both grant and revoke (entity/action/detail/project_id), and the C-U-08 guard's two edge cases described in Verify above. Four inheritance-cascade tests: `MIRROR_ALL`, `MIRROR_ROLE` (including the negative case — a differently-roled group must not cascade), `MEMBER_ONLY`, and the member-source mechanism — each granting the role to an org group *only*, with no user-level `UserProjectRole` anywhere in the chain, so a passing test can only mean the new source 8 cascade path actually worked.

### Verified

Backend: `pytest tests/test_org_group_project_roles.py` (new) — **16 passed**. Regression sweep of the files most directly exercising the code this change edits most heavily (`_direct_effective_project_roles_by_kind` and its three cascade-helper siblings): `tests/test_effective_members_provenance.py` (8), `tests/test_project_hierarchy.py` (46), `tests/test_rbac.py` (20), `tests/test_org_group_nesting.py` (7) — **81 passed, 0 failed**, confirming the PR1 kind-split and every pre-existing inheritance/member-source/nested-org-group test still passes unchanged. `ruff check` on every changed backend file clean. Per this task's own instructions, the full backend suite was not run — that happens once, after all six PRs land. `alembic heads` confirms a single head at `0021`, chained onto `0020`.

Frontend: `npx tsc -b` clean. `npx eslint src/api/types.ts` clean. No Storybook/Playwright run in this PR, per its explicit scope (type-level plumbing only, no UI wiring).

### Judgment calls and deviations from the plan

- **The C-U-08 guard on `DELETE /{project_id}/group-roles/{org_group_id}/{role}` is shaped like `delete_project_group`'s (lock, delete, flush, re-check `get_effective_project_managers` before commit), not `revoke_project_role`'s single-user pre-check (`managers == {user_id}`).** A group-role revocation can remove effective access from every member of the group at once, so there is no single user id to compare against; the post-check pattern is the correct generalisation, and it's the one `delete_project_group`/`update_project_group` already use for an equivalent whole-group access change. **This guard is, by design, very unlikely to ever actually trip on a group-role-specific cause**: `get_effective_project_managers` has never counted a group-derived manager — not the pre-existing nested-org-group mechanism, and not this new one — towards the "at least one manager" floor (Identify finding 5), so a project's *real* manager count is unaffected either way by granting or revoking a group's `project_manager` role. This is not a new gap PR4 introduces; it's the same, pre-existing, deliberately narrow C-U-08 semantics nested groups have always had (a project can have zero individually-accountable managers while a group still grants many users effective `PROJECT_MANAGER`-level access) — extending `get_effective_project_managers` to count group-derived managers would be a materially larger, separate change to a longstanding invariant across *both* mechanisms, not something this PR's scope covers, and not requested. The guard is kept anyway for defense-in-depth and consistency with its two siblings, and `test_revoking_a_groups_manager_role_is_blocked_if_no_real_manager_remains` confirms it still does something (catches a pre-existing zero-manager state) even though it can't be tripped by the group action itself through any normal API sequence.
- **`isDirectRoleKind` is not extended to return `true` for `"direct_org_group_role"`.** It is, on its current merits, a real and equally strong candidate: `OrgGroupProjectRole` is a genuine, individually-revocable row exactly like `UserProjectRole`, deleted outright (no silent no-op) by `DELETE /{project_id}/group-roles/{org_group_id}/{role}`. But reading how the function's result is actually meant to be consumed (per the task's own instruction to check this before deciding) surfaces the real problem: every existing and anticipated caller of `isDirectRoleKind` is checking "is it safe to let *this member's own row* show a toggle-off that revokes *only this member's* access" (see `ProjectMembersTable.tsx`'s `purelyDirect` check, `kinds.size === 1 && kinds.has("direct_role")` — not currently routed through this helper at all, but the same semantic). A `direct_org_group_role` entry on a user's row is not that: the backing row belongs to the *group*, not the user, so a toggle-off driven from one member's row would silently revoke the role for every other member of that group too — correct-but-surprising at best, a real footgun at worst, once a future UI naively extends this helper to "any individually-revocable kind" without re-deriving that distinction. Extended the function's doc comment substantially (rather than its return value) to spell this reasoning out explicitly, name the future direction (a group-row UI should use its own predicate against the raw kind, not this one), and point at this entry. Per the plan's own instruction, this is UI-consuming-side plumbing only for PR5/PR6 to build — no component, page, or toggle was added or changed in this PR.
- **`_direct_project_managers`/`get_effective_project_managers` were deliberately left unchanged** (not extended to count `OrgGroupProjectRole`-derived managers) — see the C-U-08 bullet above. Flagged explicitly here per this repo's "flag rather than silently diverge" rule, even though it's a decision to leave existing, longstanding behaviour alone rather than a new divergence.
- **An incidental bug found while implementing the new endpoint's own engagement cleanup — fixed in the same pass, not deferred, per this repo's "fix, don't defer" rule.** `remove_project_group_member`'s own equivalent cleanup line (`backend/app/routers/projects.py`) called `get_effective_project_roles(db, member_id, ...)`/`remove_subscriptions_and_favorites_for_projects(db, member_id, ...)` with `member_id` potentially being an org-group id or a source-project id, not a real user id, for a group/project-reference removal — its own docstring already said as much ("member_id may be a user id, an org-group id, or a source-project id here"). Since no real user shares that id, this was a silent no-op for those two cases: removing a nested org group or a project-reference from a group never actually cleaned up the now-inaccessible former members' subscriptions/favourites. Fixed by resolving the group's/source project's real member set first (a new `removed_member` lookup before the delete, then branching on which of `user_id`/`org_group_id`/`source_project_id` was actually set — the org-group branch reuses the same descendant-expansion `revoke_group_project_role` already uses; the source-project branch reuses `_direct_project_member_ids_base`, the same non-recursive roster resolution the project-reference mechanism already uses elsewhere), then looping the existing per-user check/cleanup over that real set. Two new regression tests in `test_project_group_role_editing.py` (`test_removing_a_nested_org_group_member_cleans_up_its_users_favourites`, `test_removing_a_project_reference_member_cleans_up_its_roster_favourites`) — both favourite a project as the affected user, remove the group/reference membership, and assert the favourite is actually gone; the first would have failed with the favourite still present before this fix (confirmed by running it against the pre-fix code).
- **A second, related, more consequential gap found and fixed in the same pass, discovered while writing the listing-visibility test for this PR's own new mechanism**: `_accessible_project_ids` (`backend/app/routers/projects.py`) — the function behind `GET /projects` (search/listing), `/ancestors`, and `/children`, and *distinct* from `get_effective_project_roles`/`require_project_view`, which `GET /{project_id}` uses directly — only ever covered a user's *direct* membership (a `UserProjectRole`, or being a plain `user_id` member of a `ProjectGroup`). It never accounted for a role reached only via an org group the user belongs to, whether through the pre-existing nested-org-group mechanism (`ProjectGroupMember.org_group_id`) or this PR's own new direct grant (`OrgGroupProjectRole`). Concretely: a user whose *only* access to a project was via either mechanism could already open it directly (`GET /{project_id}` returned 200, confirmed by the pre-existing `test_transitive_nesting_grants_effective_project_role_through_a_chain`), but the project never appeared in that user's own project list or search — a genuine, user-visible correctness bug, and for this PR's own new mechanism specifically, an incompleteness in PR4 itself, not just a pre-existing sibling gap. Fixed by adding two more source sets to `_accessible_project_ids`'s base computation (reusing `get_user_org_group_ids`, already used elsewhere for the identical direct+transitively-nested resolution, once per organisation the user belongs to): projects reachable via a nested org group's `ProjectGroupMember` row, and projects reachable via `OrgGroupProjectRole`. The existing iterative expansion loop (children / member-source / project-reference propagation) needed no change — it already propagates outward correctly from whatever the base set contains, and the project-reference case was independently confirmed still correct via `test_removing_a_project_reference_member_cleans_up_its_roster_favourites`, which passed without needing this fix (that test's member reaches the source project via a *direct* `UserProjectRole`, not a group). Two new tests in `test_org_group_project_roles.py` (`test_grant_makes_the_project_appear_in_the_groups_members_own_project_list`, `test_nested_org_group_membership_also_appears_in_the_project_list`) cover both sources; the first would have failed (`StopIteration`/project missing from the listing) before this fix.
- **Verified (this addendum)**: `pytest tests/test_project_group_role_editing.py` — 10/10 (8 pre-existing + 2 new). `pytest tests/test_org_group_project_roles.py` — 18/18 (16 pre-existing + 2 new). Regression sweep of every file most directly exercising `_accessible_project_ids`/the RBAC provenance functions this addendum touches — `test_project_hierarchy.py`, `test_rbac.py`, `test_org_group_nesting.py`, `test_project_config.py`, `test_effective_members_provenance.py`, plus the two files above — **115/115 passed, 0 failed**. `ruff check` on both changed files clean.
- **Two stale docstrings corrected in passing** (module docstring's source count, `_direct_project_role_holder_ids`'s reference to an already-superseded `get_project_users_by_role` behaviour) — both directly inside text this change already had to edit for the new source, not a separate drive-by pass over the file.
- **`docs/solution-architecture.md` updated**: the identity/access-control ER diagram gains `ORG_GROUP_PROJECT_ROLE` and its two relationships, with explanatory prose distinguishing it from `PROJECT_GROUP_MEMBER`'s `org_group_id` arm; the table-count line and the "Identity, organisation membership, and access control" table list are corrected from 46 to 48 tables. **A second, unrelated pre-existing drift found and fixed in the same pass**: `project_member_sources` (present in the ER diagram and every migration/model since the "Hierarchical projects" entry above) had been missing from the table list entirely, undercounting by one independently of this PR's own new table — fixed alongside rather than left as a second, separately-discovered gap.

### Files changed

`backend/app/models/project.py`, `backend/app/models/__init__.py`; `backend/alembic/versions/0021_org_group_project_roles.py` (new); `backend/app/schemas/project.py`; `backend/app/routers/projects.py`; `backend/app/services/rbac.py`; `backend/tests/test_org_group_project_roles.py` (new); `backend/tests/test_project_group_role_editing.py` (addendum); `frontend/src/api/types.ts`; `docs/solution-architecture.md`; `docs/decisions.md` (this entry).

## Unified groups directory: combined user/group autocomplete, Groups tab merges member sources (PR5 of the follow-up UX batch, 2026-09)

Fifth PR of the batch — pure frontend, no backend changes, since PR4 already built both endpoints this PR consumes (`POST`/`DELETE /{project_id}/group-roles`). Two independent pieces, both scoped by the plan's own PR5 section: (1) the "Add member" button/`Modal` PR3 built (a user-only `UserAutocomplete` + role `<select>`) is expanded so its autocomplete also matches org groups by name, and selecting one grants it a role *directly* via PR4's mechanism; (2) the Groups tab's `DirectoryTable` merges in `ProjectMemberSource` rows, previously a structurally-disconnected section on the Overview tab.

**Corrected during this PR's own review, changing the original plan** (see the plan's PR3 section, "Decision (revised after PR4 landed...)"): the plan originally described two adjacent buttons, "Add member" and "Add group," with PR3 shipping the shell and PR5 filling in the second button. The corrected design has **no second button or modal for groups at all** — a single "Add member" control whose autocomplete search itself is expanded to match both users and org groups, distinguished by a badge in the results dropdown. Nesting a group inside another group (an org group inside a project group, or an org group inside another org group) is explicitly out of scope for this control — that composition stays exactly where it already was: a project group's own "nest an org group" affordance in its `SidePanel`, and an org group's own `SidePanel` for nesting a subgroup.

### Frontend

**`frontend/src/components/UserAutocomplete.tsx`** — new optional `groups?: OrgGroup[]`/`onSelectGroup?: (groupId: string) => void` prop pair. Group matching is *always* client-side against the passed-in array (mirroring the existing default, no-`organizationId` user-matching branch's own substring-on-`name` logic exactly), regardless of whether user matching in that same instance is running client- or server-side — there is no server-side group search endpoint, and none was needed: every call site already has the full org group list loaded for the Groups tab. `Option` becomes a three-way union (`user`/`group`/`external`); group matches render after user matches and before the external-invite option, each with a `Users` icon (`lucide-react`) and a "Group" text badge. **Icon choice**: grepped every existing group-related header/row in the app (Groups tab heading, Org Groups heading, a group row's own rendering) for a precedent icon first — none exists anywhere in this codebase for "a group of users" as a concept, only plain text. `Users` (plural) was picked over the singular `User` glyph implicitly used elsewhere, since it reads as "more than one person" without colliding with an already-claimed icon meaning. Keyboard nav (`ArrowUp`/`ArrowDown`/`Enter`/`Escape`, `aria-activedescendant`) needed no new logic — it already walks the generic `options` array positionally, so it moves through group options automatically once they're part of that array; pinned by a new Storybook story (`KeyboardNavigationMovesThroughGroupOptionsToo`) rather than just trusted.

**`ProjectAdminPage.tsx`** — new `addProjectGroupRole(orgGroupId, role)` handler (`POST /{project_id}/group-roles`, then `reloadEffectiveMembers()` — same "re-fetch just effective-members, not a full `reload()`" convention `addProjectMember` already uses), wired as `onSelectGroup` on the Members tab's "Add member" modal's `UserAutocomplete`, passed `groups={orgGroups}` (already loaded for the Groups tab, no new fetch). **`OrgAdminPage.tsx`** — the identical `addManageUsersGroupRole` on the "Manage users" modal's own `UserAutocomplete`, passed `groups={allGroups}`.

**Groups tab merge** (`ProjectAdminPage.tsx`) — a new frontend-only `GroupsTabRow = {kind: "group"; group: ProjectGroup} | {kind: "member_source"; source: ProjectMemberSource}` union, the same pattern `ProjectMembersTable.tsx`'s own `member`/`invited` `Row` already established (not a new pattern invented for this PR). `groupsTabRows` puts every `memberSources` entry first (a small, always-fully-loaded fixed set), then the paginated/sortable `groups` — so `DirectoryTable`'s `LoadMoreButton` only ever appends more real groups after the fixed member-source rows, never reordering them; `total` is `groupsTotal + memberSources.length` to keep the "N of M loaded" count honest. A new "Type" column badges each row ("Project group" vs. "Project," both through `{Project}` terminology substitution — deliberately not "Org group," since a member source names another whole project this one mirrors members *from*, not an org group; see [ux-style-guide.md](ux-style-guide.md)'s new pattern below). `onRowClick` only opens the group `SidePanel` for `kind === "group"` rows; a member-source row is read-only detail rendered directly in its own cells (its mode/filter-role badge, and a "Remove" button reusing the existing `removeMemberSource` handler unchanged) rather than getting the real group's click-to-open/add-member affordances.

The Overview tab's old "Member sources" section (heading, `<ul>` list, and its inline add-controls) is removed entirely from Overview; its add-controls (the source-project/mirror-mode selects and "Add" button — same `addMemberSource`/`sourceCandidates`/`addSourceId`/`addMirrorMode`/`addMirrorFilterRole` state and validation, untouched) relocate onto the Groups tab as their own card below the merged `DirectoryTable`, keeping the "Member sources" heading so the control still has a name (and so this PR's Playwright updates below only needed a tab-navigation step added, not a wording change).

**`frontend/src/components/ProjectMembersTable.tsx`** — an incidental gap found while verifying this PR's own group-grant flow actually reads back correctly, not something the plan called out: `sourceLine`'s `switch` over `MemberSourceProvenanceKind` had no case for `direct_org_group_role` (PR4's new kind), falling through to the `default: return ""` branch — a group's direct grant would have rendered a *blank* Source cell for every member of that group, silently. Fixed with a `sourceDirectOrgGroupRole` string (`"Via group 'X' (direct)"` — distinct wording from `sourceDirectOrgGroup`'s "Via nested org group," since they're genuinely different mechanisms per PR4's own docstring) and the missing `case`. Per this repo's "fix, don't defer" rule, fixed in this same PR rather than filed separately, since it's directly in the path of the feature this PR builds.

### Tests

Storybook: `frontend/src/components/UserAutocomplete.stories.tsx` gained `GroupMatchRendersAndSelects` (a group match renders with the "Group" badge, click calls `onSelectGroup` not `onSelect`, and resets the input) and `KeyboardNavigationMovesThroughGroupOptionsToo` (arrow keys reach a group option, `Enter` selects it). `frontend/src/components/ProjectMembersTable.stories.tsx` gained `NamedOrgGroupRoleSourceLine`, pinning the `sourceLine` fix above and confirming the row's role checkbox is still checked-but-disabled (never a per-user toggle over a group-owned grant — see `isDirectRoleKind`'s own doc comment, PR4). `frontend/src/pages/ProjectAdminPage.stories.tsx` gained `MembersTabAddMemberAutocompleteMatchesGroup` (typing into "Add member" matches an org group, selecting it posts `/group-roles` with the currently-selected role, modal closes) and `GroupsTabShowsGroupsAndMemberSourcesTogether` (both row kinds render with their correct Type badge in the same table; the member-source row's "Remove" button calls `DELETE /member-sources/{id}`); `MemberSourcesListAndAdd` updated to navigate to the Groups tab first and assert against a `<tr>`/`<td>` instead of the retired `<ul>`/`<Link>`. `frontend/src/pages/OrgAdminPage.stories.tsx` gained the equivalent `ManageUsersModalAddMemberAutocompleteMatchesGroup`. Both page-level mock helpers (`mockProjectAdminApis`, `mockProjectsWorkflowWithOneProject`) gained an explicit `/users/search` branch (`{members: [], external: null}`) — needed once a story actually types into the autocomplete in `organizationId` mode; previously every story exercising that control stopped short of typing specifically to avoid triggering the unmocked debounced search.

Playwright: `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts` — its three "Member sources" heading assertions updated to select the Groups tab first (`selectProjectAdminGroup(page, "Project groups")`) before asserting, and the row-shape assertions changed from `<li>`/`<Link>` to `<tr>`/`<td>` (a member-source row has no `<Link>` any more — see the read-only-detail decision above). Grepped the full Playwright suite for any other "Member sources"/`memberSources` reference; this was the only file. `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts` — the existing "create a new project group" step gained a "Project group" Type-badge assertion; a new step, "grant an org group a project role directly, from the Members tab's combined Add member autocomplete," creates a throwaway org group with one member via direct API calls (a role grant on an empty group has no effective member to show it on), performs the grant through the real UI (typing into "Add member," clicking the group match, confirming the modal closes), asserts the granted member's row shows the new `sourceDirectOrgGroupRole` wording, then revokes the grant in a `finally` block — `memberAlphaBeta` is a shared persona several other specs assert against on the same project, so a stray stakeholder grant left behind would leak into their own assertions (this repo's test-independence rule). Per this PR's own scope, these Playwright specs were mechanically updated/written but not executed — the plan's own verification step runs the full spec suite once, after all six PRs land.

### Docs

`docs/ux-style-guide.md` gained a new "Pattern: type-badged mixed rows in a single directory" section (with a Mermaid flowchart) — the plan's own explicit requirement, since merging two backend mechanisms into one visual list, distinguished only by a badge, is a genuine new UI pattern per this guide's "one documented pattern, not a one-off" rule, not a one-off worth leaving undocumented. `docs/solution-architecture.md` gained an implementation note on the unified Groups directory (naming all four group-like mechanisms and where each is now surfaced) directly above the existing Phase D "unified Members table" note it extends.

### Verified

Frontend: `npx tsc -b` clean throughout (checked after every file's edits, not just once at the end). Storybook, targeted rather than full-suite (matching PR2/PR4's own "new/edited files in isolation" scope for this batch): `UserAutocomplete.stories.tsx` — **10 passed**; `ProjectMembersTable.stories.tsx` — **13 passed**; `ProjectAdminPage.stories.tsx` — **40 passed**; `OrgAdminPage.stories.tsx` — **56 passed**. No Docker Compose/Playwright run in this PR, per its explicit scope — full-suite verification (backend pytest, Storybook, and Playwright together) happens once, after PR6 lands.

### Judgment calls and deviations from the plan

- **A member-source row's "Remove" action is a plain button in its own `DirectoryTable` cell, not something reached by clicking the row.** The plan's own wording ("read-only detail... and a way to remove the source") didn't specify the exact mechanic. `DirectoryTable`'s `onRowClick`/`rowHref` apply table-wide, not per-row, and are already committed to opening a real group's `SidePanel` for `kind === "group"` rows — reusing the same click for a member-source row would need either a second, meaningless "detail panel" with nothing more to show than what's already in the row, or a same-table-wide handler with an inert no-op branch for member-source rows (confusing: a first cell that looks clickable but silently does nothing). Putting Remove directly in its own column avoids both, and keeps the mode/filter-role detail directly visible in the row rather than gated behind a click, which is arguably better discoverability than the original Overview-tab list already had.
- **Dropped the member-source row's outbound `<Link>` to the source project** (present on the old Overview-tab `<li>` row) rather than finding a way to preserve it. Nesting a `<Link>` inside `DirectoryTable`'s own row-click `<button>` wrapper would be an invalid nested-interactive-elements pattern; adding a second "View" action alongside "Remove" was considered and rejected as unnecessary scope growth for a control this PR's own constraints ask to keep minimal ("if you find yourself needing more than one action... keep it minimal, note it for PR6"). The source project is still reachable via the existing "Parent project"/global search paths; this is a minor, explicitly-noted regression, not a silent one.
- **`sourceDirectOrgGroupRole`'s missing `switch` case was fixed as an incidental bug, not filed separately** — per this repo's standing "fix, don't defer" rule, and because it sits directly in the render path of the feature this PR adds a UI entry point for (a group grant with no visible Source text would have made the whole feature look broken the first time someone used it).
- **No `docs/decisions.md`-mandated identify → verify → remediate write-up** for this entry, unlike PR2/PR4 — the plan's own sequencing note reserves that rigor for PRs that change or add an access-control pathway; this PR only adds UI on top of PR4's already-reviewed mechanism and moves presentation of already-existing data, with no new backend code and no change to any authorization check.

### Files changed

`frontend/src/components/UserAutocomplete.tsx`, `frontend/src/components/UserAutocomplete.stories.tsx`; `frontend/src/components/ProjectMembersTable.tsx`, `frontend/src/components/ProjectMembersTable.stories.tsx`; `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`; `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`; `docs/ux-style-guide.md`, `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Project group roles become independent grants (PR7 of the follow-up UX batch, 2026-09)

Seventh PR of the batch, moved ahead of PR6 in execution order (see the plan's own "Sequencing" note) and given the same identify → verify → remediate rigor as PR2/PR4 — the plan's own explicit instruction, since this is **the most invasive PR in the whole batch**: it doesn't just add a new grant pathway alongside an existing one (PR4's shape) or fix a redaction gap (PR2's shape), it changes the *meaning* of an existing, previously-required field. `ProjectGroup.role` — a single value fixed at creation, the only role a group could ever grant — is replaced by `ProjectGroupRole`, a separate grant table: a group now holds zero, one, or several independently-revocable roles, mirroring the exact shape PR4's `OrgGroupProjectRole` established one level up (org group → project role) and `UserProjectRole` established for a single user — "one row per role," confirmed with the product owner explicitly as "the same as users with multiple roles," never an array/JSON column.

### Identify

Read `backend/app/models/project.py`'s `ProjectGroup`/`OrgGroupProjectRole` (the exact structural precedent to mirror), `backend/app/routers/projects.py`'s full project-groups section (`create_project_group` through `remove_project_group_member`, plus `assign_group_project_role`/`revoke_group_project_role`, PR4's closest precedent), `backend/app/services/rbac.py`'s whole provenance-resolution area, the current Alembic head, `backend/tests/test_project_group_role_editing.py`/`test_default_group_migration.py` (existing coverage and the Phase C migration-test precedent to copy), and the frontend's `ProjectAdminPage.tsx` Groups tab / `UserAutocomplete.tsx` (PR5's current state) before changing anything. The plan itself flagged that its own guesses at "which functions need this" might be wrong and told this pass to verify by grepping rather than trust the plan's list — that grep surfaced real drift in both directions:

1. **Every literal reference to `ProjectGroup.role`/`group.role`**, found by grepping the whole backend rather than trusting the plan's enumerated list: `backend/app/routers/projects.py` — `_ensure_project_has_a_manager`'s fallback-group lookup (not named in the plan at all), `create_project_group`, `list_project_groups`, `_project_group_out`, `update_project_group`/`ProjectGroupUpdate` (the whole endpoint+schema, since there is no longer a single field to `PATCH`), `delete_project_group`'s C-U-08 guard, `remove_project_group_member`'s C-U-08 guard. `backend/app/services/rbac.py` — the `direct_group` branch (Step 3's own scope) *and* two sites the plan's Step 3 text didn't name: the `direct_org_group` nesting branch's own enclosing-group role read (source 3 also grants "the enclosing `ProjectGroup`'s role," so it needed the identical treatment) and the source-7 project-referencing-group branch (the *referencing* group's role, not the source project's). All three read the granting `ProjectGroup`'s role and all three needed the same one-row-per-(group,role) join rewrite as the `direct_group` branch the plan did name. Also `_direct_project_role_holder_ids` (three call sites) and `_direct_project_roles_excluding_org_wide` (two call sites) — matching the plan's guidance. And, **not named by the plan at all**: `_direct_project_managers`, the function `get_effective_project_managers` (the actual C-U-08 "at least one manager" check) is built on — missing this would have left the primary manager-floor guard querying a column the migration was about to drop, breaking project creation/every guard site outright the moment the column left the schema. `backend/app/services/project_export.py` (bundle export/import) and `backend/app/services/templates.py` (`clone_project`) also read/write `group.role`/`role=payload.role` and needed the `roles`-list equivalent — neither named in the plan's file list.
2. **`_direct_project_member_ids_base`'s `ProjectGroupMember` arms never actually referenced `ProjectGroup.role` at all**, contrary to the plan's assumption that all three "sibling helpers" PR4 extended read the scalar directly — grepping confirmed this function has no `ProjectGroup.role` reference anywhere; it already treats any group membership (regardless of role) as "belongs to this project's roster," which was harmless while every group necessarily had exactly one role. A first pass left this deliberately unchanged, reasoning membership and role-holding were separate concepts elsewhere in this module — see Verify below for why that reasoning was wrong in practice and had to be reversed.
3. **The current migration head** (`alembic history`, not a line-number guess): `0021` ("Direct org-group project role grants," PR4's own new migration), so this PR's migration chains as `0022`.
4. **Whether `ProjectGroupUpdate`/`PATCH /projects/{id}/groups/{group_id}` has any other reason to exist once `role` is gone.** It doesn't — a `ProjectGroup` has only ever had `name` (no rename endpoint, matching `OrgGroup`) and `role`; with `role` moved to its own grant table, the endpoint has nothing left to update and was removed entirely, not left as a dead stub.

### Verify

- **Ran the new migration against the live local test database both directions** (`alembic upgrade head` / `alembic downgrade 0021` / re-`upgrade head`) before writing any test for it, against a database already carrying real pre-existing `project_groups` rows from other work in this session — confirmed the backfill and the lossy-but-safe downgrade (picks the highest-privilege role a group holds; a zero-role group falls back to `'member'`, matching how a freshly-created post-PR7 group with no grants yet would have looked under the old required-field shape) both run cleanly against real data, not just a hand-built fixture.
- **`test_schema_migrations_match_models.py` (this repo's own drift guard) failed on first real run** — not against this PR's own new table, but against a **pre-existing gap already present in the working tree from PR4**: `OrgGroupProjectRole.org_group_id`/`.project_id` had no `index=True` even though migration `0021` already creates `ix_org_group_project_roles_org_group_id`/`_project_id` explicitly, so `Base.metadata` never matched the live schema for that table either — the drift test would have failed for PR4's table regardless of anything in this PR. Fixed alongside this PR's own identical new gap (`ProjectGroupRole.project_group_id` needed the same `index=True`) rather than left as a second, separately-discovered problem, per this repo's "fix, don't defer" rule — both are in `backend/app/models/project.py`, directly adjacent to code this PR already had to touch.
- **Confirmed, by writing a listing-visibility test and watching it fail before investigating why, a second real regression this PR's own schema change introduced**: `_accessible_project_ids`'s `project_ids_via_group` (`backend/app/routers/projects.py`, the function behind `GET /projects`) joined only `ProjectGroup`→`ProjectGroupMember`, with no role check at all — meaning a user whose *only* connection to a project was membership in a group that had just had its *last* role revoked still appeared in their own `GET /projects` listing, and (via the identical join shape in `rbac._direct_project_member_ids_base`) could still be pulled into an *unrelated* project through the project-reference mechanism, or still get notified via `get_project_member_user_ids`, despite `get_effective_project_roles` — the actual authoritative access check — already correctly seeing zero roles for them. This directly invalidated the Identify-phase reasoning that left `_direct_project_member_ids_base` unfiltered (finding 2 above): "membership without role still counts" was fine when every group had exactly one role by construction, but the schema change this PR makes broke that invariant, and a test proved the consequence concretely rather than the change being caught only by reasoning about it. Both `_accessible_project_ids`'s `project_ids_via_group` and `_direct_project_member_ids_base`'s two `ProjectGroupMember` arms now require an `EXISTS`/`JOIN` against `ProjectGroupRole` — a group's membership only counts (for listing visibility, project-reference roster resolution, and notification targeting alike) while that group still holds at least one role, consistent with what the authoritative check already required.
- **Checked every SQL join this PR added compiles and executes correctly, not just reads correctly** — three of the rewritten `rbac.py` queries (`direct_group_rows`, `nested_group_rows`, `project_ref_rows`) initially raised `psycopg2.errors.UndefinedTable`/`missing FROM-clause entry`: selecting `ProjectGroupRole.role` without `ProjectGroup` also appearing as a selected column meant SQLAlchemy never added `project_groups` to the FROM/JOIN chain at all, since nothing in the original `.join(ProjectGroupMember, ... == ProjectGroup.id)` call established `ProjectGroup` as a join anchor when `ProjectGroup` itself wasn't part of the select. Fixed by explicitly joining `ProjectGroup` first (`.join(ProjectGroup, ProjectGroup.id == ProjectGroupRole.project_group_id)`) before any join that references it in its `ON` clause — caught by actually running the new test module against the live database, not by code review alone; the three call sites this affected are exactly the three "not named in the plan" rewrites in Identify finding 1.
- **Checked for escalation beyond what a single role already allowed**, the specific question the plan called out: multiplicity (a group holding several roles at once) only ever produces the *union* of what each of those roles individually grants a user through the group's existing membership — the exact same union any of that group's members could already reach one role at a time by being granted each role via a *separate* group (already proven possible, and already tested, since PR1). No new capability, no new bypass, and no code path treats "holds N roles" as anything other than N independent, individually-revocable entries — `get_effective_project_members_with_provenance`'s existing per-kind loop (unchanged by this PR) already renders one Source row per entry regardless of how many a single group contributes, the same generic mechanism that already handled two *different* groups granting the same role (PR1) now also correctly handles one group granting two *different* roles, with no special-casing needed on either side.
- **Read `docs/soc2/policies/access-control-policy.md`** before touching the guard sites (per the change-management policy's practice). Its Authorization items (project role scoped to one project; every check resolves from the specific target's own ID) are unaffected in shape — every guard site still resolves from `project_id`/`group_id` exactly as before, just checking a joined table instead of a scalar column. No policy wording update needed, matching PR2/PR4's identical conclusion.

### Remediate

**Backend**
- New model `ProjectGroupRole` (`backend/app/models/project.py`, placed immediately after `OrgGroupProjectRole`) — `project_group_id` (FK `project_groups.id`, `ondelete="CASCADE"`, indexed), `role` (`ProjectRole`), unique on `(project_group_id, role)`. No separate `project_id` column, unlike `OrgGroupProjectRole` — a `ProjectGroup` already belongs to exactly one project by construction. `ProjectGroup.role` removed from the model entirely; its docstring rewritten to document the new shape and point at this entry.
- New migration `backend/alembic/versions/0022_project_group_roles.py` (`0021` → `0022`): creates `project_group_roles` + index, backfills exactly one row per existing `project_groups.role` value (`INSERT ... SELECT ... ON CONFLICT (project_group_id, role) DO NOTHING`, guarded by the same `IF EXISTS (information_schema.columns ...)` check `0014`/`0017`/`0018`/`0019` already use so a fresh database's `create_all()`-based schema — which never has the `role` column at all — is a safe no-op), then drops `project_groups.role` — the exact "backfill then drop in one migration" convention `0019`'s `is_default` removal established, chosen deliberately over a two-migration approach so there is never an intermediate revision with both the old and new shapes half-coexisting. Downgrade is lossy for a group holding more than one role (documented, same acknowledged lossiness `0019`'s own downgrade has) — picks the highest-privilege role rather than an arbitrary one, so a downgrade never silently drops a manager grant down to a lesser role.
- New schema `ProjectGroupRoleAssign` (`role: ProjectRole` — no `group_id` field needed, unlike `OrgGroupProjectRoleAssign`'s `org_group_id`, since the target group is already named by the URL path and needs no cross-tenant re-validation). `ProjectGroupCreate.role` removed. `ProjectGroupUpdate` removed entirely (Identify finding 4). `ProjectGroupOut.role` → `roles: list[ProjectRole]`.
- New endpoints in `backend/app/routers/projects.py`, replacing `update_project_group`: `POST /{project_id}/groups/{group_id}/roles` (`assign_project_group_role`) and `DELETE /{project_id}/groups/{group_id}/roles/{role}` (`revoke_project_group_role`) — mirror `assign_group_project_role`/`revoke_group_project_role` (PR4) as closely as sensible: same 204-no-body shape, same idempotent-grant-is-a-silent-no-op behaviour, same audit-log pattern. No cross-tenant re-check (unlike PR4's `org_group_id` validation) since a `ProjectGroup` already belongs to exactly one project. `create_project_group` now creates a bare group (zero roles); `list_project_groups`/`_project_group_out` compute `roles` from a live join instead of reading a stored scalar (same "reads live at query time, never a value copied at member-add/creation time" property `update_project_group` used to have).
- **Every C-U-08 "must retain a manager" guard site changed from `group.role == PROJECT_MANAGER` to "does this group currently hold a `PROJECT_MANAGER` row in `ProjectGroupRole`"**, explicitly enumerated (Identify finding 1): `_ensure_project_has_a_manager`'s fallback-group lookup (`create_project`'s manager-guarantee, template-clone-adjacent path), `delete_project_group`, `remove_project_group_member`, and the new `revoke_project_group_role` (the group-scoped guard site this PR itself adds — shaped like `delete_project_group`'s lock-then-recheck-after pattern, since a role revocation can remove access from every member of the group at once, not a single-user pre-check). Unlike PR4's org-group-role guard (defense-in-depth only, since a nested/direct-grant org group's managers have never counted toward the C-U-08 floor at all), **this one is the actually load-bearing guard for its mechanism**: `_direct_project_managers` *does* count a `ProjectGroup`'s direct user members toward the floor when the group holds `PROJECT_MANAGER` (unchanged behaviour, just re-pointed at the new table) — so `revoke_project_group_role`'s guard is what a real "last manager" scenario actually trips on, not just a defensive backstop.
- `backend/app/services/rbac.py`: the `direct_group` branch and the two additional sites Identify finding 1 surfaced (the `direct_org_group` nesting branch's enclosing-group role, the source-7 project-reference branch's referencing-group role) now join through `ProjectGroupRole` instead of reading a scalar, each producing one entry per (group, granted role) pair — mirroring PR4's `direct_org_group_role` extension pattern exactly, per the plan's own instruction. `_direct_project_role_holder_ids`, `_direct_project_roles_excluding_org_wide`, and `_direct_project_managers` (the last not named by the plan — Identify finding 1) all gained the equivalent rewrite. `_direct_project_member_ids_base` and `_accessible_project_ids`'s `project_ids_via_group` both gained a new `ProjectGroupRole` join requirement (Verify finding above) — a correction to the Identify-phase's own first-pass reasoning, not a plan-specified change. Module docstring's source-2 entry and every touched function's docstring updated.
- `backend/app/services/project_export.py`: `groups_json` export/import now round-trips `roles` (a list) instead of `role`; import reads either `"roles"` (current shape) or a legacy `"role"` singular key (a bundle exported before this PR), so an older export can still be imported without losing its group's role. `backend/app/services/templates.py`: `clone_project` copies every `ProjectGroupRole` row a template's group holds, not just one.
- `backend/scripts/seed_demo_data.py`: `create_project_group` now creates bare then grants the role via the new endpoint (two calls instead of one), matching how the frontend and every other caller now has to.

**Frontend**
- `frontend/src/api/types.ts`: `ProjectGroup.role` → `roles: ProjectRole[]`. `isDirectRoleKind`'s doc comment extended to note `direct_group` (this PR) is now, like `direct_org_group_role` (PR4), backed by a genuine per-role revocable row — and, for the identical blast-radius reason PR4's entry already gives, deliberately still excluded from this predicate (a toggle on one member's row must never silently revoke a *group's* role for every other member at once).
- `frontend/src/components/MultiSelectDropdown.tsx` reused (not extended) for two new call sites in `ProjectAdminPage.tsx`: the Groups tab's per-row "role" column, and the group's own `SidePanel` role editor — both replace the old fixed-role `<select>`/badge, both call the same new `toggleProjectGroupRole(groupId, role, checked)` handler, and both share a client-side "last manager" hint (`groupManagerSourceCount`) with the identical `cannotRemoveLastManager` title text `ProjectMembersTable.tsx`'s own Role column already uses — the same shared-component reuse this repo's style guide requires rather than a third bespoke implementation.
- "New group" modal: role `<select>` removed entirely; `createProjectGroup` no longer sends `role`. A freshly created group's row/panel shows "No roles assigned" (`MultiSelectDropdown`'s own `emptyLabel`) until a role is granted.
- `frontend/src/components/UserAutocomplete.tsx`: new optional `projectGroups?: ProjectGroup[]`/`onSelectProjectGroup?: (groupId: string) => void` prop pair, following `groups`/`onSelectGroup`'s (PR5) exact pattern — client-side substring match against the passed-in array, rendered after org-group matches and before the external-invite option. **Visual distinction from an org-group match** (the plan's own explicit requirement, since the two are genuinely different concepts — a project group is local to one project, an org group is org-wide): `FolderKanban` (the same icon `Layout.tsx`'s nav rail already uses for "Projects," reused rather than inventing a fourth group-adjacent icon) instead of `Users`, and a "Project group" badge instead of "Group"/"Org group." The existing org-group badge text was tightened from the original bare "Group" to "Org group" at the same time — see "Judgment calls" below.
- `ProjectAdminPage.tsx`'s "Add member" modal: the same `UserAutocomplete` instance now also matches project groups (`projectGroups={memberTableGroups}`, the tab's own already-loaded unpaginated group list). Per the product owner's confirmed answer, picking a project group does **not** grant it a role (unlike picking a user or an org group) — it switches the same modal into a second step, "add someone to this group," reusing the existing `addGroupMember`/`addOrgGroupMember` handlers (`POST .../groups/{group_id}/members`, no new backend call) via a second, nested `UserAutocomplete` instance (itself without a `projectGroups` prop, so this can't recurse a second level deep) scoped to that one group. A "Back" control returns to the first step without adding anything.

### Tests

Rewrote `backend/tests/test_project_group_role_editing.py` in place (its old coverage was entirely `PATCH /groups/{group_id}`, an endpoint that no longer exists) — 21 tests: bare creation with an empty `roles` list (plus a defense check that a stray legacy `role` field in the payload is silently ignored, not erroring), grant/revoke with provenance (`kind="direct_group"`, `via_group_id`/`via_group_name`), idempotent grant, no-op revoke of a never-granted role, gating (403 below manage tier, 200 for an org admin with no project role), audit logging (grant/revoke, idempotent-grant-doesn't-double-log), **a group holding two roles producing two separate provenance entries** (the core new-behaviour test, plus a regression check that two *different* groups granting the same role — PR1's original case — still also produces two entries, unchanged), every C-U-08 guard site in its new per-role form (revoke-blocked-when-sole-manager, revoke-allowed-with-backup, delete-blocked/-allowed, and a group holding `project_manager` *alongside* another role still correctly guarded on delete), and the two pre-existing engagement-cleanup regression pins (nested-org-group/project-reference removal) carried over adapted to the new bare-then-grant creation shape, plus a new one for the revoke endpoint's own cleanup path.

New `backend/tests/test_project_group_role_migration.py` (3 tests, following `test_default_group_migration.py`'s exact downgrade/seed/upgrade-via-real-migration technique): a single group's role survives as exactly one grant row (plus its live effect on `effective-members` provenance, not just the stored row); all four `ProjectRole` values backfill correctly, including a zero-member group; the backfill's `ON CONFLICT` idempotency guard doesn't error or duplicate if re-run. Project/user setup deliberately happens *before* entering the downgraded-schema window in every test here (and, after the fix below, in `test_default_group_migration.py` too) — see Verify's SQL-join-bug finding's sibling issue: `create_project`'s manager-fallback needs the new table to exist, so it can't run while downgraded past this PR's own migration.

**Fixed as a direct consequence of this PR's schema change, not a new feature**: `backend/tests/test_default_group_migration.py`'s two existing tests broke the moment `_ensure_project_has_a_manager` started depending on `project_group_roles` — both downgrade to `0018`, which now also removes this PR's table, and both called `create_project` *inside* that downgraded window. Restructured to create the project/users first, matching the new tests' own ordering; one assertion (`reloaded["role"]`) updated to `reloaded["roles"] == [...]`.

**Sibling test files updated for the `ProjectGroupCreate.role` removal** — a mechanical but real consequence found by grepping every `POST .../groups` call site across the suite (pydantic silently drops an unknown `role` field rather than erroring, so these would have failed quietly downstream at whatever later assertion depended on the role actually being granted, not loudly at the call site itself): `test_api_lifecycles.py`, `test_audit_fixes.py`, `test_effective_members_provenance.py` (gained a small `_create_group_with_role` helper, used at all 9 of its own call sites), `test_org_group_nesting.py`, `test_org_group_project_roles.py`, `test_project_hierarchy.py`, `test_rbac.py`, `test_scim.py`, `test_security_hardening.py` — each either drops the now-inert `role` key (where the test's own assertions never depended on it) or adds the follow-up `POST .../groups/{id}/roles` call (where they do).

**Playwright**: `project-admin-groups-and-fields.spec.ts`'s "create a new project group" step rewritten for the simplified modal (asserts the role `<label>` is gone, the fresh row reads "No roles assigned," then grants a role via the new row `MultiSelectDropdown` and asserts the badge updates) and extended with a new step exercising the "Add member" autocomplete's project-group match end to end (types into the search, asserts the "Project group" badge distinguishing it from an "Org group" match, picks it, confirms the modal switches to its second "add to this group" step with no grant call fired, fills the nested autocomplete, and confirms "Back" returns to the first step cleanly); its throwaway API-created fixture group also had its now-inert `role` field dropped. `role-display-collapsing.spec.ts` (three project groups created purely to exercise its role-tier-collapsing assertions) and `project-admin-members.spec.ts` (one group whose granted role a later assertion directly depends on) both updated from single-call role-at-creation to the bare-then-grant two-call shape — the former via the new `MultiSelectDropdown` UI interaction (matching how a real user would now do this), the latter via the equivalent direct API call (already using API setup for its own fixture). `project-hierarchy.spec.ts`'s one throwaway project-reference group had its inert `role` field dropped, since that test never exercised the group's granted access. Per this PR's own scope, these specs were mechanically updated/written but not executed against the Docker Compose stack — the plan's own verification step runs the full spec suite once, after all planned work lands.

### Verified

Backend: the full set of new/rewritten test modules plus every sibling file this PR's schema change touched, run together against the live local test database — **174 tests, 0 failed** (`test_project_group_role_editing.py` 21, `test_project_group_role_migration.py` 3, `test_default_group_migration.py` 2, `test_org_group_project_roles.py` 18, `test_org_group_nesting.py` 7, `test_effective_members_provenance.py` 8, `test_project_hierarchy.py` 46, `test_rbac.py` 20, `test_audit_fixes.py` 15, `test_api_lifecycles.py` 10, `test_scim.py` 15, `test_security_hardening.py` 8, `test_schema_migrations_match_models.py` 1). `ruff check` on every changed backend file clean (two real findings fixed: an unused `group` variable left over from `remove_project_group_member`'s rewrite, an unused `UserProjectRole` import in the rewritten test module). `alembic heads` confirms a single head at `0022`, chained onto `0021`. Both `alembic upgrade head`/`downgrade 0021`/`upgrade head` round-tripped cleanly against the live database by hand before any test wrote it. Per this task's own instructions, the full backend suite was not run — that happens once, after all planned work lands.

Frontend: `npx tsc -b` clean. `npx eslint` clean on every changed file. Storybook, targeted (matching PR2/PR4/PR5's "new/edited files in isolation" scope), all four run together in one pass: `UserAutocomplete.stories.tsx` — **12 passed** (10 pre-existing + 2 new: `ProjectGroupMatchRendersAndSelects`, `OrgGroupAndProjectGroupMatchesCoexist`); `ProjectMembersTable.stories.tsx` — **13 passed** (unchanged, confirms `isDirectRoleKind`'s doc-only edit didn't disturb behaviour); `ProjectAdminPage.stories.tsx` — **42 passed** (including the rewritten `GroupsTabOpensSidePanelWithRoleSelectAndDelete`, the simplified `GroupsTabCreateGroupViaModal`, and two new stories: `GroupsTabGrantAndRevokeGroupRole`, `MembersTabAddMemberAutocompleteMatchesProjectGroup`); `OrgAdminPage.stories.tsx` — **56 passed** (unaffected by this PR beyond the shared "Org group" badge text — see Judgment calls). **123 passed total, 0 failed.** No Docker Compose/Playwright run in this PR, per its explicit scope.

### Judgment calls and deviations from the plan

- **Two real, previously-passing tests broken by this PR's own schema change, both fixed in the same pass rather than deferred**: `test_schema_migrations_match_models.py` failing against PR4's own pre-existing (not this PR's) index-declaration gap, and `test_default_group_migration.py`'s two tests breaking because `create_project` now needs a table this PR's migration adds while they're testing a *different, earlier* migration's downgrade window. Neither is this PR's own bug in the sense of "code this PR wrote is wrong" — both are real regressions this PR's change caused in code/tests it didn't otherwise need to touch — flagged explicitly here rather than silently fixed with no record, per this repo's "flag rather than silently diverge" rule, even though both are clearly correct to fix rather than leave broken.
- **`_direct_project_member_ids_base` and `_accessible_project_ids`'s `project_ids_via_group` were changed beyond what the plan's Step 3 literally asked for.** The plan named `_direct_project_role_holder_ids`, `_direct_project_member_ids_base`, and `_direct_project_roles_excluding_org_wide` as "the three sibling helpers PR4 already extended" needing the equivalent treatment — but `_direct_project_member_ids_base` never actually read `ProjectGroup.role` at all (Identify finding 2), so there was nothing analogous to "join through the grant table instead of a scalar" to do there. A first pass concluded no change was needed and documented that reasoning in the function's own docstring; a listing-visibility test written for an unrelated assertion failed and traced back to this exact function (and its `_accessible_project_ids` sibling, not named by the plan at all) genuinely needing a new `ProjectGroupRole` existence check that didn't exist as a concept before this PR's schema change — see Verify above. Reversed the first-pass decision and documented why in both functions' docstrings, rather than leaving the incorrect reasoning in place alongside a silent behavioural fix.
- **`ProjectGroupUpdate`/`PATCH /projects/{id}/groups/{group_id}` removed entirely rather than left as a deprecated stub.** The plan didn't explicitly say to remove the endpoint, only that `create_project_group` should drop `role` from its payload — but once `role` is the *only* thing a `ProjectGroup` had ever exposed for update (no rename endpoint exists, matching `OrgGroup`), keeping a `PATCH` with nothing left to patch would be dead code, not backward compatibility (no meaningful request body remains for it to accept). Removed outright, replaced by the two new grant/revoke endpoints.
- **The autocomplete's org-group badge text was changed from "Group" to "Org group"**, alongside adding "Project group" for the new match kind — not requested by the plan, but leaving the existing bare "Group" text next to a new "Project group" badge in the same dropdown would read as an unexplained inconsistency (why does one group concept get a generic label and the other a specific one) rather than the two being genuinely parallel, equally-specific concepts. Every Storybook/Playwright assertion checking for the literal string "Group" was updated to "Org group" (four call sites — `project-admin-groups-and-fields.spec.ts`, `UserAutocomplete.stories.tsx`, `ProjectAdminPage.stories.tsx`, `OrgAdminPage.stories.tsx`).
- **The "add someone to this project group" second step (Add-member modal) reuses a second, nested `UserAutocomplete` instance rather than inventing a new control.** The plan's own wording ("selecting an existing project group adds the picked user (or org group) as a member of it via the existing `add_project_group_member` endpoint") is compatible with more than one concrete UI shape; the two-step modal reuses the *exact* existing user-or-org-group matching/selection logic (just without the `projectGroups` prop, so it can't recurse into a third level) rather than building a bespoke second picker, keeping this PR's frontend surface to "compose existing pieces" rather than "add a fifth reimplementation of a search-and-pick control."
- **A pre-existing, unrelated documentation drift found and fixed in the same pass**: `docs/solution-architecture.md`'s "One diagram covering all N tables" sentence had been left at a stale "45" through at least the two most recent table additions (`org_group_project_roles`, `project_member_sources`'s own re-add) — both of which updated the table count/list further down the same page but missed this earlier mention. Fixed alongside this PR's own table-count update (48 → 49) rather than left as a second, separately-discovered gap.

### Files changed

`backend/app/models/project.py`, `backend/app/models/__init__.py`; `backend/alembic/versions/0022_project_group_roles.py` (new); `backend/app/schemas/project.py`; `backend/app/routers/projects.py`; `backend/app/services/rbac.py`; `backend/app/services/project_export.py`; `backend/app/services/templates.py`; `backend/scripts/seed_demo_data.py`; `backend/tests/test_project_group_role_editing.py` (rewritten); `backend/tests/test_project_group_role_migration.py` (new); `backend/tests/test_default_group_migration.py` (fix); `backend/tests/test_api_lifecycles.py`, `backend/tests/test_audit_fixes.py`, `backend/tests/test_effective_members_provenance.py`, `backend/tests/test_org_group_nesting.py`, `backend/tests/test_org_group_project_roles.py`, `backend/tests/test_project_hierarchy.py`, `backend/tests/test_rbac.py`, `backend/tests/test_scim.py`, `backend/tests/test_security_hardening.py` (sibling-file fixes); `frontend/src/api/types.ts`; `frontend/src/components/UserAutocomplete.tsx`, `frontend/src/components/UserAutocomplete.stories.tsx`; `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`; `frontend/src/pages/OrgAdminPage.stories.tsx`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/project-admin-groups-and-fields.spec.ts`, `tests/playwright/tests/e2e-workflows/project-admin-members.spec.ts`, `tests/playwright/tests/e2e-workflows/project-hierarchy.spec.ts`, `tests/playwright/tests/e2e-workflows/role-display-collapsing.spec.ts`; `docs/solution-architecture.md`, `docs/decisions.md` (this entry).

## Per-row action menus: per-user/per-group materialize, org-user removal, "Add to group" reachability fix (PR6 of the follow-up UX batch, 2026-09)

Sixth PR of the batch, held to last per the plan's own sequencing note ("PR6's access-mutating actions... get the same review rigor as F/PR4, so they're held to last, after the lower-risk UI work, and depend on PR4's grant mechanism existing to write into"). This entry covers a resumed pass: a previous session had already built the two backend materialize endpoints (`POST /{project_id}/materialize-inherited-access/{user_id}` and `.../group/{org_group_id}`, `backend/app/routers/projects.py`, plus `get_group_inherited_project_roles` in `backend/app/services/rbac.py`) before being interrupted — confirmed complete and importing cleanly before writing anything further, per this pass's own explicit verification step, rather than trusting the interruption summary. Everything else below (their tests, the frontend UI, the org-removal endpoint, the reachability fix, and the no-org-user robustness check) was built in this pass.

### Identify

Per the plan's own "Step 0" verification list, checked before writing any code rather than assumed:

1. `materialize_inherited_access_for_user`/`materialize_inherited_access_for_group` (`backend/app/routers/projects.py`) and `get_group_inherited_project_roles` (`backend/app/services/rbac.py`) exist, are complete (not truncated mid-function), import cleanly (`python -c "import app.routers.projects"`), and `get_group_inherited_project_roles` is correctly imported into `projects.py`. All confirmed.
2. Grepped `backend/tests/*.py` for any existing coverage of these two endpoints or their URL paths — none found.
3. Grepped `ProjectMembersTable.tsx` for `ActionMenu` — not present; the per-row actions column didn't exist yet.
4. Grepped `OrgAdminPage.tsx`'s Users table actions column — still two bare buttons (`Eye`/`Lock`/`Unlock`), not an `ActionMenu`.
5. Grepped the whole backend for an existing "remove user from org" endpoint. Found `DELETE /{organization_id}/membership` (`leave_organization`, self-service only — the caller removes their own membership), `POST /{organization_id}/users/{user_id}/deactivate` (sets `is_active=False` on the *whole account*, a cross-org lockout, not a per-org membership removal), and `POST .../archive` (hides an already-deactivated user from lists, doesn't touch membership rows). No admin-initiated removal of *another* user's org membership existed — this is genuinely new, not a rename of something already there.
6. `tests/playwright/tests/e2e-workflows/no-org-user.spec.ts` didn't exist.
7. Read `ActionMenu.tsx` (reused verbatim, no new kebab-menu component built), `ConfirmDialog.tsx` (the Tier-1 plain-confirm shape — no `requireTypedText`, matching `PreferencesPage.tsx`'s own "Leave organisation" confirm's severity, not the org-deletion Tier-2 shape), and `ProjectMembersTable.tsx`'s current `Row`/column structure (post-PR1/PR5: `kind: "member" | "invited"`, a `MultiSelectDropdown` Role column already computing a per-role "purely `direct_role`" check for its own disable logic).
8. `revoke_project_role` and PR4's `assign_group_project_role`/`revoke_group_project_role` as the precedent shape for the mutations the new UI calls into (idempotent grant, 204 on no-op revoke, `require_project_manage` gate).
9. `OrgAdminPage.tsx`'s `addGroupMember` handler (existing, unchanged, reused directly) and the Roles `MultiSelectDropdown`'s self-targeting guard (`u.user_id === user?.id`, mirroring the backend's `revoke_org_role`/`deactivate_org_user` self-targeting blocks) — the precedent shape for "Remove from {org}"'s own self-targeting guard below.

### Verify

- **The "last-admin guard" originally written for `remove_org_user` turned out to be unreachable dead code, caught before shipping it, not after.** A first draft mirrored `leave_organization`'s "the org must retain at least one admin" count check directly. Working through it against `remove_org_user`'s own preconditions — the caller must already hold `ORG_ADMIN` on this exact organisation (`require_org_role`), and self-targeting is blocked earlier in the same function — shows the caller is *necessarily* a second, distinct `ORG_ADMIN` still present after any removal they perform on someone else, for every `user_id` the endpoint can ever legally be called with. This is the identical "by construction" reasoning `revoke_org_role` already documents for itself in this codebase (not `leave_organization`'s reachable version, where caller and target are the same person) — the check could never fire through the live API, sequentially or under the row lock's protected concurrency, so shipping it would have been misleading dead code rather than real defense-in-depth. Removed the count-check branch, replaced it with a docstring bullet spelling out the "by construction" reasoning explicitly (mirroring `revoke_org_role`'s own), and pinned the reasoning with a test that exercises the real, reachable shape instead (`test_remove_org_user_cannot_zero_out_org_admins`: a co-admin removes the *other* admin, then confirms their own admin access survives) rather than a test asserting an impossible 409.
- **The sole-project-manager guard, by contrast, is real and reachable** — confirmed by the same reasoning, in the other direction: an org admin's own `ORG_ADMIN` status does not imply *any* project role (PR2's whole "org admin sees hierarchy without a role" premise), so the target being removed can genuinely be the sole real manager of a project the caller has no role on at all. Built as a close mirror of `leave_organization`'s own identical check, applied to the target instead of the caller — `test_remove_org_user_blocked_if_target_is_sole_project_manager` exercises this for real, not via DB manipulation.
- **"Remove all access" eligibility checked at the member-row level, not the per-role level.** The Role `MultiSelectDropdown`'s existing `purelyDirect` check is per-role (a member can hold one role that's purely direct and another that's group-sourced). Looping the per-role `DELETE` over only a member's *currently-held direct roles* while a non-direct role silently remains would leave real access in place under an action labelled "Remove all access" — a correctness bug had the eligibility check been copied at the wrong granularity. `ProjectMembersTable.tsx`'s new `allDirectRoleSourced` check requires *every* source entry on the row to be `direct_role`-kind before the action is offered at all, not just the specific role(s) being revoked.
- Read `docs/soc2/policies/access-control-policy.md` before writing `remove_org_user` (per the change-management policy's practice for anything access-mutating). Its Authorization items are unaffected in shape: the endpoint resolves everything from `organization_id`/`user_id` in the URL, gated by `require_org_role(ORG_ADMIN)` (already-established), no new scope introduced. No policy wording update needed.
- Checked the two per-target materialize endpoints' own idempotency/rank-selection logic directly against the bulk endpoint's `_ROLE_RANK` table (both endpoints literally redeclare the same dict inline, matching the bulk endpoint's own style) rather than assuming it was correct from the interrupted attempt's docstrings alone — `test_materialize_for_user_selects_highest_ranked_inherited_role`/`test_materialize_for_group_selects_highest_ranked_inherited_role` construct a user/group with two inherited roles at different ranks and assert only the higher one is written, pinning the selection logic empirically.

### Remediate

**Backend**
- `backend/app/routers/orgs.py`: new `DELETE /{organization_id}/users/{user_id}/membership` (`remove_org_user`), placed immediately before `deactivate_org_user`. Gated by `require_org_role(ORG_ADMIN)`. Self-targeting blocked (400, directs the caller to `leave_organization` instead). No separate last-admin count check (Verify above). Sole-project-manager guard mirroring `leave_organization`'s own. Cleanup mirrors `leave_organization`'s exact shape, applied to `user_id` instead of `current_user.id`: every `UserProjectRole`/`ProjectGroupMember` row across this org's projects, every `OrgGroupMember` row for this org's groups, subscriptions/favourites (`engagement.remove_subscriptions_and_favorites_for_projects`), then this org's own `UserOrgRole` rows last. Row-locks the organisation before the admin check and each project before its own manager check (`lock_organization_for_update`/`lock_project_for_update`), same ordering as `leave_organization`. 404 if `user_id` isn't currently a member. Audited (`entity_type="user_org_role"`, `action="removed_from_organization"`).

**Frontend**
- `frontend/src/components/ProjectMembersTable.tsx`: new Actions column (only rendered when the caller supplies `onRemoveAllAccess`/`onConvertToDirect`), one `ActionMenu` per member row. "Remove all access" offered only when `allDirectRoleSourced` (every source is `direct_role`-kind) — behind the table's own `ConfirmDialog` (Tier 1), which calls the caller's `onRemoveAllAccess(userId)` on confirm. "Convert inherited access to direct roles" offered only when the member has a `forward_inherited`/`member_source_inherited` source — calls `onConvertToDirect(userId)` directly (additive, no confirm step, matching the bulk button's own confirm-free treatment). Both props wired in `ProjectAdminPage.tsx` (`removeAllProjectMemberAccess`/`convertProjectMemberToDirect`) and `OrgAdminPage.tsx`'s "Manage users" modal (`removeAllManageUsersMemberAccess`/`convertManageUsersMemberToDirect`) — each loops the existing per-role `DELETE /roles/{user_id}/{role}` over the member's currently-held roles, or calls the new per-user materialize endpoint, then refreshes `effective-members` the same way `toggleProjectMemberRole` already does.
- New `frontend/src/components/AddToGroupControl.tsx`: a small `Popover`-based quick-pick (a `<select>` of the org's groups the user isn't already in, filtered client-side, plus Add/Cancel) reusing the caller's existing `addGroupMember(groupId, userId)` handler — no new backend call. Kept as its own small control next to (not inside) the Users table's `ActionMenu`, not because the style guide asks for that specifically, but because `ActionMenu`'s items are plain `onSelect` callbacks with no way to host a second component's own anchored `Popover` trigger — a real implementation constraint, not a style preference (see "Judgment calls" below).
- `frontend/src/pages/OrgAdminPage.tsx`: Users table Actions column consolidates the two previously-bare buttons (View access, lock/unlock display name) into one `ActionMenu`, adds "Remove from {org}" (withheld on the caller's own row — self-removal routes to the existing "Leave organisation" flow instead, matching the backend's self-targeting guard) behind a Tier-1 `ConfirmDialog` calling the new endpoint, and renders `AddToGroupControl` alongside it (reusing the page's own already-loaded `allGroups`, no refetch).
- `frontend/src/i18n/strings.ts`: new `membersTable`/`orgAdmin` entries for all of the above, following the existing per-namespace convention (functions for parameterised copy, `{project}`/`{org}` template placeholders left for the terminology-substitution layer).

### Tests

- `backend/tests/test_project_hierarchy.py` (per-user materialize, alongside the existing bulk-endpoint test): idempotency (no duplicate direct role or second audit event on a repeat call), correct highest-rank role selection across two differently-ranked inherited sources, scoped-to-one-user behaviour (a second inherited user on the same project is untouched), a no-op when the user has no inherited role at all, audit log detail (`target_user_id`, `created`), and 403 for a non-manager.
- `backend/tests/test_org_group_project_roles.py` (per-group materialize, using this file's own existing inheritance-cascade fixture style — an org group's direct grant on a parent/source project under `role_inheritance_mode`/`ProjectMemberSource`): the equivalent coverage — conversion via forward inheritance and via the member-source mechanism, idempotency, rank selection across two differently-ranked direct grants, no-op with nothing inherited, audit log detail, and `require_project_manage` gating.
- `backend/tests/test_rbac.py` (`remove_org_user`, next to the existing `leave_organization` coverage it mirrors): successful removal (access actually gone, audit logged, a second removal 404s), self-targeting blocked, the "cannot zero out admins" by-construction pin (Verify above), the sole-project-manager guard, 403 for a non-admin caller, 404 for a non-member target.
- Storybook: `ProjectMembersTable.stories.tsx` gains three states (`ActionsMenuOffersRemoveAllAccess`, `ActionsMenuOffersConvertToDirect`, `ActionsMenuHiddenWhenNoEligibleAction`). `AddToGroupControl.stories.tsx` (new): picking an available group, and the "hidden when already in every group" state. `OrgAdminPage.stories.tsx` gains three states (`UsersActionsMenuConsolidatesViewAndLock`, `UsersActionsMenuRemoveFromOrg`, `UsersActionsMenuAddToGroup`) and two pre-existing stories (`ViewUserAccessPanel`, `ViewUserAccessPanelExpandRoles`) updated to open "View access" through the new `ActionMenu` instead of a now-removed bare button.
- Playwright: new `tests/playwright/tests/e2e-workflows/no-org-user.spec.ts` (the plan's own "Additional test" note) — logs in as the existing `orphan` persona (`e2e-orphan@example.com`, seeded once as an Alpha member then removed via self-service leave, already reused read-only by `two-factor-auth.spec.ts`/`org-login-2fa-handoff.spec.ts`/`user-directory-and-bans.spec.ts` for the identical "zero org memberships" reason — reused rather than a fresh signup, since it's already the established fixture for exactly this state) and confirms `/orgs` shows the empty-directory state, the nav rail has no server-admin-only section, `/projects` and `/favourites` both show the ordinary "No projects to show." empty state, and no console/page error fires anywhere in the flow.

### Verified

Backend: `tests/test_project_hierarchy.py` — **52 passed**; `tests/test_org_group_project_roles.py` — **25 passed**; `tests/test_rbac.py` — **26 passed**. **103 passed, 0 failed**, run together. `python -c "import app.routers.orgs"`/`import app.routers.projects` clean.

Frontend: `npx tsc -b` clean throughout (checked after every file's edits). Storybook, targeted to the new/edited files (matching this batch's own established scope): `ProjectMembersTable.stories.tsx`, `AddToGroupControl.stories.tsx`, `OrgAdminPage.stories.tsx` run together — **79 passed, 0 failed** (this run is what caught `ViewUserAccessPanel`/`ViewUserAccessPanelExpandRoles` breaking against the new `ActionMenu`-consolidated actions column, fixed in the same pass rather than left for the full-suite run at the end of this batch).

Playwright: `no-org-user.spec.ts` run against the live Docker Compose stack (`backend`/`frontend` rebuilt first, per this repo's own "containers don't bind-mount source" gotcha; `seed_e2e_dataset.py` re-run, idempotent) — **1/1 passed**, run twice back-to-back with no reseed between runs to confirm the spec's own read-only idempotency, both green. No crash, hang, or console error found — the app shell already degrades correctly for a zero-org user, so nothing needed fixing as a result of this check (the "Additional test" note's own expected outcome when the rest of this session's work hasn't introduced a regression, not evidence the check was skipped).

### Judgment calls and deviations from the plan

- **The last-admin guard originally planned for `remove_org_user` was dropped in favour of a "by construction" docstring, rather than built as literally instructed.** The plan's Step 3 asked for "a last-admin guard mirroring the existing self-targeting-guard precedent... a org must always retain at least one admin, same shape as this codebase's other 'must retain at least one X' guards." Building it as asked, then verifying it before shipping (this repo's own change-management practice), showed it can never fire through the live endpoint — see Verify above. Flagged explicitly here rather than silently shipping either the dead check or a silent deviation from the plan's literal wording.
- **`AddToGroupControl` is a separate control next to `ActionMenu`, not a fourth item inside it.** The plan's wording ("a quick popover/small menu listing the org's groups") doesn't mandate it live inside the kebab specifically — but `ActionMenu`'s `items` are plain `{label, icon, onSelect: () => void}` rows with no way to host a second component's own anchored `Popover` trigger without changing `ActionMenu`'s own contract for every existing call site. A separate small icon-button-plus-`Popover` control, reusing `Popover` directly (the same component `ActionMenu` itself is built on), was judged the lower-risk shape — extending a shared component's contract for one caller's one-off need is exactly the kind of change this repo's style guide asks to be deliberate about, not a default reach.
- **No frontend UI renders a group row in an "inherited" state for `materialize_inherited_access_for_group` to be called from — matching what that endpoint's own docstring (written by the interrupted prior session) already documents, not a new gap found in this pass.** PR5's unified Groups directory (`ProjectAdminPage.tsx`'s `groupsTabRows`) only has `ProjectGroup` rows (direct by construction since PR7) and `ProjectMemberSource` virtual rows (no "inherited" state of their own) — no row kind currently represents an org group's own `OrgGroupProjectRole` provenance. The endpoint is built, tested directly via the API (per-group materialize coverage above), and documented as ready for a future UI to call into without any backend change, per the plan's own framing of this as the "highest-risk, most novel" piece worth having in place ahead of a UI that surfaces it.
- **`remove_org_user` was placed in `backend/app/routers/orgs.py` immediately before `deactivate_org_user`, not next to `leave_organization`.** Both orderings are defensible; this one groups the three "admin acts on another member's standing" endpoints (`remove_org_user`, `deactivate_org_user`, `archive_org_user`) together, with `leave_organization` (self-service) staying where it already was, immediately after `revoke_org_role`.

### Files changed

`backend/app/routers/orgs.py`; `backend/tests/test_rbac.py`, `backend/tests/test_project_hierarchy.py`, `backend/tests/test_org_group_project_roles.py`; `frontend/src/components/ProjectMembersTable.tsx`, `frontend/src/components/ProjectMembersTable.stories.tsx`; `frontend/src/components/AddToGroupControl.tsx` (new), `frontend/src/components/AddToGroupControl.stories.tsx` (new); `frontend/src/pages/ProjectAdminPage.tsx`; `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`; `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/no-org-user.spec.ts` (new); `docs/decisions.md` (this entry).

## Hardening pass: org-level pending-invite endpoints regressed the I-M-05 server-admin scoping invariant (2026-09-02)

Requested as a general hardening pass over the whole `feature/ui-improvement-stage-6` branch (everything since it diverged from `main`, ~15k lines across backend RBAC/org/project-group work and the frontend UX batches). Given the identify → verify → remediate rigor this codebase's security-sensitive reviews use (per the change-management policy): a scan sub-agent proposed candidates over the full diff, oriented via `graphify` and its own reading of `rbac.py`/`orgs.py`/`projects.py`, primed specifically to look for the tenant-scoping gaps the *original* Ossa (v1) hardening pass (see that entry above) found in this exact area (group/role management across orgs and projects) — every candidate it raised was then independently re-verified by direct code reading (not by trusting the sub-agent's own claim) before being treated as real.

### Identify

The scan surfaced one real issue, in the "Org-level invites" Phase A work (see that entry above, ~line 2725): `list_org_pending_invites`/`create_org_pending_invite`/`resend_org_pending_invite` (`backend/app/routers/orgs.py`) were gated with `require_org_admin_or_server_admin` — the dependency `services/rbac.py` and this log's own I-M-05 entry (~line 164) document as a single, narrow, deliberate carve-out used *only* by `create_org_user` (bootstrapping the first user of a brand-new org, a genuine chicken-and-egg case since no `org_admin` exists yet to do it themselves). The Phase A entry's own reasoning for reusing it — "the same dependency `create_org_user` uses (the only other endpoint that creates these rows)" — never re-derives *why* the server-admin bypass is still warranted once the bootstrap justification no longer applies: an already-existing org already has an admin by the time invites are reachable, so there's no equivalent chicken-and-egg problem here. The rest of this PR's RBAC-heavy surface (`assign_group_project_role`/`revoke_group_project_role`, the `OrgGroupProjectRole` endpoints) was spot-checked against the same original-hardening-pass pattern and found correctly tenant-scoped — this is an isolated regression, not a systemic one.

### Verify

- Confirmed directly by reading `services/rbac.py:1729-1749` (the dependency's docstring, unmodified by this branch, still asserting "used only by the create-org-user endpoint") and `docs/decisions.md`'s existing I-M-05 entry, then confirming the three new call sites in `orgs.py` do in fact use it (`grep -n require_org_admin_or_server_admin backend/app/routers/orgs.py`).
- Confirmed the concrete exploit is real, not theoretical: `require_org_admin_or_server_admin` returns early for any `current_user.is_server_admin`, with no check that the caller holds any role in the *target* `organization_id` at all — a server admin with zero standing in organisation X could list every outstanding invite in X (invitee email + inviter name), create a new one (seeding a `MEMBER`-role account into a customer org), or rotate/resend an existing one's token.
- Confirmed the "already reviewed and found adequate" note in `docs/soc2/policies/access-control-policy.md`'s known-gaps section (item 4, updated by this branch's own Phase A work) addresses a *different* question — whether resending trusts an unverified caller-typed email more than the original send did (no) — not whether the *gate itself* is scoped correctly. Not a documentation contradiction, just a different concern than this finding.
- Spot-checked the rest of this PR's group/role-management surface (`assign_group_project_role`/`revoke_group_project_role` in `projects.py`) directly: both correctly re-validate `org_group_id` against `project.organization_id` (mirroring `add_project_group_member`'s existing guard), and `revoke_group_project_role`'s `DELETE` is inherently scoped by its own `WHERE org_group_id = ... AND project_id = ...` regardless. No second instance of this pattern found.
- No existing test exercised (or pinned as intentional) a server-admin-with-no-org-role call against these three endpoints — `test_org_pending_invite_endpoints_are_gated_at_org_admin_tier` only covered a plain org member, one tier *below* org_admin, never the server-admin bypass itself.

### Remediate

- `backend/app/routers/orgs.py`: `list_org_pending_invites`, `create_org_pending_invite`, `resend_org_pending_invite` re-gated to `require_org_role(OrgRole.ORG_ADMIN)` — the same I-M-05-compliant pattern every other org-property endpoint in this file uses (`update_org_branding`, `list_org_users`, etc.). `require_org_admin_or_server_admin` itself is untouched and remains used only by `create_org_user`, so its existing docstring is accurate again with no edit needed. `list_org_pending_invites`'s own docstring rewritten to explain the fix and why the old gate was wrong, in place of the "gated the same tier as `create_org_user`" claim that motivated the bug.
- `backend/tests/test_org_pending_invites.py`: new regression test `test_org_pending_invite_endpoints_reject_a_server_admin_with_no_role_in_the_org` — creates a fresh org via `create_org_admin_in` (so the bootstrap server admin, `admin_token`, holds no `UserOrgRole` in it) and asserts 403 from the bootstrap server admin on all three endpoints. The existing `test_org_pending_invite_endpoints_are_gated_at_org_admin_tier` docstring updated to name the new gate.

### Tests

`pytest tests/test_org_pending_invites.py` — 10 passed (8 existing + 2 new/updated), against the live local test database. `pytest tests/test_security_hardening.py` — 8 passed, unaffected. `ruff check app/routers/orgs.py tests/test_org_pending_invites.py` — clean. `tests/test_invites_and_external_users.py` has 12 pre-existing failures in this environment, confirmed via `git stash` to fail identically without this fix's changes — an un-mocked `send_email` in those tests trying to reach a live SMTP endpoint not reachable from this shell, unrelated to this change (none of the files it touches were modified here).

### Verified

Manual code verification (not sub-agent-reported) of both the vulnerability and the fix: read `services/rbac.py`'s `require_org_admin_or_server_admin`/`require_org_role` definitions, all three affected endpoints in `orgs.py` before and after the edit, and the spot-checked `projects.py` group-role endpoints, before concluding this was the only real finding in the branch's RBAC-heavy surface.

### Files changed

`backend/app/routers/orgs.py`; `backend/tests/test_org_pending_invites.py`; `docs/decisions.md` (this entry).

### Deeper pass: `revoke_group_project_role` fabricated audit entries on a no-op

Requested to go deeper after the above — five parallel sub-agents (`projects.py`, `rbac.py`, the rest of the RBAC-adjacent backend surface, and, per the user's explicit go-ahead to look further back, the last two PRs merged to `main`: #17 "Feature/security uplift august 2026" and #16 "UI/UX Stage 5 changes," the PR that introduced generalized cross-project permissions) audited the branch's full backend RBAC-adjacent surface plus that recent history. Four came back clean after independent verification; one (`projects.py`) surfaced a real, if narrower, issue.

**Found**: `revoke_group_project_role` (`backend/app/routers/projects.py`, `DELETE /{project_id}/group-roles/{org_group_id}/{role}`) never validated `org_group_id` before use, and unconditionally called `log_event(..., action="revoked", ...)` plus ran the affected-user cleanup query even when the `DELETE` removed zero rows (a genuine no-op — the grant never existed, or `org_group_id` didn't exist/belonged to a different organisation entirely). Its sibling `revoke_project_group_role` (the `ProjectGroupRole`-scoped endpoint, added in the same PR7) already gets this right: `_get_group_in_project` 404s a foreign group up front, and `if removed.rowcount:` guards the log/cleanup — this endpoint just didn't get the same treatment. Verified by direct read (`backend/app/routers/projects.py:3137-3204` before the fix), not sub-agent-reported.

Impact assessed as Medium, not High: the `DELETE`'s own `WHERE project_id=... AND org_group_id=... AND role=...` clause meant no real cross-tenant `OrgGroupProjectRole` row could ever actually be removed this way (such a row can only be created by the already-validated `assign_group_project_role`), so this was never a privilege-escalation or data-disclosure path — but a project manager could fabricate audit-trail entries claiming a role was "revoked" from an arbitrary (including nonexistent, or foreign-org) group on their own project, which this codebase's audit-logging control (per the change-management/secure-development policy) depends on being trustworthy.

**Fixed**: `org_group_id` is now validated to exist and belong to the project's own organisation before anything else runs (404 otherwise, matching `_get_group_in_project`'s existing convention for the exact same shape of check), and the log/cleanup block is now gated on `removed.rowcount`, mirroring `revoke_project_group_role` exactly. Three new tests in `backend/tests/test_org_group_project_roles.py`: a no-op revoke writes no audit event, a foreign-org `org_group_id` 404s, and a nonexistent `org_group_id` 404s. `pytest tests/test_org_group_project_roles.py` — 28 passed (25 existing + 3 new). `ruff check` clean.

**Everything else**: `rbac.py`'s full diff (effective-role resolution for the new `OrgGroupProjectRole`/`ProjectGroupRole` mechanisms) — no findings, every new join correctly tenant-scoped. The rest of the branch's backend RBAC-adjacent surface (`orgs.py` remainder, `requirements.py`, `change_requests.py`, `invites.py`, `project_export.py`, `templates.py`, `stages.py`, `branding.py`, `changes.py`, `services/requirements.py`, `labels.py`, `system.py`, models/schemas, migrations 0018-0022) — no findings. PR #16 ("Generalized cross-project group permissions," `main` history) — re-verified independently against its own decisions.md entry's claims, no findings, same conclusion the original PR's own identify/verify reached.

**PR #17 ("Feature/security uplift august 2026," `main` history) — found and fixed one real regression, despite the PR's own name**: its ReDoS-fix rewrite of `backend/app/routers/scim.py::_parse_simple_eq_filter` (replacing a backtracking regex with plain string operations, to close a CodeQL `py/polynomial-redos` finding) silently dropped the original regex's `\s+` requirement between the `eq` token and the filter value's opening quote — `before = s[:start_quote].rstrip()` has nothing to strip when the quote immediately follows `eq`, so a filter like `userName eq"active"` (invalid per the SCIM grammar this module implements, rejected by the original `...\s+eq\s+"..."` regex) was silently accepted instead of 400ing. Verified with a live interpreter (not just reading the diff): confirmed the exact accept/reject divergence, and confirmed `attr` is still checked against a fixed allowlist (`username`/`emails.value`/the one supported bracket-filter shape) at every call site regardless — so this was never an attribute-allowlist bypass, an org-scoping bypass, or otherwise exploitable, just a parser accepting a narrower class of malformed input than it should. Worth fixing anyway (not deferring) both because it's auth-adjacent parsing code and because the PR's own `docs/decisions.md` entry and this function's own docstring both claim the rewrite "reproduces the same match/no-match semantics" as the original — a claim this proved false for this one case. Fixed by checking the raw (pre-`rstrip()`) character immediately before the opening quote is whitespace, before doing the existing rstripped "eq" token check — same O(n), no backtracking reintroduced. New test `test_parse_simple_eq_filter_requires_whitespace_between_eq_and_the_quote` in `backend/tests/test_scim.py`; `pytest tests/test_scim.py` — 16 passed (15 existing + 1 new). `ruff check` clean. Everything else in PR #17 (the `orgs.py::_looks_like_email` ReDoS fix, the `authlib`→`joserfc` OIDC migration's algorithm-confusion/expiry enforcement, and `storage_backends/local.py`'s path-traversal confinement check) held up under direct testing (differential fuzzing against the original regex, live exploit scripts against the path confinement, and reading the installed `joserfc` library's own source to confirm its algorithm allowlist is actually enforced) — no further findings.

Files changed (this addendum): `backend/app/routers/projects.py`; `backend/tests/test_org_group_project_roles.py`; `backend/app/routers/scim.py`; `backend/tests/test_scim.py`; `docs/decisions.md` (this entry).

## Server Management's Access Review row actions consolidated onto `ActionMenu` (2026-09-02)

Requested directly: the user noticed Server Management's Access Review table ("Server Management" → "Access review") still showed up to three separate, always-visible row buttons (Deactivate/Reactivate, Ban/Unban, Grant/Revoke server admin) side by side, the exact "two-or-more secondary actions competing for the same row" shape `docs/ux-style-guide.md`'s "Pattern: action menu" exists to collapse — already applied to `OrgAdminPage.tsx`'s own Users table (PR6 of the follow-up UX batch, see the "Per-row action menus" entry above) but never carried over to this page's equivalent table.

**Fix**: `ServerManagementPage.tsx`'s `AccessReviewTab` actions column now renders one `ActionMenu` per row (`triggerLabel={strings.system.usersActionsFor(u.display_name)}`) with three items — Deactivate/Reactivate (`UserX`/`UserCheck`), Ban/Unban (`Ban`), Grant/Revoke server admin (`ShieldPlus`/`ShieldMinus`) — each swapped based on the row's current state, same as the buttons they replace. Status badges (Banned/Deactivated/Server admin) stay outside the menu, visible at a glance, matching the existing badge-plus-actions layout. New `strings.system.usersActionsFor` mirrors `strings.orgAdmin.usersActionsFor` verbatim. No backend change — this only touches how the three existing mutating endpoints are triggered from the UI.

**Tests**: `ServerManagementPage.stories.tsx`'s four affected stories updated to open the kebab (`"{name}'s actions"`) and assert/act on the `role="menuitem"` inside the resulting `role="menu"`, rather than a bare button — same pattern `OrgAdminPage.stories.tsx` already established for its own `ActionMenu` call site. `AccessReviewBannedAndAdminBadges` also gives its two `systemUser()` fixtures distinct `display_name`s (previously both defaulted to "Orphan User"), since the kebab's accessible name is now derived from it and two identically-named triggers on one page would be ambiguous to query. `user-directory-and-bans.spec.ts` (Playwright) reworked the same way — a local `openOrphanActionsMenu()` helper opens the row's kebab and waits for the portalled `Popover` menu to be visible before returning it (same "menu hasn't finished positioning yet" race already documented in `org-rename-and-test-email.spec.ts`/`org-merge-import.spec.ts`), called before each of the four state-changing steps since the menu closes itself on every selection.

**Verification**: `npx tsc --noEmit` clean; `npx eslint src/pages/ServerManagementPage.tsx src/pages/ServerManagementPage.stories.tsx src/i18n/strings.ts` clean. Storybook, targeted: `ServerManagementPage.stories.tsx` — 18/18 passing. Against the real dev/test Docker Compose stack (rebuilt via `docker compose up --build -d`, per this repo's own "services don't bind-mount source" note): `user-directory-and-bans.spec.ts` (the deactivate/reactivate/ban/unban cycle through the new menu) and `server-admin-scope.spec.ts` both passed; stack torn down afterward.

### Files changed

`frontend/src/pages/ServerManagementPage.tsx`, `frontend/src/pages/ServerManagementPage.stories.tsx`, `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/user-directory-and-bans.spec.ts`; `docs/decisions.md` (this entry).

## Compliance Module + Modular Feature System: implementation plan authored (2026-09-04)

Requested directly: a multi-session implementation plan for the new Compliance Module (`docs/Compliance_Module_Requirements.md`, 33 sections), explicitly framed as the first of four planned major uplifts (Compliance, a Decisions Record system, Pain Points collection, a better Traceability system) and required to support sessions starting and stopping with context cleared in between. Planning only — no code, migrations, or schema changes in this session.

**Why this is more than "one feature plus a toggle":** the user's own framing — four uplifts, a desire for org-level enable/disable, and a stated future interest in contributor-built custom modules — meant the real deliverable was a generic modular-feature-system architecture that Compliance is the first (and reference) implementation of, not a one-off. The design went through four rounds of correction (recorded in full inside the plan doc's "Design history" section so a future session doesn't relitigate them or simplify the design back toward an already-rejected version):
1. A simple per-org boolean + static registry was rejected as too limited.
2. Two-tier server-entitlement/org-enablement gating plus first-party module packages and Python entry-point backend plugin discovery was rejected on three points: the new server-level role needed to reuse this app's *existing* role-display UI rather than a bespoke section; third-party UI needed to be feasible, not just documented as a future gap; and modules needed to be able to register their own RBAC roles rather than Compliance getting one-off `OrgRole`/`ProjectRole` enum additions.
3. Adding `ServerRole`/`UserServerRole`, a generic module-role mechanism, and an iframe-plus-narrow-postMessage-contract third-party UI design was rejected on one further point: third-party modules must be able to use the app's *actual* shared components (Toast, modals), not a thin isolated message contract.
4. The approved design resolves "isolate untrusted code" against "let it use real shared components" with a two-tier frontend model: Tier A (installed at build time — first-party or a third-party npm package — with direct imports of every real shared component, the tier Compliance itself uses) as the primary path, and Tier B (a remote iframe with a *Host UI Bridge* that renders real host `Toast`/`ConfirmDialog` via RPC rather than reimplementing them) for the genuinely-dynamic, not-installed case.

**The approved plan** is `docs/compliance-module-plan.md`, committed to the repo (not this log, and not the ephemeral Claude Code plan-mode file, which isn't visible to a future session) specifically so a cleared-context session can resume cold. It has a "Status / Resume Here" table at the top (0 of 15 phases complete as of this entry) and a "How to Resume a Session" procedure. Phases 0–3 build the module-system infrastructure (server-level RBAC extension via a new `ServerRole`/`UserServerRole` mirroring the existing `OrgRole`/`UserOrgRole` pattern; two-tier entitlement/enablement tables plus `backend/app/modules/` plugin loading via static registration and `importlib.metadata` entry points; a generic module-contributed-RBAC mechanism Compliance uses instead of core enum edits; and the two-tier frontend integration described above). Phases 4–14 build Compliance itself as the reference implementation, each phase cited against the specific `Compliance_Module_Requirements.md` section(s) it satisfies.

**Deliberately out of scope, documented rather than silently dropped** (per this repo's standing "fix or flag, don't silently defer" rule — these are flagged, not skipped): sandboxing/security review of arbitrary third-party *code execution* (the trust boundary is "was deliberately pip-installed/npm-installed into this deployment," the same boundary any language's plugin ecosystem relies on); automatic third-party database migrations (arbitrary third-party SQL against production is its own security-review-shaped question; only first-party modules get real Alembic migrations); and module-role inheritance through org/project groups or project hierarchy (V1 module roles are direct grants only — the core RBAC engine's existing inheritance resolution for `OrgRole`/`ProjectRole` is substantial and extending it generically to module roles deserves its own dedicated review).

**Reused mechanisms confirmed by exploration**, not duplicated: the `DirectoryTable`/`MultiSelectDropdown` combination already used by `OrgAdminPage.tsx`'s Users table (every new role-display surface, server or module, must render through this, not a bespoke table); the `Organization.accent_color_hex`-falls-back-to-`ServerSettings` override shape (reused for module entitlement); `services/pats.py`'s scoping model (reused conceptually for the Tier B iframe's short-lived scoped token); the `Requirement`/`RequirementVersion` identity-plus-history shape (reused for Compliance Standard versions); the `FileAsset` + join-table-per-owner-type attachment convention (reused for Evidence); and the existing audit/notification/scheduler/bundle-export services, all cited with file paths inside the plan doc.

### Addendum: module-contributed MCP server tools (2026-09-04, same day)

Identified after the plan above was approved — not a correction, a genuinely new requirement noticed once the module-system shape was settled: modules should also be able to register their own `mcp-server/` tools, not just REST endpoints, RBAC roles, and frontend UI. Added as a new Phase 4 (renumbering the Compliance-specific phases from 4–14 to 5–15; total now 16 phases, 0–15).

`mcp-server/server.py` already has an established, principled pattern worth extending rather than duplicating: every tool is an `@mcp.tool`-decorated function calling a shared `_call_backend` helper that forwards the caller's own bearer token (the server holds no credentials and performs no authorization of its own — `docs/mcp-server.md`'s "Authentication model"); write tools exist only when `MCP_WRITES_ENABLED=true`; and, critically, no tool in either mode can approve, decide, or otherwise change workflow state — a boundary enforced by never exposing the capability as a tool, not by relying on RBAC alone.

Phase 4's design: `ModuleDefinition.mcp_tools` lets a module declare **declarative, single-REST-call tool mappings** (name, HTTP method, path template, param mapping, a `mutates` flag, an `is_approval_action` flag). A new `GET /api/v1/system/modules/mcp-tools` backend endpoint merges these across all implemented modules and **hard-excludes any `is_approval_action: true` entry server-side** — mirroring the existing approval-exclusion principle, but enforced in the manifest builder itself rather than left as an honour-system flag a module could get wrong. `mcp-server` fetches this manifest once at its own startup and registers each entry as a generic proxy tool via FastMCP's programmatic registration API, reusing the existing `_call_backend` helper verbatim; mutating entries only register when `MCP_WRITES_ENABLED` is true, same as today's hand-written write tools.

Two deliberate scope boundaries, flagged rather than silently built, consistent with the rest of this plan: only declarative single-REST-call tools are supported in V1 — genuinely custom tool logic would mean running third-party code inside a process that handles real user bearer tokens, the same trust question already deferred for the backend's own plugin loading; and because the manifest is fetched once at process startup, picking up a new or changed module's tools requires restarting the `mcp-server` container, not a hot-reload — confirmed acceptable by the user, including that this may need its own explicit stage in the deployment pipeline if the mechanism requires it.

Compliance itself (Phases 6–9, renumbered) will declare seven read-only tools (`compliance_list_standards`, `compliance_get_standard_version`, `compliance_list_requirements`, `compliance_get_project_status`, `compliance_list_non_compliant_requirements`, `compliance_list_expiring_evidence`, `compliance_list_pending_approvals`) and explicitly no tool for the approval/sign-off action itself, per the design above.

### Addendum 2: MCP-tools packaging, and a self-found hardening pass on the manifest design (2026-09-04, same day)

Two follow-up questions from the user on the addendum above, both resolved by updating the plan rather than the (still unwritten) code.

**Packaging**: the user asked whether `mcp-server` needs combining into the same container/image as the backend, reasoning that otherwise they didn't see how modules would deploy cleanly via Docker. Investigation found `mcp-server` is currently published as its own independently-deployable image (`ghcr.io/.../reqtrackmanager-mcp-server`) with a documented standalone/remote-client deployment mode (reverse-proxied separately, for clients like Copilot Studio that need a URL independent of the backend's own). A literal same-process merge was offered as an option but would have retired that documented deployment mode. Resolved by re-examining Phase 4's actual mechanism: because module-contributed MCP tools are purely declarative (a JSON manifest fetched over HTTP, proxied through the existing `_call_backend` helper), `mcp-server` never needs a module's Python package installed into its own image at all — the packaging concern doesn't actually apply to this design. `mcp-server/Dockerfile` and its separate image are unchanged; this is revisited only if the already-deferred "custom (non-declarative) MCP tool logic" capability is ever built.

**Security review of the manifest mechanism itself**: the user asked directly whether the dynamic JSON manifest becomes an attack surface. It would have, as originally designed — a self-review found three real gaps and fixed all three in the design before any code was written, per this repo's standing "fix, don't defer" practice:
1. `mutates` and `is_approval_action` were designed as fields a module declares on its own manifest entry — a malicious or buggy module could simply lie (`is_approval_action: false` on a tool that actually performs an approval), defeating the intended protection entirely. Fixed: `mutates` is now derived mechanically from the HTTP method (nothing to misdeclare); `is_approval_action` is now sourced from the real backend route's own metadata (set by that endpoint's own author, reviewed the same way the endpoint is) rather than the module's manifest entry.
2. A declared `path_template` had no constraint tying it to the declaring module's own endpoints — a module could point a tool at any backend path at all, including ones it doesn't own. Fixed: the manifest builder now rejects and logs any entry whose path falls outside that module's own registered router prefix, mechanically enforced rather than trusted from self-declaration.
3. The manifest endpoint was designed to be fetched once at `mcp-server`'s process boot, before any specific user's token exists — meaning either an unauthenticated endpoint (new reconnaissance surface) or an undesigned new service credential. Fixed: the endpoint requires the same bearer-token authentication as every other backend endpoint; `mcp-server` fetches/refreshes it lazily and authenticated, using whichever session's own token first triggers a refresh within a TTL window, cached in-process — never an unauthenticated call.

Documented explicitly as a bounded guarantee, not overclaimed: these fixes are mechanically strong for tools resolving to core/first-party endpoints (we control and review both the endpoint and its metadata). For a third-party module's own endpoints, the guarantee reduces to the same "was deliberately installed" trust boundary Phase 1 already established — which is why Phase 1 was also updated in the same pass to gate third-party module discovery (`importlib.metadata` entry points, `EXTRA_MODULES_PATH`) behind a new deployment-level flag, `ALLOW_EXTERNAL_MODULES`, default `false`, mirroring `MCP_WRITES_ENABLED`'s existing off-by-default precedent in this codebase. First-party `INSTALLED_MODULES` are unaffected.

**SOC2 planning**: the user separately asked for explicit SOC2 planning across this whole module-system effort, not just a generic per-phase reminder. Read all three directly relevant policy docs in full (`vendor-and-subprocessor-management-policy.md`, `change-management-and-secure-development-policy.md`, `access-control-policy.md`) plus the control matrix (`trust-services-criteria-mapping.md`) rather than assuming their content. Found that `trust-services-criteria-mapping.md`'s CC6.8 ("prevents or detects and acts upon unauthorized/malicious software") is already an open 🔴 gap — no automated dependency/container vulnerability scanning exists — and that this plan's plugin-loading mechanism materially raises the stakes of that already-open gap, which is the concrete reasoning behind `ALLOW_EXTERNAL_MODULES` defaulting off. Added a new "SOC2 / Security Planning" section to `docs/compliance-module-plan.md` committing Phases 0–2 and Phase 1 to concrete policy-doc edits (a new `access-control-policy.md` subsection documenting the two new RBAC scopes this plan adds — server module role, module-contributed role — including that module roles explicitly don't participate in the existing group/hierarchy inheritance the policy's role-resolution diagram shows for core roles; a new `vendor-and-subprocessor-management-policy.md` clause for installed third-party modules, distinct from a classic subprocessor because it's code running inside the deployment's own trust boundary rather than an external data-processing relationship) rather than leaving "consult the policy" as a vague per-phase reminder.

### Addendum 3: org/project scoping for module MCP tools (2026-09-04, same day)

The user asked two direct questions the plan hadn't actually answered: how does an `mcp-server` module tool behave when the module is disabled for a specific organisation, and how does it behave for a user who belongs to multiple organisations where only some have the module enabled?

The first turned out to need no new design at all: every module tool call names a specific `project_id` or `organization_id`, and the real backend endpoint it proxies to is already gated by `require_module_enabled` for that one resource, 404ing exactly like any other not-found/no-access case `_call_backend` already translates into a clear tool error — the same per-call model every existing tool already relies on.

The second exposed a genuine, previously-unresolved gap: it was never decided whether an org-level module tool (one not scoped to a single project, e.g. listing an organisation's compliance standards) takes an implicit "current org," tries to aggregate across every org the caller belongs to, or requires an explicit org. Checked this app's own existing REST convention directly (`backend/app/routers/orgs.py`'s report-template endpoints, `POST/GET/PUT/DELETE /{organization_id}/report-templates`) rather than guessing — every org-scoped resource in this codebase is addressed as `/{organization_id}/...`, never org-less or aggregate. Resolved by requiring `organization_id` (or `project_id`, for project-scoped tools) as a mandatory parameter on every module tool, mirroring both that REST convention and the existing hand-written tools' own shape (`get_project(project_id)`, `list_requirements(project_id)` always name one specific resource). This also fixed a related error in the addendum's own earlier example: a `path_template` of `/api/v1/modules/compliance/standards/{standard_id}` (no organisation in the path) was inconsistent with the app's actual convention and has been corrected to `/api/v1/orgs/{organization_id}/modules/compliance/standards/{standard_id}`.

Consequence: a user in Org A (module enabled) and Org B (module disabled) calls the same tool with `organization_id=A` and `organization_id=B` and gets correct, independent results from each — no special multi-org logic needed anywhere, since access is always resolved against the one org/project actually named in a given call. An AI wanting to enumerate across a caller's orgs composes this itself (call the existing `list_organizations()` tool, then call module tools per org of interest) rather than `mcp-server` or the manifest mechanism trying to do that aggregation — consistent with V1's declarative-single-REST-call scope boundary, and with how MCP tool composition normally works (client-orchestrated, not server-side). The manifest itself remains deliberately unpersonalized: a module's tools are listed for every session regardless of whether any of that caller's orgs actually have the module enabled, exactly like `list_projects` is listed today for a caller with zero project access anywhere.

`docs/compliance-module-plan.md`'s Phase 4 gained an explicit "Org/project scoping" subsection recording this, and every compliance tool declared in Phases 6–9 now shows its required `organization_id`/`project_id` parameter explicitly rather than leaving it implicit.

### Files changed

`docs/compliance-module-plan.md` (updated: packaging clarified, Phase 1/4 hardened, new "SOC2 / Security Planning" section added, org/project scoping fixed); `docs/decisions.md` (this entry).

## Compliance module plan, Phase 0: server-level RBAC extension (2026-09-04)

First implementation phase of `docs/compliance-module-plan.md` — the module system's foundational RBAC extension: a new `ServerRole` enum, a `UserServerRole` grant table, a `require_server_role` dependency, and `ServerSettings.default_module_entitlement_policy`, plus the frontend role-display consolidation the plan's design history flagged as its single most-corrected point.

**Backend.** `ServerRole` (`app.models.enums`): `SERVER_ADMIN`, `MODULE_ADMINISTRATOR`. `User.is_server_admin` is untouched, exactly as the plan specified — `SERVER_ADMIN` exists in the enum for composition/reference only and is never written as a `UserServerRole` row; the new grant endpoint (`POST /system/users/{id}/server-roles`) rejects a `role: "server_admin"` payload with 400, so there is no path to a second, driftable source of truth for that tier. Only `MODULE_ADMINISTRATOR` is ever actually granted. `UserServerRole` lives in a new `app/models/server_role.py` module rather than being folded into `models/organization.py` (where `UserOrgRole` lives) — this is genuinely server-wide infrastructure, not an organisation concept, so it gets its own file rather than borrowing org's. Migration `0023_server_roles.py` adds the `server_roles` table and `server_settings.default_module_entitlement_policy` (`ModuleEntitlementPolicy`: `open`/`closed`, default `open`) in one pass, no backfill (additive only, per the plan).

`services/rbac.py::require_server_role(*allowed: ServerRole)` mirrors `require_org_role`'s shape and composes with `is_server_admin` exactly as specified: a genuine server admin always passes regardless of which roles were requested, the same "higher tier implies narrower ones" principle `require_org_role` callers already rely on for `ORG_ADMIN` — and the one Phase 2's `require_module_role` will extend to module-contributed roles. No PAT carve-out was added (unlike `require_server_admin`'s explicit PAT rejection) since nothing reachable through this dependency yet accepts a PAT at all; flagged in the dependency's own docstring to revisit if that changes.

New endpoints in `routers/system.py`: `POST`/`DELETE /system/users/{id}/server-roles[/{role}]` (grant/revoke, `require_server_admin`-only — a narrower role can never grant itself or others a role, mirroring `assign_org_role`'s own `ORG_ADMIN`-only gating for the same reason) and `GET`/`PUT /system/module-entitlement-policy` (`require_server_role(MODULE_ADMINISTRATOR)` — the one settings surface this phase actually gates with the new role rather than requiring full server-admin). `SystemUserOut` gained `is_module_administrator`. Both new mutation paths audit-log via `services/audit.py::log_event` (`entity_type="user_server_role"` / `"system"`), per this repo's standing SOC 2 logging requirement.

**Frontend.** `ServerManagementPage.tsx`'s `AccessReviewTab` roster gained a "Server roles" `MultiSelectDropdown` column (options: Server admin, Module administrator), replacing the old grant/revoke `ActionMenu` items — the exact `DirectoryTable` + `MultiSelectDropdown` combination the plan's design history calls out as the single most-corrected point across four planning rounds, now applied to server roles the same way `OrgAdminPage.tsx`'s Users table already applies it to org roles. One deliberate deviation from that precedent, called out in the plan itself: unlike Org Admin's roles column (which toggles directly, no confirmation), toggling either checkbox here still goes through the existing Tier-1 `ConfirmDialog` this tab already used for grant/revoke server admin — granting cross-tenant power warrants the extra step even though granting an org role doesn't. `SERVER_ROLE_LABEL` added to `api/types.ts` alongside the existing `ORG_ROLE_LABEL`/`PROJECT_ROLE_LABEL` maps, per the style guide's label-map rule.

**Incidental fixes found while touching this code (fixed now, not deferred, per this repo's standing rule):**
1. The old `ActionMenu`'s three items (deactivate/reactivate, ban/unban, grant/revoke server admin) were all wrapped in one `{!u.has_org_membership && (...)}` guard — meaning grant/revoke server admin was only ever reachable through this UI for org-less accounts, contradicting this feature's own prior decisions-log entry ("Per-row actions adapt to what's actually valid for that user: Deactivate/Reactivate for org-less accounts, Grant/Revoke server admin for everyone"). Moving grant/revoke into the new column (rendered unconditionally for every row) fixes this as a side effect; the remaining `ActionMenu` (deactivate/ban only) keeps the org-less-only gate, which is correct for those two.
2. The old standalone "Server admin" badge became redundant once the new dropdown's own closed-state summary text started showing the same fact for a checked role — removed rather than left duplicating (a Storybook interaction test caught the resulting ambiguous-text failure immediately, which is what surfaced this).

**Deliberately not built this phase**: no frontend surface for `default_module_entitlement_policy` — the plan's own Phase 0 spec only calls for the Server Roles column, and Phase 1's "Modules" admin UI (which shows entitlement/enablement together) is the natural, non-redundant home for a control over this setting; the backend endpoint exists and is tested via the API now so Phase 1 has something to build against.

**Tests**: `backend/tests/test_module_system_rbac.py` (new) — grant/revoke round-trip, idempotent re-grant, no-op revoke of an ungranted role, `SERVER_ADMIN` rejected via the grant endpoint, org-admin cannot grant (403), a `MODULE_ADMINISTRATOR` cannot grant to others (403, escalation-safe), entitlement-policy read/write via `MODULE_ADMINISTRATOR` alone, `is_server_admin` composition with no explicit grant, plain member and org-admin-alone both rejected (403) on the policy endpoints. `ServerManagementPage.stories.tsx` updated (existing grant/revoke stories rewritten for the new dropdown flow, `AccessReviewGrantModuleAdministrator` added) rather than left broken — a `getByRole` casing mismatch during the rewrite (`SERVER_ROLE_LABEL` values are sentence-cased, so the generated accessible name is "Grant Server admin to X", matching `OrgAdminPage`'s own equivalent) and the badge-duplication failure above were both caught by actually running `vitest --project=storybook`, not just `tsc`. Full backend pytest suite and full frontend Storybook interaction suite (631 tests) run clean; `ruff check`, `tsc -b`, and `eslint` all clean on every changed file.

**A first full-suite run caught one real drift bug this phase introduced**: `UserServerRole.user_id` had `index=True` omitted from the model even though migration `0023` explicitly creates `ix_server_roles_user_id` on it (copying `ProjectGroupRole`'s pattern for its own FK index but missing the model-side declaration that has to accompany it) — `test_schema_migrations_match_models.py::test_models_and_migrations_have_not_drifted` caught the mismatch immediately. Fixed by adding `index=True` to the model column; re-ran the full suite to confirm the drift test now passes. The same full run's remaining 14 failures (`test_invites_and_external_users.py`, `test_oidc_provisioning.py`, `test_org_export_import.py`) are pre-existing and environmental, not caused by this phase: all are `aiosmtplib.errors.SMTPConnectError: Error connecting to mailhog` — the invite-email flow's SMTP host only resolves inside the `tests/container` Docker network, not from a pytest run against its exposed Postgres port from the host directly. Confirmed via `git stash` (this phase's changes stashed) that one of these same tests fails identically on unmodified code — not a regression.

### Files changed

`backend/app/models/enums.py`, `backend/app/models/server_role.py` (new), `backend/app/models/organization.py`, `backend/app/models/__init__.py`, `backend/alembic/versions/0023_server_roles.py` (new), `backend/app/services/rbac.py`, `backend/app/routers/system.py`, `backend/tests/test_module_system_rbac.py` (new); `frontend/src/api/types.ts`, `frontend/src/pages/ServerManagementPage.tsx`, `frontend/src/pages/ServerManagementPage.stories.tsx`, `frontend/src/i18n/strings.ts`; `docs/soc2/policies/access-control-policy.md` (new Authorization subsection, per the plan's SOC2 planning commitment); `docs/compliance-module-plan.md` (Phase 0 marked complete); `docs/decisions.md` (this entry).

## Compliance module plan, Phase 1: two-tier module gating + backend plugin loading (2026-09-04)

Second implementation phase of `docs/compliance-module-plan.md`, built directly on Phase 0's `ServerRole`/`UserServerRole`/`default_module_entitlement_policy`: the module registry (`backend/app/modules/registry.py`), its two new gating tables (server-tier entitlement, org-tier enablement), the `require_org_module_enabled`/`require_project_module_enabled` RBAC dependencies, org-admin and server-tier management endpoints, and the `app.main` mount loop that will host every future module's router. No first-party module exists yet — this phase is pure infrastructure Phase 5 (Compliance) builds the first real module on top of.

**Backend.** Two new explicit-override-only tables, both unique on `(organization_id, module_key)`: `organization_module_entitlements` (`app.models.module.OrganizationModuleEntitlement`, `entitled: bool`) is the server-tier licensing lever; `organization_modules` (`app.models.module.OrganizationModuleEnablement`, `enabled: bool`) is the org-tier day-to-day switch. Migration `0024_module_registry.py` follows `0023`'s exact raw-SQL, `IF NOT EXISTS`-everywhere style, adding both tables plus their `organization_id` indexes, no backfill (additive only). `Settings` gained `allow_external_modules: bool = False` and `extra_modules_path: str | None = None` (`backend/app/config.py`), documented at the same depth as `oidc_allow_private_network_targets`.

`backend/app/modules/registry.py` (new package) is the merged-registry single source of truth: `ModuleDefinition` (frozen dataclass: `key, name, description, version, default_enabled, implemented, get_router`, plus `roles`/`frontend_manifest`/`mcp_tools` placeholders for Phases 2–4 so the shape won't need a breaking change later); `INSTALLED_MODULES` (static, empty until Phase 5); `_discover_entry_point_modules`/`_discover_path_modules` (third-party sources, per-entry try/except-and-log, never abort the whole discovery); `build_registry(force=)` (process-cached, merges all three sources, installed always wins on a key collision, logs every loaded module at `INFO` with key/version/source); `get_module_registry`/`get_module`; `is_module_entitled`/`is_module_enabled` (the exact "entitled AND (enablement row if present else registry default)" formula from the plan, tolerating a missing `ServerSettings` row the same way `services/branding.py` does). Critically, when `Settings.allow_external_modules` is `False`, `build_registry` does not call either discovery function at all — verified by a test that monkeypatches both to raise and confirms `build_registry(force=True)` doesn't.

`services/rbac.py` gained `require_org_module_enabled(module_key)`/`require_project_module_enabled(module_key)`, structural siblings of `require_org_role`/`require_project_view` (same `check_pat_scope`/`_require_org_active`/`_require_org_2fa` preamble), but returning **404, not 403**, when the module is disabled or the org isn't entitled — matching the plan's explicit requirement that disabled functionality must be indistinguishable from functionality that doesn't exist. `rbac.py` importing `app.modules.registry` (not the reverse) keeps the import graph acyclic, confirmed by a plain `python -c "import app.main"` and the full test suite.

New endpoints: `GET`/`PUT /orgs/{id}/modules[/{module_key}]` (`routers/orgs.py`, `require_org_role(ORG_ADMIN)`, schemas `OrgModuleOut`/`OrgModuleEnablementUpdate` in `schemas/org.py`) — the GET includes non-entitled modules rather than filtering them (the frontend greys them out); the PUT 403s with an explicit message if the org isn't entitled, upserts `OrganizationModuleEnablement`, and audit-logs as `module.enablement_updated`. `GET /system/modules`, `GET`/`PUT /system/orgs/{id}/module-entitlements[/{module_key}]` (`routers/system.py`, `require_server_role(MODULE_ADMINISTRATOR)`, inline `BaseModel` schemas matching that file's own convention) — the PUT upserts `OrganizationModuleEntitlement` and audit-logs as `module.entitlement_updated`; a code comment at the entitlement-revoke path explains why no cascade-delete of a stale enablement row is needed (the AND in `is_module_enabled` already makes it inert). `backend/app/main.py` now builds the registry and mounts every module's `get_router()` in a loop after the existing router includes — mounts nothing today, by design.

**Frontend.** `OrgAdminPage.tsx` gained a new top-level "Modules" resource-menu group: `OrgModule[]` state fetched inside the same try/catch-403-and-hide-section block `advanced`/`sso`/`scim` already use, a `toggleModuleEnabled` function following the immediate-PUT-then-patch-local-state shape `grantOrgRole`/`revokeOrgRole` already established (not the buffered `saveAdvanced` form — a single toggle only ever changes its own row), and a small table reusing the existing shared `ToggleSwitch` component (checked out of `frontend/src/components/` before building anything new, per the style guide) rather than a new checkbox pattern. A non-entitled or not-yet-implemented module renders with its toggle disabled and its own explanatory hint text, never hidden. `OrgModule` added to `api/types.ts`; new `orgAdmin.modules*`/`groupModules` string keys in `i18n/strings.ts`.

**SOC 2 identify → verify → remediate pass** (per the plan's explicit Phase 1 commitment, `change-management-and-secure-development-policy.md`'s practice for security-sensitive changes): **Identified** — plugin loading is a genuinely new code-loading trust boundary: `_discover_entry_point_modules`/`_discover_path_modules` can import and execute arbitrary third-party code discovered via `importlib.metadata`/a filesystem scan, and `docs/soc2/trust-services-criteria-mapping.md`'s CC6.8 gap ("no automated dependency/container vulnerability scanning") already existed before this raised the stakes of it. **Verified** — three concrete mechanisms bound the risk, each covered by a test: (1) `ALLOW_EXTERNAL_MODULES` defaults `False` and, when off, the discovery functions are never even called (not just filtered), confirmed by `test_allow_external_modules_false_never_calls_discovery_functions` monkeypatching both to raise; (2) `is_module_enabled`'s entitlement-AND-enablement logic means an org admin can never self-enable a module their organisation isn't server-tier entitled to, confirmed by `test_org_admin_cannot_enable_a_non_entitled_module`; (3) `require_org_module_enabled`/`require_project_module_enabled` return 404 rather than 403 on a disabled/non-entitled module, confirmed by four dedicated dependency tests — a caller can't distinguish "disabled" from "never existed," so no information about server-tier entitlement leaks through the error shape. **Remediated/documented** — `docs/soc2/policies/vendor-and-subprocessor-management-policy.md` gained policy point 7 (installed modules are a code-execution-inside-the-boundary risk, distinct from a subprocessor; version-pinning and pre-install due diligence required) and a new Known Gaps entry (no automated vetting of installed modules yet, cross-referencing CC6.8); `trust-services-criteria-mapping.md`'s CC6.8 row gained a note that the `ALLOW_EXTERNAL_MODULES` gate is a deliberate mitigation layered on the still-open gap, without marking that gap closed.

**Incidental documentation drift found and fixed while updating `docs/solution-architecture.md` for this phase (fixed now, not deferred):** Phase 0's own `server_roles` table/`UserServerRole` model had never been added to that document's identity ER diagram or its exhaustive table list — a real, if narrow, instance of the exact drift class that document's own "Database structure" section already calls out twice from earlier phases. Added alongside this phase's own two new tables rather than left as a second, unrelated gap; total table count corrected 49 → 52 (one for the Phase 0 gap, two for this phase's own tables).

**Tests**: `backend/tests/test_module_registry.py` (new, 24 tests) — all four entitlement/enablement combinations (direct function calls and through `GET /orgs/{id}/modules`); `require_org_module_enabled`/`require_project_module_enabled` 404 behaviour (unregistered module, disabled, not-entitled, and the passing case) via direct dependency invocation against a `_FakeRequest` stand-in (simpler and more targeted than wiring a live fixture router into the shared session-scoped `app` fixture, and avoids mutating that shared app object at all); registry merge logic (entry-point module included/excluded by `ALLOW_EXTERNAL_MODULES`, installed-always-wins on key collision); the "discovery functions never called when the flag is off" guarantee; org-admin endpoint 403s, the entitlement-blocks-enablement 403, and the audit-log write; server-tier endpoint gating by `MODULE_ADMINISTRATOR`/`SERVER_ADMIN` and 404s for unknown org/module. A `fake_module` pytest fixture registers and tears down a fixture `ModuleDefinition` in `INSTALLED_MODULES` per test (rebuilding the registry cache in setup and teardown) so tests remain order-independent per this repo's standing rule. Frontend: `OrgAdminPage.stories.tsx` gained four new stories (`ModulesSectionEmptyState`, `ModulesSectionToggleEntitledModule`, `ModulesSectionNonEntitledModuleIsGreyedOut`, `ModulesSectionNotYetImplementedModuleIsDisabled`) — adding the new `/modules` fetch into `reload()` required updating **two** separate mock `api.get` implementations in that stories file (`mockOrgAdminApis` and the standalone `mockProjectsWorkflowWithOneProject`), the second found only by running the full existing story suite and seeing unhandled-rejection noise from stories that don't use the first mock at all. A new Playwright spec (`tests/playwright/tests/e2e-workflows/org-admin-modules.spec.ts`) covers the section loading and showing the empty state for an org admin — deliberately not a full toggle-interaction test, since the real module registry has zero entries until Phase 5 and fabricating a fake production module package just to test the UI is explicitly against this repo's testing conventions; the spec's own docstring explains this and points at the Storybook stories for the toggle-interaction coverage instead.

Commands actually run, all clean: `ruff check .` (backend, all touched files), `python -c "from app.main import app"` (import/circularity smoke test), `pytest tests/test_module_registry.py` (24 passed) and the full backend suite (784 passed; the same 14 pre-existing `aiosmtplib`/mailhog-hostname-resolution failures Phase 0's entry above already documents as environmental, not caused by either phase — re-confirmed here by re-running those exact tests inside the `tests/container` Docker network, where they pass), `tsc -b` and `eslint .` (frontend, clean), `vitest run --project=storybook` (full suite, 635 passed including the 4 new Modules stories). No mypy/pyright config exists in this repository (`backend/pyproject.toml` has no such section), so no separate type-checker step applies beyond `ruff`'s own type-adjacent lints. No backend model/schema change needed in either seed script (`seed_demo_data.py`/`seed_e2e_dataset.py`): the two new tables are override-only with sensible zero-row defaults, and there are zero implemented modules to demonstrate until Phase 5.

### Files changed

`backend/app/models/module.py` (new), `backend/app/models/__init__.py`, `backend/alembic/versions/0024_module_registry.py` (new), `backend/app/config.py`, `backend/app/modules/__init__.py` (new), `backend/app/modules/registry.py` (new), `backend/app/services/rbac.py`, `backend/app/schemas/org.py`, `backend/app/routers/orgs.py`, `backend/app/routers/system.py`, `backend/app/main.py`, `backend/tests/test_module_registry.py` (new); `frontend/src/api/types.ts`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`, `frontend/src/i18n/strings.ts`; `tests/playwright/tests/e2e-workflows/org-admin-modules.spec.ts` (new); `docs/solution-architecture.md` (new "Modular Feature System" subsection, ER diagram/table-list updates including the Phase 0 drift fix); `docs/soc2/policies/vendor-and-subprocessor-management-policy.md` (new policy point 7, new Known Gaps entry); `docs/soc2/trust-services-criteria-mapping.md` (CC6.8 mitigation note); `docs/compliance-module-plan.md` (Phase 1 marked complete); `docs/decisions.md` (this entry).

## Compliance module plan, Phase 2: module-contributed RBAC (2026-09-05)

Third implementation phase of `docs/compliance-module-plan.md`, built directly on Phase 0's `ServerRole`/`require_server_role` and Phase 1's registry/gating tables: lets a module declare its own named roles (e.g. a future Compliance module's `compliance_manager`/`compliance_officer`) without extending the core `OrgRole`/`ProjectRole` enums — the plan's own reversal of an earlier draft that would have hardcoded Compliance's roles onto those enums. Reuses `DirectoryTable` + `MultiSelectDropdown` for the frontend surface, per this plan's single most-corrected design-history point (already applied to server roles in Phase 0).

**Backend.** `app.modules.registry.ModuleRoleDefinition` (new frozen dataclass: `role_key, name, description, scope` where `scope: Literal["org", "project"]`) is what a module's `ModuleDefinition.roles` tuple now actually holds — that field's type hint changed from `tuple` to `tuple[ModuleRoleDefinition, ...]`, and its stale "placeholder... left as an empty tuple until Phase 2" docstring sentence was corrected. Two new tables, both in a new `backend/app/models/module_role.py`: `module_role_definitions` (`ModuleRoleDefinitionRow`) is a deliberately **append-only** database mirror of every role the registry has ever declared — `app.modules.registry.sync_module_role_definitions` upserts it on `(module_key, role_key)` at every process start (called from `app.main`'s `lifespan`, right after `run_bootstrap`), matching this plan's own "don't silently drop historical display data" philosophy (cited from Phase 8's evidence-revalidation note) so a grant's display name stays resolvable even if its module is later uninstalled; `user_module_roles` (`UserModuleRole`) is the actual grant table, `organization_id` always set (even for a project-scoped grant, via the project's own organisation) and `project_id` nullable, set only for project-scoped grants. Neither `module_key` nor `role_key` is a foreign key anywhere, for the same "defined in code, not by a database row" reason `OrganizationModuleEntitlement.module_key` already documents. Migration `0025_module_roles.py` follows `0023`/`0024`'s exact raw-SQL, `IF NOT EXISTS`-everywhere style, both tables plus `ix_user_module_roles_user_id`/`ix_user_module_roles_organization_id`, no backfill.

`app.modules.registry.list_enabled_module_roles(db, organization_id, scope)` is the one function every module-role read path funnels through: it yields `(module_key, ModuleRoleDefinition)` for every role of the given scope declared by a module that is *currently effectively enabled* for that organisation — the single place that turns "registered" into "actually offered," reused by both new `GET .../module-roles` endpoints, both new `POST .../module-roles` endpoints' 400 validation, and both `list_org_users`/`get_effective_members`'s own enabled-modules-only filtering of already-held grants.

`services/rbac.py` gained `require_module_role(module_key, role_key)` — a dependency **factory** (called at router-definition time, matching every other `require_*` factory in this file) that resolves the role's declared `scope` immediately by looking it up in the live registry, raising `ValueError` at construction time if the module or role isn't registered (a code-time contract violation, not a request-time 500). The returned dependency is shaped exactly like `require_org_module_enabled`'s/`require_project_module_enabled`'s own inner dependency (same 404-not-403 on a disabled/non-entitled module), then composes: `is_server_admin` always passes; `OrgRole.ORG_ADMIN` (org scope) or `ProjectRole.PROJECT_MANAGER` (project scope) passes with zero grants; otherwise a matching `UserModuleRole` row (`_has_module_role_grant`) is required. This is generic infrastructure with no real caller yet — same position `require_org_module_enabled`/`require_project_module_enabled` were left in after Phase 1 — proven here only against `test_module_contributed_roles.py`'s fixture module.

New endpoints, mirroring `assign_org_role`/`assign_project_role`'s existing idempotent-grant/no-op-revoke/audit-logged shape exactly: `GET /orgs/{id}/module-roles` (`require_org_role(ORG_ADMIN, PROJECT_CREATOR, MEMBER)`, same bar as `list_org_users`), `POST`/`DELETE /orgs/{id}/users/{user_id}/module-roles[/{module_key}/{role_key}]` (`require_org_role(ORG_ADMIN)`); `GET /projects/{id}/module-roles` (`require_project_view_or_manage`), `POST`/`DELETE /projects/{id}/members/{user_id}/module-roles[/{module_key}/{role_key}]` (`require_project_manage`). Both POST endpoints 400 if `(module_key, role_key)` doesn't name a real role of the right scope on a currently-enabled module (via `list_enabled_module_roles`), which is what makes a disabled module's roles ungrantable and catches a typo'd key in one check. `OrgUserOut.module_roles`/`EffectiveMemberOut.module_roles` (both `list[ModuleRoleGrantOut]`) are populated by `list_org_users`/`get_effective_members` respectively, filtered to currently-enabled modules only — a grant for a since-disabled module is simply omitted from the response, never deleted from `user_module_roles`.

**Frontend.** `ModuleRoleDefinition`/`ModuleRoleGrant` added to `api/types.ts`; `OrgUser.module_roles`/`EffectiveMember.module_roles` extended onto the existing types. `OrgAdminPage.tsx`'s Users table Roles column `MultiSelectDropdown` now appends one option per org-scoped available module role (fetched alongside `modules` in the same `reload()` block) — `value: "${module_key}:${role_key}"`, `label: "${d.name} (${moduleDisplayNameFor(d.module_key)})"` matching the plan's own worked example ("Compliance Officer (Compliance)"), backed by new `grantModuleRole`/`revokeModuleRole` functions mirroring `grantOrgRole`/`revokeOrgRole`'s exact single-row local-state-patch shape (not a full `reload()`). `ProjectMembersTable.tsx` gained two new props, `availableModuleRoles`/`onToggleModuleRole` — its Role column merges in one option per entry, checked from the member's own `module_roles`, **never subjected to the `purelyDirect`/last-manager disabling logic** the four core-role options get, since module roles are direct-grant-only with no inherited-source state to guard against. Both call sites (`OrgAdminPage.tsx`'s "manage users" modal, `ProjectAdminPage.tsx`'s own Members section) now fetch `GET .../module-roles` and wire the two new props through — the modal fetches it per-project on open (a different project can belong to a different org, hence a different enabled-module set), `ProjectAdminPage.tsx` fetches it once in its own `reload()`.

**Deliberately not built**: no `MODULE_ROLE_LABEL`-style frontend label map — `ModuleRoleDefinition.name`/`description` are already human-readable data returned by the API (set server-side by the module's own author), not a frontend-known closed enum being rendered from a raw wire value, so this repo's label-map rule (CLAUDE.md, style guide Principle 12) doesn't apply the way it does to `OrgRole`/`ProjectRole`; `d.name` is rendered directly. Explicitly confirmed as a deliberate reading of that rule, not a miss.

**Judgment calls the plan's own spec left open, recorded here so a future session doesn't re-derive them:**
- `ModuleRoleGrantOut` (`{module_key, role_key}`) and `ModuleRoleDefinitionOut` (`{module_key, role_key, name, description}`) both landed in `schemas/org.py`, imported into `schemas/project.py` from there — the reverse direction of the existing precedent `orgs.py` already set by importing `MoveDirection` from `schemas/project.py`. No new `schemas/module_role.py` was warranted: Phase 1's own notes already established this codebase only adds a new shared-schemas module when a phase's needs don't fit an existing file's convention, and reusing the existing cross-import precedent covers this.
- The project-scoped read endpoint's gate (`GET /projects/{id}/module-roles`) is `require_project_view_or_manage` — confirmed against that dependency's own docstring and `list_project_groups`'s identical gate, both existing specifically for endpoints exposing role/group *structure* rather than requirement/change-request *content*.
- `ModuleRoleDefinitionRow.description` is `Text`, not a bounded `String(n)` — matching this codebase's existing convention for other free-text description columns (`RequirementAction.description`, `ChangeRequestTask.description`), none of which pick an arbitrary character cap.
- Grant notification: both `assign_org_module_role`/`assign_project_module_role` send a `NotificationType.PERMISSION_GRANTED` notification, mirroring `assign_org_role`/`assign_project_role`'s own notification exactly (title/body adapted to name the module role). The plan flagged this as optional ("if trivial to mirror"); it was, so it's included for parity — a module-role grant is not silently unnotified next to an equivalent core-role grant.
- `ProjectMembersTable.tsx` renders a module-role option's `label` as `d.name` alone (no owning-module-name suffix), unlike `OrgAdminPage.tsx`'s parenthetical treatment — the spec's own worked example was for the org-admin surface only; reproducing it here would need a second prop purely for a disambiguation need no module with roles exists yet to exercise. Revisit if a future module's role names collide within one project's dropdown.
- `ProjectMembersTable.tsx`'s existing "Remove all access" action was left untouched — it still only loops the member's core-role `sources`, not their module-role grants. Not called out in the spec; flagged here rather than left undiscoverable, since folding module-role revocation into an already-shipped action would be a behaviour change the spec didn't ask for.

**Tests**: `backend/tests/test_module_contributed_roles.py` (new, 15 tests, separate from `test_module_system_rbac.py` per Phase 0's own stated reasoning for keeping module-system-infrastructure tests out of feature-specific files) — org- and project-scoped grant/revoke idempotency plus audit logging; `require_module_role` composition at both scopes (server admin passes with zero grants, `ORG_ADMIN`/`PROJECT_MANAGER` passes with zero grants, a user holding only the specific grant passes, a plain member/project-member with none of those is refused); `require_module_role` raising `ValueError` at construction for an unknown module or role; 404-not-403 when the module is disabled, even for an org admin/server admin; a disabled module's role disappearing from both `GET .../module-roles` and `list_org_users`'s `module_roles` field while the underlying `UserModuleRole` row still exists (re-enabling brings it back — a filter, not a deletion); 400 on an unknown role key, a wrong-scope role posted to either endpoint, and a disabled module's role; `sync_module_role_definitions` upserting an existing row's `name`/`description` in place (not duplicating) and never deleting a row for a module removed from the live registry. A `fake_module` fixture mirrors `test_module_registry.py`'s own mechanics exactly (mutate `INSTALLED_MODULES` in place, `build_registry(force=True)`, always tear down), registering two fixture roles (one org-scoped, one project-scoped) and calling `sync_module_role_definitions` once so `module_role_definitions` rows exist for it. Frontend: `ProjectMembersTable.stories.tsx` gained `ModuleRolesAvailable` (options present, one checked/one not, neither disabled, toggling calls `onToggleModuleRole`); `OrgAdminPage.stories.tsx` gained `UsersSectionModuleRoleGrantAndRevoke` (the parenthetical label, grant via `POST`, revoke via `DELETE`) — every other existing story on both files implicitly covers the zero-available-module-roles case, the real state of this deployment today, since neither new prop/fetch is exercised unless a story explicitly supplies it. Extending both stories files' mocked `api.get` implementations to handle the new `/module-roles` paths (in some cases **two** separate mock functions per file, the same class of miss Phase 1's own entry above already flagged) surfaced five unhandled-promise-rejection failures only when the *full* story suite was run, not the individual new stories — fixed by adding the branch to every mock implementation that could reach `reload()`, confirmed clean by re-running each affected file's full suite afterward. Playwright: `tests/playwright/tests/e2e-workflows/module-contributed-roles.spec.ts` (new) — same deliberately-narrow scope Phase 1's own `org-admin-modules.spec.ts` established (no real module exists to interact with until Phase 5, and fabricating one would be scope creep this repo's testing conventions explicitly warn against): confirms the Roles dropdown on both `OrgAdminPage.tsx`'s Users table and `ProjectMembersTable.tsx` renders exactly its unchanged fixed core-role option count (3 and 4 respectively) with no stray module-role option or crash from the new (empty) `GET .../module-roles` fetch, and that granting/revoking a core role through it still works end-to-end. Authored and confirmed to parse/list correctly (`playwright test --list`) but **not run against a live `tests/container` stack** in this session — the stack wasn't rebuilt with this phase's frontend/backend changes, and doing so was judged not worth the time cost for a spec whose only new assertions are option-count checks already covered more directly by the Storybook stories above and the backend pytest suite; flagged here rather than claimed as verified.

**A concurrent-pytest-process false alarm, not a real regression, worth recording so a future session doesn't repeat it:** a first full-suite verification run was started, then — before it finished — a second, overlapping `pytest` invocation was started against the same shared `reqtrack_pytest_test` database (checking a narrower test slice). Both runs then showed spurious failures (`ObjectDeletedError`, table-truncation races) in code this phase never touched (`test_encrypted_type.py`, `test_engagement.py`) — the exact "tests/container DB wedge" pattern this project has hit before. Both contaminated runs were discarded; a third, deliberately *exclusive* full-suite run (no other DB-touching process active) came back **813 passed, 0 failed, 3 warnings, in 842.06s** — genuinely clean, including every test this phase added. `SMTP_HOST=localhost` was also passed for this run (overriding the default `mailhog` hostname, which only resolves inside the `tests/container` Docker network) — the 14 invite/email-sending tests Phase 0's own entry already documented as environmental-failures-from-the-host now pass outright with this override, confirmed to be the same root cause by a `git stash` A/B check against one of them before this phase's changes existed at all.

Commands actually run, all clean: `ruff check` (backend, all touched files, plus `--fix` for two import-order lints Phase 1's own review missed catching before commit-time), `python -c "import app.main"` (import/circularity smoke test), `pytest tests/test_module_contributed_roles.py` (15 passed) and `pytest tests/test_schema_migrations_match_models.py` (1 passed) in isolation, then the full backend suite exclusively (813 passed, 0 failed — see above); `tsc -b --noEmit` and `eslint` (frontend, all touched files, clean) and `npm run build` (production build succeeds, pre-existing >500kB chunk-size warnings unrelated to this change); `vitest run --project=storybook` for each touched stories file individually (`ProjectMembersTable.stories.tsx` 17/17, `OrgAdminPage.stories.tsx` 64/64, `ProjectAdminPage.stories.tsx` 42/42, all passing with zero unhandled-rejection noise after the mock fixes above) — the full cross-repo story suite (Phase 1's own 635-test baseline) was not re-run in full this phase, only the three files this phase actually touched, each confirmed individually clean. No backend model/schema change needed in either seed script: both new tables are grant/mirror tables with a working zero-row default, and there is nothing yet to demonstrate until Phase 5 registers the first module with roles.

### Files changed

`backend/app/models/module_role.py` (new), `backend/app/models/__init__.py`, `backend/alembic/versions/0025_module_roles.py` (new), `backend/app/modules/registry.py`, `backend/app/services/rbac.py`, `backend/app/schemas/org.py`, `backend/app/schemas/project.py`, `backend/app/routers/orgs.py`, `backend/app/routers/projects.py`, `backend/app/main.py`, `backend/tests/test_module_contributed_roles.py` (new); `frontend/src/api/types.ts`, `frontend/src/components/ProjectMembersTable.tsx`, `frontend/src/components/ProjectMembersTable.stories.tsx`, `frontend/src/pages/OrgAdminPage.tsx`, `frontend/src/pages/OrgAdminPage.stories.tsx`, `frontend/src/pages/ProjectAdminPage.tsx`, `frontend/src/pages/ProjectAdminPage.stories.tsx`, and five other stories files whose `OrgUser`/`EffectiveMember` fixture literals needed a `module_roles: []` field added to keep compiling (`frontend/src/components/AddToGroupControl.stories.tsx`, `frontend/src/components/UserAutocomplete.stories.tsx`, `frontend/src/pages/ActionDetailPage.stories.tsx`, `frontend/src/pages/PreferencesPage.stories.tsx`, `frontend/src/pages/ProjectActionsPage.stories.tsx`); `tests/playwright/tests/e2e-workflows/module-contributed-roles.spec.ts` (new); `docs/solution-architecture.md` (extended "Modular Feature System" section, ER diagram/table-list updates), `docs/soc2/policies/access-control-policy.md` (Authorization section item 4's forward-looking sentence replaced with the shipped description; diagram note made present-tense), `docs/compliance-module-plan.md` (Phase 2 marked complete, "Phase 2 notes" subsection added), `docs/decisions.md` (this entry).

## Compliance module plan, Phase 3: frontend module integration, two-tier (2026-09-05)

Fourth implementation phase of `docs/compliance-module-plan.md`: gives `ModuleDefinition.frontend_manifest` its real shape and builds the two-tier frontend integration model the design history's final correction required — "third-party modules should be able to use core components... so their interfaces are consistent with the main UI." Tier A ("installed") is the primary path a first-party or npm-installed module uses, compiled directly into this bundle with full native shared-component reuse; Tier B ("remote") is a sandboxed `<iframe>` plus a Host UI Bridge for a genuinely dynamic, not-installed module, relaying real shared chrome over `postMessage` rather than reimplementing it. No actual module (first-party or third-party) ships in this phase — Compliance itself doesn't land until Phase 5+/12 — so every piece here is proven against fixture modules, the same position Phase 1/2 left their own infrastructure in.

**Backend.** `app.modules.registry.ModuleFrontendManifest` (new frozen dataclass: `tier: Literal["installed", "remote"]`, `nav_label`, `nav_path`, `frame_url: str | None`) is what `ModuleDefinition.frontend_manifest`'s type changed from `object | None` to — `__post_init__` enforces `frame_url` is set if and only if `tier == "remote"`. `app.modules.registry.get_frontend_manifest(module_key)` is the one function every read path funnels through: for a `"remote"` manifest, it cross-checks `frame_url`'s origin against the new `Settings.module_frame_allowed_origins` (env `MODULE_FRAME_ALLOWED_ORIGINS`, comma-separated, empty/`frame-src 'none'` by default — mirrors `cors_origins`' shape for the opposite direction) and returns `None` (logged at `WARNING`) if it falls outside, rather than trusting the module's own declaration — the same "verify mechanically" principle Phase 4's design already commits to for MCP tools, applied here to a different field ahead of that phase landing. `main.py`'s `security_headers_middleware` now builds `Content-Security-Policy: frame-src` from that same setting on every response (`'none'` by default) — a second, independent, browser-enforced backstop alongside the registry-level check.

**Tier B token minting and scoping.** `app.security.create_module_frame_token(module_key, organization_id, user_id, project_id=None)`/`decode_module_frame_token` mint/verify a 15-minute JWT (`purpose: "module_frame"`) scoped to exactly one module/org/(optionally)project/user tuple, mirroring `create_oidc_state_token`'s multi-claim shape. `POST /orgs/{organization_id}/modules/{module_key}/frame-token` (`routers/orgs.py`) and `POST /projects/{project_id}/modules/{module_key}/frame-token` (`routers/projects.py`) mint these, gated by two new `services/rbac.py` dependencies, `require_org_module_enabled_dynamic`/`require_project_module_enabled_dynamic` — plain-function siblings of the existing `require_org_module_enabled(module_key)`/`require_project_module_enabled(module_key)` *factories*, needed because a generic minting endpoint can't fix `module_key` as a closure constant at router-definition time the way a real module's own router does. Both depend on plain `get_current_user`, never a module-frame-accepting one, so an iframe's own already-scoped token can never mint itself a different one.

The harder design question — making the minted token actually "checked by `require_module_enabled`/`require_module_role` exactly like any other request," per the plan's own text — turned on one fact confirmed by reading `deps.py` before writing any code: `_resolve_user_from_token` already rejects any bearer token whose `purpose` isn't `"access"` (the same mechanism that already rejects a replayed 2FA challenge token), so a module-frame token is *already* mechanically unusable against `get_current_user` and therefore every endpoint outside the module-gating dependencies, with no new code needed for that half. A new `deps.get_current_user_or_module_frame(module_key)` factory — used only inside `require_org_module_enabled`/`require_project_module_enabled`/`require_module_role` — decodes either a normal token (unchanged behaviour) or a module-frame token scoped to exactly that `module_key` (401 otherwise), stamping `request.state.module_frame_scope` only in the latter case. A new `rbac._enforce_module_frame_scope(request, organization_id=, project_id=)` helper, called as the *first* check in all three dependencies' bodies — before `check_pat_scope`, before `is_server_admin`/`ORG_ADMIN`/`PROJECT_MANAGER` bypasses — 403s if a present `module_frame_scope`'s own `organization_id`/`project_id` claim doesn't match the specific resource this request names. This ordering is the load-bearing decision: a module-frame token minted for org A must 403 against org B's endpoints *even for a server admin's own token*, since the whole point of handing a remote module a narrower token is that it must not inherit the real user's broader access just because the request happens to be authenticated as that user underneath.

**Frontend.** `frontend/src/modules/registry.ts` (new) exports `installedModules: TierAModuleDefinition[]` (empty — Phase 12 registers Compliance here) and `getInstalledModule(key)`; `frontend/src/modules/types.ts` (new) declares the `TierAModuleDefinition`/`TierAModuleRoute` shapes. `frontend/src/modules/buildModuleRoutes.tsx` (new — split out of `App.tsx` itself to avoid a Fast Refresh lint break from mixing component/non-component exports in one file) builds one `<Route>` per currently-enabled module: a Tier A module's own registered routes, or a generic `<Route element={<ModuleFrame .../>}>` for a Tier B one; `App.tsx`'s `ProtectedRoutes` spreads its output directly into `<Routes>`'s children (React Router's `createRoutesFromChildren` only recognises literal `<Route>`/`<Fragment>` elements in that tree — an early draft wrapped the array in a rendered component instead, which `<Routes>` silently doesn't see into; caught before it shipped). `frontend/src/components/ModuleFrame.tsx` (new) is the Tier B host: mints a token on mount (`POST .../frame-token`), sends `init` once the iframe's `load` fires, and relays `toast`/`confirm` messages from the iframe onto the real `useToast()`/`ConfirmDialog` — every message checked both ways against the iframe's own declared origin, never `"*"`. It reads the current resolved theme directly off `document.documentElement`'s `data-theme` attribute rather than via `useTheme()`/`ThemeContext` — `ThemeProvider` already stamps the *resolved* value there for CSS to cascade from, so this is equivalent without pulling in a context dependency, and kept its own Storybook story from needing a provider wrapper no other story in this codebase has needed yet. `frontend/src/hooks/useProjectEnabledModules.ts` (new) fetches the new `GET /projects/{project_id}/enabled-modules` endpoint (`ModuleNavEntryOut[]`) and is called independently by `Layout.tsx` (nav rendering) and `App.tsx` (route splicing) rather than through a shared context provider — mirrors `BrandingContext`/`TerminologyContext`'s existing "each concern fetches its own project-derived data" convention rather than introducing a new provider-tree layer for a small, cheap GET. `Layout.tsx`'s project-scoped nav group gains one `NavRailLink` per enabled module with a usable manifest, rendered generically (a placeholder `Wrench` icon, since no per-module icon concept exists yet).

`GET /projects/{project_id}/enabled-modules` was a new endpoint, not a reuse of the existing `GET /orgs/{id}/modules` — that one is `OrgRole.ORG_ADMIN`-gated and deliberately includes non-entitled/disabled modules greyed out for the admin bookkeeping view, the wrong shape and the wrong gate for "what should any project member's nav rail show." It's gated `require_project_view_or_manage`, the same "structure, not content" bar Phase 2 already established for `GET /projects/{id}/module-roles`. `OrgModuleOut` (the admin view) also gained a `frontend_manifest` field, computed the same way via `get_frontend_manifest`, so the two views stay consistent.

**Judgment calls the plan's own spec left open:**
- Sandbox flags: `sandbox="allow-scripts allow-same-origin allow-forms"` — `allow-same-origin` scopes to the iframe's own distinct (always cross-origin, by construction) origin, needed for the module's own script/storage/fetch to function normally; not a same-origin-escape risk since the frame is never same-origin with the host to begin with.
- No shared context provider for enabled-modules state (see above) — a deliberate departure from a `ModulesProvider` design considered and rejected in favour of matching this codebase's existing per-concern-fetches-its-own-data convention.
- `docs/soc2/policies/access-control-policy.md`'s Authorization section item 4 gained a new paragraph (after this section's existing module-contributed-roles paragraph) documenting the module-frame-token mechanism as a fifth, narrower *authentication*-layer restriction — not a sixth RBAC scope — composing underneath the existing scopes rather than replacing any of them.

**Tests.** `backend/tests/test_module_frontend_integration.py` (new, 26 tests): `ModuleFrontendManifest.__post_init__` validation; `get_frontend_manifest`'s allowlist accept/reject (including "declares an origin allowlisted for a *different* module" and "no manifest at all" cases); CSP `frame-src` header defaults to `'none'` and reflects a configured allowlist; both frame-token minting endpoints (success, 404 on disabled/unregistered module, a module-frame token can't mint another); `get_current_user_or_module_frame`'s token resolution (valid token, wrong-module-key rejection, still accepts a normal access token); module-frame scope enforcement on `require_org_module_enabled`/`require_project_module_enabled`/`require_module_role` (mismatched org, mismatched project, an org-scoped token rejected against a project-scoped endpoint, a scope mismatch 403s even when constructed with a server-admin `current_user` — proving the ordering-before-bypass decision above actually holds); the two nav-facing endpoints. Full backend suite re-run exclusively (no concurrent DB-touching process, per this project's own documented wedge-avoidance practice): **839 passed, 0 failed, 3 warnings** (same pre-existing count as Phase 2's own baseline, no new ones) in 879s.

Frontend: `frontend/src/components/ModuleFrame.stories.tsx` (new, 5 stories) exercises the full Host UI Bridge contract — init/token-minting on load, toast relay, untrusted-origin messages ignored, both `ConfirmDialog` tiers' round trip (confirm and cancel) — by dispatching synthetic `MessageEvent`s at the rendered iframe's own `contentWindow` with a controlled `origin`, rather than depending on a real external origin loading inside the test browser. An early version used a genuinely cross-origin fixture `frameUrl`; the *first* story passed, but every subsequent one failed with a real `SecurityError` on `spyOn(...).mockRestore()` — the browser had, by then, actually completed (or attempted) cross-origin navigation, turning `contentWindow` into a inaccessible cross-origin proxy no test can redefine properties on. Fixed by making the story's fixture `frameUrl` same-origin to the Storybook test page itself (`window.location.origin`-based) — every assertion in these stories only depends on the `origin` *string* carried on each synthetic message, which is exercised identically either way, so this is a test-environment fix, not a narrowing of what's actually verified. `frontend/src/App.stories.tsx` (new, 2 stories, `TierARoutingHarness`) proves the Tier A mechanism end-to-end against a fixture module pushed into the real `installedModules` registry (module-scope, not per-test — mirrors this codebase's existing fixture-constant convention, and is harmless since no other story file reads `installedModules`) and rendered through the real, unmodified `buildModuleRoutes` — including that the fixture route's own component genuinely calls `useToast()` and a real toast appears, the concrete proof of "not a lookalike." `tsc -b --noEmit`, `eslint` (all touched files), and `npm run build` all clean; `vitest run --project=storybook` for every touched/new stories file individually clean (`ModuleFrame.stories.tsx` 5/5, `App.stories.tsx` 2/2, `OrgAdminPage.stories.tsx` re-run in full at 64/64 to confirm the `OrgModule.frontend_manifest` field addition didn't regress its existing fixtures).

**Not built, deliberately, consistent with Phase 1/2's own precedent:** no new Playwright spec. No real Tier A or Tier B module exists until Phase 12, so there is nothing in the actually-running app for a spec to click through beyond "the nav rail renders its existing entries with no crash" — already covered more directly, and more informatively, by the Storybook coverage above. Revisit once Phase 12 registers Compliance's own frontend module here.

### Files changed

`backend/app/config.py`, `backend/app/security.py`, `backend/app/deps.py`, `backend/app/services/rbac.py`, `backend/app/modules/registry.py`, `backend/app/main.py`, `backend/app/schemas/org.py`, `backend/app/routers/orgs.py`, `backend/app/routers/projects.py`, `backend/tests/test_module_frontend_integration.py` (new); `frontend/src/api/types.ts`, `frontend/src/modules/types.ts` (new), `frontend/src/modules/registry.ts` (new), `frontend/src/modules/buildModuleRoutes.tsx` (new), `frontend/src/components/ModuleFrame.tsx` (new), `frontend/src/components/ModuleFrame.stories.tsx` (new), `frontend/src/hooks/useProjectEnabledModules.ts` (new), `frontend/src/components/Layout.tsx`, `frontend/src/App.tsx`, `frontend/src/App.stories.tsx` (new), `frontend/src/pages/OrgAdminPage.stories.tsx` (new `frontend_manifest` field on existing fixtures); `docs/solution-architecture.md` (extended "Modular Feature System" section), `docs/soc2/policies/access-control-policy.md` (Authorization section item 4 gained the module-frame-token paragraph), `docs/compliance-module-plan.md` (Phase 3 marked complete, "Phase 3 notes" subsection added), `docs/decisions.md` (this entry).

## Compliance module plan, Phase 4: module-contributed MCP tools (2026-09-05)

Fifth implementation phase of `docs/compliance-module-plan.md`, and the last of the five module-system infrastructure phases (0–4) before Compliance itself (Phase 5+) becomes the first real module built on top of it: lets a module declare its own `mcp-server/` tools — `ModuleDefinition.mcp_tools`, a tuple of `McpToolDefinition` — proxied declaratively through `mcp-server`'s existing `_call_backend` pattern rather than a second implementation of it. Unlike Phases 0–3, this phase's own design was self-reviewed and hardened *during planning*, before any code existed (see the plan's own "SOC2 / Security Planning" section, addenda 2–3): three real gaps in the original design (self-declared `mutates`/`is_approval_action`, an unconstrained `path_template`, an unauthenticated boot-time manifest fetch) were found and fixed on paper first. Implementing exactly that already-hardened design is what this entry records — no new gap was found this time, which is itself the outcome of doing the identify→verify→remediate pass early rather than a sign the pass wasn't needed.

**Design continuity, not re-derivation.** `docs/modules.md` — a readable, example-driven module-authoring guide — was already written and merged into this repository ahead of this phase's implementation, and turned out to pin down exact, load-bearing details this phase needed: the `params` dict shape (`{"name", "type", "required", "in", "description"}`, with `"in"` rather than a more generic `"location"` key, and `"type": "uuid"` as a display hint rather than a strict JSON-Schema primitive), the literal function names `_register_declarative_tool`/`_call_backend`, and the ~10-minute manifest cache window. This phase's implementation matches that document precisely rather than inventing an equivalent but differently-shaped API, since a future module author (including whoever builds Compliance itself in Phase 5+) will read `docs/modules.md`, not this decisions-log entry, to learn the contract.

**Backend.** `app.modules.registry.McpToolDefinition` (new frozen dataclass: `name, description, method, path_template, params: list[dict]`) is what `ModuleDefinition.mcp_tools` now actually holds — that field's type hint changed from a placeholder `tuple` to `tuple[McpToolDefinition, ...]`. `app.modules.registry.build_mcp_tool_manifest()` is the one function that turns a live registry's declarations into `ResolvedMcpTool` entries (`name, description, method, path_template, mutates, params`), re-deriving or verifying every security-relevant part rather than trusting the module's own declaration: `name` is always `f"{module.key}_{tool.name}"` regardless of what local name a module supplies; `mutates` is `method != "GET"`, nothing for a module to misdeclare; `path_template` must both start with the declaring module's own `get_router().prefix` and resolve to a real route on that same router with a matching method (looked up directly in that router's own `.routes`, keyed by `(path, method)`); and whether the resolved route is an approval action is read from that route's own `openapi_extra.get("x-approval-action")` (a new exported marker, `APPROVAL_ACTION_ROUTE_EXTRA`, for a module's own route decorators to spread in), never from the module's `McpToolDefinition`. Each of these checks fails closed and per-tool (excluded + logged at `WARNING`), never aborting the whole manifest for one bad declaration — extended one level further to a single malformed param dict inside an otherwise-valid tool, which is dropped on its own rather than disqualifying that tool. A key implementation finding, confirmed by direct experimentation before writing the real code rather than assumed: a FastAPI `APIRoute`'s `.path` is already the *full*, prefix-combined path the instant it's added to an `APIRouter(prefix=...)` — prefix combination happens at route-registration time on the router itself, not later at `app.include_router()` — so `build_mcp_tool_manifest()` needs no live `Request`/`app` reference at all; it reads everything it needs straight off `definition.get_router()`, whether or not that router has actually been mounted into the running app yet. `GET /api/v1/system/modules/mcp-tools` (`routers/system.py`, new `ModuleMcpToolOut` schema) returns this manifest, gated by plain `get_current_user` — deliberately no admin-role exemption or requirement, matching this phase's own hardening commitment that the fetch must be authenticated but the manifest itself is not privileged data (per-call access to whatever a listed tool actually proxies to is still fully enforced by that endpoint's own real gating dependency, exactly like an already-listed core tool such as `list_projects` today).

**`mcp-server`.** A new, clearly-bannered "Module-contributed tools" section in `server.py` (matching the existing write-mode section's own inline-not-split-file convention, consistent with this file's deliberate single-file-app design) adds: `_ModuleToolParam`/`_ModuleToolSpec` (parsed manifest entries), `_json_schema_for_params` (builds each tool's MCP `inputSchema` directly from its params — no separate validation layer, since real validation happens at the proxied backend endpoint, the single source of truth every other request already goes through), `_DeclarativeModuleTool` (a `fastmcp.tools.Tool` subclass whose `run()` builds one `_call_backend` call from its spec — path-location values are `urllib.parse.quote(..., safe="")`-encoded before substitution into `path_template`, *unconditionally*, since this server has no way to know ahead of time which declared params are UUIDs versus arbitrary strings, the same "encode by construction" reasoning `_get_org_login_info`'s existing `quote(slug, safe='')` already uses one section up), `_register_declarative_tool` (the literal name `docs/modules.md` already committed to), `_parse_module_tool_manifest`/`_fetch_module_tool_manifest` (network fetch, tolerant of a single malformed entry the same way the backend tolerates a malformed param), `_apply_module_tool_manifest` (diffs desired vs. currently-registered tools, calling `mcp.add_tool`/`mcp.local_provider.remove_tool` only for what actually changed), `_maybe_refresh_module_tools` (rate-limited to once per `MODULE_TOOLS_REFRESH_SECONDS`, default 600s, using whichever connecting session's own already-presented token is on hand — skipping the cycle cleanly, not erroring, if none is present or the backend is momentarily unreachable), and `ModuleToolsRefreshMiddleware` (a `fastmcp.server.middleware.Middleware` triggering that refresh on every `tools/list`/`tools/call`). A mutating tool is only ever included in what gets registered when this server's own `MCP_WRITES_ENABLED` is set, the same gate its hand-written write tools already use, applied generically rather than per-tool. No packaging change to `mcp-server`'s own Docker image was needed — every declarative tool is a plain HTTP proxy call, so a module's own Python code never runs in this process.

**Judgment calls the plan's own spec text left open, recorded here so a future session doesn't re-derive them:**
- `McpToolDefinition.params` is a plain `list[dict]`, not a nested dataclass — a deliberate match to `docs/modules.md`'s own already-published worked example rather than a "more type-safe" alternative this phase might otherwise have preferred; the params are display/JSON-Schema hints for an AI client, not something this codebase itself needs to validate structurally.
- The approval-action marker's literal `openapi_extra` key (`"x-approval-action"`) and its exported constant name (`APPROVAL_ACTION_ROUTE_EXTRA`) were not specified by either the plan or `docs/modules.md`, both of which describe the mechanism without naming the key. No existing route needed retrofitting with it — the path-prefix constraint means only a module's *own* router can ever be legally referenced by that module's own tool declarations, and no such router exists yet.
- `mcp.local_provider.remove_tool(name)` is used throughout, not the top-level `mcp.remove_tool(name)` — the installed, pinned `fastmcp==3.4.5` marks the latter `.. deprecated::` in favour of the former; per this repo's standing "fix the underlying code, don't suppress the warning" rule, the non-deprecated call is used directly rather than silencing the deprecation warning.
- `_apply_module_tool_manifest` diffs before registering, rather than unconditionally re-adding every desired tool on every refresh — confirmed by direct experimentation against the pinned `fastmcp` version that re-registering an existing tool name doesn't raise (despite `LocalProvider.__init__`'s own documented `on_duplicate: DuplicateBehavior = "error"` default; the server's actual runtime default falls through to "warn" and replaces) but does log a `WARNING` every time, which diffing avoids doing every 10-minute cycle for every unchanged module tool.
- No SOC 2 policy-document edits were made this phase (unlike Phases 0–2), and that absence is deliberate, not an oversight: this phase's own design-time hardening pass (recorded in the plan's "SOC2 / Security Planning" section, written before this phase's code) already anticipated and closed the gaps a request-time identify→verify→remediate pass would otherwise surface; there was nothing left to find and therefore nothing new to document as remediated.

**Tests.** `backend/tests/test_module_mcp_tools.py` (new, 11 tests) — a fixture module with a real `APIRouter` (three routes, one `openapi_extra`-marked as an approval action) proves every exclusion/inclusion path described above, plus that a module trying to pre-prefix its own local tool name still gets double-prefixed (no special-casing), that a module with `mcp_tools` declared but no router yields zero resolved tools, and the manifest endpoint's 401-with-no-token / 200-for-a-plain-non-admin-member gating. `mcp-server/tests/test_module_tools.py` (new, 12 tests, unit-style against a mocked backend response and a mocked `_call_backend` — no live backend needed, consistent with Phases 1–3's own "unit-test declarative infrastructure against a fixture, not a fabricated production module" precedent) — manifest parsing, tool registration/removal round-tripping through the real `mcp.list_tools()`, the `MCP_WRITES_ENABLED` gate, full param-mapping proof (including the URL-encoding of a deliberately hostile `"org/../evil"` path value, confirmed to arrive at the backend call as `org%2F..%2Fevil`, never escaping its intended path segment), a clean `ValueError`/`ToolError` on a missing required argument, no-op re-registration of an identical manifest, and the refresh-window rate-limiting (including the no-token-presented skip path). Both files run clean: the backend tests against a live `reqtrack_pytest_test` database, followed by an exclusive full-suite re-run (no other DB-touching process active, per this project's own documented wedge-avoidance practice) confirming no regression; the mcp-server tests against the exact pinned `fastmcp==3.4.5`/`httpx==0.28.1` in a throwaway venv, with the full `mcp-server/tests/` collection (36 tests) confirmed to still collect cleanly alongside the pre-existing, live-backend-only `test_server.py`. `ruff check` clean on every touched file in both components. No `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` changes were needed — this phase persists nothing new (`mcp_tools` is a pure code declaration, resolved fresh per manifest request), and there is still no first-party module with tools to demonstrate until Phase 5.

### Files changed

`backend/app/modules/registry.py`, `backend/app/routers/system.py`, `backend/tests/test_module_mcp_tools.py` (new); `mcp-server/server.py`, `mcp-server/tests/test_module_tools.py` (new); `docs/mcp-server.md` (new "Module-contributed tools" section plus two pointer additions), `docs/solution-architecture.md` (Phase 4's forward-looking placeholder paragraph replaced with the full technical account), `docs/compliance-module-plan.md` (Phase 4 marked complete, "Phase 4 notes" subsection added), `docs/decisions.md` (this entry).

## Compliance module plan, Phase 5: compliance data model, as a module (2026-09-05)

Sixth implementation phase of `docs/compliance-module-plan.md`, and the first phase of the Compliance Module itself (Phases 5-15) rather than the generic module-system infrastructure (Phases 0-4) it's built on: Compliance becomes the first real entry in `INSTALLED_MODULES`, proving out that infrastructure end-to-end for the first time rather than continuing to exercise it through fixture modules. This phase is data model only, per its own spec — `docs/Compliance_Module_Requirements.md` §2 (Standards), §5 (Requirements), §6 (Required Actions), §25 (Data Model Principles), §31 (Architectural Principles) — with no API surface (Phase 6) or MCP tools (also Phase 6) yet.

**Package layout.** `backend/app/modules/compliance/` is a new, self-contained package: `enums.py` (`ComplianceStandardVersionStatus`, plus `ComplianceStatus`/`ComplianceApplicability`/`ComplianceApprovalState` per the plan's own "New enums" instruction, none of the latter three yet referenced by any model), `models.py` (the five ORM models below), and `module.py` (`MODULE_DEFINITION`, this module's `ModuleDefinition`: `key="compliance"`, `default_enabled=True` per §1, `implemented=True`, `get_router()` returning `None`, `mcp_tools=()`, and two module-contributed roles — `compliance_manager` (org-scoped, §3) and `compliance_officer` (project-scoped, §11/§26) — via `ModuleDefinition.roles`, deliberately not new `OrgRole`/`ProjectRole` enum members, the specific correction Phase 2 exists to satisfy). Enums and models live under `app.modules.compliance`, not `app.models`/`app.models.enums`, keeping the module's own domain self-contained — the first time this plan's "as a module" principle has actually had a real module to apply it to.

**Data model.** Following §25's conceptual shape: `ComplianceStandard` (org-level identity row: `organization_id`, `reference`, `name`, `description`, `issuing_organisation`, `owner_id`, `creator_id`, `is_archived`/`archived_at`/`archived_by`) has `ComplianceStandardVersion` children (`version_number`, `version_label`, `status` — draft/published/retired, `effective_date`, `change_note`, `created_by`, `published_at`/`published_by`, `retired_at`/`retired_by`), which has `ComplianceRequirement` children (`parent_requirement_id` self-referential, `reference`, `name`, `description`, `reasoning`, `sort_order`, `created_by`), which has `ComplianceRequiredAction` children (`action_type_id`, `name`, `description`, `is_mandatory`, `sort_order`, `created_by`). `ComplianceActionTypeDefinition` (`organization_id`, `name`, `sort_order`) is a fifth, sibling table — an organisation-scoped extensible vocabulary `ComplianceRequiredAction.action_type_id` references. Migration `0026_compliance_data_model.py` creates all five (`compliance_action_type_definitions` first, since `compliance_required_actions` references it), following 0025's own `CREATE TABLE IF NOT EXISTS`/raw-SQL convention exactly.

**Design decisions, not left implicit:**
- **`ComplianceStandard`/`ComplianceStandardVersion` reuse the `Requirement`/`RequirementVersion` identity-row-plus-versioned-children *shape* the plan's spec names, but deliberately not that pair's `valid_from`/`valid_to` temporal-supersession *semantics*.** A `RequirementVersion` superseded by a newer one becomes pure history; every `ComplianceStandardVersion` must stay independently live and assignable indefinitely, since different projects deliberately stay pinned to different past versions forever (§4's own worked example: Project A on v1.1, Project B on v1.0, both valid simultaneously). Versions are instead ordered/gated by `version_number` + `status`.
- **No ORM `relationship()` for `ComplianceRequirement.parent_requirement_id`** — mirrors `Project.parent_project_id`'s precedent (hierarchy traversal is a service-layer concern for a later phase to add when it actually needs to walk the tree) rather than `RequirementVersion`'s `relationship()`-backed pattern, which exists there to walk a single linear history, not an arbitrary-depth tree. `ComplianceStandard.versions`/`ComplianceStandardVersion.standard` *do* get a real `relationship(back_populates=...)` pair, since that's a plain (non-self-referential) one-to-many, the same shape `Requirement.versions` already uses.
- **`ComplianceRequiredAction` is a plain one-to-many from `ComplianceRequirement`**, not a many-to-many link table like `RequirementAction`/`RequirementActionLink` — §25's diagram places required actions directly under compliance requirements with no separate mapping table, and nothing in §6 asks for one required action to be reused verbatim across multiple requirements.
- **§6's "Status," "Assignee," "Due date," "Completion information," and "Evidence" fields were deliberately left off `ComplianceRequiredAction`** — per §25's diagram, those belong to "Required Action Assessments" (nested under "Project Compliance Requirements," i.e. Phase 7's per-project assessment of a required action), not this reusable definition. Putting project-specific state on the definition row would violate §31's "A Compliance Requirement must not contain the compliance state of a project" one level down from where the plan's own spec already applies it.
- **`ComplianceActionTypeDefinition` is organisation-scoped, not project-scoped**, resolving an ambiguity in the plan's own "project- or org-scoped vocabulary" phrasing: a required action's type is chosen within a Compliance Standard, itself an organisation-level, cross-project resource (§2), so its vocabulary is scoped the same way `ActionTypeDefinition` is scoped to whatever owns the actions it types (there, a project; here, an org).
- **§2's "Review schedule" and "Appropriate audit/history information" got no dedicated column** — the former is Phase 10's dedicated `ComplianceReview` entity (§17 covers both standards and project-compliance records with one mechanism; §25's diagram gives it its own entity, not a scalar field), the latter is `TimestampMixin` plus Phase 6's `services.audit.log_event` integration. §2's "Status" is satisfied by the existing `is_archived`/`archived_at`/`archived_by` convention, kept distinct from `ComplianceStandardVersion.status` (the lifecycle that actually needed a dedicated enum, §4).
- **No seed data** — `ComplianceActionTypeDefinition` ships empty (§6's "potential initial action types" land in Phase 15), matching module system Phase 1's own "empty, extensible tables ahead of any seed step" precedent. `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` untouched — no API exists yet to seed through.

**A genuine circular-import bug found and fixed before it shipped**, this phase's own identify→verify→remediate moment even though the plan's "SOC2 / Security Planning" section didn't flag Phase 5 as needing one (it isn't a new trust boundary): `app.modules.compliance.module` imports `ModuleDefinition`/`ModuleRoleDefinition` from `app.modules.registry` to construct `MODULE_DEFINITION`. The first draft had `registry.py` import that `MODULE_DEFINITION` straight back, at its own module scope, to populate `INSTALLED_MODULES` — a real circular import whose success depended entirely on which of the two modules a caller happened to import first. **Identified** by direct experimentation, not assumption: `python -c "import app.modules.compliance.module"` raised `ImportError: cannot import name 'MODULE_DEFINITION' from partially initialized module 'app.modules.compliance.module'` when that module was the first thing imported. **Fixed** by moving the wiring into `app/modules/__init__.py` instead: a package `__init__` is guaranteed by Python's import system to finish running before any of its own submodules can be imported by anyone, so it imports `app.modules.registry` to completion first and only then imports `compliance.module`, making the import order deterministic regardless of entry point. **Verified** directly both ways afterward (`import app.modules.compliance.module` first, and `import app.modules.registry` first) — both now succeed and produce an identically-populated registry.

**An unrelated, pre-existing documentation drift found and fixed while updating `docs/solution-architecture.md`'s "Database structure" list for this phase's own five new tables** (per this repo's standing "fix, don't defer, found issues" rule): that exhaustive table list had drifted to 52 while the schema actually had 54 pre-Phase-5 tables — Phase 2's own `module_role_definitions`/`user_module_roles` (already described in prose under "Modular Feature System" and present in that section's own ER diagram) had never actually been added to the list itself. Caught by cross-checking the list's stated count against a live migrated database's real table count (`sqlalchemy.inspect(...).get_table_names()`, minus `alembic_version`) rather than trusting the existing count. Fixed alongside adding this phase's own five tables (52 + 2 missing + 5 new = 59) and a new sixth ER diagram for the compliance data model, rather than left as a second, unrelated gap next to the fix this phase already needed to make — the same pattern the four corrections already recorded in that section's own parenthetical follow.

**Tests.** `backend/tests/test_compliance_data_model.py` (new, 5 tests): module registration (registry entry present, `default_enabled`/`implemented` true, `get_router() is None`, `mcp_tools == ()`, both roles present with the right scopes); a full standard → version → parent/child requirement → required action round trip created directly via the ORM (no API surface exists yet) and reloaded from a fresh session to prove persistence, not just in-session object state; `ON DELETE CASCADE` from a deleted parent requirement down through its child requirement and that child's required action; the `(organization_id, reference)` uniqueness constraint on `compliance_standards`; and migration 0026's upgrade/downgrade round trip (downgrade to 0025, confirm all five tables gone, upgrade back to head, confirm all five recreated — always ending back at head in a `finally`, mirroring `test_project_group_role_migration.py`'s own pattern for a schema-mutating test in this suite's shared session-scoped database). `alembic/env.py` and `tests/conftest.py` each gained one `import app.modules.compliance.models  # noqa: F401` line so `Base.metadata` includes this phase's tables for autogenerate/`test_schema_migrations_match_models.py` — the "one model-import line per first-party module" Phase 1's own spec anticipated, landing here since Phase 1 had no first-party module yet. Run against a live `reqtrack_pytest_test` database: this file's 5 tests pass in isolation, and the full backend suite was re-run afterward with no other DB-touching process active to confirm no regression, `test_schema_migrations_match_models.py` included (confirming the new migration and models agree with no drift). `ruff check` clean on every touched/new file.

### Files changed

`backend/app/modules/__init__.py`, `backend/app/modules/registry.py`, `backend/app/modules/compliance/__init__.py` (new), `backend/app/modules/compliance/enums.py` (new), `backend/app/modules/compliance/models.py` (new), `backend/app/modules/compliance/module.py` (new), `backend/alembic/versions/0026_compliance_data_model.py` (new), `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/test_compliance_data_model.py` (new); `docs/solution-architecture.md` (new "Compliance module — data model" paragraph, new ER diagram, "Database structure" table-count/category update plus the incidental Phase 2 drift fix), `docs/compliance-module-plan.md` (Phase 5 marked complete, "Phase 5 notes" subsection added), `docs/decisions.md` (this entry).

## Module system follow-up: registry-driven `Base.metadata` auto-import (2026-09-05)

A small, user-requested follow-up to Phase 5, landed the same day: removes the last hand-maintained touch point Phase 1's own spec had flagged as "an acknowledged minimal and unavoidable touch point" — a hand-written `import app.modules.<key>.models` line in *both* `backend/alembic/env.py` and `backend/tests/conftest.py` for every first-party module that defines its own ORM models. Not a new phase in `docs/compliance-module-plan.md`'s numbered sequence (it doesn't touch product scope), so it's recorded here rather than as a phase entry.

**What changed.** `ModuleDefinition` (`app.modules.registry`) gains an optional `models_import_path: str | None` field — a dotted import path to a module's own models submodule (e.g. `"app.modules.compliance.models"`). A new `import_all_module_models()` iterates the live registry and calls `importlib.import_module(...)` on every declared path, logging and skipping (never aborting) one that fails to import — the same per-module fault isolation `_discover_entry_point_modules`/`_discover_path_modules` already apply elsewhere in this file. `backend/alembic/env.py` and `backend/tests/conftest.py` each now call this once, early, instead of carrying a hand-written import line per module; Compliance's own `MODULE_DEFINITION` gained `models_import_path="app.modules.compliance.models"` and its hand-written import lines in both files were removed. Verified end-to-end: a real `alembic upgrade head` run against a live database succeeds with the new `env.py`, and the full backend suite (including `test_schema_migrations_match_models.py`, which depends on `Base.metadata` actually containing Compliance's tables) passes unchanged.

**What this does *not* change — flagged explicitly rather than silently expanded, per this repo's standing policy on regressing a documented security posture.** The user's original ask was broader: a module should be "just placed in the correct folder" — including in a mounted Docker directory — "and everything then loads automatically," with no other edits needed. That phrasing, taken literally, would extend to a dropped-in module's own **database migration** running automatically too. This change deliberately does not do that, and the two are not the same kind of risk:

- Importing a Python model class is inert with respect to any real database — it only ever registers a table *shape* with SQLAlchemy's in-process `Base.metadata`. Nothing about a real schema changes as a result, and this now happens for any module already in the live registry regardless of whether it's a reviewed first-party module or (only if an operator has explicitly set `Settings.allow_external_modules`) an external one.
- Running a migration executes arbitrary SQL/DDL against the real production database with the application's own credentials. `docs/compliance-module-plan.md`'s Phase 1 spec already treats this as categorically different and explicitly out of scope: "Automatic third-party DB migrations are out of scope. Running arbitrary third-party SQL against a production database is itself a security-review-shaped question. Only first-party modules ship real Alembic migrations, added to the core chain by a reviewed PR." `ALLOW_EXTERNAL_MODULES` already defaults to `false` specifically because of the CC6.8 gap (`docs/soc2/trust-services-criteria-mapping.md`: "no automated dependency/container vulnerability scanning exists") a plugin-loading mechanism raises the stakes of — extending that mechanism to also auto-apply a dropped-in module's own migration DDL would be a materially larger expansion of that same already-open gap, not an equivalent convenience improvement to the model-import side.

This was flagged to the user rather than implemented, per `CLAUDE.md`'s standing instruction not to introduce a change that regresses a control described in the adopted SOC 2 policies without surfacing it first — this isn't a regression yet (nothing changed on the migration-execution side), but implementing the literal ask without flagging it would have been. **Resolved same day**: the user chose to extend `ALLOW_EXTERNAL_MODULES`'s existing scope to cover this, rather than leave it manual or remove the gate entirely — see the next entry.

### Files changed

`backend/app/modules/registry.py` (`ModuleDefinition.models_import_path`, `import_all_module_models`), `backend/app/modules/compliance/module.py` (`models_import_path` set), `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/test_module_registry.py` (3 new tests), `docs/decisions.md` (this entry).

## Module system follow-up: `ALLOW_EXTERNAL_MODULES`-gated automatic migrations (2026-09-05, same day)

Resolves the question the previous entry deliberately left open rather than answering unilaterally. Presented the user three options — keep migrations manual/reviewed (status quo); auto-apply an external module's own migration, but only when the deployment operator has already explicitly set `ALLOW_EXTERNAL_MODULES`; or auto-apply unconditionally, with no gate at all — and explained the risk each one accepts. The user chose the middle option: extend the *existing* opt-in flag's scope, don't remove the gate, and don't leave it fully manual either.

**What changed.** `ModuleDefinition` gains a second optional field, `migrations_import_path: str | None` — a dotted path to a Python module exposing `run_migrations(connection) -> None`. A new `app.modules.registry.apply_external_module_migrations(engine)`, called once at startup from `app.migrations.run_migrations()` right after `alembic upgrade head` completes:

1. Returns immediately, without importing or discovering anything further, unless `Settings.allow_external_modules` is `True` (matches the existing entry-point/path discovery gate's own "not even scanned when off" behaviour, not just "filtered afterward").
2. **Even when external discovery is on, skips any module whose `key` is also present in the static `INSTALLED_MODULES` list** — checked structurally against that real list, not trusted from anything the module claims about itself. A first-party module that declared `migrations_import_path` anyway (it has no reason to) is logged as a warning and ignored; first-party schema changes still go exclusively through a reviewed PR into `backend/alembic/versions/`, exactly as Compliance's own 0026 migration does. This is the load-bearing distinction that keeps this change scoped to what the user actually chose: only a module that arrived via the *already-opted-into* entry-point/path sources gets this, never a module the deployment ships in its own reviewed image.
3. For each qualifying external module, imports its `migrations_import_path`, resolves its `run_migrations` attribute, and calls it inside its **own** `engine.begin()` transaction — one transaction per module, not one shared across all of them, so a mid-migration failure in one module rolls back only its own work and leaves the connection usable for the next module's own fresh transaction rather than poisoning a shared one. A missing/failed import, a missing `run_migrations` attribute, or an exception raised by it is logged (`logger.exception`/`logger.warning`) and that module is skipped — never fatal to startup or to any other module's own migration, the same per-module fault isolation this file already applies to a broken module during discovery itself.

`run_migrations(connection)` must be idempotent — it runs on every process start, matching the same `CREATE TABLE IF NOT EXISTS`/`CREATE INDEX IF NOT EXISTS` convention every migration in the core chain already follows, rather than being tracked against its own per-module revision history the way Alembic tracks `alembic_version` for the core chain.

**Verified end-to-end, not just by unit test**: built a real throwaway fixture module (`module.py` + a sibling `migrations.py` module declaring `run_migrations`) under `Settings.extra_modules_path`, confirmed directly against a live database that (a) with `ALLOW_EXTERNAL_MODULES` unset, `run_migrations()` leaves the fixture's table absent, and (b) with it set to `true` and the fixture directory as `EXTRA_MODULES_PATH`, the same call creates the table — proving the literal "drop a folder in, including via a Docker-mounted directory, and it just works" scenario the user asked for, for the specific case they explicitly opted into. The fixture and its table were removed afterward; nothing from it is checked in.

**Honesty note, consistent with this file's existing ones for MCP tools and Tier B frames**: this does not and cannot make running an external module's own SQL as safe as a first-party, PR-reviewed migration. It closes the "a core file needs a per-module edit" friction for a module the operator has *already* decided to trust via `allow_external_modules` — the actual trust decision is still that flag, exactly as Phase 1 designed it; this just lets what that flag already permits (arbitrary code loading) extend naturally to that same code's own schema changes, instead of requiring the operator to also hand-run a migration out of band for something they already opted the rest of the way into.

**Tests.** `backend/tests/test_module_registry.py` (4 new tests, using a real throwaway in-memory SQLite engine so `engine.begin()`/`connection.execute(text(...))` mechanics are genuinely exercised, not mocked): disallowed-by-default is a true no-op (asserts `importlib.import_module` is never even called); an external module's migration runs and its table actually exists afterward; a first-party (`INSTALLED_MODULES`) module's `migrations_import_path` is never imported even when declared and external discovery is allowed; one external module's migration raising doesn't prevent a second, healthy one from running. Full backend suite re-run exclusively afterward (862 passed, no other DB-touching process active — the previous re-run attempt collided with a background full-suite run still executing from this same session, a fresh instance of this repo's own documented pytest/pytest concurrent-run wedge pattern; re-run cleanly once that finished). `ruff check` clean on every touched file.

### Files changed

`backend/app/modules/registry.py` (`ModuleDefinition.migrations_import_path`, `apply_external_module_migrations`), `backend/app/migrations.py` (calls it after `alembic upgrade head`), `backend/tests/test_module_registry.py` (4 new tests), `docs/decisions.md` (this entry).

## Compliance module plan, Phase 6: standards management API (2026-09-05)

Seventh implementation phase of `docs/compliance-module-plan.md`, and the first phase to give the Compliance module a real HTTP API: full CRUD for compliance standards, their versions, hierarchical requirements, and required actions, plus an organisation-scoped required-action-type vocabulary, the publish/retire version lifecycle (§4), and this module's first three MCP tools (all read-only). Unlike Phases 0-4, this phase opens no new trust boundary — it reuses Phase 2's already-hardened `require_module_role` and the existing `services.audit.log_event` pattern verbatim, exactly as the plan's own "SOC2 / Security Planning" section anticipated — so it did not need a heavy design-time hardening pass the way Phases 1/4 did; it still gets the lighter identify→verify→remediate review this repo's change-management policy requires for any RBAC/audit-touching change, recorded below.

**Identify→verify→remediate (lighter pass, per the plan's own note that Phase 6 reuses already-hardened mechanisms).** Identify: the two things that could regress an existing control here are (a) a mutating endpoint accidentally gated by the weaker `require_org_module_enabled` instead of `require_module_role`, and (b) a cross-org id substitution attack (one org's compliance manager guessing another org's standard/version/requirement/action UUIDs). Verify: every mutating endpoint in `router.py` was checked against the `_require_manage`/`_require_view` dependency it actually declares (13 mutating endpoints, all `_require_manage`; 9 read endpoints, all `_require_view` — no mismatches found), and every id-addressed endpoint routes through one of `_get_standard_or_404`/`_get_version_or_404`/`_get_requirement_or_404`/`_get_required_action_or_404`, each of which walks the full ownership chain back to `organization_id` (covered directly by `test_cross_org_isolation_returns_404`, including the symmetric org-B-via-org-A-path case). Remediate: nothing found needing a fix — both mechanisms already existed and composed correctly on first implementation, which is itself the recorded outcome of the pass, not a skipped step.

**Backend.** `backend/app/modules/compliance/schemas.py` (new): Pydantic request/response models for every resource, every `*Out` schema with `model_config = {"from_attributes": True}` per this codebase's convention. `backend/app/modules/compliance/router.py` (new): `router = APIRouter(prefix="/api/v1/orgs/{organization_id}/modules/compliance", tags=["compliance"])`, with `_require_manage = require_module_role("compliance", "compliance_manager")` and `_require_view = require_org_module_enabled("compliance")` constructed once at module scope (this codebase's established "call the factory at router-definition time" convention). Every id-addressed endpoint verifies ownership transitively via a chain of `_get_*_or_404` helpers mirroring `routers.action_types`' own "wrong scope → 404, not 403" precedent exactly. A `DRAFT`-only guard (`_require_draft_version`) is checked by every requirement/required-action create/update/delete/move endpoint, enforcing §4's "a published version's requirements become immutable." `create_standard_version` optionally deep-copies a prior version's full requirement tree and required actions forward (`_clone_requirement_tree`, recursive, old-id→new-id remap) when `clone_from_version_id` is given. Action types (`ComplianceActionTypeDefinition`) get a full CRUD sub-resource reusing `services.ordering.move_ordered` and `services.definitions.delete_definition_with_reassignment` directly, with `allow_empty=True` (no "must retain at least one" floor, unlike project-scoped `ActionTypeDefinition`). Every mutation calls `services.audit.log_event` before its single `db.commit()`. `backend/app/modules/compliance/module.py` (edited): `get_router()` now returns the real router (imported inside the function body to avoid any import-cycle risk, per Phase 5's own established precedent for this exact concern); `MODULE_DEFINITION.mcp_tools` gains three `McpToolDefinition`s (`list_standards`, `get_standard_version`, `list_requirements`), each `path_template` verified against a real route by `test_module_mcp_tools.py`'s new real-registry test, not just eyeballed.

**Judgment calls the plan's own Phase 6 spec text left open** — see `docs/compliance-module-plan.md`'s "Phase 6 notes" subsection for the full list (exact path segments, reference immutability, version cloning as part of version creation rather than a separate endpoint, flat-DFS-ordered requirement listing, symmetric cross-org 404 isolation, extending the immutability rule to `move`, 400-not-404 for a bad `action_type_id`, exact audit action-name strings, no reparenting endpoint) — not repeated here to avoid the same content drifting out of sync between the two documents.

**Tests.** `backend/tests/test_compliance_standards_api.py` (new, 9 tests): full CRUD happy path through the real HTTP API (the first time this module has had one to go through — Phase 5's own tests were ORM-only), requirement/required-action reordering, RBAC composition (compliance-manager grant, org-admin override, plain-member 403, any-member read — all four proven against the real dependency, not reimplemented), publish/retire lifecycle plus immutability enforcement on a published version, version cloning (tree + required actions copied, source version untouched), symmetric cross-org 404 isolation, audit-event assertions via a direct `AuditEvent` query (mirroring `test_org_group_project_roles.py`'s own query-and-assert pattern), and action-type delete-with-reassignment (409-then-204, plus the "can be emptied to zero" case). `backend/tests/test_module_mcp_tools.py` gained a new section proving all three `compliance_*` tools resolve against the *real* (non-fixture) module registry with `mutates=False` and the right params — concrete proof the declared `path_template`s match real routes, per the plan's own instruction to verify this mechanically rather than by inspection. `backend/tests/test_compliance_data_model.py`'s two now-stale Phase-5-era assertions (`get_router() is None`, `mcp_tools == ()`) were updated to their Phase 6 reality. Run against a live `reqtrack_pytest_test` database: the new/touched test files pass in isolation, and the full backend suite was re-run afterward with no other DB-touching process active to confirm no regression, `test_schema_migrations_match_models.py` included (no migration was added or expected to be needed this phase) — 857 passed. The same run also showed 14 pre-existing failures, all in `test_invites_and_external_users.py`/`test_oidc_provisioning.py`/`test_org_export_import.py`, none touching Compliance: `SMTP_HOST` defaults to `mailhog` (the Docker Compose-internal hostname), which a bare host-run `pytest` can't resolve outside that network, unlike `DATABASE_URL`'s `localhost`, which the Postgres container's own published port makes reachable either way. Confirmed pre-existing and unrelated by reproducing the same failures via `git stash` back to the pre-Phase-6 commit before restoring this phase's changes — not a regression this phase introduced. `ruff check` clean on every touched/new file. No `backend/scripts/seed_demo_data.py`/`seed_e2e_dataset.py` changes were made — seeding compliance data is explicitly Phase 15's own, already-documented responsibility.

### Files changed

`backend/app/modules/compliance/schemas.py` (new), `backend/app/modules/compliance/router.py` (new), `backend/app/modules/compliance/module.py`, `backend/tests/test_compliance_standards_api.py` (new), `backend/tests/test_module_mcp_tools.py`, `backend/tests/test_compliance_data_model.py`; `docs/mcp-server.md` (module-contributed-tools table populated), `docs/solution-architecture.md` (Compliance module paragraph extended with Phase 6's API), `docs/compliance-module-plan.md` (Phase 6 marked complete, "Phase 6 notes" subsection added), `docs/decisions.md` (this entry).
