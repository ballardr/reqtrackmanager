[tool.ruff]
# Same rationale and settings as backend/pyproject.toml (N-E-04) — kept
# consistent across both Python components rather than each drifting to
# its own style.
line-length = 150
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B"]
