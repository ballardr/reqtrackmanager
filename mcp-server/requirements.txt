fastmcp==3.4.5
# fastmcp requires httpx>=0.28.1 (newer than the backend's own pin of
# 0.27.2 — this is a separate service/container, so the two don't need to
# match).
httpx==0.28.1
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.4
