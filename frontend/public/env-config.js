// Placeholder for local dev; overwritten at container startup by
// docker/env-config-entrypoint.sh so the API base URL is configurable at
// deploy time without rebuilding the frontend image.
window.__ENV__ = {};
