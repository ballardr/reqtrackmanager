fastapi==0.111.0
uvicorn[standard]==0.30.3
sqlalchemy==2.0.32
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.1
pydantic-settings==2.5.2
email-validator==2.3.0
python-multipart==0.0.20
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
# Pinned below 4.1: passlib 1.7.4 (the latest release) reads
# `bcrypt.__about__.__version__` to detect the backend version, which bcrypt
# 4.1+ removed. passlib traps the resulting AttributeError internally (so
# hashing/verification still work), but it logs a warning on every process
# start. Pinning to the last version with `__about__` avoids the warning
# without patching passlib itself.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
prometheus-client==0.20.0
markdown-it-py==3.0.0
reportlab==4.2.5
websockets==15.0.1
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pyotp==2.9.0
qrcode[pil]==7.4.2
aiosmtplib==3.0.2
boto3==1.35.24
